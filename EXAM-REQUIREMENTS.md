# EXAM-REQUIREMENTS.md — 学习考核（考试式）功能需求与确认版设计

> 状态：**已实施初版**（2026-08-31 确认设计 → 当日实施；本文档即最终 spec，实施清单见 §12）
> 目的：让孩子**定期考核已学知识点**，了解每课/每知识点的**掌握情况**，并据此制定**有针对性**的后续加强（复习）计划。
> 与「引导学习中的对话式抽查」（`electron/lib/recording-prompt.ts`）明确区分：本功能是**独立、结构化、可判分、锁定防偷看**的考试式考核，不是边学边测。

---

## 1. 总体模型（一句话）

家长给**每个科目写一份「考核方法说明」**（周期 + 考核对象规则 + 题量/抽题方式），并给**每课（知识点）写一份「考核要点」**文档；系统到点**自动**挑该周期内学/复习过的知识点，按说明与要点出**全主观语音题**；孩子进入**锁定视图**逐题语音作答（可重录、转写回显、卡住可打字兜底、记录每题用时）→ 提交 → agent **离线按考核要点评估** → **分知识点/课程**记录（含原音、转写、用时）→ 出报告 + **每课加强计划（计划复习时间 + 复习重点）**。家长端可见每次结果与每课考核记录表，并**可听孩子原音**核对判分。

---

## 2. 角色分工

| 角色 | 职责 |
|---|---|
| **家长端** | 写每科目「考核方法说明」、写每课「考核要点」文档；查看每次考核结果；查看每课程考核记录表（最近考核时间/掌握情况/错题难点/亮点/计划复习时间/计划复习重点）；可听孩子原音核对判分。 |
| **孩子端** | 首页见「待考核」→ 进入锁定考试视图 → 逐题语音作答 → 提交 → 看成绩报告与加强计划。 |
| **服务端** | 存考核方法说明与考核要点（属内容）；存考核记录（属学习记录）；跑自动出卷（agent）与离线判分（agent，按要点）；存语音大文件。契合已定 SPLIT 拆分。 |

---

## 3. 家长配置（两极少量，复用现有机制）

### 3.1 每科目「考核方法说明」—— `topics.assess_method`
- 复用现有 `topics.method`（存 method.md 全文）同模式，新增 `assess_method TEXT` 列，存**说明全文**。
- 内容含：
  - **周期**：天 / 周 / 月（**每科目独立**设置，科目节奏不同，如英语每天、论语每周）。
  - **考核对象规则**：取该周期内 `first_learned`/`last_review` 落在区间、或 daily 学习记录里出现过的知识点作为考核对象。
  - **题量 / 抽题方式**：由说明定义（如「每天考最近 3 课各 1 题」「每周覆盖本周全部新学」），非全局封顶。

### 3.2 每课「考核要点」—— `courses.assess_rubric`
- 复用现有 `courses.teaching_copy`（教学文案全文）同模式，新增 `assess_rubric TEXT` 列，存**每课考核要点全文**（家长写，类似课程的 teaching copy）。
- 内容含：期望孩子答到的要点、常见错法、评分口径。
- **双用途锚定**：① 自动出主观题时读它（出一道能考到这些要点的问题）；② 判分时读它（答到几点给几分）。保证两次考同一知识点标准一致，掌握度趋势真实可比。
- 孩子端不冗余存，按需经家长库查询函数取（沿用「孩子库不冗余存内容」原则）。

---

## 4. 考核流程（孩子侧，锁定）

1. **周期到点 → 「待考核」**：首页标红提醒，但**不强制弹窗打断学习**，孩子主动开始（仍保持严格一次性，只是不强迫开始）。
2. **进入全屏锁定视图**：禁资料浏览 / 导航 / AI 提示（防偷看，直到提交）。考试期间**无暂停按钮、不可跳转其它功能**。
3. **逐题展示**：题干 + **语音输入按钮**（每段语音只答本题）。
   - 可**重录**（覆盖本题录音）。
   - **ASR 转写文本回显**（孩子确认、家长端可见）。
   - 卡住可**打字兜底**（语音为主、文本为辅）。
   - **记录每题完成时间**：`startedAt`（题面展示）、`answeredAt`（提交本题答案），派生 `durationMs`（详见 §6）。
4. **全部答完 → 提交**：答案（原音 + 转写 + 每题用时）经宿主上传服务端存储（语音落服务端磁盘、答案落服务端 DB）；判分在客户端进行（见 §7）。
5. **客户端独立内存 session 判分**：提交后客户端新建一个**仅内存**的判分 session（prompt 取自服务端，含 rubric），逐题打分（0~满分）+ 评语，输出**评估报告**（整体掌握度 + 逐题反馈 + 每课加强计划）；**仅最终结果**写回服务端数据库。考试过程中无任何 AI 提示，保持考试属性。

### 4.1 防偷看（考核成立底线）
- 主观题考「记忆+理解」，若考试时孩子能打开该课资料照读即为假考核。
- 锁定视图禁资料浏览/导航/AI 提示（直到提交）。题面即展示，无需另存考核资料文件。

### 4.2 中断处理（严格一次性）
- 考试中无暂停、不可跳转；**意外关闭/崩溃 = 本次作废，重进必须从头**。
- 提交前不落盘（本地暂存答案与原音，未提交则不算）；**提交后**才上传答案与原音到服务端、生成记录。

### 4.3 网页模板（考试视图渲染）
- 考试视图 = 一个**网页（HTML 模板）**，复用现有课程资料渲染通道：宿主用 `<iframe srcDoc="考试模板HTML">` 渲染，sandbox 同资料（`allow-scripts allow-same-origin`），**额外加 `allow="microphone"`** 以授予录音权限。
- **题目注入（开始考核时填模板）**：服务端/宿主在本场考试开始时，把生成的题目数据写入模板的 `window.EXAM_DATA`（或回填 HTML 占位符）并触发渲染。因全为**主观题**，模板结构统一——无需选项/填空等交互组件，只有「题干 + 每题语音作答区」。
- 模板结构（逐题展示）：
  - 头部：考核标题 / 科目 / 进度点 / 总计时 / 「🔒 考核中」锁定标识。
  - 题目区：单题卡片，含 知识点标签 + 题干 + 语音作答区（🎤 开始/停止、录音计时、原音回放、重录、ASR 转写文本框（可改/可打字兜底））+ 本题用时。
  - 底部：上一题 / 下一题（考试内可前后翻，不可离开考试）/ 提交。
- 锁定由**宿主外壳**保证（禁用导航/资料/AI 提示、拦截退出），模板 HTML 本身只负责内容呈现与作答交互。
- 作答回传：每题答案（录音 blob / 转写 / 用时）暂存模板内；点「提交」经 `parent.postMessage({type:'exam:submit',payload})` 回传宿主，由宿主上传服务端判分。
- 设计稿：`assets/exam-template.html`（自包含、可直接浏览器预览；含可真实录音的演示与题目数据注入示例）。后续实施可在此稿基础上接宿主 bridge 与服务端接口。

---

## 5. 语音输入与原始录音保存

- 每题答案含：`audioPath`（**webm 原始录音**） + `asrText`（ASR 转写）。
- **原音必须保存并可回放**：ASR 会出错（发音不清/方言/噪声），家长端每题带「▶ 听原音」按钮，可听原始录音核对判分、区分「真不会」与「嘴瓢/转写错」。
- 存储：webm 大文件走**服务端磁盘**（契合 SPLIT 大文件在服务端）；家长库真源、孩子端缓存已下发题目。
- 提交前本地暂存、提交后随答案一并上传（严格一次性不变）。

---

## 6. 逐题完成时间（更细掌握分析）

- `perQuestion` 记录 `startedAt`、`answeredAt`、派生 `durationMs`。
- 用途：时间维度补强掌握分析——
  - 快且对 = 熟练；
  - 慢但对 = 犹豫、需巩固；
  - 快且错 = 猜；
  - 慢且错 = 卡住 / 真不会。
- 报告中可标「流畅掌握 / 迟疑掌握 / 薄弱」。
- 考试锁定无暂停，`durationMs` 为纯 wall-clock 投入时间，可信。

---

## 7. 判分（客户端内存 session，prompt 取自服务端，仅结果落 server）

- **判分在哪里跑**：在**孩子端**进行，但用**独立的内存 session**（与学习 agent 隔离），**只在内存中进行、不写任何中间文件**，结束后**仅把最终结果落盘到服务端数据库**。判分计算在客户端，符合本 app「AI 在客户端跑」的现状（客户端持有 LLM key 并本地推理）。
- **判分口径单一真源**：判分用的 prompt（含考核要点 `assess_rubric` + 评分标准）**从服务端获取**，保证所有客户端、所有版本用的都是同一套判分标准 → 掌握度趋势**可比、不漂移**。
- **不实时提示**：提交后才跑判分，考试过程中无任何 AI 引导提示（区别于引导学习有提示），保持考试属性。
- 全主观、无选择题；无客观题自动判分引擎（题型收敛为全主观）。
- **分数完整性说明**：分数为客户端自报、server 信任存储；因家长可听原音复核，家用自测场景下可接受。若未来要求「分数不可篡改」，改由 server 执行判分 session 即可（接口不变）。

---

## 8. 记录与报告（分知识点/课程）

### 8.1 考核记录主表（每次 attempt）`exam_attempts`
- 字段：科目 / 周期 / `startedAt` / `submittedAt` / 状态（grading|done） / 总分 / 逐题明细。
- 逐题明细 `perQuestion`：
  ```
  { qid, course, question,
    audioPath,        // webm 原音（家长可听）
    asrText,          // 转写（回显）
    startedAt, answeredAt, durationMs,   // 每题完成时间
    pointGot, pointMax, correct, aiComment }
  ```

### 8.2 每课程考核记录表（家长可见）
- 每课（知识点）一行，含：
  - **最近考核时间** `lastAssessAt`
  - **掌握情况**（正确率 / 掌握度等级）
  - **错题难点**
  - **亮点**（答得好、超出预期处）
  - **计划复习时间** `planReviewAt`（来自加强计划）
  - **计划复习重点** `focus`（来自加强计划，即考核发现的问题）

### 8.3 掌握度双轨
- `courses` 加 `exam_mastery`（考核掌握度），与引导学习的 `mastery` 分开记录，再合成综合掌握度，避免一次考试覆盖长期学习轨迹。

---

## 9. 加强计划（每课结构化，复习更有针对性）

- `reinforcePlan` 结构化到**课程级**（非仅 AI 自由文本）：
  ```
  reinforcePlan: {
    "<course>": {
      planReviewAt: "2026-09-03",                 // 计划复习时间
      focus: [ "未能说出XX的推导", "混淆了A与B" ], // 考核发现的问题 = 复习重点
      aiSuggestion?: "建议重学本节 + 做口述练习考"
    }
  }
  ```
- 目的：复习不再是泛泛重学，而是「到点、对准问题」地复习，直接服务「更有针对性」诉求。
- 课程考核记录表同步展示「计划复习时间 + 计划复习重点」两列。

---

## 10. 数据归属与 SPLIT 契合

- **存储（真源）在服务端**：考核方法说明（`assess_method`）+ 考核要点（`assess_rubric`）+ 判分 prompt（均属内容）+ 考核记录（`exam_attempts`，属学习记录）+ 语音大文件 → 均在服务端（契合已定 SPLIT：内容/学习记录归属服务端、大文件存服务端磁盘、跨设备同步、家长端可见）。
- **计算在客户端**：出卷与判分均在孩子端跑（客户端持有 LLM key 并本地推理，与现有学习 agent 同模式）；判分用**独立内存 session**、prompt 取自服务端、仅最终结果回写服务端 DB，不做中间落盘。
- 孩子端只跑考核 UI + 本地缓存已下发题目/资料；会话 jsonl 留客户端本地（与 SPLIT 一致），考核记录永远在服务端。

---

## 11. 关键设计决策回顾（已确认分叉）

| 决策点 | 结论 |
|---|---|
| 考核对象来源 | 系统**自动**按周期挑「该周期内学/复习过的知识点」，非家长手选题库 |
| 题型 | **全主观题**，不出选择题 |
| 作答方式 | **语音输入**为主（每题一段、可重录/回显/文本兜底），原音保存 |
| 判分标准 | agent 按服务端下发的**考核要点（rubric）prompt** 锚定打分，保证可比、不漂移 |
| 题量控制 | 由「每科目考核方法说明」定义（每主题自带考核方法，按说明出卷） |
| 防偷看 | 保留**锁定**（禁资料/导航/AI 提示至提交） |
| 中断处理 | **严格一次性**（关闭即作废，提交前不落盘） |
| 周期粒度 | **每科目分别设** 天/周/月 |
| 出卷/判分位置 | **客户端**跑（本地 LLM，独立内存 session）；判分 prompt 取自服务端（单一真源），仅结果落 server DB |
| 数据归属 | **存储上服务端**（内容+记录+语音）；计算在客户端（契合 SPLIT 的存储拆分） |
| 加强计划 | **每课结构化**：计划复习时间 + 计划复习重点 |

---

## 12. 实施任务拆分（✅ 初版已完成，2026-08-31）

1. **数据层** ✅
   - 家长库 `topics.assess_method` / `courses.assess_rubric` 列（服务端 `server/src/db/parent-lib.ts` 幂等 ALTER + 客户端 `parent-library.ts` 迁移/upsert/查询透传）。
   - 服务端 `exam_attempts` 表（主库 `server/src/db.ts`）。
   - 孩子库 `courses.exam_mastery` 列（`server/src/db/kb.ts` + `kb-sqlite.ts` ensureV7；字段白名单加「考核掌握度」）。
   - 语音大文件复用 `files` 通道（`child_id` 关联）。
2. **家长端** ✅
   - `TopicDetail.tsx` 新增「考核要点」tab：科目级「考核方法说明」编辑器（assess_method）+ 每课「考核要点」编辑器（assess_rubric）。
   - `ChildDetailPage.tsx` 新增「考核记录」tab（`ExamRecords.tsx`：每课程记录表 + 最近明细 + ▶ 听原音）。
3. **服务端（内容真源 + 存储，不跑判分 LLM）** ✅
   - `server/src/routes/exam.ts`：`GET /exam/config/:childId`（周期内学/复习知识点 + rubric + 判分 prompt 下发）、`POST /exam/attempts`（存储 + 回写孩子 `exam_mastery`）、`GET /exam/attempts/:childId`、`GET /exam/course-records/:childId`。
   - `SERVER_FEATURES` 加 `"exam"`，`SERVER_VERSION` → 0.3.0；冒烟 `scripts/verify-exam-smoke.mjs` 全绿。
4. **孩子端（跑出卷 + 判分，本地 LLM）** ✅
   - `electron/lib/exam.ts`（服务端对接）+ `electron/lib/exam-engine.ts`（出卷/判分内存 session）+ IPC/preload。
   - `Learn.tsx` 边栏「考核」入口 → `ExamView.tsx` 全屏锁定视图（`src/lib/exam-template.ts` 考试页面，`iframe srcDoc` + `allow="microphone"`；逐题语音/重录/转写回显/文字兜底/计时/前后翻题；提交 postMessage 回传）。
   - 判分：客户端独立内存 session（prompt 取自服务端），仅结果 + 语音上报服务端。
5. **报告与加强计划** ✅
   - 孩子端报告（逐题得分/评语 + 课程掌握度 + 复习计划）；家长端每课程记录表（最近考核/掌握/难点/亮点/计划复习时间+重点）与「听原音」。

---

## 13. 范围与暂不做（初版）

- **初版已实施**：数据层 + 服务端 API + 家长端编辑器/记录 + 孩子端锁定考试视图与出卷/判分/上报，详见 §12。
- **未做（后续按需）**：
  - 周期触发生成「待考核」：当前为孩子主动从边栏进入；到点自动生成待考核入口（scheduler 侧）待做。
  - 抽题轮转的高级策略（随机抽 N / 章节轮换）可在 `assess_method` 说明里表达，机制先行。
  - 强制弹窗打断、分数不可篡改（判分 session 上 server，接口不变）。

---

## 14. 固定考核 + 自定义考核（2026-09-01 设计演进，取代初版的「按主题 assess_method 周期」）

> 初版（§1-13）已实施：锁定考试视图 / 出卷判分引擎 / rubric 存储 / 记录与报告。本演进重定义**考核的组织方式**（何时考、考什么、考多少），复用初版的下层机制（模板、出卷、判分、记录、锁定）。

### 14.1 两种考核
- **固定考核**：频率**与主题无关**、全局档位：每天 / 每周 / 每月 / 半年 / 年末（多档可并存）。到考核时间点，从**该周期内学习/复习过的课程**里选题考核。
- **自定义考核**：家长用对话告诉 agent「什么时间考什么内容」，agent 生成**一次性排期**；信息不明确时（时间？范围？主题/课程？）**先向家长确认齐全再生成**。

### 14.2 排期表 `exam_schedules`（新增，核心数据）
每条 = 一次考核：
```
id, child_id, kind(固定|自定义), scheduled_at(考核时间点),
scope(内容范围 JSON：固定=空/自动；自定义=主题+课程/知识点范围),
status(待考核|进行中|已完成|过期), attempt_id(关联 exam_attempts), created_at
```
- **固定排期生成**：系统按各频率档滚动生成排期；**同一时间点多档重叠 → 只生成一次，按周期最长的那档**（例：周五同时命中「每天+每周」→ 只考每周的；长周期范围覆盖短周期）。
- **自定义排期**：家长对话 agent → agent 确认信息（时间 / 主题 / 课程范围 / 题量 N）→ 生成排期。
- 排期到点后变为「待考核」，孩子侧可见可开始；完成后回填 attempt_id 标记已完成。

### 14.3 考核量控制（长周期课程多的解法）——用户确认版
- **选题单位 = 课，不是题**：每轮按优先级选 **N 门课**，**每门课考完整**（按该主题教学方法/该课考核内容，覆盖全部知识点出题，题量由课决定——**不设全局 8 题上限**）。
- **N 门课怎么选（优先级打分排序，取前 N）**：
  1. 复习计划到期（reinforcePlan.planReviewAt ≤ 今天）——先补漏；
  2. 掌握度薄弱 / 学习中（exam_mastery）——先巩固；
  3. 最久没考核——滚动覆盖全部课程（长周期全覆盖的关键）；
  4. 最近新学——趁热打铁。
- **长周期不怕课多**：每轮只考「当前最该考的 N 门课、每门考完整」，久未考的永远排最前 → 多轮滚动自然覆盖；年末大考按同一优先级取前 N，考不完的本就是熟练且刚考过的非重点。
- **N 可配**：默认 3~5 门/轮，家长可在对话或设置里调整。

### 14.4 固定考核候选口径（用户确认）
- 候选 = **该周期时间窗口内 学习过（first_learned ∈ 窗口）或 复习过（last_review ∈ 窗口）的课程**；窗口起点 = 上次同档考核时间（首次 = 周期起点）。

### 14.5 考核流程（定稿）
到考核时间点 → 按 scope 确定考核内容（固定=按 14.3/14.4 选课；自定义=家长指定范围）→ 出题（按课完整）→ 调模板生成页面（复用 exam-template）→ 开始考核（锁定视图）→ 记录结果（exam_attempts + 语音）→ 回填数据（exam_mastery / reinforcePlan / planReviewAt）→ 排期标记完成。

### 14.6 孩子侧 UI（取代「选科目」）
- 左侧考核区显示**考核时间点列表**：每条 = 时间 + 要考核的知识点/课程范围；到期的标「可开始」。
- 孩子点「开始这次考核」→ 直接进入锁定考试视图（出卷 → 作答 → 判分 → 报告）。
- 角标「待考核」= 排期到期未完成的数量。

### 14.7 与初版的取代关系
- **保留**：每课 `assess_rubric`（含合并 markdown 考核内容，出题/判分锚定）；出卷/判分引擎（内存 session、prompt 服务端下发）；锁定考试视图；exam_attempts 记录与报告；家长端考核要点编辑器。
- **改动**：
  - `topics.assess_method`（按主题的周期说明）→ **废弃周期语义**，固定考核频率改为全局配置；assess_method 字段可保留作展示/参考。
  - `exam/config`（下发全部已学课）→ 改为**按排期 scope 返回该次候选课程**。
  - 出卷候选（最近 8 课硬裁）→ 14.3 优先级排序取前 N 门课 + 每课完整出题。
  - 孩子侧「选科目」→ 14.6 考核时间点列表。
  - 角标 → 按排期到期计算。

### 14.8 实施任务拆分（✅ 已完成，2026-09-01）
1. **服务端**：`exam_schedules` 表；固定频率配置（家长设置：多档勾选 + N）；排期生成（滚动 + 同点多档去重取最长周期）；排期接口（列表/开始/完成/取消）；`exam/config` 按 scope 改造；自定义排期接口；`exam_attempts.schedule_id` 回填。
2. **家长 agent 工具**：`exam_schedule_create`（对话生成自定义排期，信息不全先确认）。
3. **出卷引擎**：每课完整出题（按 rubric 考核内容全覆盖，题量由课决定，去掉全局 8 题上限；服务端选课 + 本地 12 门保险裁剪）。
4. **孩子端**：考核时间点列表 UI（时间 + 知识点 + 开始按钮；pending/started 均可重试）替代选科目；角标按排期到期。
5. **家长端**：设置页「学习考核」tab（频率多选 + N + 时刻）；孩子详情「考核记录」页排期查看/取消。
6. **验证**：排期去重（daily+weekly → 只生成每周）、选课优先级、自定义 scope、每课完整出题（3 课 9 题全覆）、提交关联排期、`verify-exam-smoke.mjs` 18/18 全过。

### 14.9 选课改 LLM + 家长可编辑选课 prompt（2026-09-01 v3，取代 §14.3 代码打分选课）

> 用户拍板：**选课不用代码，给每个周期设置一个 prompt，家长可编辑**；每个周期的 prompt 都要说明「如何获取本周期学习和复习的课程」+「如何考核」，抽取课程的周期里说明选择原则。

- **选课流程（两段式）**：
  1. 客户端 `exam/config?schedule=X`（无 courses）→ 服务端返回 **selectionPrompt**（该排期频率档的选课 prompt，注入今天日期/周期范围/统计/候选清单）+ **candidates**（孩子库全部学习/复习过课程的元数据：主题/课程名/首次学习/最近复习/掌握度/上次考核/计划复习，**不含 rubric** 控制体积）；
  2. 客户端独立内存 session 按 selectionPrompt 让 LLM 选课 → 返回课程名列表；
  3. 客户端带 `courses=title1,title2` 再取 `exam/config` → 服务端返回选中课程（**含 rubric**）+ 判分 prompt → 逐课完整出题。
- **周期归属标记（关键可靠性设计）**：服务端用代码精确计算每门课的周期归属，在候选清单行尾打显式标记——daily/weekly/halfyear/yearly 窗口内课程标「★ 本周期」；monthly 分「★ 本月」/「◐ 本月前」。**LLM 按标记挑选、不自己算日期**（实测 293 门长清单让 LLM 逐行算日期会漏选/误选，打标后精确）。
- **主题类型标注（必学/选学/复习，2026-09-02 增）**：家长写考核 prompt 时不知道课程的任何标注，只会用日常说法（如「考今天学习复习的课，只考核必学的」）。服务端 `listLearnedCourseMeta` 关联孩子库 `topics.rules_json.type`（家长给孩子主题设置的「每天学习量类型」），在候选清单每行注入「| 主题类型:必学/选学/复习」（未设置则 -）；选课 prompt 尾部附「标注说明」段解释周期标记、主题类型含义及家长说法映射（「必学课程」= 主题类型必学的主题下课程）。**LLM 据此筛选，家长无需理解标注**。
- **默认选课 prompt（5 档，家长可在「设置 → 学习考核」编辑，清空某档保存 = 恢复默认）**：
  - 每天/每周：考**周期内学习/复习过的所有课程**（带 ★ 标记的全部选入，不挑选）；
  - 每月：每主题考**本月课程的 50%**（★ 本月）+ 再选**本月前课程的 25%**（◐ 本月前，数量=该主题本月课数×25%）；
  - 每半年/每年：每主题按 40%/60% 抽取（可编辑调整）。
- **候选口径**：学习过（first_learned）或复习过（last_review）或已学标记（status=✅）；已学但日期未知的课 firstLearned 记「✅」归入更早学习（可被每月「本月前 25%」选中）。
- **空窗口处理**：窗口内无课程 → LLM 按规则输出空数组 → 孩子端提示「本周期暂无学习/复习过的课程」，**不自动放宽**（尊重规则；家长可改 prompt 调整）。
- 自定义排期（scope 指定范围）**不经过选课 LLM**，直接按家长指定课程返回。
- **实现**：服务端 `DEFAULT_SELECTION_PROMPTS`（5 档默认模板）+ `buildSelectionPrompt()`（注入数据+清单+标记）+ `listLearnedCourseMeta()` + `fetchCoursesWithRubric()`；客户端 `selectCoursesForSchedule()`（内存 session 选课）+ 出卷改**逐课并发出题**（并发 3，每课一次调用完整出题，防多课 rubric 撑爆上下文）；`exam-engine.ts` 对 LLM 返回课程名做前缀清理（mimo 会带「[论语] 」前缀）。
- 冒烟更新：config 两段式断言（第一段 selectionPrompt+candidates / 第二段 rubric+scoringPrompt）、fixed-config 存取 selectionPrompts；真实 LLM 验证通过（293 门候选 → 精确选窗口内 1 门 → rubric 6078 字 → 逐课出题 3 题覆盖）。

### 14.10 标签管理（每天/每周）+ 自定义多排期（2026-09-02，取代 §14.1 的五档固定频率）

> 用户拍板：固定考核**只保留 每天 / 每周**两档，用标签管理——每档含「是否启用 + 选课 prompt + 考核时间」（每周可设**周几几点**）；**去掉月度/半年/年度固定档**（由自定义考核灵活安排）；**自定义考核可创建多个**，每个有自己的「考核 prompt + 日期时间点」。

- **固定配置结构**（`exam_fixed:<parentId>`）：`frequencies`（仅 daily/weekly 由 UI 管理）+ `time`（每日时刻）+ `weekly{weekday(1=周一…7=周日), time}` + `selectionPrompts`。
- **排期生成**（`ensureFixedSchedules` 重构）：daily 按 `time` 每天生成（当天未过则当天）；weekly 按 `weekday+time` 定位当天/下个同星期几，每 7 天步进；monthly/halfyear/yearly 保留旧 anchor 步进**仅兼容旧数据**（UI 不再生成）；同日多档去重取周期最长档不变。
- **自定义考核 = 排期 + 自己的 prompt**：创建时 `scope.prompt`（选课规则）+ `scope.note`（内容说明）+ `scheduled_at`（日期时间点）。config 时：
  - scope 带 prompt → **走选课两段式**：第一段 selectionPrompt（家长 prompt + 候选清单，freq="custom" 不打 ★/◐ 周期标记、统计全部候选）+ candidates（scope.topics/courses 限定或全部）；第二段 `&courses=` 返回 rubric + 判分 prompt。
  - scope 无 prompt（旧数据）→ 直接返回范围课程，跳过选课 LLM。
  - ⚠️ 第二段（courses 参数）必须在 scope 分支内**优先处理**（曾因 scope 分支先返回导致自定义第二段拿不到 rubric）。
- **家长端**：`ExamAdminPanel` 标签「每天 / 每周」（启用开关 + prompt + 时间，每周加周几下拉）+ 自定义考核多列表（时间/状态/内容/prompt 摘要/取消）+ 创建表单（孩子 + datetime-local + prompt 必填 + 内容说明）。
- **家长端（9-02 三连改）**：`ExamAdminPanel` **三标签页**（每天/每周/自定义考核，点击只显示该标签内容，不堆叠）；每天/每周 = 启用开关 + 时间（每周加周几下拉）+ prompt + 恢复默认 + 保存；**自定义考核先设置考核（时间点+prompt+内容说明）再分配给孩子**（孩子多选，默认全选，多孩子共用一个考核；创建 = 每孩子各一条排期），列表按「时间点|prompt」聚合显示各孩子状态，可 ✕ 取消分配 / ＋补分配；输入框参照课程管理（minHeight 40vh、monospace、13px）。
- **prompt 只描述「考哪些课/知识点」**（家长不懂 JSON）：默认模板与家长编辑的 prompt **不含任何 JSON/输出格式说明**——JSON 输出格式由客户端引擎 `selectCoursesForSchedule` 自动附加（「只输出 JSON、课程名必须与清单完全一致」）；已清空 8788 库固化的旧模板全文。⚠️ 注意：PUT fixed-config 以合并后配置为 base 再存会把默认模板全文固化进库（模板升级旧库不跟），后续可考虑存 diff。
- 验证：daily/weekly 排期按配置生成、自定义两段式、冒烟 24/24。

### 14.11 每日/每周考核候选改「学习计划源」（2026-09-03，取代 §14.9/§14.10 的学习痕迹口径）

> 用户拍板：每日/每周考核的课程**从家长学习计划（study_plan_items）中获取，计划内无论是否完成都考核**；每周范围=**近 7 天（含今天）**；某天/本周无计划排期 → **无计划则不考**（不自动回退旧口径）。背景：ISSUE-033 后「每天学什么」已完全由学习计划驱动（gen 计划↔todolist 同步），考核随之计划驱动，倒逼计划执行（未学也考）。

- **候选构建**（服务端新增 `listPlanCourseMeta`）：查主库 `study_plan_items`（child+active=1+kind='date'，date∈窗口）展平 content 项 → 按 text **精确匹配孩子库课程 title**（真实计划文本即课程标题，8/8 实测全中）→ **不看 first_learned/last_review/status**（status⬜ 未学课也进候选）；每课标 `planDate`（窗口内排多次取最早）；匹配不到孩子库的计划文本进 `unmatched` 返回（暴露课号录错等数据问题，如真实数据「汉字宫第190课·储粮之仓」与课程表不符）。
- **窗口**（`planWindowFor`，本地日期）：daily=考核执行日当天；weekly=执行日往前 6 天 ~ 执行日（近 7 天滚动，与周几无关）。⚠️ 计划 date 为家长本地日期语义，窗口须**本地日期**换算（勿用 UTC toISOString）。
- **选课 prompt**（`buildSelectionPrompt` 增 `source:"learned"|"plan"`）：plan 模式候选行加「| 计划日期:YYYY-MM-DD」列、不打 ★/◐ 标记、统计段「[主题] 今天/近7天计划 N 门 → 全部选入」；尾部「标注说明」替换为计划语义（**计划内无论是否完成都考**、旧模板里「★本周期/学习/复习过」字眼是旧版残留请忽略、家长说法映射）。
- **默认模板重写**（`DEFAULT_SELECTION_PROMPTS.daily/weekly`）：由「考周期内学习/复习过的课」改为「考今天/近 7 天学习计划里的全部课程（无论是否完成）」。
- **getExamConfig 固定排期第一段**：freq=daily|weekly → 计划候选 + source="plan"；monthly+ 历史排期与自定义考核仍走「学习痕迹」候选（custom 分支不变）。第二段 rubric/判分链路不变（未学课在父库同样有 assess_rubric，可正常出卷）。
- **空窗口**：无计划 → candidates 空 → LLM 输出空数组 → 孩子端「暂无考核内容」（无计划不考）。
- 验证：冒烟 27/27（新增 study-plan 创建 / kb 补未学课 / config 第一段含未学课+planDate）；真实数据珊珊 daily@9-03 候选 5 门（未学 2 门在列）、weekly@9-07 候选 18 门（近7天含未学 11 门）。

### 14.12 孩子端作答交互：答完锁定 + 实测问题修复（2026-09-03）

> 用户拍板：**一题答完（离开本题）即锁定不可回改**——因为后面题目可能含前面题目的答案/提示（防孩子回头照着改，作答失真）。锁定题回看只读（可听录音），未答空题可前后补答，全部答完才可提交。

- **作答脚本**（`src/lib/exam-template.ts` 生成考试 iframe 内容）：`saveCurrent()` 在离开本题（上一题/下一题/提交）时保存文本框并把「有内容」（asr 非空或录音）的题标 `locked`；锁定视图 textarea disabled、麦克风禁用（显示「🔒 已答完」）、可播放录音、进度条常显「已答 x/N 题」。转写占位「识别中…」不落答案；asr:done 仅在未手工编辑时覆盖。
- **实测问题修复**：
  1. 答案串题：出卷 LLM 每课 qid 都从 q1 编号 → 跨课重复致 answers 共享。修：组装题目时 qid 用**全局序号**覆盖。
  2. 语音报 `invalid security origin`：srcDoc iframe sandbox 缺 `allow-same-origin` → 不透明源非安全上下文，getUserMedia 被拒。修：sandbox 加 allow-same-origin（配 allow="microphone"；主进程已有 setPermissionRequestHandler 放行 media）。
  3. 本题用时不刷新：进入题目每秒实时累计，答完（answeredAt）冻结，重录重计。
  4. 录音切题竞态：录音中切题会把录音存到新题（onstop 读全局题号）。修：录音开始时记录 recQid，onstop 用它定位；被切走的录音题即时锁定防漏锁。
- **考核结果入口**：家长中心「🎯 学习考核」新增第 4 标签「考核记录」（孩子选择器 + 每课程掌握表/逐题明细/原音回放）——此前仅藏在孩子详情页第 5 个 tab，家长找不到。
