# 家长 agent 使用场景梳理（场景 · 工具 · 提示词 对照稿）

> 记录日期：2026-09-24
> **文档结构约定（与孩子版一致）**：**以场景为主导** —— 每条场景一行，直接对齐「**用到的工具**」与「**用到的提示词**」，并标出**提示词怎么管**（落在哪层、谁能改、现状如何）。跨域共用的机制不重复写：**提示词的管理规则见 §2、工具全量表见 §3**。
> **每个域的讨论（过程与结论）就地写在该域表格下面的「域 X 讨论」小节里**；末尾只保留跨域的「待确认口径」。
> **范围**：不涉及实现细节（不谈接口、表结构、代码）；工具只到"叫什么、能做什么、边界在哪"，提示词只到"该说什么、落在哪层"。
> **依据**：`docs/学习伙伴-用户使用说明书.md`（家长端 §3.1–3.10、计划 §3.4、考核 §3.5、积分 §3.6、定时任务 §5.4）+ 家长端实际界面入口（侧栏 9 项）+ 服务端家长会话装配。
> **对照物**：**孩子版** = `docs/孩子使用场景梳理-2026-09-23.md`（11 域 / 53 条）。本文是它的**镜像稿**，用来回答同一个问题：**场景、工具、提示词三者是否匹配**。
> **一句话差别**：孩子版的核心结论是「**有数据、没提示词**」；家长版的核心结论是「**有通道、没专用工具**」——见 §0。
>
> **2026-09-25 修订（按讨论决定，本文已就地更新）**：
> 1. **删除"比较孩子"场景**（原 A6、D6）——不做跨孩子对比。注意：它**从来没有专用工具**，用的是 `parent_library_topics` + `parent_db_read`（两者仍在，另作他用）。
> 2. **删除整个「数据管理助手」**（原域 G，2 条）——`parent-data` 会话只是**通用数据 API 的试验田**，通用 API 不再使用：`parent_db_read` / `parent_db_write` / `parent_db_describe` / `define_namespace` **计划退场**（退场前置与过渡见 §3.2）。
> 3. 因此**场景 41 → 37 条、域 7 → 6 个**；§5–§8 与附录的计数已同步。

---

## 0. 一句话结论

家长端共梳理出 **37 个场景**，归入 **6 个场景域**。以场景为轴对齐工具与提示词后，五条主线结论：

1. **家长 agent 只有"一个助手 + 一个下游"**：`家长助手`（parent / parent-content **共用一份提示词、一套工具**）、以及**它的下游** `编程 agent`（不是会话，是被 `parent_build_material` 触达）。原来的第二人格 `数据管理助手`（parent-data，独立基底 + 独立工具）**已按决定退场**——它只是通用数据 API 的试验田，而**通用 API 不再使用**（§3.2）。
2. **家长提示词是"一份写死的基底"，没有可改层**：孩子那份有 ①基底 / ②家长自定义层 / ③运行时注入三层，家长自己**一层都没有**——`buildServerParentPrompt` 整段写死，组装时既不读任何家长自定义文本，**也没有任何界面能改**（§2）。
3. **主线能满足，但"能说"多于"讲清"**：37 条里 **24 条有工具可用**、**10 条天然属于界面 · 3 条属于系统自动**；但**只有 16 条同时有提示词口径**，**剩下 11 条是"工具能查到、话说不清"**（A1 B3 C6 D1 D5 E5 E6 E8 F2 F3 F4），其中 **F4 连工具都没有**（回归）、**F2/F3 今天是靠通用通道直读**（该通道计划退场，见 §3.2）——**这 11 条正是"按场景写口径"的主场**。
4. **最扎眼的不是缺工具，是"有专属读数通道、却没有专属工具"**：`家长助手` 能**逐表直读孩子库**（含考核逐题、知识点掌握、积分流水、错题本——走的是通用通道），但**除错题本外一条口径都没有**——
   - **家长版没有 `child_exam_result` 的对应物**（孩子端已落地 `child_exam_result` / `child_mastery_report` / `child_points_report` 三个业务语言只读工具）；
   - ⇒ 家长问「这次考核哪几题错了、为什么」**只能靠通用 `parent_db_read` 直读表行**，读得到、**讲不清**，且提示词里一个字都没提（域 D 讨论）。
5. **有一处"文档承诺、实现没有"，属回归**：用户说明书 §3.7 写明家长助手覆盖「**配置管理（Control）**：查看/修改配置（默认模型、资料上限、定时任务、孩子档案）」，但**全仓没有任何一个配置读写工具挂给家长 agent**——模型、资料上限、TTS、定时任务配置**一件都改不了**，家长必须自己点「设置」。孩子版的 H1 提醒是同一类问题（界面文案承诺了对话能力），**建议同样按回归处理**（域 F 讨论）。

> **读法**：**"说得好不好"看提示词，"能不能做到"看工具，"点得到点不到"看界面**——这三句口诀对家长端同样成立，只是家长端**第三句的比重最大**（界面已相当完整，缺的是对话出口）。

### 0.1 三条最该先定的事（供讨论起手）

| # | 事项 | 性质 | 落点 |
|---|---|---|---|
| **1** | **家长读考核结果 / 掌握度 / 积分**补专用工具（`parent_child_exam_report` / `parent_child_mastery_report` / `parent_child_points_report`）——**通用通道已决定退场，这条从"要不要做"变成"必须做、什么时候做"**。→ **✅ 已做（2026-09-25，`ISSUE-144` P4；含 D3 错题本并入掌握报告、F2 兑换并入积分报告）** | 工具缺口 + 口径缺口 + **退场前置** | 域 D 讨论 · 二 / §3.2 / §8 #2 |
| **2** | **配置管理（Control）**是补工具，还是**改文案**（把说明书 §3.7 的能力域改回事实） | 回归 / 产品取舍 | 域 F 讨论 · 二 |
| **3** | **家长提示词要不要给孩子那样的"可改层"**（多一个家长自定义规范：机器名、称呼偏好、汇报风格、数据隐私口径） | 机制变更（可后置） | §2.3、§8 #1 |

---

## 1. 梳理口径（五条规则）

1. **场景 = 一次「有触发 + 有期望结果」的最小交互单元。** 触发可以是家长说一句话、点一个入口，也可以是一个到点的自动动作。
2. **一个功能可能承载多个场景，一个场景也可能横跨多个界面**。本文按**家长的行为**切，不按功能模块切（所以"排学习计划 / 排考核 / 排生活计划"是同一个域的相邻行，而不是三个模块）。
3. **形态** = 家长用哪种方式表达：**对话**（说给 AI 听）/ **界面**（去点去看）/ **系统**（不经过家长，自己发生）。
4. **每条场景都要回答三件事**：这个场景**用哪些工具**、**用哪些提示词**、**提示词怎么管**。工具名以 §3 台账为准。
5. **提示词与工具的状态标记**（与孩子版同一套符号）：
   - **✅ 已具备** / **❌ 缺** / **⚠ 缺口径或方向相反** / **— 无需**；
   - 提示词落点：**① 写死（谁都改不了）/ ② 家长自定义层 / ③ 运行时自动注入**。

### 1.2 家长侧的"会话地形"（先看懂这张图，后面的表才不会读错）

| 会话 | 入口 | 提示词 | 工具面 | 一句话定位 |
|---|---|---|---|---|
| **`parent`**（家长助手） | 工作台**右栏常驻聊天**（所有页面都在） | `buildServerParentPrompt`（写死） | 26 个（资料/课程/辅助/数据 + 计划域 18；**通用通道退场后 23**） | **运营助手**：建课、排计划、管资料、答疑、出报表 |
| **`parent-content`**（资料助手） | 「主题编辑器」里生成/改内容时的**左栏聊天** | **同上（同一份，不是独立 prompt）** | **同上（同一套）** | 只是**换了个会话 key 与落盘目录**（避免主题编辑的上下文污染主会话）；会话落盘 `agent-sessions/<pid>/parent-content/` |
| **`programming`**（编程 agent） | **无入口**，被 `parent_build_material` 触达 | `buildProgrammingPrompt` | 仅 `read/write/edit`（限沙箱） | **造页面的人**：产出 HTML 资料落资料真源 |

> ⚠️ 读表时的两个易错点：
> 1. **`parent-content` 不是第二个助手**——它和孩子版的 `course:<课程名>` 类似，是"同一个助手、另一个会话"；**它没有独立提示词，也没有独立工具**。
> 2. **侧栏「🗃️ 数据管理」那一格已经没有 agent 了**：`parent-data` 会话（第二人格 + 通用数据 API 四件）**已按决定退场**——它只是通用 API 的试验田，而通用 API 不再使用。**退场前置与过渡安排见 §3.2**。

---

## 2. 提示词如何管理（家长版：只有①，没有②③）

> 本节是**所有域共用**的机制。各域讨论小节**只写增量**。

### 2.1 孩子 vs 家长：提示词三层机制一览

| 层 | 孩子端 | 家长端 | 入口 |
|---|---|---|---|
| **① 基底** | `buildServerChildPrompt` + 场景课独立一份 | `buildServerParentPrompt`（`buildServerDataAgentPrompt` 已随数据管理助手退场） | **都写死在程序里，谁都改不了** |
| **② 家长自定义层** | ✅ **有**（按孩子存、留 50 版、可回滚；`getAgentPrompt(dataDir,"child",childId)`） | **❌ 没有**（`parent` 组装时**不读**任何自定义文本） | 孩子：旧版客户端曾有编辑弹窗，重构后**入口丢了、通道还在**；家长：**通道都没有** |
| **③ 运行时自动注入** | 日期时间 / 孩子姓名 / 课程上下文 / 数据表清单 / 错题摘要 | **只有三项**：家长 id、今天、数据通道元数据清单（`buildDataChannelBlocks` → 两库表/列/路径清单） | 系统自动，**不需要人写** |

⇒ **判断一个家长提示词需求的落点**：家长端**只有 ① 与 ③ 两个格子**。凡是由**数据/配置决定**的（孩子姓名、模型池、统计口径），正确落点是 **③ 自动注入**；凡是**表达口径与边界**（怎么汇报、要不要复述、什么不许动），只能落 **① 写死**——**也就是说，今天想改家长助手的一句话，只能改代码发版**。

### 2.2 两份基底提示词各自说了什么（现状盘点）

**A. `buildServerParentPrompt`（家长助手 / 资料助手共用）—— 9 个内容段 + 1 段工作原则**

| 段 | 覆盖的场景（§4 编号） | 写得怎么样 |
|---|---|---|
| 当前上下文（家长 id / 今天 / 工作区） | 全篇（运行时注入） | ✅ 够用 |
| **计划域**（学习 / 生活 / 考核排期） | E1 E2 E3 E4 E7 E8 | ✅ 最厚的计划段：**先查真实课程名、信息不全必须问、改前复述**都在（**E5 重复规则 / E6 定时任务不在**） |
| **课程学习资料（唯一真源）** | C1 C2 C3 C4 C5 | ✅ 6 个材料工具的用法与"整理前必做"都写了 |
| **聊天附件（图片 / 文件怎么读）** | C7 C8 | ✅ 具体到"把引用值原样传给谁"，还写了失败怎么转述 |
| **落库主题与课程** | B1 B2 B3 | ✅ 先核对再写、进度字段不要手写、先复述 |
| **课程改名与家长库→孩子库同步** | B4 B5 B6 | ✅ 全篇最细的一段（改名**绝不能用** upsert、同步范围只到已分配主题） |
| **落库课程考核内容（知识点 + 题库）** | C4 C5 的上游（知识点·题挂载） | ✅ 四步顺序 + 整课替换语义 + 先读后写 |
| 孩子的对话记录 | D4 | ✅ 只读边界 + 概括汇报 |
| **孩子的错题本** | D3 | ✅ 聚合口径 + 反复出现＝没掌握 + **不许代孩子标 mastered** |
| **通用数据查询（受控通道）** | D1 D5 F2 F3（**计划退场**） | ⚠ **只有一句**（"查专用工具覆盖不到的表"），**没有任何口径**（且与工具描述口径不一致，见 §3.2）；**退场后这一段连同工具一起去掉，口径改由场景专用工具承载** |
| 工作原则（列清单 / 复述 / 批量分步 / 简洁中文） | 全篇兜底 | ✅ 够用 |

**⇒ 缺口一目了然**：**D1 / D5（考核与掌握解读）、E5 / E6（重复规则与定时任务）、F3（积分怎么算）** 这五条，**基底里都是空白**——工具全靠自己的 description 撑着（好在那几把工具的描述写得相当细）。

**B. `buildServerDataAgentPrompt`（数据管理助手）—— 已退场**

这份基底连同 `parent-data` 会话一起**不再使用**（通用 API 退场，§3.2）。它唯一值得留下的是"**工作原则**"那段纪律（列名不臆造、多跳优先走 path、`countOnly` 数行、**写前复述"改哪张表哪几行改成什么"**、敏感列逐条复述、批量危险操作先同意）——**退场的是通道，不是这套纪律**，将来做场景专用写工具时应照此写。

> **补充（2026-09-25）**：上述"哪一段该说什么、缺在哪"的问题，已按**场景技能（skill）**方案重新组织（常驻层 + 按场景加载），见 `docs/家长agent-场景skill方案-讨论稿.md`。

### 2.3 家长在哪设（通道都没有）

| 操作 | 孩子端 | 家长端 |
|---|---|---|
| 底层通道（读 / 保存 / 看历史 / 回滚） | 齐（按孩子隔离、留 50 版） | **无**（`scope` 只允许 `parent`/`child`，但家长会话组装时**不读**它） |
| 界面入口 | ❌ 没有（旧版有，重构丢了） | ❌ **从来没有** |
| 家长助手里的工具 | —（家长角色专属） | ❌ 没有（助手自己不能改自己的提示词） |
| 谁能改 | 只有家长（且目前无界面） | **只有改代码发版的人** |

> ⇒ 与孩子版 §2.3 的对照：孩子是「**入口丢了、通道还在**」，家长是「**两样都没有**」。**孩子那句"改完必须重开会话才生效"对家长不成立——因为家长改不了**（会话是长期累积的，重置会话＝丢历史，见 §6 #12）。

### 2.4 三个容易混的边界

1. **工具清单不由提示词决定，也不由家长决定**——服务端建会话时按**会话类型**装配（§3.1）；家长**改不了工具面**（既不能加也不能删），这与孩子端一致。
2. **`parent-content` 与 `parent` 共用一份提示词、一套工具**——但**会话历史彼此独立**（同一句话在右栏说和在主题编辑器里说，上下文不一样）。
3. **家长助手的"下游"不是家长 agent**：`parent_build_material` 拉起的是**编程 agent**（另一份提示词、另一套工具），它产出的文件**不是它自己写的**——汇报时要按"资料已生成"说，不要编造生成过程。

### 2.5 各域对提示词的依赖（速查）

| 域 | 该域场景数 | 需要提示词 | 现状要点 |
|---|---|---|---|
| **A 孩子与档案** | 5 | **2**（A1 A2） | 补孩子/改档案的工具描述里有"密码不给设"，提示词几乎没提；**建号引导零覆盖**（原 A6 跨孩子对比已按决定删除） |
| **B 主题与课程** | 6 | **4** | ✅ 覆盖最厚的一域（落库、改名、同步三大段都在） |
| **C 资料与内容生产** | 8 | **5** | ✅ C1–C5（整理四步）与 C7/C8（附件读法）写得厚；⚠ **C6（造页面）提示词里没点名 `parent_build_material`** |
| **D 数据洞察** | 6 | **4** | ✅ D3 D4 D7；❌ **D1（考得怎么样）D5（哪里薄弱）零覆盖**——有数据、无专用工具、无口径（原 D6 跨孩子对比已按决定删除） |
| **E 计划与排期** | 8 | **5** | ✅ E1–E4、E7；❌ **E5（重复规则）E6（定时任务）零覆盖**——只在工具描述里；E8 薄 |
| **F 积分与系统设置** | 4 | **2** | ❌ **F3（积分怎么算）F4（配置管理，工具不存在）零覆盖**；F1 界面自足、F2 无工具 |
| **合计** | **37** | **22** | 其中 **零覆盖 7 条**：**A1 · D1 · D5 · E5 · E6 · F3 · F4**（F4 连工具都没有，属回归）；**通用通道退场后 D1 D5 F3 会同时失去"能读"与"讲不清"两半，必须由场景专用工具接手**（§3.2） |

---

## 3. 家长的工具台账（场景表里工具名的含义以此为准）

> **装配口径**：**家长助手（`parent` / `parent-content`）**给 **26 个**（= `PARENT_AGENT_TOOL_NAMES` 里 4 个 fs + 21 个业务/数据 + `log_activity`、`get_date`）**＋ 计划域 18 个 ＋ `parent_display_report` ＋ 编程工具 `parent_build_material`**；
> **编程 agent** 只给 `read/write/edit`。
> **工具面家长改不了**（不能加也不能删）。
> **⚠ 变化（2026-09-25）**：原**数据管理助手（`parent-data`）那 5 件已整组退场**（§3.2）；家长助手那 26 个里含 **3 个通用数据通道工具**（`parent_db_read` / `parent_db_write` / `parent_db_describe`），**退场后 → 23 个**。

### 3.1 家长助手（26 + 18 + 2）

| 分组 | 工具 | 干什么 | 关键边界 |
|---|---|---|---|
| **孩子** | `parent_list_children` | 列名下孩子（按**姓名**定位一切对象） | 只回 `id`/`name`，**不读档案** |
| | `parent_child_create` | 加孩子（姓名 / AI 伙伴名·头像·性格 / 年龄·年级·兴趣） | **不设密码**（明确报错并指向界面）；受**孩子数量上限**约束；同名拒绝 |
| | `parent_child_update` | 改档案（含改名） | **不改密码**；只改传入字段 |
| **资料** | `parent_list_materials` | 列资料真源（按主题/前缀过滤） | 整理前**必做**（去重/归并/改名先看有什么） |
| | `parent_read_material` | 读资料正文（文本类） | **二进制只回元数据**（大文件不灌上下文）；200KB 截断 |
| | `parent_put_material` | 写入/覆盖资料 | 单次 **≤2MB**；覆盖前建议先读（防误盖家长手改版） |
| | `parent_move_material` | 移动 / 改名 | 记 activity-log |
| | `parent_delete_material` | 删除 | **默认只演练**：必须先复述清单取得同意，再带 `confirm:true`；不可回滚，记 activity-log |
| | `parent_build_material` | **拉起编程 agent** 造/改 HTML 资料写入真源 | 只产 HTML；**需家长先配"编程 agent 模型"**；产出不是它自己写的 |
| **家长库（真源）** | `parent_library_topics` | 看主题 + **实时聚合名下孩子进度** | 库域分工后家长库不存进度，进度实时聚合孩子库 |
| | `parent_library_courses` | 看主题下课程 | — |
| | `parent_upsert_topic` / `parent_upsert_course` | 落库主题 / 课程 | **不进"改名"**：改名只能走 `parent_rename_course`；`(topic,title)` 主键 upsert |
| | `parent_rename_course` | **改名 / 换主题（保 uuid + 联动全部孩子库）** | **改名的唯一正确入口**；保孩子进度 |
| | `parent_sync_courses_to_child` | 家长库 → 某孩子库对齐 | **只到"已分配主题"**；未分配整体跳过并如实转述；不代分配、不改进度、不删行 |
| | `parent_library_course_content` | 看某课知识点 + 挂题 | 写前**必读**（写是整课替换） |
| | `parent_upsert_course_content` | 写知识点 + 题 + 关联 | **整课全量快照（替换语义）**：没写进去的会被移除 |
| **数据**（**计划退场**） | `parent_db_describe` / `parent_db_read` / `parent_db_write` | 通用通道（表 / 路径 / ns） | 读操作**不需要先 describe**（清单在注入元数据里）；**通用 API 已决定不再使用**（§3.2）——**在场景专用工具到位前，它们仍是 D1 D3 D5 F2 F3 唯一的读数路径** |
| **视觉 / 附件** | `parent_read_image` | 理解教材扫描页 / 截图 | 只对**聊天上传的图片**（引用值原样传） |
| | `parent_read_upload` | 读聊天附件正文（txt/md/csv/json…） | 附件区与资料库**是两个隔离区域**，不许自己拼路径 |
| **对话回顾** | `parent_read_child_conversation` | 读孩子**逐字稿** | 只读、只限自己名下的孩子；默认今天，最多 7 天；**汇报要概括、不大段复述** |
| **报表** | `parent_display_report` | 把 markdown 汇总推到家长端「📊 报表」区并留存 | **只收 markdown**（不写 HTML）；数据要用刚查到的真实结果 |
| **计划（18）** | `parent_list_children` / `parent_child_create` / `parent_child_update` | 见上（**在两个数组里都注册**） | — |
| | `parent_study_plan_sources` | 看某孩子**真实课程结构**（起草计划用） | 排计划前**必查**，不猜课程名 |
| | `parent_study_plan_create` / `_list` / `_get` / `_update` | 排 / 看 / 看某天 / 删·挪天·改复习 | 复习课加「复习：」前缀；同日同课跳过；**未学完次日自动顺延**（不用手动挪） |
| | `parent_life_plan_create` / `_list` / `_update` | 生活计划（**必须完成项**） | 当天没完成影响完成率与积分；同天同标题跳过 |
| | `parent_exam_plan_create` / `_list` / `_cancel` | 预约 / 查看 / 取消考核 | `courses` 必须**精确课程名**；模糊范围先查再确认；**信息不全必须问** |
| | `parent_recurrence_create` / `_list` / `_update` | 重复计划规则（每日/每周固定项） | 系统**自动展开成当天行**；停用/删除走 `_update` |
| | `parent_scheduler_task_create` | **自定义定时任务**（自然语言指令，到点无头执行） | 执行会话工具白名单：**可查天气、可建提醒、可读写 daily**；**不能碰积分/考核/学习计划**；失败不重试 |
| **其他** | `log_activity` | 记一条家长操作日志 | 工具内部也在写（activity-log 是审计面） |
| | `get_date` | 取"今天/现在" | — |
| **文件** | `read` / `write` / `edit` / `ls` | 家长**工作区**文件 | 仅工作区；**正式资料要用 `parent_put_material` 发真源**（不能靠 write 落资料库） |

### 3.2 通用数据 API：已决定退场（含退场前置）

> **决定（2026-09-25）**：通用数据 API **不再使用**，`parent-data` 会话（数据管理助手）整组退场。它本是"给任意登记表一套通用读写"的试验田；实践下来，**场景专用工具（业务语言 + 自带口径）比通用通道更贴合家长**——孩子端 `ISSUE-142`（把通用 `child_db_read/write/describe` 换成 `child_exam_result` / `child_mastery_report` / `child_points_report`）已经证过同一件事，**家长侧是同一问题的未处理版本**。

**退场的四件**（今天的形态，留档备查）：

| 工具 | 干什么 | 关键边界 |
|---|---|---|
| `parent_db_read` | 只读查询（等值 where + 列裁剪 + 排序 + 行数上限；`countOnly` 只数行；`path=` 一次多跳） | 两个助手**同名不同契约**（见下 ⚠️） |
| `parent_db_write` | 受控 insert/update/delete（列白名单 + 校验 + **行数熔断** + 事务 + 审计；update/delete **必须带 where**） | 家长助手版自称"传 child 只能写两张表"，数据助手版是"全部登记表可写" |
| `parent_db_describe` | 查单表 / 路径 / ns 的列结构与校验规则 | 写前确认必填与引用；读操作通常不用 |
| `define_namespace` | **设计器**：新建一类自定义数据（习惯打卡…） | 草案要在「设置 → 自定义数据」点确认；**只挂数据管理助手** |

**⚠ 退场前置（硬依赖，顺序不能颠倒）**：通用通道是今天 **D1 / D3 / D5 / F2 / F3** 唯一的读数路径，**必须先有场景专用工具**：

| 先补什么 | 才能退掉什么 | 否则 | 状态 |
|---|---|---|---|
| `parent_child_exam_report`（考核逐题 + 每课概要） | `parent_db_read` 在 **D1** 的用法 | 「这次考得怎么样」**断供** | ✅ 已就位（`ISSUE-144` P4） |
| `parent_child_mastery_report`（知识点档位 + 学习结果） | **D5**（D2 的进度可退回 `parent_library_topics`） | 「哪里薄弱」**断供** | ✅ 已就位（并承接 D3 错题本） |
| `parent_child_points_report`（积分与结算 + 兑换） | **F3 / F2** | 「她这分怎么算的」**断供**；兑换审批要改走界面或专用工具 | ✅ 已就位（读；**发放仍无对话入口**） |
| 错题本（**D3**）已有口径 | — | D3 需要一段专用读工具，或并入掌握报告 | ✅ **并入掌握报告**（概览按 kind 聚合，单课/主题可下钻） |

> ⇒ **过渡期（已定 2026-09-25）**：**暂留 `parent_db_read` 当过渡桥**（不立即断供），但在提示词里**降级为"没有专用工具覆盖时的兜底"**；三个专用工具列为**下一批工具的第一优先**（§8 #2）。

> ⚠️ **一处口径不一致（随退场自动消失，但教训保留）**：工具描述写着"传 child＝该孩子库全部登记表可写，管理口径"，而基底提示词那段"通用数据查询"仍写着"传 child 只允许 `daily_entries` / `redemption_requests` 两张表可写"——**同一件事写了两处**。退场后冲突消失，但"**口径只写一处**"要成为场景专用工具的硬规矩：**工具描述只写触发条件，口径写在场景技能里**。

**代码侧退场清单（尚未执行）**：`ParentSessionKind` 的 `parent-data` 分支、`buildServerDataAgentPrompt`、`DATA_AGENT_TOOL_NAMES`、`createDataAgentTools`、侧栏「🗃️ 数据管理」入口及其视图、该会话的历史文件（归档即可）。

### 3.3 刻意没有的工具（不是遗漏，是设计）

| 没有的工具 | 为什么 | 对应场景的替代 |
|---|---|---|
| **配置读写**（模型 / 资料上限 / TTS / 定时任务配置） | **无替代——这是缺口**（说明书却承诺了） | 家长点「设置」（域 F 讨论 · 二） |
| **积分档位 / 兑换目录配置** | 无 | 界面 `RewardPanel` |
| ~~**提醒（create_reminders）**~~ | **不是缺口**：提醒由**自定义定时任务**顺手创建（`parent_scheduler_task_create` 的指令里写"建提醒"，执行会话白名单含 `create_reminders`，到点语音播报） | 归入 **E6**（家长端的"提醒"= 定时任务的一种用法） |
| **考核发起 / 判分** | 考核是另一条链路 | 界面考核页 + `parent_exam_plan_create` 只负责**排期** |
| **改孩子密码** | 认证红线：agent **永不触碰**认证/账户/密码 | 「孩子管理」界面 |
| **删孩子** | 同上（删除孩子会连带清本地空间，不可逆） | 「孩子管理」界面 |
| **直接改数据库** | 一律走受控通道（写入带校验 + 审计 + 熔断） | — |

---

## 4. 场景 · 工具 · 提示词 对照（6 域 / 37 条）

> **读法**：每条场景一行 → 这个场景**用哪些工具**、**用哪些提示词**、**提示词怎么管**（① 写死 / ② 家长自定义 / ③ 自动注入；✅ 已具备 / ❌ 缺 / ⚠ 缺口径或方向相反 / — 无需）。
> **核实程度**：**A / B / C / E 四域**为逐条核实（工具与提示词都对着代码读过）；**D / F 两域**工具已核实、提示词现状为**初判**（随各域讨论校准）。

### 域 A · 孩子与档案（6 条）

| # | 场景 | 形态 | 用到的工具 | 用到的提示词（该说什么） | 提示词怎么管 |
|---|---|---|---|---|---|
| **A1** | **添加孩子**：「帮我加个孩子叫珊珊」→ 落一个孩子档案（含 AI 伙伴名字/性格） | 对话 | `parent_child_create` | **引导口径**：建完要说明**密码得去界面设**、上限是多少、同名会被拒 | **① 应写 · ❌ 零覆盖**（只在工具描述里） |
| **A2** | **改档案 / 换 AI 人设**：「把 AI 名字改成小豆」「孩子今年 8 岁了」 | 对话 | `parent_child_update` | 改前复述、只改说的那几项；**改名会连带影响**（课程名不带孩子名，但计划对象按姓名定位） | **① 应写 · ⚠ 薄**（工作原则里有"复述"，本域没写） |
| **A3** | **看孩子名单**：「我有几个孩子」 | 对话 | `parent_list_children` | — | **— 无需** |
| **A4** | **看总体情况**：进工作台 → 每个孩子的进度卡 / 积分 / 学习看板 | 界面 | —（界面聚合） | — | **— 无需** |
| **A5** | **看某一个孩子详情**：8 个页签（进度/计划/日常/主题/复习/考核/积分/账号） | 界面 | — | — | **— 无需** |

> **已删（2026-09-25）**：原 **A6 跨孩子对比**（「珊珊和小宇谁学得多」）——按决定**不做比较孩子**，本文不再列此场景。它当时用的是 `parent_library_topics`（实时聚合进度）+ `parent_db_read`，两者仍在，供 D2 / D1 等场景使用。

#### 域 A 讨论（2026-09-24）

> **讨论起点**：孩子档案是家长"第一件要做的事"，工具够不够、提示词说清了没有？
> **核对结论**：**工具 3 个够用，提示词几乎空白；而且"建号引导"这条最自然的对话场景是零覆盖的。**

**一、工具侧：够，且边界刻意收紧**

| 能做 | 不能做（工具层直接拒绝并指向界面） |
|---|---|
| 加孩子（姓名 + AI 伙伴名/头像/性格 + 年龄/年级/兴趣） | **设 / 重置密码**（认证红线） |
| 改档案（含改名） | **删孩子**（不可逆，会连带清本地空间） |
| 列名单 | 改订阅 / 许可 |

> 这三个工具**恰好覆盖"家长张口就能说"的那部分**，而把"危险 + 需要二次确认"的部分留给界面——**这个切法是清楚的**，值得保留。

**二、提示词侧：一个真缺口**

1. **A1 的"建号引导"零覆盖**：家长说"帮我加个孩子"时，agent 应该主动**一并问 AI 伙伴名字与性格**（这是孩子端 A1 场景「我是谁」的唯一数据来源——孩子版已确认这段档案**没被接进提示词**，见孩子版 §域 A 讨论 A1）。**家长这里是这条数据的上游**，上游不问清，下游一定缺。

**三、A 域待你定（1 条）**

1. **建孩子的对话流程要不要标准化**（强制问 AI 名字/性格 → 再落库 → 提示去设密码）？还是维持"能建就行、其余引导去界面"？

### 域 B · 主题与课程（6 条）

| # | 场景 | 形态 | 用到的工具 | 用到的提示词（该说什么） | 提示词怎么管 |
|---|---|---|---|---|---|
| **B1** | **建主题**：「我想开一门『国学』」→ 落主题（目录名 + 教学方法 + 考核方法） | 对话（+ 主题编辑器五步向导界面） | `parent_upsert_topic`（+ `parent_library_topics` 核对） | **落库主题与课程**一段：先核对现有结构 → 复述 → 写；**进度字段不要手写** | **① 写死 · ✅ 已具备** |
| **B2** | **建课程 / 改课程结构**：「这主题下加三课」「把第 2 课的教法改成…」 | 对话（+ `CourseManager` / `TopicDetail` 界面） | `parent_upsert_course`（+ `parent_library_courses`） | 同上（`topic + title` 联合主键 upsert） | **① 写死 · ✅ 已具备** |
| **B3** | **落库教学文案 / 资料路径 / 考核要点**：给某课写"讲什么、用什么资料、要考什么" | 对话（+ `TopicDetail` 编辑） | `parent_upsert_course`（`teaching_copy` / `html_path` / `assess_rubric`） | 同上；`html_path` 要与资料真源里**真实存在**的文件对上 | **① 写死 · ⚠ 未明说"路径要真实存在"** |
| **B4** | **分配主题给孩子**：让孩子能学这门主题 | 界面（分配动作） | —（分配时把课程快照写进孩子库） | **边界口径**：`parent_sync_courses_to_child` **不代分配**——"想让孩子学 → 先分配" | **① 写死 · ✅ 已具备**（写在同步段里） |
| **B5** | **课程改名 / 换主题**：「把《学而篇》改叫《论语·学而》」 | 对话 | `parent_rename_course`（**唯一入口**） | 改名段：**绝不能用 upsert 改名**、会保 uuid 与孩子进度；改完提醒"旧计划里的旧课名要人工核对" | **① 写死 · ✅ 已具备** |
| **B6** | **库同步 / 体检**：「孩子那边怎么找不到这门课」→ 对齐家长库与孩子库 | 对话 | `parent_sync_courses_to_child`（+ `parent_study_plan_sources`） | 同步段：**范围只到已分配主题**、幂等、不改进度不删行、**跳过了什么要如实转述** | **① 写死 · ✅ 已具备** |

#### 域 B 讨论（2026-09-24）

> **讨论起点**：课程是**全系统真源**（孩子学什么、考什么全靠它），家长 agent 在这一点上够不够？
> **核对结论**：**这是家长 agent 建设得最好的一域**——工具齐、边界清、提示词厚，**与孩子端"数据全可读但没口径"形成鲜明对比**。**唯一薄弱点是一句缺失的"文件要真实存在"。**

**一、为什么这一域反而做得最好（推论）**

因为它**踩过坑**：改名曾经用 upsert 导致"旧行残留 + 孩子进度永久对不上"（R1），同步曾经越界写入未分配主题（孩子看不到、日后一次性冒出上百门课）。**踩过坑的地方才会有提示词**——这条经验值得推广到还没踩过的域（尤其域 D）。

**二、工具侧的两个"设计亮点"**

1. **改名与 upsert 被拆成两个工具**，并在提示词里写死"改名绝不能用 upsert"——**用具名工具承载"唯一正确入口"**，比写十条注意事项都有效；
2. **同步工具不越界**：不代分配、不改进度、不删行（家长库已删的只列出来交家长决定）——**"报告而不执行"的克制**，是 agent 设计的正面样板。

**三、缺口：`html_path` 的"真实性"没人保证**

| 环节 | 现状 |
|---|---|
| 家长对 agent 说「这门课用 `lunyu/lesson-01.html`」 | agent 直接写进课程行 |
| 该文件**不存在** | **没有任何工具会校验** |
| 后果 | 孩子端点读/展示时才发现——**孩子版 I1/C2 的失败会归因到"资料推不出来"** |

⇒ 建议：**`parent_upsert_course` 在写 `html_path` 时做一次存在性检查**（存在→正常写；不存在→**列出同主题下相近的候选路径**让家长确认）。成本低、把问题挡在真源入口。

**四、B 域待你定（1 条）**

1. `html_path` 要不要加"存在性校验 + 相近候选提示"？

### 域 C · 资料与内容生产（8 条）

| # | 场景 | 形态 | 用到的工具 | 用到的提示词（该说什么） | 提示词怎么管 |
|---|---|---|---|---|---|
| **C1** | **资料盘点**：「资料库里都有什么」→ 按主题列清单 | 对话（+ 文件/资料管理界面） | `parent_list_materials` | 资料段：**整理前必做**、先看"现在有什么" | **① 写死 · ✅ 已具备** |
| **C2** | **读资料 / 查重**：「这两份是不是重复的」 | 对话 | `parent_read_material` | 何时调用（判断重复、检查链接）；**二进制只回元数据** | **① 写死 · ✅ 已具备** |
| **C3** | **整理资料**：移动 / 改名 / 归并散落文件 | 对话 | `parent_list_materials` → `parent_move_material` | 先列清单、方案先复述、批量分步做 | **① 写死 · ✅ 已具备** |
| **C4** | **删资料**：「这份不要了」 | 对话 | `parent_delete_material`（**默认演练**） | 删除段：**演练清单 → 复述取得同意 → 带 confirm 真删** | **① 写死 · ✅ 已具备** |
| **C5** | **上传 / 发布资料**：把教案、练习页落到真源 | 界面上传（`MaterialManagerModal`）+ 对话 `parent_put_material` | `parent_put_material`（≤2MB 文本） | 何时调用；覆盖前先读（防误盖手改版） | **① 写死 · ✅ 已具备** |
| **C6** | **造互动页 / 绘本 / 小游戏**：「给这课做个互动练习页」 | 对话（拉起编程 agent） | `parent_build_material` → 编程 agent（`read/write/edit`） | 何时调用、`path` 语法、**需先配"编程 agent 模型"** | **① 写死 · ⚠ 只写了一句**（提示词里 `parent_build_material` 未点名） |
| **C7** | **看图片资料**：教材扫描页 / 截图里的内容 | 对话（附件） | `parent_read_image` | 附件段：**把引用值原样传**、**别自己拼路径**；读不到要如实转述 | **① 写死 · ✅ 已具备** |
| **C8** | **看聊天里传的文件**：txt/csv/json 附件 | 对话（附件） | `parent_read_upload` | 同上（与 C7 同一段） | **① 写死 · ✅ 已具备** |

#### 域 C 讨论（2026-09-24）

> **讨论起点**：家长"上传资料"这件事，说明书里写着"支持上传 HTML / 音视频 / 图片 / 文档"，agent 侧够不够？
> **核对结论**：**整理与理解类能力（C1–C5、C7、C8）齐且提示词厚；真正的薄弱点在 C6 的"最后一跳"和"不许自己拼路径"的边界。**

**一、把"资料"拆成三件事，工具面才对得上**

| 事 | 谁做 | 工具 |
|---|---|---|
| **放进真源** | 家长（界面上传）或 agent（写文本 / 拉编程 agent 造页面） | `parent_put_material` / `parent_build_material` / 界面 |
| **整理真源** | agent | `parent_list_materials` / `read` / `move` / `delete` |
| **理解真源** | agent | `parent_read_material` / `parent_read_image` / `parent_read_upload` |

⇒ **这三件事都有工具**，所以 C 域"工具缺口"实际为零。**视频（mp4）是唯一没有对话出口的形态**——`parent_read_material` 对其只回元数据（设计如此，防灌上下文），而**"把裸视频变成孩子能播的页面"这一跳在孩子版已单独立项（`ISSUE-141`）**，家长侧同样没有。

**二、C6 的"最后一跳"是薄的地方**

- 提示词里**没有出现 `parent_build_material` 这个名字**，也没写"造页面时要交代清楚内容、结构、交互"；
- 编程 agent **没配模型会明确报错**（工具描述里有，提示词里没有）⇒ 家长第一次用很可能碰壁且不知道去哪配。

**三、C7/C8 值得当作正面样板**

附件那一段把**"唯一正确读法"**和**"失败时怎么转述"**都写清了（还写了现场怎么失败的）。**这就是我们想要的口径写法**——建议域 D 的考核解读按同样力度写。

**四、C 域待你定（1 条）**

1. **C6 要不要在提示词里点名 `parent_build_material`**（并写清"没配编程模型怎么办"）？还是维持"工具描述够用、不重复"？

### 域 D · 孩子的数据洞察（7 条 · 家长最高频的"问"）

> 这一域是**家长开口最多的**（"学得怎么样/考得怎么样/哪里薄弱"），也是**工具与提示词错位最明显**的一域。

| # | 场景 | 形态 | 用到的工具 | 用到的提示词（该说什么） | 提示词怎么管 |
|---|---|---|---|---|---|
| **D1** | **考得怎么样**：「珊珊这次考了多少？哪几题错了？」→ 逐题 + 原因 | 对话（+ 界面考核记录） | **✅ `parent_child_exam_report`**（最近几场含趋势 / 某一场逐题 + 每课概要 + 知识点档位；未考完只给范围） | **解读口径**：先给整体（得分/范围/趋势）→ 再讲错在哪 → 区分"没学过"与"学过但错" | **① 写死 · ✅ 已具备**（`parent-scene-progress`） |
| **D2** | **学得怎么样 / 进度**：「这周学得怎么样」→ 进度 + 最近学习时间 | 对话 | `parent_library_topics`（实时聚合）/ `parent_child_mastery_report`（掌握面） | 汇报口径：进度取"最近学习时间"、掌握取"最近一次考核得分率"；**按主题聚合、不整表罗列** | **① 写死 · ⚠ 散在"工作原则"里，本域没有专节** |
| **D3** | **哪里薄弱 / 老错什么**：错题本视角的弱项概览 | 对话 | **✅ `parent_child_mastery_report`**（薄弱项按 kind 聚合；单课 / 主题可下钻） | 错题本段：**按 kind 聚合概览**、`count` 越大越没掌握、已关闭项不进概览 | **① 写死 · ✅ 已具备**（`parent-scene-progress`） |
| **D4** | **具体说了什么**：「她昨天到底问了什么」→ 读逐字稿 | 对话（+ `SessionReview` 界面回顾/导出） | `parent_read_child_conversation`（默认今天，最多 7 天） | 对话记录段：适用判断、只读边界、**概括要点不大段复述** | **① 写死 · ✅ 已具备** |
| **D5** | **知识点掌握**：「《静夜思》的背诵到底会不会」→ 知识点档位 | 对话 | **✅ `parent_child_mastery_report`**（`course` 展开逐条知识点档位；概览/主题看需要盯的） | **档位口径**：`solid/partial/weak`（考核过程）与 `not_started/learning/needs_review/mastered`（累计）两层要分开；**"没分析过" ≠ "学得差"** | **① 写死 · ✅ 已具备**（`parent-scene-progress`） |
> **已删（2026-09-25）**：原 **D6 多孩子对比**（「谁掌握得好」）——与 A6 一并按决定删除，本文不再列此场景。
| **D7** | **阶段性汇总 / 报表**：「给我来份周报」→ 生成 markdown 报表推到「📊 报表」区 | 对话（+ 报表区界面） | `parent_display_report`（+ 上述读工具） | 报表口径：**用刚查到的真实数据、不编造**；markdown 结构 | **① 写死 · ✅ 已具备**（写在工具描述里） |

#### 域 D 讨论（2026-09-24）

> **讨论起点（用户提出）**：要根据家长 agent 的使用场景，逐条核对工具与提示词是否匹配。
> **一句话结论**：**这是全篇最严重的一处错位——数据全都能拿到（通用通道逐表直读），但"家长真正会问的三句话"里有两句（考得怎么样、哪里薄弱）既没有专用工具、也没有一个字的口径。**

**一、先看"能不能拿到数据"：能，而且很全**

三张考核结果表 + 知识点两表 + 积分三表 + 错题本，**都在孩子库的登记表清单里**，`parent_db_read`（两个助手都能用）**逐列可读**：

| 家长想知道的 | 数据在哪 | 家长助手能读到吗 |
|---|---|---|
| 这次考核考了哪些题、每题几分、对不对 | `exam_plan_courses`（**一行一题**：题干 / 原文 / 得分 / 对错 / AI 评语 / 语音转写 / 录音引用） | ✅ 能（直读） |
| 每课每次的得分率与复习重点 | `exam_course_results` | ✅ 能 |
| 知识点档位（扎实/一般/薄弱）与累计叙述 | `knowledge_point_progress` / `knowledge_point_records` | ✅ 能 |
| 薄弱项（错题本） | `mistake_book` | ✅ 能，**且提示词里已经写了口径**（D3） |
| 积分流水与每日结算 | `points_ledger` / `reward_daily_stats` | ✅ 能 |

**二、再看"讲得清不清楚"：讲不清，因为口径与专用工具都不在**

| 对比项 | **孩子端（已落地）** | **家长端（现状）** |
|---|---|---|
| 考核逐题 | `child_exam_result`（**业务语言只读工具**：最近几场 / 某一场逐题 / 每课概要 / 知识点档位） | **无对应物**——只能 `parent_db_read` 拉表行 |
| 掌握与学习结果 | `child_mastery_report` | **无对应物** |
| 积分报告 | `child_points_report` | **无对应物** |
| 提示词口径 | 孩子版已定稿 E7「考后复盘」三段式（先给整体 → 一次只讲一两题 → 先问卡在哪再讲） | **零覆盖**（提示词里只有错题本那一段） |

⇒ **这不是"家长 agent 弱"，而是"家长侧的读数能力从来没被按场景设计过"**——孩子端在 `ISSUE-142` 里刚做完"把通用读换成场景专用报告工具"的改造，**家长侧是同一问题的未处理版本**。

**三、为什么这一域值得优先做（三个理由）**

1. **它是家长最高频的动机**：打开家长助手，第一句多半是"孩子最近怎么样"；
2. **它已经有现成模板可抄**：孩子端三个报告工具的**契约与口径可以直接平移**（`parent_child_exam_report` / `parent_child_mastery_report` / `parent_child_points_report`）；
3. **它能一次解掉 D1 / D5 两条"零覆盖"**（原本还含 A6/D6 的跨孩子对比，已按决定删除）：口径写在专用工具里，比写在基底提示词里更容易落地（孩子版已验证：口径写在工具描述里比写在基底里更容易生效）。

**四、⚠️ 一条红线要在设计时守住**

**家长的"讲解"和孩子的不一样**：孩子端讲错题有一条硬约束——**不给标准答案**（防套题）。**家长侧可以给答案**（`parent_library_course_content` 本来就返回 `answer`），但要注意：**家长可能把答案转述给孩子**。所以口径要写清"给家长看的解释 ≠ 给孩子用的提示"，并把"要不要让孩子知道答案"交给家长自己判断。

**五、D 域待你定（3 条）**

1. **3 个家长专用报告工具什么时候做**（考核逐题 / 掌握与学习结果 / 积分与结算）？——**通用通道已决定退场，这不再是"要不要做"，而是"退场前置"**（§3.2）。
2. **D1 的解读口径按什么力度写**：只写"先给整体再讲细节"，还是像孩子端 E7 那样写成**三段式规格**（可执行）？
3. **家长侧是否允许给出标准答案**（并提示"这是给家长看的，你可以决定要不要告诉孩子"）？

### 域 E · 计划与排期（8 条）

| # | 场景 | 形态 | 用到的工具 | 用到的提示词（该说什么） | 提示词怎么管 |
|---|---|---|---|---|---|
| **E1** | **排学习计划**：「明天上午安排珊珊学《静夜思》」→ 落排期并出现在今日计划 | 对话（+ 孩子详情页计划 tab 界面） | `parent_study_plan_sources`（先查）→ `parent_study_plan_create` | 计划域段：**先查真实课程名、不猜**；复习课加「复习：」前缀；**改前复述** | **① 写死 · ✅ 已具备** |
| **E2** | **改 / 挪 / 删学习计划**：「周三那门不学了」「挪到周五」 | 对话 | `parent_study_plan_list`（取行 id）→ `parent_study_plan_update` | 同上；**未学完系统自动顺延，不用手动挪** | **① 写死 · ✅ 已具备** |
| **E3** | **看排期**：「这几天怎么排的」 | 对话 + 界面 | `parent_study_plan_list` / `parent_study_plan_get`（某天聚合，含顺延与生活计划） | 同上 | **① 写死 · ✅ 已具备** |
| **E4** | **排生活计划**：「每天整理书包」→ 必须完成项 | 对话 | `parent_life_plan_create` / `_list` / `_update` | 计划域段：必须完成项**当天没完成影响完成率与积分**；同天同标题自动跳过 | **① 写死 · ✅ 已具备** |
| **E5** | **排重复计划**：「每周一到周五都练字」→ 规则自动展开成每天的行 | 对话 | `parent_recurrence_create` / `_list` / `_update` | **重复规则口径**：daily / weekly 语义、生效区间、停用与删除的区别、**展开后仍是"必须完成项"** | **① 应写 · ❌ 零覆盖**（只在工具描述里） |
| **E6** | **自定义定时任务 / 定时提醒**：「每天早上 6 点查天气并播报」「每天提醒她做眼保健操」→ 到点无头执行（提醒会到孩子设备语音播报） | 对话（+ 定时任务面板；孩子端「我的提醒」） | `parent_scheduler_task_create`（+ 界面 `SchedulerTasksPanel`；**提醒由它的执行会话用 `create_reminders` 落地**） | **任务口径**：**指令要写明"做什么、产出什么"**、**能力白名单**（可查天气 / 可建提醒 / 可读写 daily；**不能碰积分·考核·学习计划**）、失败不重试 | **① 应写 · ❌ 零覆盖** |
| **E7** | **排考核**：「周六考一下《学而篇》」→ 落一个考核场次 | 对话 | `parent_exam_plan_create`（`_list` / `_cancel`） | 计划域段：`courses` **必须精确课程名**；模糊范围（「最近学的 3 课」）**先查再确认**；**信息不全必须问** | **① 写死 · ✅ 已具备** |
| **E8** | **改 / 取消考核排期**：「那场考核取消吧」 | 对话 | `parent_exam_plan_list` → `parent_exam_plan_cancel` | 计划域段 + 工作原则（复述） | **① 写死 · ⚠ 薄**（cancel 未点名） |

#### 域 E 讨论（2026-09-24）

> **讨论起点**：家长端的计划工具是**最多的一组**（18 个），提示词只写了其中 3 类。
> **核对结论**：**"每天排课"这一主线（E1–E4、E7）工具与提示词都齐；"规则与自动化"这一支（E5、E6、E8）工具齐、提示词为零——全靠工具描述兜。**

**一、18 个工具的分工（一眼看清）**

| 支 | 工具 | 提示词覆盖 |
|---|---|---|
| **学习计划** | `sources` + `create` / `list` / `get` / `update` | ✅ 段里有 |
| **生活计划** | `create` / `list` / `update` | ✅ 段里有 |
| **考核排期** | `create` / `list` / `cancel` | ✅（cancel 薄） |
| **重复规则** | `recurrence_create` / `_list` / `_update` | ❌ 无 |
| **自定义定时任务** | `scheduler_task_create` | ❌ 无 |
| **孩子**（复用） | `list_children` / `child_create` / `child_update` | 部分（见域 A） |

**二、E5（重复规则）与 E2（逐天排）是同一件事的两种做法，提示词必须教"什么时候用哪个"**

| | 逐天排（`parent_study_plan_create`） | 重复规则（`parent_recurrence_create`） |
|---|---|---|
| 适合 | 一次性的、有具体日期的安排 | **长期固定项**（"每天""每周一三五"） |
| 落库 | 一天一行 | **一条规则**，系统每天自动展开 |
| 改 | 一行一行改 | **改规则**（改完影响未来所有展开） |
| 风险 | 排一个月＝30 行，改起来痛苦 | 家长以为"删了规则"其实历史行还在 |

⇒ **不写这段口径，模型会在"家长说'每天'时逐天排 30 行"**（这不是猜测：工具描述里虽然写着"无需逐天排"，但**模型先看到的是提示词**）。**建议在计划域那一段里补一小节「长期固定项用重复规则」。**

**三、E6（自定义定时任务）是全篇最"危险"的工具，提示词却一个字都没有**

- 它是**唯一一个"家长说完、系统日后自动执行自然语言指令"**的工具（无头 agent 到点跑）；
- 它的**能力白名单**（可查天气 / 可建提醒 / 可读写 daily；**不能碰积分、考核、学习计划**）**只写在工具描述里**；
- 它的失败语义（**当天失败不重试**）**也只写在工具描述里**。

⇒ 家长很自然会说「每天帮我提醒孩子做眼保健操」「每周日汇总一下这周」——**落错工具（用错 API）或写了超出白名单的指令，都会静默失败**。

**四、E 域待你定（2 条）**

1. ~~计划域那段要不要扩成三个小节（逐天排 / 重复规则 / 定时任务）~~ → **已定（2026-09-25）**：不扩小节，而是**把这一域拆成两个场景技能**——`parent-scene-plan`（**学习与考核安排**：逐天排 / 生活计划 / 重复规则 / 考核场次）与 `parent-scene-automation`（**定时任务与提醒**）；**"每天/每周"这类重复规则在两个场景里都写**，并加"**同源 + 一致性验收**"防漂移（见 `docs/家长agent-场景skill方案-讨论稿.md` §6.1 与 §9 第 18 条）。
2. **E6 的能力白名单与失败语义要不要写进提示词**（而不只是工具描述）？→ **由 `parent-scene-automation` 承接**（写进场景技能正文：可查天气 / 建提醒 / 读写 daily；不能碰积分·考核·学习计划；失败不重试）。

### 域 F · 积分、系统设置与运维（4 条）

| # | 场景 | 形态 | 用到的工具 | 用到的提示词（该说什么） | 提示词怎么管 |
|---|---|---|---|---|---|
| **F1** | **配置积分档位 / 奖罚规则**：按区间设档位与分值 | 界面（`RewardPanel`） | **❌ 无工具** | — | **— 无需**（界面自足） |
| **F2** | **配置兑换目录 / 审批兑现**：定义可兑换权益、履约后标记已兑现 | 界面 | **❌ 无工具**（读：`parent_child_points_report` 报"已提交未发放"；**发放无对话入口**） | **审批口径**：⚠️ 台账原写"**扣分发生在"家长履约"时**，不是申请时"，**与实现不一致**（实现是**提交即扣**，见 §8 #15） | **① 应写 · ⚠ 口径待拍板**（`parent-scene-points` 已按实现写） |
| **F3** | **看积分流水**：「她这分怎么算的」 | 对话 + 界面 | **✅ `parent_child_points_report`**（余额 + 规则 + 逐日结算 + 流水 + 未发放兑换） | 解释口径（与孩子端 B6/G1 同源，但**按实现的机制重写**：家长组直接评档 → 门槛只卡加分项 → 次日结算） | **① 写死 · ✅ 已具备**（`parent-scene-points`） |
| **F4** | **配置管理**：「把默认模型换成 XX」「资料上限调到 30」 | **说明书说能对话做** | **❌ 无工具（回归）** | **❌ 零覆盖** | **① 应写 · ❌ 工具不存在** |

#### 域 F 讨论（2026-09-24）

> **讨论起点**：说明书 §3.7 把家长助手的第一个能力域写成「**配置管理（Control）**：查看/修改配置（默认模型、资料上限、定时任务、孩子档案）」。
> **核对结论**：**这四件事里只有"孩子档案"有工具**（`parent_child_create/update`）；**默认模型、资料上限、定时任务配置一件都改不了**——属"**文档承诺、实现没有**"，与孩子版 H1（界面文案承诺"直接对 AI 伙伴说"、实际无工具）**同类，建议同样按回归处理**。

**一、逐项对照（说明书 vs 实现）**

| 说明书承诺（§3.7 Control 域） | 实现 | 结论 |
|---|---|---|
| 查看 / 修改**默认模型** | 无工具；`app_settings` 改动走界面「设置」 | ❌ 回归 |
| 查看 / 修改**资料上限** | 无工具 | ❌ 回归 |
| 查看 / 修改**定时任务** | 只有"**新建**自定义任务"（`parent_scheduler_task_create`）；**改/删/停用/查看现有任务全无工具** | ⚠ 半条 |
| 查看 / 修改**孩子档案** | ✅ `parent_child_create` / `parent_child_update` | ✅ |

**二、为什么这条值得当回归（而不是"顺手补个功能"）**

- **家长的使用预期已经被文档建立了**：家长会直接说"把默认模型改成 XX"，agent 只能答"我没有这个能力"——**体验断裂点与孩子版 H1 一模一样**；
- **代价不对称**：补两条工具（读配置 / 写配置白名单 + 复述确认）成本不高；而"改文案"要动的是产品说明书的功能边界；
- **风险口径现成**：写配置属于**高影响动作**，正好套用现有的"**先复述 → 再执行**"范式（`parent_delete_material` 的 dryRun 先例）。

**三、F2/F3 的口径缺口也是"账本口径"问题**

积分这件事在系统里是**四条链路**（每日结算 → 档位命中 → 门控解锁 → 兑换扣减），**家长问"她这分怎么算的"时，模型手上只有流水行**。孩子端已定 G1/B6 的解释口径（"必须完成项达标才解锁加分"），**家长侧应有同一份口径**（可与域 D 的"积分报告工具"合并解决）。

**四、F 域待你定（2 条）**

1. **配置管理走哪条路**：**已定第一步——先写"负向口径"**（如实说"这个我改不了" + 指路设置页，2026-09-25）；剩下的选择是**补工具**（读 + 写白名单，写前复述）还是**改说明书 §3.7 文案**。
2. **积分解释口径要不要与域 D 的积分报告工具合并做**（一次解决 F2 F3 + 孩子端 B6/G1 的家长镜像）？

### 域 G · ~~家长助手本体与数据管理~~（**已删**）

> **2026-09-25 删除**：原域 G 共 2 条（G1 数据管理与自定义场景 / G2 日常答疑与操作承接）。
> - **G1 删因**：数据管理助手（`parent-data`）只是**通用数据 API 的试验田**，通用 API 已决定不再使用、整组退场（§3.2）；`define_namespace`（自定义数据设计器）随之退场。
> - **G2 不是场景**：它是**常驻兜底**——"这门课怎么建""帮我把资料整理一下"由家长助手在**对应场景**里承接（建课归 `parent-scene-course`，整理归 `parent-scene-materials`），不单独立域；它的"**工作原则**"（先列清单 → 复述方案 → 批量分步 → 简洁中文说清"做了什么、影响哪些文件"）**保留为常驻层内容**。
> - 原 G1/G2 的三处"交接缝隙"随之消解（缝隙 2、3 是通用通道自身的问题，随退场消失；缝隙 1 的"换窗口"问题因第二个助手退场而不再存在）。

---

## 5. 场景分层（区分"主线"与"边角"，供后续判断优先级）

| 层 | 场景 | 特点 |
|---|---|---|
| **① 主线（开课前必做 / 每周都在做）** | B1 B2 B3 B4 · C1 C5 · E1 E2 E7 · A1 A2 | 决定"孩子能不能开始学"；这些不顺，后面全卡住 |
| **② 高频支线（每周数次）** | D1 D2 D3 D4 D7 · E3 E4 E5 E6 E8 · C3 C4 C6 C7 C8 · B5 B6 | 影响"用得顺不顺"；**D1 在这里，却连专用工具都没有** |
| **③ 事件触发（低频但重要）** | D5 · F2 F3 F4 | 低频，但缺失时体验断裂最明显 |
| **④ 环境与背景** | A3 A4 A5 · F1 | 不常被注意，出错时家长会觉得"这助手不太行" |

> 与孩子版的对照：孩子端"主线"是**每天的学习过程**（B/C 域为主），家长端"主线"是**开课与排期**（B/E 域为主）——**这解释了为什么 B/E 两域的建设质量明显高于 D 域**：它是被真实使用逼出来的。

---

## 6. 场景背后的产品级硬约束（梳理时必须尊重，不能当 bug 改）

| # | 约束 | 出现在哪些场景 |
|---|---|---|
| 1 | **家长 agent 只看得到自己名下的孩子**——系统按归属校验（对话记录、孩子库、计划全部适用） | D4 D5 E1…F3 |
| 2 | **认证 / 账户 / 密码永不触碰**：agent 不能设密码、不能改订阅、不能删孩子 | A1 A2 · F4 |
| 3 | **删除 / 覆盖这类不可逆动作要"先演练、后确认"**（`parent_delete_material` 是范式；批量写要列清单先取得同意） | C4 · F2 |
| 4 | **改排期 / 建考核 / 落库课程内容前要复述方案**——家长看不到模型的计划 | B1 B2 B5 · E1 E2 E7 · F4 |
| 5 | **改课程名只能走 `parent_rename_course`**（upsert 改名＝新建行 + 旧 uuid 残留 → 孩子进度永久对不上） | B5 |
| 6 | **库同步范围只到"已分配主题"**，不代分配、不改进度、不删行 | B4 B6 |
| 7 | **课程内容写入是"整课全量快照"（替换语义）**，不是增量 | C4 C5 |
| 8 | **"完成 / 未完成"与"积分结算"只有系统能定**，家长 agent 只能读、不能替孩子勾 | D2 F3 |
| 9 | **现实权益扣分发生在"家长履约时"**，不是孩子申请时 | F2 |
| 10 | **资料真源与上传区是两个隔离区域**——附件的唯一读法是把引用值原样传给读图/读文件工具，不许自己拼路径 | C7 C8 |
| 11 | **家长工作区 ≠ 资料库**：fs 工具只能动工作区，正式资料必须走 `parent_put_material` / `parent_build_material` | C5 C6 |
| 12 | **家长会话长期累积、不做跨天裁决**（与孩子端不同）：所以"重置会话"＝丢当前上下文（旧 jsonl 只作归档）；**改提示词必须重开会话才生效，而家长根本改不了提示词** | §2 · 全篇 |
| 13 | **自定义定时任务的执行会话有自己的能力白名单**（可查天气 / 建提醒 / 读写 daily；**不能碰积分·考核·学习计划**），且**失败不重试** | E6 |
| 14 | **孩子端读不到题库答案**（防套题）；**家长端读得到**——但"给家长的解释"与"给孩子的提示"要分开表述 | D1 · §域 D 讨论 · 四 |

---

## 7. ⚠️ "对话里说得出口、却没有对话出口"的家长场景

> 与孩子版 §7 同一性质：家长的表达方式是**自然语言**，但产品只给了界面（或什么都不给）。

| # | 家长的原话 | 现在的现实 | 讨论点 |
|---|---|---|---|
| **F4** | 「把默认模型改成 XX」「资料上限调到 30」 | **工具不存在**（说明书 §3.7 却承诺了） | **回归**：补工具（读 + 写白名单 + 复述）还是改文案？（域 F 讨论 · 二） |
| **D1** | 「她这次考得怎么样，错在哪」 | **能读表、没有专用工具、没有口径**——模型要么拉一堆表行、要么含糊 | 按孩子端模板补 `parent_child_exam_report` + 三段式口径——**这也是通用通道退场的前置**（域 D 讨论 · 三 / §3.2） |
| **D5** | 「《静夜思》的背诵到底会不会」 | 现在靠通用通道直读表行（**该通道计划退场，§3.2**） | 掌握报告工具 + "空≠差"的口径 |
| **F2** | 「她申请的那个兑换批一下」 | 界面可做；对话里原本靠 `parent_db_write` 直写，**扣分口径没写**；**通用通道退场后这条路也关了** | 并入积分报告工具（`parent_child_points_report`）或明确只走界面 |
| **F3** | 「她这分怎么算的」 | 直读三张积分表（**通道计划退场**），**解释口径零覆盖** | 与域 D 的积分报告合并为 `parent_child_points_report`（§3.2） |

> **不在本节但同样没有出口的**：**改/删已有定时任务**（只能新建）、**C6 视频类资料的孩子端播放**（孩子版 `ISSUE-141` 已立项，家长侧无对应）。

---

## 8. 待确认的口径（需要你拍板，定完才好往下走）

1. **家长提示词要不要给孩子那样的"可改层"**（② 家长自定义规范：机器名 / 称呼偏好 / 汇报风格 / 数据隐私口径）？——现状是**只有写死的 ①**（§2.3）；若要做，属机制变更，可后置。
2. **域 D 的 3 个场景报告工具**（考核逐题 / 掌握与学习结果 / 积分与结算）**什么时候做**？——通用通道已决定退场（§3.2），这是**退场前置**；**✅ 已做（2026-09-25，`ISSUE-144` P4）**：三件一次落地，D3 并入掌握报告、F2 并入积分报告。**剩下的待定项：`parent_db_read` 这座兜底桥什么时候拆**（§8 #9）。
3. **D1 的解读口径写成什么力度**（一句话原则 vs 孩子端 E7 那样的三段式规格）？——**已按"三步"写进 `parent-scene-progress`**（整体 → 定位一两题 → 一条可执行建议）；若你要更硬的三段式规格可以再加。
4. **家长侧是否允许给出标准答案**（孩子端硬约束是"不给"；家长端给了，可能被转述给孩子）？——**已按 D 域讨论·四的方向落地**：可以让家长看（另调 `parent_library_course_content`），但必须说明"**这是给家长看的解释，要不要告诉孩子由你决定**"。**仍待你最终确认。**
5. **F4 配置管理走哪条路**？——**已定第一步：先写"负向口径"**（如实说"这个我改不了" + 指路设置页，2026-09-25）；**待定**的是补工具（读 + 写白名单 + 复述）还是改说明书 §3.7 文案，**建议按回归处理**（域 F 讨论 · 二）。
6. **计划域提示词要不要扩成三小节**（逐天排 / 重复规则 / 定时任务），并写明"长期固定项优先用重复规则"？（域 E 讨论 · 二）
7. **`parent_scheduler_task_create` 的能力白名单与失败语义要不要写进提示词**？（域 E 讨论 · 三）
8. **`parent_upsert_course` 要不要校验 `html_path` 真实存在**（并给相近候选）？（域 B 讨论 · 三）
9. **通用通道的退场时点与过渡桥**：三件场景报告工具未到位前，`parent_db_read` 是**暂留**还是直接断供？（§3.2）——原"两个 `parent_db_write` 契约不一致"的问题**随退场自动消失**，不再需要拍板。**更新（2026-09-25）**：三件工具**已到位**（D1 D3 D5 F2 F3 的读数都有专用出口），`progress` / `points` 里也把 `parent_db_read` 降级成"没覆盖到的细节才用"的兜底。**待你定：是现在就拆这座桥（＝执行 P6），还是先上线观察一个版本周期？**
10. **场景的"组织方式"用哪种**：本文按**操作域**（A–F）逐条台账；`docs/家长agent-场景skill方案-讨论稿.md` 按**"家长要完成的一件事"**重划为 **8 个场景**（了解学习情况 / 备一门课 / 学习与考核安排 / 定时任务与提醒 / 整理资料 / 孩子档案 / 积分 / 改设置），并使用**按需加载的场景技能（skill）**承载口径；**执行步骤见 `docs/家长agent-场景skill实施方案-2026-09-25.md`**（P0 基线 → P1 文案 → P2 机制 → P3 全量 → P4 退场前置 → P5 自定义层 → P6 退场）。**本文保留为逐条台账，新增内容按场景走**——是否这样分工？
11. **积分解释口径与积分报告工具要不要合并做**（一次解决 F2 F3，即 `parent_child_points_report`）？——**通用通道退场后，这从"可选合并"变成"必须合并"**（§3.2）。**✅ 已合并做**（`ISSUE-144` P4）。
12. **A1 建孩子的对话流程要不要标准化**（强制问 AI 名字/性格 → 落库 → 提示去设密码）？（域 A 讨论 · 三）
13. **场景粒度是否合适**：**37 条 / 6 域**里有没有你觉得不该算"场景"的（比如 F1 纯界面配置），或者**你心里有但这里没写出来的**？
14. **家长助手的"人格"要不要定**：现在是**纯运营助手**口吻（列清单、复述、简洁中文），要不要给孩子端那样的"陪伴感"？（决定域 A/D 的汇报语气）
15. **⚠️ 兑换的扣分时点：产品口径 ≠ 实现**（2026-09-25 核实）——F2 记的是"**扣分发生在家长履约时**"，而实现是**提交那一刻就扣分**并写 `type='redeem'` 流水（`server/src/routes/plans-rewards.ts:659-708`），同时生成 `status='pending'` 记录；`fulfilled_at` **目前没有写入方**，该接口**也没有界面调用方**。**待你拍板**：① 以实现为准（那就把 F2 的口径改掉、并补一个"发放"入口）；② 还是按原设计改成"履约时扣"（那要改扣分时机 + 补发放流程）。**当前文案已按实现写**（`ISSUE-144` §3.5 表 2）。

---

## 附：场景 × 形态 统计（仅作参考）

| 形态 | 条数 | 说明 |
|---|---|---|
| **对话（家长说给 AI 听）** | ~20 | 含"界面 + 可对话"双形态的计入；**已扣除删除的 A6 D6 G1 G2** |
| **纯界面** | ~11 | 孩子管理、课程管理、题库、文件、定时任务、Token、设置、报表区、积分配置、考核记录（**原"数据管理页"随数据管理助手退场**） |
| **系统自动** | ~6 | 定时任务到点执行（E6 的下游）、计划自动展开与顺延、每日积分结算、每日记录总结、跨天自动新会话、配置同步 |

| 用到的工具（组） | 场景数 | 哪些场景 |
|---|---|---|
| **计划域（18 件套）** | 10 | A1 A2 A3 · E1–E8（复用面最广的一组） |
| **家长库 / 课程（7）** | 6 | B1–B6 |
| **资料（5）+ 编程（1）** | 7 | C1 C2 C3 C4 C5 ·（C6 走 `parent_build_material`）·（C7 C8 走读图 / 读文件） |
| **通用数据通道（3）**（**计划退场**） | **2** | D7 · F2（**读**的兜底）——D1 D2 D3 D5 F2 F3 的读数**已改由三把专用报告工具承担**（`ISSUE-144` P4），通用通道只剩"专用工具没覆盖的细节"这一层兜底；退场前置见 §3.2 |
| **对话回顾（1）** | 1 | D4 |
| **报表（1）** | 1 | D7 |
| **孩子数据洞察（3）**（P4 新增） | 5 | **D1 D3 D5**（exam / mastery 报告） · **F2 F3**（points 报告） |
| **❗ 缺工具 / 回归** | **3** | **F1 F2**（积分档位与兑换目录的**配置**无工具，且兑换的"发放"无入口） · **F4**（配置管理，回归）——~~D1 D5 无专用工具~~ 已由 P4 解决 |
| **完全不需要工具**（界面或系统自足） | 10 | A4 A5 · B4 · F1 等（含"对话 + 界面双形态"里界面自成的那条腿） |

> **口径说明**：上表按**"场景 × 工具组"**计数（同一场景跨组会重复计入），所以**不可加总**；它只回答"哪组工具在场景里出现得最多"。
> **"补口径"的盘子明显大于"补工具"**：需要**新专用工具**的是 **3 条场景**（D1 考核逐题 · D5 掌握与学习结果 · F3 积分与结算，F2 与 F3 合并即 `parent_child_points_report`）——**这三件同时是通用通道退场的前置**（§3.2），**✅ 已在 `ISSUE-144` P4 一次做完**（D3 错题本并入掌握报告、F2 兑换并入积分报告）。**至此"缺工具"只剩 F1/F2 的配置与 F4 的配置管理（都是"要不要补工具 vs 改文案"的产品取舍）**。

> 说明：统计目的是供下一步判断「哪些场景必须有对话/工具能力、哪些天然属于界面」，**不代表对话形态就更好**——家长端**界面已经相当完整**（9 个侧栏入口），本轮梳理的绝大部分价值在**"补口径"与"补专用工具"**，而不是"把界面搬进对话"。
