# 家长 agent 场景技能（skill）实施方案与步骤

> 记录日期：2026-09-25
> **状态：P0–P3 已执行完毕（见 §3 执行情况回填）；P4 / P5 / P6 未做** —— 代码已落地、类型检查与回归已过。
> **登记为 `ISSUE-144`**（`ISSUE-143` 已被「发音测评音频路径」占用），测试文件 = `test/issue144-parent-skills.test.ts`。
> **依据**：`docs/家长agent-场景skill方案-讨论稿.md`（怎么做、口径写哪、怎么验收）、`docs/家长agent使用场景梳理-2026-09-24.md`（逐条台账，口径真源）
> **同类先例**：孩子端 `ISSUE-142`（把通用读写换成场景专用工具）；提示词分层先例 `ISSUE-136`

---

## 0. 交付什么（一句话 + 三条成功判据）

**一句话**：把家长提示词从"一份写死的长基底"改成「**常驻层（短）+ 8 个按需加载的场景技能**」，工具面全程不变，技能正文由模型按家长意图自行选择加载；同时把"通用数据通道"标为退场，并给出补专用工具的顺序。

| # | 成功判据 | 结果 |
|---|---|---|
| 1 | **常驻上下文实测下降** | ✅ 常驻提示词 **4710 → 2501** 字符（-47%）；**工具块 45 把 25078 → 15997**（含参数 Schema，-36%，见 §2.6） |
| 2 | **验收样本** | ⏸ 18 条对话样本 + 2 条负样本已固化为清单（含跨场景连读、重复规则一致性），**静态自检已过**；**实跑仍需在有模型配置的会话里人工做** |
| 3 | **行为可解释、可回滚** | ✅ 有 `[parent-agent] skill loaded: <name>` 日志与工具结果首行「已加载场景：…」；每阶段可独立回滚（§6） |
| 4 | **工具块（另一块常驻成本）也瘦下来** | ✅ **全量 37 把下沉**：工具块 **25078 → 15997**（-36%），说明进技能（正文 11491 → 17706），配套**场景守卫**（见 §2.5 / §2.6） |

---

## 1. 执行期不许改变的设计约束（讨论稿已定）

| # | 约束 | 原因（一句话） | 执行情况 |
|---|---|---|---|
| 1 | 按**场景**划分（8 个），不按操作域划分 | 场景 = 家长的一句话 → 一件办完的事 | ✅ `PARENT_SKILLS` 8 个 + 台账映射 34 条 |
| 2 | 入口**由模型自选**（description + 索引），不做界面按钮、不做服务端预路由 | 家长不需要知道"域"存在（按钮方案留作 Plan B） | ✅ 常驻层放 8 行索引，措辞用家长原话 |
| 3 | **会话内绝不切工具集** | 工具数组在缓存前缀头部，改了＝整条前缀失效 | ✅ 只加 `load_skill` 一个常驻工具；测试有断言防回退 |
| 4 | **`read` 与 `load_skill` 必须常驻** | 技能是"读/加载"出来的 | ✅ `read` 原样保留 |
| 5 | 技能正文**随代码发布**（TS 模块），家长覆盖**存库** | 规避打包/资产拷贝；覆盖走现成通道 | ✅ `skills/parent/*.ts` + `agents.sqlite` 的 `skill:<name>` |
| 6 | 口径分三层：**铁律 A / 场景口径 B / 细则模板 C** | 防止"瘦身把铁律瘦没了" | ✅ 7 条铁律 A1–A7，测试逐条核对三处同留 |
| 7 | **重复规则**两处都出现且**物理同源** | 家长两处都会说"每天/每周"，但不许漂移 | ✅ 同一常量 `REPEAT_RULES_BLOCK` 被 plan / automation 引用，测试断言两处 `toContain` 同一常量 |
| 8 | **通用通道退场顺序不可颠倒** | 否则 D1/D3/D5/F2/F3 断供 | ⏸ P4 待做；`parent_db_read` 暂留为兜底桥（已定） |

---

## 2. 关键架构决定（含备选与否决理由）

### 2.1 技能正文放哪里

| 方案 | 结论 | 理由 |
|---|---|---|
| **TS 模块常量**（`server/src/agent/skills/parent/*.ts`） | ✅ **已采用** | 跟代码同版本、无打包/资产拷贝问题（`server` 是 esbuild 打包）；**重复规则可以就是同一个常量被两处引用**，物理上不可能漂移 |
| 真 `.md` 文件 + 构建拷贝到 dist | ❌ 否 | 要动构建脚本与打包清单，`asar` 场景易漏；收益（运维直接改文本）目前用不到 |
| 首次运行播种到 `dataDir` | ❌ 否 | 升级时"内置文本更新"与"家长改过的版本"会打架，得再造一套合并/迁移规则 |

### 2.2 为什么不启用 SDK 自带的 Agent Skills（已核实）

内核 0.84.1 确实有 Agent Skills（`SKILL.md` + `<available_skills>` + 渐进披露），但**不用**它，四条理由：

1. 索引里给的是**绝对路径** `<location>`，而我们的 `read` 拒绝绝对路径、`learning-guard` 又把 FS 限制在会话 cwd ⇒ 模型拿到路径也读不到；
2. 服务端默认 `noSkills: true`，打开它等于启用"默认发现目录"：`~/.pi/agent/skills`、`~/.agents/skills`、以及 **cwd 及其所有祖先目录**的 `.pi/skills`——而 cwd 是**模型自己可写**的地方 ⇒ 模型可以给自己写指令（跨天新会话生效），红线级；
3. 我们要的"家长覆盖层"SDK 不提供，反正要自建注册表；
4. server 精确 `0.84.1`、client `^0.84.1`，少依赖一层 SDK 语义就少一处两端分叉。

⇒ **自建索引 + 自建 `load_skill`**，SDK 保持 `noSkills`（现状），**`packages/agent-core` 一行未改**。

### 2.3 三层文本分层（瘦身的安全边界）

| 层 | 内容 | 写在哪 | 例子 |
|---|---|---|---|
| **A 铁律**（永不离开工具描述） | 做错会坏数据/越权的硬约束 | **工具 description + 常驻层 + 场景正文，三处同留**（每条约 1 句） | 改名只走 `parent_rename_course`（不许 upsert）· 课程内容写入是整课替换 · 不可逆先演练复述 · 认证/账户/密码永不触碰 · 同步只到已分配主题 · 家长工作区 ≠ 资料库 |
| **B 场景口径** | 怎么讲、什么顺序、什么不许讲 | **只在场景技能正文** | 考核解读三段式 · "没分析过 ≠ 学得差" · 先核对现有结构再落库 · 复述收尾 |
| **C 细则模板** | 长模板、语法、字段清单 | **技能内靠后的段落** | 报表 markdown 结构 · 互动页 `path` 语法 · 计划重复规则语义表 |

> **执行纪律**：A 层每条一个 ID（A1–A7），测试里逐条核对"三处是否都在"。

### 2.4 装配与加载的数据流（已实现）

```
建会话（ensureEntry，parent / parent-content）
  └─ buildServerParentPrompt(...)                     ← 常驻层
       = 身份 + 红线(A) + 今天 + 库表速查 + 【场景索引 8 行】+ 工作原则
  └─ customTools += load_skill                        ← 唯一新增工具
  └─ toolNames 其余不变                                ← 不切工具集

家长说一句话
  └─ 模型看索引 → 调 load_skill("parent-scene-progress")
       └─ 注册表：内置（TS 常量） ← DB 覆盖（agents.sqlite: ref="skill:<name>"）优先
       └─ 返回正文（首行「已加载场景：中文名（name）」；会话内幂等；64KB 上限截断）
  └─ 正文以**工具结果**追加到消息尾部 ⇒ 前缀缓存不动、会话历史不动、工具面不动
```

**生效时机**：家长会话**长期累积、不做跨天裁决**，systemPrompt 只在**建会话时**读一次。⇒ 部署后**服务端重启**即对所有会话生效（`entries` 是内存表，重启后重建会话，**历史 jsonl 仍在**）。**不需要**强制重置会话。

### 2.5 试点：工具说明下沉 + 场景守卫（2026-09-25 执行，2 把）

**动机（实测）**：工具块才是常驻上下文的大头——45 把 / **25078 字符**（`description` 10585 + 参数 Schema 14493）≈ 常驻提示词（2501）的 **10 倍**。而 Schema 里 **43%（6279）是散文**（属性说明），本就属于 B/C 层。⇒ 说明**下沉到技能**（按需才进上下文），**只留结构**。

| 层 | 放什么 | 为什么不能动 |
|---|---|---|
| 工具 `description` | **一句"干什么" + 自我指路**（`先 load_skill("…")；未加载时拒绝执行`） | 模型**选工具那一刻**手里还没有技能，所以必须留一句 |
| 参数 Schema **结构** | 字段名 / 类型 / 必填 / 嵌套 / 枚举 | **执行前校验与类型转换的依据**（`agent-loop.js:404` → `pi-ai` `validateToolArguments`）；撤了＝关校验（实测空 schema 连 `{随便:1}` 都放行） |
| 参数说明 / 用法 / 铁律 | **场景技能正文** | 技能是"加载后才进上下文"的，正是它该在的地方 |

**场景守卫（`parent-skills.ts`）**：`createParentSkillState()` 建一份**会话级**状态，`load_skill` 与工具**共用**；工具执行前 `scenarioGuard(state, "parent-scene-…")`——本会话没加载过该场景就**拒绝执行**并回可读提示（先 `load_skill`）。**没有它，"按需"只是建议**：说明搬走后模型会靠猜写数据，而结构校验拦不住语义错误。状态不传＝不拦（脚本/测试直调工具仍可用）。

**主动改动一条已拍板决定**：A 层铁律从"**三处同留**"（工具描述 + 常驻层 + 技能）在**下沉工具上**变为"**常驻层 + 技能 + 守卫**"。测试有专门断言守住这条边界（**全量铺开后对所有下沉工具生效**，见 §2.6）。

**试点数字**：这两把 3572 → **1477**（-59%）；39 把业务工具 `description` 9833 → **8895**；**工具块全量 25078 → 22983**；技能正文 11491 → **12705**（+1214 = 下沉的说明）。常驻提示词仍是 2501。

**全量目标（当场估的口径，后已被实测修正）**：`description` 压成一句 + Schema 只留结构，当时估 45 把 **25078 → 10778**；**实测终点是 15997**（差在"通用设施 9 把 / 4843 字符不下沉"），见 §2.6。

### 2.6 A 路线全量铺开（2026-09-25 执行，37 把）

**机制**（`server/src/agent/parent-tool-compact.ts`，只在注册点 `parent-registry.ts` 的 `customTools` 过一道）：

| 机制 | 作用 |
|---|---|
| `firstSentence(desc)` | 取原描述**第一句**作自述（括号内不切句，>140 字截断）⇒ 源码长描述仍是"说明书真源"（不进上下文），**单一真源、不会漂移**，且**幂等**（试点两把由此重新派生，逐字一致） |
| `stripParamDescriptions(schema)` | **原地**删掉 Schema 里所有 `description`；字段名/类型/必填/嵌套/enum/`anyOf` 一字不动（保留 TypeBox 符号与 `prepareArguments`，ISSUE-134 还原层不受影响） |
| `scenesOfTool(name)` | 工具 → 场景，真源＝各技能 `tools` 清单；**多场景任一加载即放行** |
| `compactParentTool(s)` | 描述 → 一句 + 指路（含 `load_skill` 与"拒绝执行"）；`execute` 外包 `scenarioGuard` |
| `COMPACT_EXEMPT_TOOLS` | **不下沉**：`load_skill`、`parent_db_read`/`parent_db_write`/`parent_db_describe`（跨场景通用通道）、`log_activity`（通用设施）；`read/write/edit/ls` 无场景归属 |

**实测（同一套装配 `description + 参数 Schema`）**

| 项 | 原始 | 试点后 | 全量后 |
|---|---|---|---|
| 工具块（45 把） | 25078 | 23086 | **15997（-36%）** |
| `description` | 10585 | 9702 | **6447** |
| 参数 Schema | 14493 | 13384 | **9550**（结构性 7640 是地板） |
| 下沉工具数 | — | 2 | **37** |
| 未归属/豁免 | — | — | **9 把 / 4843** |
| 8 个技能正文 | 11491 | 12705 | **17706**（各技能新增「## 参数速查（本场景工具）」） |
| 常驻提示词 | 4710 | 2501 | **2501（未变）** |

**守卫覆盖全部下沉工具**（含**只读**工具）：说明搬走后，"没加载场景就查询"会变成"误读数据 + 误报给家长"（例如把 `kind=fixed` 考核的"不列课程"当成数据缺失）。**多场景工具**：`parent_list_children`（progress/plan/child）、`parent_read_image` / `parent_read_upload` / `parent_put_material`（course/materials）、`parent_library_topics` / `parent_library_course_content`（progress/course）。

**为什么通用三把不下沉**：它们是跨场景的通用数据通道（P6 退场候选），语义没有单一场景归属；把它们的说明塞进某个技能等于制造错误归属。评估见 `ISSUE-144` §3.3。

---

## 3. 任务分解与执行情况（P0–P6）

> 规模：S ≈ 半天内，M ≈ 1–2 天，L ≈ 3 天以上。

### P0 基线与对照（不改行为）

| ID | 做什么 | 状态 | 结果 |
|---|---|---|---|
| P0.1 | 量基线：常驻提示词字符数（分段）+ 工具 description 总量 | ✅ | 改前常驻 **4710**（计划域 572 / 资料 429 / 附件 340 / 落库课程 387 / 改名同步 835 / 考核内容 640 / 对话记录 277 / 错题本 508 / 通用查询 300 / 工作原则 185）；工具描述 **10732**（39 把） |
| P0.2 | 20 条验收样本固化成清单 | ✅ | `test/issue144-parent-skills.test.ts` 末尾 `PARENT_SKILL_SAMPLES`（18 条对话 + 2 条负样本），静态自检通过 |
| P0.3 | 定可观测性口径 | ✅ | 日志 `[parent-agent] skill loaded: <name>`；工具结果首行「已加载场景：…」；界面展示留 P3.2 |

### P1 文案层（不动机制）——✅ 已完成

| ID | 做什么 | 状态 | 结果 |
|---|---|---|---|
| P1.1 | `buildServerParentPrompt` 拆成「常驻层」+「移出清单」 | ✅ | 8 个业务域段落全部移出；常驻只留 身份 42 / 上下文 141 / **索引 987** / **铁律 691** / 通用通道兜底 338 / 工作原则 261 |
| P1.2 | 写 8 个场景技能正文 + 索引；重复规则抽共享常量 | ✅ | `server/src/agent/skills/parent/{shared,progress,course,plan,automation,materials,child,points,config,index}.ts`；正文合计 **11491** 字符 |
| P1.3 | 工具 description 瘦身（铁律保留） | ✅（**有意收敛**） | 只瘦 `parent_exam_plan_create`（1275 → ~470，把"说法对照表"移进 plan 技能）与 `parent_sync_courses_to_child` 的"何时调用"；总量 10732 → **9833**。**结构性规格与铁律一律不瘦**（理由见下） |
| P1.4 | 改后预算表 | ✅ | 见 §0 判据 1 与上表 |

> **为什么工具描述只瘦这一点**：39 把工具的描述里绝大部分是**参数语义 + 铁律 + 参数化约束**（`methodSpec` 怎么传、执行会话白名单…），删了会让"没加载技能"的模型直接做错；真正冗余的只有 `exam_plan_create` 那一类"把参数说明再复述一遍 + 说法对照表"。测试里加了总量上限（10500）与单把上限（≤800）防回涨。

### P2 机制最小可用 ——✅ 已完成（一次到位，未做"先只上 progress"的中间态）

| ID | 做什么 | 状态 | 结果 |
|---|---|---|---|
| P2.1 | 技能注册表：类型 / 内置表 / 索引生成 / 覆盖合并 / 未知处理 | ✅ | `skills/parent/{shared,index}.ts`（`PARENT_SKILLS` / `PARENT_SKILL_COVERAGE` / `UI_ONLY_SCENARIOS` / `visibleParentSkills` / `buildSkillIndexBlock`） |
| P2.2 | `load_skill(name)` 工具 | ✅ | `parent-skills.ts`：白名单、64KB 上限截断、**会话内幂等**、未知技能报可读错并列出可用项、加载日志 |
| P2.3 | 装配（toolNames + customTools + 索引注入） | ✅ | `parent-registry.ts`：`parent` / `parent-content` 共用；`parent-data`（待退场）不给 |
| P2.4 | 覆盖层读取 | ✅ | `db/agents.ts` 新增 `listAgentPrompts`；`resolveParentSkill` **DB 优先、内置兜底**；清空覆盖＝回落内置（沿用 `saveAgentPrompt` 语义） |
| P2.5 | **缓存回归断言** | ✅ | 测试：常驻提示词构建结果与会话内是否加载技能无关；`load_skill` 只在 `toolNames` 出现一次 |
| P2.6 | 只放行 `progress`（灰度） | ⏭ **跳过** | 灰度是为"边看效果边放开"；本轮 8 个一次可见（`visible?: boolean` 开关保留，随时可缩） |

### P3 全量上线 + 观测 ——✅ 代码完成；界面展示留后续

| ID | 做什么 | 状态 | 结果 |
|---|---|---|---|
| P3.1 | 8 个技能全量可见；`parent_db_read` 降级为兜底 | ✅ | 常驻层「通用数据查询（**兜底通道，计划退场**）」段；各技能里明确"现状走兜底、专用工具到位后优先用" |
| P3.2 | 观测：日志 + 界面显示 | ✅ 日志 / ⏸ 界面 | 日志与工具结果首行已可确认"读了哪个技能"；前端「已进入：{场景名}」需改 `src/components/ParentChatPanel.tsx` 并重建 Web 产物，单独排 |
| P3.3 | 跑满 20 条样本 | ⏸ | 需有模型配置的会话，人工跑（清单已就绪） |

### P4 退场前置：三个场景专用读工具 ——✅ 已做（2026-09-25）

`parent_child_exam_report`（考核：最近几场 + 逐题 + 每课概要 + 知识点档位）/ `parent_child_mastery_report`（掌握与薄弱：主题 / 单课 / 知识点档位 / 错题本）/ `parent_child_points_report`（积分与兑换：余额 + 规则 + 逐日结算 + 流水 + 未发放兑换）。

| ID | 做什么 | 状态 | 结果 |
|---|---|---|---|
| P4.1 | 三个业务语言只读工具（D1 / D5+D3 / F3+F2） | ✅ | `server/src/agent/parent-child-report-tools.ts`：取数范围写死在代码里、不接受表名/列名/SQL；**未考完的场次只给范围不给题**；不吐内部标识 |
| P4.2 | 对象定位（`child` 姓名 + 归属校验） | ✅ | 复用 `resolveConvoChild`（导出）；名下唯一孩子可省略，**多孩子必须问清**，找不到报可选名字——绝不默认挑一个 |
| P4.3 | 共用零件（口径只写一处） | ✅ | 新增 `server/src/agent/report-utils.ts`（档位解析与默认档位 / 中文字典 / 日期与百分比），`child-report-tools.ts` 改为复用 |
| P4.4 | 技能侧同步（工具→场景单一真源 + 能力清单 + 参数速查） | ✅ | `skills/parent/progress.ts` / `points.ts`：`tools` 补齐；「数据从哪来」改成**哪个问题用哪个工具**（不再列原始表名）；账本口径按**核实后的机制**重写 |
| P4.5 | 装配 | ✅ | `parent-registry.ts`：`customTools` 与 `toolNames` **同时** +3；仍走注册点的 `compactParentTools`（说明下沉 + 场景守卫） |
| P4.6 | 回归 | ✅ | `test/issue144-parent-report-tools.test.ts` **18/18**；`issue144-parent-skills` **31/31**；`issue142-child-report-tools` **14/14**（孩子端行为不变）；`tsc` 0 错；全量零新增失败 |

**⚠️ 顺带核出两处"文档 ≠ 实现"（已按实现改写，待家长拍板）**：
① 积分口径——**家长组按率直接评档**（可能真扣分）、**门槛只卡"孩子自排的加分项"**（门槛＝当天家长组达标）、**次日日终结算**（当天那行只是实时进度），台账/旧技能里"必须完成项达标才解锁加分 / 每天结算一次"是简写；
② 兑换扣分时点——实现是**提交那一刻就扣分**并写 `type='redeem'` 流水（`routes/plans-rewards.ts:659-708`），而 F2 记的产品口径是"家长履约时扣"；该接口目前**没有界面调用方**、`fulfilled_at` 也没有写入方。详见 `ISSUE-144` §3.5。

### P5 家长自定义层（可后置）——⏸ 通道已通、界面未做

- ✅ **存储与读取已通**：`scope=parent` + `ref=skill:<name>`，覆盖优先、清空回落、截断、非 `skill:` 前缀不混入（均有测试）。
- ⏸ 待做：`server/src/routes/db.ts` 放开 `scope=parent` 下的 `skill:*` ref（**现在会把 ref 强制成 `parentId`**：`db.ts:329/340/766/777`）、设置页编辑框（复用 50 版历史）、红线关键词校验。

### P7 工具块瘦身（说明下沉 + 场景守卫）——✅ 已做 2 把试点；全量待定

| ID | 做什么 | 状态 | 结果 |
|---|---|---|---|
| P7.1 | 量清工具块真实占用 | ✅ | 45 把 / **25078 字符**（`description` 10585 + Schema 14493，其中 Schema 散文 6279 = 43%） |
| P7.2 | 两把最肥工具：描述压成一句 + schema 只留结构 + 说明归入 course / plan 技能 | ✅ | 3572 → **1477**（-59%）；工具块全量 25078 → **22983** |
| P7.3 | **场景守卫**（未加载所属场景 → 拒绝执行 + 指路 `load_skill`） | ✅ | `createParentSkillState` / `scenarioGuard`，会话级状态由 `load_skill` 与工具共用；不传状态＝不拦 |
| P7.4 | 全量铺开（A 路线） | ✅ | **37 把**下沉（余 9 把属通用设施，见 §2.6）；工具块 **23086 → 15997**（相对原始 25078 **-36%**）；技能正文 12705 → **17706**（新增各技能「参数速查」）；**机制化**：`parent-tool-compact.ts` 在注册点统一压（不再逐把手改） |

### P6 通用通道退场 ——⏸ 独立 ISSUE
删 `parent-data` 会话分支、`buildServerDataAgentPrompt`、`DATA_AGENT_TOOL_NAMES`、`createDataAgentTools`、侧栏「🗃️ 数据管理」入口与视图，并把该会话历史归档。**前置已清（P4 ✅）**；剩下触发条件＝上线观察一个版本周期无断供。⚠️ 退场时要同步删掉 `progress` 技能里那两句"兜底通道"话术与 `parent_db_read` 的声明（否则提示词会教模型调不存在的工具）。

---

## 4. 验收体系（三层）

| 层 | 判据 | 方法 | 结果 |
|---|---|---|---|
| **单元 / 预算** | 索引正确 / 覆盖优先 / 未知技能可读错 / 幂等 / 上限 / A 层"常驻层 + 技能 + 守卫" / 重复规则同源 / **systemPrompt 与 toolNames 不因加载技能而变** / 预算上限 / **逐把 schema 无说明但结构完整** / **工具→场景覆盖完整** / **守卫拒跑与放行（含只读与多场景）** | `npx vitest run test/issue144-parent-skills.test.ts` | ✅ **31/31 通过**（P7.4 全量铺开 + P4 后） |
| **单元（P4 报告工具）** | 三把工具的输出契约 / **未考完不给题** / 内部字段不泄漏 / 对象定位（唯一孩子可省略·多孩子必须问清·越权读不到）/ 场景归属 | `npx vitest run test/issue144-parent-report-tools.test.ts` | ✅ **18/18 通过**（P4） |
| **类型** | 服务端编译 | `cd server && npx tsc --noEmit` | ✅ **exit 0，0 错** |
| **行为** | 20 条样本（含跨场景连读、重复规则一致性、2 负样本）+ **P4 三把报告工具的触发是否准确** | 有模型配置的会话里人工跑（清单见 P0.2） | ⏸ 待人工 |
| **全量回归** | 不破坏既有测试 | `npx vitest run` | 见 §7 证据回填 |

---

## 5. 风险与对策（含本轮实际发生的两条）

| # | 风险 | 触发信号 | 对策 | 回滚 | 实际 |
|---|---|---|---|---|---|
| 1 | **模型不读技能**，凭常识答 | 样本命中率低、答案泛泛 | 索引措辞按家长原话写；A 层铁律仍在工具描述里；"读了哪个技能"可见 | 技能全隐藏（回到纯常驻） | 待人工跑样本验证 |
| 2 | **口径漂移**（重复规则两处） | 第 18 条样本失败 | 两处引用**同一常量**；测试断言两处 `toContain` 同一常量 | 改回单处 | ✅ 已用同源常量消除 |
| 3 | **瘦身把铁律瘦没了** | 出现"用 upsert 改名"类行为 | A 层 ID 核对表 + 测试逐条断言（含构造真实工具读 description） | revert P1.3 | ✅ 测试覆盖 |
| 4 | **缓存退化** | 首 token 变慢、input 全价 | 会话内不改工具集/不改 prompt（测试断言）；P5 若选"立即生效"要明确代价 | 关闭覆盖层 | ✅ 断言就位 |
| 5 | **老会话行为不一致** | 同一句在两个家长处表现不同 | 明确"部署 + 服务端重启后新建会话即生效，历史不丢" | — | 已写进 §2.4 |
| 6 | **路由 ref 被强制成 parentId** | P5 存 `skill:*` 失败 / 串到家长级提示词 | P5.1 显式放开 + 单测 | 自定义层后置 | ⏸ P5 |
| 7 | **打包/部署** | 线上找不到技能文件 | 选 TS 模块规避 | — | ✅ 规避 |
| 8 | **家长看不到"读了什么"** | 出问题无法归因 | 日志 + 工具结果首行 | — | ✅ 已可观测（界面待 P3.2） |
| 9 | **本机 shell 的文本回写会破坏 UTF-8**（本轮实际发生） | 文件里出现 `锛?`、U+FFFD，且换行被吞 | **不要用 `Get-Content`/`Set-Content` 做中文文件的原位替换**（该 shell 用 GBK 解码 UTF-8）；改用 `edit`/`write` 工具或 .NET 显式 UTF-8 | 从 `git HEAD` 恢复 + 用安全工具重放改动 | ⚠️ 已发生并已修复（详见 `ISSUE-144` §七） |
| 10 | **说明下沉后"没读说明书就靠猜写数据"**（P7 新增风险） | 参数语义/红线写错、静默写坏数据（结构校验拦不住语义错）；只读工具则是**误读后误报给家长** | **场景守卫**：未加载所属场景 → 拒绝执行 + 指路 `load_skill`（**含只读工具**；多场景工具任一加载即放行）；测试覆盖拒跑与放行 | 把说明搬回工具 `description`（单把即可回滚）；或在注册点去掉 `compactParentTools` 一层 | ✅ 已消除（37 把全覆盖） |
| 11 | **A 层第三处（工具描述）撤掉后口径丢失** | 出现"用 upsert 改名"类行为 | 铁律仍在**常驻层 + 技能**（测试逐条断言）；**守卫**保证执行前必定读过技能 | 同上 | ✅ 测试守住边界（全量生效） |
| 12 | **`items` 字符串化率上升**（schema 提示移走后） | `items.0: must be object` 类报错变多 | 该提示已进 course 技能；`string` 兜底分支 + `tool-kit` 还原层仍在（ISSUE-133/134） | 把提示写回 schema（局部） | ⏸ 待真实会话观察 |
| 13 | **守卫多一次往返**（P7.4 新增） | 工具返回"请先 load_skill"，多一轮 | ① 索引与工具描述都写了"先加载"；② 拒跑提示自带确切技能名；③ 只对**本次会话未加载**的场景生效（加载过就不再拦） | 把可疑工具加进 `COMPACT_EXEMPT_TOOLS` | ⏸ 行为层样本要盯"命中率 / 自愈率" |
| 14 | **工具→场景误判**（多场景工具给少了） | 合法场景下被拒跑 | 单一真源＝技能 `tools` 清单；测试断言"每个下沉工具都有归属、每个声明工具都存在" | 在对应技能 `tools` 里增删一项 | ✅ 覆盖完整（测试） |
| 15 | **P4 报告工具与通用通道并存的"过渡期归属"** | 模型仍习惯去 `parent_db_read` 拉表；或反过来用报告工具做它不该做的事（改数据） | 技能里写**优先级**（专用报告优先、`parent_db_read` 仅兜底）；三把工具**全只读**、无写入口；样本里 D1/D5/F3 三条已改指新工具 | 把技能里那两句兜底话术删掉（等价于提前 P6） | ✅ 静态就位（`ISSUE-144` §3.5） |
| 16 | **报告工具把"没数据"讲成"学得差"** | 家长误判孩子状态（D5 最怕这个） | 工具输出显式区分**"还没分析过"**与"需要复习"；技能口径写死"空 ≠ 差"；测试断言原话到场 | 口径回到技能文案层 | ✅ 测试覆盖 |
| 17 | **兑换口径与实现不一致**（本轮核出） | 模型对孩子/家长说"还没扣分"，而流水里已经扣了 | 按**实现**改写口径（提交即扣、未发放状态），并在台账标"待拍板"；工具输出里直接注明 | 改文案即可 | ⚠️ 待家长拍板（`ISSUE-144` §3.5 表 2） |

---

## 6. 分批与顺序（每批都能独立交付、独立回滚）

| 批次 | 内容 | 交付价值 | 状态 |
|---|---|---|---|
| **第 1 批** | P0 + P1 | **先回答"是不是真臃肿、口径齐不齐"**（不动机制，纯文本） | ✅ 完成（-47%） |
| **第 2 批** | P2 + P3 | 按需加载真正跑起来，家长沟通自由 | ✅ 代码完成（行为样本待人工） |
| **第 3 批** | P4 | 让"考得怎么样/哪里薄弱/积分怎么算"讲得清，并为退场铺路 | ⏸ 待做（建议下一批） |
| **第 4 批** | P5（自定义层）→ P6（退场，独立 ISSUE） | 家长可改口径；通用通道下线 | ⏸ 待做 |
| **第 5 批** | P7（工具块瘦身：说明下沉 + 场景守卫） | 常驻上下文再降一截：**实测 25078 → 15997（-36%）** | ✅ 完成（37 把全量下沉；通用设施 9 把不下沉，见 §2.6） |

**回滚手段**：第 1 批 `git revert`（纯文本）；第 2 批把技能可见性全置 `visible:false`（索引空、`load_skill` 无用武之地，行为退回纯常驻）；第 3 批新工具保留即可（旧通道不动）；第 4 批删 DB 覆盖行 / 单独回滚 P6；**第 5 批**：整体回滚＝注册点去掉 `compactParentTools` 一层（工具数组与源码里的完整说明立即回到上下文），**逐把回滚**＝把该工具名加进 `COMPACT_EXEMPT_TOOLS`；守卫可用"不传 `skillState`"关掉。**注意**：改动只在**新建会话**生效（已在跑的会话保留旧工具面，避免中途换头）。

---

## 7. 执行前拍板的三件（已按建议执行）

| # | 事项 | 决定 |
|---|---|---|
| 1 | 技能正文载体 | ✅ **TS 模块常量**（无打包风险、重复规则可物理同源） |
| 2 | 改动生效方式 | ✅ **部署 + 服务端重启即生效**（历史 jsonl 不丢，不强制重置会话） |
| 3 | `load_skill` 中文别名 | ✅ **不做**（模型自选前提下用处不大；未知技能会报可读错并列出可用项） |

---

## 附：可观测性约定

- **日志**：`[parent-agent] skill loaded: <name>`（家长覆盖版会标 `(家长覆盖版)`）。
- **工具结果**：`load_skill` 返回文本首行固定为 `已加载场景：<中文场景名>（<name>）`；家长覆盖过的会追加一行提示。
- **界面**（P3.2 待做）：在会话面板显示"已进入：{中文场景名}"（只读工具事件，不改数据流）。
- **验收记录**：20 条样本每次跑完留一份"命中 / 未命中 + 口径偏差"记录，作为下一轮改 description 的输入。
