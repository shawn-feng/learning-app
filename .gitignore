node_modules/
out/
dist/
dist2/
dist3/
data/
cloud-service/database/
cloud-service/storage/
cloud-service/__pycache__/
cloud-service/app/__pycache__/
cloud-service/*.log
cloud-service/server*.log
*.db
.pi/
aliyun-aksk.txt
tencent-aksk.txt
# SSECP/声希口语评测凭证（明文密钥，不入库）
ssapi.txt
data/assessment-config.json
.npm-cache/
electron.vite.config.*.mjs
学习技能和资料/
# 网络抓包排查文件（含真实流量，不入库）
pi-app-wireshark*.txt

# Python 依赖缓存（benefit-auth）
benefit-auth/.deps/

# 数据迁移备份（不入库）
data-backup-*/

# 旧架构产物归档目录（2026-08-28：dist/cloud-service/benefit-auth 等历史产物，不入库）
legacy/

# WorkBuddy 临时文件
.workbuddy/tmp/

# 一次性调试/迁移脚本（2026-08-28 排查与迁移用，不入库）
scripts/probe-electron.js
scripts/test-render-iframe.js
scripts/render-test-app/
scripts/migrate-old-data.py
dist4/
dist-release/
dist-mac/
dist-ubuntu/

# 第三方平台密钥（切勿提交）
douyin-client.txt
# SSH 或连接元数据（含主机指纹，不入库）
.known_hosts
dist-release-0*/
dist-ubuntu-0*/
dist-mac-0*/

# 2026-09-10 计划域重构新增：
# 服务端 tsc 产物（不入仓，CI 跑 tsc/build 时再产出）
server/dist/
# 前端调试/旧 renderer 临时构建产物
src/out/
# 一次性冒烟/迁移脚本（不入仓，验证用）
server/tmp-*.mts
# 顶层调试脚本与临时目录（排查用）
tmp/
tmp-*.py
scripts-decode-token.mts
scripts-probe-material.mts
# ASAR 调试产物 / 临时 zip
.tmp-asar/
linux-*.zip
# 离线调研目录与抓包文件（不入仓）
lunyu_exam/
voice_*.csv
