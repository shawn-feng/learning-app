# M7 端到端验证清单（客户端 + 服务端拆分）

> 目的：验收 SPLIT 拆分全链路（M1–M6 + M8-A~E 已实现），需真实家长账号与实际运行环境。
> 执行方式：按 A→I 顺序逐项执行，勾选 ☐，记录实际结果与差异。

---

## A. 前置准备

- ☐ **A1 启动服务端**：`cd server && npm run dev`（或运行 `dist/learning-server.exe`）
  - 预期：监听 8788，浏览器/接口访问 `GET http://127.0.0.1:8788/api/v1/health` 返回 `{"ok":true,...,"db":"ok"}`
- ☐ **A2 确认服务端地址**：本机 `http://127.0.0.1:8788`；局域网用 `http://<主机IP>:8788`
- ☐ **A3 启动客户端**：`npm run dev`（electron）→ 应显示登录页（含「服务端地址」配置区）

## B. 认证链路（客户端 → 服务端 → 公网 www）

- ☐ **B1 登录页配置服务端地址**：填入 A2 地址 → 点「保存」→ 提示消失
- ☐ **B2 未配置地址点登录** → 提示「请先填写服务端地址」（不应再直连 www）
- ☐ **B3 真实家长账号登录** → 成功进入家长页（服务端转发公网认证，返回 session token）
- ☐ **B4 错误密码登录** → 显示公网返回的错误（如 `Invalid email or password`）
- ☐ **B5 授权信息**：家长页可见套餐/有效期（服务端 `/auth/license` 返回）

## C. 数据读写 RPC（孩子）

- ☐ **C1 创建孩子** → 成功；服务端主库 `server.sqlite` 的 `children` 表新增一行
- ☐ **C2 进入孩子模式** → 会话正常创建；若家长编辑过该孩子 AGENTS，**首次会话**即使用服务端版本（已修：会话创建前远程预取）
- ☐ **C3 学习对话触发写入** → 对话中产生 kb_insert/kb_update → 服务端 `data/kb/<parentId>/<childId>.sqlite` 出现数据（不再写客户端本地 kb.sqlite）
- ☐ **C4 家长页查看学习进度** → 展示孩子学习记录/进度（读服务端）

## D. 配置 2 分钟轮询

- ☐ **D1 修改配置**（设置页改默认模型/资料保留数量）→ 保存成功；服务端 `settings` 表更新、revision +1
- ☐ **D2 另一设备生效**：另一台客户端（或本机重启后）≤2 分钟内拉到新配置（app-settings.json 被轮询写回）

## E. Materials 按需缓存

- ☐ **E1 首次打开课程资料** → html 正常渲染（按需从服务端拉取到 `data/parents/<pid>/materials/`）
- ☐ **E2 服务端更新资料后再次打开** → 显示新版（后台版本比对重拉）
- ☐ **E3 断网浏览已缓存资料** → 可正常查看（读不联网）
- ☐ **E4 断网打开未缓存资料** → 显示红色错误条「资料获取失败：{error}」（不再静默降级）

## F. 大文件跨设备

- ☐ **F1 上传录音/图片** → 成功；服务端 `data/files/<parentId>/` 出现文件
- ☐ **F2 另一设备下载** → 内容一致

## G. 断网 UX

- ☐ **G1 停掉服务端后聊天** → 发送显式报错（⚠️ 错误提示），不静默回退
- ☐ **G2 停掉服务端后设置保存** → 本机文件已保存（本地优先），同步失败静默（可接受）
- ☐ **G3 断网时已缓存资料可浏览**（同 E3）

## H. 版本协商

- ☐ **H1 客户端启动** → 调用 `/version`；正常版本组合无提示（版本不兼容时才提示升级）

## I. 服务端部署形态

- ☐ **I1 Windows 安装包**（可选，需 makensis+nssm 环境）：编译 `installer.nsi` → 安装 → 服务 `LearningServer` 注册成功、开机自启、崩溃自动重启；客户端连本机地址可用
- ☐ **I2 Linux systemd**（可选）：部署 `learning-server` + unit → `systemctl status` active；`SERVER_DATA_DIR` 数据目录隔离生效
- ☐ **I3 多设备同时在线**：两台客户端连同一服务端 → 同时使用互不阻塞；A 设备孩子学习产生记录，B 设备家长页可见

---

## 已知限制（验收时确认接受）

1. 配置为**文件级粒度**：多设备同时改同一配置（如 app_settings）后写覆盖，后保存者生效。
2. child AGENTS 在**服务端不可达**时首次会话回退本地缓存/代码默认（离线降级）。
3. 存量本地数据不迁移（D13）；会话 jsonl 留在客户端本地（换设备即新会话）。
