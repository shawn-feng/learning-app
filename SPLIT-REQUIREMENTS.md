# 客户端 + 服务端拆分需求（v0.3）

> 状态：**需求已收敛**（2026-08-27 两轮需求讨论拍板），待进入设计阶段
> 范围：学习伙伴 App 演进为「客户端 + 服务端」双组件架构
> 关联：独立于 cloud-service（www.aixuexihao.top）、pi-web；仅复用 benefit-auth 认证中台；不依赖现有 10 阶段开发进度

---

## 1. 背景与目标

现状：学习伙伴为单机 Electron 应用，数据全在本地磁盘。

目标：演进为「客户端 + 服务端」双组件架构。**核心目标是多设备共享数据**——一台服务端承载同一份数据，多台客户端设备（家长电脑、孩子平板等）访问同一份数据，授权管控不是本次主要矛盾。

关键约束（用户拍板）：

| 约束 | 说明 |
|------|------|
| 双独立安装包 | 客户端包与服务端包分离，版本各自升级，降低在线升级复杂度 |
| 服务端可云可本地 | 部署于云上 ECS 或家庭局域网主机；运行时为无 UI 常驻服务 |
| 认证复用中台 | 家长账号经 benefit-auth（auth.aixuexihao.top）公网认证，取授权与套餐 |
| 必须在线（写操作） | 数据库读写每次联网；断网显式提示不可用 |
| 资料可离线浏览 | 已缓存的学习资料断网可看（读不联网），写操作必须在线 |

---

## 2. 总体形态

- **客户端安装包**：家长界面 + 孩子界面（与当前 App 一致）+ AI Agent（本地调用）+ 本地会话与缓存。
- **服务端安装包**：数据库 + 大文件（磁盘）+ Materials（内容 + 最新时间戳）+ 家长认证代理。无 UI 常驻服务。
- 客户端可配置所连服务端地址；**同一台机器可同时安装两个包**，实现「自用模式」（装服务端的机器也能打开客户端界面直接用）。

```
┌─ 客户端安装包 ─────────────┐   ┌─ 服务端安装包 ─────────────┐
│ 家长界面 + 孩子界面          │   │ 数据库（记录/账号/配置/知识库）│
│ AI Agent + 会话 jsonl(本地)  │◄─►│ Materials + 大文件(磁盘)    │
│ 本地缓存（配置轮询+资料缓存） │   │ 家长认证·授权（对接公网）     │
└───────────────────────────┘   └───────────────────────────┘
  配置变更轮询(2min) / 数据库读写 / 资料拉取缓存
```

---

## 3. 关键决策记录（D1–D15）

| # | 决策 | 说明 |
|---|------|------|
| D1 | 核心目标 = 多设备共享数据 | 授权管控不是本次主要矛盾 |
| D2 | 必须在线（写操作） | 数据库读写每次联网；断网明确提示不可用 |
| D3 | 已缓存资料断网可离线浏览 | 读不联网；聊天/AI/记录写入必须在线 |
| D4 | 双独立安装包 | 客户端包、服务端包分离，版本独立升级 |
| D5 | 复用 benefit-auth | 认证链路：客户端 → 服务端 → auth.aixuexihao.top |
| D6 | 多端同时在线 | 孩子端学习 + 家长端管理互不阻塞 |
| D7 | 会话接续本期不做 | 换设备即新会话；记录与学习数据跨设备同步 |
| D8 | 服务端机器可自用 | 同一台机器同时装客户端 + 服务端两个包 |
| D9 | 会话 jsonl 留在客户端本地 | agent 会话本地读写，不上服务端 |
| D10 | 大文件存服务端磁盘 | 录音/图片/视频；不做对象存储 |
| D11 | Materials 版本 = 最新时间戳 | 客户端比对决定是否刷新缓存；不做版本切换/回滚 |
| D12 | 孩子不单独公网认证 | 家长创建孩子、数据在服务端、跟随家长授权 |
| D13 | 本期不做数据迁移 | 存量本地数据不动 |
| D14 | 独立于 cloud-service / pi-web | 三条线互不隶属、互不依赖 |
| D15 | 家长端不翻原始会话 | 只看服务端学习记录；预留定时推送能力 |

---

## 4. 客户端需求

### C1 界面
- 家长界面 + 孩子界面，与当前 App 一致（交互、视觉不因拆分改变）。

### C2 AI Agent 调用
- Agent 在**客户端本地调用**（模型 API 请求发生在客户端，如 DeepSeek）。
- 模型 / provider 参数由服务端下发，缓存到本地使用。
- 客户端**每 2 分钟**向服务端拉取配置变更事件，更新本地缓存。
- 2 分钟延迟为**产品选择**：跨设备配置软实时同步——家长在 A 设备改配置，B 设备最多 2 分钟后生效。

### C3 会话（jsonl）
- 会话 jsonl 在**客户端本地读写，不上服务端**（D9）。
- 换设备即新会话（D7，不做会话接续）。
- 预留：后续可增加功能，定时将会话内容推送到服务端数据库（见 §9）。

### C4 数据库操作
- 每次读写（学习记录、知识库、AGENTS、家长配置、孩子账号、scheduler 等）连接服务端执行。
- 本地不再有业务数据库，仅剩缓存目录（配置缓存、资料缓存、连接配置）。
- 网络失败**显式报错**，禁止静默降级。

### C5 Materials
- 从服务端获取内容并缓存到本地。
- 与服务端比对**最新更新时间戳**，决定是否刷新本地缓存（D11）。
- 已缓存资料断网可离线浏览（D3）。

### C6 在线要求
- 写操作（聊天 / AI / 记录写入）必须在线；断网时明确提示不可用。
- 已缓存资料可离线浏览（读不联网）。

### C7 多端与设备
- 多端同时在线：孩子端学习与家长端管理互不阻塞（D6）。
- 家长端查看孩子学习情况**仅依赖服务端学习记录**，不翻原始会话（D15）。
- 客户端可配置所连服务端地址；可同时安装客户端 + 服务端包实现自用（D8）。

---

## 5. 服务端需求

### S1 数据库
- 唯一真源：学习记录、知识库、AGENTS、家长配置、孩子账号、scheduler。
- 多客户端并发读写；**childId 隔离在服务端维持**。
- 会话 jsonl 不在服务端（D9）。

### S2 大文件
- 录音 / 图片 / 视频等大文件存**服务端磁盘**（D10），不做对象存储。

### S3 Materials
- 维护资料内容与**最新更新时间戳**（D11）。
- 供客户端比对 / 拉取缓存；不做版本切换 / 回滚。

### S4 认证与授权
- 家长账号经 benefit-auth 公网认证（客户端 → 服务端 → auth.aixuexihao.top），取使用授权与套餐（D5）。
- 孩子由家长创建、数据在服务端、跟随家长设备授权，**不单独公网认证**（D12）。

### S5 部署形态
- 无 UI 常驻服务；可部署云上 ECS 或家庭局域网主机。
- 独立于 cloud-service（www.aixuexihao.top）与 pi-web，互不隶属（D14）。

---

## 6. 数据边界

| 归属 | 内容 | 读写方式 |
|------|------|---------|
| 服务端 | 业务数据库（学习记录 / 知识库 / AGENTS / 家长配置 / 孩子账号 / scheduler） | 客户端每次读写联网 |
| 服务端 | Materials（内容 + 最新时间戳） | 客户端比对时间戳后拉取 / 缓存 |
| 服务端 | 大文件（录音 / 图片 / 视频，磁盘） | 上传 / 下载 |
| 客户端本地 | 会话 jsonl | agent 运行时本地读写，不上服务端 |
| 客户端本地 | 配置缓存、materials 缓存、服务端连接配置 | 2 分钟轮询更新配置；资料按需拉取 |

**分层硬边界**：会话 jsonl 永远不上服务端；学习记录永远在服务端（家长端跨设备可见性的唯一通道）。

---

## 7. 认证与授权（需求层）

- **家长登录**：客户端 → 服务端 → benefit-auth → 返回授权与套餐。
- **孩子使用**：由家长创建账号（数据在服务端），使用时跟随家长授权，不单独认证。
- **套餐 / 授权管控内容**：待设计阶段细化（如 AI 调用配额、并发设备数等）。

---

## 8. 兼容与升级

- 客户端与服务端版本独立升级（D4）。
- 需**版本兼容约定**：API 带版本号 + 服务端声明最低支持的客户端版本；不兼容时客户端明确提示升级（具体机制设计阶段定）。

---

## 9. 预留能力

- 客户端**定时推送会话内容到服务端数据库**（本期不做；届时家长端可翻查原始对话）。

---

## 10. 本期不做（明确排除）

- 跨设备会话接续（D7）。
- 存量数据迁移（D13）。
- Materials 多版本切换 / 回滚（D11，仅时间戳比对）。
- 对象存储 / CDN（D10，磁盘即可）。
- 与 cloud-service / pi-web 的任何打通（D14）。
- 孩子端独立公网认证（D12）。
