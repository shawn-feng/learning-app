# 英语"学习+场景合一"课程方案（Design）

- 日期：2026-09-10
- 状态：需求已拍板（用户 2026-09-10 四选全按推荐）、方案待确认后实施
- 范围：**仅 English topic 内 wowenglish 剧集课重组**；不动会话/评测/子会话等已落地基建（ENGLISH-AGENT-REQUIREMENTS.md §1-§8 继续生效）

## 1. 需求（用户拍板口径）

把「英语学习」与「场景英语」合一：**一门课 = 一个真实场景**，课内先学后练、一次进入完成闭环；学习资料合并为单份 HTML；整体作为 english 主题下的普通课程走既有体系（分配/排计划/进度/daily/独立子会话全部复用）。

已拍板四项：
1. **场景来源 = 复用 wowenglish 单元**：9 大单元（hello/colour/garden/toys/body/food/bugs/feelings/clothes）各合为一门「场景课」，不做新场景。
2. **合一课程形态 = 一课两阶段单页**：一次进入 → Stage1 学（场景词汇/表达+示范）→ Stage2 练（同场景英语对话）；学习资料合并成一页 HTML。
3. **练习形态 = 场景角色对话**：agent 在场景中扮演角色（如 food 课扮店员）用英文带孩子对话、自由表达、自然纠错。
4. **与现有课关系 = 重排成场景课**：12-44 的 33 门单集课按单元合并为 9 门场景课；01-11 基础课、45-51 语法课保留原样。

## 2. 现状盘点（已核实）

english 资料 = 51 门课目录（`server/data/materials/<pid>/english/`）：
- 01-11 基础课（无 teach-data，`learn/` 共享知识页：phonetics/phonics/grammar/vocabulary）
- 12-44 yellow wowenglish 剧集课（**33 门**，每门 = 1 集：剧集视频 lesson-XXX.mp4 + 逐句字幕 index.html + teach-data.js）
- 45-51 语法模块课
- teach-data.js：`mastered`（本集词→IPA）、`lines`（逐句台词 + 逐词 IPA + modules 句式）

### 合课映射（33 集 → 9 场景课）

| 场景课 | 原单元目录 | 合并集数 | 媒体(lesson-) | 汇总词数 | 汇总台词行 |
|---|---|---|---|---|---|
| Hello（问候） | 12-14 | 3 | 012/013/014 | 26 | 62 |
| Colour（颜色） | 15-16 | 2 | 015/016 | 18 | 57 |
| Garden（花园） | 17-20 | 4 | 017-020 | 28 | 107 |
| Toys（玩具） | 21-24 | 4 | 021-024 | 25 | 97 |
| Body（身体） | 25-28 | 4 | 025-028 | 27 | 100 |
| Food（食物） | 29-32 | 4 | 029-032 | 34 | 120 |
| Bugs（虫子） | 33-36 | 4 | 033-036 | 25 | 105 |
| Feelings（情绪） | 37-40 | 4 | 037-040 | 22 | 100 |
| Clothes（衣服） | 41-44 | 4 | 041-044 | 26 | 108 |

保留不合并：01-11（11 门）+ 45-51（7 门）= 18 门。重排后 english topic 课程 = **9 场景课 + 18 基础/语法课 = 27 门**。

## 3. 合一课程设计

### 3.1 课程内部流程（一次进入，串行两阶段）

1. **进入**：家长排计划 / 举手标记 → english 子会话（复用已落地机制），prompt 注入场景课教法。
2. **Stage1 · 学**：agent 全英文开场 → `display_content` 展示该场景单页 HTML → 带孩子过"场景词卡 + 关键表达"（可点读发音）→ 播示范剧集视频、逐句点读跟读。目标：理解并会说该场景词汇/表达。
3. **Stage2 · 练（场景角色对话）**：agent 切换角色模式（prompt 明示"现在你是<角色>"，如 food 课=甜品店员）→ 在该场景带孩子对话（孩子自由表达，agent 英文引导、自然纠错、控制用词在已学范围）→ 结尾视掌握度挑 1-2 个重点句做发音测评（复用已落地发音测评）。
4. **收尾**：自然结束 → 对话进 daily → 上云家长可回顾（全部复用既有机制）。

### 3.2 单页资料 HTML（每场景课一份）

`english/scene/Unit<N>-<name>.html`（沿用 wowenglish.css 视觉），单页两区：

- **学习区（上）**：
  - ① 场景词卡：单元内各集 `mastered` 汇总**去重**（词 + IPA + 🔊发音）
  - ② 关键表达/句式：从 `lines` 各集 modules + 高频句提炼（如 Food 课：`I like…` `Can I have…` `Let's share…`）
  - ③ 示范剧集：单元各集 lesson-XXX.mp4 播放器（Tab 切换或纵向排列），每集字幕列表可点读/循环（复用现有 episode.js 交互）
- **练习区（下）**：
  - 场景说明卡：一句话场景设定（"你现在在<地点>，AI 老师是<角色>"）
  - 开场提示（可选 2-3 句，如 "Hi! What would you like?" 供孩子参考）
  - 练习区底部注明"跟 AI 老师说英语即可开始"，发音测评按钮/引导由 agent 触发

生成方式：**脚本自动聚合**（扫描 33 集 teach-data.js + index.html → 输出 9 份 scene html + 更新 `english/yellow/yellow.html` 列表入口指向 scene 页）。旧 33 集 index.html **保留磁盘不删**（作历史），yellow.html 列表只指新页。

### 3.3 课程内容落库（家长库 courses）

9 门场景课各一行，复用 courses 字段（`allocateTopicToChild` 重分配即快照到孩子库）：
- `title`：`Unit<N>-<name>`（如 `Unit6-food`，与目录一致，作为 courseKey 的 title 段——**不建议再带"场景·"中文前缀**，中文说明放 teaching_copy）
- `html_path`：`english/scene/Unit6-food.html`
- `lesson_method`：两阶段教法全文（"先学后练：Stage1 词卡/表达/剧集跟读 → Stage2 角色对话；全程英文"）
- `teaching_copy`：生成脚本自动写入 = 场景词表 + 关键句式 + 角色对话引导（**无需手写**）
- `sort_order`：排在基础课之后、语法课之前

**旧 33 门单集课退出**：从家长库 courses 删除（仅删课程名册行，磁盘资料保留）→ 家长端重新分配 english 主题 → 孩子库快照只含 27 门。旧孩子快照中的旧课行不物理删（历史进度保留在库，仅不再展示/排期）。

## 4. 孩子进度迁移（旧单集 → 场景课）

真实孩子（如珊珊）若已学部分单集课，重排后会显示"未学"。提供一次性迁移脚本：
- 映射：旧单集课 title → 所属场景课 title（按上表 33→9）
- 规则：场景课 `status='✅'` 当且仅当单元内任一集已学；`first_learned` 取最早、`mastery` 取该单元最高；`last_review` 取最近
- 孩子旧快照中未在映射表的课（基础/语法）不动

## 5. 场景角色扮演（buildEnglishPrompt 扩展）

子会话 prompt 增补规则段（进入 Stage2 时生效，agent 自演不改基建）：
- 练习阶段 = 场景角色对话：agent 扮演"场景角色"（food=店员/body=医生/…），孩子扮演自己
- 语言与纠错：全英文；孩子卡住用已学词/动作/复述帮助，仍不解才走双语兜底开关；自然纠错一句（不说"错"，示范正确说法）
- 控制词域：优先用本场景 teaching_copy 词表句式，新词一轮 ≤1 个并即时确认
- 发音测评：练习末由 agent 挑 1-2 个本场景重点句引导跟读（复用现有评测链路）

## 6. 实施任务

1. **生成脚本**：聚合 teach-data → 9 份 `scene/Unit<N>.html`（词卡/表达/剧集字幕播放器）+ 更新 yellow.html 入口
2. **家长库重排**：courses 增 9 门场景课（lesson_method/teaching_copy 由脚本生成内容写入）+ 删 33 门单集课名册行
3. **进度迁移脚本**：孩子库旧单集课 → 场景课（§4 规则）
4. **子会话 prompt 扩展**：场景课两阶段教法 + Stage2 角色扮演规则（buildEnglishPrompt 注入；角色由 agent 按场景自选）
5. **验证**：tsc/vitest/build + 冒烟（拿 1 个场景课全流程：进子会话→学→角色对话→发音测评）

## 7. 保留/不变（避免回退）

- 独立子会话、英文教学、双语兜底、发音测评入 daily、session-sync 递归、今日计划"进入课程"按钮 + agent 举手切换、topic_key 自动反查——全部保持（ENGLISH-AGENT-REQUIREMENTS.md）
- 基础课/语法课内容与课程不变

## 8. 待确认点（实施前）

1. **课程显示顺序/编号**：重排后 courses 是否按 01-27 重新 sort_order 展示（家长端课程名册更整齐）；历史单集课 title 是否仍留在家长端供回溯
2. **进度迁移是否本期必做**：若孩子尚未系统学 yellow 剧集（多数孩子刚开始），迁移脚本可延后
3. **scene html 交互范围**：Stage2 练习区是否需要页面上的"发音按钮/回合按钮"等强交互，还是纯由 agent 会话引导（首版建议后者，HTML 主要服务 Stage1 学习展示）
4. 中文场景名展示（问候/颜色/食物…）放 teaching_copy 与页面 header，不入 title
