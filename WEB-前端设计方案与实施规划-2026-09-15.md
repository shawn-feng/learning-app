# 学习伙伴 · Web 前端设计方案与实施规划

> 日期：2026-09-15 ｜ 设计：GLM-5.3 ｜ 实施：glm-5.3-flash（按本文档分阶段工单执行）
> 目标：为现有系统生成**纯浏览器可用的 Web 前端**，覆盖《学习伙伴-用户使用说明书》全部客户端功能，**实现机制与 Electron 客户端保持一致**。

---

## 0. 结论先行（核心架构决策）

**采用「共享渲染层 + `window.api` Web 适配层（shim）」方案：**

1. 新建 `web/` 目录的 Vite + React 项目，**直接复用现有渲染层源码 `src/`**（App 五态状态机、40+ 组件、page-bridge、styles.css），不复制不 fork。
2. 在 React 挂载前安装一个与 `electron/preload.ts` **同签名的 `window.api` 适配层**：把原来走 IPC 的约 150 个方法映射为 HTTP(`fetch`) + SSE(`EventSource`) + 浏览器原生能力。
3. 渲染层对 Electron 的唯一依赖面就是 `window.api`（已核实：`src/` 零 electron import，`App.tsx:9-13` 将其声明为 `any`）；仓库注释（`electron/lib/server-agent-client.ts:10`）早已预留「Web 版复用同一份 SSE 客户端」的设计意图。
4. 服务端做 **3 个附加式小补丁**（不改任何既有路由行为，Electron 客户端零影响）：
   - GET 二进制路由支持 `?token=` 认证（`/materials/content/:id`、`/files/:id`）；
   - 新增 `GET /api/v1/materials/doc/:id` HTML 文档网关（移植 `electron/lib/material-doc.ts` 的「拉取→改写→注桥」逻辑到服务端）；
   - （可选，生产部署用）`@fastify/static` 托管 `web/dist` 实现同源部署。

**为什么不是「重写一个新 Web 应用」**：客户端 40+ 组件承载了 PiBridge 协议、考核流程、积分门控等大量已调试成熟的机制细节；重写必然漂移。共享代码 + 单一适配缝隙（shim）是「机制与客户端保持一致」的最强保证——因为它**就是同一份客户端代码**。

---

## 1. 现状事实（设计依据，均已核实）

### 1.1 客户端（Electron + React 19）

| 事实 | 出处 |
|---|---|
| React 19 + electron-vite，**无路由库**，`View = "loading" \| "parent-login" \| "home" \| "dashboard" \| "learn"` 五态状态机 | `src/App.tsx:15-93` |
| 渲染层**不直接发 HTTP**，一切经 `window.api`（preload contextBridge，~150 方法 / 176 invoke 通道） | `electron/preload.ts` |
| 主进程统一 HTTP：`fetch(${serverUrl}/api/v1${path})`，`Authorization: Bearer <token>`，默认 15s 超时 | `electron/lib/server-client.ts:53-88` |
| 默认服务端 `http://127.0.0.1:8788`，持久化于 `data/server-connection.json`，可改 | `electron/lib/config.ts:150` |
| Token+许可缓存在 `data/license.json`；`authCheck` 本地过期判断 + 云端复核，云端不可达**降级放行** | `electron/lib/auth-manager.ts:71-205` |
| SSE：主进程手写解析器 fetch 流，**token 走 URL query**，断线指数退避重连 + Last-Event-ID 回放 | `electron/lib/server-agent-client.ts:150-280` |
| 录音：`getUserMedia` + `MediaRecorder`（webm/opus，250ms 分片）；STT 由主进程 ffmpeg 转 16k wav 后调云端 provider；TTS 走 edge-tts 返回 MP3 | `src/hooks/useAudioRecorder.ts`、`electron/lib/voice/*` |
| 资料文档：iframe 顶层加载 `asset://local/parent/{pid}/{topic}/{rest}?doc=1`，协议层完成「拉 HTML→改写相对资源→注入桥脚本→text/html 返回」 | `src/components/MaterialsPanel.tsx:166-184`、`electron/lib/material-doc.ts` |
| 配置每 2 分钟轮询 `/config/revision` 增量同步 | `electron/lib/config-sync.ts` |
| 本地定时任务 node-cron（总结/新会话/提醒）；到期提醒由渲染层轮询 `GET /scheduler/reminders?childId=`（就地标记已触发） | `electron/lib/scheduler.ts` |

### 1.2 服务端（Fastify v5，端口 8788，SQLite）

- API 前缀 `/api/v1`；家长 JWT（`Authorization: Bearer`），**SSE 端点已支持 `?token=` query**；**无 CORS**（同源部署或反代）。
- SSE 事件目录：`hello / user_message / text_delta / thinking_delta / tool_start / tool_end / message_end / agent_end / turn_end / error / display_content / page_cmd`；15s ping 保活；**`Last-Event-ID` 头或 `?lastEventId=` query 均支持重放**（`server/src/routes/agent.ts:100-103`）——浏览器原生 EventSource 自动带该头，天然兼容。
- `POST /agent/:childId/prompt` 受理即返回（409=busy），增量全走 SSE；`turn_end` 表示本轮结束。
- 发音评测全在服务端（腾讯 SSECP / 阿里 SSECP，服务端统一转 16k wav）：录音先 `POST /files/upload`（multipart）拿 fileId，再 `POST /assessment/assess {childId, audioFileId, refText, provider}`。
- 资料：`data/materials/<parentId>/[<topic>/]<file>`，id=base64url(相对路径)，`GET /materials/content/:id` 支持 Range/206，**仅认 Authorization 头**（web 需补 `?token=`）。
- 二进制路由当前**不支持 query token**（`server/src/routes/materials.ts:28-33` 只读 header）→ Phase 0 补丁点。

### 1.3 服务端 API 面（对接清单，摘自路由源码）

认证：`POST /auth/login|register`、`GET /auth/license`；孩子：`POST /children`、`GET /children`、`POST /children/auth`、`PATCH/DELETE /children/:id`；配置：`GET /config/revision|/config`、`POST /config/set`、`GET/POST /models*`；数据 RPC：`POST /db/query|exec`（24 个 op）；资料：`POST /materials/index|upload`、`GET /materials/content/:id`、`DELETE /materials/:id`、`GET /materials/list`；大文件：`POST /files/upload`、`GET /files/:id`、`DELETE /files/:id`、`GET /files/list`；备份：`GET /backup`、`POST /backup/restore`；会话回顾：`GET /sessions/:childId/dates|?date=`；孩子 agent：`GET /agent/:childId/stream`、`POST /agent/:childId/prompt|events|open|reset|abort|page-result`、`GET /agent/:childId/history`；家长 agent：`GET /parent-agent/stream`、`POST /parent-agent/prompt|reset|abort`；考核：`GET /exam/config/:childId`、`GET /exam/schedules/:childId`、`POST /exam/schedules`、`POST /exam/schedules/:id/start|complete`、`DELETE /exam/schedules/:id`、`POST /exam/plans/:id/cancel`、`GET|POST /exam/fixed-config`、`POST /exam/attempts`、`GET /exam/attempts/:childId`、`GET /exam/course-records/:childId`、`GET /courses/status/:childId`、`GET /assess/topics/:topic/knowledge-points`、`GET|POST /assess/*`、`POST /exam/agent/generate|grade`；评测：`GET|POST /assessment/config`、`POST /assessment/assess`；调度：`GET|POST|PATCH|DELETE /scheduler/tasks*`、`POST /scheduler/reminders`、`GET /scheduler/reminders(/list)?childId=`、`GET /scheduler/runs|effective-config`；计划：`GET|POST /study-plans*`、`GET /plans/today`、`POST /plans/status|life|exam`；积分：`GET /rewards/:childId(/config)`、`PUT /rewards/:childId/config`、`POST /rewards/:childId/redeem`。

---

## 2. 机制映射表（客户端机制 → Web 实现）

| # | 客户端机制 | Web 实现 | 一致性说明 |
|---|---|---|---|
| 1 | IPC invoke → 主进程 `serverFetch` | shim `http()` → `fetch(baseURL + /api/v1/..., {Authorization: Bearer})`，15s 超时、`{error}` 语义对齐 `server-client.ts` | 同路由、同鉴权、同错误语义 |
| 2 | token/许可存 `data/license.json` | `localStorage`（`web.token` / `web.license` / `web.parentId` / `web.serverBase`） | `authCheck` 同样实现「本地过期判断 + `/auth/license` 复核 + 不可达降级放行」 |
| 3 | 主进程 fetch 流式 SSE 解析 | **原生 `EventSource`**（`?token=`，自动重连自动带 Last-Event-ID 头，服务端已支持重放） | 事件目录与 `translateAgentEvent`/`bridgeAgentEventCore` 翻译逻辑 1:1 移植，含「message_end 文本缓冲到 turn_end 再发 `pi:reply`」的轮末语义 |
| 4 | `asset://...?doc=1` 协议层文档渲染 | **服务端文档网关** `GET /api/v1/materials/doc/:id?token=&font=`：拉 HTML→改写相对资源为 `/api/v1/materials/content/<b64>?token=` 绝对 URL→注入桥脚本→`text/html`。iframe 仍走**真实 URL 顶层导航**，正文脚本随导航执行 | 机制同构：Electron 在协议层做的事移到服务端路由层；改写与注桥逻辑直接移植 `electron/lib/material-doc.ts` + `src/lib/page-bridge.ts` 的 `injectBridge` |
| 5 | `media://` 音视频代理（Range） | 直接 `/api/v1/materials/content/:id?token=`（服务端已支持 Range/206） | `<video>/<audio>` 原生支持 Range |
| 6 | TTS：edge-tts（主进程）→ MP3 blob → `new Audio()` | 浏览器 `speechSynthesis`：shim 提供 `voiceTts(text, opts)` 兼容签名（返回标记对象）+ `voiceSpeak(text, opts): Promise<void>`；**播放点分支**（ChatWindow 朗读、资料点读）web 走 `voiceSpeak`，播完 resolve（对齐 `page:tts:done` 回执时机） | 「宿主代播 + 完成回执」机制不变；仅合成引擎换成浏览器内置（Web 无法跑 Node edge-tts） |
| 7 | STT：录音→ffmpeg 16k wav→云端 provider | `MediaRecorder`（保留音频 blob 供气泡回放）与 `webkitSpeechRecognition` **并行**跑转写；返回 `{text, audio(base64)}` 同签名 | ASR 文本可编辑的交互不变；已确认浏览器无 ffmpeg、服务端无 STT 路由，Web Speech API 是唯一纯前端方案（Chrome/Edge 支持，不支时友好报错） |
| 8 | 多段录音 ffmpeg PCM 合并 | WebAudio `decodeAudioData` → PCM 拼接 → WAV 编码（`voiceMerge` 同签名） | 评测侧服务端自动转 16k，前端只需容器合并 |
| 9 | 发音评测 | 与客户端完全一致：`/files/upload`(multipart, fetch 可带鉴权头) → `POST /assessment/assess` | 零偏差 |
| 10 | 本地 node-cron 定时任务 | 页面存活期 `setInterval` 轮询 `GET /scheduler/reminders?childId=`（客户端本就轮询此端点触发提醒）+ 配置同步 2min 轮询 `/config/revision` | 重任务（每日总结/积分结算/固定考核生成）服务端 worker 本就是真源；web 与客户端一样「不在线不本地跑」 |
| 11 | `pageTakePending` 页面操作缓冲（主进程环形缓冲 + 中文格式化） | shim 内存队列 + 移植 `formatPageEvent` 格式化，发 prompt 前拼 `[页面操作] ...` | 语义一致：发送即清空 |
| 12 | 窗口控制/自动更新/托盘 | TitleBar web 分支隐藏窗口按钮；更新类 API no-op | 浏览器天然能力 |
| 13 | 系统对话框 `confirmDialog`/文件选择 | `window.confirm` / `<input type=file>` / anchor 下载 | 同签名 |
| 14 | 考试 iframe 麦克风（Electron 注入 Permissions-Policy 头） | iframe 增加 `allow="microphone; camera"` 属性（web 分支） | 浏览器等价机制 |

**明确的环境差异（如实声明，非功能缺失）**：Web 版离线不可用（客户端的「云端不可达降级放行」保留，但业务请求本就依赖服务端）；TTS 音色为系统语音而非 edge-tts 音色；STT 依赖 Chrome/Edge。

---

## 3. 服务端补丁规格（Phase 0，附加式）

> 原则：**只增不改**。既有路由的行为、Electron 客户端路径零影响。所有补丁集中在 `server/src/`。

1. **query token 认证**：`server/src/routes/materials.ts` 与 `files.ts` 的 `authParent()` 扩展为 `Authorization: Bearer` 或 `?token=`（复用 `verifySession`）。仅限 GET 二进制/文档路由（POST 仍仅 header，防 token 泄露到 POST 日志）。
2. **文档网关** `GET /api/v1/materials/doc/:id?font=&token=`：
   - `:id` = base64url(相对路径)（与 content 同编码）；
   - 仅接受 html/htm（其余 400）；
   - 移植 `electron/lib/material-doc.ts`：从磁盘读 HTML → 相对资源改写为 `/api/v1/materials/content/<base64url>?token=<req.token>`（`token` 参数化拼接，音视频同 content 路由，Range 天然可用）→ 注入桥脚本（复用 `src/lib/page-bridge.ts` 的 `buildBridgeScript`/`injectBridge` 纯函数，注意该文件不得 import 渲染层 React——若耦合则在 `server/src/agent/` 下做共享拷贝并注明来源）→ `Content-Type: text/html; charset=utf-8` + `Cache-Control: no-store`；
   - query `font`（资料字号）通过 `page:mat-font` 机制下发或注入变量，对齐 Electron 行为。
3. **静态托管（可选）**：`web/dist` 存在时注册 `@fastify/static`（`/` → dist，SPA fallback 到 index.html）。开发期不需要（用 Vite proxy）。

---

## 4. Web 项目结构（Phase 1 起）

```
web/
  package.json            # 独立包：vite、@vitejs/plugin-react、typescript（react 等版本对齐根 package.json）
  tsconfig.json           # include: src + ../src；jsx: react-jsx；paths 不需要（相对导入）
  vite.config.ts          # server.proxy: '/api' → http://127.0.0.1:8788（changeOrigin）
  index.html              # <div id="root"> + /src/main.tsx
  README.md               # 启动/部署说明
  src/
    main.tsx              # installWebApi() → createRoot(<App/>)（App 从 ../../src/App 导入）
    env.d.ts              # Window.api any 声明 + SpeechRecognition 类型
    shim/
      core/
        server-fetch.ts   # http()/httpBinary()/upload()/download()，baseURL 解析（默认同源，localStorage 可覆盖），错误语义对齐 server-client.ts
        event-bus.ts      # on/off/emit（pi:* 事件）
        sse.ts            # EventSource 管理：ensureChildStream(childId)/ensureParentStream(kind)；事件翻译（translateAgentEvent 移植）+ 轮末缓冲（bridgeAgentEventCore 移植）
        page-events.ts    # 页面操作环形缓冲 + formatPageEvent 移植
        config-sync.ts    # 2min revision 轮询
        reminders.ts      # 提醒轮询
      domains/            # 每文件导出该域的 window.api 方法子集，install.ts 合并
        auth.ts children.ts db.ts config.ts models.ts sessions.ts scheduler.ts
        materials.ts files.ts backup.ts parent.ts plans.ts exam.ts assessment.ts
        voice.ts skills.ts learning.ts agents.ts dialogs.ts window.ts misc.ts
      voice/
        tts.ts            # speechSynthesis 封装（打断模式/FIFO 队列模式，对齐 page-bridge TTS 语义）
        stt.ts            # SpeechRecognition + MediaRecorder 并行
        wav.ts            # decodeAudioData 合并 + WAV 编码
      install.ts          # 组装全部域 → (window as any).api = {...}，并挂 __web: true 标志
```

**共享 `src/` 的最小分支点（仅限下列，全部 `window.api.__web` 守卫，Electron 路径零改动）**：

| 文件 | 分支 |
|---|---|
| `src/components/MaterialsPanel.tsx` | `buildDocUrl` web 分支返回 `/api/v1/materials/doc/<b64>?token=...&font=N`（token 从 shim 内部 getter 取）；iframe 加 `allow="microphone"` |
| `src/components/TitleBar.tsx` | web 隐藏最小化/最大化/关闭按钮 |
| `src/components/ChatWindow.tsx`（及 TTS 消费点，实施时 grep `voiceTts` 确认全集） | web 播放分支走 `window.api.voiceSpeak` |
| `src/components/TopicDetail.tsx` | html 预览的取数来源 web 分支（经 doc 网关 fetch 文本；srcDoc 渲染，相对资源已是绝对 URL） |

**绝对禁止**：修改 `electron/`；改变 `src/` 既有 Electron 行为；引入路由库/状态库/组件库；动 `server/` 既有路由逻辑。

---

## 5. 实施规划（flash 执行工单）

> 每阶段一个独立工单。**铁律**：实现每个 `window.api` 方法前，必须先读 `electron/preload.ts` 中该方法的签名与 `electron/lib/ipc-handlers.ts`（2544 行）中对应通道的实现，再映射到服务端路由/浏览器能力——**禁止凭猜测实现**。完成后 `npx tsc --noEmit` + `npm run build` 必须通过。

### Phase 0 —— 服务端补丁（先行，独立验证）
- 按 §3 实现；`cd server && npx tsc --noEmit` 通过；用 curl 验证 `?token=` 取二进制与 doc 网关（本地起服务）。
- 验收：`curl -I "http://127.0.0.1:8788/api/v1/materials/content/<id>?token=<jwt>"` 200/206；doc 网关返回 text/html 且含桥脚本标记。

### Phase 1 —— 脚手架 + 可启动
- 建 `web/` 全套（§4）；`install.ts` 挂载含全部域 **stub**（未实现方法 `throw "not implemented"`）的 api；App 启动 → `authCheck` 返回未认证 → 渲染登录页。
- 验收：`npm run dev` 打开页面无白屏、无模块解析错误；登录页 UI 与客户端一致（同 styles.css）。

### Phase 2 —— 基础域
auth / children / db / config / models / sessions / scheduler / dialogs / window / edit / view / app-version / server-config。
- 覆盖：家长登录注册（token 入 localStorage）、`authCheck` 降级逻辑、孩子增删改查与密码校验、`db/query|exec` 全 24 op、配置读写与 revision 轮询、模型池、会话回顾、定时任务与提醒轮询、`confirmDialog`→`window.confirm`。
- 验收：家长可登录 → Home 出现孩子卡片 → Dashboard 各 tab 数据可加载（不含 agent 对话）。

### Phase 3 —— 资料域
materials / files / backup / parent-library（`parent*` 系列）/ skills / learning 读写。
- 上传（`<input type=file>` → multipart）、列表/索引同步、删除、备份 zip 下载（blob → anchor download）与恢复（zip multipart）、家长库 topics/courses/tags 全 op。
- MaterialsPanel/TopicDetail 的 web 分支（§4 表）。
- 验收：家长端可上传 html 资料、预览渲染（含相对图片）；备份下载文件完整。

### Phase 4 —— SSE + agent 域（孩子 Learn / 家长助手对话全链路）
- `core/sse.ts`：孩子流 `ensureChildStream`（caps=`material-panel,mic`——web 无 electron 能位）、家长流 `kind=parent|parent-content`；事件翻译 + 轮末缓冲；`piRemoveListeners` 清理。
- 方法：`piStartChild/piStartParent(+/Content)/piPrompt(+/Parent/ParentContent)/piAbort/piReset(+/Parent)/piDispose/piListSessions/piGetSessionMessages/scene*(5 个)/page 三件套/parentChildDaily/token 统计。
- `pageEvent→pageTakePending` 缓冲语义 + `POST /events` 转发（以 ipc-handlers 实际行为为准）。
- 验收：孩子端可对话（文本），气泡流式渲染、工具卡片、`display_content` 打开资料页、家长助手可对话。

### Phase 5 —— 语音域
- `voice/tts.ts`（打断/FIFO 两模式 + `voiceSpeak`）、`voice/stt.ts`（并行录音+识别，返回 `{text, audio}`）、`voice/wav.ts`（合并）；`voiceConfig*`、`assessmentTest`。
- ChatWindow 等 TTS/STT 消费点 web 分支。
- 验收：聊天可语音输入（Chrome/Edge）、AI 回复可朗读、考核录音可多段合并上传。

### Phase 6 —— 考核/积分域
exam 全家桶（config/schedules/start/complete/cancel/fixed-config/attempts/submit(含 voices 上传)/audio/generate/score/assessSpeech）、assess 题库、plans/study-plans/rewards。
- 验收：孩子端考核页三区可用、背诵题隐藏原文+录音评测回显、提交后家长端考核记录可见；积分面板余额/流水/档位配置可读写。

### Phase 7 —— 总装验证
- `npx tsc --noEmit` + `npm run build` 零错误；`npm run dev` + 本地 server 走通：登录 → 选孩子 → 对话 → 资料 → 考核 → 积分 主干路径。
- 产出 `web/README.md`（启动：`npm --prefix web run dev`；构建：`npm --prefix web run build`；生产：server 静态托管或反代）。
- 根 `package.json` 增加脚本 `web:dev` / `web:build`（附加式）。

### 并行与依赖
Phase 0 与 Phase 1 可并行；Phase 2/3 依赖 1；Phase 4 依赖 1（core/sse）；Phase 5/6 依赖 4（场景/考核页内嵌对话）；各阶段文件所有权互不重叠（domains/*.ts 按域分文件），`install.ts` 仅 Phase 1 建立。

---

## 6. 功能覆盖对照（《用户使用说明书》→ Web 支持）

| 说明书章节 | Web 支持度 |
|---|---|
| §2 首次使用与登录（家长账号/添加孩子/配模型） | ✅ 完整 |
| §3.1–3.3 家长工作台/孩子管理/主题课程管理 | ✅ 完整（五步建主题向导、课程编辑、分配） |
| §3.4 学习/考核/生活计划 | ✅ 完整（AI 排计划 + 今日计划 + 自动完成判定 + 家长修正） |
| §3.5 学习考核管理（排考核/知识点题库/考核记录） | ✅ 完整 |
| §3.6 积分奖励体系 | ✅ 完整（注：兑换目录/审批服务端本就无 HTTP API，与客户端现状一致） |
| §3.7 家长 AI 助手 | ✅ 完整（三能力域 + 操作分级由服务端 prompt 保障） |
| §3.8 每日记录与对话回顾/导出 | ✅ 完整（导出 = 浏览器下载） |
| §3.9 学习资料库（PiBridge/点读/场景互动页） | ✅ 完整（doc 网关 + 桥注入 + postMessage 协议原样） |
| §3.10 系统设置（模型/语音评测/定时/备份/上限） | ✅ 完整 |
| §4.1–4.3 进入学习/主界面/课程学习 | ✅ 完整（双栏布局、模型切换、查词浮层——字典已在 bundle） |
| §4.4 英语场景课 | ✅ 完整（scene 子会话 + 角色扮演 + 点读） |
| §4.5 学习考核（背诵/选择/问答 + 发音评测） | ✅ 完整 |
| §4.6 我的积分 | ✅ 完整 |
| §4.7 孩子端设置（模型/语音） | ✅ 完整 |
| §5.1 离线使用 | ⚠️ Web 环境限制：离线不可用（客户端亦为薄客户端，业务数据无本地缓存）；token 许可缓存与降级放行逻辑保留 |
| §5.4 定时任务 | ✅ 页面存活期等价实现；不在线时服务端 worker 兜底（与客户端「未运行不执行」一致） |
| 自动更新 | ➖ 不适用（Web 天然最新） |
