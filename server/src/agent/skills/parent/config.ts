/**
 * 场景技能 · 改设置（台账 F4）。当前**没有工具**，所以本技能的内容是"负向口径"：
 * 如实说明改不了 + 指路设置页，不许假装改成功（说明书 §3.7 承诺与实现不一致，按回归处理）。
 */
import type { ParentSkill } from "./shared.js";

export const configSkill: ParentSkill = {
  name: "parent-scene-config",
  title: "改设置（只解释、不改）",
  triggers: "「把默认模型换成 XX」「资料上限调到 30」「语音换成哪个声音」「定时任务配置改一下」",
  summary: "系统设置类请求：agent 没有工具，如实说明并指路设置页。",
  tools: [],
  body: `# 改设置（只解释、不改）

## 什么时候用
家长说"把默认模型换成 XX""资料上限调到 30""TTS 换成哪个声音""定时任务配置改一下"这类**系统设置**请求。

## 口径（负向口径，必须照此回答）
- **这些配置 agent 改不了**：默认模型、资料上限、语音 / TTS、定时任务的面板配置——**目前都没有对应工具**。
- 正确回答三件事：
  1. **如实说做不到**："这个我没法直接改"（**不许**说"好的我已经改了"，也不要说"我试试"）；
  2. **指路**：告诉家长去「设置」页的哪一项；
  3. **说清影响**（家长给了具体值时可补充）：例如默认模型影响新建会话使用哪个模型；资料上限影响还能放多少资料。
- 家长追问"为什么不能"，就说这是当前能力边界，不要编原因。

## 能做的近邻（别把家长推走）
- **定时任务的"新建"可以做**：走 \`parent-scene-automation\`；已建任务的**改 / 停用 / 删除目前没有工具**，引导去「定时任务」面板。
- **孩子档案**（姓名 / 年龄 / AI 伙伴人设）**有工具**，走 \`parent-scene-child\`——这不算"系统设置"，别一起推给界面。
- 家长问的是"这门课 / 这个资料"时，那是别的场景，别答成设置问题。

## 结束
明确交回家长：**去哪个页面的哪一项**；如果他同时提了"能不能顺手帮我…"，把能做的那部分做完再说清做不到的那部分。`,
};
