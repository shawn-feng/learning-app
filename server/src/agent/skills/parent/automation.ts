/**
 * 场景技能 · 定时任务与提醒（台账 E6）。
 * 与 parent-scene-plan 的分工：这里是**到点无头执行**；重复规则引用 REPEAT_RULES_BLOCK（同源）。
 */
import type { ParentSkill } from "./shared.js";
import { REPEAT_RULES_BLOCK } from "./shared.js";

export const automationSkill: ParentSkill = {
  name: "parent-scene-automation",
  title: "定时任务与提醒",
  triggers: "「每天早上 6 点查天气并播报」「每天提醒她做眼保健操」「每周日汇总一下这周」",
  summary: "家长说完、系统到点自动执行的自然语言指令（含提醒）；含能力白名单与失败语义。",
  tools: ["parent_scheduler_task_create"],
  body: `# 定时任务与提醒

## 什么时候用
家长要**到点自动做一件事**：每天早上 6 点查天气并播报、每天提醒她做眼保健操、每周日汇总一下这周。
**排进日程的课 / 考核不走这里** → 读 \`parent-scene-plan\`。

## 步骤
1. 把家长的话整理成**一条可独立执行的指令**，写清三件事：**做什么、产出什么、什么时候**。
   例：「每天早上 06:00 查询当天天气，把结果用一句话播报给家长」。
2. 向家长**复述这条指令与时间**，确认后调用 \`parent_scheduler_task_create\`。
3. 提醒类需求：在指令里写明"创建一条提醒"（到点由执行会话落到孩子设备的「我的提醒」，语音播报）。
4. **新建之后不能改、停用或删除**（当前没有这些工具）——家长要调整时如实说明，并引导到「定时任务」面板处理（或在确认后新建一条替代）。

## 参数速查（本场景工具）
- \`parent_scheduler_task_create\`：\`childName\`（必填，执行对象与产物归属的孩子）· \`name\`（必填，任务名，如「每日天气播报」）· \`instruction\`（必填，自然语言指令：做什么、产出什么）· \`time\`（必填，\`HH:mm\` 触发时刻）· \`frequency\`（可选：\`daily\`（缺省）/ \`weekly\` / \`once\` / \`interval\`）——\`weekly\` 带 \`weekday\`（0=周日…6=周六）、\`interval\` 带 \`intervalMinutes\`、\`once\` 带 \`fireAt\`（ISO 时间）。

## 重复怎么写（本场景与计划场景同源）
${REPEAT_RULES_BLOCK}

## 能力白名单（写指令前先自查）
执行会话**能做**的：查天气、建提醒、读写孩子 \`daily\` 记录。
执行会话**不能碰**的：**积分、考核、学习计划**。
- 家长的指令如果落到"不能碰"的那几类（例："每天自动给她加 5 分"），**换做法**：说明这条做不了，给出可行替代（积分与计划请走对应场景/界面），**不要写一条做不到的指令**。

## 口径与红线
- **失败不重试**：当天跑失败就过了。如实说明"到点自动执行，失败不会重试"，**不要承诺"每天都会播报"**。
- 不要说"我已经每天帮你看着"，也不要把"建了任务"说成"已经执行了"。
- 指令里不要写需要人接手的动作（如"提醒家长确认"）。
- 时间写清楚（几点、每周几）；家长没说时间就**问一句**再建。

## 结束
复述"建了什么任务、什么时间跑、跑起来会做什么、失败会怎样"。`,
};
