/**
 * 场景技能 · 了解孩子最近学得怎么样（台账 D1 D2 D3 D4 D5 D7）。
 * 家长最高频的一句；**不含积分**（积分见 parent-scene-points）。
 */
import type { ParentSkill } from "./shared.js";
import { ironRules } from "./shared.js";

export const progressSkill: ParentSkill = {
  name: "parent-scene-progress",
  title: "了解孩子最近学得怎么样",
  triggers: "「最近学得怎么样」「这次考了多少、哪几题错了」「哪里薄弱、老错什么」「到底会不会」「她昨天到底问了什么」「来份周报」",
  summary: "按考核 / 知识点 / 错题 / 逐字稿讲清孩子的学习情况，并可按需出报表。不含积分。",
  tools: [
    "parent_list_children",
    "parent_library_topics",
    // ISSUE-144 实跑修正（2026-09-25）：问"《静夜思》到底会不会"要**先定位这门课在哪**，
    // 用的就是 course 场景的课程名册。它只归 course 时，progress 场景一伸手就撞守卫
    // （实测样本 3 连撞两次、被迫加载 course + materials）。⇒ 声明为**多场景工具**（守卫任一放行）。
    "parent_library_courses",
    "parent_library_course_content",
    "parent_read_child_conversation",
    "parent_display_report",
    // ISSUE-144 P4：孩子的数据洞察——考核逐题 / 掌握与薄弱（专用只读报告工具，自带口径）
    "parent_child_exam_report",
    "parent_child_mastery_report",
  ],
  body: `# 了解孩子最近学得怎么样

## 什么时候用
家长想了解孩子的学习情况：最近学得怎么样、这次考了多少 / 哪几题错了、哪里薄弱老错什么、某个知识点到底会不会、孩子昨天到底问了什么、要一份周报。
**不含积分与兑换**——家长问到积分时读 \`parent-scene-points\`。

## 步骤
1. **先确认对象与范围**：哪个孩子（不确定先 \`parent_list_children\`）、看哪段时间（默认"最近一次考核"或"本周"，家长说了一周/一个月就按他说的）。
2. **考核结果**：\`parent_child_exam_report\`——不带参数＝最近几场（含整体与趋势）；要看细节就把那一场的 \`id\` 传给 \`plan_id\`。
3. **掌握与薄弱**：\`parent_child_mastery_report\`——不传参＝各主题概览 + 薄弱项（错题本，反复出现的排前面）；说主题名看逐课、说课程名看知识点档位与学习结果。
4. **对照着讲**：把"考核里错的题"和"掌握档位弱的课/知识点"对上——这才是家长要的"哪里薄弱"（不要只报一个分数）。
5. 家长问"她昨天到底问了什么"这类细节时，再读**逐字稿**。
6. 家长要"周报 / 汇总"时，用 \`parent_display_report\` 推一份 markdown 到「📊 报表」区。

## 用哪个工具（先看这里）
| 家长想知道 | 用哪个 | 怎么用 |
|---|---|---|
| 这次考了多少、哪几题错了 | \`parent_child_exam_report\` | 不传 \`plan_id\`＝最近几场（含整体得分率与趋势）；传 \`plan_id\`＝那一场的**逐题明细 + 每课概要 + 知识点档位** |
| 哪里薄弱、某个知识点到底会不会 | \`parent_child_mastery_report\` | 不传参＝各主题概览 + **薄弱项（错题本）**；\`topic\`＝该主题逐课 + 要盯的知识点；\`course\`＝单课掌握叙述 + 教学建议 + 知识点档位 + 学习结果 + 本课错题 |
| 学到哪了、最近什么时候学的 | \`parent_library_topics\` | 实时聚合：已学/总数/下一课 + 权威 \`topic_key\` |
| **"《静夜思》到底会不会"这类要点名一门课的问题** | \`parent_library_courses\` | **先定位这门课在哪**（\`topic\`=主题目录名 → 该主题下的课程名册）；拿到**准确课程名**再去 \`parent_child_mastery_report\`（\`course\`=课程名）或 \`parent_library_course_content\` 看该课内容。**不要凭记忆猜课程名** |
| 她昨天到底问了什么 | \`parent_read_child_conversation\` | 默认今天，最多 7 天 |
| 来份周报 | \`parent_display_report\` | 用**刚查到的真实数据**推一份 markdown 到「📊 报表」区 |

> **优先级**：上面两把专用报告工具**自带口径**（下表的口径就写在那两把的输出里），**优先用它们**，不要自己拼。
> 2026-09-25（\`ISSUE-144\` P6）：原先这里还留着 \`parent_db_read\` 这条"通用兜底通道"，**已随通用数据 API 整组退场**——
> 对话里**没有**任何"自己查表"的入口。这张表覆盖不到的问题，如实说"我现在查不到这个"并说明差什么，
> **不要臆造工具名、不要用别的工具硬凑**（缺工具是产品要补的事，不是你要绕的事）。

## 参数速查（本场景工具）
- \`parent_list_children\`：无参数 → 只回**孩子姓名**（不读档案）。
- \`parent_child_exam_report\`：\`child\`（孩子姓名；**名下只有一个孩子时可省略**）· \`plan_id\`（缺省＝列最近几场已完成的考核，带整体得分率与趋势）· \`course\`（只看某门课，可选）· \`limit\`（缺省 5，最多 10）→ 列表里每场都带 \`id=\`，**追问某一场就把那个 id 传给 \`plan_id\`**。没考完的场次只给范围、不给题。
- \`parent_child_mastery_report\`：\`child\`（同上，可省略）· \`topic\`（主题名或拼音目录名）· \`course\`（课程名）——三个层级：概览（含薄弱项）/ 主题 / 单课。
- \`parent_library_topics\`：无参数 → 每个主题的进度（已学/总数/下一课）与**权威 \`topic_key\`**。
- \`parent_library_courses\`：\`topic\`（必填，主题目录名如 \`lunyu\`）→ 该主题下的**课程名册**（准确标题）。**"这门课在不在 / 准确叫什么"只能靠它**，别猜。
- \`parent_library_course_content\`：\`topic\` + \`title\`（必填）→ 该课知识点与题（含**标准答案 \`answer\`**，见下"红线"）。
- \`parent_read_child_conversation\`：\`child\`（必填）· \`date\`（可选：\`YYYY-MM-DD\` / \`all\` / 口语「今天/昨天/前天」；缺省今天）· \`days\`（\`date=all\` 时读最近几天，缺省 3、最多 7）。
- \`parent_display_report\`：\`markdown\`（必填，完整 markdown 正文，**不要 HTML**）· \`title\`（可选，缺省「学习报表」）。

## 口径（本场景的重点）
- **先整体，再细节**：先说这段时间的整体情况（考了几场、得分区间/趋势、哪门课最好/最差），再讲具体错在哪、卡在哪——不要一上来就罗列表行。
- **讲考核的三步**（家长问"这次考得怎么样"时按这个走）：① 整体（得分率 + 趋势 + 哪门课拖后腿）→ ② 定位（**一次讲一两道**最值得说的题：错的原因 + 是哪类问题）→ ③ 给一条可执行的建议（复习哪块 / 要不要重考 / 家里可以怎么配合）。**不要评价"考得差"**，也不要一口气把错题全倒出来。
- **区分"没学过"与"学过但错"**：没学过＝该课/知识点还没开始（**不能当成掌握差**）；学过但错＝真问题，要指出卡在哪一步（审题、步骤、记忆、表达）。
- **两层档位别混**：**考核知识点**是过程结果三档——\`solid\`=扎实 / \`partial\`=部分掌握 / \`weak\`=薄弱；**课程与知识点的累计掌握**是四档——\`not_started\`=还没开始 / \`learning\`=学习中 / \`needs_review\`=需要复习 / \`mastered\`=已经掌握。**空着/没分析过 ≠ 学得差**——如实说"这块还没有数据"。
- **进度 ≠ 掌握**：进度取"最近学习时间"，掌握取"最近一次考核得分率"；**按主题聚合，不整表罗列**。
- **错题本口径**：只把 \`status=open\` 当作"当前没掌握"；\`count\` 越大＝反复出现＝越没掌握；本周新增看 \`first_seen\`；\`kind\`：wrong_question=错题 / unknown_word=生字 / weak_point=稳定薄弱点；**按 kind 聚合概览**（别逐条念）；已关闭项一般不进概览。
- **报表**：只用**刚查到的真实数据**，不编造；markdown 结构；标题写清孩子名与时间范围。

## 红线
- 逐字稿**只读**、只限**自己名下**的孩子；向家长**概括要点**，不要大段复述原文。
- **不要代孩子标 \`mastered\`**（掌握要孩子自己验证）；错题本只在"这条记错了"时按管理口径纠错：一次一行、改前复述。
- **还没考完的场次不要提前给题目**（考核报告只给范围）：家长很可能转述给孩子，提前知道题＝这次考核作废。
- 家长**可以**看标准答案（家长库 \`parent_library_course_content\` 本来就返回 \`answer\`）：说的时候明确"**这是给家长看的解释，要不要告诉孩子由你决定**"。（这条口径的方向见台账 §8 #4 / 域 D 讨论·四，尚未最终拍板）
${ironRules("A7")}

## 结束
复述三件事：**看了哪段时间、结论是什么、哪里数据不足**；家长要的话再问一句"要不要把这份情况推到报表区"。`,
};
