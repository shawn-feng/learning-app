/**
 * 场景技能 · 孩子档案与账号（台账 A1 A2 A3；A4/A5 界面自足）。
 */
import type { ParentSkill } from "./shared.js";
import { ironRules } from "./shared.js";

export const childSkill: ParentSkill = {
  name: "parent-scene-child",
  title: "孩子档案与账号",
  triggers: "「帮我加个孩子叫…」「把 AI 名字改成…」「孩子今年 8 岁了」「我有几个孩子」",
  summary: "添加孩子（含建号引导）、改档案与人设、看名单；账号类操作一律引导界面。",
  tools: ["parent_child_create", "parent_child_update", "parent_list_children"],
  body: `# 孩子档案与账号

## 什么时候用
家长要添加孩子、改孩子档案或 AI 伙伴人设、看自己有几个孩子。

## 参数速查（本场景工具）
- \`parent_child_create\`：\`name\`（必填，孩子姓名）· \`aiName\`（AI 伙伴名字）/ \`aiEmoji\`（头像表情）/ \`aiPersonality\`（AI 性格）/ \`age\`（数字）/ \`grade\`（年级）/ \`interests\`（兴趣）——后六项可选。受**孩子数量上限**约束，**同名会被拒**。
- \`parent_child_update\`：\`childName\`（必填，现有姓名，定位用）+ 其余可选（\`name\`＝改名 / \`aiName\` / \`aiEmoji\` / \`aiPersonality\` / \`age\` / \`grade\` / \`interests\`）——**只改你传的字段**。
- \`parent_list_children\`：无参数；只回**孩子姓名**（不读档案）。

## 添加孩子（步骤 + 必须问清的东西）
1. **一并问清 AI 伙伴的名字与性格**（这是孩子端"我是谁"的唯一数据来源；不问清，孩子端就缺一段）。可以顺带问年龄 / 年级 / 兴趣。
2. 确认姓名后调用 \`parent_child_create\`。
3. **建完必须说清三件事**：
   - **密码要去「孩子管理」界面设置**（工具不能设密码）；
   - 孩子数量**有上限**（到上限会被拒，工具会明确报错）；
   - **同名会被拒**。
4. 不要替家长起名、也不要替孩子决定人设——家长没给就先留默认（之后能改），别自己编一个。

## 改档案 / 换人设
- \`parent_child_update\` **只改传入的那几项**，不动其它字段。
- 改前**复述**："把 X 从 A 改成 B"，确认后再改。
- **改名要提醒连带影响**：课程名里不带孩子名，但计划 / 考核等对象是按**姓名**定位的——改完提醒家长"旧称呼的地方需要核对"。

## 看名单
- \`parent_list_children\` 只回**孩子姓名**，**不读档案**。
- 家长要看细节（进度 / 计划 / 日常 / 主题 / 复习 / 考核 / 积分 / 账号）时，引导去「孩子详情」页的对应页签——界面已经齐了。

## 红线（认证与账户）
${ironRules("A4")}
- 家长要求"设个密码""换个订阅""把这个孩子删了"时：**明确说"这个我做不到"**，并指路对应界面（孩子管理 / 设置）。不要含糊其辞，也不要假装去试。

## 结束
复述"建了 / 改了什么"，并把"去界面设密码""去核对旧称呼"这类待办**明确交回家长**。`,
};
