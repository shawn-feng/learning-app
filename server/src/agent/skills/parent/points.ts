/**
 * 场景技能 · 积分怎么算的 / 兑换（台账 F2 F3；F1 界面自足）。
 */
import type { ParentSkill } from "./shared.js";
import { ironRules } from "./shared.js";

export const pointsSkill: ParentSkill = {
  name: "parent-scene-points",
  title: "积分怎么算的 / 兑换",
  triggers: "「她这分怎么算的」「怎么扣成这样」「兑换批了没」「奖励规则是怎么设的」",
  summary: "按账本口径解释积分（分组评档 → 门槛只卡加分项 → 次日结算 → 兑换扣分时点）与兑换现状。",
  tools: [
    // ISSUE-144 P4：积分与兑换的专用只读报告（自带下面的账本口径）
    "parent_child_points_report",
    // 报告按孩子姓名定位：不确定名字时先看名单（多场景工具，任一场景加载即可用）
    "parent_list_children",
    // 通用兜底通道（不属于本场景，计划退场）
    "parent_db_read",
  ],
  body: `# 积分怎么算的 / 兑换

## 什么时候用
家长问"她这分怎么算的""怎么扣成这样""兑换批了没""奖励规则怎么设的"。

## 用哪个工具
| 家长想知道 | 用哪个 | 怎么用 |
|---|---|---|
| 她这分怎么算的、为什么没加分、最近加了扣了多少、还差多少能兑 | \`parent_child_points_report\` | \`child\`（孩子姓名；**名下只有一个孩子时可省略**）· \`days\`（缺省 7，最多 30）——余额、规则、逐日结算（完成率/漏项/档位/门槛）、流水、未发放的兑换，**一次全给** |
| 档位、奖品目录怎么配 | **对话里没有** | 指路界面（家长侧没有配置积分/兑换目录的工具） |

> \`parent_db_read\` 是**通用兜底通道**（不属于本场景，计划退场）：积分问题用它＝自己拼口径，**不要这样用**。
> **账本口径**（下节）已经写在那把报告工具的输出里——照着讲就行，不要另起一套说法。

## 口径（账本口径，与孩子端同源）
积分在系统里是**几条链路**，解释时按这个顺序讲，**不要只念流水行**：

1. **分两组算**：**家长排的（必做项：学习计划 + 生活计划）** 一组；**孩子自己排的（加分项）** 一组；考核再各算一组（每天最多四条结算行）。
2. **档位命中**：每组的完成率 / 得分率**按区间命中档位**（档位与分值在「积分」界面配置），按档加减分——这是"为什么加/扣这么多"的直接答案。
3. **门槛（只卡"孩子自排的加分项"）**：当天**家长组达标**（计划完成率 ≥ 阈值、考核得分率 ≥ 阈值）才解锁；**未解锁＝加分项结算 0 分，不等于扣分**——这是"她明明做完了为什么没加分"的标准答案。**家长组自己没有门槛**：完成率低于最低档会**真扣分**（除非家长把档位设成不倒扣）。
4. **结算时点**：**次日日终结算前一天**，**当天那一行只是实时进度**（还没评档、没发分）——家长问"今天怎么还没加分"，标准答案就是这条。
5. **兑换扣减**：兑换是**提交那一刻就扣分**并写进流水（类型「兑换」），同时生成一条**待发放**记录；**发放在对话里没有入口**（指路界面）。⚠️ 台账 §8 #5 记的产品口径原本写的是"扣分在家长履约时"——**与当前实现不一致**，以实现为准，遇到家长问起时如实说明。
6. **余额**：＝流水求和（每次结算后重算），不是各自累加的独立数字。

补充口径：
- **负分**：如实说清是哪几笔扣减叠加的结果（日期 + 原因 + 分值），不要用"表现不好"这类模糊说法。
- **"看过"不等于"学会"**：任务完成率只说明计划执行，不等于掌握。
- 数字前后要一致：先给结论（这周净变化多少），再逐笔解释。
- 家长说"批一下兑换"时：先说清"这个兑换**已经扣过 N 分**（提交时扣的）、现在只是没标已发放"，再指路界面；**不要**说成"还没扣分"。

## 参数速查（本场景工具）
- \`parent_child_points_report\`：\`child\`（孩子姓名；**名下只有一个孩子时可省略**）· \`days\`（看最近几天的结算，缺省 7、最多 30）→ 一次返回：余额 / 加分规则 / 逐日结算（完成率·漏项·档位·门槛·结算分）/ 最近流水 / 已提交未发放的兑换。**不要自己去算档位或漏项**，直接用它的输出。
- \`parent_list_children\`：无参数 → 只回**孩子姓名**（不确定是哪个孩子时先看这里）。

## 红线
${ironRules("A7")}
- **不替孩子勾完成、不改积分流水**：完成与结算只有系统能定（孩子的完成由孩子端确认）。
- **对话里没有兑换的写入口**（发放/扣分都不行）：涉及实际权益时先复述"已扣多少分、给的是什么权益"，再指路界面——**不要假装能替家长执行**。
- **档位与兑换目录的配置在界面**（家长侧没有对应工具）——家长问"怎么改档位/加一个奖品"时，指路界面。

## 结束
按"哪几天、因为什么、加 / 减多少分"讲清；数据不足的项明确说"这里没有数据"。`,
};
