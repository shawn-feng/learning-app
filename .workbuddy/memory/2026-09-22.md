# 2026-09-22 工作日志

## 部署 learning-server 0.5.4 → 201（07:07，用户「部署到201」；ISSUE-129 token 用量统计）

- **版本**：0.5.3 → **0.5.4**（`server/package.json` + `SERVER_VERSION` 同步）；`npm run build` 产物 24,357,671 字节，本地标记校验（0.5.4×8 / token_usage×9 / scanTokenUsageIntoDb×2 / token-usage/days×1）后上包。
- **流程**：`tmp/deploy/deploy_server_054.py` + `deploy_054.sh`（文件脚本 + 字面路径）——停服（~3s）→ bundle 备份 `server.cjs.bak-20260922-0707` → 数据快照 `data/backups/deploy-0.5.4-20260922-0707/`（server/agents VACUUM INTO + .secret/server-config/app-settings/license）→ 换包 → 起服。
- **验证**：`version`=0.5.4、health `{"ok":true,"db":"ok"}`、`token_usage`/`token_usage_files` 表启动自动建好、journal ERR_COUNT=0。
- **首次回填探针**（`probe_054_token.sh`：现场读 jwtSecret 用 node:crypto 手签 HS256 JWT → 打真端点）：回填 **1179 行 / 26 个会话文件**（09-15~09-21，201 上 agent-sessions 从 09-15 起）。**真实消耗首次可见**：09-21 家长助手 184 轮 input 132 万 + 缓存读 1813 万 = 1962 万 tok；珊珊主会话 80 轮 295 万；闻闻主会话 65 轮 121 万；cost 全 0（token-plan 包月，符合预期）。未登录 401 正确。
- **回滚**：`cp -a server.cjs.bak-20260922-0707 server.cjs && systemctl restart learning-server`（token_usage 表留着不碍事）。
- **遗留**：客户端 UI（重写的 TokenStatsPanel + preload/IPC）随下个客户端包发布——旧客户端包的 Token 面板仍调已删的 IPC 会显示为空/报错但不影响其它功能，家长端想看数据需发新客户端包；考核/worker 的 inMemory 会话不落盘扫不到（ISSUE-129 已记）。
- **文档**：部署文档 §12.4、MEMORY.md「当前已部署 0.5.4」、ISSUE-129 详情/索引行改已部署。

## web 前端同步到 201（ISSUE-129 客户端 UI，同日补）
- 0.5.4 部署只上了 server.cjs，201 托管的 web/dist 还是 09-17 旧构建（旧 TokenStatsPanel 调死 IPC 不可用）；本次把本地新构建（index-BN3Iin8Q.js，含 tokenUsageDays）同步上去：`/opt/learning-server/web/` root 属主 → shanshan 直写 EACCES → 上传 /tmp/webdist-054 + `web_swap_054.sh` sudo 换目录，旧目录备份 `dist.old-20260922`；静态按请求读盘即时生效，验证：首页引用新 hash、资产 200、bundle 含 tokenUsageDays。web 侧 shim 方法（sessions 域 tokenUsageDays/tokenUsageSessions）随本构建上线。
- 教训：**web/dist 是部署物的一部分**，纯服务端 issue 若涉及渲染层，部署要同时带 web/dist（本次漏了被用户问出来）。

## web 家长模式白屏修复（用户报障「家长界面白屏、孩子模式正常」→ 已修复 + 真浏览器回归）
- **根因**：家长 Dashboard 挂载即同步调 `window.api.parentReportGet()`（ISSUE-108，09-21 新增 preload 方法），而 **web shim 里该方法不存在** → undefined 调用在 useEffect 内同步抛 TypeError（`.catch` 拦不住）→ React 树崩 → 白屏。孩子端 Learn 不调它故正常。旧 201 web（09-17 构建）不含该调用，所以是「部署新渲染层后暴露的 shim 缺口」。
- **系统性排查**：多行感知正则比对「渲染层 window.api.* 使用集 vs web shim 定义集」→ 缺 16 个（含多行写法漏统计的）：parentReportGet、mistakesList/mistakeReport/mistakeAction、namespacesList/namespaceDecide/namespaceStatus、piStart/Prompt/ResetParentData（家长数据助手聊天）、wechatFeishu*/wechatBindings/wechatBindRequests/wechatBindDecide/wechatBindingRemove。
- **修复**：① shim 新增 `domains/ops.ts`（13 方法，形状逐一对齐 ipc-handlers 包装：{success, data}）；② agents.ts 补家长数据助手聊天三方法（sse.ts `ParentKind` 扩展 `"parent-data"`）；③ install.ts 注册 ops 域。比对复跑 = 缺口清零。
- **部署**：web/dist 重新构建并同步 201（index-BiRAcmS8.js，含 ops 域；旧版本备份 dist.old-20260922-r2）。
- **验证（真浏览器 E2E）**：本地隔离服务（8799+临时目录+web 托管）+ Electron 无 preload 探针（=纯 web shim 路径）走完整流程：注册→登录→身份选择→家长密码验证→家长中心完整渲染（dashOk）→ Token 用量面板打开（渠道筛选/空态正确）→ 6 个缺失 API 全为 function、无页面 JS 错误。browser-use CLI 在本机不可用（只吐 dotenv 噪音），改用仓库自带 Electron 写探针（无 preload = 等价纯 Chromium）。
- **教训**：渲染层新增 window.api 方法时必须同步 web shim（建议 CI 加「使用集 vs shim 定义集」比对测试）。

## ISSUE-132 题库管理优化（维度查询 + 家长界面新建/编辑，2026-09-22 实施完成）

- **需求**：家长界面题库按 主题/课程/知识点 维度查询；可添加/编辑题目——在某知识点/课程下新建（挂载），也可直接新建不关联。
- **服务端**：`assess-content.ts` 恢复 `BANK_LIST_LIMIT=10000`（ISSUE-074 修复被 F10-b 重构改回 2000 的回归，生产 3575 条只显示 2000）+ 新增 `listBankFacets/linkQuestionToKnowledgePoint/unlinkQuestionFromKnowledgePoint/deleteBankQuestion`；`routes/exam.ts` 挂 `GET /assess/questions/facets`、`POST /assess/questions/link|unlink`、`DELETE /assess/questions/:id`（均家长 JWT）。`bank_question_contexts` 注册路径 returns 增补 kpId/courses.uuid（agent 侧同享）。
- **客户端三层**：`assess-admin.ts` 五函数 → `ipc-handlers.ts` 五 handler（assess:bankFacets/questionSave/questionDelete/questionLink/questionUnlink）→ `preload.ts` 同名暴露；web shim `domains/assessment.ts` 同签名补齐（web-shim-coverage 通过）。
- **QuestionBankPanel 重构**：主题→课程→知识点级联筛选（facets 全量树 + contexts 客户端过滤）+ 关键字搜索；「＋ 新建题目」（表单 + 可选挂载级联，支持按名新建知识点；当前筛选落在课程/知识点时自动预选；不选=未挂载题）；详情页「编辑」（表单回填）+ 挂载增删（context ✕ 摘除 / 添加挂载级联）+「删除」（确认，事务清挂载）；列表项显示「未挂载」金标。
- **维度查询实现取舍**：列表接口每题已带 contexts，客户端级联过滤不改 API 形状（electron/web 双端零成本）；服务端分页/服务端过滤仍归 ISSUE-074 待办。
- **验证**：新测试 `test/issue132-question-bank.test.ts` 7 用例（facets/挂载定位与幂等/越课 kp 拒绝/摘挂载/删题清挂载/2100 条不被 2000 截断）全绿；相关回归（web-shim-coverage/db-channel/data-channel-v2/issue121/issue122/exam）55 过 1 挂——挂的正是 coverage 测试抓到我注释里 `window.api.assess*` 字样被扫描正则误匹配，改措辞后全绿；server tsc 0 错；electron-vite build + web build 通过；**HTTP 冒烟 14/14**（隔离实例 8799+临时数据目录：注册→login→facets→存题→挂载（courseId+新kp名 / topic+title+kpId / 跨课 kp 拒绝 400）→列表 contexts 带 ids→unlink→编辑保留挂载→delete→401）。
- **未部署**：服务端改动随下次 201 发版；客户端 UI 随下个客户端包。

## 重新讨论 app 学习设计需求（三环闭环）——现状对照结论（讨论阶段，未改码）

用户重述学习侧三大环节，要求先「理解需求」，并让我评估第 2 环记录完整性与第 3 环进度表。已读源码核对（未改动任何文件）：

- **第 1 环 内容域：判定已达标**。家长库 `parent.sqlite` 已是真源：`topics`（name/topic_key/**method 教学方法**/assess_method/method_spec）+ `courses`（topic/title/sort_order/**lesson_method**/**teaching_copy 教学文案**/**material·send_material 学习资料**/html_path/assess_rubric/uuid）+ `knowledge_points(course_uuid,name,detail,seq)` + `question_bank`(stem/answer/scoring/point_max/behavior/options) + 挂载表 `course_knowledge_questions(course_id,knowledge_point_id,question_id,seq,overview)`。层级=主题1:N课程、课程1:N知识点、知识点1:N题，与需求一致；家长 agent 已能管理（ISSUE-132 刚补 facets/挂载/删题）。
- **第 2 环 计划执行：考核达标（甚至超配），学习不达标**。
  - 考核：`exam_plans`（计划层 score/result/done_at/attempt_id）+ `exam_plan_courses`（**逐题**：course_uuid/course_name/knowledge_point_id/question_id/point_got/point_max/seq，plan-domain.applyExamAttempts 从 exam_attempts.per_question 回填）→ 比「到知识点」更细，可聚合出知识点级；`per_question` 由客户端提交（routes/exam.ts POST attempts），`knowledgePointId` 可能为空（LLM 现出题未挂库时）→ 分知识点统计有漏数风险。
  - 学习：**只有 `study_plans.result` 一句纯文本**（"学习完成"，plan-domain.applySignals 写死）+ 完成信号 = `daily_entries` 里 block='学习' 且 title==课程名（raw 自由文本）。**无课程概要、无知识点级记录**，即第 2 环的学习半边基本空缺。
- **第 3 环 分析汇总：全新建**。现状只有两个**视图**：孩子库 `course_progress`（课程级，仅 lastExamAt / lastExamRate / lastLearnedAt）、`topic_progress`（主题级课程计数 learned/total/next）；无知识点级、无持久化、无「教学建议」。现成信号源：`mistake_book`（带 knowledge_point_id + count + status，天然知识点级薄弱）、`exam_attempts.reinforce_plan`（focus[]/planReviewAt/aiSuggestion，仅被 latestReinforcePlan 浅消费）。**scheduler 里没有任何「汇总分析」WorkerTask**（registerTask 只有 recording / autoNewSession；plan-stat 是 scheduler 内直调）。
- **待用户拍板的 5 点**：①学习结果谁产生（LLM 从学习会话抽取 vs 孩子端结构化上报）与粒度；②「只记录在当前计划里」的确切语义（结果行必须带 plan_id？软删后历史是否保留）；③掌握度口径（单次/累计/衰减 + 等级划分 + 是否并入学与考）；④分析结论是否允许自动写回教学方案（改 method_spec/lesson_method）还是仅作建议注入；⑤范围是否含主题级汇总。
- 用户下一步：确认理解 → 出设计稿（尚未动手实现）。

## 掌握闭环设计稿产出（同日补：用户拍板 5 点 → 出稿，未实施）

- **用户决策**：D1 新建独立「知识点掌握情况表」，用 `knowledge_points.id` 挂到本次计划上，**学习与考核共用一张表按 source 分类**，考核不另建知识点明细；D2 计划完成后不再删除；D3 掌握度=**累计 + 自然语言描述**（最开始/中间/最新）；D4 **不改教学方案**，建议单独存一处、下次教学读取发给 LLM；D5 只做课程级 + 知识点级。
- **产出**：仓库根 `DESIGN-mastery-loop-2026-09-22.md`（已进 MEMORY.md 核心文档表）——3 张新表（孩子库）`knowledge_point_records`（流水，UNIQUE(source,plan_id,kp_id) 幂等）/ `knowledge_point_progress`（知识点累计：mastery_desc+level+计数）/ `course_mastery`（课程进度：累计描述 + **teaching_advice** + weak_knowledge_json）；计划表加 `result_summary` 列（课程层概要，不动旧 `result`）；新增 WorkerTask `progressAnalysis`（默认启用、默认 21:30、catchUp=latest，幂等判据=「done 且 records 无该 plan_id」→ 支持补跑与历史回填）；消费侧三处（家长 agent 读工具 / `kb.courses.get` 扩展 + 课程子会话 systemPrompt 注入 / 家长界面）；P0~P5 顺序（P2 考核侧规则聚合不依赖 LLM，上线即有进度数据）。
- **关键取舍（待用户确认，§7）**：新表命名 `course_mastery` 而非接管现有 `course_progress` **视图**（同名接管需 DROP VIEW→CREATE TABLE，会有"视图已删表还空"的口径真空期）；`exam_plan_courses` 逐题表保留作不可变审计/重算源，知识点结论只进 records。
- **D2 调查结论（源码核实）**：会删，且 **2 处硬删无状态保护** —— `agent/parent-plans.ts:527`（`parent_study_plan_update` act=delete）与 `routes/study-plans.ts:443`（`DELETE /api/v1/study-plans/:id`），家长可硬删已完成学习计划；已合规的：孩子侧两处 delete（plan-tools.ts:658/713）有 done 拦截、家长考核侧 `parent_exam_plan_cancel` 软删+done 拦截；`expireAndCarry` 只改状态+插顺延行不删行。**records.plan_id 依赖计划行 → 硬删会让历史记录失联，保护是闭环必要条件。**

## 掌握闭环设计稿 v2（同日第二轮修订：用户答 6 点 → 三处结构性调整）

用户对 §7 待确认项的答复：**①课程概要**「可以，但考核应记 exam_plan_courses —— 考核计划=一次考核、含多门课，考核结果要**分课程**记录，知识点绑课程」；**②**「**孩子库 courses 就是记录孩子学习进度的**」；**③**「exam_plan_courses **要保留**，作为**考核结果的记录表**」；④老数据 kp_id 为空→只记说明 ✅；⑤按课程一次批量 LLM ✅；⑥分析时间**家长可配置**。

据此 `DESIGN-mastery-loop-2026-09-22.md` 改为 v2（8 项决策 D1~D8），三处结构性调整：

- **课程进度不新建表** → 落**孩子库 `courses` 表加列**：`mastery_level` / `mastery_desc` / `teaching_advice` / `mastery_updated_at`（D6）。⚠️ 关键坑：9/10 重构删掉的旧列 `mastery/exam_mastery/first_learned` 现在仍被 `db/kb.ts:dropLegacyCourseColumns()` **幂等删**，新列用 `mastery_*` 前缀且必须把新列名**排除出删除名单**，否则下次启动被误删。`course_progress` 视图保留（时间/比率实时口径），不 DROP VIEW。
- **考核结果分课程记录** → `exam_plan_courses` 正式定为**考核结果记录表**（保留 + 加 `course_summary` 列，按 (plan_id,course_uuid) 分组同值）；知识点级结论才进 `knowledge_point_records(source='exam')` 且必须带 `course_uuid`（D7）。**`exam_plans` 不加课程级 result_summary**（一次考核多课程），只有 `study_plans.result_summary`（一课一行天然课程级）。
- **分析任务家长可配**（D8）→ 走既有「定时任务」体系：`SchedulerTaskType`/`SCHEDULER_TASK_TYPES` 加 `progress_analysis` + `buildEffectiveChildConfig` 解析 `analysis{enabled,times[]}` + `schedulerTaskTypeFor` 映射 + 首次幂等插一条 @21:30 启用任务行 + 客户端下拉加项。
- 保留：`knowledge_point_records` + `knowledge_point_progress` 两张新表、`progressAnalysis` 幂等判据（done 且 records 无该 plan_id → 支持补跑/回填）、P0~P5 顺序（P2 考核侧规则聚合不依赖 LLM）。
- 状态：**v2 待用户确认（§7 剩 5 项）**，尚未改任何代码。

## 掌握闭环设计稿 v3（同日第三轮：知识点拆出 + 默认建任务 + 两处说明）

用户第三轮答复：①「**可能要把知识点从 exam_plan_courses 里拆出来**，放到知识点掌握情况表，再与 exam_plan_courses 做关联，关联用 **plan_id + course 的 uuid**」；②默认建 ✅；③要 LLM 成本**详细举例**；④要说明**新列名各记什么**；⑤确认。

`DESIGN-mastery-loop-2026-09-22.md` → v3（9 项决策 D1~D9，文末加变更记录表）：

- **D8 新增（结构核心变更）**：知识点维度从 `exam_plan_courses` 拆出，**只落 `knowledge_point_records`**；两侧关联键 = **`plan_id` + `course_uuid`**。具体：`exam_plan_courses` 加 `course_summary` + **`DROP COLUMN knowledge_point_id`**（不可逆，迁移顺序＝备份 → 按 (plan_id,course_uuid,kp_id) 聚合回填 records → 校验行数 → 才删列；可选保守做法＝先停写保留一版本，见 §7①）；records 的 UNIQUE 改为 `(source, plan_id, course_uuid, knowledge_point_id)` 并加 `idx_kpr_link(plan_id, course_uuid)`；题目↔知识点映射的真源是家长库 `course_knowledge_questions`（`applyExamAttempts` 改造后：kp_id 缺失要回退查它）。
- **§2.3 补列含义说明表**：`mastery_level`（枚举档位，机器用）/ `mastery_desc`（课程级累计叙述，最开始→中间→最新）/ `teaching_advice`（下次教学建议，进 systemPrompt，不改教学方案）/ `mastery_updated_at`（刷新时间戳），各带示例值；并明确比率与时间**不重复存**（继续用 `course_progress` 视图）。
- **§3.3 补 LLM 调用详细举例**：一天 2 学习计划 + 1 考核（含 2 课）→ 去重 3 门课 → **当天只调 3 次**；同一天同课既学又考也只调 1 次，无学习/考核则 0 次。给出单次完整输入构成（知识点清单 + 本次学习素材 + 本次考核素材 + 历史记录摘要 + mistake_book 证据 + 输出 JSON 契约）与量级估算（单次输入 6~8k / 输出 1~1.2k token；3 门课合计 ≈2~2.4 万 / 3~3.6k），对照 09-21 家长助手实测（input 132 万 + 缓存读 1813 万）≈ 不到 2%；并说明为何「按课程一次」优于「每知识点一次」（后者要重复塞上下文、调用数 3 倍以上）。
- **D9**：默认建任务行 ✅（名字拟「学习情况分析」@21:30，家长可改/停用）。
- 实施顺序改为 **P0（建表加列）/ P0-b（知识点拆出迁移）/ P1（硬删保护）/ P2~P5**；验收标准加入"拆出迁移无知识点丢失 + (plan_id,course_uuid) 能正确关联"。
- **§7 剩 4 项待点头**：①拆列方式（直接 DROP vs 先停写一版本）②`course_summary` 落在逐题行同课程同值可否 ③默认任务显示名「学习情况分析」 ④新列名合意。
- 仍未改任何代码。

## ISSUE-133：数据通道工具参数被「整串 JSON 序列化」导致校验硬失败（已修复，未上 201）

用户报 `parent_db_write` 报错：`rows: must be array / must be object / must match a schema in anyOf`，
实参里 `rows` 是字符串 `"[{\"status\":\"done\",\"result\":\"done\"}]"`（op=update，child=珊珊，
study_plans，where id=ba4180ab）。

**根因两层（实证）**：① 模型对 anyOf 联合参数（`Type.Union([数组, 对象])`）偶发整串序列化成字符串——
已核实**不是运行时**干的（`pi-ai/dist/api/openai-completions.js` 只 `parseStreamingJson(partialArgs)`
一次，全仓无嵌套 stringify 路径）；② SDK `pi-ai/dist/utils/validation.js` 的 `validateToolArguments`
只跑 TypeBox `Value.Convert`（标量转换）→ `Compile().Check()`，字符串变不回对象/数组 ⇒ 校验在
**execute 之前**硬失败，模型只拿到 schema 报错、无法自愈。本地用 typebox 1.3.12 精确复现三条报错。

**修复**（同 ISSUE-122 思路：执行器兜底才是治本）：
- 新增 `server/src/agent/tool-shapes.ts`：`WriteRowsParam`/`JsonObjectParam`/`JsonStringArrayParam`
  （anyOf 追加 `Type.String()` 分支，描述写明「直接传结构、别传字符串」）。
- `db-channel.ts` 新增 `parseJsonArg`/`coerceObjectArg`/`coerceColumnsArg`，接入 `normalizeWriteRows`、
  `executeWrite`（含审计 where）、`executeRead`、`buildPathQuery`（路径读，失败抛出→上层转可读文案）、
  `tier2Write`/`tier2Read`；`WriteRequest`/`ReadRequest`/`PathReadRequest`/`Tier2WriteRequest` 相关字段放宽 `unknown`。
- `parent-tools.ts`（read/write 两工具 + 重嵌入取主键改用 parse）、`child-db-tools.ts`（read/write）接线。

**验证**：新增 `test/issue133-json-string-args.test.ts` 14 用例全绿（真实工具对象 + SDK 同款校验 + 真写库）；
相邻回归 `db-channel*`/`data-channel-v2`/`issue122`/`issue128` 61 用例全绿；server tsc 0 错；
`scripts/build.mjs` 通过且 `dist/server.cjs` 含新符号。文档已同步 `技术实现文档` §8.3 + §17 #19。

**遗留**：`courses`/`titles`/`require`/`exclude`/`enumValues` 等裸数组/Record 参数**未覆盖**（同类风险），
候选方案＝自建 `defineTool` 包装器对任意 schema 递归加 string 分支 + execute 入口递归 parse（ISSUE-134 提案，待用户拍板）。
**未部署 201**（待用户同意；部署需把 server 版本 bump 0.5.4 → 0.5.5）。
