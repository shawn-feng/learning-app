# 2026-09-12 工作日志

## 诊断 + 登记：家长上传文件后家长 agent 读不到（ISSUE-078）
- 现象：家长聊天上传文件后，家长 agent 第一轮读不到【附件文件】标记里的文件，第二轮（ls 猜全路径）才找到。
- 根因 A（直接）：`src/components/ParentChatPanel.tsx:241` `toRel` 把 `parents/<pid>/` 前缀剥掉 → 标记成 `uploads/xxx`；但家长 agent cwd=`data/`（`pi-session.ts:1035/1058/1099/1116`），实际去读 `data/uploads/xxx`（不存在）。孩子端 `Learn.tsx:1177` 同款处理正确（孩子 cwd=`data/children/<id>`）。
- 根因 B（连带）：`ParentChatPanel.tsx:337` 渲染 `<ChatWindow owner="parent">` 未传 `parentId` → `ChatWindow.tsx:178` `pid=parentId||"default"` 恒为 `"default"`，家长聊天上传全落 `data/parents/default/uploads/`，未按登录家长隔离（`config.ts:37-38`「default 已废弃」注释已与事实不符）。
- 磁盘实证：`data/parents/` 下含 `86a84278-…`（登录真实 id）、`default`、`_guest` 三目录；`default/uploads/` 真有 10 个文件（含刚传的「论语原文.txt」「教学文案生成要求.txt」、mp4、index.html）；`.session.json` 的 `parentId=86a84278-…`。
- 修复建议：①最小修 `ParentChatPanel.tsx:241` `toRel` 改为透传（不剥前缀），图片标记同修；②隔离修 `ParentChatPanel.tsx:337` 给 `<ChatWindow>` 传 `getCurrentParentId()`；③清理 `config.ts`「default 已废弃」旧注释。
- 索引：`ISSUES.md` 加 078 行，计数 76→78；详情 `ISSUES/ISSUE-078.md`。本次仅登记未改代码。
- 待确认（实施时）：历史 `parents/default/uploads` 文件是否迁移到登录家长目录；`parent/default` 是否继续作兜底目录。

## 登记：家长 agent 无法整理/治理服务端课程学习资料（ISSUE-079）
- 现象：让家长 agent「整理课程学习资料」时，文件在服务端、agent 无法操作，学习资料治理能力缺失。
- 根因：SPLIT 下课程资料真源在服务端；但家长 agent 工具集（`pi-session.ts:1069/1121`）只暴露 `parent_upload_material`（上传）一个碰服务端的材料工具，`move_file`/`copy_file` 仅作用本地 `data/`；缺 list/read/delete/move 的 agent 工具封装（grep 确认全代码库无 `parent_list_materials`/`parent_read_material`/`parent_delete_material`/`parent_move_material`）。底层函数与 IPC 其实都在（`parent-library.ts` L37/788/1026/46、preload.ts L244-253、ipc-handlers.ts L573-627）——只差包成 agent 工具这最后一公里。
- 建议方案 A（推荐）：把服务端 list/read/delete 封装成 agent 工具（仿 `parentUploadMaterialTool`，注册进 `pi-session.ts` 两个会话的 customTools）；move 先用「下载+重传+删旧」组合，可选给 cloud-service 加 `POST /materials/move` 端点；同步修正 `pi-session.ts:211` 提示词（本地工具只管临时文件、真源整理用 parent_*）。方案 C（资料真源回本地）不推荐作本 issue 解法。
- 索引：`ISSUES.md` 加 079 行，计数 78→79；详情 `ISSUES/ISSUE-079.md`。本次仅登记未改代码。
- 待确认：delete 是否强制先问家长+列出 dryRun；是否新增服务端 move 端点；read 是否限类型/大小；与 ISSUE-078 联动（先修上传落登录家长目录再整理）。

## 19:00 处理 ISSUE-078（parent/default）✅ 已实施
- 根因 A 修复（找不到文件）：`ParentChatPanel.tsx` `toRel` 改为全路径透传（不再剥 `parents/<pid>/` 前缀）——家长 agent cwd=`data/`，`saveParentUpload` 返回的 `parents/<pid>/uploads/xxx` 即正确路径；图片/文件标记共用一并修好。
- 根因 B 修复（隔离失效）：新增只读 IPC `session:get_parent_id`（ipc-handlers.ts，`getCurrentParentId()` 原已 import）+ preload `getSessionParentId()`；`ParentChatPanel` mount 时取登录家长 id 存 state，`<ChatWindow parentId={parentId}>` 透传，上传/打开/读取落 `data/parents/<真实pid>/uploads/`；未登录时 ChatWindow 仍兜底 `default`。
- 文档纠偏：`config.ts` 「default 已废弃」注释改写为「default 是未显式传 parentId 时的兜底落盘目录」（与 parent-library/delivery 现状一致）。
- 裁决：历史 `parents/default/uploads/` 文件**不迁移**（单机单家长）；`default` 保留为兜底目录，多家长上线再评估。
- 验证：`electron-vite build` 全绿（main 42s + preload + renderer 14s，仅既有 chunk 警告）。注意：本 workspace `node_modules/typescript/lib` 缺失全部 `lib.*.d.ts`（既有环境问题），tsc 独立类型检查不可用。
- 待用户实测：登录家长上传 txt → agent 第一轮直接读到；文件落真实家长目录。

## 19:12 讨论：ISSUE-079 方案对齐 + ISSUE-080 登记（agent 迁 server 评估）
- ISSUE-079 讨论：确认根因与方案 A；补充要点——先修 078（已完成于上方 19:00）、move 组合须先传新后删旧（最坏留重复不丢数据）、delete 建议 confirm/dryRun+activity log+路径白名单、read 限流（文本≤200KB、音视频只返元数据）、pi-session 两处工具数组建议提取共享常量。4 项待用户拍板（move 端点/confirm 方案/常量重构/顺序）。
- ISSUE-080 新增（设计讨论）：评估「agent 从客户端迁 server 端、客户端专注显示」。结论=**分治**：孩子 agent 留客户端（display_content/TTS/录音/语音评测/浏览器预览等工具执行面天然在客户端，硬迁需工具回传协议、复杂度高）；家长 agent 中期可迁（工具面 ~80% 是服务端数据操作，正是 078/079/055/056 整类跨机 impedance 的根源；ISSUE-028 worker 的 pi runtime 已在服务端跑通）。判据：agent 位置由「工具执行面」决定而非「数据真源」。迁移前 079 仍按方案 A 落地（工具函数将来 server 端可复用）；可考虑通用 serverFS 工具（list/read/write/delete/move + parentId 路径沙箱）替代 parent_* 系列膨胀。迁移前置：客户端真源盘点上收、HTTPS/token、per-parentId 会话权威翻转、server 不可达显式降级。
- 索引：`ISSUES.md` 加 080 行，计数 79→80；详情 `ISSUES/ISSUE-080.md`。

## 19:22 ISSUE-080 结论修订：孩子端确认需要 web/手机多设备
- 用户新信息：孩子也要通过 web/手机/其他设备使用系统 → 多端从「家长中期可能」变为「孩子端确定需求」，触发原「长期才评估」的孩子 agent 迁移条件。
- 修订结论：**server agent 成为战略目标架构（家长+孩子都迁）**。关键推演：孩子 agent 的「客户端工具」大多可重归类——display_content=输出推送（server 材料存储+多端订阅）、TTS=server 合成+客户端播放、语音输入/评测=输入事件（上传+server ASR/API）、H.264 转码=server 更合适；真正设备绑定的只剩 page_inspect 类少数，用「设备能力协商」（客户端连接上报能力集，server 按能力集裁剪工具表）覆盖，不需要全量双向回传协议。
- 分阶段路线：P0=079 方案 A 落地；P1=会话权威上移 server（028 sessions 镜像翻转为权威，多端 attach 地基）；P2=家长 agent 先迁（worker runtime+SSE 交互端点）；P3=孩子 agent 迁（输入/输出重归类改造）。可用性贯穿：ECS 作外网中继/备份入口，server 不可达显式降级。
- 新增前置/风险：learning-server 需补 SSE/WS 流式端点；多设备会话锁语义；web 端资料渲染（iframe 沙箱有 MATERIAL-BRIDGE 底子）；HTTPS 升为硬前置（孩子语音出 LAN）；token/key 成本收敛 server；learning-guard/会话树语义平移 + childId 隔离体系 server 侧重做（023 教训对照）。
- ISSUE-080.md 追加「五、补充讨论」节 + ISSUES.md 行更新；核心判据不变（工具执行面决定 agent 位置）。

## 19:35 ISSUE-080 二次评估（代码实测版）
- 实测底数：`electron/lib` 44 文件 ≈16.3k LOC；`src` 53 文件 ≈17k LOC，**0 处 import electron**，全部经 398 行 preload 契约（280 处 window.api / 40 文件）；`server/src` 36 文件 ≈10.0k LOC（Fastify REST+multipart）。
- 有利发现：①`window.api` 即现成的客户端-服务端契约 → web 端只需 HTTP+SSE 版适配层即可复用 17k 渲染代码（仅 TitleBar 等替换）；②领域逻辑过半已在服务端（考核 1702/计划域 566+419/材料 246），server agent 可直调 handler（worker/kb-tools 已有先例），比现状少一跳；③prompt 已由服务端下发（fetchAgentPromptRemote）；④音频侧已是 web 标准技术（useAudioRecorder MediaRecorder、exam-template iframe getUserMedia+allow-same-origin），手机浏览器可跑；TTS 走 edge-tts（API 型）可服务端合成。
- 服务端缺口：①无持久会话（worker 用 SessionManager.inMemory，tasks.ts L111）+会话表仅客户端权威镜像；②无 SSE/WS（index.ts 只注册 REST）；③worker runtime 仅 58 行 vs 客户端 pi-runtime 401 行；④无 learning-guard/无课程级会话语义（客户端 4 类会话+buildChildPrompt 需平移）；⑤孩子 20 工具（pi-session L761）中 read/write/edit/ls 需 server 作用域版、display_content 需改建推送、page_action/page_inspect+scene_command 设备绑定走能力协商。
- 工程量：P1 会话权威+持久化+SSE ~1.5–2k；P2 家长 agent ~1–1.5k；P3 孩子 agent ~2–3k；P4 web/手机客户端 ~1.5–2k；合计 +7–8k LOC。
- 结论：迁移技术成立、成本低于一版预期；但它是「孩子多端」的宿主前提，不是独立架构理由。可选过渡 P0.5 只读 web 伴侣（资料/进度/记录/回看，无交互）。不建议双模并存（两套工具表漂移，056 教训）。
- 强烈建议前置重构：抽共享 agent 核心包（会话/运行时/prompt/工具表）——坏味道已现：`server/src/worker/recording-prompt.ts` 是客户端同源副本（130/139 行）。
- 待确认：`/materials`、`/sessions` 路由是否强校验 parentId。

## 19:55 ✅ 定案：agent 全量上移服务端（server-only agent）
- 用户拍板三条：**不要过渡方案；agent 只在 server 端；client 不再有 agent**。连带：客户端不再持有 apiKey；server 不可达时显式报错禁用对话（不做本地兜底/不静默回退）；接受单点风险（不做双模）。
- 实测客户端 agent/LLM 调用点 **9 处**（迁移影响面准确定义）：pi-session L745（孩子）/L856（场景）/L1057（家长）/L1115（家长 content）、exam-engine L72/L208/L440（选课 LLM/出题/判分）、daily-summary L159（上下文压缩）、parent-vision L82（图片理解）、programming-agent L97（ISSUE-020）。
- 新增设计文档 `DESIGN-server-agent-migration-2026-09-12.md`（仓库根）：目标架构图、职责边界表 + 三条红线（不双跑/不静默降级/隔离不放松）、接口契约（SSE 流式 + prompt 入口 + 能力协商 caps + Last-Event-ID 重放 + 单活动会话与接管）、共享包 `packages/agent-core`（sessions/runtime/prompts/guard/tools/paths，禁 import electron、禁自拼路径）、P0 抽共享包 → P1 会话权威上移+持久会话+SSE（分水岭）→ P2 家长 agent → P3 孩子 agent（含 display_content 改建推送、exam-engine 上移、能力协商）→ P4 客户端瘦身 + web/手机端（HTTP+SSE 版 window.api 适配层）。
- 未决设计点 5 项待用户拍板：①page_inspect/page_action 何去何从（删 or 改为页面事件上报，建议后者）；②TTS 合成位置（建议 server）；③上课提醒（建议 server 下发时间+本地播放）；④旧客户端硬断代（建议是）；⑤programming-agent 是否随 P3。
- ISSUE-080 追加 §七定案；新增 **ISSUE-081（P1 分水岭，含验收 6 条 + 红线）**；**ISSUE-079 标记形态变更**——方案 A（客户端封装 agent 工具）废弃，改为 server 端直调 handler 并入 P2，其底层函数盘点与待确认项仍作 P2 输入。
- ISSUES.md 计数 80→81 并更新 080/081 行；项目记忆索引 MEMORY.md 加入新设计文档。

## 20:10 五项设计点全部定案 + 修正一处误判（资料页工具）
- 用户拍板：②TTS 用 server 合成；③提醒 server 下发时间+客户端本地播放；④硬断代；⑤programming-agent 也在 server 端、由 server agent 调用（不单独立项）。
- **修正误判（用户反问很有价值）**：早期把 `page_inspect`/`page_action` 归为「设备绑定工具需能力协商裁剪」是错的。真相：**资料源（html 真源）本就在 server**，server agent 可直接读源文件完成「内容感知」；孩子操作本就经 PiBridge 约定信封（`page:app` + 动作目录）上抛，`formatPageEvent`（`electron/lib/page-bridge.ts`）只是格式化成 agent 可读文本。当年走客户端的原因是**渲染态**在客户端，不是资料在客户端。→ **两个工具保留并上移，改传输层（IPC → SSE/HTTP），客户端仍作执行端点**，`src/lib/page-bridge.ts`、`MaterialsPanel.appCmd` 基本不动。
  - 三层拆分：看内容=server 直读材料源；知道孩子做了什么=PiBridge 事件经 events 通道上行（互动摘要 server 侧存储）；改页面/读实时 DOM=SSE 下发 `page_cmd` → 客户端 `appCmd` → iframe → 回执上行（requestId 配对）。实时 DOM 快照为可选降级。
  - `caps` 只用于真实设备差异（mic / electron / material-panel），不再用于裁剪 page_*。
- 文档同步：`DESIGN-server-agent-migration-2026-09-12.md` 新增 §4「资料页感知与操作（修正说明）」+ §2 职责边界表加资料页三层行 + §3 契约加 page_cmd 下行与 page_cmd_result 上行 + §5 共享包加 `bridge/` 模块；§6 P3 改为「传输改造 + programming-agent 上移」；§7 五设计点全部记定案；章节重编号。`MATERIAL-BRIDGE-PROTOCOL.md` 加 2026-09-12 修订行（拓扑变更、信封与动作目录不变）。ISSUE-080 §八 记录五定案；ISSUE-081 抽包模块 6→7、P1 加 caps 骨架、注明 page 传输属 P3。
