# 待解决问题清单 (Open Issues)

> 本文件为**索引**。每条 issue 的详情已拆分到独立文件：`ISSUES/ISSUE-XXX.md`（重复编号用 `-2` 后缀区分）。
>
> **2026-08-30 重置说明**：项目已切换至「客户端 + 服务端」拆分架构（SPLIT，见 `SPLIT-REQUIREMENTS.md` / `DESIGN-SPLIT.md`）。
> 原 ISSUE-001 ~ 052 为旧架构（一体化 Electron）时期记录，已整体归档至 `ISSUES-archive-2026-08-30.md`，不在本清单保留。
> 本清单只记录新架构下的问题。

> 共 **146** 条 issue（详情见 `ISSUES/` 目录）。

| 编号 | 标题 | 优先级 | 记录时间 | 详情 |
|------|------|--------|----------|------|
| 001 | 家长界面孩子管理：孩子卡片上恢复学习进度展示 | 已完成 | 2026-08-30 | [详情](ISSUES/ISSUE-001.md) |
| 002 | 家长设置·定时任务：去掉「会话重置」，用「自动新建会话」即可 | 已完成 | 2026-08-30 | [详情](ISSUES/ISSUE-002.md) |
| 003 | 家长设置·数据备份：改为服务端数据备份/恢复（zip 上传覆盖），去掉跨机进度查询 | 已完成 | 2026-08-30 | [详情](ISSUES/ISSUE-003.md) |
| 004 | 孩子管理·分配学习主题：支持移除某孩子的某主题，有学习记录则保留记录 | 已完成 | 2026-08-30 | [详情](ISSUES/ISSUE-004.md) |
| 005 | 家长模式：孩子卡片进度改为 icon 入口，点击进入详细进度看板（与孩子模式一致） | 已完成 | 2026-08-30 | [详情](ISSUES/ISSUE-005.md) |
| 006 | 学习进度：孩子模式点主题后看不到详细课程学习情况；两模式界面/操作需完全一致 | 已完成 | 2026-08-30 | [详情](ISSUES/ISSUE-006.md) |
| 007 | 孩子详情页：点击孩子卡片进入详情页（标签页组织进度/主题/prompt 等），不再用弹窗 | 已完成 | 2026-08-30 | [详情](ISSUES/ISSUE-007.md) |
| 008 | 孩子界面：学习资料展示区可折叠；display_content 调用时自动展开并展示最新内容 | 已完成 | 2026-08-30 | [详情](ISSUES/ISSUE-008.md) |
| 009 | 聊天消息 markdown：字体放大一倍便于孩子阅读；行间不留空行、保持正常行距 | 已完成 | 2026-08-30 | [详情](ISSUES/ISSUE-009.md) |
| 010 | 聊天消息输入框：语音输入（按住说话）按钮消失，需补回 | 已完成 | 2026-08-30 | [详情](ISSUES/ISSUE-010.md) |
| 011 | 学习资料 html 里的语音（朗读按钮）音色与聊天语音不一致，需统一 | 已完成 | 2026-08-30 | [详情](ISSUES/ISSUE-011.md) |
| 012 | 分配学习主题列表与课程管理不一致：课程管理 9 个主题，分配主题时不足 9 个 | 已完成 | 2026-08-30 | [详情](ISSUES/ISSUE-012.md) |
| 013 | 知识记录：Electron 里 speechSynthesis 为什么选不到 Edge 在线神经语音 | — | 2026-08-30 | [详情](ISSUES/ISSUE-013.md) |
| 014 | 孩子 agent 调用 page_inspect 多次失败，需定位失败档位 | 已完成 | 2026-08-30 | [详情](ISSUES/ISSUE-014.md) |
| 015 | 孩子页面操作不自动投递 agent，随下一轮消息附带发送 | 已完成 | 2026-08-30 | [详情](ISSUES/ISSUE-015.md) |
| 016 | 孩子界面：中间展示区折叠按钮仅学习资料页有，学习进度等页也需支持折叠 | 已完成 | 2026-08-30 | [详情](ISSUES/ISSUE-016.md) |
| 017 | 孩子界面：学习资料点击/选中不认识的字词，显示读音+释义（不修改资料 html） | 已完成 ✅ |  | [详情](ISSUES/ISSUE-017.md) |
| 018 | 孩子界面：每学完一课压缩当前会话历史，节省 token | ⏸ 暂缓 / 暂不处理 | 2026-08-31 | [详情](ISSUES/ISSUE-018.md) |
| 019 | 家长端按孩子设置课程时间段，上课/下课在 app 顶部 1/3 区域提醒（铃声 + 语音播报） | 已完成 | 2026-08-31 | [详情](ISSUES/ISSUE-019.md) |
| 020 | 孩子端左侧「切换展示页」浮层：鼠标移到选项框就消失、难选中 | 已完成 | 2026-08-31 | [详情](ISSUES/ISSUE-020.md) |
| 021 | 孩子端学习资料列表：课名显示"未命名" + 重发资料不刷新/卡在别的课程 | 已完成 | 2026-08-31 | [详情](ISSUES/ISSUE-021.md) |
| 022 | 测试债务：9 个测试文件未随 SPLIT 迁移更新，全量 vitest 稳定失败 39 例 | 低 | 2026-08-31 | [详情](ISSUES/ISSUE-022.md) |
| 023 | 孩子聊天框字号设为可调节设置项，入口放孩子左侧边栏 | 已完成 | 2026-08-31 | [详情](ISSUES/ISSUE-023.md) |
| 024 | 聊天框调宽手柄：当前是"点一下进入拖拽模式、再点一下退出"，应改成"按住拖拽、松手即停" | 已完成 | 2026-08-31 | [详情](ISSUES/ISSUE-024.md) |
| 025 | 孩子 Todolist：家长/孩子/agent 共建，定时生成+统计，边栏弹框查看，家长规定项不可改 | 已完成 | 2026-08-31 | [详情](ISSUES/ISSUE-025.md) |
| 026 | 孩子左侧边栏常驻折叠，所有交互统一改为点击 icon 弹框（不再内联展开） | 已完成 | 2026-08-31 | [详情](ISSUES/ISSUE-026.md) |
| 027 | 学习考核（全主观题语音作答 + 客户端出卷/判分 + 服务端存储 + v3 选课 LLM）——需求文档 `EXAM-REQUIREMENTS.md`，2026-09-01 实施完成 | 已完成 | 2026-09-01 | [详情](ISSUES/ISSUE-027.md) |
| 028 | 服务端增加 agent 功能：会话同步上云 + 无头 worker + 家长对话回顾（方案B） | 已完成 | 2026-09-01 | [详情](ISSUES/ISSUE-028.md) |
| 029 | 英语学科：复用现有科目体系 + 英文教学 + 教学后发音测评入 daily（终版 2026-09-04） | 需求终版已定(2026-09-04)，任务 1 已完成，任务 2-4 待实施。 | 2026-09-01（内容 2026-09-04 终版重写） | [详情](ISSUES/ISSUE-029.md) |
| 030 | 学习资料显示字号可调（孩子左侧边栏加「资料字号」按钮） | 已实施 | 2026-09-01 | [详情](ISSUES/ISSUE-030.md) |
| 031 | 查词浮层优化（拼音放大 + 多音字分行各有朗读 + 不显示意思） | 已实施 | 2026-09-01 | [详情](ISSUES/ISSUE-031.md) |
| 032 | 全链路加载态：登录 / 主页 / 内容未加载完都要显示「正在干什么」，加载完再显示页面 | 已实施 | 2026-09-02 | [详情](ISSUES/ISSUE-032.md) |
| 033 | 灵活学习计划：对话驱动的逐日排期表（study_plans 替换 rules_json.daily） | ✅ **实施完成 | 2026-09-02（2026-09-03 更新） | [详情](ISSUES/ISSUE-033.md) |
| 038 | 定时任务新模型：任务管理页（先创建任务 → 分配给孩子）+ 执行结果查询 | 已完成 | 2026-09-02 | [详情](ISSUES/ISSUE-038.md) |
| 034 | 聊天气泡 markdown 渲染空行过多、消息被拉长（需紧凑化） | 已实施 | 2026-09-02 | [详情](ISSUES/ISSUE-034.md) |
| 035 | 取消右侧聊天框可调节的最大宽度限制，宽度持久化到本地（重启 app 仍保留） | 已实施 | 2026-09-02 | [详情](ISSUES/ISSUE-035.md) |
| 036 | 聊天框上传应支持任意文件类型（JSON 等现被拦截） | 已实施 | 2026-09-02 | [详情](ISSUES/ISSUE-036.md) |
| 037 | 家长聊天框支持上传文件，落到家长 uploads 目录且家长 agent 能读取 | 已实施 | 2026-09-02 | [详情](ISSUES/ISSUE-037.md) |
| 039 | 家长 agent 会话历史退出再进不显示（落盘有、进入不加载） | 已实施 | 2026-09-02 | [详情](ISSUES/ISSUE-039.md) |
| 041 | 家长页面左侧边栏去掉「孩子列表」，添加孩子入口收进「孩子管理」 | ✅ 已完成 | 2026-09-02 | [详情](ISSUES/ISSUE-041.md) |
| 042 | 家长 agent 也要支持 /reset、新建会话等会话管理命令（对齐孩子端） | 已实施 | 2026-09-02 | [详情](ISSUES/ISSUE-042.md) |
| 043 | 生产环境珊珊（Mac 客户端）会话未同步上云：家长回看空白 + 服务端每日汇总跳过 | 高 | 2026-09-03 | [详情](ISSUES/ISSUE-043.md) |
| 043-2 | 现场取证 + 同步机制澄清（2026-09-03 登 201 `192.168.1.201`） | — |  | [详情](ISSUES/ISSUE-043-2.md) |
| 044 | 客户端 + 服务端统一日志系统（便于远程排查） | 中 | 2026-09-03 | [详情](ISSUES/ISSUE-044.md) |
| 045 | 孩子 agent 会话注入：去掉「学习进度概览」，改为注入当天学习计划（无 todolist 则不注入） | 中 | 2026-09-03 | [详情](ISSUES/ISSUE-045.md) |
| 046 | 学习考核 Ubuntu 客户端点「按住说话」录音按钮提示「没有权限」（Linux 特有媒体权限预检缺失） | P0 | 2026-09-03 | [详情](ISSUES/ISSUE-046.md) |
| 047 | 孩子端：让 agent 直接制定「定时任务」（到点语音提醒 + 频率设置） | ✅ 已完成 | 2026-09-04 | [详情](ISSUES/ISSUE-047.md) |
| 048 | 考核录音在 Ubuntu+Mac(Chromium143) 报 permission denied + 「下一题」点亮却点不动 | P0 | 2026-09-05 | [详情](ISSUES/ISSUE-048.md) |
| 049 | 家长端孩子管理：新增「每日记录(Daily)」标签页，左列条目 / 右显内容，默认最近 7 天 + 日期范围选择器 | 待定 | 2026-09-05 | [详情](ISSUES/ISSUE-049.md) |
| 050 | 考核出题改异步流式：首门课就绪即开考，其余课程后台生成并增量加入答题流 | — |  | [详情](ISSUES/ISSUE-050.md) |
| 051 | 会话同步：珊珊（本地环境）持续报 500 internal server error | 高 | 2026-09-05 | [详情](ISSUES/ISSUE-051.md) |
| 052 | 本地语音 ASR：默认千问 token-plan 报「千问识别失败：未返回识别文本」 | 高 | 2026-09-05 | [详情](ISSUES/ISSUE-052.md) |
| 053 | 定时任务·课程时间段：当天设置当天触发，过一天就丢失需重设 | 高 | 2026-09-06 | [详情](ISSUES/ISSUE-053.md) |
| 054 | 设计讨论：每个学习主题用独立会话+独立 system prompt（当天所有课程在该会话完成）——对省 token / 防主题串味是否有用？ | 待定 | 2026-09-06 | [详情](ISSUES/ISSUE-054.md) |
| 055 | 家长 agent 缺「上传资料到 server 指定目录」工具：自动制作课程资料后无法推上服务端真源 | 中 | 2026-09-06 | [详情](ISSUES/ISSUE-055.md) |
| 056 | 家长端 agent 制作学习资料注意事项：视频必须 H.264（禁用 HEVC/H.265）+ 必须走 upload 接口上服务端 | 高 | 2026-09-06 | [详情](ISSUES/ISSUE-056.md) |
| 057 | 家长端删除孩子：该孩子 uuid 目录（本地 + 服务端真源）未清理 | 高 | 2026-09-06 | [详情](ISSUES/ISSUE-057.md) |
| 058 | 201 生产环境：21:00 recording 任务在同一孩子会话里当天执行多次（珊珊 3 次 / 闻闻 2 次） | 高 | 2026-09-06 | [详情](ISSUES/ISSUE-058.md) |
| 059 | 课程时间表优化：按「模板 + 星期」分配（上学日 / 周末分别设置，周末可不固定） | 中 | 2026-09-07 | [详情](ISSUES/ISSUE-059.md) |
| 060 | 学习资料 iframe ↔ 主页面通讯：开放「作者可控」协议（特定操作才通讯 + 接口契约） | 中 | 2026-09-07 | [详情](ISSUES/ISSUE-060.md) |
| 061 | 场景化角色扮演学习：网页模拟真实场景，孩子在场景中扮演角色，用语音与场景内多个 agent 角色互动（v1 样例已实现并实测，持续迭代） | — | 2026-09-07（讨论，未开工） | [详情](ISSUES/ISSUE-061.md) |
| 062 | 设计讨论：用「会话树分支」让一个课程/主题成为 pi 会话树的一支，以节省 token 缓存消耗（prompt cache） | 低 | 2026-09-07（讨论，未开工） | [详情](ISSUES/ISSUE-062.md) |
| 063 | 审计所有工具的报错信息：是否详细、准确、能指导 agent 下一步（含「查课程找不到→建议先列全部课程名核对」） | 中 | 2026-09-08 | [详情](ISSUES/ISSUE-063.md) |
| 064 | 主页（选择身份）孩子列表溢出无滚动条：孩子多了显示不全 | 中 | 2026-09-08 | [详情](ISSUES/ISSUE-064.md) |
| 065 | 选课机制重构：固定档必学全考/自定义须课程/下线长周期档（2026-09-09） | — | 2026-09-09 | [详情](ISSUES/ISSUE-065.md) |
| 066 | 家长 agent 课程内容职责增强：考核内容(rubric)/考核方法(assess_method)编写能力 + 格式约定送达（需求记录，后续处理） | ✅ 已实施 | 2026-09-09 | [详情](ISSUES/ISSUE-066.md) |
| 067 | 考核内容结构化：题型目录 + 类型块(题干+内嵌评分,DB真源) + methodSpec 按孩子选类型（设计定案，待实施） | 待实施 | 2026-09-09 | [详情](ISSUES/ISSUE-067.md) |
| 068 | 停止 agent 后仍报 "Agent is already processing"：会话中止后重发消息被 SDK 重入守卫拦截（bug，已修复） | 中 | 2026-09-09 | [详情](ISSUES/ISSUE-068.md) |
| 069 | 自动化测试客户端：用真实对话驱动孩子/家长 agent，断言行为与工具调用是否符合预期（测试框架讨论，待实施） | 中 | 2026-09-10 | [详情](ISSUES/ISSUE-069.md) |
| 070 | 背诵/跟读题：一次录音后题目即被锁定（变「完成」），无法多段录音拼接 | 已实施 | 2026-09-11 | [详情](ISSUES/ISSUE-070.md) |
| 071 | 家长可撤销孩子某一条积分变动记录（流水备注名「撤销」，撤销后重算余额） | 中 | 2026-09-11 | [详情](ISSUES/ISSUE-071.md) |
| 072 | 自定义考核计划需有「标题名」，家长 agent 创建时必须填入（区分 note 给孩子的说明） | 中 | 2026-09-11 | [详情](ISSUES/ISSUE-072.md) |
| 073 | 考核内容模型切换：类别(topic_categories) → 知识点(knowledge_points)（数据已就位，差代码切换；改 API 形状，须与客户端同批） | 中 | 2026-09-11 | [详情](ISSUES/ISSUE-073.md) |
| 074 | 家长端列表静默 LIMIT 截断（题库 2000→3575 只显示 2000 已修；排期 100→1000 已修；记录 60 待评估；题库需分页/过滤） | 中 | 2026-09-11 | [详情](ISSUES/ISSUE-074.md) |
| 075 | 客户端推送空「固定档频率」→ 服务端**无作用域 DELETE** 掉未来排期（生产 136→18 行；已清空排期并暂时关闭考核）——**✅ 已随 ISSUE-094 彻底解决（2026-09-14）：exam_schedules 排期表已取消（DROP TABLE），无作用域 DELETE 代码一并删除，风险根源不存在** | ✅ 已解决（2026-09-14，随 ISSUE-094） | 2026-09-11 | [详情](ISSUES/ISSUE-075.md) |
| 076 | 学习计划完成判定只认 `courses.last_review`（唯一写入方是 AI 手写），`last_review='-'`/无日期的课永远判不了完成（珊珊 383/613 门；todolist 7 条 pending 全因无日期） | **高** | 2026-09-11 | [详情](ISSUES/ISSUE-076.md) |
| 077 | DeepSeek 官方模型名变更：`deepseek-v4-flash` 已下线应改 `deepseek-flash`（官方直连通道 SDK；百炼套餐快照名待核实） | 中 | 2026-09-11 | [详情](ISSUES/ISSUE-077.md) |
| 078 | 家长上传文件后家长 agent 读不到：【附件文件】标记路径被错误剥 `parents/<pid>/` 前缀 + 聊天未透传登录家长 id（全落 `parents/default`） | ✅ 已实施 | 2026-09-12 | [详情](ISSUES/ISSUE-078.md) |
| 079 | 家长 agent 无法整理/治理服务端课程学习资料：缺 list/read/delete/move 的 agent 工具封装（仅暴露上传） | 高 | 2026-09-12 | [详情](ISSUES/ISSUE-079.md) |
| 080 | 设计讨论：agent 从客户端迁到 server 端——**✅已定案：不要过渡 / agent 只在 server / client 零 agent；5 项设计点全部拍板（page_* 保留并上移、TTS server 合成、提醒本地播放、硬断代、programming-agent 上移）** | **高** | 2026-09-12 | [详情](ISSUES/ISSUE-080.md) |
| 081 | agent 服务端化：共享包 + 会话权威 + SSE + 家长/孩子/考核/编程 agent 上移 + 客户端零 agent——**✅ P0~P4 已实施（服务端 0.4.0，server_agent/parent_agent/exam_agent 特性；本地 agent 代码已删）** | 高 | 2026-09-12 | [详情](ISSUES/ISSUE-081.md) |
| 082 | 家长 agent「parent-content」会话：注释声称"专门提示词、与通用助手解耦"，实际与 parent 共用同一 `buildServerParentPrompt`，解耦仅限会话实例/历史/流层面，提示词层面未真正解耦 | 待定 | 2026-09-13 | [详情](ISSUES/ISSUE-082.md) |
| 083 | agent 提示词不再对用户开放编辑与查看：仅存于系统内部，孩子端 prompt 因含系统信息更不可展示，收敛 AgentPromptEditor 入口 | ✅ 已解决（2026-09-13） | 2026-09-13 | [详情](ISSUES/ISSUE-083.md) |
| 084 | agent 工具/后端方法命名未体现目标库表，可读性差（`parent_plan_create` 实际写孩子库 life_plans，名称看不出落库）；建议统一"库简写_表名_动作"命名规范 | ✅ 已解决（2026-09-13） | 2026-09-13 | [详情](ISSUES/ISSUE-084.md) |
| 085 | 考核场次无删除/取消操作：孩子库 `exam_plans` 全仓无 DELETE/cancel 端点（既无 agent 也无 REST），一旦生成永久留存；主库 `exam_schedules` 删除仅限 pending 状态（误排进行中无法撤） | ✅ 已解决（2026-09-13） | 2026-09-13 | [详情](ISSUES/ISSUE-085.md) |
| 086 | 添加孩子表单密码标签「登录密码（仅存本地）」与实际实现不符：密码 bcrypt 哈希经 `POST /children` 上传并存入服务端 `children.profile_json`，登录由 `POST /children/auth` 服务端校验（多设备共享），本地仅离线回退——文案误导用户隐私预期 | ✅ 已解决（2026-09-14） | 2026-09-13 | [详情](ISSUES/ISSUE-086.md) |
| 087 | 家长 agent 缺「添加/编辑孩子」能力（仅有只读 `parent_list_children`）；需求定调：应能添加、编辑孩子，**不应**提供删除（删除仍由家长 UI 走 REST）；另需决定 `max_children` 上限校验是否上移服务端（现仅客户端校验，agent 创建会绕过） | ✅ 已解决（2026-09-14） | 2026-09-13 | [详情](ISSUES/ISSUE-087.md) |
| 088 | agent 上移服务端后客户端遗留死轮询/死接口：5min 会话同步仍打**已删除**的 `POST /sessions/:childId/sync`、10min `/version` 探测无消费者（`hasServerFeature` 全仓无调用）、`app-config.ts` 孤儿模块、云端 eventPoll 每 2min 打旧消息交换通道（ISSUE-041 遗留）；另 `app_settings` 客户端只剩 `materialsLimit` 有消费方 | ✅ 已解决（2026-09-14） | 2026-09-13 | [详情](ISSUES/ISSUE-088.md) |
| 089 | `parent_upsert_topic` / `parent_upsert_course` 已定义、已进 `PARENT_AGENT_TOOL_NAMES` 白名单，但**未加入 `createParentAgentTools` 的 return 数组** → 白发白名单、工具实际不可调；家长 agent prompt 还明确指引用它们落库主题/课程 → 对话式建主题/加课当前不可行（只能走 UI REST） | ✅ 已解决（2026-09-14） | 2026-09-13 | [详情](ISSUES/ISSUE-089.md) |
| 090 | 客户端 SSE 桥 `translateAgentEvent` **无 `page_cmd` 分支** → 场景课/资料页受控下行指令（`scene.move/act/show/highlight/update`、`page_action`）全部被丢弃，仅 `say` 台词经旁路可达 = "台词有、动作无"；回执 `page-result` 链随之失效 | ✅ 已解决（2026-09-14） | 2026-09-13 | [详情](ISSUES/ISSUE-090.md) |
| 091 | 孩子无法通过 AI 创建「学习 / 考核」计划（只有 `child_life_plan_create`；缺 `child_study_plan_create` / `child_exam_plan_create`），与说明书 §3.4「学习/考核/生活三种计划孩子都能自己定（加分项）」不符；另 `plan_recurrences` **全仓无写入路径**（重复规则不可设置）、`/plans/exam`、`/plans/life`、`/study-plans` 三个落库 REST 成孤儿（无调用方）；数据模型与加分项计分（creator 分组）已就绪，补工具即可 | ✅ 已解决（2026-09-14） | 2026-09-13 | [详情](ISSUES/ISSUE-091.md) |
| 092 | 自定义考核「本次方法覆盖」用户不可达：`assess-selection.ts` 的排期级 `scope.methodSpec`（require/exclude/recitePass，优先于主题级）**读取侧已实现**，但 `parent_exam_plan_create` 的 scope 只有 `{topics,courses,note}` → 家长 AI 无法表达"本次只考背诵/只考某知识点"；REST `POST /exam/schedules` 接受任意 scope 但无调用方 —— 与说明书 §3.5.1 不符 | ✅ 已解决（2026-09-14） | 2026-09-13 | [详情](ISSUES/ISSUE-092.md) |
| 093 | 家长端发音评测「测试测评」结果展示错乱：总分 undefined、流利度 [object Object]（`AssessmentSettings.tsx` 读 `res.score`/`res.fluency`，但 `SpeechAssessment` 真契约总分是 `pron`、流利度是 `{overall}` 对象） | ✅ 已解决（2026-09-14） | 2026-09-14 | [详情](ISSUES/ISSUE-093.md) |
| 094 | 考核域重构（用户定案）：**取消 `exam_schedules` 排期表**（DROP TABLE）——每日/每周固定考核改为**配置项**（`exam_fixed:<parentId>`），worker plan tick 每天检查配置生成当天 `exam_plans`（幂等，GET 列表兜底补跑）；**自定义考核直接写入 `exam_plans`**（agent 工具 + 管理面板同口径）；`/exam/schedules*` 路由路径保留但语义全部改为操作 `exam_plans`（渲染层零改动）；副产品：ISSUE-075 无作用域 DELETE 风险随表取消彻底消除 | ✅ 已实施（2026-09-14） | 2026-09-14 | [详情](ISSUES/ISSUE-094.md) |
| 095 | 家长端点「停止」agent：UI 显示「⏹ 已停止」但实际仍在运行——`pi:abort` 主进程是 no-op（注释明写"服务端尚无中止能力"），服务端 `parent-registry` 也无 abort 端点，导致家长/孩子端停止都不生效 | ✅ 已解决（2026-09-14） | 2026-09-14 | [详情](ISSUES/ISSUE-095.md) |
| 096 | 服务端重启后家长/孩子 agent「永远思考中」：`openSse` 一次性连接断后 `agentStreams` 死句柄占位、**从不重连**（服务端日志/落盘证实回复正常、纯送达链路断）——修复：`openSse` 重写为自动重连 + `?lastEventId=` 续传，断线轮次自动补齐 | ✅ 已解决（2026-09-14） | 2026-09-14 | [详情](ISSUES/ISSUE-096.md) |
| 097 | 家长 agent 调 `parent_build_material` 报「agent 模型未配置」，但家长设置页显示已配置——设置页走「本地 app-settings.json（旧构建）/服务端」显示已配置，而服务端 agent 读服务端 `app_settings` 取不到 `programmingModel`；当前源码三路都收口同一服务端键故正常构建不该复现，疑为线上旧客户端构建仍走本地分支导致本地/服务端存储分裂 | ✅ 已解决（2026-09-14）：核实当前链路自洽（服务端实存值 + 端到端验证）；根除残留覆盖机制——app-settings 本地文件只存 materialsLimit、保存改走 /models/app_settings 合并端点（旧实现整键推送会覆盖服务端模型配置）、reconcile 去掉模型字段补齐、未配置报错带 parentId | 2026-09-14 | [详情](ISSUES/ISSUE-097.md) |
| 098 | 考核功能两项：①「背诵考核」配置未生效——背诵题仅由题库 `behavior:speech_recite`/语料驱动，而 LLM 出题链路被硬编码禁止生成背诵（`exam-engine.ts` L78-81），`methodSpec` 只有 `recitePass` 通过线无「只背诵」模式开关，`attachStructuredQuestions` 按知识点而非 behavior 筛题→非结构化课 `recQ=0` 不出背诵、有语料课也只是「1背诵+2~3文字」而非纯背诵；② 珊珊会话出题单次 LLM 调用 32~92s（~40s 对应论语 04:39 那次 41.3s），判分每题重建 session 致 45~104s，端到端约 1.5~2 分钟 | 🟡 待处理 | 2026-09-14 | [详情](ISSUES/ISSUE-098.md) |
| 099 | 珊珊 life plan「洗两双袜子」完成状态不更新（仍 pending）：`summarize_conversation` 已写 daily，但 `daily_entries` 出现两行——① `洗两双袜子（孩子自定加分项）` plan_id 列正确但 **plan_outcome=unknown**；② `洗袜子` **plan_id/plan_outcome 列全空、done 信号只写在 raw 正文**（`- planOutcome：done`）；完成判定只匹配 `plan_id!='' AND plan_outcome='done'` → 两行都不中；根因=AI 把 planId/planOutcome 退化成 raw 文本（`insertMany` 仅读结构化字段）+ `unknown` 是死路无收敛/无人工覆盖 + `parent_life_plan_update` 无 complete 动作 | ✅ 已解决（2026-09-14）：F1 家长 complete 动作（pending/missed 可标 done）+ F2 applySignals raw 正文兜底回捞（治愈存量）+ F3 insertMany 正文解析与同 plan 同日去重合并 + prompt 明令禁止 planId/planOutcome 写进正文；真实数据端到端验证通过，珊珊计划已治愈为 done；F4（unknown 自动放行）维持现状防钻空子 | 2026-09-14 | [详情](ISSUES/ISSUE-099.md) |
| 100 | agent 上移服务端后「每日新建会话省 token」机制丢失：旧客户端 `pi-session.shouldAutoNewSession`（开会话检测最后消息非当天→开新会话）已随零 agent 移除（仅存 `tmp/pi-session-old.ts`）；服务端 `session-registry.ts:261` 的 `shouldAutoNewSession` 只判 `resetMarks`（显式 reset），**不按日期**；会话 key=childId 跨天持久累积（落盘 `data/agent-sessions/<parentId>/<childId>-<slot>/`）；客户端 `scheduler.ts:552-571` 残留定点重置块但默认 `enabled:false` 且仅"客户端在线那一分钟"触发，等于失效 → token 不省 | ✅ 已解决（2026-09-14）：F1 冷路径（/open 进会话跨天裁决 + shouldAutoNewSession 日期保险）+ F2 热路径（服务端 autoNewSessionTask 到点重置，配置默认仍 opt-in）+ 客户端进会话加载切 /open；待部署 201 验证 | 2026-09-14 | [详情](ISSUES/ISSUE-100.md) |
| 101 | 孝经/千字文「背诵」题结构已存在但数据残缺：两主题 18+30 门课每课都有「背诵」知识点 + 1 条 `speech_recite` 题；但**千字文 30 段的背诵题 answer 全是占位符 `重点字词（童趣版）`**（非原文，背诵评测据此打分→不可用），**孝经 18 章答案为真原文但实测 18 章末尾全被拼上 `重点字词读音` 噪声**（原 issue 误判仅 4 章）——并非缺知识点/缺题，而是建课时把解读误填进参考文本列 | ✅ 已解决（2026-09-14）：千字文 30 条 answer 替换为 `teaching_copy`「原文吟诵」真原文（带标点）；孝经 18 条 `REPLACE` 剔除 `重点字词读音` 噪声；已备份，待同步生产 201 | 2026-09-14 | [详情](ISSUES/ISSUE-101.md) |
| 102 | 家长 agent 缺「读取孩子全部会话内容」工具：现有家长工具只覆盖资料/家长库/计划域，**无任何读孩子对话逐字稿的工具**（计划工具仅给摘要）；但底层读链路已存在且经「家长对话回顾」页验证——`db/sessions.ts` 的 `indexAgentSessionsIntoDb`/`querySessionMessages`/`listSessionDates` + `routes/sessions.ts` 的 `assertChildOwned` 归属校验。**⚠️ 本需求扩张原隐私红线「家长 agent 只读孩子数据 summary、不触碰原始对话」**——属家长授权扩张，应保留只读 + 归属校验 + 按天/限量读取 | ✅ 已实施（2026-09-15）：新增 `parent_read_child_conversation`（只读；按天 / all+days≤7；单条 600 / 总量 16000 字符截断）+ prompt 边界段 + 文档隐私边界更新；真实数据冒烟 5 例通过 | 2026-09-15 | [详情](ISSUES/ISSUE-102.md) |
| 103 | 家长 agent 管理课程时缺「知识点 + 题库」管理工具：主题/课/教学/资料已有工具（parent_upsert_topic/course、parent_put_material 等），但**无法创建课程知识点、无法创建/维护题库题、无法把题关联到知识点**——第 4、5 步只能走 UI（REST）；底层 DB（assess-content.ts 的 question_bank/knowledge_points/course_knowledge_questions + saveQuestion/getOrCreateKnowledgePoint/replaceCourseContent）与 REST（POST /assess/questions、POST /assess/courses/save）均已齐备可用，属"封装 agent 工具"缺口，非新设计 | ✅ 已解决（2026-09-15）：新增只读 `parent_library_course_content` + 写 `parent_upsert_course_content`（整课替换，含只读预校验 + 移除告警）+ prompt 段；冒烟 29/29；服务端 0.4.2 已部署 201 | 2026-09-15 | [详情](ISSUES/ISSUE-103.md) |
| 104 | 家长端「考核管理 → 自定义考核」**白屏** + 家长 agent 查考核计划**看不到课程/细节**——同一根因：`exam_plans.scope_json.courses` 自 2026-09-14 起改为 `[{title,kps:[{name,count}]}]`（计划生成时即约定出题参数），但两个消费方仍按旧格式 `["课程名"]` 处理：① `ExamAdminPanel.tsx` 直接把数组项当 React 子节点渲染 → `Objects are not valid as a React child` → 整树卸载白屏（本地 dev 无自定义考核故不复现）；② `parent_exam_plan_list` 用 `String(x)` 转对象 → `[object Object]`，agent 看不到考了哪些课、更看不到每课考哪些知识点 | ✅ 已解决（2026-09-15）：服务端新增 `parsePlanCourses`/`formatPlanCourses` 归一化（两格式兼容）；agent 列表改为「课程（知识点×题数）+ 考核方法 + 说明」；`GET /exam/schedules/:childId` 把 courses 归一成课程名数组（老客户端不再白屏）+ 新增 `courseSpecs` 供新端展示；客户端抽 `PlanCourseList` 组件 + `src/lib/plan-scope.ts`，9 条回归用例（含"对象当子节点确实抛错"的机理对照）；**服务端 0.4.3 已部署 201（13:33）**，接口级验证 `scope.courses` 已是字符串数组 → 老客户端即不再白屏 | 2026-09-15 | [详情](ISSUES/ISSUE-104.md) |
| 105 | 设计讨论/实施：让 agent 具备受控 SQLite 直接操作能力（db-channel 通道，终结「一个新场景加一个工具」）——P1 家长受控写 + P2 孩子受控读写已实现（回归 30 项全绿），P3 受控只读 SQL 待观察，**待部署 201** | 🚧 分期实施中 | 2026-09-16 | [详情](ISSUES/ISSUE-105.md) |
| 106 | 孩子界面字号设置入口合并：聊天字号（Type icon）+ 资料字号（TextSelect icon）两个独立 icon/弹框收进**一个 icon 探出的页面**（弹框内分「聊天字号」「资料字号」两组档位按钮；state/持久化/CSS 变量链路不动） | ✅ 已解决（2026-09-17）：侧栏收口为一个 Type icon（title 汇总两组当前值）+ `showFontPanel` 单弹框两分组（`.modal-section-label` 新样式）；fontSize/matFontSize 与 localStorage key、CSS 变量下传链路零改动；build 通过 | 2026-09-17 | [详情](ISSUES/ISSUE-106.md) |
| 107 | 家长聊天区域重新进入看不到当前会话历史消息——ISSUE-081 服务端化把 ISSUE-039 修复架空：`pi:start_parent` 硬编码返回 `history:[]` + 服务端无家长会话 history/open 端点（孩子端已走 `POST /agent/:childId/open`，家长端联调点未接）；落盘与 agent 上下文记忆正常，纯 UI 恢复链路断 | ✅ 已解决（2026-09-17）：服务端新增 `POST /parent-agent/open`（kind 区分 parent/parent-content；家长**不做跨天裁决**、返回现会话全部历史）+ 客户端桥 `openParentSession()`（复用 mapHistoryMessages）+ `pi:start_parent`/`pi:start_parent_content` 透传真实 history（旧服务端 404 兜底空数组，向后兼容）+ ParentChatPanel 回填零改动恢复、TopicEditor 同批接上；**服务端改动待部署 201** | 2026-09-17 | [详情](ISSUES/ISSUE-107.md) |
| 108 | ✅ 已实施（2026-09-21 简化定案：markdown 报表，不做 HTML widget）——家长 agent 新工具 `parent_display_report`：`{markdown, title?}` → 写 settings `report:<parentId>` + agentStreamHub 发 display_content（source=report）→ 家长端「📊 报表」卡片/视图 react-markdown 渲染（新报表红点+自动切换+重启读回 `GET /parent-agent/report`）；复用家长 SSE 流既有 pi:display_content 桥（客户端零新桥接）；测试 4 用例。原 7 类 widget 方案作废；历史多份报表留存二期 | 中 | 2026-09-17 | [详情](ISSUES/ISSUE-108.md) |
| 109 | 家长 agent db 通道需能读主库 `exam_attempts` 表（成绩/逐题/错题/巩固建议）——家长侧现无任何考核成绩读取工具（prompt 只能引导去 UI）；且家长侧 db 通道只有 describe+write、**无 `parent_db_read`**（孩子侧三件套齐全）。方案：新增 `parent_db_read`（补齐对称性）+ 主库受控只读登记表（exam_attempts 第一张，强制 parent_id/child_id 归属过滤、只读、行数上限+审计、JSON 大列截断）——「主库不进直连白名单」原则的首个受控豁免，仅此一张、仅读 | 中 | 2026-09-17 | [详情](ISSUES/ISSUE-109.md) |
| 110 | 盘点：`parent_db_*` 工具能操作哪些表？——`parent_db_describe`/`parent_db_write` 均基于 `parentLibTableRegistry()`（db-channel.ts L79），**只能操作家长内容库 parent.sqlite 的 6 张表**：topics/courses/tags/question_bank/knowledge_points/course_knowledge_questions（均 insert/update/delete，带列白名单+外键校验+行数熔断+审计+敏感列二次确认）。爆破半径严格限定课程内容，**不触达孩子库/计划/积分/成绩/会话**。缺口：parent 侧**无 `parent_db_read`**（只能 describe+write），读内容只能走专用 `parent_library_*` 或 UI；主库 `exam_attempts` 读取仍无工具（ISSUE-109 覆盖） | 中 | 2026-09-18 | [详情](ISSUES/ISSUE-110.md) |
| 111 | SQLite 向量旁表 + 「精确匹配落空 → 向量检索兜底」：LLM 记错课程/主题名时 `WHERE =` 查不到 → 只能全表列出肉眼匹配（courses 600+ 行，上下文爆炸）。方案：`embeddings` 旁表（业务表零变更、JS 内存余弦，sqlite-vec 缓议）+ 首批向量化 `courses.title`/`topics.name` + 新增 embedding 模型配置键 + 写路径异步重嵌入/backfill + 收口 `lookupWithFallback`（精确命中原样返回；0 行 → top-K 候选并明确标注「精确匹配无数据，以下为向量检索返回，请判断选哪一个」，只提示不代入；服务不可用静默降级） | 中 | 2026-09-18 | [详情](ISSUES/ISSUE-111.md) |
| 112 | ✅ 已解决（2026-09-19）：计入积分的考核得分率为 0（珊珊 9/10 被记 0% 误扣 15 分）——根因=09-14 考核 v2 提交路由先行置 `exam_plans.done+attempt_id`，worker `applyExamAttempts` 仅凭 attempt_id 判重跳过 → `exam_plan_courses` 从未回填 → `computeExamRate` 分母 0 → rate 恒 0。修复：幂等判据改为「attempt_id 匹配且明细已有行」+ computeExamRate 无明细时回退主库 per_question 求和 + 空明细告警；存量治愈脚本回填明细并重算流水（09-17 改 +10 良好、09-14/15 金额不变仅 rate 修正），回归测试 3 用例 | 高 | 2026-09-17 | [详情](ISSUES/ISSUE-112.md) |
| 113 | 孩子会话重进后左侧资料列表清空（对话还在、资料没了）——display_content 只推不存（SSE fire-and-forget，无登记）+ `pi:start_child` 硬编码返回 `materials:[]`（ipc-handlers.ts:1335「联调点」历史已由 ISSUE-100 接上、资料至今没人接）；客户端回填逻辑健在恒拿空数组。方案 A：display_tool 推送后持久化 `{path,title,source,ts}` 登记（/reset 清空，与 materials:[] 语义对齐）+ `/open` 一并返回 + 薄桥回填；备选 B 扫会话 toolCall 重建（否决：耦合 jsonl 结构、跨天语义模糊） | 中 | 2026-09-18 | [详情](ISSUES/ISSUE-113.md) |
| 114 | 错题/生字跟踪：把孩子的「漏洞信号」（对话口述错题、不认识的字）结构化沉淀为可复习的错题本——需求分析盘点四类信号源（对话口述流失/查词浮层不留痕/考核 wrong_questions 无闭环/口语评测二期）；方案：孩子 kb 新表 `mistake_book`（kind/status/count 去重）+ 三采集渠道（agent 新工具 `child_mistake_log` + 查词浮层自动上报 + 考核错题同步）+ 孩子端 icon 弹框/家长端 widget + 复习闭环（教学 prompt 注入 open 错题、验证后标 mastered）；明确不做 LLM 全文扫描自动判错。**设计问答已定（2026-09-18）：知识点可选关联（逻辑引用+名称快照，按知识点聚合薄弱视图）；不进题库（孩子 kb 私有数据 ≠ 家长共享内容库，撞 ISSUE-105 边界；错题经 question_id 引用原题做重做，统计可反向供出题参考）；C1 调用时机已定（只认明确漏洞信号：口述错题/对话问字词/稳定薄弱点，反面清单防滥用，先教学后静默记录）** | 中 | 2026-09-18 | [详情](ISSUES/ISSUE-114.md) |
| 115 | ✅ 已实施（2026-09-19）：考核当天重考——`exam_plans` 加 `retake` 字段（幂等迁移，''=不重考，值为重考标准自然语言）；提交路由置 done 后同步钩子（`exam-retake.ts`）：LLM（复用考核会话）按标准+评分摘要生成重考计划 JSON → schema+课程/知识点校验 → 错误反馈重试环（3 轮/100s 预算）→ 按现有口径创建当天计划（origin=retake、确定性 id 幂等）；服务端强制 retake=''（防连环）、count_in_rate 按设置 `exam_retake:<parentId>` 默认不计入（fixed-config 路由已带字段）、失败不丢分仅告警；agent 工具/创建路由加 retake 参数，客户端提交超时 120s+报告页重考提示条；测试 14 用例。待定已落：fixed 首批不做录入入口（custom 先行）。遗留：面板设置开关控件 | 中 | 2026-09-18 | [详情](ISSUES/ISSUE-115.md) |
| 116 | ✅ 已实施（2026-09-19）：定时任务新增「自定义任务」——`scheduler_tasks` 加 type='custom' + `instruction` 列（幂等迁移，owner 恒 parent）；worker tick（每 2 分钟）触发判定沿 last_fired_at 幂等（daily/weekly 到点当日首次命中即跑、once/interval 原生），触发占位后**异步**逐孩子跑独立无头 ephemeral 会话（白名单 get_date/kb 读写 + **weather_query**[Open-Meteo 免 key 7 天预报] + **create_reminders**[source 标记滚动替换+精确去重]）+ 5 分钟看门狗 → 摘要写 task_runs；关键语义：「未来 N 天各播一次」引导建 N 条 once（daily 会每天全量重复播）；面板 + `parent_scheduler_task_create` 工具入口；测试 13 用例。四个待拍板全部落地（白名单/Open-Meteo/5min/source 标记） | 中 | 2026-09-19 | [详情](ISSUES/ISSUE-116.md) |
| 117 | agent 工具并发调用——**框架已支持并行**（SDK `executeToolCalls` 默认 `executeToolCallsParallel`，`toolExecution ?? "parallel"`，服务端未覆盖、无工具声明 sequential——已核实到 bundle 源码级）；`parent_build_material` 每调用独立编程会话（key=parentId:path）不同 path 天然并发安全。**实际串行根因=模型一轮只发一个调用**。方案：①新增批量工具 `parent_build_material_batch`（Promise.all 框架强制并发+上限 3+逐份成败汇总，绕开模型习惯，推荐）②prompt/工具描述鼓励同消息多调用（辅助）。注意 rate limit/成本放大、同 path 仍串行复用会话 | 中 | 2026-09-19 | [详情](ISSUES/ISSUE-117.md) |
| 118 | 调查：家长 agent 穿管 materials 时创建的 HTML 落到哪——**三处落点**：①资料真源 `<dataDir>/materials/<parentId>/<topic>/…`（parent_put_material/parent_build_material，正规主路径）；②孩子工作区 `workspaces/<parentId>/<childId>/outputs/…`；③~~家长会话工作区（不可穿管）~~ **P-a ✅ 已修复（2026-09-19）**：根因=parent_build_material 对非 `materials/` 前缀路径静默落家长工作区，且前缀语法与同族工具（parent_put_material 用根相对路径）不一致——已重构 `resolveLessonOutputPath`：家长侧恒落真源+根相对路径语法（兼容旧前缀）+topic 校验，测试 7 用例。穿管=display_content 读正文内联推 SSE + 登记 display_contents 表（无文件副本）。遗留：P-b 落点边界文档化；P-c 测试家长 76 目录污染 | 低-中 | 2026-09-19 | [详情](ISSUES/ISSUE-118.md) |
| 119 | 背诵题打分链路梳理：**纯发音评测引擎分线性映射，不经 LLM**——题库直出（refText=原文/recitePass 通过线默认 90/pointMax 默认 10）→ 多段录音合并 WAV → 服务端评测（**实际用阿里声希 SSECP**：中文背诵走 cn.pred.score 长文本免拆段，81=声希 overall 合成分；腾讯 SOE=SuggestedScore 为代码兜底）→ toSpeechAssessment（pron=score）→ 客户端 `pointGot=round(total/100×pointMax)`、`correct=total≥pass`。**引擎分本身是黑盒**（声希内部对准确/完整/流利加权合成，权重不公开，我方仅可控 precision=1.0）。**三个注意点**：①`overall??pron` 双字段残留（映射从不设 overall，误导）②得分与通过两条线（9 分但 correct=false 可能，影响错题本/重考口径）③评测失败软失败记 0 分无区分 | 低 | 2026-09-19 | [详情](ISSUES/ISSUE-119.md) |
| 120 | 家长/孩子 agent 共用「知识库」：孩子聊天问历史/自然现象等事实问题时 agent 检索高相关可信知识再答（家长供料圈定内容边界，不联网）；家长提供视频/网页/PDF 自动摄取为可检索知识。方案：`knowledge_docs`（状态机 pending→ingested）+`knowledge_chunks`（分块+向量 BLOB），**向量复用 ISSUE-111 已落地的 embeddings 基建**（首个纯语义消费方）；摄取管线 PDF 抽文本/网页正文提取/**视频 ffmpeg 抽音轨+ASR 转写**（两者均已有能力）；孩子 agent 只读 `knowledge_search`（prompt 教触发时机）+ 家长 agent 管理 tools；二期 LLM 清洗结构化条目/混合检索。待拍板：存放位置（knowledge.sqlite vs parent.sqlite 表）/ASR 通道/分块参数。**检索已调研 Obsidian（2026-09-21）：主路线（结构分块+本地向量+余弦）与 Smart Connections 同构已验证；吸收①结构感知分块替代纯定长 ②混合检索（FTS5 BM25+向量）维持二期但融合排序单列验收（Copilot #1799 教训）③链接图一跳扩展（反链思想，二期，chunks 预留 entities 字段）** | 中 | 2026-09-21 | [详情](ISSUES/ISSUE-120.md) |
| 121 | ✅ 已解决（2026-09-21 拍板 B+命名）：自定义考核取名 + 同日多场——`parent_exam_plan_create` 加 `name` 参数、INSERT 落库 title（不再硬编码「自定义考核」）；两处守卫（agent 工具 + `POST /exam/schedules`，后者收 `body.name`/`scope.name`）去重收窄为**同日同名**（对齐孩子自请语义），不同名未考计划同天并存；孩子端 todayOpen 列表渲染无单条假设、computeExamRate 多条 done 同天合并求和均不受影响。**附带发现**：面板 ExamAdminPanel 新建/编辑表单已是死代码（无 JSX 引用、scope 格式残留 ISSUE-104 前），面板创建入口实际不存在，考核创建只剩 agent 对话一路——恢复 UI 需按 courses 口径重做（新 issue 候选）。测试 4 用例 | 中 | 2026-09-21 | [详情](ISSUES/ISSUE-121.md) |
| 122 | ✅ 已修复（2026-09-21）：`parent_db_write` 的 update 恒报「列 0 未登记」（schema 强制 rows 数组、执行器期望对象的自相矛盾，update 语义整体不可用）——修复三件套：①schema 改 `Type.Union([行数组, 列值对象])`（parent_db_write/child_db_write/Tier2WriteRequest 同批）②执行器双向兼容：新增 `normalizeWriteRows`，update 数组取首元素（多元素拒绝提示按 where 逐条）、insert 对象视为单行，`executeWrite` 与 `tier2Write` 全改走归一 ③`digitColHint`：纯数字列名报错附形状自愈提示；连带修正 parent-tools 重嵌入对单行对象 rows 的取主键。测试 8 用例。**未上 201**（当日 0.5.0b 部署在先，随下次发版） | 高 | 2026-09-21 | [详情](ISSUES/ISSUE-122.md) |
| 123 | 孩子库 courses/topics 与家长库同步机制梳理——**无定时同步（设计上不需要）**：2026-09-18 库域分工后孩子库 courses=纯进度表/topics=主题分配表，教学字段真源在家长库；唯一写入口是**分配时**（kb.courses/topics.upsert，routes/db.ts:580/615），worker 只读。**关联字段两层**：`courses.topic_key`=真引用（→topics.topic_key→家长库）；`courses.uuid`=分配时 `resolveCourseUuid` 冻结副本（topic+title 精确→title 兜底，可空）；主键=(topic,title) 显示名对。**遗留漂移风险**：R1 家长库改名/重建→孩子库孤儿行；R2 分配后家长库新加课程→考核创建报「孩子库不存在」（parent-plans.ts:776 按孩子库校验）；R3 title 兜底跨主题重名误连 uuid。候选：轻量对账 worker/读取时联查/维持现状+声明。**title 改名同步已深化（2026-09-21）**：锚点=uuid 且改名必须保 uuid——现状 upsert ON CONFLICT(topic,title) 下改 title=新行新 uuid（旧行残留），须先立显式 rename 动作；同步=写时联动（rename 后遍历孩子库 `UPDATE courses SET topic=?,title=? WHERE uuid=?`，进度原地保留）+ 对账 worker 兜底修存量。**落地工具已定稿（2026-09-21）**：家长 agent `parent_sync_courses_to_child`（child 必填 + topic/titles 过滤）——①补齐孩子库缺失 uuid（topic+title→家长库解析回填，失败列清单交家长）②按 uuid 三态同步（命中→只更新显示/排序字段，**进度一概不动**；撞空 uuid 旧行→归并防重复行；全新→插入 ⬜）③摘要返回；不做删除方向同步；名字寻址路径全量清单同见文件。**已实施（2026-09-21）**：新增 `parent_rename_course`（改名保 uuid + 联动全孩子库显示名，孩子库同名冲突跳过）与 `parent_sync_courses_to_child`（补 uuid + 按 uuid 三态同步 + 孤儿/冲突/主题未分配汇报，幂等、进度一概不写）+ `resolveCourseUuid` 收窄为同主题（修 R3）+ `parent_upsert_course` 显式写 uuid；prompt/文档同步。冒烟 34/34 PASS；**服务端 0.5.1 已部署 201（09-21 14:54）**，真实数据 dry-run：闻闻缺 147 门课/珊珊缺 15 门（R2 实证）、主题未分配 6/3。**遗留**：改名后考核计划 scope 里的旧课程名只在返回里提醒、未自动改写；uuid 主键化（方案 B）未做。**第二轮修正（用户纠正，09-21 下午）**：首版同步默认「家长库全量」越界（未把「主题是否分配给该孩子」当边界），用户 201 上首跑即给闻闻+147 / 珊珊+15 行未分配主题的课（无进度痕迹）；已改为**只同步已分配主题**、未分配主题整体跳过并在返回里列明、`topic` 传未分配主题直接拒绝、无分配主题拒绝执行、补 uuid 也限在范围内；`parent_rename_course` 不受限（只改已存在的行）。冒烟 28/28 PASS；**0.5.2 已于 09-21 15:24 部署 201**（部署后复核数据未变）；162 行存量**用户决定暂不删除** | 低-中 | 2026-09-21 | [详情](ISSUES/ISSUE-123.md) |
| 124 | 家长聊天框上传图片 → 家长 agent 读不到 uploads 目录，无法走视觉模型（bug 兼需求）：上传落点 `parents/<pid>/uploads/`（相对 dataDir；家长维度目录约定见 `db/parent-lib.ts:3`），但 `parent_read_image`（`agent/parent-tools.ts:1202`）虽已写 `uploads/`/`files/` 分支却**半成品**——①agent 收到的路径带 `parents/<pid>/` 前缀、不以裸 `uploads/` 开头→走 `materialAbsPath` 去材料库（`materials/<pid>` 根）找→「材料不存在」；②即使传裸 `uploads/xxx`，`resolveWithin(dataDir, rel)` 解析到 `dataDir/uploads/xxx`、**缺 `parents/<pid>/` 段**→实际文件在 `dataDir/parents/<pid>/uploads/xxx`→仍读不到。**视觉模型能力本身存在**（`vision.ts:62` `describeImageViaVision`，被 `parent_read_image` 调用），只差把 upload 文件喂进去。材料库/家长库工具（`parent_read_material`/`parent_library_*`/`parent_db_*`）一律碰不到 uploads 区。→ 整条「上传作业图→agent 读图→转课程/计划」断在读取步。**方案 F1**：`parent_read_image` 增补识别 `parents/<pid>/uploads/`、`parents/<pid>/files/`，强制校验路径 pid==deps.parentId 防越权，解析 `resolveWithin(dataDir, rel)`；**F2**（需求）家长端「转存为资料」把 uploads 落到 `materials/<pid>/<topic>/media/`；**F3** prompt 引导收到上传图直接 `parent_read_image` 传附件相对路径、读不到提示转存/文字；**F4** 端到端联调+冒烟。优先级高（卡住上传作业图主流程）。**已实施（2026-09-21）——根因比 F1 更深一层**：只读探针证实 201 上 `parents/<pid>/` 下**没有 uploads/**、`files/<pid>/` 里 186 个文件**全是考试录音**，也就是说附件**从未上送服务端**（客户端注释「图片暂未随 prompt 上送」即此），光修路径解析仍读不到。落地：①客户端主进程 `file:save_upload_parent` 落盘后顺带 `POST /files/upload`，返回 `ref`（`files/<id>`），失败不阻断但提示；②新增 `src/lib/attachment-ref.ts`（`attachmentMarker`，ref 优先/path 兜底）+ 8 条回归；③新增 `server/src/agent/upload-ref.ts` `resolveAttachmentRef`：裸 uuid / `files/<id>`（查主库 files 表）/ `files/<pid>/<stored>` / `uploads/`·`parents/<pid>/uploads/`（本机）三类，pid≠当前家长→forbidden、一律 `resolveWithin` 沙箱；④`parent_read_image` 接上解析器 + **新增 `parent_read_upload`**（非图片附件：文本回正文 ≤200KB、图片提示改用 read_image、pdf 只回元数据）；⑤读不到时 `missingRemoteHint` 明令不要再试 materials/uploads/parents 路径（现场就是这么绕圈失败的）；⑥prompt 增「聊天上传的图片/文件」段。冒烟 30/30 PASS；**服务端 0.5.3 已构建待部署**，**Windows 客户端 0.1.20 已打包（`dist-release-020/学习伙伴 Setup 0.1.20.exe`，asar 内已核实含改动）但未发布**。遗留→ISSUE-125（孩子聊天附件同款缺口） | 高 | 2026-09-21 | [详情](ISSUES/ISSUE-124.md) |
| 125 | **孩子聊天附件同款缺口**（ISSUE-124 的同源问题，未处理）：孩子上传的图片/文件同样只落在**孩子电脑本机** `data/children/<childId>/uploads/`（`ipc-handlers.ts:1846`），`Learn.tsx:1210` 还把路径剥成裸 `uploads/xx` 再塞进标记 `【附件图片：名\|uploads/xx】`；而服务端孩子 agent 全仓**没有任何读 uploads 的工具**（`grep uploads server/src` 只命中家长侧），`POST /agent/:childId/prompt` 的 body 也只取 `{text, pageEvents, session}`——客户端传的 `images`（base64 内联图）**根本没被服务端消费**（`server/src/routes/agent.ts:134`）。⇒ 孩子发照片给 AI 伙伴这条链路整体不通（家长侧已按 ISSUE-124 修好，可复用同一套：上送 `/files/upload` + 引用解析 + 读图工具，并让服务端消费 inline images）。 | 中 | 2026-09-21 | [详情](ISSUES/ISSUE-125.md) |
| 126 | **模型错误全程不可见 + 会话模型绑定焊死**：qwen-tokenplan 周额度用尽（429）后客户端永远「等待模型返回」、按钮变回发送。三层叠加：① `ensureEntry` 内存命中即返回，`pickWorkerModel` 只在会话首次创建时执行（`session-registry.ts:242`，家长侧 `parent-registry.ts:219` 同）→ 改 defaultModel 对已存在会话永不生效；② SDK 对模型 API 失败不 emit error 事件，只记 `stopReason:"error"`+`errorMessage` 的空 assistant 消息；③ `attachStream` 不检查 stopReason（`session-registry.ts:437`）、客户端 `handleReplyEnd` 不清无输出的工作气泡（`Learn.tsx:793`，场景/家长面板同）→ 429 全链路静默。修复：模型热切换（submit 前 setModel，不重建）+ message_end 识别 error 转 error 事件（友好文案）+ 客户端空回复兜底。 | P0（已修复） | 2026-09-21 | [详情](ISSUES/ISSUE-126.md) |

| 127 | ✅ 已修复（2026-09-21）：学习计划创建不校验课程名存在性——错名计划 topic_key/course_uuid 落空照样插入，而学习域完成判定按 course_name 精确匹配 daily → 错名计划永远无法完成 → missed 顺延 + 拖累完成率扣分（ISSUE-112 同构）。修复三路径：parent_study_plan_create 查不到整单拒绝（报不存在清单）；POST /api/v1/study-plans 同款 400（家长库不可用时跳过校验）；expandRecurrences study 规则课程失效跳过+告警（游标照常推进）；child_study_plan_create 原有校验保持。测试 3 用例。未上 201 | 中-高 | 2026-09-21 | [详情](ISSUES/ISSUE-127.md) |

| 128 | ✅ 已实施（2026-09-21 用户拍板，修订 ISSUE-105 矩阵）：家长 db 通道开放孩子库**全部登记表**（管理口径）——`childKbAdminWriteSpecs`（全 specs + 全 ops + rowLimit 50），parent_db_write+child 切换到该规格（孩子 agent 自己的写面两表白名单**不变**）；护栏保留：列校验/行数熔断/事务/审计/只读列/redemption child_id 强制；风险=状态机表直写绕过受控流程（points_ledger 直插不重算余额链），靠审计+先 read 后写+复述兜底；工具/元数据 prompt/describe 文案同步；测试 5 用例（含用户原始场景改 study_plans.course_name）。未上 201 | 中 | 2026-09-21 | [详情](ISSUES/ISSUE-128.md) |

| 129 | ✅ 已实施（2026-09-21）：token 用量按日期×会话展示。服务端 `token_usage` 表（一行一条 assistant 消息，模型字段原样直传）+ 游标扫描器（读时增量、可回填历史）+ `GET /token-usage/days\|sessions\|dates`（家长 JWT）；客户端 preload/IPC/web-shim 三层接线 + `TokenStatsPanel` 重写为两级视图（日期→渠道行→点开会话明细）；删除旧死代码（token-stats.ts + token:summary/token:log IPC）。验证：tsc 0 错、新测试 3/3、双端 build 通过、真实数据 E2E 冒烟。**已部署 201（0.5.4，2026-09-22）**：首次查询回填 1179 行/26 会话文件，真实消耗首见（09-21 家长助手 184 轮 1962 万 tok）；journal 0 错误。**遗留**：客户端 UI 随下个客户端包发布；考核/worker 的 inMemory 会话不落盘扫不到 | 中 | 2026-09-21 | [详情](ISSUES/ISSUE-129.md) |

## 记录格式（模板）

- **类型**：bug / 需求 / 架构 / UI / 其他
- **描述**：
- **影响范围**：
- **排查/修改入口**：
- **优先级**：待定 / 高 / 中 / 低
- **记录时间**：YYYY-MM-DD
| 130 | ✅ 已实施（2026-09-22）：家长侧边栏收敛——去掉 学习计划/学习考核/积分 三项，全部并入 孩子管理→孩子详情（tabs：进度/**计划**/每日记录/主题/**考核计划**/**积分**/账号/对话回顾）。计划 tab 新组件 ChildDailyPlans：14 天逐日三域聚合、每天分「家长制定（必须完成项）/孩子自己制定（加分项）」两组（对齐孩子端 TodoModal 语义）；重复规则未来命中日只读虚拟展开（服务端新 `GET /api/v1/plans/range` + collectPlanRange 可测函数 + preload/web-shim plansRange）。考核计划 tab 新组件 ChildExamPlans：孩子端 ExamView pick 同布局（今天可考核卡 + 全部计划列表时间/状态筛选 + 右详情课程/要点/未来提示/已完成成绩逐题+听原音），家长只读不发起。删除无引用组件 StudyPlanPanel/ExamRecords/ExamAdminPanel；RewardPanel 单孩子隐藏切换器。测试 4 用例 | 中 | 2026-09-22 | [详情](ISSUES/ISSUE-130.md) |
| 131 | **文件区统一 + 前端网盘双端**（实施方案定稿，2026-09-22 拍板四项：①materials **索引删除**（纯镜像，列表改现场 walk）②家长网盘管孩子**可读可写** ③孩子端也要文件管理 ④**层级**：家长工作区根=`workspaces/<pid>`（uploads/materials/scratch + 孩子目录在内），孩子工作区=`workspaces/<pid>/<cid>/`（uploads/outputs/scratch），**孩子 agent 文件工具严格限自己目录**（现状语义保留+越权断言）。三期：P1 双端网盘（fs 路由 list/mkdir/rename/move/delete/upload，scope 解析家长=整树/孩子=自己目录；materials 写走 putMaterial 语义+引用影响检查）；P2 物理归并（**12 项写入点切换清单**：/files/upload、upload-ref 新位置置顶+旧位置永久兜底、materialAbsPath 根切 `workspaces/<pid>/materials`、display 三源、孩子 fs 根断言、三处 cwd 挪 scratch、孩子上传链路=ISSUE-125 收口、家长旧 `parent/` 目录废除；存量不迁移靠兼容层）；P3 退役+文档。**10 条回归 checklist**（+R-10 索引摘除后 walk 等价） | 中 | 2026-09-22 | [详情](ISSUES/ISSUE-131.md) |
| 132 | 题库管理优化：家长界面按 主题/课程/知识点 维度查询 + 直接新建/编辑/删除题目（可挂载到知识点/课程，也可不关联）——服务端新增 facets/link/unlink/delete 路由；QuestionBankPanel 加级联筛选与 CRUD；附带修复 ISSUE-074 题库 LIMIT 10000 被 F10-b 重构改回 2000 的回归 | ✅ 已实施（2026-09-22）；**服务端接口已随 0.5.5 部署 201（2026-09-23 09:05）**，客户端面板待下次发客户端包 | 2026-09-22 | [详情](ISSUES/ISSUE-132.md) |

| 133 | ✅ 已修复（2026-09-22/23）：`parent_db_write` 报「rows: must be array / must be object / must match a schema in anyOf」——模型把复合参数整串序列化成字符串（`rows:"[{\"status\":\"done\"}]"`），而 SDK `validateToolArguments` 只跑 TypeBox `Value.Convert`（标量转换）、**不做 string→结构 解析**，校验在**执行器之前**硬失败 → 工具没跑、模型拿不到自愈线索（已排除运行时：`openai-completions.js` 只 `parseStreamingJson` 一次、`json-parse.js` 是忠实解析器、`tool.parameters` 原样透传；反证＝09-18 同模型 `rows` 真数组成功落库）。修复两层（同 ISSUE-122 思路）：①新增 `tool-shapes.ts`（`WriteRowsParam`/`JsonObjectParam`/`JsonStringArrayParam`/`JsonArrayParam`，anyOf 追加 string 分支）；②`db-channel.ts` 新增 `parseJsonArg`/`coerceObjectArg`/`coerceColumnsArg`/`coerceArrayArg` 并接入 `normalizeWriteRows`/`executeWrite`/`executeRead`/`buildPathQuery`/`tier2Write`/`tier2Read`；③接线 `parent_db_read`/`parent_db_write`/`child_db_read`/`child_db_write` + **`parent_upsert_course_content.items`**（jsonl 量化实测 12/69＝17% 被字符串化、09-15 连续重试 6 次）；重嵌入取主键改用 parse。**实测风险分布**：**mimo-v2.5 无法表达 anyOf——凡联合类型属性一律退化成 JSON 字符串**（201 最近 12 家长会话实测：唯一被字符串化的参数就是 `parent_db_write.rows`，26 次全部来自 mimo-v2.5、单会话连败 22 次；同 schema 下 `deepseek-v4-flash-0731` 20 次调用零字符串化）；深长数组套对象 `items` 12/69（17%）＞ 扁平 map 与标量数组 0 次（where 0/35、courses 0/37、days 0/7）。**生产影响**：0.5.4 + mimo 下家长助手凡走 `parent_db_write` 的操作 100% 失败；临时绕过＝把家长默认模型换回 deepseek-v4-flash-0731（非治本）。验证：新回归 17 用例 + 相邻回归 77 用例全绿、tsc 0 错、bundle 已含修复。**未覆盖**：`courses`/`titles`/`require`/`exclude`/`fields`/`days` 等仍为裸参数（ISSUE-134 提案：自建 defineTool 包装器递归加 string 分支 + 递归 parse）。**已部署 201（server 0.5.5，2026-09-23 09:05）** | 高 | 2026-09-22 | [详情](ISSUES/ISSUE-133.md) |

| 134 | ✅ **已实施（2026-09-23，P0~P2；按用户决定先不部署）**：工具参数「被模型序列化成字符串」的治本方案——不再逐参数打补丁（已漏 `courses`/`titles`/`require`/`exclude`/`fields`/`days`），改为**统一还原层**：新增 `server/src/agent/tool-kit.ts` 包装 `defineTool`，给**每个工具**注入 SDK 官方钩子 `prepareArguments`（`agent-loop.js:403` 在**参数校验之前**调用；内置 `edit` 工具同款先例），把字符串化参数 `JSON.parse` 回结构 —— **schema 一字不改**、14 个工具文件只改 import 一行即全覆盖（`packages/agent-core` 无需改）。还原规则白名单式下沉（只在 schema 明确声明 object/array 处 parse、parse 后类型须吻合、`additionalProperties:{}` 视为 unknown 不解析 → `answer`/`scoring` 等含 JSON 文本的普通字段永不被改写）；失败抛可读中文原因（转成 isError 工具结果）。**实施要点**：包装器类型 `as typeof sdkDefineTool`（规避 SDK 自带 typebox 与服务端 typebox 的跨包类型噪音）；工具自带 `prepareArguments` 时先跑它再跑本层。**验证**：`test/issue134-arg-coercion.test.ts` 19/19 绿（含 3 条误伤防护 + 真实工具真执行：rows/items 真改库、days/courses/methodSpec(内层 require+exclude)/titles 全通过）、**全量对照基线零新增失败**（同 10 文件/19 用例，通过 427→449）、tsc 0 错、bundle 已含。**逐参数补丁按用户决定保留**作第二道保险。**P3 已部署 201（server 0.5.5，2026-09-23 09:05）** | 中-高 | 2026-09-23 | [详情](ISSUES/ISSUE-134.md) |
| 135 | ✅ **P0-a + P4 已实施并部署 201（0.5.8，2026-09-23）**：**学习/考核掌握闭环**（第 2 环结果记录 + 第 3 环分析汇总）——用户 13 项决策（D1~D13）：①新建 `knowledge_point_records`（**学习/考核同表**按 `source` 分类，用家长库知识点 id 挂到计划，靠 **plan_id + course_uuid** 关联计划结果，`UNIQUE(source,plan_id,course_uuid,kp_id)` 幂等）②`knowledge_point_progress`（知识点累计掌握：`mastery_desc` **累计自然语言**"最开始→中间→最新" + level + 学/考计数）③**课程进度落孩子库 `courses` 加 4 列**（mastery_level/mastery_desc/teaching_advice/mastery_updated_at；⚠️ 必须排除出 `dropLegacyCourseColumns` 删除名单；比率/时间仍用 `course_progress` 视图不重复存）④学习侧 `study_plans.result_summary`（exam_plans 不加——一次考核跨多课）⑤**新增 `exam_course_results` = 课程每次考核结果概要**（一行 = 考核计划 × 课程：Σ得分/Σ满分/**显式 `rate`**/question_count/course_summary/plan_review_at/focus_json，`UNIQUE(plan_id,course_uuid)`）⑥**`exam_plan_courses` 保持不变（考核明细表，逐题）**并补题级富字段（`ai_comment`/`asr_text`/`audio_file_id`/`duration_ms`/`correct`/`behavior`）+ 删冗余 `score`——否则三处「考核记录」界面与"听原音"会失数据 ⑦⭐**废弃 `exam_attempts`（不搬）**：实测主库 19 行、100+ 孩子库均无此表 ⇒ 09-10 定案只执行了一半（逐题**得分**已进 `exam_plan_courses`；场次表与逐题富信息至今留主库且仍是写入真源）→ 提交端点改为直写孩子库三层，主库表停写保留一版后 DROP；**ISSUE-109 的"主库受控豁免"整案取消** ⑧新增每日 `progressAnalysis` WorkerTask（**家长可配**、默认建 @21:30；按课程一次批量调 LLM，一天 3 门课≈3 次、输入 6~8k/次 ≈ 家长助手日耗 2%）⑨教学建议只写 `courses.teaching_advice`，**从不回写**方法字段 ⑩**计划不可删**——查实 2 处硬删无保护（`parent-plans.ts:527` + `routes/study-plans.ts:443` 家长可硬删已完成学习计划），且 records/结果表的 `plan_id` 依赖计划行，硬删会失联 ⑪实施 **P0-a（考核结果重构，最大一块）**/P0/P0-b/P1~P5 + 完整验收 ⑫`speech_assessments`（**题级**口语评测存档：一行一题、但旧表**缺 `question_id`** 且全仓无读取方=纯存档）**搬到孩子库 + 补 `plan_id`/`course_uuid`/`question_id`**，与明细按 `(plan_id,course_uuid,question_id)` 关联（D13）⑬**旧 `exam_attempts` 数据处置：不迁、直接 DROP**（实测对照确认**结构数据（课程/知识点/题目/得分）确实都在 `exam_plan_courses`**，但题级富信息只在 attempts：83 条 `aiComment`/55 条 `asrText`/46 条 `audioFileId`/25 条 `speech`，均为 09-01~09-17 早期测试数据，用户确认放弃；音频文件本体不受影响）⑭另两条实测发现：**明细表老数据有重复行**（行数=逐题数×2，因旧数据 `question_id` 为空致去重键失效 → P0-a 顺手按 `(plan_id,course_uuid,question_id,seq)` 去重）+ **`knowledgePointId` 仅 31/83 有值**（分知识点统计必然漏数，靠回退挂载表兜底）⑮待拍板清零，P0-a 可开工 ⑯⭐**P0-a 已实施（2026-09-23）**：新增 `server/src/exam-results.ts`（`persistExamResult` 一次提交写全孩子库三层：明细/课程概要/知识点流水 + 评测存档 + 计划回填；课程 uuid 三跳解析、缺知识点时回退家长库挂载表）；提交端点改为调用它并**前移错题本同步**（原在 worker）；主库 `exam_attempts`/`speech_assessments` 改为建表 SQL 里幂等 DROP；`GET /exam/attempts`、`/exam/course-records`、`/courses/status`、`/assess/questions/:id/records` 全切孩子库（**响应形状不变 → 三处界面与听原音零改动**）；worker `applyExamAttempts` 收窄为单库幂等兜底、`computeExamRate` 去掉跨库兜底；孩子库 4 新表 + `exam_plan_courses` 补 9 列删 `score` + 老数据按 `(plan_id,course_uuid,question_id,seq)` 去重（meta 游标）；`courses` 掌握四列 + `study_plans.result_summary`（列名避开删列名单）；`course_progress` 视图改读 `exam_course_results`；**P1**：两处已完成计划硬删改软删 + agent 列表过滤取消行；注册表同步后 `probe:registry-drift` 无漂移。验证：tsc 0 错、bundle 构建通过、相关 7 文件 56 用例全绿、全量 458 过/15 失败（均既有无关）。⑰⭐**P4 已实施（2026-09-23 下午，用户拍板改向）**：掌握闭环归纳**不新增 worker 任务类型**，改为复用 app 已有的「**自定义任务**」——默认播种「学习情况分析」（`type='custom'`、每天 21:30、启用、分配给现有孩子，settings 标记 `mastery_task_seeded:<pid>` 只播一次、家长可改可停）+ 自然语言指令 `DEFAULT_MASTERY_TASK_INSTRUCTION` + 新增 `server/src/worker/mastery-tools.ts`（4 个 `mastery_*` 工具：`mastery_todo_list` 看待归纳范围 / `mastery_plan_context` 一次给全素材 / `mastery_save_records` 写知识点流水+学习概要 / `mastery_save_course_mastery` 写课程掌握四列+知识点累计）；自定义任务 agent 的工具白名单改为**并集**（原定制任务工具 ∪ mastery ∪ `parent_db_describe/read/write`，漏登记=工具静默不可见，已单测锁住）；**P3 学习侧抽取并入该任务指令**；不动客户端。⑱**已部署 201：0.5.6（11:29）→ 0.5.7（11:39）**。0.5.6 首次部署即暴露 P0-a 漏网 bug：`GET /exam/attempts/:childId` 线上 500 `no such column: topic_key`（读取侧误 select `exam_plans.topic_key`，该表无此列）→ 0.5.7 修复：SELECT 去该列、topic 改从 `exam_course_results` 取；`assess/questions/:id/records` 的静默 catch 改为留痕 warn；**新增 `test/issue135-exam-routes.test.ts`（7 用例）**——真 fastify + 真 sqlite + 真 JWT 覆盖 4 读 + 1 写 + 401/403，并用「注入原 bug→复现 500→还原」验证过非假绿。端到端（自签家长 JWT 直连 201）全部 200。⑲**0.5.8（12:05）：掌握分析任务改为「显式添加」**——用户反馈「我没设置定时任务，为什么 21:30 会自动触发」，确认是 P4 初版的服务端自动播种（worker tick + 列表接口）所致：已删除自动播种与 `mastery_task_seeded` 标记，改为 `MASTERY_TASK_TEMPLATE` + `GET /task-templates`（只读）+ `POST /tasks {template}` 显式创建（固定 id 幂等），201 上原任务行已删、跨 tick 复查未重建；新增 `test/issue135-scheduler-task-template.test.ts`（6 用例）。**P0-b/P2/P5 未做；前端「推荐任务一键添加」未做（需改 SchedulerTasksPanel + 重打客户端包）**。详见详情文档 §11.4~§11.6 | 中-高 | 2026-09-22 | [详情](ISSUES/ISSUE-135.md) |

| 136 | **孩子端 agent 提示词按「场景」整理（agent 操作规范稿）**——把①基底从"按主题分段"改为**按域 → 按场景组织**（一个"需要提示词"的场景 = 一节，三段式：**孩子会说 / 怎么做 / 边界**），**全量 27 条规格已写出**（通用 + A4 + B5 + C6 + D3 + E3/F5/G1/I1…；E4/H1 待拍板）。配套：覆盖度对账表（可逐域验收）、搬家清单（现有句子归位到哪个场景）、落地方式、唯一需动代码的一处（**A1 伙伴名字/性格属③自动注入，组装时一个字都没带**）。顺带定稿 **E6「自己创建考核」** 的完整口径（课名必须先确认不许编、两种失败原因如实说、同日同名会跳过、建完去考核页），并记下 `child_exam_plan_create` 描述里**悬空引用不存在的 `child_library`**（全仓仅此一处）。**本轮只出稿、未改任何代码**；改完**必须重开会话才生效** | 中-高 | 2026-09-23 | [详情](ISSUES/ISSUE-136.md) |

| 137 | **「针对错题创建一次考核」（F5）怎么处理**——现状两条硬约束核实到字段级：①**考核必须挂课程**（`scope_json.courses` 是顶层维度，创建时先按课名落孩子库→家长库 uuid→知识点）②**最细只到"知识点 + 题数"**（`PlanKpSpec` 只有 `{name,count}`，`attachPlanQuestions` 池内随机抽，**锁不到某道题**）。**关键洞察：只要错题带 `question_id`，课程/知识点都能反查出来——"必须挂课程"不是死结**。三条路线：**A 题级锁定**（scope 加 `questions[]`，协议级改造，二期）/ **B 知识点级**（现状可做，但 `child_mistake_log(list)` **不返回 course_ref/knowledge_point_name/question_id**，须补吐字段或改走 `child_db_read` 读 `mistake_book`；且话术须诚实"同类题非原题"）/ **C 不进考核**（走错题本复习闭环，ISSUE-114 已设计）。建议 **C 为主、B 先行、A 二期**。另记两处小缺陷：`course` 参数"主题/课程名"混用（写进 `course_ref` 会造成按课名反查失败）、口述错题**没有知识点入参**（永远进不了闭环）。**本轮只出方案、未改代码** | 中 | 2026-09-23 | [详情](ISSUES/ISSUE-137.md) |
| 138 | **孩子 agent 的工作区与文件能力（域 I）**——用户要求「孩子 agent 也要有同样的操作场景、要知道工作区目录架构、要能操作文件」。核实结论：**工具早齐**（`read`/`write`/`edit`/`ls` 四件套 + `display_content` + `create_html_lesson`，根=`workspaces/<pid>/<cid>/`，`resolveWithin` 防越界），**缺口在提示词只有一句"你的工作区：X（只能在此目录内操作）"**——一个字没提目录架构。实测工作区三层：**`outputs/`= 孩子作品柜**（`create_html_lesson` 孩子侧缺省落 `outputs/<标题>.html`；`display-tool` 按 `outputs/` 前缀判定孩子工作区）、`scratch/.pi/`= 编程 agent 运行区、`.pi/<cid>-main`= 孩子自己的会话槽（⚠️不是学习内容）。缺口：①目录架构不在提示词 ②I3「找我的产出」无提示词（模型会凭记忆编）③I6「存我的成果」连场景口径都没有 ④**实现级：`ls` 的 `listTree` 不过滤隐藏目录 → `ls .` 会列出 `.pi/`**（模型可能读自己的会话 jsonl → 上下文污染 + 隐私面），建议跳过点号目录 ⑤I1「看资料清单」对话无出口（孩子侧无列资料工具）。**有意边界（不改）**：孩子对家长资料库"能按课程取（`parent_content(htmlPath)`）+ 能展示，但不能浏览、不能读内容、不能写"。**本轮只出方案（含提示词规格：通用「我的工作区」段 + [域 I] I3/I6），未改代码** | 中 | 2026-09-23 | [详情](ISSUES/ISSUE-138.md) |

| 139 | **考核复盘与错题分析（E7）**——用户要求新增场景「孩子能自己重考错题，agent 要能读到**某一次考核计划的所有题目的考核结果**，给孩子分析错题」，并要「能完整读取课程/知识点/题目」。**核心核实（好消息）：数据与工具已具备、无需新工具**——ISSUE-135 P0-a 之后一次考核的结果分三层落在**孩子库**：**`exam_plan_courses`（逐题：题干/`ref_text` 原文/`point_got`/`point_max`/`correct`/`ai_comment`/`asr_text`/`audio_file_id`/知识点名/课程名/`seq`）** + `exam_course_results`（每课 Σ/得分率/概要/复习重点）+ `knowledge_point_records`（`source='exam'` 档位）；全部已登记在孩子可读表里，`child_db_read` 一条 `where:{plan_id}` + `orderBy:"seq"` 就能读完（预算 50 行/4 万字符/单元格 4000，通常够）。**逐项可读性**：课程 ✅ 完整 / 知识点 △ 只有考过学过的（全量清单与 `detail` 在家长库，孩子侧无表无工具）/ 题目 △ 考后 ✅、**考前读不到（防泄题，应保持）** / **标准答案 ✗ 建议永不开放**（否则一问就套出答案，考核失去检验意义；讲错题靠原文+评语+课程资料足够）。**重考三态**：①同一场再来一次（家长设 `retake` → 服务端自动生成；孩子无出口，待拍板）②围绕错题另排（`child_exam_plan_create`，同类题非原题 = ISSUE-137 路线 B）③锁原题（协议级改造，二期）。**E7 与 F5 分工**：E7 = 读结果+讲错题（考后），F5 = 出题；E7 是 F5 上游。**本轮只出方案（含 [域 E] E7 三段式提示词规格：定位→读逐题→先给整体→一次只讲一两题→先问再讲→给同类练习；边界：不倾倒全部错题/不报内部数字/不承诺原题/不给答案/不评价"考得差"），未改代码** | 中-高 | 2026-09-23 | [详情](ISSUES/ISSUE-139.md) |

| 140 | **H1「提醒我…」的对话出口（工具 + 记录口径）**——用户要求：H1 要有对话出口、要有工具记录提醒、并说明**如何记录**。**核实结论：这是回归，不是新需求**——① 界面在承诺：`MyRemindersModal.tsx:132-136` 原文「你让 AI 伙伴帮你设的定时提醒」「想新增提醒，直接对 AI 伙伴说，例如"每天 9 点提醒我读英语"」，而弹框内**没有任何新建入口**，创建通道只有对话；② 后端在等：`POST /api/v1/scheduler/reminders` 注释原文「ISSUE-047：孩子端 agent 自建定时提醒……owner 默认 child（agent 创建）」；③ **工具没了**：`computeChildToolNames` 无任何提醒工具，旧工具 `schedule_task`（ISSUE-047 落在客户端 `electron/lib/custom-tools.ts`）**全仓已无命中** → agent 上移未迁移。**后端链路完整不用动**：创建/到期拉取（客户端每分钟轮询→语音播报+横幅）/列举三接口 + `db/task-runs.ts` 的 `createReminderTask`/`listChildReminders`/`takeDueReminders`。已有的 `create_reminders`（`worker/custom-task-tools.ts`）**只装给家长自定义定时任务执行会话**（批量数组、`owner=parent`、带 `source_task` 滚动替换），语义不同**不建议复用** → 孩子侧**新增 3 个小工具** `child_reminder_create`/`_list`/`_cancel`（单条 / 客话时间 / 只能撤自己的），装主会话+课程会话。**"如何记录"定死四步**：① 先 `get_date`（不许凭印象算日期）② **没说时刻必须先反问**（库层 `time` 必填）③ 日期→频率映射（今天/明天/后天/下周X→`once`+`fireAt`；每天→`daily`；每周X→`weekly`；每隔 N 分钟→`interval`）④ ⚠ **`interval` = "每隔 N 分钟一直提醒"，不是"过 N 分钟后提醒一次"**（"半小时后"= `once`+`fireAt`）。**边界**：不许口头答应不落库；重复/超上限如实说；只能给自己建；只在孩子在设备上时播报。**本轮只出方案，未改代码** | 中-高 | 2026-09-23 | [详情](ISSUES/ISSUE-140.md) |

| 141 | **I4「放视频」——家长上传的视频要有一个网页播放方式**（**用户明确要求单独建 issue 讨论**）。核实结论：**链路通了一大半，缺"最后一跳"**。① **上传侧已可用**：`POST /api/v1/materials/upload` 无扩展名/大小限制，`mp4/webm→"video"` 已被识别（家长端管理页已有 🎬 图标）→ 家长"整理上传"今天就能做；② **播放侧只认 HTML**：`display_content` 硬校验 `.html/.htm`（`display-tool.ts:50`），孩子端展示区 `MaterialsPanel` 的 `HtmlFrame` 只渲染 HTML → 视频要播必须有网页装着；③ **网页放哪决定能不能播**：**家长库** HTML（`topic/xxx.html`）走**真实 URL + `<base>`**（Web `/materials/p/<token>/…`；非 HTML 磁盘直读 + MIME + `Range`206）→ **同目录 mp4 能播**（英语课程页 `emma/*.mp4` 就是这么跑的）；**孩子工作区** HTML（`outputs/`）走 **dataURL 内嵌** → **无 base、相对路径全废 → 播不了**。**⚠ 第一条硬约束：播放页必须落在家长库，"让孩子 agent 写 HTML 放 outputs 里播视频"这条路线直接被否掉**。另：孩子 agent **读不到视频清单**（家长侧 `parent_list_materials` 不开放，孩子只有 `parent_content(主题+课程名)`）→ 「放哪个视频」现在无法判断（与 ISSUE-138 的 I1"列资料"同源）。**四条路线**：A 客户端直渲原生 `<video>`（最省，与 display_content 语义不再一致）/ **B 上传时自动生成同名播放页（推荐，落在家长库→天生能播、零家长操作）** / C agent 现场生成（撞硬约束，不推荐）/ D 家长端内联预览（不解决孩子侧）。**建议 B 为主 + A 兜底**。待拍板 6 条（路线/谁生成/是否开放视频清单/大文件上限/第一版控件/是否允许主动推送）。**本轮只出方案，未改代码** | 中 | 2026-09-23 | [详情](ISSUES/ISSUE-141.md) |

| 142 | **通用读写工具 `child_db_read` / `child_db_write` 的场景需要度与裁剪**——用户判断「从使用场景看，这两个工具没有需要」。**核实结论：写侧基本成立（但原因是链路没通，不是冗余）、读侧不成立**。① **写侧**：`daily_entries` 那半**确属冗余**（正统通道是 `kb_insert`/`kb_update`，多语义解析+去重+计划联动三件事，C 域一个场景都用不上）；`redemption_requests` 那半**是孩子侧唯一活着的兑换通道**，但**兑换链路整体没通**——`redemption_items`（商品表）**全仓无写入点**（只有建表 `db/kb.ts:322` + 登记 + 引用校验）→ 家长没有发布入口；`src/` `web/src/` `electron/` **全仓无兑换交互**（只有 RewardPanel/TodoModal 的"已兑换"展示）→ **文档里 G2/G3 写的"界面自带"是错的**（本轮改判为"没有出口"）；`POST /rewards/:childId/redeem`（`routes/plans-rewards.ts:659`）是**家长 JWT 且当场扣分**（与 §6#6"家长履约才扣分"冲突）、**客户端零调用点**。⇒ 删 write = 承认 G2/G3 彻底没出口。② **读侧删不掉**：**4 类数据、5 条场景只有它能读**——积分三表（B6/G1，无任何积分工具、kb_query 5 查不含积分）、掌握四列+`study_plans.result_summary`（F4/C1，`kb_query(progress)` 只 select 五列 `worker/kb-tools.ts:215`）、考核逐题三层（E7，无考核结果工具）、错题关联字段（F5，`child_mistake_log(list)` 返回不含，`child-db-tools.ts:262-268`）。③ **真正收获**：它是**"缺专用工具时的兜底"**（注册表 22 表 + 受控 SQL，是服务端实现方便的产物），不是场景需要的能力。**三条路线**：**A（推荐）先补 `child_points_report` / `child_mastery_report` / `child_exam_result` 三个专用只读工具（错题关联字段直接扩 `child_mistake_log(list)` 返回）+ 再删 `child_db_read`/`child_db_write`/`child_db_describe`**（接口契约已写进 issue；收益=把"有数据没口径"在工具层消解 + 安全面收窄）/ B 只删 `child_db_describe`（与 ③ 注入的表清单重复，零风险）/ C 现在就删（丢 5 条场景的"读出数据"，不建议）。附影响面 7 处（含 `prompt.ts`/`registry-prompt.ts` 的表清单文案要同步改）与验收口径 5 条。**当晚已按路线 A 落地**（用户拍板「既然梳理了场景，就针对场景设计工具或优化现有工具吧，不用通用工具了；再遇到没考虑到的场景就再增加工具」）：新增 `server/src/agent/child-report-tools.ts`（`child_points_report` / `child_mastery_report` / `child_exam_result`）+ 撤掉通用三工具 + 扩 `child_mistake_log(list)` 返回（课程/知识点/有无原题）+ 同步提示词与元数据块（`registry-prompt.ts` 不再暴露表列清单）+ 新增 11 例回归；`tsc --noEmit` 通过、新测试全绿。详见 §九 | 高 | 2026-09-23 | [详情](ISSUES/ISSUE-142.md) |

| 143 | ✅ **已修复 + 已部署 201（0.5.9，2026-09-24 11:16），真录音回放验证通过**：发音测评全线报「音频文件不存在」——ISSUE-131 P2 归并后写入点全切 `workspaces/<pid>[/cid]/uploads`，但 `routes/assessment.ts` 的 `readAudioBytes()` 私有复制了一份旧根路径解析（只查 `files/<pid>/`）→ 0.5.5（09-23 09:05）上 201 后凡「上传录音→评测」100% 失败：家长端设置页「测试」报错、09-24 背诵考核口语题全部无发音评分（录音本体与 `audio_file_id` 仍在，「听原音」不受影响）。**生产实测证据**：files 表当日最新 8 条全「旧根=false 新根=true」、旧根 09-24 新增=0（最后一批止于 09-23 07:47）、journal 评测请求几十毫秒瞬时失败且**零日志留痕**（ApiError 被 catch 转 502 只回客户端，坏了一天才被发现）。修复：`readAudioBytes` 改用 `files.ts` 已导出的 `resolveStoredFileAbs`（新根优先、旧根永久兜底）+ 评测失败补 `req.log.warn` 留痕；客户端零改动。回归 `test/issue143-assessment-audio-path.test.ts` 6 用例（新根孩子区/新根家长区/旧根存量兜底/有记录无文件/未知 id/缺 token 401），注入旧逻辑验证非假绿（①②③全红）；server tsc 0 错、相邻回归（issue131-p2-merge + issue135-exam-routes）23 用例全绿、bundle 构建通过（含 `resolveStoredFileAbs`）。**部署（0.5.9，随包 ISSUE-142 + ISSUE-144 P0~P4+P7，提交 195f7ab/11c8794/87eb421）**：真录音回放（今早 07:51 失败考核的 voice-q2/q3/q4，按 audio_file_id 反查真实 ref_text）3 条全部 200 返回真实评分（pron=17/32/42）；备份 `server.cjs.bak-20260924-1116` + `data/backups/deploy-0.5.9-20260924-1116/`；脚本 `tmp/deploy/verify_059_api.js`。⚠ 考核明细/files 表 `created_at` 是 **UTC** ISO，按本地日期 LIKE 会整批漏行（验证脚本第一版踩中）。可选：对 09-23 09:05 后两场考核重评补写 `speech_assessments`（需一次性脚本）；201 磁盘 ~95% 待清旧备份（需用户确认） | 高 | 2026-09-24 | [详情](ISSUES/ISSUE-143.md) |
| 145 | ✅ **已修复 + 已部署 201（0.5.10，2026-09-24 15:53；web 前端包同步换装）** —— **家长端两个「服务端正常、客户端却坏事」的缺口：①SSE 静默停摆（永远「等待模型返回」）②会话历史从不回填（刷新/重进一片空白）**——2026-09-24 14:18/14:23 两轮（含「继续」）气泡停在「等待模型返回…（已等待 1 分 52 秒）」，**服务端与模型完全正常**（会话落盘两轮均完整收尾：14:19:30 方案确认 439 字 / 14:23:38 ✅已完成 242 字；只读 SSE 回放探针 `lastEventId=1` 拿到 `message_end→agent_end→turn_end` 全链 #1428~#1550）。**故障段＝服务端→浏览器 JS**：那条 `192.168.1.200:58185`（12:33:57 建连）到 14:35 仍 ESTAB，`ss -ti` 40s 采样 `bytes_sent` 216103→216142（+39＝3 次 15s `: ping`）、`bytes_acked` 全额跟上 ⇒ 路由 keepAlive 与 hub 订阅都还在、事件写得出服务端也进得了浏览器；但渲染层一条都没消费（气泡「等待模型返回」+「响应较慢」＝无 thinking/无工具，而这两轮必有 301 条 thinking_delta + 8 次 tool_start/end），且**全天只有一次 stream 请求 ⇒ 客户端从未重连**（读循环既无 done 也无异常＝静默停摆）。**根因＝缺口叠加**：①服务端 15s `: ping` 心跳被客户端 `parseSseChunk` 当注释直接丢弃、不刷新任何"最后收数据"时间戳 → 无存活判据；②`openSse` 只在 `done`/`throw` 时重连，无超时看门狗 → 静默停摆永不重连；③`pi:sse_state` 通道已存在但 `ParentChatPanel` 未订阅 → 断流对家长不可见。**与 ISSUE-096（同类先例，修的是"断开后从不重连"）互补，本案是其未覆盖路径**。**排除项**：201 线上 bundle（`index-BiRAcmS8.js` 09-22）与本地 12:20 重建的 `index-CbfDa-gx.js` 的 `openSse` **逐字等价** ⇒ 重新部署 web 包对本问题无效。**立即恢复**＝硬刷新；**治本方案待拍板**：P0-a 心跳看门狗（任何字节含 ping 刷新 lastByteAt，45s 超时 abort → 走既有 lastEventId 续传重连）+ P0-b 轮内 60s 静默提示 + P1 订阅 `onPiSseState` 显示重连横条 + P2 可选服务端只读诊断端点。**未改任何代码、未部署**。**②历史不回填（用户补充症状）**：全天来自浏览器的 `POST /parent-agent/open` = **0 次**（14:46 那两次是我的探针，来自 127.0.0.1）⇒ `web/src/shim/domains/agents.ts` 的 `piStartParent`/`piStartParentContent` 把 history **写死成 `[]`**（误以为"历史由服务端事件推送驱动"，实际服务端只在 prompt 时推增量）；服务端接口两处实测均正常（201 → messages=26 / 本地 8788 → messages=12 可还原 5 个气泡）。**修复（已实施，未部署）**：①两端 `openSse` 加「**任何字节（含 `: ping` 注释行）刷新 `lastByteAt`** + 45s 看门狗 `abort()` 走既有重连（带 lastEventId 续传）」+ **每次 `connect()` 新建 AbortController**（旧实现复用同一个 ac → abort 之后退化成死循环重连）；②web shim 新增 `openParentSession(kind)` 并让两个 start 真回填（失败 `console.warn` 留痕，不再静默吞空）；③`ParentChatPanel` 回填改为不覆盖期间已到的流式增量；④新增 `test/issue145-sse-stall-and-parent-history.test.ts` 5 条守护（沿用静态扫描风格），**注入旧逻辑 → 对应用例准确变红（防假绿）→ 还原复绿**。验证：web build 通过（`dist/assets/index-dyU_VfIB.js`）、30 用例全绿、改动点 tsc 0 错（仓库既有 5 处 `src/` 组件报错与本次无关）。**Electron 桌面端需重新打包（`npm run dist:win`）才生效**；P0-b 轮内 60s 提示与 P2 服务端诊断端点未做。**✅ 部署实测（0.5.10，2026-09-24 15:53）**：201 的 web 前端此前停在 09-22 07:46 的 `index-BiRAcmS8.js`（`parent-agent/open`=0 次 / `未收到任何数据`=0 次 ⇒ **线上一直跑的是含两个缺口的旧包**），换装为 `index-BSngxzoI.js`（md5 `6e1f8266ee27dfa41cfe5589579dd85d` 两端一致，三个 marker 全中）；自签家长 JWT 直连 `127.0.0.1:8788` 实测 `POST /parent-agent/open{kind:parent} → 200 messages=26`（历史回填确有真数据）、`GET /parent-agent/stream` 35s 观察收到 hello=1 + `: ping`=2 + bytes=139（**送达链路非静默**，正是故障的反面）、`/api/v1/version → 0.5.10`、health ok、journal ERR_COUNT=0。⚠ **Electron 桌面端（含 201 上的 xuexihub 0.1.15）仍不含本修复**，须 `npm run dist:win` / 重打 deb 才生效；`lastEventId` 回放未实测（服务重启后 hub 为空，需真跑一轮才有事件可回放） | 高 | 2026-09-24 | [详情](ISSUES/ISSUE-145.md) |
| 146 | **服务端 240s「挂死看门狗」误杀长时工具轮**——用户问「加了 45s 看门狗，长时工具（制作网页）会怎么处理？」，排查结论：**45s 那条不会误伤（判据是"流上有没有字节"，而服务端 `: ping` 心跳在建连时无条件起表，与 agent 忙不忙无关；且 `prompt` 路由是提交即返回、SSE 断开只 `unsubscribe()` 不 abort 会话 ⇒ 重连不打断任务）**；**真正在误杀的是服务端 240s 那条**（`parent-registry.ts:234` / `session-registry.ts:481`，判据是"agent 会话 240s 没 emit 任何事件"）。机制：`parent_build_material` / `create_html_lesson` 的 `execute` 里 `await` 一个**独立嵌套编程会话**（`programming-agent.ts:210,226`），该会话**全文无 subscribe/publish ⇒ 事件一个都不进 hub**，父层在工具执行期间只有 `tool_start` 一个事件、之后静默到 `tool_end`（全仓**无任何工具用 `onUpdate`**，SDK 的 `tool_execution_update` 从未产生过）。**生产实证（201 只读 journal）**：制作网页实测 9 次 `min=28.0 / median=321.4 / max=654.2s`，**6 次 >240s**；全历史 `判定挂死` 8 次（6 家长 + 2 孩子），其中 **5 次与 09-20 的 lesson-01~05 生成完成 1:1 严格配对**——最尖锐的一条 `12:32:51 parent 判定挂死` → `12:32:52 生成完成 lesson-01（242.8s）`：**外层被 abort 后 1 秒嵌套会话才跑完**。后果：①报错文案「**模型服务超过 240 秒无响应**」**归因完全错误**（模型好好的，是系统自己砍的）；②产物其实已落盘（`生成完成` 日志在非空校验之后）却被当失败，家长重试→重复烧 4~11 分钟；③一句话多轮任务被砍断上下文。⚠ 另 3 次挂死**无配对生成记录，很可能是这条看门狗本要抓的"模型 API 真挂起"⇒ 不能简单删，只能把判据改准**。**✅ P0-a + P0-b 已实施（2026-09-24，本地验证全绿：server tsc 0 错 / 三端构建 exit 0 / 新增守护测试 26 全绿 + 相关 10 文件 150 全绿 / 全量失败与既有基线「8 files 15 tests」逐项一致 / bundle 烟测 health ok；**未部署**）**：**P0-a**「工具执行期间不计入静默」（`runningTools` 计数，`tool_execution_start` +1 / `tool_execution_end` −1，watchdog 见到 >0 即跳过）+ 工具轮另设 30 分钟硬上限且错误文案改为「工具 X 超时」；**P0-b** 让长工具有进度（接 SDK `execute` 第 4 参 `onUpdate` 节流推 `tool_execution_update`，或嵌套会话 subscribe 后映射成独立 `tool_progress` 事件，**别污染 `turnTextBuffers`**）；**P1** 回放 gap 检测（`replayAfter` 不告知"你漏的已被挤出 500 条缓冲"，建议补发 `gap` 事件让客户端重拉 `/open`）。⚠ 取证踩坑：`sudo` 不带 `-S` 传密码会**静默返回空**（第一版脚本因此误得"0 次"结论）；`journalctl -o cat` 无时间戳，配对必须用 `-o short-iso`。**✅ P0-a + P0-b 已实施并部署 201（0.5.10，2026-09-24 15:53）**：bundle marker 复核 `runningToolCount`=3 / `TOOL_EXEC_TIMEOUT_MS`=8 / `tool_progress`=3 / 旧文案「判定挂死」=0（已改为按分支插值文案）；`/api/v1/version → 0.5.10`、health ok、journal ERR_COUNT=0、服务 15:53:28 正常监听并起 worker。⚠ **真实长工具轮尚未复跑**（需一次 >240s 的 `parent_build_material`/`create_html_lesson` 才能实证「不再被砍中段」，本轮只证明了新判据代码在位与启动无异常）；P1 未做 | 高 | 2026-09-24 | [详情](ISSUES/ISSUE-146.md) |
| 147 | **「用工具调另一个 agent（编程 agent）」是不是 pi 里的最优范式？——pi 委派原语盘点与方案选型**——用户问「pi 是否有更好的方案，比用工具好的方案」。逐条核实 pi SDK 0.84.1 后：**pi 官方刻意不做 sub-agent**（`README:500`「No sub-agents」），而其**唯一参考实现 `examples/extensions/subagent/` 本身就是一个工具**（`registerTool` + `exec` spawn 子进程 + JSONL 流解析）⇒ **「用工具调子 agent」就是官方范式，不是错路**；本项目与官方的差距在**四个机制**：① 子会话 `subscribe`（编程会话 0 处，`parent-registry.ts:240` 有现成样板）② `onUpdate` 进度回报（全仓 0 处，唯一命中是未使用的 `_onUpdate` 形参）③ `signal`→`abort` 传播 ④ 子 agent 定义外置（官方 markdown frontmatter 配 `model`/`tools`，本项目硬编码）。**换子进程形态不划算**（`server.cjs` 已 esbuild 内联 pi；模型 runtime 从 DB 动态读，跨进程要重建凭据链）。**推荐路线 B：进程内补齐四机制**（一举修掉 ISSUE-146 的 240s 误杀 + 「黑洞 11 分钟」）——**✅ P0 已于 2026-09-24 实施并随 0.5.10 部署 201**；P1 把硬编码 26 行 prompt 外置（⚠ **必须沿用 ISSUE-144 的自建技能机制**：TS 常量正文 + 自建索引 + 自建 `load_skill`；**不启用 SDK 原生 Agent Skills**——144 §四·2 已定案，2026-09-24 修正了本行原「pi 原生 Skill」的错误表述）；P2 并行编排（官方 8 任务/4 并发；本项目单份中位 **321.4s**，「5 课资料」串行约 27 分钟且途中被砍）。附带发现：`sessions` Map 永不释放（每份资料一个常驻 inMemory 会话）。**P0 已随 0.5.10 部署；P1/P2 未做** | 中高 | 2026-09-24 | [详情](ISSUES/ISSUE-147.md) |

| 144 | ✅ **P0–P7 全部执行完毕（2026-09-25）**：家长 agent 的「场景技能（skill）」——把写死的长基底提示词改成「**常驻层（2185 字符）+ 8 个按需加载的场景技能（正文 19315）**」，配套 **P7 工具说明下沉 + 场景守卫**（工具块 25078 → **13786**，-45%）、**P4 三把场景专用读工具**（考核/掌握/积分）、**P5 家长自定义层**（设置 →「场景口径」，按场景覆盖 + 红线校验 + 不可覆盖条款 + 跨家长隔离修正）、**P6 通用数据通道整组退场**（`parent-data` 会话 + `parent_db_read/write/describe` + `define_namespace` 全删；口径「以后场景缺工具就加工具」）。验收：`tsc` 0 错、**三端构建 exit 0**、单测 skills 36 + P4 报告工具 18 + P5 覆盖层通道 8 全绿、全量回归零新增失败（8 files/15 tests 为既有基线）。**行为层 20 条已实跑（2026-09-25）**：13/20 完全命中、2 条负样本 100% 守住（真账号 test@qq.com + mimo-tokenplan/mimo-v2.5），报告 `docs/家长agent-行为样本实跑-2026-09-25.md`（含 3 条待修真缺陷）。**未做**：P3.2 界面显示“当前场景”（家长明确不需要）、样本 #7/#8 与 P5 双账号实跑。**家长拍板**：Tier 2 自定义数据**先冻结**、兑换扣分时点**以后再讨论**、P5 粒度**不加全局层**。详见 `ISSUES/ISSUE-144.md` | 高 | 2026-09-25 | [详情](ISSUES/ISSUE-144.md) |
