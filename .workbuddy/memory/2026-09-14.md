# 2026-09-14 工作日志

## 更新说明书（学习伙伴-用户使用说明书.md）
- 按 §17 #13 与代码核对结论，整文件重写修正（同文件并行编辑发现竞争问题，改为整文件一次性写入）：
  - §2.2/§5.2 孩子密码：「仅存本地/不上传」→「加密存服务端、多设备一致、本地仅离线回退」。
  - §4.5/§5.3 发音评测：「客户端本地完成」→「服务端完成（凭证收归服务端加密）」。§3.10 评测配置补「凭证加密存服务端、多设备同步」。
  - 移除已下线的「AGENTS 提示词编辑」设置项（§3.10）、「HTML 经安全消毒」不实表述（§3.9）、配置管理能力域里的「提示词」（§3.7）。
  - Q7 由「提示词版本覆盖」改为「配置约 2 分钟内同步」；版本号更新为服务端 0.4.x。

## 处理待办 issue（086~092 全部解决）
- **ISSUE-086**：`AddChildModal.tsx` 密码标签「登录密码（仅存本地）」→「登录密码」；顺带清理 Dashboard 两处遗留「AI 提示词」文案。
- **ISSUE-087**：`parent-plans.ts` 新增 `parent_child_create`/`parent_child_update`（写主库 children，不设密码）；`maxChildrenAllowed` 读 `parents.license_json.max_children`；`routes/children.ts` POST /children 补服务端 max_children 兜底校验（与 agent 一致）。
- **ISSUE-088**（客户端迁移遗留清理）：删除 `session-sync.ts`/`sync-logger.ts`/`server-features.ts`/`app-config.ts`/`assess-tools.ts` + `src/components/SessionSyncPanel.tsx`；main.ts 解绑 5min 会话同步与 10min /version 探测；ipc-handlers 删 4 个 sessions:sync* 处理器；preload 删 4 个 sessionSync* API；Dashboard 删「会话同步」卡片+view；`scheduler.ts` `DEFAULT_EVENT_POLL_CONFIG.enabled=false`（云端 eventPoll 下线）；`electron.vite.config.ts` 去掉 SDK 的 exclude（不再 bundle）。根 package.json 的 SDK 依赖保留（仅 scripts/*.mjs 与 packages/agent-core 开发期用）。
- **ISSUE-089**：`parent-tools.ts` 把 `upsertTopicTool`/`upsertCourseTool` 加进 return 数组。
- **ISSUE-090**（P0 场景课）：`server-agent-client.ts` `translateAgentEvent` 补 `page_cmd` 分支 → `pi:page:exec`；`ipc-handlers.ts` `pi:page:exec:result` 补 childId + `postPageResult` 回执。
- **ISSUE-091**：`plan-tools.ts` 新增 `child_study_plan_create`/`child_exam_plan_create`（creator=child/task_type=optional）；`parent-plans.ts` 新增 `parent_recurrence_create/list/update`（补 plan_recurrences 写入路径）；session-registry（import+customTools+白名单）+prompt.ts 接线。
- **ISSUE-092**：`parent_exam_plan_create` 增加可选 `methodSpec`（require/exclude/recitePass）写入 exam_schedules.scope。

## 验证
- server `tsc --noEmit` + esbuild 构建通过（dist/server.cjs 含全部新工具）。
- 根 `tsc --noEmit`（electron+src+packages）：仅 5 条预存的环境级全局类型错误（TS2318/TS2552，与本次改动无关），触及文件无类型错误。
- 技术文档 §0.4/§2.5/§3.4/§5.1/§5.4/§6.4/§17 全部同步；ISSUES.md 索引 086~092 标记已解决，计数修正为 92。

## 遗留/待跟进
- 根 package.json 的 `@earendil-works/pi-coding-agent` 依赖彻底移除需先迁移 scripts/*.mjs（prompt 校验脚本）与 packages/agent-core 的去向。
- 云端 eventPoll 仅默认关闭，`delivery.ts`/`handleCloudInbox` 整链路可后续整体删除。
- 未跑 electron-vite 全量 build（较重）；本次 electron 改动均为纯移除 + 分支/回执补充，建议发布前跑一次。

## 计划域 CRUD 对称性补齐（用户核对「两侧是否都有三表增删改查」后）
核实发现缺口并补齐（2026-09-14）：
- **孩子端**缺 study/exam 的改删：`plan-tools.ts` 新增 `child_study_plan_update` / `child_exam_plan_update`（delete/reschedule，仅限 `creator='child'` 行，`done` 拒绝）；session-registry（import + customTools + 白名单）与 prompt.ts 接线。
- **家长端**缺 exam_plans 的「查」：`parent_exam_plan_list` 扩展为两段输出（主库排期 + 孩子库已生成场次含孩子自请），`parent_exam_plan_cancel` 的 id 现在可直接从 list 拿；描述同步更新。
- 最终矩阵：家长 = study 全 CRUD / life 全 CRUD / exam（场次改删 ✅、查 ✅、直接建场次设计上无——走排期生成）；孩子 = 三表均 create/list/update（study/exam/life 的 update 仅限自己创建行）。家长 study/life 可管理任意 creator 行（含孩子的加分项）。
- server `tsc` + esbuild 构建通过；技术文档 §5.1（家长/孩子挂载清单）与 §10 孩子挂载列表已同步。

## 术语纠正（用户指出）：exam_plans ≠ 场次，exam_attempts 才是场次
- 正确口径：`exam_schedules`（主库）= 排期配置；`exam_plans`（孩子库）= **考核计划**（进「今日计划」的计划行）；`exam_attempts`（主库）= **考核场次**（孩子真正提交的一次考试，逐题记录在 `per_question`）。
- 代码改正：`parent-plans.ts`（parent_exam_plan_list/cancel 的 label/描述/错误文案/返回语）、`db/kb.ts`（exam_plans 表注释）、`worker/plan-domain.ts`（头部与 applyExamAttempts 注释）、`routes/exam.ts`（cancel 路由注释与 404 文案）。
- 文档改正：技术文档 §5.1 分层段改为「三张表」（排期/考核计划/考核场次）、§6.1 三表说明、§6.3 schema 注释、§6.4 取消行、§17 #11 补术语口径；ISSUE-085 标题下加术语纠正注。
- 保留的正确用法：settleRewards/stat 日志里的「考核场次」指 exam_attempts，不动。
- server `tsc` + esbuild 重建通过。

## 考核域架构重构（ISSUE-094，用户定案）：取消 exam_schedules
- **定案**：① 排期表 `exam_schedules` 取消；② 自定义考核直接写 `exam_plans`；③ 每日/每周固定考核改为配置项（`exam_fixed:<parentId>`，设置页 UI 不变）；④ worker 每天检查配置生成当天考核计划。
- **实施**：`db.ts` DROP TABLE；`routes/exam.ts` 删 `ensureFixedSchedules`/`normalizeExamScheduleDays`/`freqToMs`/废弃 `buildSelectionPrompt`，新增 `ensureTodayExamPlans`（幂等生成当天 fixed 计划，daily 每天生成/weekly 按配置周几/同日双档只留 weekly），`parent_exam_plan_create` 与 `POST /exam/schedules` 直写孩子库 `exam_plans`（同日去重），`GET /schedules`、`start/complete/DELETE`、`GET /config?schedule=`、`POST /attempts` 全部改为操作 `exam_plans`（路由路径与响应形状保留，渲染层零改动）；`worker/scheduler.ts` runPlanTick 接入每日生成。
- **副产品**：ISSUE-075 的无作用域 DELETE（`normalizeExamScheduleDays` + 固定排期清理）随表取消彻底消除，ISSUE-075 关闭。
- **验证**：server tsc + esbuild 通过；产物中 exam_schedules 仅剩 DROP 语句。踩坑：schema 模板字符串里的行内反引号（`exam_fixed:...`）会终止 JS 模板串导致语法错误，SQL 注释里不要用反引号。
- 文档 §0.2/§5.1/§5.4/§6.1/§6.3/§6.4/§17 #11+#14/附录 A 同步；ISSUES.md 计数 94，075 关闭、094 新增。

## ISSUE-098 修复执行（kind 收敛 + 题库结构化）
- **A kind 收敛**：child_exam_plan_create / POST /plans/exam 改 kind='custom' 且 courses 必填（scope_json.courses，解析不到真实课程名不落库）；kb.ts 幂等迁移 UPDATE kind='self'→'custom'。config 路由无需改（只认 custom）。
- **B4 note 进出题链路**：exam.ts custom 分支把 plan scope.note 拼进每课 assessMethod（【本次考核要求（家长说明）：…】）。
- **B 题库结构化**：
  - ⚠️ 事故：seeding 前误清 question_bank（清理守卫只查 max(created_at) 漏判 09-09 存量 3575 行）。已从 `.workbuddy/tmp/rehearsal` 快照 100% 恢复（server/scripts/restore-question-bank.mjs，保留原 id/created_at），**该快照目录勿删**。
  - 最终：489 门 lunyu 按旧分类挂 KP（背诵/句意白话/道理/字词/典故，1969 KP）+ 孝经 18 /千字文 30 模板挂 3 KP（seed-knowledge-points.mjs，背诵题 answer 提取自 teaching_copy 原文/白话/道理段）。全库 537 门结构化课、488 道背诵题（qb 3719 / kp 2113 / 挂载 3719）。
  - 两个脚本均幂等，落 server/scripts/。
- 验证：server tsc + esbuild 通过；学而篇第一章抽查（背诵题 speech_recite + answer 逐字原文）通过。
- 教训：清库脚本守卫必须校验 min(created_at) / 按日期范围删，不能只看 max；运维脚本先 dry-run、快照先行。
- ISSUE-098 状态更新：①已解决（结构化路线）；②性能优化（判分每题重建 session 等）未做。

## 出题方法优先级显式化（用户提问驱动）
- 现状澄清：结构化选题层优先级本就正确（计划级 methodSpec > 主题 method_spec > 默认）；LLM 出题层原本只有"方法优先于通用规则"一句，且计划 note 拼在方法末尾（实际优先级最低）。
- 改动：exam.ts custom 分支把 scope.note 改拼在 assessMethod **最前**（【本次考核要求（…优先级最高）：…】）；buildCourseGenerationPrompt 方法块头显式声明三级优先级（本次要求 > 主题方法 > 通用规则，冲突以高优先级为准）；GENERATION_PER_COURSE_RULES 补充说明"禁背诵"不因方法让位的原因（背诵题需题库原文做发音评测，LLM 无原文不可硬编）。
- 踩坑：esbuild 默认 charset=ascii，产物中非 ASCII 是**大写** \uXXXX 转义——用 raw/小写 JSON.stringify 转义检查字符串会误报"不存在"。
- server tsc + esbuild 通过（产物经转义形式复核确认含新文本）。

## 修复「珊珊新建考核出题失败」（题库恢复后的连带 bug）
- 现象：珊珊新建「学而篇第一章背诵考核」（child 工具新版正常落库 kind='custom' + scope.courses），考核页提示出题失败；审计无 generate 行 → 客户端拿到的是结构化**空** questions。
- 根因（两层叠加）：① 论语主题 method_spec 的 require/exclude 键是**旧分类体系 uuid**（快照 topic_categories id），题库恢复后 KP id 全为新值 → 全不命中 → picked 空；② 代码缺陷：主题级 spec 的 require 只认 uuid 不认名称（仅计划级 override 走名称解析）。
- 修复：assess-selection.ts 主题级 spec 改走 resolveOverride 统一解析（uuid 或名称，全解析失败回退全考）；数据修复 lunyu method_spec 键 → 知识点名（require 背诵/句意白话/道理 各1，exclude 字词/典故）。
- 模拟验证：学而篇第一章选出 3 题（背诵 speech_recite 置首 + 句意白话 + 道理），符合家长方法预期。
- 教训：跨课共用的主题级 method_spec 键只能用知识点名（uuid 每课不同）；题库重建/恢复后必须排查 method_spec 的旧 id 引用。

## 「只考背诵」计划仍按默认出题的修复
- 根因：珊珊计划的 scope 只有 courses，「只要背诵」只在标题文字里，选题层读不到 → 按主题方法默认出 3 题。
- 修复：① child_exam_plan_create 增加可选 methodSpec 参数（require/exclude/recitePass，键=知识点名）并写 scope_json.methodSpec；孩子 prompt 加用法引导（只考背诵→{"require":{"背诵":1}}，没提特殊考法不传）。② KP 名全库统一：本章原文背诵→背诵、句意翻译→句意白话、道理应用→道理（48 行×3），全库 5 种标准名（背诵 488/句意白话 508/道理 437/字词 390/典故 290）。③ 数据修复珊珊已有计划 a5917018 补 methodSpec.require={"背诵":1}。
- server tsc + esbuild 通过；需重启 dev server 生效。

## 计划即契约：出题参数前移到计划生成时（用户定案，2026-09-14）
- 定案：考核计划生成时就约定**全部**出题信息（课程 + 每课知识点 + 各几题）；课程必须有知识点和题库题，否则不能生成计划；出题环节严格按计划抽题 → **不再有结构化/非结构化之分，LLM 出题链路退出考核流程**（判分仍用 LLM）。
- 实施：
  - scope_json 升级：`courses: [{title, kps:[{name,count}]}]`（物理列不加，scope_json 内结构化承载）；
  - assess-selection.ts 新增 `buildPlanSpecEntries`（创建时展开：默认按主题 method_spec 过滤、计划级 methodSpec 严格覆盖、无内容课记 missing）与 `attachPlanQuestions`（出题时严格按约定抽题，缺题记 problems）；
  - 接入点：parent_exam_plan_create / child_exam_plan_create / POST /exam/schedules（面板）/ worker ensureTodayExamPlans（固定计划生成时展开写入 scope，无内容课跳过并记日志，全无则不生成）；
  - config 路由 custom/fixed 分支改为按 scope 约定抽题；旧格式（字符串课程+methodSpec）现场懒迁移展开；出题失败（内容缺失/不一致）返回 400 带明确原因。
  - 自检脚本 scripts/check-plan-spec.mts（tsx）：4 用例全过（只考背诵 1 题 rq+refText、默认主题过滤 3 KP、无内容课被拒、按约定抽题）。
- server tsc + esbuild 通过；技术文档 §6.2 同步。

## 修复考核页「最后一题答完提交按钮仍灰」
- 根因：背诵/文字题允许多段录音拼接、答题后**刻意不锁定**（靠切题时 saveCurrent 补锁），而提交按钮启用条件只统计 locked；最后一题没有"下一题"可切 → 永不锁定 → 按钮永远灰。纯背诵单题卷（只考背诵改造后）必现。
- 修复（src/lib/exam-template.ts）：① updateDone 的"已答"判定放宽为 locked **或 hasAnswer（有录音/文字）**；② 提交点击 handler 先遍历补锁（未锁但有内容 → 补 answeredAt/durationMs/locked，口径同 saveCurrent）再校验提交；③ asr:done 与 asr 手输后补调 updateDone（此前只有 paintState 触发刷新）；④ 顺修 hasAnswer/paintState 对无 segs 答案对象的潜在 TypeError。
- electron-vite build 通过（renderer 2114 模块）；需重启客户端生效。

## 修复「孝经背诵考核仍出多题型」（agent 漏传 methodSpec）
- 库记录核查：计划 scope.courses 约定了 3 个 KP（背诵/句意白话/道理 各1），note 写着"背诵考核…"但 note 不参与范围计算——家长 agent 没把「只考背诵」转成 methodSpec（新架构工作正常，是意图转译缺失）。
- 修复：① parent_exam_plan_create 描述加「说法→methodSpec」对照表 + 明确"意图必须转 methodSpec，不能只写 note"；② 服务端确定性兜底 `inferReciteOnlyFromNote`（assess-selection.ts）：note 含背诵且不含其它类别词 → 自动 require {背诵:1}，两侧创建工具接入，返回消息注明"（依据考核说明自动识别）"；③ 创建返回消息改为输出完整出题约定（每课 知识点×题数），agent/家长可立即发现不符。
- 自检 scripts/check-note-infer.mts 5 用例全过（含"背诵+讲道理"不误判）；server tsc + esbuild 通过。

## 新增 ISSUE-093（发音评测测试展示错乱）
- 用户反馈：家长端配置阿里服务（aliyun-ssecp）后点「测试评测」，结果文案「总分 undefined 分，准确度 90，流利度 [object Object]（1 个词）」。
- 根因已定位：`src/components/AssessmentSettings.tsx` 的 `handleTest` 读 `res.score`/`res.fluency`，但服务端 `toSpeechAssessment` 把总分放进 `pron`、流利度包成 `{overall}` 对象；`SpeechAssessment` 契约总分字段是 `pron`（考核判分 `speechPronToPoint` 也读 `pron`）。
- 修复：展示层改读 `res.pron` 与 `res.fluency?.overall` 即可。详情见 `ISSUES/ISSUE-093.md`，索引计数更新为 93（标记中优先级、待处理）。

## 新增 ISSUE-095（家长端停止 agent 不生效）
- 用户反馈：家长端点「停止」，消息显示「已停止」但 agent 实际还在跑。
- 根因已定位：`electron/lib/ipc-handlers.ts` 的 `pi:abort` 是 no-op（注释明写"服务端尚无中止一轮能力，联调点"）；服务端 `routes/parent-agent.ts` / `agent/parent-registry.ts` 无 abort 端点/AbortController；孩子端 `Learn.tsx` 走同一 no-op，故两端停止都无效。属 ISSUE-080/081 agent 服务端化遗留的"中止一轮"联调点。
- 修复方向：服务端 `parent-registry` 持 AbortController 并在运行循环逐步检查；`routes/parent-agent.ts`(及 agent.ts) 加 abort 端点；`pi:abort` 改为调该端点。前端 UI 无需改。详情见 `ISSUES/ISSUE-095.md`，索引计数更新为 95。

## ✅ 解决 ISSUE-093（发音评测测试展示错乱）
- 改动文件：`src/components/AssessmentSettings.tsx` 的 `handleTest`：
  - `res.score` → `res.pron`（总分权威字段；`?? res.overall` 兜底消除 undefined）
  - `res.fluency` → `res.fluency?.overall`（流利度是 `{overall}` 对象，取数字消除 `[object Object]`）
  - `res.completeness` → `res.integrity`（完整度字段名对齐）
  - `res.words.length` → `res.words?.length ?? 0` 防御；缺失字段文案落「未返回」
  - 新增 `import type { SpeechAssessment } from "../../electron/lib/exam"`（同 ExamRecords.tsx 写法）
- 现象对照：修复后文案应为「总分 NN 分，准确度 NN，流利度 NN（1 个词）」；正式考核判分走 `speechPronToPoint` 读 `pron`，本就正常不受影响。
- 验证：Windows 端配置 `aliyun-ssecp` 点测试评测确认；建议再回归 `tencent-soe` 一次（同契约）。
- ISSUE-093 索引行已标记 ✅ 已解决（2026-09-14）。
- **用户实机验证通过（07:47）**：配置 `aliyun-ssecp` 点「测试评测」文案已正常（总分/流利度/完整度显示正确，无 undefined / [object Object]）。ISSUE-093 闭环。

## ✅ 解决 ISSUE-095（agent「停止」不生效：pi:abort no-op → 接通服务端中止）
- 根因：agent 上移服务端后停止链路断——主进程 `pi:abort` 是 no-op、服务端无 abort 端点，家长/孩子端点停止只改 UI 不真停。
- 关键发现：SDK `@earendil-works/pi-coding-agent` 的 `AgentSession` 原生有 `abort(): Promise<void>`（中止当前操作并等 agent idle），无需自造 AbortController/cancel 标志。
- 改动（6 文件）：
  - `server/src/agent/session-registry.ts`：新增 `abortSession(parentId, childId, kind?)`——kind 省略时中止该孩子全部会话（main/scene/course:*）中 busy 的一轮；
  - `server/src/agent/parent-registry.ts`：新增 `abortParentSession(parentId, kind?)` 同构；
  - `server/src/routes/agent.ts`：新增 `POST /api/v1/agent/:childId/abort`（家长 JWT + assertChildOwned，body.session 可选）；
  - `server/src/routes/parent-agent.ts`：新增 `POST /api/v1/parent-agent/abort`（body.kind=parent|parent-content）；
  - `electron/lib/server-agent-client.ts`：新增 `abortChildAgent` / `abortParentAgent`；
  - `electron/lib/ipc-handlers.ts`：`pi:abort` 改为按 childId 语义（"parent"/"parent-content"→家长会话，否则孩子）调服务端，失败仅记录不阻塞。
- 中止后 submit 的 finally 清 busy + SSE 推 turn_end → 前端 pi:reply_end 解禁忙碌态；前端 handleStop 零改动。
- 验证：server `tsc --noEmit` 通过；根 `tsc --noEmit` 仅 5 条预存环境级错误（TS2318/TS2552，与本次无关）；esbuild 构建通过（dist/server.cjs v0.4.0，含 abort 路由）。实机验证：家长对话中点停止应立刻无后续工具/回复；孩子端回归一次。
- 技术实现文档 §0.1 已同步（路由清单 + abort 链路说明）。

## ✅ 解决 ISSUE-096（服务端重启后 agent「永远思考中」：SSE 断线从不重连）
- 用户反馈（10:54）：本地环境家长 agent 发「你好」一直显示思考中、无输出。
- 排查（证据链）：本地服务端 0.4.0 正常（uptime 刚重启）→ `server/data/logs/server-log.jsonl` 每次POST /prompt 200 → parent 会话 jsonl 落盘里每条消息都有完整 assistant 回复（「你好」回复已生成，stopReason: stop）→ **服务端正常，纯事件送达链路断**。
- 根因：`electron/lib/server-agent-client.ts` `openSse` 一次性连接，断后死；`ipc-handlers.ts` `ensureParentStream/ensureChildStream` 的 `agentStreams` Map `has` 即返回，死句柄永久占位、无重连。服务端 10:48 重启 → SSE 断 → 之后所有 text_delta/turn_end 丢失 → 前端卡「思考中」。此前一直存在，首次被「重启服务端」触发。附带收获：ISSUE-095 的 abort 在 10:49:05 实测真实生效（服务端日志「已中止会话…的当前一轮」）。
- 修复：`openSse` 重写为**自动重连 + Last-Event-ID 续传**——每次连接重建 URL（最新 serverBase/token/lastEventId）；断线静默指数退避重连（2s 起步、上限 15s）；重连带 `?lastEventId=` 让服务端回放缺失事件（断线中那一轮自动补齐显示）；仅 401/403 终止并提示重新登录；close() 清理定时器。`streamChildAgent/streamParentAgent` 改传 URL 构建器（签名去掉一次性 token 参数，仅 ipc-handlers 一处调用已核对）。
- 改动文件：`electron/lib/server-agent-client.ts`（openSse/streamChildAgent/streamParentAgent）；新增 `ISSUES/ISSUE-096.md` + 索引。
- 验证：根 tsc 通过（仅 5 条预存环境级错误）。实机：重启服务端、客户端不重启 → 发消息应正常回复；重启期间在跑的那轮重连后自动补齐。
- **用户侧操作**：重启客户端（dev 进程/应用）让主进程新代码生效即可，服务端无需再动。
- **用户实机验证通过（11:30）**：重启客户端后正常；服务端中断后可继续沟通（SSE 自动重连 + 续传生效）。ISSUE-096 闭环。

## ✅ 解决 ISSUE-097（parent_build_material 报「agent 模型未配置」但设置页显示已配置）
- 排查（证据链）：
  - 当前源码三路（设置页显示 /models/settings、写入 /models/app_settings、agent readParentSettings）全部收口服务端 `<parentId>:app_settings`，parentId 同源 JWT；
  - 服务端 DB 实查：主家长 86a84278 的 app_settings 自 9/7 起 `programmingModel="qwen-tokenplan/deepseek-v4-flash-0731"`；另有一条**无前缀全局 app_settings**（历史遗留，无读取方，无害）；其余 16 家长行只有 materialsLimit；
  - 铸诊断 JWT 实调运行中服务端 `GET /models/settings` → 返回 programmingModel 与 agent 同源 ⇒ **现行链路自洽，bug 为旧构建/状态滞后（假设 A 成立）**。
- **真凶机制（仍在源码里，本次根除）**：`app-settings.ts saveSettings` 每次保存（含改 materialsLimit）`pushConfig("app_settings", 整个本地文件)`，而服务端 /config/set 是**整键替换** → 本地过期/缺失的模型字段会把服务端真源整体覆盖（与 ISSUE-075 同构）。一旦某设备本地文件旧，一次无关保存即打没服务端配置。
- 修复：
  - `electron/lib/app-settings.ts` 重写：本地文件只存 materialsLimit（离线回退），删除全部模型字段本地读写函数（零调用方）；setMaterialsLimit 改走 /models/app_settings **合并端点**（绝不整键覆盖模型配置）；
  - `electron/lib/config-sync.ts`：reconcileMissingSecrets 移除 app_settings 模型字段补齐（防过期值补传），保留 auth 补齐；
  - `server/src/agent/programming-agent.ts`：「未配置」报错带所查 parentId（可诊断）；
  - `src/pages/Settings.tsx`：过时注释更新（真源=服务端）。
- 验证：根 tsc 通过（仅 5 条预存环境错误）、server tsc 通过、esbuild 重建 dist/server.cjs v0.4.0。
- 冒烟建议：重启两端 → 设置页确认编程模型 → agent 调 parent_build_material 跑通 → 再改「资料保留数量」确认编程模型配置仍在（不被覆盖）。
- **用户实机验证通过（12:36）**：ISSUE-097 闭环。

## Git 提交（本地，未推送）
- `7eeb88e` fix: ISSUE-082~097 家长/孩子 agent 服务端化收尾与四项修复（100 文件，+4923/−8716；含 086~092、094 考核域重构、发音评测上移、093/095/096/097 修复、文档归档、ISSUE 详情入库）。
- `04344df` chore: 移除误提交的构建调试残渣（server/build2.txt、server/tsc2.txt）并补全 ignore 规则（server/build*.txt、server/tsc*.txt）。
- 未入库（有意留下）：`server/scripts/_verify-exam-create.mts`（一次性验证脚本）；`.gitignore` 新增排除 build/tsc 调试输出与 agent-tools-report.log、tmp-*.txt。
- 用户明确：**不要到 201 上操作**；未 push。

## ✅ 解决 ISSUE-099（life plan「洗两双袜子」完成状态不更新）
- 按既有根因分析实施 F1~F3 + prompt 加固（F4 维持现状）：
  - F1 `parent-plans.ts`：`parent_life_plan_update` 新增 `complete` 动作（pending/missed → done，result=生活完成（家长确认）；missed 补标提示流水不追改）；
  - F2 `plan-domain.ts` applySignals 生活域：判定前扫 raw 含 planId/planOutcome 的行，正则 `plan_?id`/`plan_?outcome` 解析并补写回结构化列（幂等，治愈存量）；
  - F3 `routes/db.ts` insertMany：结构化缺失时从 content 同款正则回捞（只补缺失）；同 plan_id+同日已有行 → done 升级旧行并附加 raw，否则跳过（防双行）；
  - prompt（`recording-prompt.ts` §8）：明令 planId/planOutcome 只能作 kb_insert 参数、严禁写进 content 正文。
- **关键坑**：`daily_entries` 主键是复合键 (date, block, title)、**无 id 列**——首版兜底用 `WHERE id=` 会静默失败（try/catch 吞掉），实测 schema 后改为复合键定位。
- 验证：一次性脚本对珊珊孩子库跑真实 `runPlanStat` → `signals=1`，「洗袜子」行结构化列回捞补齐，**「洗两双袜子」pending → done**；missed/carried/reward 无副作用；server tsc 通过、dist/server.cjs 重建（v0.4.0）。
- 改动文件：`server/src/worker/plan-domain.ts`、`server/src/routes/db.ts`、`server/src/agent/parent-plans.ts`、`packages/agent-core/src/prompts/recording-prompt.ts`。未提交（待用户验证后一起 commit）。

## 新增 ISSUE-100（agent 上移服务端后「每日新建会话省 token」机制丢失）
- 用户问：旧方案（客户端 agent）每次开 app 检测最后消息非当天→开新会话、定点自动新建会话；agent 上移服务端后是否还有同样机制？
- 排查结论：**没有，机制已断链**。证据：
  - 服务端 `session-registry.ts:261` 的 `shouldAutoNewSession` 只判 `resetMarks.has(key)`（显式 reset），**不按日期**；会话 key=childId、落盘 `data/agent-sessions/<parentId>/<childId>-<slot>/` 跨天持久累积。
  - 冷路径「开会话按日期判断」原在客户端 `pi-session.ts`，已归档 `tmp/pi-session-old.ts`，运行代码无此裁决。
  - 热路径定点重置残留 `scheduler.ts:552-571`，但默认 `enabled:false` 且仅「客户端在线那一分钟」触发，等于失效。
- 影响：省 token 初衷落空（上下文跨天增长）；行为不一致（开 app 续昨、定点重置默认不开）。
- 修复方向（未改代码）：F1 在 `submitChildPrompt` 提交前检查最后消息非当天→先 `resetSession`；F2 把定点重置迁服务端 + 让设置开关生效；F3 会话按天分文件（改动大，先 F1+F2）。详情见 `ISSUES/ISSUE-100.md`，索引计数更新为 100（标记🟡待处理）。

## ✅ 修复 parent_exam_plan_create 落库失败（exam_plans INSERT 列/值错位）
- 用户报：本地 dev 环境，家长会话 agent 调用 `parent_exam_plan_create` 工具直接失败。
- 根因（唯一真源 `server/src/agent/parent-plans.ts:748-751`）：exam_plans 的 INSERT 在编辑时被破坏——
  - `done_at` 槽位误填成字面量 `'required'`（该值本属 `task_type`）；
  - 末尾漏写 `updated_at` 的 `?` → 列数 23、VALUES 仅 22 项，node:sqlite 抛 `22 values for 23 columns`；
  - 顺带把 `start_at` 写成字面量 `'?'`（修复前），`active` 错占 created_at 的占位。
- 对照权威写法 `worker/plan-domain.ts:327`（调度器落地用）逐列对齐修正：
  - `done_at` → `''`（空串；schema `done_at TEXT NOT NULL DEFAULT ''`，不能填 NULL）；
  - `task_type` → `'required'`；
  - 末尾补 `updated_at` 的 `?`；8 个占位符与 `.run(id,parentId,child.id,scope,start,due,now,now)` 一一对应。
- 验证：写临时回归脚本 `_verify-exam-create.mts`（import 当前源码 `createPlanDomainTools`，真实调用工具 + 落库断言），跑通：返回「已为孩子创建考核计划」、`start_at=2026-09-20 00:00:00`、status=pending、scope_json 正确。脚本保留在 `server/scripts/`。
- 同文件姊妹工具 `parent_life_plan_create`(583) / `parent_study_plan_create`(390) 经逐列核对**均正确**，未受影响。
- 附带发现（未改，待用户确认）：`server/src/routes/exam.ts:243-256`（/plans/exam 固定考核生成端点）存在**同类缺陷**——`start_at` 是字面量 `'?'`、`done_at` 误填 `'required'`、漏 `updated_at`，同样会抛列/值不匹配；与本次同源的坏模板。是否需要一并修，问用户。
- 环境注：本地 `server/node_modules/@pi/agent-core` 缺失（npm workspaces 未在根目录 install），已建 junction 软链使其解析；根治需 `npm install`（仓库根）。

## 新增 ISSUE-097（家长 agent 调 parent_build_material 报「agent 模型未配置」，但设置页显示已配置）
- 用户反馈：家长设置里「编程 agent 模型」已选好并显示已保存，但家长 agent 调 parent_build_material 时报「编程 agent 未配置模型」。
- 报错唯一起源：`server/src/agent/programming-agent.ts` `getProgrammingSession` 读 `readParentSettings(...).appSettings?.["programmingModel"]` 为空即抛错（构建产物 `server/dist/server.cjs:375516` 同逻辑）。
- 排查结论：当前源码里「设置写入（pi:set_programming_model→POST /models/app_settings）」「设置显示（pi:get_programming_model→GET /models/settings）」「agent 读取（readParentSettings）」三条路径都收口到同一个服务端键 `${parentId}:app_settings`，且 parentId 同源（同一次 verifySession 的 parent_id）→ **正确构建下不可能出现本 bug**。
- 根因假设（最可能）：线上跑的是旧客户端构建，仍走 `electron/lib/app-settings.ts` 的本地 `app-settings.json` 分支——显示读本地→"已配置"，agent 读服务端 DB→空→"未配置"，典型本地/服务端存储分裂。次可能因 pushConfig 静默失败导致服务端缺字段。
- 落地前需核对：① 服务端 DB 里 `${parentId}:app_settings` 是否真有 `programmingModel` 字段；② 线上客户端 `pi:set/get_programming_model` 究竟引 `server-agent-client` 还是 `electron/lib/app-settings.ts`；③ 重新构建部署两端消除分裂。
- 详情见 `ISSUES/ISSUE-097.md`，索引计数更新为 97（标记🟡待处理）。

## 新增 ISSUE-098（考核功能两项：背诵考核未生效 + 珊珊会话耗时分析）
- 用户反馈 ① 设了「背诵考核」但出题没有背诵、出了别的题，应只有背诵；② 分析珊珊会话考核过程，出题耗时约 40s。
- 根因①（架构性，不止现象）：背诵题仅由题库 `behavior:speech_recite`/语料驱动；`exam-engine.ts` L78-81 硬编码禁止 LLM 出背诵题；`methodSpec` 只有 `recitePass` 通过线、无「只背诵」模式开关；`attachStructuredQuestions` 按知识点筛题而非 behavior → 非结构化课 `recQ=0` 不出背诵、有语料课也只是「1 背诵+2~3 文字」而非纯背诵。无代码路径能产出「仅背诵」。
- 根因②（耗时，基于 `data/exam-audit/1f050a7f.../*.jsonl` 实测）：单次生成 LLM 调用 32~92s（~40s 对应论语 09-09 04:39 那次 41.3s）；判分 `scoreExamAttempt` 每题重建 in-memory session（`exam-engine.ts:371`）致 45~104s；端到端自定义 3 题约 1.5~2 分钟。瓶颈=每次生成/判分都 `createExamSession` 重初始化运行时，无复用/批处理。
- 修复方向：methodSpec/assess_method 增 `mode:"recite"`/`reciteOnly`；结构化侧按 behavior 过滤、非结构化侧新增「语料直出 cn_recitation」能力；判分复用 session/批处理优化。
- 详情见 `ISSUES/ISSUE-098.md`，索引计数更新为 98（标记🟡待处理）。

## ISSUE-099【已撤销，非缺陷】孩子会话更新 life plan 报「act 仅支持 delete / reschedule / rename」
- 原现象：珊珊会话里孩子 agent 调 `child_life_plan_update` 传 `{"act":"complete","id":"343e953a"}`，工具抛「act 仅支持 delete / reschedule / rename」。
- 用户裁定（设计意图）：完成判定走「今日汇总」（worker 证据驱动，`plan-domain.ts:240` 扫 daily_entries `plan_outcome='done'`）或**家长 agent 操作**，**不允许孩子 agent 标记完成** → 该报错属正确行为，不是 bug。
- 已撤销：删除 `ISSUES/ISSUE-099.md`，移除索引 099 行，索引计数回退为 98。无代码改动。

## ISSUE-098 出题流程核实（以珊珊 2026-09-14 两场实测为数据源）
- 数据源：`server/data/exam-audit/1f050a7f.../20260914.jsonl`（39 行）、孩子 kb `exam_plans`、主库 `exam_attempts`、家长库 `parents/86a84278.../parent.sqlite` topics.
- 实测：上午场（孩子自请 kind='self' 计划 b75be768，scope_json='{}'）→ config 走非 custom 分支 freq 兜底 weekly → 近 7 天必学课全考 **36 门**，36 次 LLM 出题 91 题 0 背诵，~6.5 分钟；中午场（家长自定义 ep_...y3dbf1，scope.courses=学而篇1/2，note=背诵考核，无 methodSpec）→ 2 课 LLM 出题各 2 题（35.6s/39.1s），4 题 0 背诵；判分每题一 session 并发 3，30.7s 全 0 分（孩子答"不知道"）。
- 断点确认：① 论语主题 assess_method 文本写了「每课一道背诵题」但 prompt 里 GENERATION_PER_COURSE_RULES 硬禁背诵且"系统单独生成背诵题"无实现；② scope.note 不进出题 prompt；③ methodSpec 无 mode/reciteOnly；④ **新发现**：child_exam_plan_create 写 scope_json='{}' → kind='self' 落进 fixed-weekly 分支 → 全窗口课程爆炸（应按 title/scope 定范围）。
- 修复方向待用户确认后实施。

## 新增 ISSUE-099（life plan 完成状态不更新：daily 已写但 plan_outcome=unknown / done 退化成 raw 文本）—— 与上方「已撤销」的 099 是不同问题，重新占用 099 编号
- 用户反馈：珊珊 life plan「洗两双袜子」(`343e953a-…`) 在会话里调 `summarize_conversation` 汇总并写入 daily，但计划完成状态始终未更新（仍 pending）。
- 实证（库 `server/data/kb/86a84278-…/1f050a7f-….sqlite`）：daily 2026-09-14「生活」有两条——① `洗两双袜子（孩子自定加分项）` plan_id 列=`343e953a-…` 但 **plan_outcome=unknown**（raw 说"未提及是否实际完成"）；② `洗袜子` **plan_id/outcome 两列全空**，done 信号只写在 raw 正文（`- planOutcome：done` + "完成了洗两双袜子"）。
- 根因：完成判定 `plan-domain.ts:238` 只匹配 `plan_id!='' AND plan_outcome='done'`，两行都不中。① AI 把 planId/planOutcome 退化成 raw 文本（`insertMany` 仅读结构化字段 `db.ts:430`，不解析正文）；② `unknown` 是死路无收敛（只能等 expireAndCarry 翻 missed）；③ 同计划出现"unknown + 残缺 done"双行冲突；④ 设计上"家长 agent 操作完成"路径未实现——`parent_life_plan_update`(`parent-plans.ts:641`) 也无 complete 动作，家长也标不了。
- 修复方向：F1 给 `parent_life_plan_update` 加 complete（落实家长兜底，与"仅家长可标完成"一致）；F2 `applySignals` 回退解析 raw 里的 planId/planOutcome 并写回结构化列；F3 recording 出现 done 时 kb_update 更新原 unknown 行而非新建；F4 unknown 视为"待确认"不直接判 missed。
- 详情见 `ISSUES/ISSUE-099.md`，索引计数更新为 99（标记🟡待处理）。

## 新增 ISSUE-101（孝经/千字文「背诵」题结构已存在但千字文答案是占位符）
- 用户诉求：给孝经、千字文增加知识点（背诵）+ 题库对应背诵题。
- 核查结论（库 `server/data/parents/86a84278-…/parent.sqlite`）：**并非缺结构**——千字文 30 段、孝经 18 章每课都已有「背诵」知识点（detail=发音评测自动评分 90 通过）+ 1 条 `speech_recite` 题。真实缺口是数据残缺：
  - **千字文 30 段的背诵题 answer 全是占位符 `重点字词（童趣版）`**（建课时把童趣解读误填进参考文本列）→ 背诵评测据此打分，完全不可用；
  - **孝经 18 章答案为真原文**，仅第 6/11/14/15 章末尾被拼上 `重点字词读音` 噪声需清理。
- 数据模型（落地套用）：`question_bank.answer`=参考原文(refText)，`behavior='speech_recite'`、`scoring=NULL`；挂载在 `course_knowledge_questions`；可经 `assess-content.saveQuestion` 按 id 覆盖 answer 或家长 agent `parent_exam_content_*`。
- 落地：取权威千字文全文按 30 段映射替换 answer；孝经 4 章 TRIM 噪声；端到端验收背诵题显示原文且可评分。
- 详情见 `ISSUES/ISSUE-101.md`，索引计数更新为 101（标记🔴高：千字文背诵考核当前不可用）。

## ISSUE-100 F1 冷路径触发时机裁定（用户）
- 用户裁定：F1 冷路径**必须放在"进入账户 / 打开孩子对话视图那一刻"**，不能放在 `submitChildPrompt`（发首条消息时）——否则用户先看到昨天消息、一发消息会话突然清空，会误以为"会话丢了"。
- 已更新 `ISSUES/ISSUE-100.md` 的 F1：触发点改为服务端"取/初始化会话"入口（非发消息），进会话即完成跨天检测+新建，客户端直接渲染空会话。对齐旧系统 `pi-session.createChildSession` 冷路径行为。
- 未改代码，待用户确认后实现。

## ✅ 解决 ISSUE-101（孝经/千字文「背诵」题答案数据修复）
- 数据真源：`server/data/parents/86a84278-c8ae-415e-8fbc-6140b1b7c88e/parent.sqlite`（珊珊家长库）。
- 千字文：30 段「背诵」`speech_recite` 题 answer 原全为占位符 `重点字词（童趣版）` → 替换为各课 `courses.teaching_copy` 的「原文吟诵」段**带标点真原文**（与孩子端学习资料逐字一致，最权威、零臆测）。
- 孝经：原 issue 误判仅第 6/11/14/15 章带噪声，**实测 18 章全部** answer 末尾都拼了 `重点字词读音` → 全部 `REPLACE(answer,'重点字词读音','')` 剔除（幂等）。
- 验证：千字文 30/30、孝经 18/18 已清理；两主题各 exactly 1 条 speech_recite/课；全库 `speech_recite` 中占位符+噪声命中=0。
- 备份：`parent.sqlite.bak-ISSUE101-20260914-173641`（16,646,144 字节）。
- 待办：修复已落到本地源库，需按 `PACKAGING.md` 走 OSS 发布流水线推到生产 201（用户未授权到 201 操作，待确认是否现在同步）。
- 索引 ISSUES.md 101 行已标记 ✅ 已解决（2026-09-14）。

## ISSUE-100 实施计划（F1 冷路径 + F2 热路径，待确认后实施）
- 结论：agent 上移服务端后「每日新建会话省 token」机制丢失。已规划精确到函数级的实施方案，写入 `ISSUES/ISSUE-100.md`「实施计划」节。
- F1（进会话按日期裁决，用户裁定 UX=进会话即新建、不先显旧消息）：服务端 `session-registry.ts` 新增 `lastMessageTimestampInDir`/`isLastMessageToday`/`openChildSession`；`shouldAutoNewSession` 加日期保险；`routes/agent.ts` 加 `POST /open`；客户端 `server-agent-client.ts` 加 `openChildSession` 并把进会话初始加载切到 `/open`（含 `src/` 调用点）。会话文件格式复用旧 `tmp/pi-session-old.ts:354` 的 `.jsonl` 扫描逻辑（同款 SDK）。
- F2（固定时刻服务端重置，让既有 `autoNewSession` 配置真生效）：`worker/tasks.ts` 新增 `autoNewSessionTask`（`catchUp:"latest"`，`run` 内 lazy `import` `resetSession` 规避 tasks→session-registry→scheduler→tasks 静态环），注册即接入现有 2 分钟调度器；`scheduler.ts` 无需改。
- F3（按天分文件）暂缓。
- 验证与部署：需重新构建 server 并发布到 ubuntu(192.168.1.201:8788)+OSS 流水线（Windows 先验、ubuntu 后验）。

## ISSUE-100 每日新建会话机制恢复（F1+F2 实施完成）
- F1 冷路径：session-registry.ts 新增 lastMessageTimestampInDir/isLastMessageToday/openChildSession（最后消息非今天→resetSession 后 ensureEntry）；shouldAutoNewSession 加日期保险；routes/agent.ts 新增 POST /agent/:childId/open。
- F2 热路径：worker/tasks.ts 新增 autoNewSessionTask（effective 配置 autoNewSession，默认 opt-in 21:00；lazy import resetSession 避免静态环，重置该孩子全部会话）。
- 客户端：server-agent-client.ts 新增 openChildSession；ipc-handlers.ts pi:start_child 与 scene:history 两处进会话加载由 getChildHistory 切到 openChildSession（getChildHistory 保留给回顾场景）。
- 验证：server typecheck+build、根 electron-vite build 通过；冒烟测试 8 断言全过（今天/昨天/空/不存在目录）。待部署 201 生产验证。

## ISSUE-100 后续：用户报障排查 + 201 部署 + 端到端验证（晚间）
- 用户报「18:30 自动新建未生效 + 历史消息时间全是登录时刻」。
- 报障1双根因：① 18:30 时服务端还是旧构建（18:40 才部署 0.4.1）；② DB 里根本没有 18:30 任务（scheduler_tasks 只有 9/3 的 21:30 任务；scheduler_config 只有 21:30/23:00），当日无新建任务行 → 用户那次创建未落库，待用户重试确认。
- 补充修复：scheduler.ts schedulerTaskTypeFor 增加 autoNewSession→auto_new_session 映射（否则 task_runs 挂不上任务）。
- 部署 201：server 0.4.1（deploy_server_041.py，有备份）；version/health//open(401) 全过。
- 端到端验证：临时改任务 time=18:54 → worker 桶命中即触发，两孩子 ok + task_runs 落库正确 → 恢复 21:30。今晚 21:30 该任务会正常跑。
- 报障2修复：mapHistoryMessages 补 historyTimeLabel（SDK ISO 字符串/ms 数字 → MM-DD HH:mm），HistoryMessage.time 透传；子/场景/家长三条回填路径均认 m.time，一处修全覆盖。
- 客户端 0.1.15 打包完成（dist-release-021/学习伙伴 Setup 0.1.15.exe，build.output 已改回 dist）。

## 用户指示（20:17）：不要部署到 201
- 已写入 MEMORY.md 操作约定：未经用户明确同意不得部署 201（构建/本地验证可自主做，部署动作先询问）。
- 说明：本轮 server 0.4.1 已于 18:40 在用户发此指示前部署（deploy_server_041.py，备份 server.cjs.bak-*）；是否回退待用户定。

## 201 回退完成（20:20）+ 重要发现
- 按用户选择用 server.cjs.bak-20260914-1840 回退，201 现运行 0.3.5（version/health 正常，/open 404 符合预期）。
- **重要发现：201 生产从未部署 0.4.x** —— 0.3.5 的 features 不含 server_agent/parent_agent/exam_agent，即 9/12~14 的 agent 服务端化全部改动（ISSUE-081~099）只在本地开发环境跑过，生产验证从未发生。此前「18:30 未生效在 201 排查」的诊断前提有误，用户实际连接的 agent 服务端是本地 dev server。
- 后续含义：201 要上线 ISSUE-081~100 全批改动需用户统一决策；0.1.15 客户端时间戳修复对 0.4.x 服务端生效，对 0.3.5 行为同旧版（/agent/* 404 → 空恢复，无回归）。

## ISSUE-100 本地环境根因修正 + 多时间点修复（20:45 定案）
- 用户澄清问题在本地 dev server；查 201 的「未落库」结论作废（本地库任务 18:19 创建成功、分配正确）。
- 真根因：buildEffectiveChildConfig 的 find() 只取第一个 auto_new_session 任务 → 9/2 的 21:00 任务遮蔽今天新建的 18:30（单时间点建模缺陷，recording 是 times[] 多点）。
- 修复：EffectiveChildConfig.autoNewSession + times[]；points() 优先 times；WorkerSchedulerChildConfig 同步。typecheck+build 过。
- 验证：重启 dev server → catch-up 补跑 ok@18:30（珊珊+闻闻），task_runs/worker_state 落库正确，任务名挂接正确；客户端 SSE 已自动重连。
- 运维：dev server 现由助手后台任务托管（pctwmc），建议用户自行终端重启接管；21:00 旧任务今晚照常触发。

## 21:35 任务排查（21:47）
- 用户建「21:35 新建会话」实际建成 recording 类型（UI 表单类型默认「每日学习记录总结」，漏切）→ 21:35 触发的是对话总结；autoNewSession 无 21:35 触发。
- 21:00 的自动新建其实已触发（三孩子 ok@21:00）——但服务端重置不会自动清空开着客户端的聊天 UI（已知体验缺口）。
- 处理：直接 UPDATE 本地库把任务 f8128e91 类型改为 auto_new_session（名字改「自动新建会话 21:35」）；重启 server → catch-up 补跑 ok@21:35（珊珊+闻闻），重置生效。
- 待办建议：①UI 新建任务表单的类型默认值/确认提示可优化；②服务端重置后可考虑向在线客户端推 UI 清空事件（新需求，未做）。

## 21:52 任务检查（21:53）
- 21:52 任务创建/分配/触发全链路正常：task_runs ok@21:52（珊珊+闻闻），游标 [18:30,21:00,21:35,21:52]。
- 「会话没重建」感知 = 已知体验缺口（服务端重置不清客户端 UI）；用户选择暂不做重置推送，手动切出切回验证。
