# 2026-08-21

- **检查珊珊今日论语学习记录**：会话（08:51）完整执行记录链路（kb_query 查进度 → kb_update 状态/掌握度/首次学习/最近复习/frontmatter → kb_insert daily），SQLite 落库正确（learned 284→285、next→第二十章、daily 19章详式记录、无重复写入）。发现 2 个既有偏差（非迁移引入）：① daily 学习条目缺「课程名」字段（method.md 模板历史如此，SPEC 3.1 要求有）；② 写入顺序「先进度后 daily」与 SPEC 4.3「先 daily 后进度」相反（method.md 模板如此）。
- **整理 KB-SQLITE-SCHEMA.md**：6 表（daily_entries/topic_progress/topics/rules/tag_links/meta）+ 3 索引；life/inquiries/tasks 月索引不建表（查询替代）；记录 topics.name(中文)≠topic_progress.topic(目录名) 关联坑；写入/查询语义与 kb 工具对应；迁移与归档说明。

- **ISSUE-023 schema v3 修订（用户拍板，已实施）**：① **daily_entries 删 fields_json 列**——字段由 method 灵活设定，信息在 raw 原文；字段白名单机制整体废弃（kb-schema 删 DAILY_FIELDS/PROGRESS_FIELDS/INDEX_FIELDS/legalFieldsFor，kb-lint 删字段 warning，592 条历史 warning 归零）；kb_query daily 非 listOnly 直接回 raw。② **进度明细化**：新增 courses 表（topic/title/sort_order/status/mastery/first_learned/last_review/review_count/material/send_material/tags），旧 topic_progress 表删除，learned/total/next/updated 改为**视图实时计算**（kb_update 只更新单课字段，复习次数 value="+1" 自增，传聚合字段被拒绝）。③ openKbDb 加 ensureV3 就地迁移（v2→v3 不丢数据）+ busy_timeout=3000（修 vitest 并行锁冲突）。④ ⚠️ 操作约束：migrate-kb-sqlite.mjs 是全量重灌，SQLite 真源后不能再对真源库跑（会覆盖丢新增数据），仅首次建库/灾难恢复用。⑤ 同步：recording SKILL.md、study-tracker SKILL.md、AGENTS.md 模板（pi-session.ts）、8 个 method.md（fix-method-progress-v3.mjs + fix-method-backtick.mjs）、KB-SQLITE-SCHEMA.md 重写。验证：tsc 0、build 过、相关 7 套件 89 用例全过、lint 全孩子 error 0 / warning 0。真实库 v3 迁移成功：courses 1300 行（lunyu 512）、daily 696、视图 lunyu learned=286/next=20章/updated=08-21。

- **ISSUE-023 schema v4 修订（用户拍板，已实施）——标签体系改造 + rules 并入**：① `topics` 加 rules_json、删独立 rules 表（rules.md key 是中文名，匹配 topics.name；曾误用目录名匹配致合并失败已补丁）；② `tag_links` 倒排 → `tags` 定义表（tag+dimension+criteria 判断标准）；`daily_entries` 加 tags 列（生活事件打标，kb_insert 从 content `- 标签：` 行自动解析）；courses.tags 确认保留；反查用逗号包裹 LIKE 扫数据行（查询替代索引）；③ 工具面：kb_query tags 查定义、daily/progress 支持 tag 过滤、kb_insert 移除 tags；kb-lint 新增标签词表合规校验（daily/courses tags ⊂ 定义表）；④ 迁移/补丁：ensureV4 就地迁移 + fix-v4-tags-rules.mjs + fix-v4-backfill-tags.mjs（历史 57 个非词表标签补入 dimension=历史）；⑤ 坑：rules key 中文名匹配、AGENTS 模板反引号转义（TS1127）、历史标签 # 前缀归一化。验证：tsc 0、build 过、相关 7 套件 88 用例全过、lint 全孩子 0/0。

- **kb_update/kb_insert 参数重命名（用户质疑驱动）**：用户发现珊珊会话里 `kb_update {table:"progress"}` 看似操作 progress 表——实为**参数命名误导**（"progress" 是「更新学习进度」类别名，底层一直操作 courses 表，10:42 会话已实证：courses 20 章 status=✅、视图 learned 286→287）。修正：table 值 "progress" → **"course"**（execute 兼容旧名 "progress" 别名，老 method.md/旧调用不坏）；同步更新 recording SKILL.md、AGENTS.md 模板、8 个 method.md、测试（scripts/rename-progress-to-course.mjs，12 文件）。kb_query 的 query:"progress" 保留（查询语义）。验证：tsc 0、build 过、相关 3 套件 48 用例全过。
