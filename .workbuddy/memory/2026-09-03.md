# 2026-09-03

## ISSUE-033 收尾：8788 重启 + 反引号 bug 修复 + 提交
- 本地 8788 旧进程 PID 7472 已 kill，用 managed node 重启新 dist/server.cjs（00:11 重建，SERVER_DATA_DIR=C:/Users/79734/Documents/pi/server/data）。验证：health 200、/api/v1/version 含 worker、/study-plans/today 401。
- **关键 bug 修复**：server/src/worker/recording-prompt.ts 的 RECORDING_PROMPT 模板字符串内含有未转义反引号（行内代码 `- [ ]`）。esbuild 打包时模板字符串提前闭合，运行时 `TypeError: [] is not a function`，8788 启动即崩。修复：模板字符串内所有字面反引号去除（行内去反引号、代码块围栏改 `~~~`）。⚠️ 当时误判"electron 端 vite 正常"，实际 electron 版同源问题于 07:20 复现（见下）。
- git 提交 `063311a`：ISSUE-033 —— gen/stat 去 LLM 改纯代码、scheduler 5 分钟游标、完成态取课程状态、recording 汇总判定自定任务、客户端移除 gen/stat 定时。16 files, +693/-288。未跟踪临时文件（linux-016.zip 等）未提交。

## 07:20 客户端 npm run dev 崩溃修复（同源反引号 bug，electron 版）
- 症状：`npm run dev` 启动报 `TypeError: [] is not a function` at out/main/index.js:192422。
- 根因：electron/lib/recording-prompt.ts 的 step 8 未转义反引号（昨天只修了 server 版，electron 版漏同步）→ electron-vite 打包同样让模板字符串提前闭合。教训：**esbuild 与 electron-vite 都会破坏未转义反引号**，不是 vite 才正常。
- 修复：electron 版三处与 server 版对齐（step 8 / step 0 去反引号、代码块围栏改 `~~~`）；两份文件现 diff 仅剩注释差异。
- 验证：electron-vite build 成功；plain node 加载新 out/main/index.js 错误变为"缺 electron 运行时（registerSchemesAsPrivileged undefined）"——即已越过崩溃点，真实 npm run dev 不会再崩。
- git 提交 `6f73706`（electron/lib/recording-prompt.ts + memory），已推 github+gitee。
- ⚠️ 用户决定：**暂不部署 201**（07:26 明确"现在不要部署到201，只修复问题"）；201 部署待用户另行确认后再做。

## 07:45 家长界面学习计划完成态：status 标记 → 当天活动判定（复习课）
- **问题**：学习计划某课可能是「复习」，该课 courses.status 早已 ✅——UI 按 status 显示完成，无法反映「当天是否真的学/复习了」。
- **改法**：done 判定 = `courses.first_learned === 目标日期 || courses.last_review === 目标日期`（目标日期：today 卡片=今天；list 每行=r.date）。
- 客户端：`electron/lib/ipc-handlers.ts` loadCourseDoneMap 改 date-based（签名加 date 参数，list/today 各传日期）；`src/components/StudyPlanPanel.tsx` 今天卡片 ✅（今天已学/补昨天已学）/⬜/📌 组合渲染，未来排期不再高亮 status 的"已学完"（未来按当天活动无记录），只标 📌。
- 服务端同步（stat 与 UI 须同口径，否则 UI ⬜ 而 stat 打勾、carry 矛盾）：`server/src/worker/tasks.ts` courseDoneFor 加 today 参数，比较 first_learned/last_review==today；注释同步。
- 验证：两端 tsc 0 错；server.cjs 重建 07:41，本地 8788 已重启加载（health 200）。客户端需重新 npm run dev 生效。
- 未提交 git；201 未部署。

## 08:00 珊珊 carry 未顺延 + todolist 自动加项（两条修复）
- 现象：珊珊 9/2 计划（汉字宫190/191/192）未完成未顺延到 9/3；9/3 todolist 自动多出"阅读课外书/户外运动/整理书包"三项。
- 数据定位：9/2 child_todo_stats **无记录**（stat 未产出）→ carry v1"stats 缺失不顺延"跳过 → 汉字宫漏延；9/3 todo【昨日未完成项】三条 = gen 从 9/2 todo 自动并入（那三项源自 9/2 旧客户端 LLM 补的自规划项）。
- 代码修复（server）：
  - `tasks.ts` runTodoGenServer：删昨日未完自动并入（gen 只写学习计划当日项）；
  - `study-plan-carry.ts` carry v2：判定真源从 child_todo_stats → **昨天 child_todos 未勾 `[家长]` 行**（stat 已按当天活动确定性打勾），无 stats 也能正确顺延；删 readStats。
- 存量修复（本地 8788 珊珊）：9/3 study_plan_items 补 carry 行（汉字宫3课）；9/3 child_todos 重写 = 5 conversation 课 + 3 carry 课，删自动三项。SQL 复核通过。
- 验证：server tsc 0 错；server.cjs 重建 07:49；8788 已重启。
- ⚠️ 顺带发现：9/2 计划文本「汉字宫第190课·储粮之仓」与课程表不符（课程表 第190课=走上陡坡 / 储粮之仓=第200课），疑课号录错，供家长核对。
- 未提交 git；201 未部署。
