# 2026-09-03

## ISSUE-033 收尾：8788 重启 + 反引号 bug 修复 + 提交
- 本地 8788 旧进程 PID 7472 已 kill，用 managed node 重启新 dist/server.cjs（00:11 重建，SERVER_DATA_DIR=C:/Users/79734/Documents/pi/server/data）。验证：health 200、/api/v1/version 含 worker、/study-plans/today 401。
- **关键 bug 修复**：server/src/worker/recording-prompt.ts 的 RECORDING_PROMPT 模板字符串内含有未转义反引号（行内代码 `- [ ]`）。esbuild 打包时模板字符串提前闭合，运行时 `TypeError: [] is not a function`，8788 启动即崩。修复：模板字符串内所有字面反引号去除（行内去反引号、代码块围栏改 `~~~`）。⚠️ 当时误判"electron 端 vite 正常"，实际 electron 版同源问题于 07:20 复现（见下）。
- git 提交 `063311a`：ISSUE-033 —— gen/stat 去 LLM 改纯代码、scheduler 5 分钟游标、完成态取课程状态、recording 汇总判定自定任务、客户端移除 gen/stat 定时。16 files, +693/-288。未跟踪临时文件（linux-016.zip 等）未提交。

## 07:20 客户端 npm run dev 崩溃修复（同源反引号 bug，electron 版）
- 症状：`npm run dev` 启动报 `TypeError: [] is not a function` at out/main/index.js:192422。
- 根因：electron/lib/recording-prompt.ts 的 step 8 未转义反引号（昨天只修了 server 版，electron 版漏同步）→ electron-vite 打包同样让模板字符串提前闭合。教训：**esbuild 与 electron-vite 都会破坏未转义反引号**，不是 vite 才正常。
- 修复：electron 版三处与 server 版对齐（step 8 / step 0 去反引号、代码块围栏改 `~~~`）；两份文件现 diff 仅剩注释差异。
- 验证：electron-vite build 成功；plain node 加载新 out/main/index.js 错误变为"缺 electron 运行时（registerSchemesAsPrivileged undefined）"——即已越过崩溃点，真实 npm run dev 不会再崩。
- git 提交 `6f73706`（electron/lib/recording-prompt.ts + memory），已推 github+gitee。
- ⚠️ 用户决定：**暂不部署 201**（07:26 明确"现在不要部署到201，只修复问题"）；201 部署待用户另行确认后再做。

## 07:45 家长界面学习计划完成态：status 标记 → 当天活动判定（复习课）
- **问题**：学习计划某课可能是「复习」，该课 courses.status 早已 ✅——UI 按 status 显示完成，无法反映「当天是否真的学/复习了」。
- **改法**：done 判定 = `courses.first_learned === 目标日期 || courses.last_review === 目标日期`（目标日期：today 卡片=今天；list 每行=r.date）。
- 客户端：`electron/lib/ipc-handlers.ts` loadCourseDoneMap 改 date-based（签名加 date 参数，list/today 各传日期）；`src/components/StudyPlanPanel.tsx` 今天卡片 ✅（今天已学/补昨天已学）/⬜/📌 组合渲染，未来排期不再高亮 status 的"已学完"（未来按当天活动无记录），只标 📌。
- 服务端同步（stat 与 UI 须同口径，否则 UI ⬜ 而 stat 打勾、carry 矛盾）：`server/src/worker/tasks.ts` courseDoneFor 加 today 参数，比较 first_learned/last_review==today；注释同步。
- 验证：两端 tsc 0 错；server.cjs 重建 07:41，本地 8788 已重启加载（health 200）。客户端需重新 npm run dev 生效。
- 未提交 git；201 未部署。

## 08:00 珊珊 carry 未顺延 + todolist 自动加项（两条修复）
- 现象：珊珊 9/2 计划（汉字宫190/191/192）未完成未顺延到 9/3；9/3 todolist 自动多出"阅读课外书/户外运动/整理书包"三项。
- 数据定位：9/2 child_todo_stats **无记录**（stat 未产出）→ carry v1"stats 缺失不顺延"跳过 → 汉字宫漏延；9/3 todo【昨日未完成项】三条 = gen 从 9/2 todo 自动并入（那三项源自 9/2 旧客户端 LLM 补的自规划项）。
- 代码修复（server）：
  - `tasks.ts` runTodoGenServer：删昨日未完自动并入（gen 只写学习计划当日项）；
  - `study-plan-carry.ts` carry v2：判定真源从 child_todo_stats → **昨天 child_todos 未勾 `[家长]` 行**（stat 已按当天活动确定性打勾），无 stats 也能正确顺延；删 readStats。
- 存量修复（本地 8788 珊珊）：9/3 study_plan_items 补 carry 行（汉字宫3课）；9/3 child_todos 重写 = 5 conversation 课 + 3 carry 课，删自动三项。SQL 复核通过。
- 验证：server tsc 0 错；server.cjs 重建 07:49；8788 已重启。
- ⚠️ 顺带发现：9/2 计划文本「汉字宫第190课·储粮之仓」与课程表不符（课程表 第190课=走上陡坡 / 储粮之仓=第200课），疑课号录错，供家长核对。
- **9/2 stats 缺失根因（追问确认）**：task_runs 显示 9/2 珊珊两条 `todo`(旧 LLM stat/gen 任务) @17:13/@17:17 = `error: No API key found for qwen-tokenplan` —— 服务端 auth 缺 qwen-tokenplan key，LLM 版 stat 一调即崩没走到写 stats。旁证：child_todo_stats 全表为空（该孩子 stat 从未成功产出过）；同一家长闻闻同日同 error。9/2 我加 config-sync「密钥补齐」正是为此。stat 去 LLM 后此路径已消除（纯代码不需 key）；carry v2 也不再依赖 stats。
- 未提交 git；201 未部署。

## 08:45 gen 改「计划↔todolist 每次同步」（家长中途改计划自动反映）
- 需求：gen 原来"当天生成一次就锁死"（todo_gen 游标），家长中途改计划 todolist 不更新。
- 改：`tasks.ts` runTodoGenServer = 每次 plan tick 以最新 study_plan_items 重写【家长规定项】：同 text 已勾([x])保持、孩子自定内容(nonParentBody)原样保留、计划增删自动反映、无变化不写；`scheduler.ts` runPlanTick 去掉 todo_gen 游标一次性门控改为每次同步（stat 游标不变）。
- 端到端验证（本地珊珊）：临时插测试课→重启→todo 自动多该课；删测试课→重启→todo 移除；同时观察到 `[x] 论语颜渊篇第二十四章` 勾选被保留、`【今日计划】- [ ] 画画🎨` 孩子内容保留。✔ 三个特性全过。
- server.cjs 重建 08:40；8788 已重启（当前 WLljNM）。未提交 git；201 未部署。
- 测试同步：`verify-study-plan-carry.mts` 改写为 v2 语义（临时库造昨天 child_todos 勾选 → 只顺延未勾项/全勾不顺延/无 todo 不顺延/幂等）4/4 过；`verify-study-plans.mjs` 冒烟 17/17 过。

## 09:00 孩子会话左侧资料：退出重进后标题变「未命名资料」（修复）
- 根因：`getSessionMaterials`（pi-session.ts）恢复历史资料时 title 只读 assistant toolCall 的 `args.title`——SPLIT 后 display_content 只传 path 不带 title；内容能经 toolResult.panelContent 兜底，**title 没兜底** → 重进后 undefined → 面板 fallback「未命名资料」。实时链路（Learn handleToolEnd 用 panelContent.title）正常，故只在重进时暴露。
- 修复（electron/lib/pi-session.ts）：resultContent map 同时记录 panelContent.title；push title 依次兜底 `args.title → panelContent.title → extractHtmlTitle(content)`（新增 <title> 提取函数）。
- 验证：根 tsc 0 错。纯客户端主进程改动，重跑 npm run dev 生效；8788/201 均无需动。
- 未提交 git。

## 09:50 每日/每周考核候选改「学习计划源」（用户需求，2026-09-03）
- **需求**：每日/每周考核课程从**家长学习计划（study_plan_items）**取，**计划内无论是否完成都考核**；每周=**近7天（含今天）**；无计划排期 → **无计划不考**（不自动回退旧口径）。背景：ISSUE-033 后每天学什么全由计划驱动，考核倒逼计划执行。
- **真实数据验证口径**：计划 content[].text 即课程标题（精确匹配孩子 kb courses.title，8/8）；计划含大量 status⬜ 未学课。
- **改动（server/src/routes/exam.ts）**：
  - 新 `CourseMeta` 接口（+planDate?）；新 `localDateStr`/`planWindowFor`（**本地日期**，勿用 UTC——计划 date 是家长本地语义）；新 `listPlanCourseMeta(db,dataDir,parentId,childId,start,end)` 查主库 study_plan_items（kind='date' 窗口行）→ text 精确匹配孩子库 title（不看学习痕迹，未学也进）→ 每课 planDate（最早排期）；匹配不到的计划文本进 `unmatched` 返回（暴露课号录错，如真实「汉字宫第190课·储粮之仓」课程表不存在）。
  - `buildSelectionPrompt` 加 `source:"learned"|"plan"`：plan 模式候选行加「| 计划日期」列、不打 ★/◐、统计「[主题] 今天/近7天计划 N 门 → 全部选入」、尾部标注说明换计划语义（旧模板「★本周期/学习/复习过」字眼=旧版残留请忽略）。
  - `DEFAULT_SELECTION_PROMPTS.daily/weekly` 重写为计划语义；getExamConfig 固定排期第一段 freq=daily|weekly → 计划候选（monthly+ 历史/custom 不变，第二段 rubric 链路不变——未学课父库也有 rubric 可出卷）。
- 验证：server tsc 0；**冒烟 27/27**（新增 study-plan 创建/kb 补未学课/config 第一段含未学课+planDate；冒烟需清空 /tmp/exam-smoke-data 防历史累积干扰）；真实数据珊珊 daily@9-03 候选 5 门（未学 2 门在列）、weekly@9-07 候选 18 门（未学 11 门）✓。EXAM-REQUIREMENTS §14.11。8788 已重启加载（QYSga6）。

## 10:00 计划源收尾：文案适配 + 存量模板清理
- **家长端 ExamAdminPanel**：DEFAULT_HINTS 与规则 label 改计划语义（daily「考今天学习计划里的所有课程（无论是否完成）」/ weekly「考近 7 天…」）。
- **孩子端 ExamView**：空选课提示按 `data.schedule.freq` 分流——daily/weekly 固定档提示「今天/近7天学习计划里还没安排课程（有计划无论是否完成都会考核）」；自定义档保留旧文案。
- **8788 存量清理**：test@qq.com 的 exam_fixed selectionPrompts——daily 为家长自定义「只考核必学的」（保留，标注说明兜底纠偏旧"学习/复习"措辞）；**weekly 是固化旧默认模板 → 已置空回退新默认**（GET config 实测 weekly@9-07 prompt=新默认 ✓ 候选 18 门未学 11）。
- 验证：根 tsc 0 业务错。

## 10:15 孩子端考核 5 个问题修复（用户实测反馈）
1. **答案串题（根因=qid 跨课重复）**：出卷 LLM 每课独立编号都从 q1 起 → 多课聚合后 `answers[qid]` 共享，改一题动上一课。修：ExamView 组装题目时 **qid 用全局序号 `q${i+1}` 覆盖**（不再用 LLM qid）。
2. **语音 invalid security origin**：iframe srcDoc + sandbox 无 `allow-same-origin` → 不透明源非安全上下文 → getUserMedia SecurityError。修：sandbox 加 `allow-same-origin`（主进程已有 setPermissionRequestHandler 放行 media）。⚠️ 教训：srcDoc iframe 要录音必须 allow-same-origin + allow="microphone" 同时满足。
3. **本题用时没计时**：exam-template qTimer 只在录音/切题时写一次。修：renderQuestion 起每秒 interval（startQTimer/stopQTimer），进入题目即累计、作答完成（answeredAt）冻结；重录重置重计。
4. **不能返回修改（同 #1 表象）**：qid 修复后 prev 回显/修改各自独立，可正常回改（UI 本就支持 prev/next）。
5. **app 没地方看考核结果**：入口实际已存在于孩子详情页「🎯 考核记录」tab（ExamRecords 组件完整：每课程记录表+逐题明细+原音回放）——但太深找不到。修：ExamAdminPanel 加第 4 标签「考核记录」（孩子选择器 + ExamRecords），家长中心「学习考核」直达。
- 验证：根 tsc 0、build 通过。未提交。

## 10:30 #1 按用户确认改为「答完锁定不可回改」（推翻 10:15 的"允许回改"）
- **用户拍板**：一题答完（离开本题）即锁定，回看只读不可改——防孩子看到后题提示后回头改前题（用户原话"后面可能会有前面题目的答案或提示"）。10:15 我默认做成"允许自由回改"，方向反了，已问询修正。
- **exam-template.ts 整体重写作答脚本**：
  - 状态模型 `answers[qid]={startedAt,asr,asrEdited,audioBlob,audioUrl,answeredAt,durationMs,locked}`；`hasAnswer`=asr 非空（排除"识别中…"占位）或 audioBlob。
  - 锁定时机 `saveCurrent()`（prev/next/submit 前）：保存 textarea → 有内容 → answeredAt+durationMs+locked；空题不锁（可回补答）。
  - 锁定题视图只读：textarea disabled、mic 禁用「🔒 已答完」、可播放录音、lockTag 提示「本题已答完，不可修改」；rerec 仅未锁题显示。
  - **录音切题竞态修复**：recQid 捕获开始录音的题 id，onstop 用它定位（原来读全局 idx——录音中切题会把录音存到新题）；切走且未锁的录音题在 onstop 里立即锁定（防漏锁导致永无法提交）。
  - 转写占位「识别中…」不入答案（saveCurrent/payload 排除）；asr:done 只在未手工编辑（!asrEdited）时覆盖。
  - updateDone：doneTag 常显「已答 x/N 题（答完的题不可再改）」/全答完「可提交 ✓」。
- 验证：根 tsc 0、build 通过（1m27s）。未提交。

## 11:15 作答交互 v3（用户实测 6 反馈 → 全面重构 exam-template + ExamView）
用户反馈：①没答不能看下一题（后题是前题提示）②改**按住说话**录音（同聊天）、多次录音拼接成一个保存 ③识别中要有提示（防以为没录上）④**去掉重录按钮**（说错直接补说）⑤每题计时**累计了前面题目**不对，要只记本题 ⑥最后一题**无法提交**（必须切回前题）。
- **exam-template.ts v3**：
  - 按住说话（push-to-talk）：mousedown/mouseup/mouseleave + touch 事件；停止→短录音(<2000B)提示「太短」；否则按钮「🔄 识别中…」+ 状态行「正在识别你的回答…」（#3）。
  - 多段录音（#2）：每段 blob→dataURL 存 `a.segs`，识别文本**追加**到 asr 文本框（聊天语义）；提交 payload `audioB64s[]`；**宿主把 ≥2 段用主进程 voice:merge 拼成单 WAV 再上传**（复用聊天 ISSUE-021 合并，ExamView 端 b64→Blob）。
  - 去重录按钮（#4）；hint 引导「说错直接说新的/改文字」。
  - 顺序作答防偷看（#1）：nextBtn 可用 = 当前题**已有作答内容**（录过音/打过字）——没答不能看下一题；离开（saveCurrent）即锁定只读；prev 自由回看已锁题。
  - 每题独立计时（#5）：右上角「总用时」全局秒表；「本题用时」仅当前未锁题前台每秒 +1（`a.sec`），锁定时 durationMs=sec*1000 定格——不累计别题。
  - 提交（#6）：按钮**恒可点**「完成并提交」；click→saveCurrent 锁当前→有未答题则 alert 跳转；全锁才提交（消除「最后一题没法触发 saveCurrent」死锁——原提交按钮 disabled 于 all-locked，而最后题锁定只能靠提交触发=鸡生蛋）。
  - 坑：next 判定**不能**用 locked（锁只在离开时发生，会造成「答完也点不了下一题」死锁）→ 用 hasContent。asr:done 一律追加（含切走/已锁题，判分文本不能丢）。
- **ExamView.tsx**：handleSubmit 适配 audioB64s：1 段→直接上传 webm；≥2 段→`voiceMerge(childId,b64[])`→WAV 上传（b64 dataURL 剥离前缀）；attempt.startedAt 用排期时间。
- 验证：根 tsc 0、模板内嵌 JS 语法校验通过（esbuild transform + new Function）、build 通过。未提交。

## 11:21 ISSUES.md ISSUE-033 段更新为最新实现情况
- 补「2026-09-03 机制收敛 v2」小节：carry v2（读昨日 todo 未勾[家长]行）、stat 纯代码+当天活动判定、scheduler 5 分钟+stat 事件驱动、gen=计划↔todo 每次同步、客户端去 gen/stat 定时、家长面板完成态当天判定、资料标题恢复修复；优先级/遗留刷新（git 已推、201/公网部署待用户确认）。已提交推送。

## 11:22 发布 0.1.9（客户端）+ 0.3.1（服务端）公网 + 201（完整发布）
- 版本：客户端 0.1.8→**0.1.9**，服务端 0.3.0→**0.3.1**（含自 v0.1.8 以来 25 个提交：ISSUE-033 学习计划全链路、考核作答交互 v3、study-plan carry/面板、worker gen/stat、面板/文件上传/家长历史/侧边栏、recording 反引号修复）。`package.json`+`server/src/routes/version.ts`+`server/package.json` 三处版本同步改（amend 进发布 commit `1e59db4`）。
- 构建：Windows `npm run dist:win`（学习伙伴 Setup 0.1.9.exe 186MB + blockmap + latest.yml）；服务端 `node server/scripts/build.mjs`→server.cjs 16.6MB（baked SERVER_VERSION=0.3.1 已核对）。
- 推送：tag v0.1.9 推 github+gitee 双远端（历史已清假密钥，推送顺利），触发 GitHub Actions build-linux(#9)+build-mac(#17) CI 均 success；`gh run download` 取 deb/AppImage/双 dmg。
- **201 部署**：paramiko 传 server.cjs→`systemctl restart learning-server`（sudo 密码管道 `-S`）→ /api/v1/version=0.3.1、health ok、study_plan_items 表已迁移（node:sqlite 确认）；dpkg -i 0.1.9 deb → `dpkg -l`=0.1.9。GUI 进程 SSH 无法杀（permission denied），需 201 本地重启客户端生效。
- **公网发布**：OSS(oss2) 传 deb/AppImage/双 dmg/latest-linux.yml/latest-mac.yml + `publish-update.py` 传 windows exe/blockmap/latest.yml 并登记 /api/version 0.1.9；再 `aliyun-run.py` curl 把全部文件（含 windows 三件）拷进 ECS `/opt/learning-cloud/download/`。⚠️ 一度漏拷 windows 文件致 /download/latest.yml 仍显示 0.1.8，补 `ecs_copy_win` 修正（**publish-update.py 只传 OSS 不拷 ECS，windows 也必须单独拷 ECS**）。
- 验证：/api/version=0.1.9；latest.yml/latest-linux.yml/latest-mac.yml 均 0.1.9；exe 186462450B、deb 141274692B、AppImage 187867536B、双 dmg 169530646/177666394B，全部 HTTP 200。清理：ci-artifacts/dist-019-build.log/dist 及 Temp 部署脚本已删。

## 12:46 记录 ISSUE-043：生产环境珊珊（Mac 客户端）会话未同步上云
- 现象：201 server `data/sessions/<parentId>/<珊珊>` 为空 → 家长「会话回看」空白 + 服务端每晚每日汇总（recording）跳过珊珊。自 0.1.8 起，Mac 客户端特有。
- 根因链路（已查）：客户端 `electron/lib/session-sync.ts` `syncChildSessions` 读 `getChildDir(childId)/.pi/agent/sessions/*.jsonl` → `POST /api/v1/sessions/:childId/sync`；三触发点（每轮对话后 ipc-handlers.ts:1098 / 5min 定时 / 退出前），失败静默吞。server 镜像 `db/sessions.ts:13`；家长回看 `GET /sessions/:childId?date=`、服务端汇总 `readServerDailyConversation` 均读该镜像 → 空则三症状齐发。结论：珊珊 Mac 从未成功上传。
- 候选根因（待取证）：① Mac 连公网 cloud 而非 LAN 201（cloud 未部署 session_sync/worker）② `sync-state.json` 游标卡死 ③ parentId 串号 ④ ATS 明文 HTTP（次）。
- 诊断：查珊珊 Mac `~/Library/Application Support/学习伙伴/app-data/children/<id>/.pi/sync-state.json`+sessions 字节、201 `data/sessions/` 目录、Mac 日志 `[session-sync]` warn、实际连的 server 与 `session_sync` feature。
- 优先级：高，仅记录未实施。

## 13:30 ISSUE-043 现场取证（登 201 `192.168.1.201`，shanshan/<redacted>）
- 连法：本机无 sshpass/paramiko，用 OpenSSH 10.3 的 `SSH_ASKPASS_REQUIRE=force` + askpass 脚本（echo 密码）免交互登录（Git Bash 下 stdin 非 tty）。201 主机 `shanshan-Macmini`（Ubuntu 24.04），shanshan 在 sudo 组。
- **推翻"服务器为空/从未同步"**：珊珊=childId `1f050a7f-df8a-45b0-925a-1ffe2aa35674`，家长 `86a84278-...`（test@qq.com）。`data/sessions/<par>/1f050a7f/` 有 **23 jsonl / ~3.0MB / 2026-08-19~08-30**；`session_messages` 表珊珊 **732 条**（闻闻 398）。服务端镜像非空。
- **推翻"汇总跳过"**：`worker_state` 珊珊 recording last_run=09-02T13:00Z、last_key={"date":"09-02"} → 汇总正常跑过；仅 08-31+ 无新会话而"无内容可汇总"，正常。
- **真实缺口**：珊珊最后同步 **08-30T23:39**，08-31~09-03 全空；闻闻 09-02 仍有同步 → **服务端 sync 接口正常，问题在珊珊 Mac 客户端 08-30 后停传**。13:13 批量 mtime = 服务端当日把会话解析进 `session_messages` 的时点，非客户端重传。
- **无法从服务端定论**：201 无请求级日志（`journalctl` 需 sudo）。下一步必须上珊珊 Mac：取 `~/Library/Application Support/学习伙伴/app-data/children/1f050a7f-.../.pi/sync-state.json` 字节游标 + sessions 实际字节 + 客户端 `[session-sync]` 日志，排查 ①离 LAN 走公网 cloud ②游标卡死。
- 已在 ISSUES.md ISSUE-043 末尾加「现场取证」段修正结论（标题应改为"珊珊会话 08-31 起停更上云"）。临时 askpass/token 脚本已删（含明文密码）。

## 14:13 ISSUE-043 同步机制澄清 + 修正上轮两处误判（用户质疑"回看应读 DB、server 不该有 jsonl"）
- 机制：回看 `GET /sessions/:childId?date=` 读 `session_messages` 表（用户判断正确）；server 端 jsonl 是 `POST .../sync`→`appendAndIndexSession`(db/sessions.ts:55) 双写产物（fs.appendFileSync 落盘 data/sessions/ + INSERT session_messages 索引），**jsonl=源头、DB=派生索引，设计内必需**（worker 汇总 `readServerDailyConversation` 直接读 jsonl）。故"server 有 jsonl"与"回看读 DB"不矛盾、须并存；删 jsonl 会废汇总。
- 误判①修正：jsonl 文件名 `2026-08-30T23-39-59...` 是客户端创建 session 时间戳，**非同步时间**。`session_files.updated` 全 = **09-03T05:13:52~54Z（北京13:13）** → 23 个历史文件是 **09-03 13:13 一次性批量补传**到 201，非 08-19~08-30 逐日同步。此前"最后同步停在 08-30"错。
- 误判②修正：此前"13:13 非客户端重传"错——13:13 正是客户端补传落盘时刻（与 server.sqlite-wal 13:13:54 一致）。强证：session_files.synced_bytes 与磁盘 jsonl 大小逐一吻合 → 确为 sync 落盘，非手工导出。
- 架构根因：session_sync/worker 只部署 LAN 201 的 learning-server，公网 cloud 不部署 → 孩子离 LAN 时对话既不回看也不汇总。ISSUE-043 本质是"回看依赖孩子设备能连 201 LAN"的脆弱前提。08-31~09-03 空白待上 Mac 验证：A.那几天没开 App/没对话（空白正常）B.Mac 走了公网 cloud（对话未回流 201）。
- ISSUES.md ISSUE-043 段已重写（同步机制澄清 + 现场事实修正 + 架构性根因）。

## 14:48 ISSUE-043 Mac 本地取证进展（用户现场确认 08-31~09-02 有会话）
- **排除假设 A**：用户已在珊珊 Mac 确认 08-31~09-02 本地有会话（agent/sessions 下 jsonl）→ 锁定「本地有会话但未上云」，根因在客户端 sync 链路（server-connection 指向 cloud 或游标卡死）。
- **客户端日志不落盘（核实）**：package.json 无 electron-log/winston/pino，[session-sync] 仅 console.warn 到主进程 stdout，打包 app 不写 .log。取证改走：Mac 终端跑 app 可执行文件看 stdout；或直接查 server-connection.json + `curl <url>/api/v1/version` 看连的 server 与 features。
- **分水岭**：config.ts `getServerUrl()`→`getDataDir()/server-connection.json`({url})，未配默认 127.0.0.1:8788。url=cloud → sync 发往无 session_sync 的 cloud（根因）；url=201 但那几天 Mac 不在 LAN → fetch 失败；游标曾被误连 cloud 推进 → 永久静默跳过。ISSUES.md 已补「Mac 本地取证进展」段。

## 14:55 ISSUE-043 闭环（自愈实锤，降级为设计限制）
- **数据已全量恢复**：14:55 重查 201 `session_messages`（修正上轮 `ts>='2026-08-31'` 字符串比较 bug——`ts` 是整数毫秒，永远小于文本，误显 0）。正确毫秒边界后珊珊 **08-31=248、09-01=25、09-02=68、09-03=12**，总 744 条 → 家长端回看 8-31~9-3 即此。
- **"什么也没做就恢复" = 客户端自动补传 backlog**：用户 14:48 在珊珊 Mac 查本地记录时 App 连上 201 LAN，后台 session-sync 把本地 08-31~09-02（及 09-03）未 sync 的 session 增量自动推上 201（14:40 落盘 09-03 文件，余下批次随后补齐）。非手动修复，是同步机制自愈。
- **最终定性**：非数据丢失、非客户端 bug，是"会话回看/汇总强依赖孩子设备与 201 同处 LAN"的设计特性——离 LAN 时堆积+sync 暂停+回看空白，重连 201 后自动补齐。13:13 那批(08-19~08-30)与 14:40 这批(08-31~09-03)是两波连 201 补传。ISSUE-043 降级为"已自愈+设计限制记录"，无需修复。
- **遗留建议（非本次范围）**：①离线期回看滞后无家长感知；②确认珊珊 Mac `server-connection.json` 指向 201 家庭真源（若误配 cloud 则对话只能等连 201 才上云）；③sync 失败/游标异常仍静默，建议加 UI/日志告警。ISSUES.md ISSUE-043 已加「闭环结论」段。

## 15:30 ISSUE-043 完善「同步失败处理」已实施（完整版：日志落盘 + 设置页状态卡片 + 立即同步 + 导出日志）
- 新增 `electron/lib/sync-logger.ts`：落盘 `data/sync-log.jsonl`（append-only + 2000 行轮转），记录每次同步 ts/childId/trigger/serverUrl/ok/errType(network|http:<status>|server)/errMsg/bytes；内存 `statusMap` 维护每孩子 lastSyncAt/lastResult/consecutiveFails/lastError/connectedServer/pendingBytes/lastTrigger。
- 改造 `session-sync.ts`：`syncChildSessions(childId, trigger)` 加触发源参数（prompt/timer/quit/manual），成功/失败/无增量分别 markSyncSuccess/markSyncFailure(从 ServerError.status 区分网络与http)/markSyncSkip；原 console.warn 保留。
- 新增 IPC（ipc-handlers.ts）：`sessions:syncStatus`/`sessions:syncLog`/`sessions:forceSync`(立即同步)/`sessions:exportLog`(dialog.showSaveDialog 导出到本机)。
- preload.ts 暴露 4 个 api；前端新增 `src/components/SessionSyncPanel.tsx` + Dashboard 侧边栏「📡 会话同步」入口（每孩子状态卡 + 立即同步 + 导出日志 + 最近日志表，5s 自动刷新）。
- 验证：`electron-vite build` 通过（main/preload/renderer 均打包成功）。范围：客户端失败显式化 + 自助排查层，未动架构根因（cloud 部署 session_sync/强制发往 201）。ISSUES.md ISSUE-043 已加「修复」段。

## 16:01 ISSUE-043 根因修正（用户两次更正后实锤：非 LAN 不可达 / 非 cloud 误配，是客户端版本滞后）
- **用户更正**：①15:58 确认珊珊 Mac `server-connection.json` 指向 `http://192.168.1.201:8788`（家庭真源）→ 排除"误连公网 cloud"；②16:01 确认 8-31~9-2 珊珊能正常连 201 查进度/课程 → 排除"LAN 不可达 / 201 不可达"。→ 推翻 14:55「强依赖同 LAN」诊断。
- **git 实锤（关键）**：引入整条 session-sync 链路的 commit `de2ef67`（feat: 客户端 Agent 部分上云方案B）提交于 **2026-08-31 23:24**；`git ls-tree v0.1.7 | grep session-sync` = **NOT in v0.1.7**（连 server 端 sessions 路由都没有），**v0.1.8（2026-09-02 11:31）/ v0.1.9（2026-09-03）才包含**。版本线：v0.1.7(8-31)、v0.1.8(9-2)、v0.1.9(9-3)。
- **真因**：观察窗口内珊珊 Mac 跑 ≤v0.1.7（**无 session-sync 代码**）→ 读接口（进度/课程）正常（早就有），但本地会话永不上传；家长端回看空白。9-3 客户端升级到 0.1.9 后 sync 首次运行，从游标 0 把全部历史（08-19~09-03）一次性补传（13:13 批 08-19~08-30 + 14:40 批 08-31~09-03）。**数据从未丢失**。
- **结论修正**：ISSUE-043 = 客户端版本滞后导致的"观察窗口内旧客户端无上云能力"，随升级自愈。LAN/cloud 假设均排除。遗留真问题变为"server 有 sessions 能力但旧客户端永不推送 → 旧客户端孩子对话长期不可见"，建议 server 识别客户端版本弹更新提示 / 同步面板展示客户端版本+最后成功 sync。ISSUES.md ISSUE-043「闭环结论」+「用户确认」两段已改正。

## 16:23 记录 ISSUE-044：客户端 + 服务端统一日志系统
- 诉求：给 Electron 客户端 + learning-server 都补持久化/结构化/可远程取回的日志，替代零散 `console.*`（ISSUE-043 取证实锤打包 app 不落盘、远程拿不到）。同时答"记什么"。
- 现状：客户端唯一结构落盘是 `sync-logger.ts`(data/sync-log.jsonl，JSONL+2000行轮转+statusMap+面板，可复用范式)，其余全 console；服务端 Fastify logger:true 仅 stdout，worker 散 console，无文件 sink。
- 方向：①统一 JSONL 格式 `{ts,level,scope,component,childId?,parentId?,reqId?,msg,err?,durMs?}`，各自落 client-log.jsonl/server-log.jsonl，轮转；②客户端 `app-logger.ts` 重定向 console + 捕获 uncaughtException（照搬 sync-logger append/prune/吞异常）；③客户端记生命周期/孩子&家长期话(prompt入参长/模型切换/耗时/工具调用名/compaction)/sync(已覆盖)/上传/net-IPC；④服务端 pino 加 file transport + 访问日志 hook + worker/task_runs/provider/sync接收/500 落 logger；⑤可远程取回：客户端复用 exportLog 范式加 `app:exportLog` 设置页导出；⑥隐私：绝不记 prompt 正文/token/密钥，只记长度摘要，日志文件不入 git。优先级中，仅记录未实施。

## 20:10 学习计划「复习项不打勾」修复（用户生产实测反馈；201 已部署并自愈）
- **症状**：计划里「复习：<课程名>」项学完后——家长面板/计划列表不打勾、todolist [家长] 行不勾选、完成统计不动。珊珊&闻闻 2026-09-03 复现。
- **根因 1（匹配 Bug A）**：计划文本带「复习：」动作前缀，courses 真实标题无前缀；三处「计划文本→课程」精确匹配全部落空：`tasks.ts courseDoneFor`（worker stat）、`ipc-handlers.ts loadCourseDoneMap`（家长面板 done）、`exam.ts listPlanCourseMeta`（每日/每周考核选题）→ 复习项永不判完成、考核进 unmatched。闻闻学而篇一/二章明明 last_review=2026-09-03 仍 ⬜ 实证。
- **根因 2（stat Bug B）**：`scheduler.ts runStatTick` 游标=今天「一天只统计一次」，首跑后当天不再刷新——珊珊 18:50 首跑，19 点后学的子路篇八/九章全不勾（courses 已有 last_review=今天但 todo [ ]）。
- **修复**：
  - 新增 `server/src/plan-text.ts`：`planTextToCourseText`/`findCourseByPlanText`（剥动作前缀：复习/温习/学习/预习/背诵/朗读/跟读/听读/挑战/巩固/掌握 + 尾部（复习）标注；精确优先）；tasks/exam/ipc-handlers（客户端内联同款正则，⚠️须同步）接归一化匹配。
  - stat 改「事件驱动+当天可多次」：`worker_state(childId,'todo_stat').last_key` 存 JSON `{date,count}`（count=上次统计当天 daily 条数）；daily 新增→下次 tick 重跑；旧纯日期当天补跑一次并升级（存量自愈）。cron 与顺序不变。
  - 顺带清闻闻今日 todolist 残留「空天」占位行（gen 把空计划提示当 nonParentBody 保留所致，纯 cosmetic）。
- **验证**：本机 node_modules/typescript=7.0.2(native preview) tsc 缺 lib 不可用 → `npx -y -p typescript@5.6.3 tsc`：server 0 错；server.cjs 构建过；electron-vite build 过；verify-study-plans.mjs 17/17、verify-study-plan-carry.mts 4/4、新 server/scripts/check-plan-match.mts 9/9（真实闻闻样例）。
- **201 部署**：备份 `server.cjs.bak-20260903-2010` → systemctl restart；stat 12:14Z 自愈：珊珊子路篇 7/8/9 [x]（学而复习未做仍⬜正确）、闻闻泰伯+复习一/二 [x]、worker_state 已 JSON、child_todo_stats 3/6 与 3/4。
- **遗留**：家长面板 done 修复在客户端 main 进程（loadCourseDoneMap），需发新版客户端生效；exam/stat/todolist 已在服务端生效。
