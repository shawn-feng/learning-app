# 2026-08-30

## 实施 ISSUE-001~004（10:33-11:00，全部完成）
- **001 孩子卡片学习进度**：`server/routes/children.ts` GET /children 每孩子聚合 topic_progress（topics/learned/total/lastUpdated）；`child-auth.ts` ChildProfile+progress 透传；`Dashboard.tsx` 卡片进度条+最近学习时间（侧栏简要）。
- **002 去掉会话重置**：`SchedulerSettings.tsx` 删 UI/字段；`scheduler.ts` 删 sessionReset 配置、tick/catch-up 分支、TaskState "session-reset" 键；`pi:reset` 保留（聊天 /reset + 自动新建会话热路径）。
- **003 服务端数据备份/恢复**：新增 `server/src/routes/backup.ts`（GET /api/v1/backup 家长库+孩子 kb 打 zip，WAL checkpoint 后读主文件；POST /backup/restore multipart，**恢复前自动快照 pre-restore** 到 data/backups/<pid>/，只允许 parent.sqlite + 归属本家长的孩子 kb，校验 CRC）；zip 手写（与客户端同款），zipPack/zipUnpack 已导出并 smoke 通过（中文文件名/空文件/CRC 损坏检测）。`server-client.ts` 加 serverFetchBinary + serverUploadFile；`backup.ts` 改为服务端拉取/上传；`BackupSettings.tsx` 去掉跨机查进度；preload/IPC 删 syncQueryProgress。
- **004 移除分配主题**：`db.ts` exec op `kb.topics.deallocate`（只删 topics 分配行，**保留 courses + topic_progress 进度**，重新分配可续上）；`parent-library.ts` deallocateChildTopic；IPC/preload parent:deallocate；`ChildTopicsModal.tsx` 已添加行加移除按钮（confirmDialog 提示记录保留）。
- **验证**：server tsc/build 过；客户端 tsc 过滤环境告警后 0 业务错；electron build 过（主/preload/renderer）；backup.test.ts 重写为 mock server 版 4/4 过；zip smoke 过。全套件 82 失败均为 SPLIT 既有（parent-library/parent-stats/kb-tools/learning-summary/sync/voice 走 serverFetch 未 mock），非本次引入。ISSUES.md 已标 4 条已完成；未提交 git。

## 记录 ISSUE-001~004（10:31，新架构首批 4 条需求）
- 001 家长孩子卡片恢复学习进度（高）：`Dashboard.tsx` 无进度字段；服务端已有 `kb.progress.list`/`parent_lib.progress.list`（topic_progress）可聚合。
- 002 定时任务去掉「会话重置」：`SchedulerSettings.tsx:458-485` sessionReset 区块 + 后端 runSessionReset/pi:reset 分支，保留「自动新建会话」。
- 003 数据备份改为 server 端数据 zip（下载到本地 / 上传覆盖恢复 / 恢复前先自动备份），去掉跨机查进度；当前 `BackupSettings.tsx` 是本地 data/ zip，server 无备份端点，需新增。
- 004 分配主题支持移除：`ChildTopicsModal.tsx` 无移除 UI，server 无 deallocate 接口；移除只取消分配、保留 topic_progress 学习记录。

## ISSUES.md 清空重置（10:22，SPLIT 架构切换）
- 用户确认：ISSUE 文件里 ISSUE-001~052 全是旧架构（一体化 Electron 学习伙伴）内容，架构已调整（客户端+服务端 SPLIT），清空重来。
- 备份：`ISSUES.md`（352KB）→ `ISSUES-archive-2026-08-30.md`（同目录，归档留查）。
- 新 `ISSUES.md` = 空清单 + 重置说明 + 新架构记录模板（类型/描述/影响范围/入口/优先级/时间）。

## 0.1.5 发布（客户端）
- **内容**：base 注入修复（英语音标 emma 视频）+ display_content 复用家长端渲染（孩子端 CSS/第11课空白）+ scanMaterials 软链（服务端已在 201）。
- **流程**（同 0.1.4/0.1.5）：output=dist-release-015 → unset NODE_OPTIONS 打包 → OSS 中转 → 云助手覆盖 ECS → 登记 0.1.5 → 改回 output=dist 提交 push → tag v0.1.5 → Actions linux/mac 成功 → 下载产物 dist-ubuntu-015/dist-mac-015。
- 公网验证：latest.yml=0.1.5、/api/version=0.1.5 ✅。git 4456500 + 8369045。

## 孩子账户 server 真源化（多设备共享）
- **设计缺口确认**：listChildren 原只扫本地 data/children/*/profile.json（dev 时代），SPLIT 后未改——新装客户端看不到 server 上别的设备创建的孩子（用户本机只有珊珊、看不到闻闻）。
- **修复**：listChildren → async server 优先（GET /api/v1/children + token），本地 profile 补详情，server 有/本地无自动落盘默认 profile；未登录回退本地。addChild/deleteChild 同步 server（POST 支持传 id / DELETE）；server 不可用不阻塞本地。
- **服务端**：children.ts POST 支持可选 id（uuid 校验）；已重打 pkg + 部署 201（08:22）。
- **⚠️ 客户端生效**：需发布 0.1.6（本次改动在 0.1.5 之后）。测试 9 失败为环境 app.isPackaged 未 mock（非本次引入），孩子列表测试通过。

## SPLIT 遗漏清扫：客户端仍读本地的业务数据（系统排查）
- **排查结果**（应读 server 却读本地/旧架构）：
  1. ✅ 已修：**learning:summary 预取**（新设备看板空）——IPC 先 fetchProgressRemote 再返回
  2. ✅ 已修：**sync:query_progress（备份与进度页）**——旧 ISSUE-041 云端 sync + 本地 kb.sqlite 摘要（本地库已不写→必空），改读服务端 kb 进度汇总为旧结构
  3. ✅ 已修：**ChildTopicsModal「迁移存量资料」入口下线**（写本地 parent.sqlite，server 不认；保留函数仅提示）
  4. ⚠️ 已知限制：孩子 profile 详情（头像/年龄/AI名）仍本地（server 只存 id/name）；他设备孩子占位 passwordHash 空→本机 child:auth 失败（需重置密码）。详情上云=下一阶段。
- **已 SPLIT 确认无问题**：parent-library 核心（tags/topics/courses/分配/每日量/教学文案/材料）；getLearningSummary（server 预取缓存）；scheduler task-state/backup（设备级合理）；getSessionMaterials（server+本地 outputs 兜底）。
- **非活跃遗留**：delivery buildProgressSummary（本地 kb 摘要，旧 sync 推送链已由 server 进度取代）；learning:list/read/write（死组件 TopicEditor）；migrateChildrenToParent（入口已下线）。
- 提交：progress 修复 1 个 commit。客户端生效需 0.1.6。

## 孩子数据全量上云 + 删除存量迁移（用户两项需求）
- **需求1**：migrateChildrenToParent（存量迁移孩子→家长库）彻底删除（函数/类型/ipc/preload/UI runMigrate）。
- **需求2**：孩子详情+密码哈希唯一真源在 server：
  - server children 表 +profile_json（启动 ALTER 迁移，schema_version 6）；POST 收 profile、GET 返详情、PATCH 更新、**POST /children/auth bcrypt 校验**；依赖 bcryptjs（pkg --public 打包含）。
  - 客户端：addChild 上传 profile；authChild 服务端校验（离线回退本地 hash）；resetChildPassword/updateChildProfile 同步 PATCH；listChildren 服务端详情落盘 + 本地有 server 无自动上传。
  - 闻闻/珊珊 profile（含 hash）已推送 201；server 已部署（08:40，列确认）。
- ⚠️ 客户端生效需 0.1.6。孩子密码跨设备：服务端校验统一。

## 模型配置读取位置 + 重启后模型为空（排查修复）
- **读取位置**（客户端本地，非 server）：
  - 模型选择/默认模型：`<data>/app-settings.json`（defaultModel/programmingModel/visionModel；安装版 data = `%APPDATA%/learning-app/app-data/`，dev = 项目 data/）
  - 模型 API keys：`<data>/shared/auth.json`（provider → {type, key}；deepseek/mimo/qwen 各段；**设备本地不上云**）
- **重启后模型为空根因**：安装版 app-data 无 app-settings.json（dev 配置在项目 data/ 读不到）+ config-sync 全量覆盖（201 曾被覆盖丢 2 字段）→ 拉缺字段配置清空本地。
- **修复**：① config-sync 改**合并写回**（mergeJsonFile，server 值覆盖同名 key、本地独有保留）② 201 settings 已补全（mimo 默认 + qwen 编程/视觉）③ 安装版 app-settings.json 已直接写入。
- ⚠️ auth.json 只有 deepseek key——mimo/qwen 模型需在设置里补 key 才可用。

## 模型配置 + key 上云（用户决策，改设计）
- **决策**：模型配置（app_settings）**和 API key（auth）都随家长账号存 server**，多客户端/孩子登录 2 分钟轮询同步缓存到本地；**按家长隔离**（不全局共用）。
- **server**：/config 键改 `{parent_id}:{key}`（settings 表复用，LIKE 前缀查询）；revision 全局（A 改 → B 多拉一次仅自己键，无害）。已部署 201 + 迁移旧 settings 到家长前缀 + 写入 deepseek key。
- **客户端**：setProviderApiKey 保存后 pushConfig('auth') 全量上云；config-sync 轮询新增 auth 键 → mergeJsonFile 合并写回 auth.json（key 跨设备同步）。app_settings 已有 pushConfig。
- ⚠️ 客户端生效需 0.1.6。key 明文存 server（家庭/自部署可接受，公网 server 需 HTTPS + 权限）。

## 客户端本地配置按家长分区（用户决策：多家长同 app 不混数据）
- **问题**：本地 parents 只有 default，auth.json/app-settings.json/scheduler-config 全局 → 多家长登录混乱。
- **方案**：当前家长 id 记 `<data>/.session.json`（登录写/登出清）；配置路径全部进 `parents/<parentId>/`：
  auth.json、app-settings.json、scheduler-config.json（getParentConfigDir 自动建）。
- **家长 AGENTS 按家长隔离**：server agents 的 parent scope ref 强制=ctx.parentId（不再 main/content）；
  客户端 prefetchAgents/buildParentPrompt/IPC agents:* 全部用 getCurrentParentId()。历史 main/content 无数据（迁移 0 条）。
- **登录迁移**：activateParentSession 把旧位置（shared/auth.json、根 app-settings/scheduler-config）复制到家长目录。
- **201 已部署**（config 家长前缀 + agents 家长隔离）。客户端生效需 0.1.6。
- ⚠️ children/ 保持全局（childId 全局唯一无冲突）；license.json 保持全局（会话态）。

## 家长页面 key 空——根因：未登录读 default 目录 + revision 相同不拉
- 201 server auth 键有 key（86a84278:auth 含 deepseek）✅ 非空。
- 客户端本机：未登录（无 .session.json）→ getCurrentParentId()="default" → 读 parents/default/auth.json（不存在）→ 空。
- **key 同步链路**：登录 → startConfigSync 首次 syncOnce → 拉 server config（auth 键）→ 写 parents/<pid>/auth.json → 家长页面显示。
- **修复**：syncOnce 增加「auth.json 缺失强制拉」（revision 相同也拉），保证登录后必拿到 key（0.1.6 生效）。
- **用户操作**：app 内登录家长账号 → 2 分钟内自动同步 server key。
