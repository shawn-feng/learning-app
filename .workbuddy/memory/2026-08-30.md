# 2026-08-30

## 实施 ISSUE-001~004（10:33-11:00，全部完成）
- **001 孩子卡片学习进度**：`server/routes/children.ts` GET /children 每孩子聚合 topic_progress（topics/learned/total/lastUpdated）；`child-auth.ts` ChildProfile+progress 透传；`Dashboard.tsx` 卡片进度条+最近学习时间（侧栏简要）。
- **002 去掉会话重置**：`SchedulerSettings.tsx` 删 UI/字段；`scheduler.ts` 删 sessionReset 配置、tick/catch-up 分支、TaskState "session-reset" 键；`pi:reset` 保留（聊天 /reset + 自动新建会话热路径）。
- **003 服务端数据备份/恢复**：新增 `server/src/routes/backup.ts`（GET /api/v1/backup 家长库+孩子 kb 打 zip，WAL checkpoint 后读主文件；POST /backup/restore multipart，**恢复前自动快照 pre-restore** 到 data/backups/<pid>/，只允许 parent.sqlite + 归属本家长的孩子 kb，校验 CRC）；zip 手写（与客户端同款），zipPack/zipUnpack 已导出并 smoke 通过（中文文件名/空文件/CRC 损坏检测）。`server-client.ts` 加 serverFetchBinary + serverUploadFile；`backup.ts` 改为服务端拉取/上传；`BackupSettings.tsx` 去掉跨机查进度；preload/IPC 删 syncQueryProgress。
- **004 移除分配主题**：`db.ts` exec op `kb.topics.deallocate`（只删 topics 分配行，**保留 courses + topic_progress 进度**，重新分配可续上）；`parent-library.ts` deallocateChildTopic；IPC/preload parent:deallocate；`ChildTopicsModal.tsx` 已添加行加移除按钮（confirmDialog 提示记录保留）。
- **验证**：server tsc/build 过；客户端 tsc 过滤环境告警后 0 业务错；electron build 过（主/preload/renderer）；backup.test.ts 重写为 mock server 版 4/4 过；zip smoke 过。全套件 82 失败均为 SPLIT 既有（parent-library/parent-stats/kb-tools/learning-summary/sync/voice 走 serverFetch 未 mock），非本次引入。ISSUES.md 已标 4 条已完成；未提交 git。

## 记录 ISSUE-001~004（10:31，新架构首批 4 条需求）
- 001 家长孩子卡片恢复学习进度（高）：`Dashboard.tsx` 无进度字段；服务端已有 `kb.progress.list`/`parent_lib.progress.list`（topic_progress）可聚合。
- 002 定时任务去掉「会话重置」：`SchedulerSettings.tsx:458-485` sessionReset 区块 + 后端 runSessionReset/pi:reset 分支，保留「自动新建会话」。
- 003 数据备份改为 server 端数据 zip（下载到本地 / 上传覆盖恢复 / 恢复前先自动备份），去掉跨机查进度；当前 `BackupSettings.tsx` 是本地 data/ zip，server 无备份端点，需新增。
- 004 分配主题支持移除：`ChildTopicsModal.tsx` 无移除 UI，server 无 deallocate 接口；移除只取消分配、保留 topic_progress 学习记录。

## ISSUES.md 清空重置（10:22，SPLIT 架构切换）
- 用户确认：ISSUE 文件里 ISSUE-001~052 全是旧架构（一体化 Electron 学习伙伴）内容，架构已调整（客户端+服务端 SPLIT），清空重来。
- 备份：`ISSUES.md`（352KB）→ `ISSUES-archive-2026-08-30.md`（同目录，归档留查）。
- 新 `ISSUES.md` = 空清单 + 重置说明 + 新架构记录模板（类型/描述/影响范围/入口/优先级/时间）。

## 0.1.5 发布（客户端）
- **内容**：base 注入修复（英语音标 emma 视频）+ display_content 复用家长端渲染（孩子端 CSS/第11课空白）+ scanMaterials 软链（服务端已在 201）。
- **流程**（同 0.1.4/0.1.5）：output=dist-release-015 → unset NODE_OPTIONS 打包 → OSS 中转 → 云助手覆盖 ECS → 登记 0.1.5 → 改回 output=dist 提交 push → tag v0.1.5 → Actions linux/mac 成功 → 下载产物 dist-ubuntu-015/dist-mac-015。
- 公网验证：latest.yml=0.1.5、/api/version=0.1.5 ✅。git 4456500 + 8369045。

## 孩子账户 server 真源化（多设备共享）
- **设计缺口确认**：listChildren 原只扫本地 data/children/*/profile.json（dev 时代），SPLIT 后未改——新装客户端看不到 server 上别的设备创建的孩子（用户本机只有珊珊、看不到闻闻）。
- **修复**：listChildren → async server 优先（GET /api/v1/children + token），本地 profile 补详情，server 有/本地无自动落盘默认 profile；未登录回退本地。addChild/deleteChild 同步 server（POST 支持传 id / DELETE）；server 不可用不阻塞本地。
- **服务端**：children.ts POST 支持可选 id（uuid 校验）；已重打 pkg + 部署 201（08:22）。
- **⚠️ 客户端生效**：需发布 0.1.6（本次改动在 0.1.5 之后）。测试 9 失败为环境 app.isPackaged 未 mock（非本次引入），孩子列表测试通过。

## SPLIT 遗漏清扫：客户端仍读本地的业务数据（系统排查）
- **排查结果**（应读 server 却读本地/旧架构）：
  1. ✅ 已修：**learning:summary 预取**（新设备看板空）——IPC 先 fetchProgressRemote 再返回
  2. ✅ 已修：**sync:query_progress（备份与进度页）**——旧 ISSUE-041 云端 sync + 本地 kb.sqlite 摘要（本地库已不写→必空），改读服务端 kb 进度汇总为旧结构
  3. ✅ 已修：**ChildTopicsModal「迁移存量资料」入口下线**（写本地 parent.sqlite，server 不认；保留函数仅提示）
  4. ⚠️ 已知限制：孩子 profile 详情（头像/年龄/AI名）仍本地（server 只存 id/name）；他设备孩子占位 passwordHash 空→本机 child:auth 失败（需重置密码）。详情上云=下一阶段。
- **已 SPLIT 确认无问题**：parent-library 核心（tags/topics/courses/分配/每日量/教学文案/材料）；getLearningSummary（server 预取缓存）；scheduler task-state/backup（设备级合理）；getSessionMaterials（server+本地 outputs 兜底）。
- **非活跃遗留**：delivery buildProgressSummary（本地 kb 摘要，旧 sync 推送链已由 server 进度取代）；learning:list/read/write（死组件 TopicEditor）；migrateChildrenToParent（入口已下线）。
- 提交：progress 修复 1 个 commit。客户端生效需 0.1.6。

## 孩子数据全量上云 + 删除存量迁移（用户两项需求）
- **需求1**：migrateChildrenToParent（存量迁移孩子→家长库）彻底删除（函数/类型/ipc/preload/UI runMigrate）。
- **需求2**：孩子详情+密码哈希唯一真源在 server：
  - server children 表 +profile_json（启动 ALTER 迁移，schema_version 6）；POST 收 profile、GET 返详情、PATCH 更新、**POST /children/auth bcrypt 校验**；依赖 bcryptjs（pkg --public 打包含）。
  - 客户端：addChild 上传 profile；authChild 服务端校验（离线回退本地 hash）；resetChildPassword/updateChildProfile 同步 PATCH；listChildren 服务端详情落盘 + 本地有 server 无自动上传。
  - 闻闻/珊珊 profile（含 hash）已推送 201；server 已部署（08:40，列确认）。
- ⚠️ 客户端生效需 0.1.6。孩子密码跨设备：服务端校验统一。

## 模型配置读取位置 + 重启后模型为空（排查修复）
- **读取位置**（客户端本地，非 server）：
  - 模型选择/默认模型：`<data>/app-settings.json`（defaultModel/programmingModel/visionModel；安装版 data = `%APPDATA%/learning-app/app-data/`，dev = 项目 data/）
  - 模型 API keys：`<data>/shared/auth.json`（provider → {type, key}；deepseek/mimo/qwen 各段；**设备本地不上云**）
- **重启后模型为空根因**：安装版 app-data 无 app-settings.json（dev 配置在项目 data/ 读不到）+ config-sync 全量覆盖（201 曾被覆盖丢 2 字段）→ 拉缺字段配置清空本地。
- **修复**：① config-sync 改**合并写回**（mergeJsonFile，server 值覆盖同名 key、本地独有保留）② 201 settings 已补全（mimo 默认 + qwen 编程/视觉）③ 安装版 app-settings.json 已直接写入。
- ⚠️ auth.json 只有 deepseek key——mimo/qwen 模型需在设置里补 key 才可用。

## 模型配置 + key 上云（用户决策，改设计）
- **决策**：模型配置（app_settings）**和 API key（auth）都随家长账号存 server**，多客户端/孩子登录 2 分钟轮询同步缓存到本地；**按家长隔离**（不全局共用）。
- **server**：/config 键改 `{parent_id}:{key}`（settings 表复用，LIKE 前缀查询）；revision 全局（A 改 → B 多拉一次仅自己键，无害）。已部署 201 + 迁移旧 settings 到家长前缀 + 写入 deepseek key。
- **客户端**：setProviderApiKey 保存后 pushConfig('auth') 全量上云；config-sync 轮询新增 auth 键 → mergeJsonFile 合并写回 auth.json（key 跨设备同步）。app_settings 已有 pushConfig。
- ⚠️ 客户端生效需 0.1.6。key 明文存 server（家庭/自部署可接受，公网 server 需 HTTPS + 权限）。

## 客户端本地配置按家长分区（用户决策：多家长同 app 不混数据）
- **问题**：本地 parents 只有 default，auth.json/app-settings.json/scheduler-config 全局 → 多家长登录混乱。
- **方案**：当前家长 id 记 `<data>/.session.json`（登录写/登出清）；配置路径全部进 `parents/<parentId>/`：
  auth.json、app-settings.json、scheduler-config.json（getParentConfigDir 自动建）。
- **家长 AGENTS 按家长隔离**：server agents 的 parent scope ref 强制=ctx.parentId（不再 main/content）；
  客户端 prefetchAgents/buildParentPrompt/IPC agents:* 全部用 getCurrentParentId()。历史 main/content 无数据（迁移 0 条）。
- **登录迁移**：activateParentSession 把旧位置（shared/auth.json、根 app-settings/scheduler-config）复制到家长目录。
- **201 已部署**（config 家长前缀 + agents 家长隔离）。客户端生效需 0.1.6。
- ⚠️ children/ 保持全局（childId 全局唯一无冲突）；license.json 保持全局（会话态）。

## 家长页面 key 空——根因：未登录读 default 目录 + revision 相同不拉
- 201 server auth 键有 key（86a84278:auth 含 deepseek）✅ 非空。
- 客户端本机：未登录（无 .session.json）→ getCurrentParentId()="default" → 读 parents/default/auth.json（不存在）→ 空。
- **key 同步链路**：登录 → startConfigSync 首次 syncOnce → 拉 server config（auth 键）→ 写 parents/<pid>/auth.json → 家长页面显示。
- **修复**：syncOnce 增加「auth.json 缺失强制拉」（revision 相同也拉），保证登录后必拿到 key（0.1.6 生效）。
- **用户操作**：app 内登录家长账号 → 2 分钟内自动同步 server key。

## 验证 ISSUE-001~004（用户要求实测）——全部通过
- **部署**：提交 3ec46f8（10:58 实施 ISSUE 001~004）在 09:49 部署之后 → 已重新打包部署 201（11:16）。
- **ISSUE-001 孩子卡片进度** ✅：GET /children 实测返回 progress（珊珊 8主题/536/1300/08-28；闻闻 5/289/1194/08-25）；Dashboard progressText 进度条组件在。
- **ISSUE-002 去会话重置** ✅：SchedulerSettings + scheduler.ts 无 sessionReset 残留。
- **ISSUE-003 服务端备份** ✅：GET /backup 3MB zip（manifest+parent.sqlite+2 kb）；POST /restore 正常恢复（**恢复前自动快照** pre-restore zip）+ 坏/截断 zip 优雅 400（"无效的 zip 文件"）；BackupSettings 无跨机查询。
  - ⚠️ 测试中真实执行了一次恢复（同源 zip）→ 数据无损（已验证）+ 快照保留。curl 坏 zip 显示 HTTP 000 是 curl 问题，python 实测 400 正常。
- **ISSUE-004 移除主题** ✅：kb.topics.deallocate op 注册 + deallocateChildTopic；孩子有分配（珊珊8/闻闻5）；ChildTopicsModal 移除按钮在（记录保留确认）。
- tsc 两端 0 错。

## 记录 ISSUE-005/006（11:30，仅记录未实施）
- 用户两条进度交互需求（用户明确"本会话只记录，不执行"）：
- **ISSUE-005**：孩子卡片进度不再直铺（ISSUE-001 现状），改为卡片「学习进度」icon 入口，点击进入详细进度看板，界面与孩子模式 LearningDashboard 完全一致。排查入口：Dashboard.tsx:206-233 改 icon；复用 LearningDashboard（Learn.tsx 挂载）；ProgressView.tsx 现无人引用可参考/废弃。
- **ISSUE-006**：孩子模式点主题后看不到该主题详细课程学习情况（LearningDashboard 代码已有三级下钻 openTopic→learning:topic→CourseDetail，但实测不显示明细，疑在 getTopicProgress 是否真返回每课 CourseItem）；并要求家长/孩子两模式进度界面与操作完全一致。

## ISSUE-005/006 学习进度看板（实施 + 实测验证）
- **ISSUE-005 家长进度 icon 入口**：Dashboard 孩子卡片直铺进度（ISSUE-001 的进度条/摘要）→ 改 BarChart3「学习进度」icon；点击弹窗（maxWidth 760）复用孩子模式同一 LearningDashboard 组件；侧栏去掉直铺；删 progressText 函数。
- **ISSUE-006 孩子模式点主题看不到课程明细（真 bug）**：根因=`learning:topic`→getTopicProgress 只查 kb.progress.list（topic_progress 视图行，无 items），组件期望 TopicDetail{items}→`d.items` undefined（filter 崩溃/空白）。修复=并行查 kb.progress.list + kb.courses.list(topic 过滤)组装 TopicDetail；新增 CourseRow/CourseItem/TopicDetail 类型导出。
- **实测（tsx 冒烟走 201 真实数据）**：summary 8 主题 536/1300；english items=51（✅14）；lunyu items=512（✅305/⬜207 与 progress 一致）、首课当课总结 2 条（daily_entries 关联 chapterKey 正常）——三级下钻全通。tsc 0 错。
- 测试：functional/app 12 失败为既有环境问题（app.isPackaged/PI_TEST_DATA_DIR），本次 diff 不涉及其链路。
- ⚠️ 客户端生效需 0.1.6。

## 记录 ISSUE-007（12:05，仅记录未实施）
- 用户需求：点击孩子卡片本身（非仅 icon）进入孩子**详情页**，不再用弹窗展示学习进度/分配主题/编辑 prompt/重置密码，改为详情页内**标签页（tabs）**，每块内容占满 dashboard 内容区，详情页带返回。
- 背景：ISSUE-005/006 已被另一会话实施（commit 912c4fe，Dashboard 卡片 5 个 icon + 弹窗复用 LearningDashboard；getTopicProgress 修复返回 CourseItem 明细，三级下钻通）。ISSUE-007 在其基础上把弹窗（progressChild:273/ChildTopicsModal:266/resetModal:296/AgentPromptEditor:316）迁移为详情页 tabs。

## 12 个失败测试全绿（环境问题根因分析 + 修复）
- **根因 4 类**：
  1. **[真bug] user-init.ts `app.isPackaged` 无 `?.`**——非 Electron 环境（vitest node 无 electron mock）直接 TypeError → `app?.isPackaged`
  2. **[真bug] pi-runtime.ts getSharedRuntime 写 auth.json 前未建父目录**——按家长分区后路径 parents/<pid>/auth.json，未登录(_guest)/新家长目录不存在 → ENOENT → mkdirSync(dirname)（补 path import）
  3. **[测试滞后] listChildren async 化后 functional 8 处漏 await**——`listChildren().find` → TypeError（:95/105/139/159/201/221/237/248）
  4. **[断言过时] tags/taxonomy.md、daily 目录**（ISSUE-032 废弃）→ 改 SQLite 真源断言（tags 表≥20 + 文件不存在 + 废弃目录全部不存在）
- **[测试基建]**
  - registers parent 依赖云端服务 → vi.mock server-client（/auth/register、/auth/license）
  - 历史残留污染：pi-test-data 累积旧架构夹具目录（ans-before-*/test-child-033 avatar=None）→ 测试文件 beforeAll 清理 PI_TEST_DATA_DIR
  - ⚠️ **vitest 4 的 globalSetup 配置实测不执行**（config 类型无该项）→ 改用 beforeAll + `fileParallelism:false`（共享 tmp 目录防并行互删）
- **结果**：functional(17)+app(4) 21/21 全绿，连续两次稳定；tsc 0 错。

## ISSUE-007 孩子详情页（tabs 替代弹窗）
- **方案**：点击孩子卡片整体进入详情页（新组件 `ChildDetailPage.tsx`），tabs：学习进度(LearningDashboard)/学习主题/AI 提示词/账号密码(重置+删除危险区)。
- **组件拆分**：`ChildTopicsModal`→`ChildTopicsContent`（平铺）、`AgentPromptEditor`→`AgentPromptContent`（平铺）；弹窗容器组件保留（default export 仍可用）。⚠️ IconButton 不渲染 children，文字用 label prop。
- **Dashboard**：删 4 弹窗 state + 5 icon 按钮；view=children 时 detailChild 优先渲染详情页；onDeleted 返回列表并 refresh。
- 验证：npm run build ✓、functional/app 21/21 ✓、tsc 0 错。客户端生效需 0.1.6。

## 记录 ISSUE-008/009（13:58，仅记录未实施）
- **ISSUE-008**：孩子界面学习资料展示区（MaterialsPanel，view==="materials"）可折叠；display_content 调用时自动展开并展示最新内容（可叠加在 Learn.tsx:215 现有 display_content 自动打开链路）。左侧 learn-sidebar 折叠已有（sidebarCollapsed），资料区无折叠。
- **ISSUE-009**：聊天 markdown 字体放大一倍（.markdown-body 现 15px）+ 行间不留空行（p margin-bottom 8px 改紧凑）；⚠️ .markdown-body 全局共享（家长端/资料面板同用），需确认作用域。
- 另：ISSUE-007 已被另一会话实施（commit 后，ChildDetailPage.tsx tabs 方案落地）。

## 记录 ISSUE-010（14:00，仅记录未实施）
- 语音输入（按住说话 Mic）按钮消失：ChatWindow.tsx:827-838 为条件渲染 `voiceEnabled && ...`；voiceEnabled 来自 voice:config:get → getMaskedConfig().enabled，**默认 false**（voice-config.ts:21）；若 voice-config.json 丢失/被重置（SPLIT 迁移等）→ 按钮消失。修复方向：恢复 enabled 或按钮常显。

## ISSUE-008/009/010（孩子界面三连）
- **008 资料区可折叠**：MaterialsPanel `onCollapse` + PanelRightClose 折叠按钮（详情/列表态）；Learn.tsx `materialsCollapsed` state，折叠窄条（PanelRightOpen 展开）；display_content 的 materials effect 自动 setMaterialsCollapsed(false)+选中最新。
- **009 孩子聊天 markdown 放大**：AI 气泡 `bubble-md-child`（仅 owner!=="parent"，家长端不受影响）；正文 30px、p/ul/ol margin 4px 紧凑、标题 36/34/32/30、代码块 22px。注意：聊天 markdown 样式在 .bubble-md（非 .markdown-body），别改错文件。
- **010 语音按钮补回**：根因=DEFAULT_CONFIG.enabled=false（voice-config.json 缺失/丢失）+ ChatWindow 条件渲染。修复=Mic 始终渲染，未开启时点击提示「请家长在设置→语音输入中开启」。
- 验证：build ✓、21/21 ✓、tsc 0 错。⚠️ 客户端生效需 0.1.6。

## 记录 ISSUE-011（14:32，仅记录未实施）
- 资料 html 语音音色与聊天不一致，根因已定位：课程 html（hanzigong lesson-*/index.html speak() 等）用 **Web Speech API**（speechSynthesis + Edge 在线神经语音选择，不可控，沙盒下常 fallback 本地机械音）；聊天用**主进程 edge-tts**（voice:tts → tts-config 默认 edge-tts，音色稳定）。
- 推荐方案：injectBridge（page-bridge.ts:336）接管 window.speechSynthesis → postMessage 上抛 → window.api.voiceTts 走聊天同链路，课程 html 无需逐个改。

## 记录 ISSUE-012（14:35，仅记录未实施）
- 分配主题列表与课程管理数量不一致（课程管理 9 个，分配时不足 9）。
- 已排查：服务端 parent.sqlite topics 实测 9 行（english/hanzigong/lunyu/qianziwen/reading/taodi/xiaojing/xiaozhuan/feizhougu）；CourseManager 与 ChildTopicsContent 同调 parent:listTopics（ipc-handlers:316 → listParentTopics → parent_lib.topics.list 全量无过滤），前端 topics.map 无过滤。代码层两处应一致，待用户提供实际差异/登录态/构建版本排查。

## 记录 ISSUE-013 + 修正 ISSUE-011（14:40）
- 用户追问"为什么沙盒 iframe 下选不到在线语音"→ 结论：**与沙盒无关**，根因是 Electron(Chromium) 没有 Edge 特供的微软在线神经语音（Xiaoxiao Online Natural 是 Edge 云 TTS 专属，Chromium getVoices 无此条目；Chromium 唯一在线语音 Google network speech 需 API key、Electron 默认无）→ cvInitVoices 在线分支全落空，fallback 本地 SAPI 机械音。已修正 ISSUE-011 表述并新增 ISSUE-013 知识记录。

## 记录 ISSUE-014（14:50，仅排查+记录未实施）
- 孩子 agent 调 page_inspect 多次失败。已梳理完整链路：pageInspectTool(custom-tools:1019) → executePageAction(read, 10s 超时) → transport(webContents.send pi:page:exec) → Learn.tsx handlePageExec(childId 不匹配静默丢弃) → MaterialsPanel.exec(iframe 未就绪→"页面未就绪或已关闭")。
- 失败档位：①未展示资料/无 iframe（最常见）②childId 不匹配→10s 超时 ③iframe 桥未就绪 ④transport 未注入/主窗口 null ⑤iframe 内快照慢/异常。需 agent 报错原文定位。

## 记录 ISSUE-015（15:07，仅记录未实施）
- 孩子页面操作反复发给 agent 导致每环节都回复。根因：queuePageEvent（page-bridge.ts:169）→ 600ms 批处理 → injectToSession（:135）空闲 followUp/运行中 steer 自动注入 `[页面事件]`。
- 目标：仅记录不自动投递；孩子下一轮消息（Learn.tsx:433 handleSend 组装 promptText :528）附带「[页面操作] 孩子在页面的操作：…」，发送后清空；page_inspect/action 保留。

## ISSUE-008 折叠后聊天区占满（用户修正）
- 原实现：折叠资料只显示 40px 窄条，聊天区保持固定宽度（440 可拖拽）→ 右侧空白。
- 修复：learn-chat style 三元——chat.collapsed 优先（44px 窄条）→ materials 折叠时 flex:1 1 auto 占满 → 否则保持 chat.width 可拖拽。

## ISSUE-012/014/015
- **012 核实**：server topics 9 行全量 + 客户端无过滤 → 当前一致，原现象=旧构建/迁移前数据，无代码改动。
- **014 修复（真 bug）**：page_inspect 报错原文「Cannot read properties of undefined (reading 'catch')」——pageExecTransport 主窗口 null 时返回 undefined，executePageAction `.catch` 崩 → `Promise.resolve(...)` 包装（undefined 按 10s 超时「页面无响应」语义）。
- **015 实施（行为调整）**：页面操作不再自动注入 agent（删 injectToSession/setSessionProvider/batchTexts 链路），queuePageEvent 入环形缓冲 + pendingByChild；takePendingPageEvents + IPC pi:page:pending + preload pageTakePending；Learn handleSend 附带「[页面操作] 这部分是孩子在页面上的操作：…」并清空；page_inspect/page_action 保留。page-bridge.test 旧注入测试重写为新语义（25/25）。
- 验证：46/46（page-bridge 25 + functional 17 + app 4）全绿、tsc 0 错。客户端生效需 0.1.6。

## ISSUE-011 资料朗读统一 edge-tts（桥接 speechSynthesis，课程 html 零改动）
- **方案**：BRIDGE_SCRIPT 注入 shim——getVoices 返回模拟 Edge 在线语音（Xiaoxiao Online voiceURI=pi://edge-tts/...，课程 cvInitVoices 可选中）、speak 上抛 kind=tts、cancel 上抛 tts-cancel、父级回执 page:tts:done 触发 utterance.onend（按钮复位）；MaterialsPanel 处理 tts：voiceTts(edge-tts) 播放 + 播完 postMessage 回执 + cancel/卸载停止；tts 事件不进页面操作记录。PageEventKind 加 tts/tts-cancel。
- **测试**：page-bridge.test 新增 shim 行为测试（stub window/document/speechSynthesis eval 桥脚本：getVoices 模拟列表、speak 上抛、cancel 清 active、tts:done 触发 onend）——⚠️ 坑：桥脚本多个 message listener 需遍历触发；cancel 清 active 后需重新 speak 再回执。47/47 全绿。
- 客户端生效需 0.1.6。至此 ISSUE-001~015 全部处理完毕。

## 连续两条 AI 回复只显示一条——修复（用户实测反馈）
- **现象**：珊珊会话 agent 连续两条消息（学习总结+今日总结）实时只显示一条，退出重进显示两条。
- **根因**：一次 session.prompt() 内 agent 产生多条 assistant 消息（工具调用轮中间文本 + 最终回复）。pi:prompt/pi:prompt_parent/pi:prompt_parent_content 三处原实现只提取**最后一条** assistant 文本回发（倒序遍历遇第一条 assistant break）→ 中间文本实时丢（jsonl 已写入 → 历史恢复两条都在）。
- **修复**：改为收集本轮新增 [beforeCount, end) 每条 assistant 消息文本，**逐条 send pi:reply**（第一条替换前端 working 气泡、后续追加新气泡，与历史恢复呈现一致）。实测旧 1 条 vs 新 2 条。
- 47/47 全绿、tsc 0 错。客户端生效需 0.1.6。

## 语音输入自动开启（用户决策）
- getMaskedConfig 的 enabled 改为 `cfg.enabled || 任一语音 provider 已配置`（qwen/mimo/qwen-tokenplan/mimo-tokenplan 任一 isProviderConfigured：自身 apiKey 或 auth.json 模型 key）——配了模型 key 即自动开启，无需手动开关；显式关闭不生效。
- 实测本机（auth.json 有 mimo）→ enabled=true。VoiceSettings/ChatWindow 无需改动。客户端生效需 0.1.6。

## ISSUE-013 修正：资料朗读点击无播放（ISSUE-011 回归）
- **现象**：汉字宫/english 资料朗读按钮点击后无播放。
- **根因**：shim getVoices 返回模拟 plain object 语音 → 课程脚本 `utter.voice = <plainObject>` → WebIDL SpeechSynthesisVoice 类型校验抛 TypeError → speak() 中断（不上抛）。
- **修复**：getVoices 返回**空数组**（课程 voice 选择判空跳过；音色由 edge-tts 决定）；speaking 动态化（speak=true/cancel·done=false）恢复「再点停止/切新停旧」语义。测试同步（26/26）。47/47 全绿。
- ⚠️ 教训：WebIDL 接口类型属性赋值 plain object 必抛 TypeError；模拟语音对象不可行，靠判空跳过。

## 资料朗读仍无法播放——真正根因（speaking 只读属性）
- E2E（真实 Chromium iframe，electron.exe 冒烟）验证：speak 可替换、postMessage 可上抛、edge-tts 合成 OK、Audio.play() 无手势可播（Electron 默认不拦 autoplay）——链路全通。
- **真根因**：speechSynthesis.speaking/paused/pending 是 WebIDL **只读属性**（getter-only），BRIDGE_SCRIPT strict mode 下 `syn.speaking = true` 抛 TypeError → shim speak 中断 → 不上抛（getVoices 空数组修复后仍崩）。
- **修复**：移除只读属性赋值，改 Object.defineProperty 重定义 getter（speaking=active!==null）；defineProperty 失败静默（不影响播放）。MaterialsPanel 加 [pi-tts] 日志。测试 mock 改只读属性模拟真实 Chromium（防回归）。47/47 全绿。
- ⚠️ 教训：桥脚本里模拟/替换 WebIDL 接口成员时，只读属性（speaking/paused 等）不能赋值——要么 defineProperty，要么别动。另：本机 Electron v43.3.0，沙箱下跑 e2e 需 env -u ELECTRON_RUN_AS_NODE（全局被设为 1）。

## 拖拽上传文件（多文件批量）
- ChatWindow 支持拖拽上传：chat-window 根 div 监听 dragenter/over/leave/drop（dragDepthRef 计数防误消），dragActive 时显示提示层（.chat-drag-overlay + .chat-drag-hint，紫色虚线框 outline）；drop 取 e.dataTransfer.files 批量处理。
- 抽 `processFiles(files)` 统一逻辑：上传按钮多选（input multiple 原有）+ drop 共用——图片→预览、音频→转写、txt/md→读取。
- chat-window 加 position:relative（overlay 基准）。47/47 全绿。客户端生效需 0.1.6。

## 记录 ISSUE-016（19:54，仅记录未实施）
- 孩子界面中间展示区折叠按钮仅学习资料页有，学习进度等页无。根因：Learn.tsx:114 `materialsCollapsed` 仅绑定 materials；折叠条/material-collapsed-bar 条件 `view==="materials" && materialsCollapsed`（:777-785）；中间区分支 :775 `view==="materials" ? MaterialsPanel : LearningDashboard`（progress 直接渲染 LearningDashboard 无折叠包裹）；聊天区宽度 :806 同样绑 materials 折叠。
- 目标：所有展示页（学习资料/学习进度等）左侧都有折叠/展开按钮，聊天区能占满。改造方向：折叠能力抽象为不依赖 view（泛化 state）；中间区统一包裹折叠；各展示页暴露 onCollapse（如 LearningDashboard）；折叠外层容器即可不动内部。优先级待定，未实施。

## 0.1.6 发布（客户端，20:3x）
- **内容**：孩子 server 化 + 家长分区 + ISSUE-001~016 客户端改动（进度看板/详情页/资料折叠/朗读 edge-tts 两轮修复/语音自动开启/双消息修复/拖拽上传）。
- **流程**（同 0.1.5）：version 0.1.6 + output=dist-release-016 → unset NODE_OPTIONS 打包（166MB exe）→ publish-update.py 上传 OSS（bucket aixuexihao-app，**公共读**）→ **⚠️ OSS 签名 URL 全部 403（AK 无 GET 权限/签名问题）→ 改用公共读直链** → 云助手 RunCommand curl 覆盖 ECS /opt/learning-cloud/download/ → 登记 0.1.6（ADMIN_TOKEN 从 ECS /opt/learning-cloud/.env 读取：2c87898e…）→ 公网验证 latest.yml=0.1.6 + api/version=0.1.6 ✅ → output 改回 dist 提交 push → tag v0.1.6 push（Actions 出 linux/mac）。
- **⚠️ 坑**：① oss2 sign_url 生成 %2F 编码 URL 403（AK 只写权限）；② 云助手 RunCommand 里中文/空格文件名必须**单引号包 -o 路径 + URL percent-encode**（curl 对中文 URL 报 malformed）；③ Git Bash /tmp 与 Windows python 路径不通，临时文件放项目下。
- **待办**：Actions 完成后下载 dist-ubuntu-016/dist-mac-016 归档。

## PACKAGING.md 打包发布手册 + 0.1.6 发布后状态
- 新建 `PACKAGING.md`（仓库根）：完整打包发布操作手册——版本升级/output 目录、unset NODE_OPTIONS 打包、OSS 公共直链上传、云助手 RunCommand 覆盖 ECS、ADMIN_TOKEN 现取、公网验证、双远端 push + tag、Actions linux/mac、坑速查（签名 403/中文文件名/Defender/safe-delete）。以后每次打包按文档操作。
- .gitignore 通用化：dist-release-0*/dist-ubuntu-0*/dist-mac-0*（替代逐个 0XX）。
- 0.1.6 发布状态：latest.yml=0.1.6、api/version=0.1.6 ✅；tag v0.1.6 已推 gitee+github；**Actions linux/mac 构建 in_progress**（v0.1.5 时 linux 3m32s/mac 2m13s），完成后下载 dist-ubuntu-016/dist-mac-016 归档。
- ⚠️ 之前漏推 github remote（Actions 在 GitHub，gitee 不会触发）——已补推。
