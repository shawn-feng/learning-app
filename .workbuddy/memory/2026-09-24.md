# 2026-09-24 工作日志

## 排查：发音测评「音频文件不存在」（用户报告：家长端测试报错 + 09-24 背诵考核没有发音评分）

- **现象**：家长端 设置→发音评测→测试 报「发音评测失败：音频文件不存在」；当天两场背诵考核的口语题全部没有发音评分。
- **定位**：`routes/assessment.ts` 的 `readAudioBytes()` 为评测私有复制了一份路径解析，**只查旧根 `data/files/<pid>/`**——ISSUE-131 P2 归并（随 0.5.5 于 09-23 09:05 上 201）把上传落盘切到 `workspaces/<pid>[/cid]/uploads` 后，files 路由/fs 路由/upload-ref 都改了双根兜底，唯独这条私有读取点漏切 → 凡「上传录音→评测」100% 404。
- **201 只读诊断**（`tmp/deploy/diag_audio_0924*.sh/.py`，全程只读）：files 表当日最新 8 条全部「旧根=false 新根=true」（含 `assessment-test.webm` 两次 + 两孩子 `recite-q*.wav`/`voice-q*.webm`）；旧根 09-24 新增=0（最后一批 09-23 07:47，恰在 0.5.5 部署前）；journalctl 里 07:44:40 三连发 assess 后几十毫秒即提交 attempts = 瞬时失败，且**服务端零日志留痕**（ApiError 被 catch 转 502 只回客户端）。09-23 07:47 的早读考核还能正常评测（旧根时期），时间线吻合。
- **影响面**：口语/背诵题无发音评分、无 ASR 文本；录音本体与 `audio_file_id` 都在（「听原音」不受影响）。

## 修复 + 回归（ISSUE-143，未部署 201）

- `readAudioBytes()` 改用 `files.ts` 已导出的 `resolveStoredFileAbs()`（SELECT 补 `child_id`），删私有拼接；评测失败 catch 里补 `req.log.warn` 留痕。客户端零改动。
- 新增 `test/issue143-assessment-audio-path.test.ts` 6 用例（真 fastify + 真 sqlite + 真 JWT）：新根孩子区/新根家长区/旧根存量兜底/有记录无文件/未知 id/缺 token。判定口径＝配置未启用时报「发音评测未启用」证明文件已取到。
- **防假绿**：旧逻辑注入复跑 → ①②③全红，还原复绿。
- **验证**：server tsc 0 错；相邻回归 issue131-p2-merge + issue135-exam-routes + issue143 共 23 用例全绿；`node scripts/build.mjs` 通过（bundle 含修复，版本仍 0.5.8）。
- 文档：ISSUE-143 详情 + ISSUES.md 索引行。

## 部署 0.5.9 → 201（11:16，用户「部署吧」+「先提交本地 git」）

- **先提交（用户要求防丢）**：`195f7ab`（ISSUE-143 修复 + 版本 0.5.9）/`11c8794`（ISSUE-142 + ISSUE-144 家长场景 skill P0~P4+P7，含并行会话的全部工作）/`87eb421`（根目录文档迁 docs/ R100 重命名 + ISSUE-136~144 归档；docs/ 下 aliyun/tencent/douyin/ssapi 密钥 txt 已被 .gitignore 覆盖未入库，逐一 check-ignore 核验过）。
- **插曲（重要）**：提交前发现 `parent-registry.ts` 在几分钟内被反复改写（一次构建曾报「未终结字符串」）——**不是磁盘损坏，是有另一个会话在同一工作树并行做 ISSUE-144**，撞上了它写到一半的文件。之后核对：并行会话自述 P0~P4+P7 已完成（49/49 单测 + tsc 0 错），提交时 142/144/143/131/135 六套件 86 用例全绿。
- **流程**：`tmp/deploy/deploy_server_059.py` + `deploy_059.sh`——停服 → 备份 `server.cjs.bak-20260924-1116` + `data/backups/deploy-0.5.9-20260924-1116/` → 换包（24,621,257 字节；标记 ver=1/resolve=4/142=9/144=2）→ daemon-reload + 起服。version=0.5.9、health ok、ERR_COUNT=0。
- **端到端验证**：① `exam/attempts` 两孩子 200（读面回归）；② ⭐**真录音回放**：今早 07:51 失败考核的 3 条真实录音（voice-q2/q3/q4，按 `audio_file_id` 反查明细拿真实 ref_text）调 `POST /assessment/assess` → **全部 200 返回真实评分**（pron=17/32/42），修复闭环。脚本 `verify_059_api.js`/`verify_059_r3.js`。
- **坑**：考核明细/files 表 `created_at` 是 **UTC** ISO——本地 09-24 07:44 存成 `2026-09-23T23:44Z`，按本地日期 `LIKE '2026-09-24%'` 整批漏行（验证脚本两轮踩中后才改对）。
- **文档**：MEMORY.md 环境备忘改「当前已部署 0.5.9」、ISSUE-143 详情/索引行改已部署。

## 清理 201 一周前的部署备份（用户指令）

- **口径**：按文件名内嵌日期（mtime 被 cp -a 保留不可靠）判 < 2026-09-17。删除 **bundle bak 19 个（304M，09-03~09-16）+ deploy 数据快照 4 个（0.4.1~0.4.4，150M）**；保留 09-17 及以后（含 0.5.6 的 DROP 前 exam_attempts 快照）。脚本 `tmp/deploy/prune_backups_0924.sh`（非标准名一律跳过，只碰两类前缀）。
- **未动**（非「部署备份」，可另行确认后删）：09-11 操作前快照×4、lunyu-sync-20260920、08-30 孩子数据备份、yunlvcao 原始 mp4 8.4M、`learning-server.pkg030` 83M（pkg 已弃用）。
- **效果**：data/backups 814M→670M；磁盘 4.7G→5.1G 可用（96%）。仍偏紧，大头在 learning-server 之外。

## 待办 / 待用户拍板

- 可选：09-23 09:05 之后两场考核的录音可对 `audio_file_id` 重评补写 `speech_assessments`（assess 路由不落库，需一次性脚本）。
- 201 磁盘清理（09-24）：一周前部署备份已删（约 454M）；仍 96% 只剩 5.1G，进一步清理（09-17 后快照等）需逐项确认。
- 本地 3 个提交**只提交未推送**（github/gitee 待用户说推再推）。
