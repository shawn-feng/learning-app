# 2026-09-01 工作日志

## 学习考核功能实施完成 ✅（06:30-06:50，EXAM-REQUIREMENTS.md §12 全量落地）
- **盘点**：工作区已有未提交实现（electron/lib/exam.ts + exam-engine.ts、server/src/routes/exam.ts + db.ts exam_attempts 表、src/components/ExamView.tsx + ExamRecords.tsx、src/lib/exam-template.ts、ipc/preload/custom-tools/parent-library/kb-sqlite/TopicDetail/ChildDetailPage/Learn 改动）——数据层迁移（parent v4: assess_method/assess_rubric；kb v7: exam_mastery）、服务端 4 路由、孩子端锁定考核视图、家长端编辑入口+记录表、agent 工具 parent_content 支持 assessRubric 全齐。
- **本次补的缺口**：
  - `getExamPending`（周期到点待考核，exam.ts 已实现）**未接 UI** → 新增 `exam:pending` IPC（ipc-handlers.ts）+ preload `examPending` + Learn.tsx 考核按钮**红色角标**（科目数，点开清零，失败静默）。
  - 新增 `test/exam.test.ts`：parsePeriodDays（每天1/每周7/每月30/每N天/每N周/兜底7）+ extractJson（围栏剥除/截大括号/无JSON→null）共 8 例。
- **验证**：主 tsc 仅 5 条已知环境告警（0 业务错误）；server tsc 0 错；vitest **33 文件/302 例全绿**；electron-vite build 通过（2105 modules）；服务端冒烟 `server/scripts/verify-exam-smoke.mjs` **10/10 通过**（config 下发/提交/列表/每课聚合/跨家长403）。

## ⚠️ 服务端冒烟跑法（踩坑记录，供复用）
- 冒烟脚本硬编码 BASE=http://127.0.0.1:8899 + DATA_DIR=/tmp/exam-smoke-data（node 解析为 **C:\tmp\exam-smoke-data**，Git Bash mkdir 用 `/c/tmp/exam-smoke-data` 对齐）。
- 起临时实例：`SERVER_DATA_DIR="C:/tmp/exam-smoke-data" node dist/server.cjs`（config.ts 的 dataDir = env SERVER_DATA_DIR ?? cwd/data；**必须用 run_in_background 起**，`&` 起的进程随回合结束被清，导致 ECONNREFUSED）。
- 配置需先写入 `C:/tmp/exam-smoke-data/server-config.json`（port 8899 + jwtSecret + dataDir），node 写（JSON.stringify）保证格式。
- 杀进程：`taskkill /PID <netstat 查到的 PID> /F`；每次跑前**清库重跑**（前次数据残留会让断言 count===1 失败——曾因另一路并发后台任务污染库误判为 bug）。
- ⚠️ 8899/8788 可能已有遗留实例（别的会话起的），先 netstat 确认归属再 kill。

## 21:09 记录 ISSUE-030（学习资料显示字号可调）
- 孩子左侧边栏加「资料字号」按钮（复用 ISSUE-023 聊天字号范式：FONT_OPTIONS/showFont/弹框/CSS 变量/按 childId 持久化）。
- 三处字号表面：① 资料列表（.material-list*/.material-row* 纯 CSS）② markdown 正文（.markdown-body 需作用域限定）③ HTML 资料 iframe（opaque origin，须经 ISSUE-017 的 injectBridge 注入通道下 font-size）。
- 入口代码点：Learn.tsx:28-35/263/275/278/290/896-902/995-1004/1160-1182；MaterialsPanel.tsx:95/387/401-430；styles.css:1052/1058/1105/1114。优先级待定，未实施。

## 学习考核两处修复（06:49，用户 continue 后）
- **① 判分缺 rubric 传递（关键）**：`ExamAnswerIn` 原无 rubric 字段，服务端 SCORING_PROMPT 声明"给你每题考核要点"但 LLM 实际看不到家长写的要点，判分锚定落空。修复：`exam-engine.ts` ExamAnswerIn 加 `rubric`；answersLines 拼入"考核要点(rubric)：xxx"；`ExamView.handleSubmit` 从 `examTopic.courses` 按 course 匹配 assessRubric 传入。
- **② `getChildDir("default")` 硬编码**：`generateExamQuestions`/`scoreExamAttempt` 加 `childId` 参数（`getChildDir(childId||"default")`）；`exam:generate`/`exam:score` IPC 与 preload 签名加 childId；ExamView 两处调用传 childId。
- 验证：tsc 0 业务错、vitest 33/302 全绿、build 通过。
# 2026-09-01

## ISSUE-026 完成（07:08，孩子端边栏常驻折叠 + 交互弹框化）
- `Learn.tsx`：删 `sidebarCollapsed` state / `sidebar-toggle` 展开按钮 / view-switcher popover（viewMenuOpen/ref/timer/click-outside 全删）；`learn-sidebar` 恒 `collapsed`（64px 图标栏）。
- 5 类交互 icon→弹框：切换展示页（showView，点选项 setView）、模型（showModel）、朗读语速（showRate，rate-grid）、聊天字号（showFont，FONT_OPTIONS）、今日课程（showClass，SidebarClassSchedule 全量）。
- ⚠️ 模型弹框**常驻挂载**（overlay display:flex/none 控制，ModelSelector 不卸载）——ModelSelector 初始化 effect 依赖 []，卸载重挂会重新拉模型并 switchTo 切回默认（原边栏注释已警示）。
- `SidebarClassSchedule` 去掉 collapsed/onExpand props 恒全量渲染；CSS 新增 `.sidebar-actions`（图标竖排容器）与 `.view-options-modal`。
- tsc 0 业务错误、build 通过（2105 modules）。ISSUES.md 已标完成。

## 发音评测服务接入完成 ✅（10:0x-10:2x，ENGLISH-AGENT-REQUIREMENTS §10 任务1）
- **新增 `electron/lib/assessment/`**（仿 voice 模块）：`types.ts`（AssessmentResult 统一结构：score/accuracy/fluency/completeness/words[phones]）、`assessment-config.ts`（enabled/provider/providers，shared/assessment-config.json，打码/补丁复用 voice-config.maskSecret）、`providers/tencent-soe.ts`、`providers/aliyun-kid.ts`、`index.ts`（assessAudio 入口）。
- **智聆新版（完整实现，协议已确认）**：`wss://soe.cloud.tencent.com/soe/api/<AppID>?参数&signature`；签名原文=`soe.cloud.tencent.com/soe/api/<AppID>?`+除 signature 外**全部参数按 key 字典序**拼 `k=v&`（不 urlencode），**SecretKey HmacSha1→base64**；参数 eval_mode=1(句子)/rec_mode=1/score_coeff=1.0(儿童最低苛刻度)/voice_format=1(wav)/server_engine_type=16k_en；发送=wav 分片 1280B/40ms(1:1 实时率防引擎报错)+文本帧 `{"type":"end"}`；返回 `{code,result:"{...}",final:1}`，result 为 JSON 字符串（SuggestedScore/PronAccuracy/PronFluency/PronCompletion/Words[PhoneInfo]）。
- **阿里儿童 en.word_kid.score（实验性，协议基于 DEMO 逆向还原）**：鉴权 `POST https://api.cloud.ssapi.cn:8080/auth/authorize`（form：appid/timestamp/user_id/user_client_ip/request_sign；request_sign=**MD5("app_secret=&appid=&timestamp=&user_client_ip=&user_id=" 字典序拼接)**）→ 返回 warrant_id（缓存，2h 有效）→ `wss://api.cloud.ssapi.cn` 发 connect 包（param.app{timestamp,applicationId,sig=MD5(appSecret+timestamp)} + param.sdk{version,type,source:7,protocol:1,os,os_version,arch,product}）→ start 包（param.request{coreType,refText,rank:100}+param.audio{sampleRate:16000,channel:1,sampleBytes:2,audioType:"wav"}+param.app{userId}）→ 分片音频 → `{"cmd":"stop"}` → 收 `{request_id,eof,params,refText,result{overall,details[{char,score,phone[{char,score,start,end}]}]}}`。⚠️ 技术方=声希科技，connect/start 细节与 wss path 依据 `libssound.so` strings + 官方文档还原，**首次实测报错按 errId/error 微调常量**（aliyun-kid.ts 头部注释）。
- **IPC/preload/UI**：`assessment:config:get/set`、`assessment:test`(录音→评测固定 "hello")；preload `assessmentConfigGet/Set/Test`；Settings.tsx 新 tab「发音评测」+ `src/components/AssessmentSettings.tsx`（仿 VoiceSettings：启用开关+两 provider chips+字段+保存/默认/测试）。
- **验证**：tsc 0 业务错；vitest 全量 **34 文件/311 例全绿**（新增 test/assessment.test.ts 9 例：配置打码/补丁/智聆签名自洽+字典序/双解析映射）；build 通过。
- 说明：英语角链路（录音→ASR 与评测并行、refText 自评分注入 agent）属下一任务，本轮仅服务接入+配置+测试；阿里端需真实 appKey/appSecret 实测确认协议。

## asrcheck 珊珊昨日(2026-08-31)语音文件时长统计（10:25）
- 目录 `asrcheck/shanshan asr/`：101 个文件（60 webm + 41 wav），文件名=Date.now()毫秒，全部 2026/8/31 本地 CST。
- **总时长 2804.35s（46分44秒）**：webm 段 730.45s（12.2分）/ merged.wav 2073.90s（34.6分）。平均单文件 27.77s；最长 245.76s(merged.wav)，最短 1.08s(webm)。
- 时段分布：08-10 点最密（26.6分），14 点 8.7分，其余零星；18:58 最后一条。
- **这是上一轮查不到的「第二个孩子」真实录音**——珊珊实际音频 2804s。⚠️**用户纠正：merged.wav 也是送过 ASR 的**（合并后原始段删除，wav 是留存下来的已识别音频），所以 ASR 真实输入 = webm+wav 全部 = 2804s（不是之前误算的仅 webm 730s）。珊珊 2804s + 闻闻 866s = 3670s，已能解释上报 4000s 的主体 → **推翻上一轮「可能重复转写」的猜测，4000s 基本是真实语音量**。
- ⚠️ **修正上一轮 c2187ac9「≈0s」为假结果**：本地 Windows ffmpeg.exe 不认 `/c/...` POSIX 路径（报错 No such file 但 exit 0、duration 解析为空→0）。必须用 `cygpath -w` 转 `C:\...` 才能读。201 Ubuntu 上是真 ffmpeg 无此问题。
- 清单导出 `asrcheck_durations_2026-08-31.csv`（record_time/size_bytes/ext/duration_sec/filename）。

## 语音临时文件泄漏修复（10:38，用户选「只修临时文件泄漏」）
- `electron/lib/voice/audio.ts` `webmToWav16k` 的 `cleanup()` 原只删 `tmpOut`(wav)，不删 `tmpIn`(100行写入原始 webm) → 每次成功转录在 `os.tmpdir()` 留孤儿 webm。
- 修复：`cleanup()` 内补 `fs.unlinkSync(tmpIn)`（成功/cleanup 路径删）。错误路径(120-134)按原设计**有意保留 tmpIn** 便于复现，未动。零风险 1 行改动。
- 未做：fallback 硬化（index.ts:52-68）。原因：4000s 已确认是真实语音量（非重复计费），且当前 reachable 客户端（201 闻闻/本机PC）均单服务商，fallback 不触发 → 降为前瞻防御项，用户选择暂不动。

- ✅ 上述 audio.ts 修复已提交 git：`1b2d591 fix(voice): 转录成功后清理临时输入 webm，避免 os.tmpdir 残留`（仅 audio.ts，4 insertions）。其余今日改动（exam/assessment/ISSUE-026/memory）按用户选择**未提交**。asrcheck/、.known_hosts、CSV 分析产物、linux-016.zip、scripts-decode-token.mts 一律未纳入（隐私/产物/可能含密钥）。

## 学习考核记入 ISSUES.md（10:58，用户要求"方便后续查找"）
- 追加 **ISSUE-027**（`.workbuddy/memory/ISSUES.md` 末尾，编号顺延自 ISSUE-026）：完整索引条目——架构约定（存储服务端/计算客户端/判分prompt服务端下发/HTML模板+锁定/严格一次性）、数据模型（parent v4 assess_method+assess_rubric、kb v7 exam_mastery、server exam_attempts）、关键文件清单（exam.ts/exam-engine.ts/ipc/preload/exam.ts路由/ExamView/ExamRecords/TopicDetail/custom-tools/test/冒烟）、验证结果、已知注意点（判分必须带 rubric、childId 隔离、冒烟跑法）、优先级=已完成。

## 英语学习模块记入 ISSUES.md（11:0x，用户要求"单独 issue 便于查询"）
- 追加 **ISSUE-029**（英语学习模块/英语角总索引）：需求描述 + 4 已确认分叉（显式入口/课程提取+家长补充词表/自由对话全评/上云可回顾）+ 文档引用（ENGLISH-AGENT-REQUIREMENTS.md / RESEARCH-pronunciation-assessment-2026-08-31.md）+ ✅ 已完成任务1（assessment 模块两 provider 要点+文件清单+测试 9 例）+ ⏳ 待办任务 2-7（会话骨架/prompt、前端入口+路由、语音评测链路、词表、session-sync 递归、daily 增强）+ ⚠️ 注意点（daily 递归零改动、session-sync 需改递归、阿里端未实测）+ 优先级=实施中。


## lunyu_exam 真实数据测试学习考核（13:00-13:45 ✅，用户决策：选择题当主观题素材 / md+json 合并 / 本机 8788 直接测）
- **数据**：lunyu_exam/ 489 章（20 篇）×（考核知识点.md + 考核内容.json[3套可选题+1套必考题,含选择题+问答题+LLM评分标准表] + 69 raw.txt）。
- **合并脚本** `scripts/merge-lunyu-exam.mjs`：每章 md+json → `lunyu_exam/merged/<章节>.md`（知识点+题目+评分标准）。⚠️ json 是 LLM 原始输出质量参差（71 章直接 JSON.parse 失败：未转义英文引号嵌套/```json围栏/"好的，作为..."前缀/截断）→ 容错解析：extractJsonText（剥围栏+截首尾{}）→ sanitizeJsonText（状态机：字符串内未转义 " → 中文引号“”交替、裸换行/制表符→空格）→ 失败试 raw.txt → 仍失败降级只知识点。最终 461 章完整 + 28 章降级。
- **导入脚本** `scripts/import-lunyu-exam.mjs`：经 8788 `/api/v1/db/exec` parent_lib.courses.upsert 写 assess_rubric（合并 md 全文，幂等，489/489）+ topics.assess_method（周期每天/最近3课各1题/按考核内容逐点给分）+ kb.courses.upsert 标记已学（闻闻 09406c05，title 与家长库一致）。支持参数过滤（`为政篇第一章`）先测后全量；`--learn` 标记已学。
- **本机服务端升级**：8788 原 v0.2.0（无 exam）→ 停 PID → `node dist/server.cjs`（v0.3.0，build.mjs 重编）→ features 含 exam；生产家长库 86a84278 经 openParentLib 自动补 assess_method/assess_rubric 列（首次 exam/config 调用触发）；server.sqlite exam_attempts 建表（启动时）。
- **链路验证（真实 LLM，mimo/mimo-v2.5）**：exam/config 下发 191 门已学课 + 为政篇第一章 rubric 6078 字 ✓ → 出卷 agent 按 rubric 出生活场景口述题（锚定"道理应用"）✓ → 判分（答对 4/10 / 答错 0/10，评语带 rubric 锚定）✓ → 提交写库 + course-records 聚合（难点/计划复习 2026-09-02）+ 孩子库 exam_mastery='薄弱' 回写 ✓。
- **🐛 修复真实 bug**：`exam-engine.ts` 的 createAgentSession 返回值**必须解构 `{ session }`**（daily-summary 同款）；原来直接当 session 用 → `session.dispose is not a function`，**主进程 exam:generate/exam:score 都会崩**。已修两处。
- **⚠️ vitest 跑真实 LLM 的坑（复用）**：vitest.config 强制 `PI_TEST_DATA_DIR`=Temp\pi-test-data 隔离 → ① app-settings 真源在**家长分区** `parents/<pid>/app-settings.json`（不是 data/ 根！getAppSettingsPath）；② 需把真实 `auth.json`+`app-settings.json` 拷贝到 `Temp\pi-test-data/parents/<pid>/` 再 `setCurrentParentId(pid)`；③ runtime 有全局缓存 `__learningAppModelRuntime`（不区分家长），换家长后必须 `delete globalThis[...]` 重建，否则复用 _guest 空 auth → 默认模型回退 qwen-tokenplan 报 "No API key"（mimo-v2.5 getModel 其实是成功的）。
- 临时 e2e 测试已删（`test/exam-e2e.tmp.test.ts`，含真实 LLM 调用不能进全量）；验证脚本留存参考：`scripts/verify-exam-engine.mts`（tsx 跑不通 SDK 包解析，仅参考）。

## 🐛 修复 ws 包 bufferutil 解析错误（14:4x，智聆/阿里评测连不上）
- **现象**：家长配置智聆 aksk 后点「测试评测」→ `评测失败: Could not resolve "bufferutil" imported by "ws". Is it installed?`。
- **根因**：assessment provider 动态 `import("ws")`，electron-vite/esbuild 打包 ws 时对其可选依赖 `bufferutil`/`utf-8-validate`（ws/lib/buffer-util.js:117、validation.js:144 的 try-require）静态解析失败。
- **修复（两 provider 同改）**：**弃用 ws 包，改用 Electron/Node 22+ 内置全局 WebSocket（undici）**——`new WebSocket(url)` + `ws.binaryType="arraybuffer"` + `addEventListener("open"/"message"/"error"/"close")`；message 的 `ev.data` 按 arraybuffer 转 Buffer.toString；send 直接传 Buffer（Uint8Array）✓；error 事件无 message 字段 → 记 connError 标志，close 时统一报「连接失败（请检查网络/密钥）」。文件：`electron/lib/assessment/providers/tencent-soe.ts`、`aliyun-kid.ts`。
- **⚠️ 长期约定**：Electron 主进程**不要用 ws 包**做 WebSocket 客户端——electron-vite 打包 ws 必踩 bufferutil/utf-8-validate 可选依赖解析错误；直接用全局 WebSocket（Node 22+ undici，@types/node≥22 有类型，实测 tsc 0 错）。

## 🐛 智聆 rec_mode=1 只能发一个数据包（15:05 修复）
- **现象**：智聆测试评测报 `智聆返回错误(4015): 录音识别模式下，只能发送一个数据包`。
- **根因**：`tencent-soe.ts` 按流式思维把 wav 分片（1280B/40ms）发送，但 URL 参数 `rec_mode=1`（录音评测模式）**只允许整个音频一次性发送**（一个二进制帧）；分片只适用于 `rec_mode=0`（流式实时模式，才需 1:1 实时率）。
- **修复**：open 后 `ws.send(wav)` 整包一次 + `ws.send({type:"end"})`。验证：tsc 0 错、9 测试全绿、build 通过。注意阿里 kid 仍为分片发送（声希协议流式 feed），若实测同样报单包限制再改。

## 🐛 智聆 4107 音频对齐错误（15:10 修复）
- **现象**：智聆测试评测报 `智聆返回错误(4107): 输入音频数据指针或音频长度必须为偶数`（=InvalidParameterValue.AudioSizeMustBeEven）。
- **根因/官方建议**：wav 头解析不可靠 → 官方 FAQ 建议「对音频格式进行转换」；智聆支持 pcm/wav/mp3/speex，pcm 为 16k/16bit/单声道裸数据（长度必为偶数）。
- **修复（tencent-soe.ts）**：`import { extractWavPcm } from "../../voice/audio"`（现成函数，校验 16k/单声道/16bit PCM 并抽 data 区）；`voice_format: 1→0`（pcm）；open 后 `ws.send(pcm)` 整包 + end 帧；wav 非标时抛「智聆音频格式异常：…」。测试补 `voice_format=0` 断言。验证：tsc 0 错、9 测试全绿、build 通过。

## 🐛 智聆「非 WAV 文件」+ result 对象解析（15:16-15:18 修复并自测通过 ✅）
- **现象①**：`智聆音频格式异常：非 WAV 文件`——测试评测传的是**录音原始 webm/opus**，`assessAudio` 直接当 wav 解析。
- **修复①（assessment/index.ts）**：`assessAudio` 入口统一 `webmToWav16k(audio)` 转 16k 单声道 wav 后再调 provider（复用 voice/audio.ts，与转写链路一致）；转换失败报「音频转 16k wav 失败（录音太短或格式异常）」。
- **现象②（自测暴露）**：真实连接后 `解析智聆结果失败: "[object Object]" is not valid JSON`——**录音模式（rec_mode=1）最终 result 是已解析的对象**，不是流式的 JSON 字符串。
- **修复②（tencent-soe.ts）**：`const r = typeof msg.result === "string" ? JSON.parse(msg.result) : msg.result`。
- **✅ 真实链路自测通过**（临时脚本 scripts/verify-soe.mts，用家长配置的真实智聆密钥 + data/ 真实 webm，测后即删）：webm→wav 36558B→pcm 36480B（1.14s）→ assess **耗时 578ms** 返回 `score=37 accuracy=37 words=[{"word":"*","score":-1,"dpType":1},{"word":"hello_0","score":37}]`——低分因样本是中文日常语音非读 hello，**链路（签名/鉴权/连接/发音频/收结果/解析）全通**。
- 验证：tsc 0 错、9 测试全绿、build 通过。⚠️ ffmpeg-static 本地 Windows 不可用（fallback 系统 PATH ffmpeg 成功，已知）。

## lunyu_exam 测试补充（14:00，本会话独有两处）
- **① 出卷 prompt 微调**（`electron/lib/exam-engine.ts`）：要求 agent **优先直接采用 rubric「考核内容」里的现成题目**（含选择题），选择题**去掉 A/B/C/D 选项改造为口述题**（保留题干，10 分制）；不再让 agent 自由新出题。实测出卷按 rubric 选题正确（q2=思无邪、q3=道之以德）。
- **② 🐛 planReviewAt 日期 bug 修复**（本会话发现）：判分 prompt 未告知模型今天日期 → LLM 瞎猜产出 **2025-03-24** 错误年份。修复：`server/src/routes/exam.ts` SCORING_PROMPT 加 `{{TODAY}}` 占位 + `buildScoringPrompt()` 下发时替换为服务器当天日期；config 接口改用 buildScoringPrompt()。重建 server.cjs + 重启 8788 后复测 planReviewAt=2026-09-08/09-03（正确）。⚠️ 判分 prompt 类模板凡涉及"按今天推算日期"必须注入具体日期，勿再漏。
- 复验：主 tsc 0 业务错、vitest 311 全绿、build 通过。8788 当前运行含日期修复的 v0.3.0（后台任务 678Itd）。

## lunyu_exam 测试补充 ②（14:10，出卷输入裁剪）
- **③ 🐛 出卷 prompt 全量 rubric 撑爆上下文**：lunyu_exam 导入后 exam/config 下发 **191 课 × 89 万字符 ≈ 44 万 token**，出卷全量拼 prompt 必炸。修复：`exam-engine.ts` `generateExamQuestions` 内部按 lastReview（无则 firstLearned）倒序**裁到最近 8 课**作为候选（MAX_CANDIDATE_COURSES=8），prompt 注明"只能从列出课程里选"。实测：传全量 191 课 → 出卷 3 题（为政篇第一章必考题 + 泰伯篇四/五章），选题正确、不炸（98s）。
- 复验：主 tsc 0 业务错、vitest 311 全绿、build 通过。临时验证文件已删。

## 教学文案丢失修复（16:30-16:50，用户反馈"家长页课程管理论语章节教学文案大量缺失"）
- **根因**：`scripts/import-lunyu-exam.mjs` 用 `parent_lib.courses.upsert`（**全字段覆盖**）写 assess_rubric，未传的 teaching_copy/html_path/status 等被覆盖成空 → 本机 8788 库 489 章教学文案全丢（201 生产库 512 章 teaching_copy/html_path 完整，未污染——201 服务端还是旧版无 exam）。
- **修复**：SSH 201 导出家长库 + 闻闻孩子库 lunyu 全字段（base64 传回）→ `scripts/restore-lunyu-teaching.mjs`（家长库按 title UPDATE 除 assess_rubric 外全字段，512/512）+ inline 恢复孩子库（保留 exam_mastery）。恢复后：家长库 512 行 teaching_copy/html_path 全非空 + rubric 489；孩子库 html_path 512/已学✅195（与 201 对齐）。
- **去重**：lunyu_exam 文件名"论语乡党篇 第十三章"（带空格）与 201 库"论语乡党篇第十三章"重复 → rubric 并入标准行后删带空格行，lunyu 收敛 512 行。
- **防再犯**：import 脚本加 `upsertPreserving()`——upsert 前先 `/db/query`（注意：**读走 /db/query、写走 /db/exec**，走错端点报"未知执行操作"）`courses.list` 拿原字段回填，只更新 assess_rubric/sort_order；孩子库同样 preserve html_path 等。重跑验证 teaching_copy 1380 字保留 ✓。
- 数据源保留 `scripts/restore-data/lunyu-courses-restore.json`（201 导出 512 行）；临时 b64/201 临时文件已清。⚠️ 教训：**凡是全字段 upsert 的写入，调用方必须先 list 回填原值**。

## 课程管理显示考核方法（16:37，用户反馈"各主题考核方法字段没出现在课程管理里"）
- 根因：`CourseManager.tsx` 主题卡片只显示 方法(method)/课程数/html/规则，**未渲染 assess_method**；`ParentTopic` 类型也未声明 `assessMethod`（数据链其实已通：服务端 `parent_lib.topics.list` 返回 assess_method → `listParentTopics` 映射 assessMethod）。
- 修复：`ParentTopic` 加 `assessMethod?`；卡片统计行加「· 考核已填写/未填写」；新增「考核要点」卡片按钮（ClipboardCheck）→ `setDetail({topic, tab:"assess"})` 直达 TopicDetail 的考核 tab（每科目考核方法说明 + 每课考核要点编辑器）；`Tab` 类型补 "assess"。
- 验证：tsc 0 业务错、build 通过。效果：课程管理卡片能看到每主题考核方法是否填写 + 一键进入编辑。

## 论语教学方法丢失修复（16:44，用户反馈"课程管理里论语的教学方法没有了"）
- 根因：import-lunyu-exam.mjs 的 `parent_lib.topics.upsert` 传了 `method: ""`（全字段覆盖）→ 把 lunyu 主题的**教学方法全文（topics.method，201 上 2123 字）清空**（其它主题未动：english 1230 / feizhougu 1653 / hanzigong 1145 等均在）。
- 修复：SSH 201 导出 lunyu topic 的 method/progress/rules_json（base64）→ 本机 UPDATE 恢复（method 2123 字 ✓，assess_method 108 保留 ✓）。
- 防再犯：import 脚本 topics.upsert 改为先 `parent_lib.topics.list` 回填 method/progress/rules_json，只更新 assess_method。重跑验证 method 2123 保留 ✓。
- ⚠️ 教训补充：**parent_lib.topics.upsert / courses.upsert 都是全字段覆盖**，凡调用方只更新部分字段，必须先 list 回填其余字段。

## 课程管理"考核方法"独立标签（16:55，用户要求：考核方法标签放教学方法右边，不要藏在考核要点里）
- 改造：`TopicDetail.tsx` 把原「考核要点」(assess) tab **拆成两个独立 tab**——「考核方法」(assessMethod，只含每科目 topics.assess_method 说明编辑器) + 「考核要点」(assess，只含每课 courses.assess_rubric 编辑器)；顶部 tab 顺序 = 基本信息/学习资料/教学方法/**考核方法**/考核要点/课程详情（考核方法紧跟教学方法右）。
- `CourseManager.tsx` 卡片按钮顺序改为：教学方法 | **考核方法** | 课程详情 | 学习资料管理（考核方法在教学方法右边，ClipboardCheck，tab="assessMethod"）；统计行保留「· 考核 已填写/未填写」。
- 验证：tsc 0 业务错、build 通过（中途 JSX 括号拆块时错位，补闭合后过）。

## 考核设计演进 v2：固定+自定义考核（19:06，EXAM-REQUIREMENTS.md 新增 §14）
- 用户定稿 4 点：①固定考核多档并存、同日多档重叠**只考周期最长档**（周五含每天+每周→考每周的）；②自定义考核 agent **信息不全先向家长确认**再生成排期；③固定考核候选=**周期窗口内学习(first_learned)或复习(last_review)过的课程**；④**按每个主题的教学方法把一课考完整**（选题单位=课，每课完整出题，不设全局 8 题上限；每轮选 N 门课，N 默认 3~5 可配）。
- 架构：新增 `exam_schedules` 排期表（kind 固定|自定义 / scheduled_at / scope / status / attempt_id）；固定排期按频率档（每天/每周/每月/半年/年末）滚动生成+去重；自定义排期由家长对话 agent 生成；孩子侧左侧改显示**考核时间点列表**（时间+知识点，点「开始这次考核」），取代「选科目」。
- 选课优先级（长周期控制量核心）：①复习计划到期 planReviewAt≤今天 ②掌握度薄弱 ③最久没考核（滚动覆盖）④最近新学。
- 取代：topics.assess_method 周期语义废弃（改全局频率配置）；exam/config 改按排期 scope；出卷候选 8 课硬裁→优先级取前 N 门课每课完整出题。保留：rubric/出卷判分引擎/锁定视图/记录报告。
- 文档已更新（§14，264 行）。**尚未实施**，实施拆分见 §14.8。

## 考核 v2（固定+自定义）实施完成（19:21-19:45，EXAM-REQUIREMENTS §14 全量落地）
- **服务端**（server/src/routes/exam.ts + db.ts）：
  - `exam_schedules` 表（kind fixed|custom / freq / scheduled_at / scope JSON / status / attempt_id）；
  - 固定频率配置存 settings `exam_fixed:<parentId>`（frequencies 多档 + courseCount N + time 时刻 + anchorAt），默认 weekly/3/20:00；
  - `ensureFixedSchedules` 懒生成：各档从 anchor 按周期步进、**同日多档去重取 rank 最大（周期最长）**（实测 daily+weekly → 60 条 = 52 daily + 8 weekly，无同日重复）；
  - 选课 `selectCoursesForSchedule`：候选=周期窗口内学习/复习的课（**三级放宽**：严格窗口 → 最近7天 → 全部有痕迹），4 因子打分（复习到期+3/薄弱+3·学习中+2·熟练-1/久未考每30天+1封顶2·从未考+2/新学7天内+1）取前 N；
  - 自定义排期 scope（topics/courses/note）按家长指定范围选课；
  - 接口：GET /exam/schedules/:childId（懒生成+列表）、POST /exam/schedules（自定义）、/schedules/:id/start、/schedules/:id/complete、GET+POST /exam/fixed-config；config 路由支持 `?schedule=<id>`（返回 schedule + courses + scoringPrompt）；提交 attempts 带 scheduleId → 排期 done+attempt_id。
- **出卷引擎**：每课完整出题（一课 2~4 题、覆盖考核内容全部知识点、**去掉全局 8 题上限**；本地裁剪放宽到 12 门仅保险）。
- **孩子端**：ExamView 改为**考核时间点列表**（时间+标题+自定义标签+可开始按钮），点开始 → scheduleStart → config?schedule → 出卷 → 锁定考核 → 提交关联排期；角标按排期到期（getExamPending 改用 schedules）。
- **家长端**：Settings 新增「学习考核」tab（ExamSettings.tsx：频率多选+N+时刻）；家长 agent 新工具 `exam_schedule_create`（信息不全 throw 向家长确认）。
- **验证**：排期去重 ✓、选课（3 门带 rubric）✓、自定义 scope（2 门）✓、config 按排期+日期注入 ✓、**每课完整出题 9 题覆盖 3/3 课**（真实 LLM 94s）✓、提交关联排期 done ✓；tsc 0 业务错、vitest 311 全绿、build 通过。
- ⚠️ 踩坑：vitest 跑真实 LLM 每次要先把真实 auth.json+app-settings.json 拷到 `$TEMP/pi-test-data/parents/<pid>/`（defaultModel=mimo/mimo-v2.5 在 app-settings，缺则兜底 qwen-tokenplan 无 key 崩）。
- 待办：verify-exam-smoke.mjs 还是旧版（未覆盖排期）；孩子端 UI 端到端实测需 app 操作。

## 考核 v2 收尾（19:45-20:0x）
- 服务端两处细节：固定排期锚点改用「配置时刻 time（默认 20:00），今天已过则明天」——排期不再落在随机时刻；自定义排期 scheduledAt 非法时间返回 400。
- `verify-exam-smoke.mjs` 补 7 项排期断言（fixed-config 保存/懒生成/多档去重/自定义创建/config 按排期选课/提交关联/排期 done），**冒烟 18/18 全通过**。

## 考核 v2 二轮收尾（20:1x-20:2x）
- **8788 切到最新构建**（此前 19:2x 起的还是锚点修复前版本，用户 app 连的是旧逻辑）。
- 🐛 **旧锚点排期按天去重挡住新时刻**：11:26 锚点生成的排期铺满未来 60 天，改成 20:00 后同日已存在排期被跳过 → 新时刻排期插不进去。修复：POST fixed-config 配置变化时**删除未来 pending 的 fixed 排期重建**；anchorAt 变化时置空（走「今天配置时刻，已过则明天」锚点）；手动清了一次本机库旧排期。验证：新排期落在 20:00（UTC 12:00）✓。
- 顺带：Learn.tsx 考核退出后**刷新待考核角标**（排期状态变化即时反映）。
- 验证：server tsc 0 错、主 tsc 0 业务错、build 通过。

## 考核 v2 三轮收尾（19:5x）
- **A. `exam_attempts.schedule_id` 回填**：db.ts 加幂等迁移（ALTER 加列，15 列）；提交路由 INSERT 写入 scheduleId；rowToAttempt 返回 scheduleId。验证：提交带 scheduleId → attempts 记录带 scheduleId ✓。
- **B. 家长端排期取消**：服务端 `DELETE /api/v1/exam/schedules/:id`（仅 pending 可删；fixed 删后懒生成自动补）；客户端 cancelExamSchedule + IPC/preload `exam:scheduleCancel`；ExamRecords 顶部新增「📅 考核安排」区块（排期列表 + pending 可取消，confirm 确认）。验证：建 custom → DELETE → 已删 ✓。
- 🐛 **排错记录**：INSERT 加 schedule_id 时 VALUES 占位符多写一个（15 列 16 值 → "16 values for 15 columns"，连续几轮误判为构建/迁移问题）。教训：**改 INSERT 列数后必须数 VALUES 占位符**；且 server.cjs 旧 mtime（19:52）一度掩盖了"构建未更新"——改 src 后确认 `ls dist/server.cjs` mtime 是新值（必要时 rm 强制重建）。
- 验证：server tsc 0、主 tsc 0 业务错、vitest 全绿、build 通过；8788 已跑最新构建（含全部修复）。

## 考核 v2 四轮收尾（20:0x）
- 🐛 修复**出卷失败卡死排期**：ExamView 开始考核时 scheduleStart 把排期标 started，若出卷/判分失败排期停在 started、列表不再显示开始按钮 → 无法重试。修复：列表对 `pending || started` 都显示「开始/可继续」；start 调用失败 catch 忽略不阻断。
- 冒烟脚本在最新构建（含 schedule_id 写入/回填）复跑 **18/18 全过**；tsc 0 业务错、build 通过。

## 考核 v2 文档收尾（20:0x）
- `EXAM-REQUIREMENTS.md` §14.8 标注「✅ 已完成（2026-09-01）」并回填实际落点（schedule_id 回填/取消/重试等）。
- `scripts/verify-exam-engine.mts` 参考脚本头部更新为 v2 链路说明（排期 → config?schedule → 每课完整出题）。
- `ISSUES.md` ISSUE-027 补 v2 演进段（固定+自定义/排期表/选课/每课完整/双端 UI）。

## ISSUE-030 实施：孩子端「资料字号」按钮（21:3x）
- 需求：左侧边栏加「资料字号」按钮，调整学习资料字体大小，按 childId 持久化，不波及聊天字号(ISSUE-023)/家长端。复用聊天字号弹框范式。
- 三处字号表面统一由 `--material-font` CSS 变量驱动（Learn 层状态 matFontSize，localStorage `chat:${childId}:matFontSize`，默认 16px，档位 MAT_FONT_OPTIONS=16/22/30/38）：
  ① 列表/标题：`.material-list-title`/`.material-list-count`/`.material-row-title`/`.material-row-time`/`.material-title` 改 `var(--material-font,<原px>)`；
  ② 正文 markdown：作用域限定 `.content-panel .markdown-body, .content-panel .markdown-body *`（不波及家长端/聊天）；
  ③ HTML 资料 iframe（opaque origin）：初始字号经 `injectBridge(html, matFontPx)` 前置 `window.__PI_MAT_FONT` 注入、加载即套用；运行期变化由 MaterialsPanel 在 matFontSize 变更时 `postMessage({type:"page:mat-font",px})` 下发，桥脚本注入 `<style>`（`html,body,body * { font-size: Npx !important }`）——**不改资料 html 本体**；用 font-size(非 zoom) 以免破坏查词浮层坐标。
- 改动文件：`Learn.tsx`(按钮 TextSelect + 状态/持久化/弹框 + 传 matFontSize)、`MaterialsPanel.tsx`(prop + --material-font + runtime postMessage)、`page-bridge.ts`(BRIDGE_SCRIPT applyMatFont + 消息监听 + injectBridge 入参)、`styles.css`(变量化 + 作用域 markdown 规则)。
- 验证：tsc 0 业务错（已过滤 5 条 TS2318/TS2552 环境告警）。待回归：markdown/HTML 资料字号随设置变化、刷新保留、不波及聊天字号。

## ISSUE-031 实施：查词浮层优化（22:2x）
- 需求：拼音放大 + 多音字分行各有朗读 + 去掉释义（仅 `MaterialsPanel.tsx` + `styles.css`）。
- `WordLookupOverlay`：删释义渲染；`en.pinyin.split(/\s+/).filter(Boolean)` 拆读音数组；每个读音一行（`.word-lookup-reading`：拼音 + 独立 🔊，`onSpeak(py)` 传该读音拼音串）；无拼音显示 `·`；高度按条目/读音数动态估算防溢出。
- CSS：`.word-lookup-item-word`/`.word-lookup-item-py` 字号改 `var(--material-font,22px)`（与 ISSUE-030 资料字号联动）；新增 `.word-lookup-readings`/`.word-lookup-reading`/`.word-lookup-py-none`；删 `.word-lookup-item-meaning`；朗读按钮改 `flex:0 0 auto`。
- 音频源决策：按 issue 建议先用「拼音串 TTS」，后续可加「载字最小词」优化自然度；整字朗读按钮已移除。
- 验证：tsc 0 业务错。待回归：单音字正常、多音字分行各有 🔊、无释义。

## ISSUE-031 二次优化：浮层加「朗读选中文本」按钮（22:5x）
- 需求：选中文本弹框后，增加一个播放**整段选中文本**的音频按钮（与逐音朗读区分）。
- `WordLookupOverlay` 头部新增 `.word-lookup-head-actions`(flex 容器) + `word-lookup-play-all` 按钮（`onSpeak(state.text)`，`state.text` 即整段选中文本）；`styles.css` 加 `.word-lookup-head-actions`/`.word-lookup-play-all`(紫色)。
- 验证：tsc 0 业务错。

## ISSUE-031 扩展到聊天框（23:0x）
- 把查词浮层抽成共享组件 `src/components/WordLookupOverlay.tsx`（`LookupState` / `WordLookupOverlay`(forwardRef) / `useWordLookup` 选区捕获 hook）；`MaterialsPanel.tsx` 删本地副本改引用；`ChatWindow.tsx` 接入：在 `messagesRef` 容器内捕获中文选区 → `lookupText` → 浮层，新增 `speakText`(任意文本 edge-tts) 作 onSpeak，浮层渲染于 ChatWindow 根。灰盒：仅中文触发、点击外部/Esc 关闭、整段朗读可用。
- 验证：tsc 0 业务错（已过滤 5 条环境告警）。

## 21:43 记录 ISSUE-031（查词浮层优化）
- 针对 ISSUE-017 查词浮层三处优化：① 拼音放大到与字同大（.word-lookup-item-py 14px→22px 或 var(--material-font)）② 多音字 pinyin 空格分隔 → split 后分行、每音独立 🔊（onSpeak 传该读音拼音串，音频源 TTS 读拼音 vs 实际读音待确认）③ 删 .word-lookup-item-meaning 不显示释义。
- 代码点：MaterialsPanel.tsx:111-159/138-149/141/408；dictionary.ts:19-20（pinyin 空格分隔多音）；styles.css:3178/3187/3193。优先级待定，未实施。

## 考核 v3：选课改 LLM + 每周期可编辑选课 prompt（22:0x-23:0x，用户拍板"选课不用代码"）
- **用户决策**：①选课**不用代码**，给每个周期（每天/每周/每月/半年/年末）设置一个 prompt、**家长可编辑**；②默认规则：每天/每周=考周期内学习/复习过的**所有**课程；每月=每主题考本月课程的 **50%** + 再选本月前课程的 **25%**（数量=本月课数×25%，例：本月 40 课→20 本月+10 本月前）；③每档 prompt 都要说明「如何获取本周期学习和复习的课程」+「如何考核」，抽取档说明选择原则。
- **服务端**（server/src/routes/exam.ts）：
  - `DEFAULT_SELECTION_PROMPTS`（5 档默认模板，占位符 {{TODAY}}/{{RANGE}}）；FixedExamConfig 加 selectionPrompts（getFixedConfig 默认+覆盖合并；PUT 支持部分更新、空串=恢复默认）；
  - **config 两段式**：`?schedule=X`（无 courses）→ selectionPrompt（注入统计+候选清单）+ candidates（全部学习痕迹课程元数据，无 rubric）；`?schedule=X&courses=a,b` → 选中课程带 rubric + scoringPrompt；自定义排期 scope 直接返回课程（不经过选课 LLM）；
  - `listLearnedCourseMeta`：候选口径 = 学习/复习/已学标记(status=✅)，**含已学但日期未知**（firstLearned 记「✅」归入更早学习，可被每月"本月前 25%"选中）——修了 v2 就存在的候选漏 status=✅ 问题；
  - `buildSelectionPrompt`：**周期归属标记**（关键可靠性设计）——服务端代码精确计算窗口归属，清单行尾打「★ 本周期」/「★ 本月」/「◐ 本月前」，LLM 按标记挑选不自己算日期（实测 293 门长清单 LLM 逐行算日期输出空数组，打标后精确选 1 门）；
  - 删除 `selectCoursesForSchedule` 代码打分选课（4 因子权重方案废弃）。
- **客户端**：`selectCoursesForSchedule(selectionPrompt, childId)` 内存 session 选课（**课程名前缀清理**：mimo 会输出 "[论语] 学而篇第一章" 带主题前缀）；`generateExamQuestions` 改**逐课并发出题**（并发 3、每课一次调用完整出题，防多课 rubric 撑爆）；exam.ts 类型拆 ExamSelectionConfig/ExamCoursesConfig + getExamCoursesForSchedule；IPC `exam:selectCourses` + exam:config 透传 courses；ExamView startExam 两段式（选课 → 取 rubric → 出题），scoring 遮罩显示阶段文案（选课中/出题中/判分中）。
- **家长端**：ExamSettings 重写——移除失效的"N 门课"输入（v3 由 prompt 规则决定数量），新增「各周期选课规则」5 档 textarea 编辑器 + 恢复默认按钮（清空保存=回退默认模板）。
- **验证**：server tsc 0 / 主 tsc 0 业务错 / vitest 311 全绿 / build 通过 / 冒烟 20/20（新增 config 两段式 + selectionPrompts 断言）/ **真实 LLM 全链路**（293 门候选 → ★ 标记精确选窗口内 1 门 → rubric 6078 字 → 逐课出题 3 题覆盖）。8788 已跑最新构建。
- **踩坑**：① 空窗口：weekly 排期 9-09 窗口内真实数据 0 门（学习记录都 8 月底前）→ LLM 严格返回空是**正确行为**，测试需把模拟复习日期设在排期窗口内；② LLM 长清单逐行算日期会漏选（→ ★ 标记方案）；③ LLM 复制清单行 "[论语] " 前缀（→ clean 正则 `^(?:\d+\.\s*)?\[[^\]]*\]\s*`）；④ 测试恢复数据要 finally 保证。

## 23:16 修「进入孩子模式白屏」回归（根因：ChatWindow 未导入 useCallback）
- 现象：进入孩子模式整页白屏（Learn 主界面渲染 ChatWindow）。
- 根因：「聊天框实现 ISSUE-031」那轮在 ChatWindow.tsx 加了 `speakText = useCallback(...)`，但文件顶部 `import { useState, useRef, useEffect } from "react"` **漏导入 useCallback**。vite dev 走 esbuild 不检查未声明变量 → 运行时 `useCallback is not defined` 抛错 → ChatWindow 渲染崩溃 → 无 ErrorBoundary 兜底 → 整页白屏。tsc 能抓 TS2304，但 dev 不拦。
- 修复：`src/components/ChatWindow.tsx:1` import 补 `useCallback`。修复后 tsc 对本轮改动文件 0 业务错。
- ⚠️ 经验：dev/esbuild 不会报未声明标识符，新增 hook 后务必 tsc 全量核对 import；白屏第一嫌疑=某组件 render 时抛 ReferenceError/TypeError（常因漏导入 hook 或 props 解构 undefined）。

## 23:36 git 提交（commit 3e7ba6c）
- 提交 29 个文件（+2552/-357）：ISSUE-030 资料字号、ISSUE-031 查词浮层优化+聊天框接入、孩子模式白屏修复、学习考核/语音评测进度、记忆归档。
- 新建：src/components/WordLookupOverlay.tsx、src/components/ExamSettings.tsx。
- ⚠️ 未提交（保持未跟踪，已排除）：tencent-aksk.txt、.known_hosts、linux-016.zip、lunyu_exam/、voice_*.csv、scripts-*.mts 等敏感/外部/数据文件。
- ⚠️ 安全提醒：tencent-aksk.txt 含腾讯云凭据且**未被 .gitignore**（aliyun-aksk.txt 已在忽略），后续易误提交泄露，建议加入 .gitignore。
