## 06:56 安全加固：凭据文件加入 .gitignore
- 把 `tencent-aksk.txt`（腾讯云 AK/SK）与 `.known_hosts`（主机指纹）加入 `.gitignore`，防止误提交泄露。
- 提交 d5be572（仅 .gitignore，+3 行）。验证：git check-ignore 命中，status 不再列出两文件。
- 用户明确要求：加入 gitignore 但**不推送远端**，故未 push。

## 07:27 孩子端「考核安排」返回按钮（用户反馈"进入考核安排后没有返回按钮"）
- 根因：ExamView pick 阶段唯一的返回按钮在排期列表**最底部**，排期多（60+ 条 daily/weekly）时被推到很下面，页面头部无返回入口，看起来像没有返回。
- 修复：`ExamView.tsx` 常驻头部左侧加「← 返回」按钮（`stage !== "exam" && stage !== "scoring"` 时显示——exam 锁定、scoring 处理中不显示），任何滚动位置可见；底部原返回按钮保留。
- 验证：tsc 0 业务错、build 通过。

## 07:31 考核列表只显示今天的 + 🐛 修复"当天排期缺失"
- **需求**：孩子端考核安排列表只显示**今天**的考核。`ExamView.tsx` loadSchedules 后按本地时区过滤（排期 scheduledAt 为 UTC ISO，转本地年月日 == 今天）；空列表提示改"今天没有考核安排…"。
- **🐛 连带发现真实 bug（服务端）**：`ensureFixedSchedules` 候选从 `anchor + step` 起步（**不含 anchor 当天**）→ 当天 20:00 的排期从不生成（如配置 daily+weekly，孩子当天永远看不到当天考核）。修复：`let t = anchor;`（从当天开始）。验证：今天 9-02 20:00 weekly pending 正常生成，同日 daily 被 weekly 顶替 ✓。
- **顺带清理**：本机 8788 库一条测试残留排期（9-02 11:26 done，旧锚点+提交关联测试产物，会按天挡住当天新排期生成）。
- 验证：server tsc 0、冒烟 20/20（复跑通过）、build 通过；8788 已跑最新构建。

## 07:48 提交 git（2 个 commit）
- `4a838a3` fix(exam)：当天排期 anchor 起步修复 + 考核安排只显示今天 + 头部返回按钮（exam.ts + ExamView.tsx）。
- `7fc7aa8` chore：考核数据导入/恢复/验证脚本 + 记忆更新（scripts/* + memory）。
- 排除未跟踪：lunyu_exam/、linux-016.zip、voice CSV×2、scripts-decode-token.mts、scripts-probe-material.mts（数据/产物/可能含密钥）。
- 发现：**v3 考核代码已在 3e7ba6c（9-01 23:38 并行会话提交）里**，本次只提交今天修复。

## 08:12 学习考核从「设置」挪到家长中心左侧边栏（用户要求）
- **新组件 `src/components/ExamAdminPanel.tsx`**（家长端，dashboard-main 新 view "exam"）：① 固定考核配置（频率多选+时间点，家长级）② **各周期选课规则**（每天/每周/每月/半年/每年 5 档 prompt 编辑+恢复默认）③ **自定义考核**（孩子下拉 + 已安排列表[时间/状态/内容/取消] + **datetime-local 设置时间点创建**，scope.note 内容说明）。
- **Dashboard.tsx**：侧边栏加「🎯 学习考核」导航（view="exam"，children 传入面板）。
- **Settings.tsx**：移除「学习考核」tab（import/tab 数组/渲染）；**删除 ExamSettings.tsx**（内容并入 ExamAdminPanel）。
- 自定义创建走已有 `examScheduleCreate(childId, iso, scope)`；列表走 `examSchedules(childId)` 过滤 kind=custom。
- 验证：tsc 0 业务错、build 通过。

## 08:23 考核管理重构：标签管理（每天/每周）+ 自定义多排期（用户要求）
- **需求**：① 考核详情页用标签管理「每天/每周」，每档有 是否启用 + prompt + 考核时间（每周可设**周几几点**）；② **去掉月度/半年/年度**固定档（由自定义考核灵活安排）；③ **自定义考核可建多个**，每个有自己的 **prompt + 日期时间点**。
- **服务端**（server/src/routes/exam.ts）：
  - `FixedExamConfig` 加 `weekly{weekday(1=周一…7=周日),time}`（getFixedConfig 默认周一20:00；PUT 接受 weekly 校验）。
  - `ensureFixedSchedules` 重构：daily 按 `time` 每天生成（当天未过则当天）；weekly 按 `weekday+time` 定位（`(target-d.getDay()+7)%7`，target=weekday%7）；monthly/halfyear/yearly 保留旧 anchor 步进（兼容旧数据）；同日去重不变。**验证：周五 19:30 排期正确、每天 19:30 正确**。
  - **自定义排期 scope.prompt 选课**：config 自定义分支——有 prompt → 第一段 selectionPrompt（buildSelectionPrompt 支持 freq="custom"：不打 ★/◐ 标记、统计全部候选、RANGE=自定义）+ candidates（scope.topics/courses 限定或全部）；**第二段 courses 参数优先于 scope 分支**（修 bug：原来自定义排期忽略 courses 参数 → 第二段拿不到 rubric）；无 prompt → 旧行为直接返回范围课程。
- **客户端**：`exam.ts` FixedExamConfigData 加 weekly；**ExamAdminPanel 重构**：标签「每天/每周」——每档 启用开关 + 考核时间（每周加周几下拉）+ prompt 编辑 + 恢复默认；去掉月度/半年/年度勾选；自定义考核：多列表（时间/状态/内容/prompt 摘要/取消）+ 创建表单（孩子 + datetime-local + **prompt 必填** + 内容说明可选）→ scope.prompt。
- **冒烟更新 24 项**：weekly 周几配置保存/排期落在周五 19:30/自定义带 prompt 两段式/清理测试排期（修 DELETE 空 body 报 FST_ERR_CTP_EMPTY_JSON_BODY——api() DELETE 去掉 content-type）。
- **验证**：server tsc 0、主 tsc 0、vitest 311 全绿、build 通过、冒烟 **24/24**；8788 已跑最新构建（配置恢复 daily+weekly 20:00/周一）。

## 08:53 考核管理界面标签化（用户反馈"太挤，内容堆叠"）
- ExamAdminPanel 改为**三标签页**（类似 Settings 样式：点击标签只显示该标签内容）：**每天 / 每周 / 自定义考核**——每天/每周标签各含 启用+时间（每周加周几）+prompt+恢复默认+保存；自定义考核标签含 孩子下拉+创建表单+多排期列表。不再堆叠。
- 验证：tsc 0 业务错、build 通过。改动未提交（连同 08:23 一批，等用户确认）。

## 08:56 记录 ISSUE-032：全链路加载态（登录/主页/内容未加载完都要提示「正在干什么」）
- 根因（已查代码）：Home.tsx:33-34 与 Dashboard.tsx:36-38 都 `useEffect` 调 `childList()` 但**无 loading flag**——pending 期间 `children=[]`，页面渲染**空列表无提示**；网络慢时家长/孩子看到「孩子列表没了」，误以为数据丢失（用户原话点名）。App.tsx:37 的 authCheck「加载中…」有提示但笼统、且不覆盖 childList；CourseDetail/LearningDashboard 已有 `.placeholder` 范式但 home/dashboard 未复用。
- 改造方向：① Home 加 `childrenLoading` 门控，pending 显示「正在加载孩子列表…」+spinner，仅 `!loading&&length===0` 才显「还没添加孩子」 ② Dashboard `refresh()` 加 loading flag 同处理 ③ 新增统一 `<LoadingBlock>`/`<PageLoading>` 组件（复用 `.working-spinner`）④ 文案具体化（正在加载孩子列表…/正在验证家长身份…/正在进入学习…）⑤ 登录→home 过渡由 Home 内部 gate 兜住。**关键原则：加载完再显示页面，绝不把空数组当无数据渲染**。优先级待定，未实施。


## 08:57 自定义考核改为「先设置考核，再分配给孩子」（用户要求：孩子共用考核）
- **流程反转**：不再先选孩子再建——创建表单分两步：① 设置考核（时间点 + prompt + 内容说明）② 分配给孩子（**孩子多选**，默认全选）；创建 = 为每个选中孩子各生成一条排期（内容相同，各自独立完成）。
- **列表按考核聚合**：拉**所有孩子**的自定义排期（并发 examSchedules 逐个孩子，childId 补进行），按 `scheduledAt|prompt` 分组显示：时间+内容+prompt 摘要 + 每个孩子状态（✓已完成/可开始/待考核）+ ✕取消分配（DELETE 该孩子排期）+ 「＋孩子名」虚线按钮补分配（给未分配孩子建同内容排期）。
- 纯前端改动（ExamAdminPanel），复用现有 `examScheduleCreate`/`examScheduleCancel` API，无服务端改动。
- 验证：tsc 0 业务错、build 通过。

## 09:00 实施 ISSUE-032 全链路加载态
- 新建 `src/components/Loading.tsx`：占位型 `LoadingBlock` + 整页门控型 `PageLoading`（复用 .working-spinner）；styles.css 加 .loading-block/.loading-text/.page-loading/.page-loading-text/.working-spinner-lg/.avatars-loading。
- `Home.tsx`：加 childrenLoading，childList 期间显示「正在加载孩子列表…」占位，空主页误判消除；真·空才显示「还没有添加孩子」。
- `Dashboard.tsx`：refresh 加 childrenLoading flag；sidebar 与孩子列表主区 pending 期间占位，消除「列表没了」误判。
- `App.tsx`：初始 loading 文案「加载中…」→「正在验证身份…」。
- 验证：tsc 0 业务错。未提交（用户仅说「处理新的issue」）。

## 09:13 考核 prompt 简化 + 输入框放大（用户要求）
- **① prompt 不再含 JSON 输出说明**：家长编辑的选课 prompt 只描述「考哪些课/知识点、怎么选」——从 `DEFAULT_SELECTION_PROMPTS` 5 档模板**删除「【输出】只输出 JSON…」行**；JSON 格式说明由客户端引擎 `selectCoursesForSchedule` 自动附加（家长不可见，不要求懂 JSON）；引擎尾巴补「课程名必须与清单完全一致」。
- **清理已存模板**：8788 库 selectionPrompts 之前被保存流程固化了旧全文（含 JSON 行）→ 已清空覆盖（保留 frequencies/time/weekly），回退新默认模板。⚠️ 教训：PUT fixed-config 用 `cur=getFixedConfig()`（合并后）作为 base 再存——会把默认全文固化进库，模板升级后旧库不跟；本次手动清，后续可考虑存 diff。
- **② 输入框放大（参照课程管理 TopicDetail 编辑器）**：ExamAdminPanel 的 prompt textarea 改 `minHeight:40vh、fontSize:13、monospace、padding:10`；input 改 `padding:8px 12px`；自定义创建 prompt `minHeight:160px`。
- 验证：server tsc 0、主 tsc 0、build 通过、冒烟 24/24；8788 已跑最新构建（新模板无 JSON 行）。

## 09:30 自定义考核改「左列表 + 右详情表单」布局（用户要求）
- **需求**：自定义考核标签页改为 左侧考核列表、右侧考核信息（时间/prompt/分配孩子）；新建时右侧空白，家长设置好后点「创建考核」保存。
- **ExamAdminPanel.tsx 重构**：custom 标签 左列=「＋ 新建考核」按钮+考核列表（时间+内容说明+「分配给 N 个孩子 · X 已完成」，点击选中高亮）；右列=编辑表单（考核时间 datetime-local + 内容说明 + prompt textarea + 分配孩子多选），新建模式空白、编辑模式预填。
- **编辑=取消重建**（服务端无 update API，只有 create/delete）：`saveCustom` 编辑分支——`changed`（时间/prompt/note 任一变化）→ 取消组内所有非 done 行再按勾选孩子重建；内容没变只改分配 → 只 cancel 移除的孩子/补 create 新增的孩子；**done 行永远保留为历史**（勾选框对已完成孩子锁定禁用+灰显「（已完成）」；组全 done 时右侧只读提示不可修改）。新建分支=原 createCustom 逻辑。
- 布局 maxWidth 720→880；删除 statusText/assignTo/unassign（分配增删并入保存动作）；msg 反馈移到各标签保存按钮旁（原底部全局块删除）。
- 验证：tsc 0 业务错、build 通过（45s）。未提交。

## 定时任务新模型（ISSUE-029，实施完成 11:50 ✅）
- 家长中心左侧边栏新增「⏰ 定时任务」页（view=scheduler，SchedulerTasksPanel.tsx）：卡片 grid 展示任务（类型/时间/开关/已分配孩子/最近执行状态）+ 新建任务表单 + 分配孩子弹窗 + 执行结果表（最近30次 ok/skip/error+信息）。
- 数据模型：server.sqlite 新增 scheduler_tasks / scheduler_task_assignments / task_runs（schema v8）；routes/scheduler.ts + db/task-runs.ts（recordTaskRun/findTaskForRun/listTasksWithAssignments/buildEffectiveChildConfig/listTaskRuns）。
- **关键设计（执行链路零改动）**：任务+分配 → effective-config（每孩子 recording/todo/autoNewSession）→ 客户端合并 classTimes/archiveLimit → 仍走 scheduler_config 推送；worker 照常执行并写 task_runs（runTaskAtPoint 捕获 ok/skip/error）。
- 设置页 SchedulerSettings.tsx 移除 recording/todo/autoNewSession 区块，保留 classTimes/archiveLimit/家长 autoNewSession/事件轮询，加跳转提示。
- 任务类型 4 种：recording / todo_gen / todo_stat / auto_new_session（后者客户端行为）。
- ⚠️ 坑：node -e 内联中文/引号被 shell 吞 → 改脚本文件执行；字符串先删一段再删第二段时索引位移 → 必须重取 indexOf。
- 验证：server tsc 0 错、smoke 15 项全过（含新 5 项）、客户端 tsc+build 过。当前服务端 version 0.3.0（含 exam features，另一会话已升）。

## 09:45 选课 prompt 注入「主题类型」标注（必学/选学/复习，用户需求）
- **需求**：家长写考核 prompt 只会用日常说法（如「考今天学习复习的课，只考核必学的」），不知道系统标注；LLM 必须能找出「必学课程」并执行考核。
- **数据源**：「必学/选学/复习」= 家长给孩子主题设置的「每天学习量类型」，存**孩子 kb topics.rules_json.type**（courses.topic=topic_key 可关联，setChildTopicDaily 写入）。
- **改动（server/src/routes/exam.ts）**：`listLearnedCourseMeta` 读孩子 kb topics 表构建 topic_key→type map，每门课新增返回 `topicType`；`buildSelectionPrompt` 候选清单每行注入「| 主题类型:必学/选学/复习」（未设则 -），尾部附「标注说明」段：周期标记含义 + 主题类型语义 + 家长说法映射（「必学课程」=主题类型必学主题下的课）+ 明示「家长对标注一无所知，请按日常说法映射」。
- 验证：server tsc 0；**冒烟 25/25**（8899 临时服务 + C:/tmp/exam-smoke-data，SERVER_PORT/SERVER_DATA_DIR 环境变量）；curl 实测 weekly 排期 config 第一段 selectionPrompt 含「| 主题类型:必学」+ 标注说明段 ✓。EXAM-REQUIREMENTS §14.9 补说明。
- **真实数据验证（8788）**：珊珊（英语/汉字宫/论语=必学、千字文/孝经=复习、春风阅读/陶笛/小篆=选学）与闻闻 daily 排期 config 注入 ✓（样例「1. [英语] 01·什么是英语 | 主题类型:必学 | …」）；小明必学主题下无学习痕迹课程 → 无候选行（正常）。客户端 selectCoursesForSchedule 透传 prompt 无截断、课程名清理兼容，**无需改**。提交 2a6799b。

## 09:52 记录 ISSUE-033：灵活学习计划（家长对话制定 + 服务端监督执行 + 未完成叠加 + 上学/假期区分）
- 背景：现有计划模型 = `kb.topics.rules_json.daily`(每主题每天X课)+`type`(必学/选学)，经 `setChildTopicDaily`(parent-library.ts:541) 写入、被 `todo-scheduler.ts:102-115` 生成「今天学X课」todo、study-tracker 做达标评估——**没有课程级天粒度/某天多课/跨周期叠加/上学假期**，太死板（用户明确嫌死板）。
- 需求：① 家长与 agent 对话制定 ② 存服务端、服务端监督执行 ③ 未完成项 roll over 叠加到下一周期 ④ 取代死板 daily 模型（支持「单课学N天」「某天多课」「无固定配额」）⑤ 区分上学/假期两套 ⑥ 对话完成、服务端执行。
- 改造方向：① 服务端新增 `study_plans` 表(mode/period/items JSON{done_days,status}) ② 复用 scheduler.ts 每分钟 tick + todo-scheduler gen/stat 展开成每日 todo、未达标置 rolled 叠加 ③ roll over 语义（period 结束 pending 项 carry 到下一周期、标「📌延续自上期」）④ 家长 agent 新工具 `study_plan_create/update` 持久化 ⑤ school/holiday 两份按日期自动选用 ⑥ rules.daily 可导入默认 plan 或 deprecated。与 ISSUE-025 todolist 关系：学习计划是其更灵活上游数据源。优先级待定，未实施。

## 10:29 记录 ISSUE-034：聊天气泡 markdown 渲染空行过多、消息被拉长
- 现象（附截图）：agent 回复在气泡里用 markdown 渲染后，块级元素之间空行太多（标题前/列表项间/段落间均标注红框），消息被拉得很长。
- 根因（已查代码）：① 家长端 `.bubble-md` margin 偏大（h1-h6 `margin:14px 0 8px`、ul/ol `4px 0 8px`）——孩子端 `.bubble-md-child` 在 ISSUE-009/023 已紧凑化但家长端没做 ② **最可能的罪魁=空 `<p>` 标签**：LLM 输出连续空行 `\n\n\n` 被渲染器转成空 `<p></p>` / `<p><br></p>`，带 `margin-bottom:8px` 叠加形成大段空白 ③ 全局 `.markdown-body` 规则（h1/h2 `16px top`、p `8px bottom`）也叠加。
- 改造方向：① 空 p 标签 `display:none`（最优先，预计解决 80%）② `.bubble-md` 块级 margin 收窄到与 `.bubble-md-child` 对齐（h 8px0 4px / ul 2px0 4px / p 0 0 4px / li>ul 0）③ 全局 .markdown-body 同步收窄或限定 bubble 内生效 ④ 不破坏 child 已有紧凑规则 + 资料面板保持宽松。优先级待定，未实施。

## 10:35 实施 ISSUE-034 聊天气泡 markdown 紧凑化
- 关键更正：查证 `ChatWindow.tsx:728` 气泡容器 = `bubble bubble-md`（孩子端加 `bubble-md-child`），**不带 `markdown-body` 类** → 全局 `.markdown-body` 规则不作用于气泡，原 issue「markdown-body 叠加」判断基于旧代码已失效。故只改 `.bubble-md`，资料面板不受影响。
- styles.css 改动（仅 .bubble-md 作用域）：
  - 空标签消除：`.bubble-md p:empty, .bubble-md p:has(> br:only-child) { display:none }`（解决 LLM 连续空行产生的空 `<p>`/`<p><br></p>` 空隙，家长+孩子气泡通用）。
  - 收窄块级 margin：h1-h6 14/0/8→8/0/4；p 0/0/8→0/0/4；ul,ol 4/0/8→2/0/4；li>ul,li>ol 2/0→0。
- 未动 `.markdown-body`/`.content-panel .markdown-body`/`.bubble-md-child`，资料面板与孩子端既有规则保留。
- 验证：纯 CSS，无 TS 影响；待 dev 回归（家长聊天空行消失、标题/列表/段落更紧凑）。

## 10:50 修正 ISSUE-034（闻闻会话仍大量空格的根因）
- 用户反馈闻闻会话最新两条仍有大量空格。实测闻闻最新 assistant 消息（re-render 计划表）用 react-markdown 渲染，DOM 为正常 `<p>/<ol>/<hr>`，**无空 `<p>`**——说明上一版 `:empty` 选择器全无效（初版误判主因）。
- **真正根因**：`.bubble-md` 的 `white-space: normal` 被 `.message .bubble` 的 `white-space: pre-wrap` 按 CSS 特异性压死（两者皆 0,2,0，但 `.message .bubble` 在前）。导致 markdown 文本每个 `\n` 被当真实换行，`\n\n` 直接变整行空白 → 大量空格。
- **修复**：`src/styles.css` 把 `.bubble-md` 的 `normal` 改为 `.bubble.bubble-md`（0,2,0，靠后出现胜出）真正压过 pre-wrap；并将 `.bubble-md hr` margin 收到 `8px 0`（挡住 UA 默认 0.5em，孩子端 30px 下 ~30px 空白）。用户气泡（仅 `bubble` 类）保留 pre-wrap。
- 清理：移除误加的重复 `.bubble-md hr` 规则，合并进既有 1932 处。删除临时渲染测试脚本 _tmp_render_test.mjs。
- 经验：CSS 改了不生效第一嫌疑=特异性被更高/同特异性前置规则压死；markdown 气泡一定要 `.bubble.bubble-md` 双类保 white-space:normal。ISSUES.md ISSUE-034 记录已更正（初版误判）。

## 17:00 发布收尾：客户端 0.1.8 + 服务端 0.3.0（公网 + 201）
- 服务端 0.3.0 此前已部署 201（`node /opt/learning-server/server.cjs`，pkg 二进制因 agent SDK 动态 import 已弃用）。
- Windows 0.1.8 本地 `npm run dist:win` 产出，publish-update.py 传 OSS+ECS /download/，/api/version 已登记 0.1.8。
- **GitHub 推送被 Push Protection 误拦截**：`test/assessment.test.ts:59` 假测试桩 `REPLACE_WITH_YOUR_TENCENT_SOE_SECRET_ID`(非真实密钥)被当腾讯云 SecretId。用 managed venv 的 `git filter-repo --replace-text` 改写历史（占位替换），强推 github+gitee，v0.1.8 tag 触发 build-linux + build-mac CI 均成功。
- CI 产物：`learning-app_0.1.8_amd64.deb` / `学习伙伴-0.1.8.AppImage` / `学习伙伴-0.1.8-arm64.dmg` / `学习伙伴-0.1.8-x64.dmg`。
- **201 ubuntu 客户端升级**：paramiko 上传 deb → `sudo dpkg -i` → `dpkg -l` 显示 `ii learning-app 0.1.8 amd64`（0.1.7→0.1.8）。
- **公网发布 Linux/Mac**：手工生成 `latest-linux.yml`(path=AppImage)+`latest-mac.yml`(arm64/x64 dmg+arch)，oss2 传 OSS `aixuexihao-app/learning-app/`，aliyun-run.py 在 ECS `i-bp15zfctbt147ktl39pk` 上 curl 拉进 `/opt/learning-cloud/download/`（nginx site learning-cloud 对外）。全部 HTTP 200、size 与本地一致。
- 踩坑：本地 Windows electron-builder 打不出 linux deb（缺 fpm）；linux/mac 只能走 CI。已清理本回合仓库临时物（ci-artifacts 645M、build 日志、filter 脚本、pre-rewrite bundle）。

## 13:20 ISSUE-033 需求重写为 vFinal5 并写回 ISSUES.md（学习计划：对话驱动的逐日排期表）
- 与家长多轮确认（09:52 那版已过时），推翻点：school/holiday 切换、items goal/target_days/done_days 状态机、周期 roll-over、自动每日挑选、可选池、共存过渡 → **直接替换** `rules_json.daily`，存量导入为首份 plan。
- vFinal5 核心：学习计划=「每天具体学什么」排期表；家长对话 → agent 起草逐日安排（查内容结构、有疑问先问家长不擅自猜）→ 家长「确认/改」生效；空天=不要求学；未完成自动加到下一天持续累加；多计划同天按天合并；随时对话改任意一天；客户端 scheduler gen/stat tick 展开（服务端只存）；派生 todo 标 `[家长]`；工具 `study_plan_create/update/list/get`；家长端两处**只读**展示（侧边栏「学习计划」面板 + ChildDetailPage tab），编辑仍走对话。
- 已写回 `.workbuddy/memory/ISSUES.md`（659 行起整条重写；加「编号历史」注记：033 曾用于 AGENTS 纯 SQLite，归档在 ISSUES-archive-2026-08-30）。
- Explore 查证新增事实（影响实施拆解）：① 家长会话 tools 清单在 pi-session.ts:516/562（getParentSession/getParentContentSession），白名单 isAllowedTool :442-446，家长会话**无 kb_* 工具**；② todo gen/stat **双端都有**——electron todo-scheduler.ts buildParentLines:108-120 与 server worker tasks.ts runTodoGenServer:288/runTodoStatServer:335（hasServerFeature("worker") 时本地跳过）→ worker 模式需同步展开逻辑（R1 待拍）；③ server.sqlite 建表=openDb CREATE TABLE IF NOT EXISTS + meta.schema_version 写死 '8' 改 '9'（无迁移数组）；④ todo 数据在每孩子 kb 文件 child_todos（items_md）非主库；⑤ Dashboard.tsx view 为字面量联合（"children"|"courses"|"exam"|"scheduler"|"tokens"|"settings"）+ JSX 边栏 + :240-248 分发；ChildDetailPage.tsx TABS 数组（progress/topics/prompt/exam/account/review）:17-24；⑥ kb 粒度：topics 表(y=总课数) + courses 表(一行一课) 可支撑起草，但家长侧无工具暴露；⑦ rules_json.daily 读写链：ChildTopicsModal → preload:184 → ipc-handlers:413 → parent-library.ts:541 setChildTopicDaily → 服务端 routes/db.ts:452 kb.topics.upsert。
- 实施拆解已给（见会话）：P0 服务端表 + /study-plans 路由（today 聚合）→ P1 存量导入 → P2 家长工具+prompt → P3 scheduler 展开替换旧分支（含 worker）→ P4 只读 UI（view "plan" + StudyPlanPanel + ChildDetailPage tab）→ P5 回归。未开工。

## 13:35 ISSUE-033 拆解落盘 + 执行架构澄清（R1 已定）
- **R1 澄清（用户）**：学习计划只在客户端展示；服务端 = 存储 + **每日定时检测当日完成情况、未完成自动顺延叠加到下一天**（carry 由服务端定时执行，非客户端 statTime 顺手写）。gen 展开无需双端两份——客户端 scheduler 与 server worker 都只读服务端当日聚合（/study-plans/today）。
- 已把「实施拆解 P0-P5 + R1 结论」写进 ISSUES.md ISSUE-033：P0 study_plan_items 表(schema v9) + routes/study-plans.ts；P0b 服务端每日 tick 完成检测+顺延；P1 存量导入（语义待拍 R2）；P2 家长工具 4 个 + prompt（pi-session.ts:442/516/562）；P3 todo-scheduler buildParentLines 替换 + worker 同源 + learning-summary 摘除 + ChildTopicsModal 收敛；P4 Dashboard view "plan" + StudyPlanPanel + ChildDetailPage tab；P5 回归。待拍：R2 迁移物化语义、R3 起草课程结构工具形态。用户下一步=落盘后再定（未开工）。

## 13:45 ISSUE-033 R2/R3 拍板 + 开工 P0
- **R2=不迁移**（推翻 vFinal4/5「存量导入为首份 plan」）：旧 rules_json.daily 不作数，家长对话重建；无迁移代码，P1 移除。上线时现有孩子每日安排变空属预期（agent 引导「还没有学习计划」）。
- **R3=新增家长只读课程结构小工具**（起草排期专用，列主题+课数+未学清单），不放权 kb_query。
- ISSUES.md 已同步（需求稿第 1 条/改造方向/P1 删除/待拍已决）；MEMORY.md 同步。开始实施 P0（服务端表+schema v9+routes/study-plans.ts+today 聚合）。

## 13:56 ISSUE-033 P0 数据层实施完成（tsc 0 + 冒烟 12/12）
- `server/src/db.ts`：openDb schema 块加 `study_plan_items`（id/parent_id/child_id/kind('date'|'daily' 预留)/date/content JSON/origin('conversation'|'carry')/active/created_at/updated_at + child/date、parent/date 索引），schema_version '8'→'9'（CREATE IF NOT EXISTS 惯例，无迁移数组）。
- 新 `server/src/routes/study-plans.ts`（fastify，仿 scheduler.ts：authParent/assertChildOwned/handleAuthError 私有 helper）：GET /api/v1/study-plans?childId=（列表 date 倒序，active=1）；GET /study-plans/today?childId=&date=（聚合 kind=date+active+date=@d 行 → 展平 items[{planId,text,topicKey,carry}]，carry=origin==='carry'；gen 侧据此生成 [家长] todo）；POST（create，kind 仅收 date，content=[{text,topicKey?}] ≤100 项）；PATCH /:id（content/date/active）；DELETE /:id。行归属按 parent_id，child 归属 assertChildOwned。
- `server/src/index.ts` 注册 registerStudyPlanRoutes。
- 验证：cd server && npx tsc --noEmit 0 错；node scripts/build.mjs 产出 dist/server.cjs（13:53 新）；新 `server/scripts/verify-study-plans.mjs` 冒烟 **12/12**（create 同日两行→today 3 项→list→patch 内容→patch active=false→直连插 carry 行→today carry 标记→delete→跨家长 403→无 token 401）。⚠️ Windows 清理坑：子进程 kill 后 sqlite 句柄未释放，rmSync 立即执行 EBUSY → 等 exit+sleep 再 rm（try/catch）。未部署、未提交 git。

## 14:20 ISSUE-033 P0b 每日顺延实施完成（tsx 验证 4/4）
- `server/src/worker/study-plan-carry.ts`（新）`runStudyPlanCarryTick({dataDir, db})`：每分钟 cron + 启动补跑触发（worker/scheduler.ts 已接入，carry 先于 todo gen 落库）；幂等游标=worker_state(child_id,'study_plan_carry').last_key 存昨天日期。
- **判定口径（用户语义落地，保守版）**：只处理「昨天有 date 排期行」的孩子（SELECT DISTINCT 预筛）；读孩子 kb child_todo_stats 昨天 parent_total/parent_done——有统计且未达标才顺延；**stats 缺失不顺延**（todo 未生成/未统计时不臆断，防无限堆积）；达标不顺延。顺延=昨天 items 中今天未排 text → 今天 origin='carry' 新行（同 text 去重）。
- 验证：`server/scripts/verify-study-plan-carry.mts`（tsx 直调源码、秒级，无需等 cron）4/4 ✓：A 未达标去重顺延（今天已排同 text 不重复）/B 无 stats 不顺延/C 达标不顺延/同天重复 tick 幂等不新增。tsc 0；build 过；HTTP 冒烟 12/12 复跑无回归。未部署、未提交。
- ️⚠️ 遗留语义点（后续可调）：顺延以「孩子整体 parent 完成度」判定，未精确到单行——同天多行其中一行学完也会整批顺延未学 text（去重后通常只补没学的；粒度问题后续按需细化）。

## 14:30 ISSUE-033 P2 家长 agent 工具 + 提示词实施完成
- **custom-tools.ts 尾部新增 5 工具**（学习计划组）：
  - `study_plan_create`（childName + days[{date,content:string[]}] 一次排多天；create 前先 GET list?date= 查重，同文本去重防重复行；服务端单行 POST 循环调用）；
  - `study_plan_list`（childName + 可选 from/to；行 markdown 回显带 [id=前8位]，origin carry 标 📌 延续）；
  - `study_plan_get`（childName + date 缺省本地今天 → /study-plans/today 展平清单）；
  - `study_plan_update`（act=replace/removeItems/delete + id（支持唯一前缀，findPlanRowById）+ content；removeItems 删光自动转 delete；找不到/前缀多匹配给 list 提示）；
  - `study_plan_sources`（**R3 只读课程结构**：kb.topics.list + kb.progress.list + kb.courses.list 组「主题(共X课已学Y未学Z)+未学课清单」；指定 topic 时列出全部课+已学/未学标注；带「旧规则 daily 已停用」提示）。
  - helper：resolvePlanChild（listChildren 按姓名匹配，同 exam_schedule_create 口径）、assertPlanDate、planRowsToMarkdown、mapPlanRows、findPlanRowById。
- **pi-session.ts**：import 5 工具；getParentSession(:516)/getParentContentSession(:562) 的 tools 白名单 + customTools 数组各对称加 5 个 name。
- **buildParentPrompt 加「### 2.5 学习计划」专节**：本质=逐日排期表(服务端真源，旧「每主题每天X课」已停用)；制定流程=①sources 查结构②起草具体排期(不确定先问家长)③聊天列提案等「确认/改」再落库；日常修改=先 list 再 update/create；数据目录段+两库职责段标 rules_json.daily 已停用；「设置每日目标」引导词移除。
- **服务端 GET /study-plans 加可选 date 精确过滤**（create 查重依据）；冒烟补一条 → **13/13**。
- 验证：客户端 tsc 0 业务错；electron-vite build 全过（主进程+preload+renderer）；server tsc 0 + build + 冒烟 13/13。⚠️ 提示词用户版本优先：已保存过家长 prompt 的安装环境不自动吃到新段落（AgentPromptEditor「恢复默认」或清库后才生效）。
- 未提交 git、未部署。P3（todo-scheduler 展开替换 + worker 同源 + learning-summary/ChildTopicsModal 收敛）待续。

## 14:0x ISSUE-033 P2 家长工具+prompt 完成 + P3 展开替换/收敛完成
- **P2（对话制定链路）**：custom-tools.ts 尾部新增 5 工具——study_plan_create（多天 days[{date,content[]}]，落库前 GET list?date 去重）、study_plan_list（可选 from/to）、study_plan_get（date 缺省=本地今天→/today）、study_plan_update（act=replace/removeItems/delete，id 唯一前缀匹配）、study_plan_sources（R3 只读课程结构：dbQuery kb.topics/progress/courses.list，不放权 kb_query）；pi-session.ts getParentSession/getParentContentSession 的 tools 白名单 + customTools 对称注册 + buildParentPrompt 加「### 2.5 学习计划」节（起草→确认→落库、旧 rules_json.daily 已停用注明）。⚠️ serverFetch 无 query 参数，GET 一律手拼 URL；服务端 GET /study-plans 加 date 精确过滤（create 去重依据）。
- **P3a 客户端展开替换**（todo-scheduler.ts）：删 TopicRule/loadTopicRules/buildParentLines；新增 loadTodayPlanItems（serverFetch GET /study-plans/today?childId&date，失败降级空=「空天不学」不阻塞 gen）+ planItemsToParentLines（同文本去重；carry → `[家长] X（昨天没学完，今天补上）`）；**extractUnfinished 改为只取非 [家长] 行**（[家长] 未完成由服务端 carry 确定性顺延，避免同文本双条）；gen/stat prompt 文案同步（家长规定项来自学习计划）。
- **P3b server worker 同源**（tasks.ts）：删本地 TopicRule 副本，新增 loadTodayPlanItemsServer 直查 ctx.mainDb study_plan_items（SQL 与 GET /today 同口径：parent/child/active=1/kind='date'/date ORDER BY created_at）+ planItemsToParentLines 同渲染；extractUnfinished 同步只取自规划项。修 1 处类型笔误（parsePlanContent 多写一层 []）。kb-tools.ts topics 输出摘 daily 注入。
- **P3c daily 收敛**：learning-summary.ts TopicSummary 删 daily 字段+解析+progressSummaryToMarkdown 注入；custom-tools get_progress 描述与 kb_query topics 输出摘 daily；ProgressView「今日评估」→「今日学习情况」（去每日目标列，条件 type 存在时显示）+ 去每日徽章；LearningDashboard 去 daily 字段/徽章；CourseManager 卡去「每日 N 课」；TopicDetail 基本信息去「每日目标」InfoRow（保留主题类型）；**ChildTopicsModal 收敛**：删「每天学习量」输入，只留「主题类型」select（TOPIC_TYPES），落库 parentSetChildTopicDaily(…,"",type) 顺带清旧 daily；parent-library/ipc-handlers 注释更新（daily 停用、参数保留清遗留）；server exam.ts 主题类型文案去「每天学习量」措辞（type 仍用于选课 topicType 注入，不能删）。
- **验证**：electron tsc 0 业务错（仅 5 条已知环境告警）→ electron-vite build 通过（main/preload/renderer）；server tsc 0 → scripts/build.mjs 重建 dist/server.cjs（14:24 新）→ verify-study-plans.mjs 冒烟 13/13 全绿。

## 14:3x ISSUE-033 P4 只读 UI 完成（tsc 0 + build 过）
- **IPC 透传**：preload.ts + ipc-handlers.ts 加 `studyPlanList(childId,{from,to})`（serverFetch GET /study-plans，from/to 客户端过滤）与 `studyPlanToday(childId,date?)`（date 缺省本地今天 → /study-plans/today，items 含 carry）。错误均 {success:false,error}。
- **新组件 `src/components/StudyPlanPanel.tsx`**：孩子切换 chips（多孩子）/ 今天卡片（📌 carry 标注「昨天没学完，今天补上」/ 空天提示）/ 未来 N 天排期表（默认 13 天，按日卡片 origin=carry 显 📌）/ 刷新按钮 / 「在对话里修改」按钮（onAskInChat 回调）。纯只读——编辑引导去右侧家长 AI 对话（study_plan_* 工具）。
- **Dashboard.tsx**：view 联合加 "plan"、侧边栏加「🗓 学习计划」入口（置课程管理后、学习考核前）、分发 StudyPlanPanel（onAskInChat=展开右侧聊天）。**ChildDetailPage.tsx**：TABS 加 {key:"plan", label:"🗓 学习计划"}（放 progress 后）+ 渲染 StudyPlanPanel children=[child]。
- 验证：tsc 0 业务错（仅 5 条环境告警）、electron-vite build 通过。未提交 git。剩余：P5 回归（vitest + 冒烟 study-plan→gen→carry→展示；部署 201/公网待用户确认）。

## 14:34 ISSUE-033 P5 回归全绿 → ISSUE-033 全部实施完成 ✅
- vitest 全量 **34 文件 / 311 用例全绿**（96s；todo-scheduler.test 4/4、learning-summary/kb 等与改动前基线 311 一致，P3/P4 无回归）。
- server 冒烟 verify-study-plans.mjs 13/13（此前最终构建后已跑）+ verify-study-plan-carry.mts 4/4（本轮复跑通过）。
- ISSUES.md ISSUE-033 优先级行改「✅ 实施完成」；P5 标记 ✅（附发布前待办：家长中心→学习计划面板真机渲染/空态/carry 人工过一遍；部署 201 的 dist/server.cjs 与客户端新版本未做）。
- 遗留：P0–P5 全部代码未 git commit；201 服务端 + 客户端版本未发布；家长 prompt 用户版本不自动吃到 2.5 学习计划段。
