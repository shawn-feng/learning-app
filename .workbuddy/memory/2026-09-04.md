# 2026-09-04 工作日志

## 续：关闭后台服务 + 验证修复 + 清掉"三项"
- 关闭后台 learning-server：PID 21988 (`node dist/server.cjs`, 8788) 已 taskkill，端口释放。
- 静态复核修复生效：server/src/worker/tasks.ts runTodoGenServer 空天逻辑确认写 `""`（358-366）；custom-tools.ts todo_list read 兜底返回"还没有安排 Todolist"提示（1271-1273）。
- 关键澄清：09-03 库里 `## 今日计划\n\n（今天没有待办事项～）` 不是 worker 写的——是孩子端 agent 会话经 todo_list update 写回的空天友好展示（单行、不含"今天的学习计划没有安排内容"短语，不会 trigger 反复 prepend），无害，修复后不会再膨胀。
- 完成用户原始"去掉这三项吧"：那 3 条（阅读课外书/户外运动/整理书桌）实为 09-02「孩子自规划」被顺延到 09-03 的；09-03 已被 worker 自动清空，故源头在 09-02。该 child 库无 study_plan_items 表（仅 child_todos/child_todo_stats），删 child_todos 即终态。已从 09-02 删除 3 条 + 旧格式占位行 1 条，保留日期/段落结构空壳。
- 注意：本地服务当前是关的。恢复运行需从 server/ 目录 `node dist/server.cjs`（含修复的 dist/server.cjs 构建于 09-03 23:10）。

## 在已启动的 8788 上跑 exam 冒烟（用户选择对主服务跑，接受污染）
- 冒烟脚本固定 BASE=8899 + /tmp/exam-smoke-data；用户已在跑 8788 主服务(server/data)。改副本指向 8788。
- ⚠️ Windows 下 node 不认 Git Bash /tmp（解析成 C:/tmp）→ sed 副本须放项目内再跑。
- 改法：`sed -e 's#8899#8788#' -e 's#/tmp/exam-smoke-data#C:/Users/79734/Documents/pi/server/data#'` 原脚本 → server/scripts/_smoke8788.mjs；读 server/data/server-config.json jwtSecret（与服务一致 token 有效）。
- 结果：**27/27 全绿**（含新断言「每周排期落周五且=该日 0 点」），确认「考核只按日期」改动在真实服务上生效。
- 清理 smoke 残留：server.sqlite 删 exam_attempts/exam_schedules/study_plan_items/settings(exam_fixed:smoke-parent)/children/parents（child smoke-child-1 / parent smoke-parent）+ rm -rf server/data/kb/smoke-parent server/data/parents/smoke-parent。8788 健康无残留。临时副本已删。备份在 C:/tmp/server.sqlite.bak-smoke。

## 架构评估：学习计划 & todolist 改「多列表」方案（用户提议，已出评估，未动手）
- 用户提议：计划/todolist 从文本改多列，便于精确匹配；家长 agent 不再写 markdown 而写表。
- 现状盘点（已钉死代码）：
  - `study_plan_items`（主库 server.sqlite, db.ts:166）：一行=一天，content 是 JSON 数组 `[{text,topicKey}]`——课程名/「复习：」前缀/多课全塞 text。无完成态列。
  - `child_todos`（孩子 kb, kb.ts:56）：一行=一天，items_md 整块 markdown `- [ ] [家长] X（昨天没学完）/ - [x] [孩子] Y`。
  - 3 处 LLM agent 直接读写 md/JSON：家长 `study_plan_create/update`（写 content）、孩子 `todo_list`（整读整写 md，pi-session/custom-tools.ts:1250）、recording `todo_list read→改勾→整体 update`（recording-prompt.ts:91-101）。
  - UI 消费：TodoModal.tsx parseItems(md)、StudyPlanPanel、服务端 child_todo_stats + 客户端同款计数。
- 结论：方向对，但实质是「2 表 schema + 3 个 LLM 工具契约 + UI 读接口」全链路重构（~1-2 天跨双端），非只改表。文本病灶=课程名/新学复习/完成/来源藏字符串→本次「复习项不打勾」即此根因。
- 建议落地顺序（未开工，等用户确认路线）：①计划 content→新表 study_plan_courses 一课一行 + mode(new/review) + **挂 course 外键(或 topic,title)**（改动面小、收益最高）；②stat 把 status/done_at 写回计划行（呼应用户「stat 该更新计划库」）；③服务端 /study-plans 下发 done；④todolist 新建平行 todo_items 当新真源、md 仅作展示视图由代码拼（杜绝 LLM 整写 md 的覆盖/膨胀病）、todo 工具改结构化 add_task/check_task；⑤UI 切新接口。
- 待用户定夺关键判断：② stat 是否回写 plan 表；④是否新建平行 todo_items 表（vs 直接换掉 child_todos）。

## 08:40 新增 ISSUE-047（孩子端 agent 制定定时任务 + 语音提醒 + 频率）
- **内容**：孩子 agent 直接创建定时任务（如"半小时后提醒喝水/每天9点读英语"），到点语音播报提醒内容，支持频率 once/daily/weekly/interval。
- **入口**：`server/src/routes/scheduler.ts`（任务结构缺 frequency/owner）、`server/src/worker/scheduler.ts` runTaskAtPoint（无频率）、`electron/lib/ipc-handlers.ts:658` scheduler:task:*、`electron/lib/scheduler.ts:396` class:reminder 语音事件、`electron/lib/pi-session.ts:476` 孩子 agent 工具（缺 schedule_task）。
- 与 ISSUE-038（定时任务模型）、ISSUE-019（语音播报）强耦合；优先级待定。写于 ISSUES.md 末尾。

## 学习计划/todolist 全链路「改多列表」重构（用户拍板：彻底重写，不兼容旧数据/旧客户端，删旧表旧逻辑）
- **决策**：用户明确「不用管旧数据和兼容旧客户端，直接改新方式，旧表和代码逻辑直接去掉」。
- **新 schema**：
  - 主库 `study_plan_items`（db.ts：预 exec 前加「旧表有 content 列→DROP 重建」迁移）：**一课一行**，删 content JSON，加 `topic_key/course_name/mode('new'|'review')/status('pending'|'done'|'carried')/done_at`。origin=conversation|carry。完成态由 stat 回写（解决了用户「stat 该更新计划库」）。
  - 孩子 kb：`child_todos(items_md)` → **`todo_items`** 一事一行：`id/child_id/todo_date/title/source('parent'|'child')/plan_id/status('pending'|'done')/done_at/note/sort/created_at/updated_at`（openKb 预建前 DROP 旧 child_todos）。child_todo_stats 保留，改从 todo_items 汇总。
- **db.ts(kb ops)**：删 `kb.todo.get/put`；新增 query `kb.todo.list`；exec `kb.todo.add(仅source=child)/addParent(source=parent,plan_id)/set(check/uncheck+done_at)/remove(仅child)/removeByPlan(仅parent by plan_id)`。
- **worker**：
  - gen(`runTodoGenServer` tasks.ts)=以 study_plan_items 今日排期物化 parent todo_items（按 plan_id 幂等；删计划已删的 parent todo；孩子项绝不动）。
  - stat(`runTodoStatServer`)=courses 当天活动判定→①回写 study_plan_items.status/done_at ②按 plan_id 同步勾 parent todo ③saveTodoStatsServer 从 todo_items 汇总 child_todo_stats。
  - carry(study-plan-carry.ts 全重写)=**纯 SQL**：昨日 status='pending' 行 → UPDATE date=今天+origin='carry'+status='pending'，昨日原行置 'carried'+active=0，去重（同日同 course 已排则折叠）。不再读 todo md。
  - **⚠️ stat 已踩坑修复**：第2步勾 todo 用的 `doneByPlanId` 若 map 自 `r.status`（函数开头 load 的陈旧内存值）→ 第1步 UPDATE 后不反映→todo 永不勾。须用第1步 `planCourseDone` 判定结果 `doneOfPlan`（stale planRows 的 status 不可信）。冒烟 FAIL→PASS 实证。
- **service routes/study-plans.ts 全重写**：一课一行 DTO（id/courseName/mode/origin/status/doneAt/done/active）。`/study-plans` 与 `/study-plans/today` **done 由服务端 loadCourseDoneMap(openKb 查 courses 当天活动) 下发**——客户端不再本地算（实现 ISSUE-033 用户诉求：面板以服务端为准）。POST 幂等合并（同日同课跳过/升级 review）。
- **exam.ts**：listPlanCourseMeta 改读 course_name（不再 plan-text 剥前缀，旧 import 已删）。
- **plan-text.ts 删除**（无前缀剥取；前缀在 agent-tool 入口归一为 mode）。check-plan-match.mts / verify-study-plans.mjs / verify-study-plan-carry.mts / fix-study-plan-dups.mjs 全删；新集成冒烟 `server/scripts/verify-study-plan-v2.mts`（**10/10 ALL PASS**：gen 2条→stat 回写 plan+勾todo+写stats→carry 顺延）。
- **electron**：custom-tools `todo_list`(read/add/check/uncheck/remove 结构化) + `study_plan_*` 工具改到新 API（study_plan_update 行级 act=delete/reschedule/setmode）；ipc-handlers todo:get→rows、studyPlan:list/today→透传服务端 done；删本地 loadCourseDoneMap/planTextToCourseText；TodoModal 从 rows 渲染；StudyPlanPanel 用 courseName/mode/carry/done；learning-summary 改 kb.todo.list 序列化注入；pi-session 孩子/家长 prompt、recording-prompt(双端) step8 改结构化工具。**删 electron/lib/todo-scheduler.ts**（孤儿死代码）。
- **验证**：server tsc 0 错；server.cjs 构建过；electron-vite build 过；verify-study-plan-v2.mts 10/10。**未提交、未推送**（用户此前说勿推远端；且工作区有另一窗口的 exam/Ubuntu 音频并发改动 CourseDetail/Exam*/LearningDashboard/verify-exam-smoke 未提交，勿混入本提交）。

## 学习计划 v1→v2 数据迁移（本地 server/data，用户要在本地测新功能）
- **数据真源**：本地 server/data 是开发库；唯一真实家长 86a84278-c8ae-415e-8fbc-6140b1b7c88e（珊珊1f05…/闻闻0940…/小明c218…）。其余 kb/* 下几百个 parent 均为「测试孩子/投递测试孩子」冒烟残留（勿动）。
- **迁移内容**（用户定：mode 按文本「复习」前缀→review，无→new；完成判定源=courses 当天活动）：
  - 主库珊珊 study_plan_items 旧 content JSON（32 行一天多课）→ 新**一课一行 128 行**（course_name/mode/origin/date；topic_key 从 courses 补全；status/done_at 按 first_learned/last_review==date 判，实测 09-03 颜渊24+子路1 标 done，其余 pending）。
  - 珊珊 kb child_todos(md) → **todo_items**（[家长] 行 source=parent 并按 course_name 关联 plan_id；孩子自规划 source=child；勾选态转 status/done_at）；闻闻/小明无结构化 plan，其历史 child_todos 也已一并迁成 todo_items。
- **db.ts 启动迁移从「DROP 旧表」改成「就地转换」**：新增 `migrateStudyPlanV2(db)`（meta 建后调用），检测旧 content 结构→展开一课一行，写 meta study_plan_v2_migrated=1 幂等；**不再静默丢数据**。status 留 pending 待 stat 回写。
- **迁移脚本** `server/scripts/migrate-study-plan-v2.mts <dataDir>`：一次性脚本，含备份(.pre-v2mig-<ts>)，主库+child_todos 全做。
- **验证**：跑前先 checkpoint WAL（`PRAGMA wal_checkpoint(TRUNCATE)`，node:sqlite）；停 8788 → 备份迁移 → 重启；服务端 v2 后 gen/stat 在真实迁移数据上无崩、今日(09-04)6 条家长 todo 由 plan_id 幂等物化、stat 判 pending 正确。**8788 现跑新 server.cjs（background SwX4LL）供本地测试**。
- **教训**：拷 WAL 模式 sqlite 只拷 .sqlite 不带 -wal 会得到不一致副本（曾致测试副本多出「学而篇」假行）；迁移前须先 checkpoint。

## 孩子「约定时间完成任务」特性（用户需求：培养孩子时间规划，统计是否按时+反馈）
- **设计决策（用户定）**：due_time 只加 `todo_items`（孩子自规划），**不动 study_plan_items**（计划=当天学什么，无时刻）；第一版只孩子会话自规划可设 due；**不做主动语音催办**——只记录、TodoModal 卡片汇总 + recording 写「今日时间规划」daily，孩子主动会话了解。
- **schema**（kb.ts todo_items + ensureTodoTimeColumns 幂等 ALTER）：加 `due_time`(约定截止 HH:MM)/`done_time`(真实完成时刻 ISO)。`done_at` 保留日期语义。
- **链路**：
  - routes/db.ts：kb.todo.add 收 due_time(HH:MM 校验)；kb.todo.set done 时写 done_at=日期 + done_time=ISO now，uncheck 清空；list 返回新列。
  - kb-tools/custom-tools 双端 todo_list add 加 due_time；read 行带 ⏰HH:MM 前。
  - pi-session 孩子段引导：孩子说「几点前做完X」→ title 干净 + due_time；recording-prompt 双端 step8b 写 daily「生活」block 标题「今日时间规划」（按时/超时/未完成 + 完成时刻）。
  - TodoModal：每行设 due 显示 ⏰ badge；done 后按时(绿)/超时(红)；底部「N 项设时间 · M 按时」。`isOnTime` 按 done_time 本地 HH:MM ≤ due_time 现算。
- **验证**：tsc server 0；双端 build 过；真实珊珊「下午3点前完成英语第11课」已拆为 title=英语第11课 + due_time=15:00（下午→+12h，本地 TZ GMT+8）；todo-time 逻辑 14:40按时✓ /16:10超时。**未提交**。
- ⚠️ 时间解析要分上下午：下午3点→15:00（勿拆成 03:00）；done_time 存 UTC ISO，判按时要转本地 HH:MM。

## 本地提交 41bb313（未推送，用户选「整体提交含另一窗口」）
- **内容**：学习计划 v2 重构 + 数据迁移(migrate-study-plan-v2) + 约定时间完成任务 + 记忆文件；**同次含另一窗口**的考核只按日期 + ISSUE-047 定时提醒(scheduler_tasks/routes-scheduler/SchedulerTasksPanel/MyRemindersModal/smoke-reminder)。
- **范围**：44 文件 +3219/-1876。`git add -u`(所有 tracked M/D) + 合法 ??(memory 2026-09-04、migrate-study-plan-v2.mts、verify-study-plan-v2.mts、seed-parent-config.mts、smoke-reminder.ts、MyRemindersModal.tsx)。
- **排除(仍 untracked 垃圾)**：linux-016.zip、tmp/、voice_*.csv、lunyu_exam/、scripts-decode-token.mts、scripts-probe-material.mts。
- **混改文件**：custom-tools.ts/pi-session.ts/ipc-handlers.ts/exam.ts/routes-db.ts 里我的计划改动与另一窗口考核排课改动叠加——用户拍板整体提交。
- 远端 master 仍 0eaca95，本地 HEAD 41bb313（未推）。

## 英语学习模块(ISSUE-029)需求待定项确认（09:0x，仅讨论确定需求,未实施）
- 用户拍板 §11 四项待定项：①评测服务商=**智聆+阿里双备,智聆为主**(阿里儿童单词跟读更细但协议未实测,门控:阿里 provider 默认关闭,真实密钥验证通过再开放);②评测结果落库=**服务端独立表 english_assessments**(发音成长曲线,家长端可见);③评测 key=家长端 assessment-config.json(已实现);④零基础兜底=**中英混合开关**(家长可配「允许中文」,agent 检测孩子连续不理解自动中文解释,平时全英文)。
- 已回填：`ENGLISH-AGENT-REQUIREMENTS.md`(状态/§4 身份/§9 数据模型/§11 待定项→已确认) + `ISSUES.md` ISSUE-029 优先级行。**未实施任务 2-7**。需求文档 §11 闭环。

## ISSUE-047 孩子端 agent 自建定时提醒（09:0x，已实现并冒烟通过）
- 孩子 agent 工具 `schedule_task`（create/list/cancel，owner=child），复用 class:reminder(type="custom") 语音播报；服务端 scheduler_tasks 扩 owner/frequency/reminder_text/weekday/interval_minutes/voice/fire_at/last_fired_at/expired + ALTER 迁移；takeDueReminders 按频率去重（daily/weekly 本地日期、interval 从创建时刻起算、once 触发置 expired）。家长面板 reminder 卡片只读可见可删，新建下拉过滤 reminder。
- 改后 `server.cjs` 需同步 201（本机 node scripts/build.mjs 已过）；electron-vite build 过。冒烟 scripts/smoke-reminder.ts 14/14。未提交/推送（勿与另一窗口 exam 改动混入）。

## 英语角需求新增：课程驱动 + 解锁 display_content（09:3x，仅讨论拍板,已回填文档）
- 用户三连问：主题范围约定 / 聊中展示资料 / 能否系统课程驱动对话。现状盘点：english/ 课程资料已存在(wowenglish 风格,每课一 html 目录);主会话 display_content 已能渲染该资料(MaterialsPanel + asset:// 沙盒)。需求 §3 原本**禁止**英语会话挂 display_content。
- 用户拍板两项新决策(§11 新增 5/6)：①内容来源 = **课程驱动 + 手填兼容**(默认绑 english/ 课程,该课单词/句式/背景注入 prompt;家长仍可手填主题覆盖,回退抽象标签模式);②对话中资料展示 = **解锁 display_content**(推翻 §3 限制,英语会话可展示课程单词卡/图/互动页,前端复用 MaterialsPanel,资料默认英文版保沉浸;page_action/todo_list 仍不挂)。
- 已回填 ENGLISH-AGENT-REQUIREMENTS.md：状态行、§3(工具集解锁 display_content)、§4(主题限定改课程驱动+手填兼容)、§5(词表基线来源=绑定课程逐课提取)、§10(任务3 含 display_content 接入 / 任务4 升为课程绑定选课 UI)、§11(新增决策 5/6)。
- 已确认评测/落库/双语/服务商决策不受影响。实施影响：任务4 升级为「课程绑定/选课 UI + 课程词表提取 + 家长编辑 + prompt 注入」。**仍未实施任务 2-7**。

## 英语角评测范围二次调整（10:1x，用户要求，已回填文档）
- 用户要求：**去掉『每次对话都做发音评测』**(原分叉③ 自由对话全评)。改为：①课程环节内置**特定发音练习**(跟读/朗读重点词句式,有参考文本,即时评测);②当天对话完成后**挑选部分语音**评测(优先 ASR 识别错误、当天重点单词/短语)。
- 已回填 ENGLISH-AGENT-REQUIREMENTS.md：状态行(二次调整注)、§1 总体模型、§2 分叉③(自由对话全评→特定练习+当天挑选)、§6(拆环节一课程内练习+环节二当天挑选,普通对话仅 ASR 不评测)、§9(评测结果落库 english_assessments)、§10(任务1 标已完成+触发改特定/挑选;任务5 注触发时机)。
- 影响：任务1 的 assess provider 链路仍有效,只是调用点从「每条语音并行」改为「两环节触发」;任务5 评测卡 UI 仍需要。实施范围不变(任务2-7)。

## 英语角需求澄清：家长配课位置 / 每课教学内容 / 与其他科目异同（10:4x，仅讨论,已回填文档）
- 盘点家长库课程体系(parent.sqlite 唯一真源)：`topics`(科目)→`courses`(每课,含 `lesson_method` 教法/`html_path` 资料/`teaching_copy` 教学文案/`assess_rubric` 考核要点);`allocateTopicToChild` 快照拷贝进孩子库;孩子 agent 经 `parent_content` 取内容、`display_content` 展示资料、末了 exam 用 assess_rubric。
- **关键发现**:english/ 每课已是结构化教学数据(`server/data/materials/<pid>/english/`):`teach-data.js`(`mastered`=单词→IPA、`lines`=逐句台词含逐词 IPA + `modules` M1-M4 搭配/句式)、`learn/` 下 phonetics/phonics/grammar/vocabulary HTML、`emma/*.mp4` 各音素口型视频、`media/lesson-XXX.mp4` 剧集动画。→ **无需为英语角重编内容**:任务4 词表提取=解析 teach-data.js mastered+lines.modules 注入 prompt;§6 环节一 refText 直接取 mastered 词与 lines 句式;display_content 展示 learn/ HTML + 口型/剧集视频。
- 家长配课 3 步:①家长库课程管理(编辑/配 key)②分配 english 主题给孩子(与其他科目同机制)③**英语角选课/绑课 UI 选具体课(任务4 新增)**——其他科目按 study-plan 顺序推进,英语角由家长显式绑课。
- 与其他科目异同:同=同一套 topics/courses 家长库+分配快照+display_content+进度追踪;异=①全英文沉浸身份 ②发音评测 vs 知识考核 ③家长绑课 vs 学习计划顺序。本质=在现有科目体系上挂一个语音化沉浸式特殊科目。
- 已回填 ENGLISH-AGENT-REQUIREMENTS.md §5(英语课内容结构写实:teach-data.js 解析=词表+refText、display_content 接 learn/+口型视频;注明无需重编内容)。**仍未实施任务 2-7**。

## 英语需求终版定调：完全复用现有科目体系（11:1x，用户拍板,已整体重写文档）
- **用户决策(重大简化)**:英语=一门普通学科,课程管理/引导/考核/记录/家长排计划**全部复用** topics/courses 体系,不单独创建。差异仅两点:① 教学时 agent 用**英文**(topic `teach_lang='en`);② 教学后 agent 引导**发音测评**(跟读重点词/句式,智聆/阿里),测评作为学习情况**记入 daily**(复用 readDailyConversation/RECORDING_PROMPT,不建独立表)。
- **推翻的初版"英语角"设计**:独立英语会话(`<childId>-english` AGENTS ref)/显式入口路由/独立评测表 `english_assessments`/`session-sync` 递归特殊化/独立选课绑课 UI/每条语音评测——全部删除。教学内容资料存 topic/course 与其他科目同。
- **整体重写** ENGLISH-AGENT-REQUIREMENTS.md 为终版(§1 复用总体模型/§2 仅两点差异/§3 教学语言切换/§4 发音测评入 daily/§5 内容存 topic/course/§6 简化任务1-4/§7 全复用数据模型/§8 范围)。同步重写 ISSUES.md 的 ISSUE-029 索引段(删旧"英语角独立会话"描述,改终版复用模型)。
- **保留的已确认项**:评测=智聆+阿里双备(智聆主,阿里门控);key=家长端 assessment-config.json;零基础=中英混合开关;english/ 资料结构化可直接接入(teach-data.js 词+IPA+句式、learn/ HTML、emma 口型视频)。
- 任务重排为 4 项:①agent 按 topic teach_lang 切英文 ②教学后发音测评+评测卡+入 daily ③解析 teach-data.js 接 course 内容+display_content ④复用 study-plan/SessionReview 仅验收。**仍未实施任务 1 已完成、2-4 待实施**。

## ISSUE-047 方案A 孩子端「我的提醒」UI（10:0x，已实现）
- 用户反馈：提醒≠要做的事，不要放进今日计划/Todolist。故做独立弹框：新增 src/components/MyRemindersModal.tsx（🔔 标题、列该孩子 reminder 类、逐条取消=task:delete、once 优先/时间排序、过期/停用置灰分组）；Learn.tsx 侧栏加 Bell 图标入口；preload 暴露 reminderList/reminderCancel。electron-vite build 过。未提交。

## 11:55 发布 0.1.11/0.3.2 并部署 201（本地Windows 打最新版，只部署局域网 201）
- **版本决策**：HEAD(13013c5) 领先已发布 v0.1.10(76ecf9d, 2026-09-03 Ubuntu录音修复) 4 提交（study-plan v2 重构 c2d19ad 等）。用户拍板按规范 bump：client **0.1.11**/server **0.3.2**，只部署 201 不碰 ECS。
- **版本编辑**：package.json 0.1.10→0.1.11；server/src/routes/version.ts SERVER_VERSION 0.3.1→0.3.2；server/package.json 0.3.2；PACKAGING.md 基线更新；**server/scripts/learning-server.service ExecStart 从 pkg 路径改为 /usr/bin/node .../server.cjs**（201 上该文件早已正确，repo 副本同步修正）。提交 13013c5 + tag v0.1.11 推双远端。
- **服务端构建**：`cd server && node scripts/build.mjs` → dist/server.cjs 16.5MB(16567372B, learning-server v0.3.2)。
- **Linux deb 走 GitHub Actions**：push tag v0.1.11 → 触发 build-linux(run#33835562598, ~4min, success)，产物 `learning-app_0.1.11_amd64.deb`(141MB)+AppImage(188MB)。⚠️ Actions **构件下载需 API token**（`gh auth status` 显示未登录但 `gh auth token` 能取到有效 gho_ token，用 `Authorization: token $T` curl 下载 328MB zip，本机首次 SIGTERM 后须 run_in_background）。
- **201 部署（paramiko，shanshan/123456，sudo 同密码）**：
  - 服务端：备份 server.cjs.bak-20260904-1207 → 上传新 server.cjs → systemctl restart → **version=0.3.2** ✓；启动日志 `[db] study_plan_items v1→v2 就地转换 250 条课程行`（201 真实数据迁移）；`/api/v1/health`={"ok":true,"db":"ok"}（注意真实 health 路由是 /api/v1/health 非 /health）。
  - 客户端：`sudo dpkg -i learning-app_0.1.11_amd64.deb` → `dpkg -l` = **ii learning-app 0.1.11**（原 0.1.9，v0.1.10 Ubuntu修复当时也未装）。⚠️ GUI 进程(PID 784156, user-data-dir=/home/shanshan/.config/learning-app)仍在内存跑旧版，**须 201 本地手动重启「学习伙伴」**才生效。
- **脚本留存**：tmp/deploy/*.py（probe/deploy_server/deploy_client/final_verify 可复用）。大文件已删。

## 12:39 worker cron 5→2 分钟（scheduler.ts，未提交）
- cron `*/5 * * * *` → `*/2 * * * *`（startWorkerScheduler）。gen/stat 反映延迟从 ≤5 分钟变 ≤2 分钟（含 todo 同步、stat 打勾、daily 新增驱动重跑）。相关 ≤5 分钟注释同步改 ≤2 分钟。
- **保留「5 分钟桶」不动**（bucketStartMin/pointInBucket 及 recording 触发）：那是把配置 recording.times 的 HH:mm 触发点映射到 5 分钟桶做去重的粒度，与 cron 频率无关；2 分钟 cron 下 recording 仍在点所在桶内触发一次（点落在非整 5 分也能命中，最多晚 2 分钟，不漏）。勿把它也改成 2 分钟，否则去重粒度错乱。
- server.cjs 已重建（v0.3.2）。未提交。

## 英语课会话隔离修正（13:3x，用户问"是否每次用新会话避免干扰",已回填文档）
- 用户担心：英语若复用主会话(单一持续窗口、所有科目共用),前面中文对话污染上下文、无法一直英文教学。盘点确认:孩子端主会话同 childId 仅一个会话(getChildSession 并发保护只建一次);daily-summary 递归 walk sessions 子目录(故子会话自动进 daily);session-sync 上云需递归(终版误删,已恢复为实施点)。
- **修正终版**:推翻"复用主会话不新建独立会话"——英语课教学**用独立子会话(上下文窗口隔离)**,但身份/AGENTS/工具/课程管理复用主 agent(仅隔离窗口防污染)。= 部分复活初版"独立会话"但身份仍复用主 agent,非独立 English-buddy。
- **粒度(用户拍板)**:按课隔离子目录 `sessions/english-<courseKey>/` + 每次进入该课 `newSession()` 开干净窗口;该课历史 jsonl 全保留供 daily/上云/SessionReview 回顾;复习进度靠 courses.first_learned/last_review 驱动不靠会话历史。
- 已回填 ENGLISH-AGENT-REQUIREMENTS.md:状态行(加会话隔离)、§1(不新建列表改"用独立子会话隔离")、§3(会话隔离段+粒度)、§6(任务2 含独立子会话+newSession)、§7(会话=独立子会话复用主 AGENTS)、§8(本期不做去掉"独立英语会话";终版确认新增第7点)。**仍未实施(任务2 重排含子会话)**。

## 13:30 修复 201「todolist 空」根因：gen 被 cc.todo.enabled 卡住 → 有计划就自动生成（已部署 201 验证）
- **现象**：201 珊珊/闻闻 todolist 一直空、拿不到数据；服务端 plan(250行/今日学而篇review)有但 todo_items 空。
- **根因**：新模型里 gen/stat 真源触发本应是「今天有学习计划」，但 runPlanTick/runStatTick 仍用 `if(!cc.todo?.enabled) continue;` 门控；而 buildEffectiveChildConfig 只在孩子被分配了 `todo_gen/todo_stat` 的 scheduler_task 时把 cfg.todo.enabled 置 true，或回退 legacy scheduler_config.todo.enabled=false（201 该家长就是 false，且 scheduler_tasks 只有 recording+auto_new_session，无 todo_gen/todo_stat）→ gen 永不跑 → todo_items 空。
- **修复（用户拍板：改代码「有计划就自动生成」）** scheduler.ts：
  - runPlanTick 改遍历所有 child，gen 触发 = `childHasPlanToday`（查 study_plan_items date=today active=1）→ 去掉 cc.todo?.enabled 门控；buildTodoCtx 传 cc={}（gen/stat 不用 schedulerConfig）。
  - runStatTick 同理：触发 = childHasPlanToday || childHasTodosToday（读 child kb todo_items 有无今日行），保留 daily 条数去重重跑。
  - 新增 helper listChildIds/childHasPlanToday/childHasTodosToday。
- **201 部署**：server.cjs（bak-20260904-1329）→ restart；日志 `[worker:todo-gen] 珊珊新增6/闻闻新增4` + stat 首次跑通；child_todo_stats 2026-09-04 已写（珊珊6/闻闻4）。cron 已是每2分钟（重启日志「每2分钟」）。
- **客户端侧注意**：服务端数据已通；但 201 客户端 xuexihub 是否为新版能读 todo_items/新 IPC 未验证——若 UI 仍空需发新客户端。
- 未提交本地源码改动。

## 英语课独立子会话实现路径澄清（13:5x，需求讨论延续,未落地）
- 实现可照搬家长端 parent/parent-content 双会话先例(pi-session.ts:506-508):各自 sessions/<sub>/ 子目录,cwd/agentDir 共用 → 英语课 = activeSessions key 加 course 维度 + 新增 getChildCourseSession(childId,courseKey);sessionsDir=childDir/.pi/agent/sessions/english-<courseKey>/,cwd/agentDir 仍复用主 agent(AGENTS/skills/config 共用,仅 jsonl 隔离);进入即 mgr.newSession() 每次干净窗口。
- 零/低改动复用:SessionManager.continueRecent 支持子目录;daily-summary walk 递归(:48-76 已支持)自动进 daily;systemPromptOverride(:446)注入 teach_lang+lesson_method;工具集复用主 agent 全套(display_content/todo_list 等)。
- 实施改动点:pi-session.ts(入口+sessionsDir子目录+newSession)、buildChildPrompt(支持当前教学课程参数)、IPC(piStartChild 加 course 维度)、前端 Learn(课程进入触发+顶部课程/语言标签)、session-sync(递归上云 english 子会话)。
- 孩子端 UI(基于现有 Learn/ChatWindow mockup):顶部课程/语言标签+左侧资料面板+英文对话气泡+语音气泡(可回放+转写)+单词卡(display_content 复用)+发音评测卡(任务5 新增 UI)+底部按住说话;独立子会话对孩子透明。

## 英语课切换/退出模型澄清（14:3x，用户问"学完英语去学其他课会话怎么组织"）
- **结论**：主会话 sessions/ 承载所有中文课共享历史(数学/语文 continueRecent 带历史);英语课各自开 sessions/english-<courseKey>/ 独立子会话、按 courseKey 隔离、只承载英语(不在英语子会话学其他科目)。
- 四步切换：①进入某英语课→开/复用对应 english-<courseKey>/ 子会话并 newSession 干净窗口；②学完退出→归档该子会话 jsonl(自动进 daily/上云/家长回看)→回主会话或课程列表；③退出后学其他中文课→主会话 continueRecent 带原历史；④退出后学另一门英语课→开对应 english-<courseKey2>/ 新子会话(干净、不串 Unit1)。
- 前端顶部「课程/语言」标签+课程列表完成切换;孩子界面看不到主会话中文历史、也看不到其他英语课气泡(隔离),agent 经 course 状态/KB 知整体进度(不显气泡)。
- 已回填 ENGLISH-AGENT-REQUIREMENTS.md §3(会话隔离段后新增"切换/退出模型"整段)。需求现已完整闭环(分叉4+评测/落库/双语/服务商+课程驱动/资料展示+评测范围+本课内容结构+会话隔离粒度+切换模型)。**仍未实施(任务2 起)**。

## 任务2 落地实施完成（14:55-15:20，英语课独立子会话+教学语言切换，全链路）
- **任务列表 7 项全部完成**。改动文件：electron/lib/kb-sqlite.ts、pi-session.ts、session-sync.ts、ipc-handlers.ts、preload.ts；src/pages/Learn.tsx、src/components/TodoModal.tsx；server/src/routes/db.ts；新增 test/english-course-session.test.ts（7 例）。
- **数据层决策（免 schema 迁移）**：不新增 teach_lang 字段（推迟到多外语时再加）——英语判定 = courseKey 格式 `<topic>:<title>` 中 topic==='english'（资料目录名约定）。kb-sqlite 新增 parseCourseKey + getCourseLessonSync（openKbDb 同步读课程 lesson_method/teaching_copy/html_path，查不到返回 null 降级）。
- **pi-session.ts**：getChildSession/createChildSession 加 courseKey? 维度，缓存 key=`childId|courseKey`（主会话纯 childId 不变，dispose/reset/listChildSessions 等既有调用点零改动）；课程子会话 sessionsDir=sessions/<topic>-<title安全化>/、每次进入 mgr.newSession() 干净窗口、跳过 Todolist 注入、cwd/agentDir 复用主 agent；buildChildPrompt 加 courseKey/courseLesson 参数——english 课注入「全程英文+简短句+一轮≤1-2新词」指令、中英混合开关（scheduler-config children.<id>.english.allowChinese 默认 true）、「允许中文关闭则全英文」分支、教法截断注入（method 1500/copy 2500 字符，模板字符串内无裸反引号）；新增导出 disposeChildCourseSession。
- **IPC/preload**：pi:start_child(childId, courseKey?)——有 courseKey 先 disposeChildCourseSession 再创建（保证"每次进入=干净窗口"）；pi:prompt 第4参 courseKey 路由到子会话（否则英语课消息会错发主会话，关键点）；preload piStartChild/piPrompt 透传。
- **session-sync.ts**：collectDeltas 改递归 walkJsonlFiles（导出），name 用相对路径（根目录文件=旧裸文件名，游标兼容）；**单测抓出并修复递归基准 bug**（原实现递归后 relative 基准变成子目录、丢父路径前缀）。
- **前端**：Learn.tsx currentCourseKey state + courseKeyRef + startSession effect 依赖 [childId,currentCourseKey]（切换时清 messages/materials 防跨会话残留）+ piPrompt 带 courseKeyRef + 聊天面板顶部英语课横条（课程名+退出按钮回主会话）；TodoModal 行带 topic_key/course_name（服务端 kb.todo.list 经 plan_id 回查主库 study_plan_items 补字段），英语未完成项显「🌍 进入课程」按钮 → onStartCourse('english:<course_name>')。
- **验证**：tsc(electron+src) 0 业务错（5 条环境告警）；server tsc 0 错；server.cjs 重建（15.8MB）；electron-vite build 过（第二次 out 目录文件锁失败，重试即过）；vitest 新测试 7/7+相关 44/44；删除孤儿测试 test/todo-scheduler.test.ts（其模块已删，全量曾 1 文件 4 例 FAIL 与本次无关）。
- **真实数据现状（重要）**：家长库 86a84278 的 english topic 有 3 课（01·什么是英语/02·音标基础/03·音标元音），teaching_copy 已填充（1.5-1.8k字）但 **lesson_method 全为空**（任务4 解析 teach-data.js 时填充）→ 当前进英语课会话=全程英文+课程名+teaching_copy 注入、无教法段落（预期降级）；本地孩子库均无 english 快照（家长分配 english 主题后 todo_items 才会出现可点击的英语课项）。
- **未部署**：server.cjs 未同步 201；客户端未出 deb。未提交 git。

## 任务2修正轮（15:30-16:20，用户指出"english有很多课且有教学方法"——两处真问题）
- **冒烟结论更正**：english topic 实有 **51 门课**（此前 LIMIT 3 误判"只有3课"），含 12·Yellow wowenglish 系列；teaching_copy 家长库全有（1.5-2k字）。**教学方法真源 = topics.method（主题级）**，courses.lesson_method 列全库未填充（所有科目都空）。
- **真问题1（架构）**：本地孩子库 topics/courses 是空壳（SPLIT 真源服务端），原 getCourseLessonSync 本地直读在真实环境拿不到数据。**修复**：改项目既有「会话前远程预取」模式——服务端新增 query handler `kb.courses.get`（child_id/topic/title 精确查一行，SQL COALESCE 课程级 lesson_method 空→回退主题级 topics.method，临时库三态验证过）；learning-summary.ts 新增 fetchCourseLessonRemote（dbQuery→本地 cache/course-lesson-<childId>.json，按 topic:title 多课共存）+ getCourseLessonCached（同步读缓存，miss 回退本地库直读兜底）；pi-session createChildSession 改为先 await fetchCourseLessonRemote 再 getCourseLessonCached。
- **真问题2（分配链路既有 bug）**：服务端孩子库 86a84278/1f050a7f 有 51 门 english 快照但 topics.method/courses.teaching_copy 全空——根因：allocateTopicToChild 对 parent_lib.topics.list 的返回类型漏 method 且 kb.topics.upsert **硬编码 method:""**（主题级教法从不快照）；teaching_copy 空是老分配早于 v5 字段。**修复**：upsert 改传 String(topicRow.method ?? "")（服务端 SELECT 本就有 method，客户端类型补 method?）。**用户侧动作：家长端重新分配一次"英语"主题即可补齐**（分配幂等：保留进度、补齐内容字段）。
- parent-library.test.ts 旧断言（method/teaching_copy 不拷贝）与代码矛盾（v5 后已拷 teaching_copy），更新为断言新行为（method/teaching_copy 随快照）。
- **验证**：server tsc+build（v0.3.2 重跑）；electron tsc 0 业务错；COALESCE fallback SQL 临时库三态验证（课程级空→主题级/课程级有值→优先课程级/不存在→null）；vitest english-course-session 7/7 + parent-library 10/10 + kb-sqlite 29 + sync 8 全过；electron build 重跑。

## 用户拍板修正（17:00-17:15，教学方法不快照、读家长库真源）
- **用户否决"method 随快照拷入孩子库"**：孩子端读家长库的主题教学方法（与 parent_content 读家长库机制一致）。**回滚** allocateTopicToChild 的 method 拷贝（恢复硬编码 ""，保留注释说明决策）+ 测试断言回滚（孩子库 method 恒空串）。
- **kb.courses.get 改造**（替代 COALESCE join）：课程行从孩子库取（行存在=已分配，天然校验）；教法 = 课程级 lesson_method（家长若填充过）→ 为空时**实时读家长库 topics.method**（openParentLib 二次查询）。真实数据端到端验证：孩子 1f050a7f 的 12·Yellow 课行 + 家长库 1230 字符英语教法全文均取到。
- **保留的真源约定**：主题级教法=家长库 topics.method（唯一真源）；课程行/teaching_copy=孩子库快照（老快照 teaching_copy 空 → 家长端重新分配一次即补齐，分配幂等保进度）。
- **验证**：server tsc+build、electron tsc、vitest parent-library 10/10 + english-course-session 7/7 + kb-sqlite 29 全过；electron build 重跑。

## 15:55 完成判定语义调整：new 行「学过即完成」不限日期（用户拍板；未部署 201）
- **规则**：planCourseDone——review 行必须 last_review==当天（不变）；**new 行只要 courses 标注学过（status✅ 或 first_learned 非空）即 done，不要求日期在计划当天**（提前学=完成）。依据：mode 由家长 agent 按已学/未学判定，不存在「没学过被排成复习」。
- **改动三处消费点**（只改 stat 会裂：提前学行 pending 被 carry 反复顺延）：
  1. tasks.ts planCourseDone（new 分支改为 learned-ever）+ stat courseByTitle 补 status。
  2. **gen 预判**（runTodoGenServer）：物化前按同口径判每行，已完成的计划行直接 UPDATE status=done/done_at=今天，todo 带 status=done 建行（kb.todo.addParent 新增可选 status/done_at 参数）——覆盖「提前学且当天无 daily、stat 不跑」的路径，且防 carry 顺延已完成行。
  3. study-plans.ts 面板：loadCourseDoneMap(按日期) → loadCourseStates(全量课程态) + planRowDone(按行 mode)；/study-plans 与 /today 都改（顺带少开 N 次 kb）。
- **冒烟** verify-study-plan-v2.mts 13/13（新增提前学 2 断言：gen 预判 todo done + plan 行 done；原有 review/new/carry 断言不变全过）。
- 未部署 201（review 语义没变，201 现有 review 排期不受影响）；要生效需重传 server.cjs。

## 17:55 本地提交 41155c8（未推送）
- 主体：完成判定语义调整（new 行学过即完成/提前学=完成；review 仍须当天）——stat/gen 预判/面板三处同口径，冒烟 13/13。
- 同次含另一窗口 ISSUE-029 英语课子会话相关（routes/db.ts 混改、pi-session/preload/kb-sqlite/parent-library/session-sync/Learn/TodoModal、english-course-session.test.ts 新增、todo-scheduler.test.ts 删）。
- 19 文件 +762/-162；垃圾（zip/csv/lunyu_exam/tmp/probe）仍未跟踪未提交。

## ISSUE-047 提交状态确认（18:4x）
- 用户要求"完成提交"时发现工作区已干净：ISSUE-047 全部改动（服务端 reminder 模型/路由、electron tick/IPC/preload、agent schedule_task、家长面板卡片、孩子端 MyRemindersModal、scripts/smoke-reminder.ts、ISSUES.md/日志）**已被另一窗口的 c2d19ad（study-plan v2 大提交）git add -A 捎带入库**，日志部分随 3f3f955。
- 分支 master **无远端跟踪**（git status -sb 无 origin），与"勿推远端"约定一致，无需推送。未拆分历史（另一窗口可能仍活跃，rebase 有风险）。

## 19:47 部署 201：完成判定新语义上线（server.cjs bak-20260904-1905，md5 06f2f289）
- 重启后 health ok；worker 每2分钟 cadence，日志无错误。
- **真实数据验证新语义正确**：闻闻今天复习了学而篇三四五六（daily 6 条）→ 4 条 review 行 done + todo 全勾；珊珊今天未复习学而篇（daily 4 条为其他记录）→ 6 条 review 行 pending + todo 未勾（review 必须当天，正确）。
- new 行「学过即完成」当前 201 无此类排期，未触发但冒烟已验证。
