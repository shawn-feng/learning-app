# 2026-08-23

## study-tracker 从技能改为纯代码定时任务（ISSUE-035）
- 用户要求：study-tracker 不再作为技能、改为定时任务，**判断逻辑由代码直接从 kb.sqlite 取数**（不经 AI）。
- 删除 `data/shared/skills/study-tracker/`（连同再次删除被外部恢复的 `recording/`——skills 目录现为空，`additionalSkillPaths` 加载空目录无害）。
- 新建 `electron/lib/study-tracker.ts`：`runStudyTracker(childDir, today?)` 纯代码——`queryTopicsMeta`（rules_json：daily 目标/必学选学）+ `queryTopicProgress`（learned/total/next/updated + 每课 first_learned）；今日新增 = first_learned==today 且状态✅；必学主题 todayLearned>=daily 判定达标；生成 markdown 写 `learning/tracker-latest.md`（latest 快照）+ 返回结构化结果；导出 `formatLocalDate`（scheduler 复用，删本地重复定义）。
- `scheduler.ts`：`runTracker` 改同步调用 + 广播 `pi:study_tracker` 事件（前端无监听无副作用）；不再建 AI session、不再 logRound。
- `pi-session.ts` L53 文案与 L377 注释更新；regenerate-agents.mjs 重新生成 AGENTS.md。
- **数据匹配要点**：topics.file 带路径（lunyu/lunyu.md）↔ courses.topic 纯目录名（lunyu）→ `file.split("/")[0]`；rules_json.daily 是字符串需 Number；first_learned 存 YYYY-MM-DD（''/'-' 无值）。
- 验证：`test/study-tracker.test.ts` 4 用例全过（临时库隔离）；tsc 过滤 TS2318/2552 后 0 业务错误；`npm run build` 通过。
- 遗留：① 前端评估结果展示 UI（接 pi:study_tracker 或读 tracker-latest.md）待定；② recording/ 技能目录被恢复的根因（疑技能导入 IPC）待查。
- 注意：工作区还有一批**非本会话**的改动（ipc-handlers/kb-lint/parent-library/user-init/preload/ChatWindow 等，疑似其它会话/手动），git 提交时只 add 本次相关文件，未打包他人改动。
- 修复 ISSUE-033 AI 提示词编辑器空白问题：agents:get 在「无用户整体版本」时原本返回空串，导致孩子/家长编辑 AI prompt 时文本框空白、家长无从在默认基础上修改。新增 getDefaultPrompt(scope, ref)（孩子读磁盘 AGENTS.md 或按 profile 生成 buildAgentsMd；家长返回代码默认），agents:get 无用户版本时返回该默认内容。同时 agents:save 对孩子 scope 同时写回磁盘 AGENTS.md，保存即时生效且与编辑器所见一致。tsc 相关文件 0 错，agents-md.test 3/3 通过。

## study-tracker 功能整体移除（用户拍板：学习进度界面已覆盖当日完成情况）
- 用户确认 study-tracker 不需要了——「孩子的学习进度」界面已能看到当天是否完成任务，单独评估任务重复。
- 已移除：`electron/lib/study-tracker.ts`、`test/study-tracker.test.ts`、scheduler 内 studyTracker 配置/调度/runCatchUp 段、`SchedulerSettings.tsx`「学习进度追踪」UI 段、生成的 `tracker-latest.md`；`formatLocalDate` 收回 scheduler 本地。
- 测试断言同步更新：scheduler-task-state（三键结构）、archive-limit（配置无 studyTracker）、app/functional（技能目录空断言，这两文件混有另一会话改动只改断言不提交）。
- 验证：tsc 过滤后 0 业务错误；build 通过；scheduler-task-state 4 用例过；archive-limit 仅 1 个已知环境性失败（resetChildSession 冷路径超时，safe-delete 拦截清理，与本次无关）。
- ISSUES.md ISSUE-035 标记已废弃；commit 463117a（实现）保留历史，本次为移除提交。

## recording 改造为「每日学习记录总结」（ISSUE-036）
- 用户拍板：改名「每日学习记录总结」；触发改多时间点（设置页可配，可增删）；新增「新建会话前自动总结」开关；做成 AI 工具（用户要求汇总时 agent 自动调用）；每次汇总按天（读 jsonl 某天会话，无会话跳过）。
- 新建 electron/lib/daily-summary.ts（定时/会话前/AI 工具三路共用）：readDailyConversation（参数化按天）、findLast/FindLatestConversationDate、createEphemeralSession（从 scheduler 挪入）、summarizeDailyConversation、summarizeConversationTool（name=summarize_conversation）。
- scheduler.ts：recording 配置改 {enabled, times[], onNewSession}，默认 ["21:00"]，兼容旧 intervalHours；startScheduler 按时间点触发（lastRun 日期+HH:mm 判重）；runCatchUp 补跑。
- pi-session.ts：newSession 前 fire-and-forget 会话前总结；挂 summarize_conversation 工具；AGENTS 记录段补工具说明。**pi-session.ts 与 ISSUE-033 同文件未提交**。
- SchedulerSettings.tsx：「每日学习记录总结」+ 多时间点列表 + 会话前开关。
- 验证：tsc 0 业务错误；build 通过；daily-summary 6 用例 + scheduler-task-state 全过；archive-limit 仅 1 已知环境性超时（resetChildSession 冷路径）。

## 同天多次汇总去重（ISSUE-036 补充）
- 用户确认策略：同天每次都跑（保留多时间点），但增量去重——跑 AI 前先 queryDaily 查当天已有 daily 条目，formatDailyExistingList 拼「已存在清单 + 不重复规则」进 prompt（已存在禁止重复插入/新信息 kb_update 更新/只写清单外新条目），主键 (date,block,title) 幂等兜底。
- 三路触发统一走 summarizeDailyConversation 的去重段；formatDailyExistingList 纯函数（raw 截断 120）。
- 验证：daily-summary 9 用例全过（含新增 2 个去重清单用例）、tsc 0 业务错误、build 待确认。
