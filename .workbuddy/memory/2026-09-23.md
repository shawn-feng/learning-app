# 2026-09-23

## ISSUE-133 第二轮：把「参数被整串 JSON 序列化」从偶发定性为「模型在特定参数形状上的固有习惯」

用户补充关键线索——**`where` 正常（对象）、`rows` 无论传数组还是对象都变成字符串**，怀疑框架层有问题。
据此翻本机会话 jsonl（`server/data/agent-sessions/**`，20 会话 / 550 次工具调用 / 771 处 `"model"` 标记）量化核对：

| 参数 | 形状 | 调用 | 被写成 JSON 字符串 |
|---|---|---|---|
| `parent_upsert_course_content.items` | 数组套对象（深、长） | 69 | **12（17%）** |
| `parent_db_write.rows` | `anyOf[数组, 对象]` | — | 同型（用户现场） |
| `parent_db_read.where` | 扁平 map | 35 | 0 |
| `parent_exam_plan_create.courses` | 标量数组 | 37 | 0 |
| `parent_study_plan_create.days` | 数组套对象（短） | 7 | 0 |
| `parent_upsert_course_content.topic/title` | 标量 | 70/70 | 0 |

- 模型 = `mimo-tokenplan/mimo-v2.5`（该家长 `app_settings.defaultModel`，三个模型位全是它）。
- `items` 历史失败：09-14/15 连续 12 次 `items.0: must be object`，其中一场**重试 6 次照旧**——
  报错只给一句 schema 文案，模型没有可用线索。
- **反证框架无关**：09-18 同一模型调 `parent_db_write` 时 `rows` 就是**真数组**并成功落库；
  链路复核：`openai-completions.js:1099` 原样透传 `tool.parameters`、只 `parseStreamingJson` 一次、
  `utils/json-parse.js` 是忠实解析器（JSON.parse → 字符串字面量修复 → partial-json 兜底）、
  `utils/typebox-helpers.js` 只是 enum 辅助。⇒ **字符串来自模型自己的输出**。
- 结论：触发条件 =「深层嵌套 + 长内容」的复合参数；`anyOf` 联合类型是额外放大器；
  扁平 map / 标量数组从不出问题（这也解释了 `where` 好 / `rows` 坏）。

### 据此扩修（同一 ISSUE-133，未开新 issue）

- `tool-shapes.ts` + `JsonArrayParam(item, desc)`；`db-channel.ts` + `coerceArrayArg(raw, label)`。
- `parent_upsert_course_content`：`items` schema 换 `JsonArrayParam`，执行器开头 `coerceArrayArg`
  取代 `Array.isArray(params.items) ? ... : []`（后者对字符串得到空数组 → 报出误导性的「items 不能为空」）。
- 回归扩到 **17 用例**全绿（新增 items 字符串化 → 校验过 + 知识点/题/挂载真落库；真数组零回归；
  非法 JSON → 可读原因）。相邻 7 个套件 **77 用例**全绿；server tsc 0 错；bundle 重新构建。
- 文档同步：`技术实现文档` §8.3 参数形状契约（补 items + 实测口径 + 「已排除运行时」证据）、§17 #19 更新；
  `ISSUES.md` #133 与 `ISSUES/ISSUE-133.md` 补第二轮证据表。
- 诊断手法沉淀进技能 `pi-local-server-agent-diagnose` §8（新增「用 jsonl 量化模型参数形状」探针配方）。
- **仍未部署 201**（待用户同意；部署需 bump 0.5.4 → 0.5.5）。
- 待用户拍板：ISSUE-134 —— 自建 `defineTool` 包装器（14 个工具文件改 import），递归给 schema 加
  string 分支 + 递归 parse，一处覆盖全部工具与未来新工具，替代逐参数打补丁。

## 追加：读 201 生产会话，定性从「形状敏感」收敛到「模型确定性差异」

按用户要求读 201 `/opt/learning-server/data/agent-sessions/86a84278…/parent/`（只读 SSH，拉最近 12 个 jsonl 到本地分析）。

**决定性结论**：全 12 个会话里被写成 JSON 字符串的参数**只有 `parent_db_write.rows`**，26 次**全部来自 `mimo-v2.5`**；
同一工具同一 schema，`deepseek-v4-flash-0731` 的 20 次调用（对象 14 + 数组 6）**零字符串化**。
⇒ **mimo-v2.5 无法表达 `anyOf`：凡联合类型属性一律退化成 JSON 字符串**（26/26＝100%）；
深长数组套对象 `items` 17%；扁平 map / 标量数组 / 标量 0%（两模型皆 0）。

**故障会话因果链**（`2026-09-22T07-53-57-…jsonl`，mtime 今天 07:07）：第 4 行用户让记作业 → 第 5 行 list_children ✓
→ **第 7 行首次 `parent_db_write` 调用 rows 就已是字符串**（此前无任何报错）→ 第 8/10/14/16/18 行 5 次重试，
rows 在 `{...}` 与 `[{...}]` 之间来回切但**始终带引号** → 第 19 行放弃并让用户「反馈给开发者」（即用户贴给我的那段）。
该会话共 **22 次校验失败**、50 处 `"rows": "` 形态；`where={"id":"ba4180ab"}` 全程是**对象** ✓。
⇒ 用户看到的「对象也变字符串」= 模型**想**传对象却永远写成字符串；它看不到自己的原始 token，自述不可信。

**生产影响**：0.5.4 + mimo 下家长助手凡走 `parent_db_write` 的操作 100% 失败（改计划/记日常/改课程全废）。
**临时绕过**（不部署也行）：家长默认模型换回 `deepseek-v4-flash-0731`（实测零字符串化）；非治本。

**产物**：`ISSUES/ISSUE-133.md` §5 重写（含 201 证据表 + 因果链 + 链路复核）、`ISSUES.md` #133 更新、
`技术实现文档` §8.3 更新为「mimo 不能表达 anyOf」口径。探针脚本与 201 会话副本**已删除**（含家庭数据，不留副本）。

## 掌握闭环设计稿 v4（07:38）：`exam_plan_courses` 收敛为「一行 = 考核计划 × 课程」

用户第四轮答复：①删列方式「可以」；②**质疑 `course_summary` 的写法**——「course_summary 只有一行啊，一个学习计划只有一个课程，知识点的学习情况放到了 `knowledge_point_records` 里啊。你再评估一下」；③任务显示名「可以」；④新列名「可以」。

### 复查结论：用户是对的，原方案是反模式

- 原方案「保留逐题行 + `course_summary` 同课程多行写同值」确实冗余；正解是 `exam_plan_courses` **收敛为一行 = 考核计划 × 课程**（与 `study_plans` 一课一行口径一致）。
- **全仓引用清单（客户端 / UI 零引用）**，全部按 plan 做 SUM，收敛后口径不变：
  1. `db/kb.ts:346/349` `course_progress` 视图（`SUM(ec.point_got)/SUM(ec.point_max)` 按 `ec.plan_id` 分组）
  2. `worker/plan-domain.ts:524` `computeExamRate()`（`SUM(...) WHERE plan_id = ?`）
  3. `worker/plan-domain.ts:379` `coursesFilled` 存在性判据
  4. `agent/db-channel.ts:986` 注册表（只需改 label/desc/列注释）
- **逐题明细不丢**：主库 `exam_attempts.per_question` 一直是全量逐题真源（qid/courseId/questionId/knowledgePointId/pointGot/pointMax/aiComment），知识点级聚合改从这里算。
- **顺带查实**：`db/kb.ts:109` 注释称本表承担「考核范围 → 回填」，但唯一写入点 `applyExamAttempts()` 是**开考后**回填，**计划期范围从未写入本表**（范围在 `exam_plans.scope_json`）→ 收敛不丢范围信息，注释要改对。

### 结构定案（v4）

- `exam_plan_courses`：新增 `course_summary`；**DROP COLUMN `knowledge_point_id` + `question_id`**、`DROP INDEX idx_epc_kp`、新建 `UNIQUE INDEX ux_epc_plan_course(plan_id, course_uuid)`。语义变化：`point_got/point_max` → 该课本次 Σ得分/Σ满分；`score` → 该课本次得分；得分率不另存。
- 迁移 P0-b（先备份）：①从 `per_question` 按 `(plan_id, course_uuid, kp_id)` 回填 `knowledge_point_records(source='exam')` → ②本表按 `(plan_id, course_uuid)` 收敛一课一行 → ③校验（行数 + Σgot/Σmax 与收敛前一致）→ ④再删列 + 建唯一索引。
- `applyExamAttempts()` 改造：按 `course_uuid` 聚合插一行/课；records 按 `(course_uuid, kp_id)` 从 `per_question` 聚合（kp 缺失回退家长库 `course_knowledge_questions`）；`coursesFilled` 判据保留。
- 设计稿 `DESIGN-mastery-loop-2026-09-22.md` 升至 **v4**（§2.5 重写为「课程级考核结果表」并附引用清单与迁移校验；§2.6/§3.2/§6/§7/§8 同步；D7/D8 措辞更新）。

### 状态

- **§7「仍待点头」剩 3 项**：②列语义变化（`point_got/point_max` 变课程级合计；`score` 建议改存"该课得分率"）③逐题只留主库是否接受 ④同步改 `kb.ts:109` 注释 + `db-channel` 表描述。
- 用户已同意①**删列时机＝直接删**（带备份 + 行数/Σ 校验）。
- 仍未改任何业务代码。

## v4.1（07:43）：补「收敛前后对照」+ 查实 `score` 是只写不读的列

用户：「2、3 说的详细一些，我看不懂」→ 在 §2.5 补两块说明（含真实列名示例），并顺带查实一处事实：

- **收敛前后对照**：一次考核考 2 门课共 4 题 → 现在 4 行（逐题）vs 收敛后 2 行（一课一行 + `course_summary` 唯一）；列语义逐项对照（`point_got` 8 → 8+10+5=23；`point_max` 10 → 30；`score` 该题得分 → 建议改存得分率 0.7667）。
- **`score` 是只写不读的列（新查实）**：全仓无任何读取点（`computeExamRate` 与 `course_progress` 视图都用 `point_got/point_max` 现算），仅暴露在 `db-channel` 给 agent 看 → 因此**建议把它改存"该课程本次得分率（0~1）"**，比存与 `point_got` 重复的得分有用；用户可否决。
- **「逐题只留主库」详解**：逐题现在有两份 —— 主库 `exam_attempts.per_question`（客户端上传的原始 JSON，含 ASR/音频/用时，最全）与孩子库 `exam_plan_courses` 的**副本**；收敛＝副本不再保留、原始一条不删。查题走 `exam_plans.attempt_id` → 主库；知识点聚合也从 `per_question` 算。唯一变化是"看逐题要跨库查"（路径变化，非数据丢失）；顺带孩子库该表行数从"题"降到"课"。
- 设计稿升 **v4.1**。

## v4.2（07:54）：用户质疑 `exam_attempts` 为何在主库 → 查实为历史错位，建议搬到孩子库（P0-a）

用户问：「exam_attempts 是孩子的考核数据，应该在孩子库，怎么在主库呢？」→ 查证后**用户判断正确**，写进设计稿 §2.7：

- **事实**：`exam_attempts`（考核场次 + `per_question` 逐题 + course_mastery/reinforce_plan/wrong_questions）在**主库 `server.sqlite`**（`db.ts:194`，带 parent_id/child_id）；同类还有 `speech_assessments`（`db.ts:220`）。
- **历史原因三条、均已失效**：①考核上报是服务端在线路径（POST /exam/attempts）先落主库最省事；②一体化时期孩子数据在客户端 kb，服务端拿不到 → 只能自存；**9/12 agent 上移后服务端已能直写孩子库（openKb），理由消失**；③ISSUE-105/109 承认别扭并定了「主库不进直连白名单」，却为它开「唯一一张表的受控只读豁免」（ISSUE-109 至今未实施）。
- **已付代价 4 条实证**：跨库读写（`plan-domain.ts:356`/`:490` 用 ctx.mainDb）、跨库聚合（`routes/exam.ts:425 listCourseStatus`）、为它破原则（ISSUE-109 + `parent-plans.ts:837/941` 只能引导家长去 UI 看）、拖累本设计的"逐题明细退主库"（§7③ 别扭的根因）。
- **建议**：搬到孩子库 → 归属正确 / 考核域单库自洽（ctx.mainDb 消失）/ **ISSUE-109 整案可取消**（归属=文件路径天然隔离，无需租户过滤硬编码）/ 逐题明细争议自动消解 / 数据治理回到文件级。影响面约 10 处引用（routes/exam.ts 8 处、plan-domain 3 处、db.ts DDL、两处文案）；`speech_assessments` 以 exam_attempt_id 逻辑引用，建议同批搬。**排期建议 P0-a（先于 P0 建表）**，做法＝孩子库建同构表 → 双写一个版本 → 校验一致 → 切读 → 停写主库旧表（保留不删）。
- **顺带发现（新，待确认是否已记录）**：`routes/exam.ts:583 listPlanCourseMeta()`（调用点 `:255` 固定档 config 下发、`:861`）读**主库 `study_plan_items`** 构建固定档考核候选，而该表自 2026-09-10 计划域重构后**已停止写入**（`routes/study-plans.ts:98` 自述"仅作历史归档"）→ 固定档候选可能只看到 9/10 前的旧计划，疑似既有缺口。
- 设计稿升 **v4.2**；§7 待点头改为 4 项（第 1 项即本次搬迁决策）。
- 仍未改任何业务代码。

## 08:08：实测核实「exam_attempts 到底搬没搬」+ 按用户要求把规划改为 ISSUE-135（删除独立设计稿）

用户追问「之前不是搬了吗？你确定没搬？把这次的规划记录为 issue，不要单独写个文档了。」

### 实测（本地 `server/data` 现场探测，用 node:sqlite 只读打开）
- **主库 `server.sqlite` 有 `exam_attempts`，19 行**（建表在 `db.ts:194`）。
- **100+ 个孩子库 `kb/<parentId>/<childId>.sqlite` 全部没有 `exam_attempts` 表**（含 `test-parent`）；孩子库里对应的是 `exam_plans` + `exam_plan_courses`。
- ⇒ **确认没搬**（`_probe_ea.cjs` 探测脚本用完已删）。

### 与 2026-09-10 定案对账（澄清"搬了/没搬"都对）
`memory/2026-09-10.md` 17:45 定过：`study_plan_items`/`exam_schedules`/`exam_attempts`(含 per_question) 从主库按 child_id 拆入孩子 kb，"迁后主库表停写只读保留一版"；16:50 的迁移映射写的是 **`exam_attempts`+per_question → `exam_plan_courses`**；15:45 说 "exam_attempts 历史明细被考核计划表+考核计划课程表**接管后退场**"。
**实际只执行了一半**：逐题**得分**被 `applyExamAttempts()` 回填进孩子库 `exam_plan_courses`（这部分确实搬了）；但**场次主表没搬也没退场**（至今主库、仍是 `POST /exam/attempts` 的写入真源），且 `per_question` 的**富字段没搬**（题干/标准答案/ASR 转写/音频 file_id/用时/AI 评语 + course_mastery/reinforce_plan/wrong_questions）。
⇒ 用户记得的"搬了"= 明细得分副本；我说的"没搬"= 场次表本身与逐题富信息。

### 归档改造（用户要求：规划不再单独出文档）
- 新建 **`.workbuddy/memory/ISSUES/ISSUE-135.md`**（学习/考核掌握闭环 —— 结果记录 → 知识点掌握情况 → 课程进度与教学建议），把原设计稿 `DESIGN-mastery-loop-2026-09-22.md` 的**全部内容迁入**并按 issue 格式重组：需求三环 → 用户 11 项决策 D1~D11 → 现状判定 → 数据结构（2 张新表 + `courses` 加 4 列 + `study_plans.result_summary` + `exam_plan_courses` 收敛，含收敛前后逐行对照与列语义表）→ 写入路径（含 LLM 成本举例）→ 消费侧 → **计划不可删（2 处硬删无保护清单）** → 实施顺序 P0-a/P0/P0-b/P1~P5 + 验收 → **§8 `exam_attempts` 归属专项（实测证据 + 09-10 历史对账 + 已付代价 + 同构表搬迁方案 + 与"退场"措辞的冲突）** → §9 待拍板 3 项 → §10 变更记录。
- `ISSUES.md` 加 **#135 索引行**（含 D1~D11 与待拍板项摘要）。
- **删除** `DESIGN-mastery-loop-2026-09-22.md`（内容已迁；避免双份漂移）。
- `MEMORY.md`：移除该设计稿的表行/列表行/索引校正提及，新增**「规划归档约定」**——功能规划一律写 ISSUE，不再单独出设计稿（首个适用 ISSUE-135）。
- **§9 待拍板 3 项**：①§8.4「09-10 退场」作废与否（搬同构表 vs 拆富字段进 JSON、不留同构表）②`score` 语义（推荐改存该课得分率，因其只写不读）③描述同步（`kb.ts:109` 过时注释 + `db-channel` 表 label/desc/列注释 + `parent-plans.ts:837/941` 文案）。
- 顺带仍待确认的既有缺口：`listPlanCourseMeta`（`routes/exam.ts:583`，调用点 `:255`/`:861`）读**已停写**的主库 `study_plan_items` 构建固定档考核候选。
- 仍未改任何业务代码。

## 08:20：ISSUE-135 方案改写 v2 —— `exam_attempts` 废弃 + 新增课程考核结果概要表

用户决定：「既然有 exam plan courses 做明细，就不要办 attempts 了，直接废掉吧，再创建新的表记录课程每次考核的结果概要」+「2 和 3 按你推荐的做」（②`score` 用显式 `rate` 表达 ③描述同步一并改）。

### 改写要点（ISSUE-135 已重写，D11/D12 新增）

- ⭐ **D11：`exam_attempts` 废弃不搬**（取代 07:54 的"搬到孩子库"方案）；**新增 `exam_course_results` = 课程每次考核结果概要**。
- ⭐ **`exam_plan_courses` 撤销"收敛为一课一行"**，恢复/保持**考核明细表**（逐题）身份，并**补题级富字段**：`ai_comment` / `asr_text` / `audio_file_id` / `duration_ms` / `correct` / `behavior`，同时 **`DROP COLUMN score`**（与 point_got 重复且只写不读）。
- **`exam_course_results` DDL**：一行 = 考核计划 × 课程 → `plan_id / attempt_ref(溯源) / topic_key / course_uuid / course_name / exam_at / point_got / point_max / **rate**(0~1) / question_count / course_summary / plan_review_at / focus_json`，`UNIQUE(plan_id, course_uuid)`；`course_summary` 的唯一落点（不再往逐题冗余）；承接旧 `reinforce_plan` 的 `planReviewAt`/`focus`。
- **目标四层模型**：`exam_plans`（考核计划头）→ `exam_plan_courses`（逐题明细含评语/录音）→ `exam_course_results`（课程概要）→ `knowledge_point_records`（知识点，靠 plan_id + course_uuid 关联）。
- **关键连带发现（已在 issue 里列出切换清单）**：三处界面 + 4 处服务端读取依赖 `exam_attempts`，废表必须一起切：客户端 `src/components/ExamView.tsx:145`（孩子端考核页历史成绩+逐题报告）、`CourseDetail.tsx:134`（课程详情逐题评语）、`ChildExamPlans.tsx:72`（家长端考核计划 tab，含 **`audioFileId` 听原音**，靠 `perQuestion` 的 asrText/aiComment/audioFileId —— 这就是明细表必须补富字段的原因）；服务端 `listCourseStatus`/`lastExamAtByCourse`/`latestReinforcePlan`/`GET /exam/course-records` 改读 `exam_course_results`；worker `applyExamAttempts` 降级为幂等兜底；**ISSUE-109 主库白名单豁免整案取消**。
- **实施顺序**：**P0-a = 考核结果重构（本 issue 最大一块）**，然后 P0（建表加列）/ P0-b（回填 records）/ P1（硬删保护）/ P2~P5。
- **§9 待拍板剩 2 项**：①`speech_assessments`（主库，键 `exam_attempt_id`）→ 建议同批搬孩子库并改键 `(plan_id, question_id)`/`audio_file_id`，备选留主库改键 ②旧 attempts 19 行 → 建议迁出后保留只读一版再 DROP，或导出 JSON 留档后弃。
- `ISSUES.md` #135 行已按 v2 重写；ISSUE-135 §10 变更记录加 08:20 一行。
- 仍未改任何业务代码。

## 08:27：`speech_assessments` 定性（D13）—— 题级存档，搬孩子库并与明细关联

用户问「speech_assessments 是特定题目的吗？就与 exam plan courses 建立关联。」→ 查证后确认**是题级的**，并发现两处关键事实：

- **一行 = 提交时 `perQuestion` 里带 `speech` 结果的那道口语题**：`routes/exam.ts:1375` 循环 `perQuestion`、`if (!speech) continue` 后逐题 INSERT（`is_exam=1`）；**唯一写入口 = 考核提交**（`routes/assessment.ts` 不写该表）。
- ⚠️ **旧表没有 `question_id`/`qid` 列**（只有 course_name / question_type / ref_text / audio_file_id / exam_attempt_id）⇒ 它是"题级但没键"，要建立关联**必须补一列 `question_id`**，否则只能靠 `(course_name, audio_file_id)` 模糊匹配。
- ⚠️ **全仓无任何读取方**（只有 INSERT + 一处失败告警 `req.log.warn`）⇒ 纯存档表，**搬迁风险为零**。

**D13 定案**（写入 ISSUE-135）：搬到孩子库（同名同构 + 补 `plan_id`/`course_uuid`/`question_id`，旧 `exam_attempt_id` → `attempt_ref` 仅作溯源），按 **`(plan_id, course_uuid, question_id)` ↔ `exam_plan_courses`** 关联；同一题多行 = 多次评测（重练/重考）。新增 §3.7 完整 DDL + 关联契约，并在 §3.6 分层模型、§3.8 既有表定位、§5 消费侧（将来可按 key join 展示维度分）、§7 P0-a、§8.4 数据处置（`question_id` 由该场 `per_question.qid` 回填）全部同步。
- 备选方案也记进了 issue：若判定考核内 1:1，可把评测并成明细行 `speech_json` 一列、整表废弃；按 D13 采用独立表（保留同题多次评测历史）。
- **§9 待拍板只剩 1 项**：旧 `exam_attempts` 19 行数据 —— 迁出后保留只读一版（推荐）vs 导出 JSON 留档后直接弃。
- `ISSUES.md` #135 行同步为 D1~D13。
- 仍未改任何业务代码。

## 08:31：旧 attempts 数据处置定案「不迁、直接废弃」+ 两条实测发现（待拍板清零）

用户：「exam_attempts 的数据在 exam plan courses 里都有，不用迁出数据了，直接废弃吧。」→ 实测核对后**部分证实 + 两条新发现**（探测脚本用完已删）：

### 核对结果（本地 19 场 attempt ↔ 孩子库 exam_plan_courses，逐场对照）
| 数据 | 是否在 `exam_plan_courses` |
|---|---|
| 课程 / 知识点 / 题目 / 每题得分（**结构数据**） | ✅ **在**（每场都有对应明细行） |
| `aiComment` AI 评语 | ❌ 不在（**83 条**） |
| `asrText` 孩子回答转写 | ❌ 不在（**55 条**） |
| `audioFileId` 录音引用（听原音） | ❌ 不在（**46 条**；**音频文件本体在 files/磁盘，不受影响**） |
| `speech` 发音评测维度分 | ❌ 不在（**25 条**）+ `speech_assessments` 存档（D13 已定搬走） |

⇒ **用户判断对了一半**：结构数据确实都在，但**题级富信息只存在于 attempts**。经用户确认：这批（20 场考核 / 83 道题，全为 2026-09-01~09-17 早期测试数据、同一个孩子）**放弃不留**，执行 `DROP TABLE exam_attempts`（不导出、不留只读副本）。ISSUE-135 §8.4 已如实记录放弃范围 + 备注"若日后改主意，回填只需一个脚本"。

### 两条实测发现（已写进 ISSUE-135 §8.5，P0-a 一并处理）
1. **`exam_plan_courses` 老数据有重复行**：多个旧场次明细行数 = 逐题数 **×2**（`sch_1788400121` 20 行/10 题、`sch_1788478231` 38 行/19 题、`ep_17893587520` 4 行/1 题）。根因之一：**旧数据 `question_id` 为空**（83 题里仅 37 题有 `questionId`）→ 去重键失效、`COUNT(DISTINCT question_id)` 恒为 1。→ P0-a 按 `(plan_id, course_uuid, question_id, seq)` 去重（`question_id` 空的行只能按 `(plan_id, course_uuid, seq)`）。
2. **`knowledgePointId` 缺失严重**：83 题里仅 **31 题**带知识点（`knowledgePointName` 31、`questionId` 37）→ 印证老数据"分知识点统计"必然漏数，靠 §4.2③ 回退家长库 `course_knowledge_questions` 兜底。

### 状态
- ISSUE-135 头部状态改为「✅ 规划定稿、**待拍板清零，可开工**」；§9 改为「待拍板：已清零」表 + 「开工前置：无，P0-a 可直接开工」；§10 加 08:31 变更行；`ISSUES.md` #135 行同步（D13 保留 + 追加 ⑬⑭⑮）。
- 仍未改任何业务代码。

## 出治本方案（ISSUE-134 / DESIGN-tool-arg-coercion-2026-09-23.md）——方案 C 定案，待拍板

用户要「治本方案」。核心是找到 SDK 官方为**同一问题**预留的钩子，而不是自己造轮子：

**关键发现**（读 SDK 源码）：
- `pi-agent-core/dist/agent-loop.js:403-404`：`prepareToolCallArguments(tool, toolCall)` **在参数校验之前**执行，
  随后才 `validateToolArguments`；`prepared.args` 一路传给 `execute`。
- `types.d.ts:346`：`prepareArguments?: (args: unknown) => Static<TParameters>` —— **公开类型字段**
  （注释原话 "Optional compatibility shim for raw tool-call arguments before schema validation"）。
- `agent-loop.js:445-451`：`prepareArguments` **抛错会被转成模型可见的 isError 工具结果** → 可读错误有了着落。
- **官方先例**：内置 `edit` 工具就是这么修的，注释写着
  「Some models (Opus 4.6, GLM-5.1) send edits as a JSON string instead of an array」。
- 包装层不透传丢失：`wrapToolDefinition` / `createToolDefinitionFromAgentTool` 都保留 `prepareArguments`。
- `utils/deferred-tools.js` 不涉及参数校验 → 校验只有 agent-loop 一条路径。

**方案 C（定案）**：包一层 `defineTool`（新增 `server/src/agent/tool-kit.ts`）注入 `prepareArguments`，
校验前把字符串化参数还原成结构。**schema 一字不改**（不动发给 provider 的 schema/描述/提示词体积），
只需 14 个工具文件改 import，`packages/agent-core` 无需改。逐参数补丁（`tool-shapes.ts` + 执行器 parse）
保留作第二/第三道保险（幂等、成本≈0）。

**还原规则护栏**：白名单式下沉——只在 schema **明确声明** object/array 的节点 parse，parse 后类型必须吻合；
`additionalProperties: {}`（`where` 的值、`Type.Unknown`）视为 unknown **不解析** ⇒ `answer`/`scoring`
这类「内容形如 JSON 的普通文本」永不被改写。返回**新对象**，模型原始输出仍如实落盘（便于日后诊断）。

**PoC 已跑**（`tmp/poc-prepare-arguments.mjs`，不进产品代码）：9 用例 —— 基线（无钩子）复现现场三条报错；
字符串‑数组/字符串‑对象/真对象/非法 JSON→可读原因/`items` 深长嵌套/**嵌套内层**字符串/两条误伤防护 = 全部符合预期。

**对比备选**：A 逐参数补丁（现状，已漏 6 个参数）/ B 递归改 schema（会改 provider 看到的 schema，弃）/
D 换模型（仅临时止血）。

**产物**：`DESIGN-tool-arg-coercion-2026-09-23.md`（设计稿）、`ISSUES/ISSUE-134.md`、`ISSUES.md` #134 行。
**未动任何产品代码**（按「先讨论方案、再动手」）。待用户拍板：是否实施 / 是否先换模型止血 / 逐参数补丁是否收敛。

**归档调整（用户要求「记录到 issue 里，不要单独一个文件」）**：原独立设计稿 `DESIGN-tool-arg-coercion-2026-09-23.md`
**全文已并入 `ISSUES/ISSUE-134.md`（含 SDK 源码证据、A/B/C/D 方案对比、还原规则与四道护栏、9 用例 PoC 结果、
P0~P3 步骤、风险表、验收标准、待拍板三项），文件已删除**；`MEMORY.md` 的索引行/文档清单/索引校正已同步清理，
归档约定扩大为「**功能规划与治本方案一律写成 ISSUE，不再单独出文件**」。

## ISSUE-134 实施（P0~P2 完成，未部署）——用户拍板：实施 / 不换模型 / 保留逐参数补丁

**新增**：`server/src/agent/tool-kit.ts`（统一还原层：`coerceToolArgs` + 包装器 `defineTool`）、
`test/issue134-arg-coercion.test.ts`（19 用例）。
**改 import**：14 个工具文件（agent 12 + worker 2）把 `defineTool` 从 SDK 切到 `tool-kit`；
其中 `parent-registry`/`session-registry`/`programming-agent` 是多行导入，已把 `defineTool` 从中摘出。
`packages/agent-core` 无需改（只透传 customTools）。

**与设计的两点差异（都记进 issue）**：
1. 包装器类型用 `as typeof sdkDefineTool`（内部走 `any`）——**SDK 自带一份 typebox、服务端另有一份**，
   直接写泛型签名会因 `Static<TParams>` 跨包不同源爆一屏类型噪音；这样 14 个文件只改 import 一行。
2. 加固：工具**自带** `prepareArguments` 时（SDK 内置工具如 edit 就有）先跑它再跑本层，不覆盖别人的兼容逻辑。

**验证**：issue134 19/19 绿（单元 11：还原规则 + **3 条误伤防护**（`answer` 含 JSON / 顶层 note / `where` 的 Unknown 值）
+ 不改入参返回新对象；集成 8：**三个构造器的每个工具都挂上了 prepareArguments**（缺失清单为空）+
`parent_db_write` rows 字符串真改库成功 + `items` 字符串真落库 + `days`(含内层 content)/`courses`/`methodSpec`(含内层 require+exclude)/`titles` 全部校验通过）。
**全量对照基线零新增失败**：改动前后都是 10 文件 / 19 用例失败（同一批既有环境/历史问题），通过数 427 → 449。
`tsc --noEmit` 0 错；`scripts/build.mjs` 成功，bundle 已含 `coerceToolArgs`/`prepareArguments`。
**未部署 201**（用户明确先不部署），server 版本仍 0.5.4。

## git 提交 + 部署 201（0.5.5，09:05）

**提交（本地 master，未 push）**：
- `08f45a5` chore: 补提交既往会话未入版本库的源码与测试 —— 发现上个会话只提交了一部分，
  **ISSUE-129 的两个新文件（`server/src/db/token-usage.ts`、`server/src/routes/token-usage.ts`）
  一直没入库**（而 0.5.4 已经部署到 201 了！），另有 ISSUE-132 题库前后端/shim/测试、ISSUE-135 计划稿。
  这次补齐，使「版本库 = 部署产物」这条基线恢复。
- `2b12265` fix: ISSUE-133/134 工具参数序列化治本（server 0.5.5）。

**版本号坑（记一笔）**：`SERVER_VERSION` 的**真源是 `server/src/routes/version.ts`**（客户端版本协商用），
`server/package.json` 的 version 只喂 esbuild banner。只改 package.json 时 bundle 里搜不到 `"0.5.5"`，
差点误判。**两处都要 bump**。

**部署**：`tmp/deploy/deploy_055.sh` + `deploy_server_055.py`（模板照 0.5.4 改，bundle 24.4MB）。
- stop 约 4 秒；备份 bundle `server.cjs.bak-20260923-0905` + 数据 `data/backups/deploy-0.5.5-20260923-0905/`
  （server.sqlite / agents.sqlite 在线 VACUUM 快照 + .secret / server-config / app-settings / license）。
- 验证：`/api/v1/version` 0.5.4 → **0.5.5**；health `{ok:true,db:ok}`；bundle 标记
  `coerceToolArgs`×2/`prepareArguments`×8/`JsonArrayParam`×2/token_usage×9/`assess/questions/facets`×1；
  新路由 401（非 404）＝已注册；`journalctl` ERR_COUNT=0。
- **本次 bundle 还带上了工作树里既往未部署的改动**：ISSUE-132 题库接口、ISSUE-129 两个文件（已随之入库）。
- 数据目录已 2.7G（备份要注意空间）。
- 文档同步：`MEMORY.md` 环境备忘（当前已部署 0.5.5 + 验证口径 + 回滚命令）、`ISSUES.md` #132/133/134
  行状态、`ISSUE-133/134.md` 状态与部署记录、`技术实现文档` §17 #19。

**运维隐患（本次部署时发现）**：201 根分区 **95%（104G/116G，仅剩 6.3G）**。可回收：
`server.cjs.bak-*` 36 个共 686M（09-11 起全部历史 bundle）、`data/backups/` 790M、`data/` 2.7G。
已写进 `MEMORY.md` 环境备忘；**清理属删除操作，等用户点头再做**（建议保留最近 3~5 个）。

## ISSUE-135 P0-a 实施（09:30~10:15，未部署）——考核结果重构

用户指令「处理 issue 135」。按 ISSUE-135 §7 实施顺序做 **P0-a（最大一块）+ P0 结构 + P1 防硬删**。

### 落地清单（14 个文件）

**新增**：`server/src/exam-results.ts`（`persistExamResult()` —— 一次提交写全孩子库三层 + 评测存档 + 计划回填；
课程 uuid 三跳解析：孩子库 → 家长库 → 回写孩子库；知识点缺失时回退家长库 `course_knowledge_questions`）、
`test/issue135-exam-results.test.ts`（5 用例）。

**改**：`db/kb.ts`（4 新表 `exam_course_results`/`knowledge_point_records`/`knowledge_point_progress`/`speech_assessments`；
`exam_plan_courses` 删 `score` 补 9 列 + 老库幂等补列/删列/按 `(plan_id,course_uuid,question_id,seq)` 去重（meta 游标）；
`courses` 掌握四列 + `study_plans.result_summary`；`course_progress` 视图改读 `exam_course_results`）；
`db.ts`（主库 `exam_attempts`/`speech_assessments` 改幂等 DROP）；`routes/exam.ts`（提交端点改调 `persistExamResult` +
错题本同步前移；`GET /exam/attempts` 改为孩子库三表现场组装、**响应形状不变**；`course-records`/`listCourseStatus`/
`lastExamAtByCourse`/`latestReinforcePlan`/`assess/questions/:id/records` 全切孩子库）；`worker/plan-domain.ts`
（`applyExamAttempts` → 单库幂等兜底 `DO NOTHING`；`computeExamRate` 改「概要 → 明细」单库回退；删 `attemptDetailSums`）；
`routes/study-plans.ts` + `agent/parent-plans.ts`（P1 已完成计划禁硬删改软删 + `readStudyPlans` 过滤取消行）；
`agent/db-channel.ts`（注册表同步：新表登记 + 列清单更新）；`test/issue112-exam-rate.test.ts`（改写口径）；
3 处注释（`src/components/CourseDetail.tsx`/`ChildExamPlans.tsx`、`web/src/shim/domains/exam.ts`）。

**验证**：`tsc --noEmit` 0 错；`scripts/build.mjs` 通过（bundle 已含改动，版本仍 0.5.5）；`probe:registry-drift` **无漂移**；
相关 7 测试文件 56 用例全绿；全量 481 用例 → 458 过 / 15 失败 / 8 跳过，失败清单**全部既有无关**
（assessment / assess-guide / event-poll-config / kb-sqlite(旧 electron 版) / page-bridge / sync / token-stats / english-course-session）。
**未部署 201**（用户未同意）。

**文档同步**：`ISSUES/ISSUE-135.md`（状态改「P0-a 已实施」+ 新增 §11 实施记录：清单/未做/取舍与实测）、
`ISSUES.md` #135 行、`技术实现文档`（30 处逐行更新 + 新增 §18「变更（2026-09-23 · ISSUE-135 P0-a）」）。

### ⚠️ 事故：`git stash` 运行后 `.git` 对象库与 refs 被清空（**待用户处理**）

为做"改动前基线对照"，我执行了 `git stash push -- <paths>` 想临时撤回改动跑一次全量测试，结果：

- git 报 `fatal: 633a64… is not a valid object`，随后 `git status` 报 `fatal: not a git repository`；
- 现场核查（bash + Glob 双工具一致确认）：**`.git/refs/` 整个目录消失、`.git/objects/**` 只剩空目录（0 个对象文件）、
  `.git/objects/pack/` 只剩 `pack-*.idx`（`.pack` 数据文件不存在）、`.git` 总大小仅 987K**；
- **工作树完好无损**（所有源码 + 本次改动都在，已另行备份到 `tmp/iss135-backup/`）；
- 已用 `mkdir` 重建 `.git/refs/heads` 并写入 `refs/heads/master=2573c12…`（取 `.git/logs/HEAD` reflog 末行），
  但 `git status` 报 `bad object HEAD` → 该 commit 对象已不在对象库。
- **未提交的本地改动与本地提交对象可能已丢失**；两个 remote 仍在 config 里：
  `github = git@github.com:shawn-feng/learning-app.git`、`origin = git@gitee.com:nanchang-um-um-network/learning-partner.git`；
  `git ls-remote` 实测两端 master 均为 `2d98290…`（与本地 reflog 末提交 `2573c12…` **不同**）。
- **教训：本环境不要用 `git stash`**（疑似触发 `gc --auto` 打包 + 剪枝，而 `.git/objects/pack` 的写入未落盘 →
  新 pack 缺失、旧松散对象已被剪枝）。后续如需临时撤回改动，用**复制文件到 tmp/** 的方式，别动 git。
- 恢复建议（**待用户拍板后再动**）：`git init`（补齐缺失目录，不删对象）→ `git fetch github` → 用 fetch 到的 ref
  重建本地 master → 再把工作树（含本次改动）作为新提交。**未在本轮擅自执行**。

### 10:37 精确诊断（回答用户「是所有提交记录都没了，还是只有本次工作区的？」）

| 事实 | 结论 |
|---|---|
| `.git/objects` 只剩 `info/commit-graph` + `pack/*.idx`（60KB，Sep 3），**`.pack` 数据文件缺失**、松散对象 **0 个** | 本地**读不到任何 commit**（不只本次），但**不代表永久丢失** |
| 元数据仍在：`.git/logs/HEAD`（**243 条 reflog**，2026-09-03 ~ 09-23 09:07，含全部 commit 消息+时间戳）、`packed-refs`（旧 master + 5 个 tag + remotes）、`filter-repo/commit-map`（144 行）、`ORIG_HEAD`、`COMMIT_EDITMSG` | **所有提交的 hash 与消息都可考**，可据此重建 |
| 远端 github + gitee 两端 master 均为 **`2d98290…`（2026-09-21 19:47）** | 远端是**祖先**且**落后**：**9/21 19:47 之前的历史可完整拉回** |
| reflog 中 `2d98290` 之后有 **16 个本地提交**（cfb7edd 09-21 20:07 … 2573c12 09-23 09:07，含 ISSUE-108/127/128/129/130/131/114/133/134 与今天 5 个） | **这 16 个从未 push** → commit 对象本地丢失、远端没有；**内容仍在工作区**，消息在 reflog |
| 工作区文件全部完好（含本次 ISSUE-135 改动），并另存 `tmp/iss135-backup/`（13 文件） | **本次工作成果零丢失** |

**可恢复程度结论**：① `2d98290` 及之前的全部历史 → 远端可拉回（哈希不变）；② 之后 16 个提交 → 对象不可恢复，
但可用工作区内容 + reflog 消息**重建为新提交**（哈希会变）。

### 10:45 git 修复完成（方案 A：以远端为基线重建）

用户拍板「做 A」。执行步骤与结果：

1. **备份**：`.git` 整目录 + reflog + packed-refs + pack.idx → `tmp/git-backup-20260923/`（含 `reflog-HEAD.txt`
   243 条原始记录、`reflog-HEAD.final.txt` 247 条）；本次 ISSUE-135 的 13 个改动文件另存 `tmp/iss135-backup/`。
2. `git config gc.auto 0` —— **关掉自动 gc/剪枝**（本次事故疑似成因），已留存于仓库配置。
3. `git init` 补缺目录（不删对象）；删掉指向已丢失对象的 `refs/heads/master`。
4. **fetch 首次失败**：`fatal: You are attempting to fetch cf3a5131…, which is in the commit graph file but not in
   the object database` → 清掉失效的 `.git/packed-refs`（备份已存）、`.git/objects/info/commit-graph`、
   `.git/index` 三个残留物后，`git fetch --no-auto-gc --refetch github` **成功**（拉回全部对象 + 全部 tag v0.1.x）。
5. `git update-ref refs/heads/master 2d98290` + `git reset --mixed`（不动工作区）→ `git status` 显示
   **66 M / 4 D / 97 ??**，与「16 个提交的累积内容 + ISSUE-135 改动」完全吻合（4 个 D 也对应
   ISSUE-130 删除的三个组件 + ISSUE-129 后的 `electron/lib/token-stats.ts`）。
6. **两次提交**：
   - `6b038d9` chore: 重建 2026-09-21 19:47 之后的工作区内容（原提交对象已丢失）——66 M + 4 D + 33 A =
     103 文件 / +10957 −2726；提交消息里**抄录了 16 个丢失提交的短哈希 + 完整消息**（trip 可追溯）。
   - `53f954b` feat: ISSUE-135 P0-a 考核结果重构——3 个纯新增文件（`server/src/exam-results.ts` /
     `test/issue135-exam-results.test.ts` / `ISSUES/ISSUE-135.md`）。
   - 说明：被两批工作先后改过的文件（kb.ts / exam.ts / db.ts / plan-domain.ts 等）内容无法拆分，统一进
     `6b038d9` 并在其消息里注明。
7. **推送**：`git push github master` + `git push origin master` 均 fast-forward（无 force），
   两端 master 现均为 **`53f954b`**。
8. **收尾**：死 reflog 条目（指向 16 个已丢失提交，导致 `git fsck` 报 invalid reflog entry）清掉 ——
   `.git/logs/HEAD` 重置，原文件留档 2 份；`git fsck` 现已 **0 错**；本地历史 358 个提交（含 2d98290 及之前全部）。
9. **未入库**：仓库根 61 个本地临时产物（`_*.py`/日志/`artifacts-*`/`tmp/` 等，原本也未入库）保持未跟踪；
   已排除 `aliyun-aksk.txt`（gitignore 命中）等敏感文件，未误提交。`master` 未设 upstream（双远端，推送需显式指定）。

**代价（与事前说明一致）**：9/21 19:47 之后的工作失去提交级粒度 —— 11 个 issue（108/114/123/127/128/129/130/
131/132/133/134）与 3 个已部署版本（0.5.3/0.5.4/0.5.5）不再有独立提交与原始 diff；文档里引用的
`08f45a5`/`2b12265`/`16f7550`/`2573c12` 等 hash 已永久失效（其内容现归 `6b038d9`）。

## ISSUE-135 P4 改向 + 实施（11:00~11:20）：掌握闭环归纳 = 「自定义任务 + 自然语言指令 + 工具」

用户指令：「定时任务做成自定义的，用自然语言描述任务内容，调用工具完成操作。app 当前已经有自定义任务功能了。」

### 调研结论（先摸清现有机制，再定方案）

- **已有**：客户端「定时任务」页已支持 `type='custom'` + 自然语言「任务指令」文本框 + 频率/时刻 + 分配孩子（`SchedulerTasksPanel.tsx`）；
  服务端 `executeCustomTask()` 到点起无头 ephemeral 会话（每孩子一轮、5 分钟看门狗），末轮文本写 `task_runs`。
- **现成通用数据工具**：`createDataAgentTools()` → `parent_db_describe/read/write`，`child=` 作用域下**管理口径全表可写**，
  正好覆盖 P0-a 新登记的结果四表 + 掌握四列（`db-channel.ts` 已同步注册）。
- **缺口**：自定义任务 agent 白名单只有 `get_date/kb_query/kb_insert/kb_update/weather_query/create_reminders`，
  其中 `kb_*` 是曲目式的（只支持 daily/course/tags，`kb_update course` 明确改不了掌握四列）。
- ⇒ **原设计的 `progressAnalysis` 任务类型 + `cfg.analysis` 配置 + 客户端类型下拉全部不需要**（那是要新造机器）。

### 用户拍板：工具面选「混合」（通用读写 + 4 个专用工具）

- 复用 `parent_db_read/write`（agent 可自查、出错能自愈）+ 4 个**确定性**专用工具（取数与写回固化在代码，LLM 只出判断与措辞）。

### 实施（3 改 2 新 + 文档）

- **新增 `server/src/worker/mastery-tools.ts`**：`mastery_todo_list`（待归纳学习计划 + 需刷新掌握的课程，`days` 默认 3、0=全量补跑）、
  `mastery_plan_context`（一次给全素材：知识点 id 清单/计划窗口内学习记录原文/已有记录/历史掌握/错题本未掌握项）、
  `mastery_save_records`（幂等 UPSERT `knowledge_point_records` + `study_plans.result_summary`）、
  `mastery_save_course_mastery`（`courses` 掌握四列 + `knowledge_point_progress`；**学考次数/最近档位由工具实时统计**）；
  另含 `ensureDefaultMasteryTask()`（默认任务播种）与 `DEFAULT_MASTERY_TASK_INSTRUCTION`（自然语言指令）。
- **改 `worker/custom-tasks.ts`**：自定义任务 agent 工具面 = 原有 ∪ 4 个 mastery ∪ `parent_db_describe/read/write`
  （`customTaskToolNames()` 固化并集——**SDK 的 `tools` 白名单对自定义工具同样生效，漏登记=工具静默不可见**）；
  系统提示补「掌握分析类任务用 mastery_*，不要自己写这些表的 SQL」；tick 里顺便播种默认任务。
- **改 `routes/scheduler.ts`**：`GET /tasks` 幂等播种默认任务（打开 UI 即见）。
- **新增 `test/issue135-mastery-task.test.ts`**（6 用例）：白名单契约 / 默认任务播种（幂等、删后不重建、新孩子补分配）/
  todo_list / plan_context / save_records（幂等 + 非法输入被拒）/ save_course_mastery（四列 + 累计计数）。
- **文档**：ISSUE-135 §4.3 重写（原 `progressAnalysis` 方案作废留档）+ §7 的 P3/P4 行 + §11.1 第 13 行 + 新增 §11.4 + 变更记录；
  `ISSUES.md` #135 加 ⑰；`技术实现文档` 新增 §18.1。

### 单测逼出来的两个真问题（已修）

1. **`cut()` 会补省略号**：`cut("2026-09-23 00:00:00", 10)` → `"2026-09-23…"`，拼进 SQL 日期比较**永远匹配不上**
   （daily 窗口查询恒返回 0 行）→ 拆出 `dayOf()` 专供日期，`cut()` 只用于展示。
2. **`exam_plans` 没有 `topic_key` 列**（只有 `exam_plan_courses`/`exam_course_results`/`knowledge_point_records` 有）
   → 照抄 `study_plans` 的取法会直接报 `no such column`。

### 验证

`tsc --noEmit` 0 错；`scripts/build.mjs` 通过；相关 5 文件 **46 用例全绿**（issue116 定制任务 + issue135×2 + issue112 + issue134）；
全量 **464 过 / 15 失败 / 8 跳过**，失败文件与 P0-a 那次**完全一致**（assessment / assess-guide / event-poll-config /
kb-sqlite(旧 electron 版) / page-bridge / sync / token-stats / english-course-session）＝ 零新增失败。**未部署**。

---

## 部署 201：0.5.6 → 0.5.7（11:20~11:45，用户明确同意后执行）

### 0.5.6（11:28~11:29）——含 ISSUE-135 P0-a + P4

- 版本 0.5.5 → 0.5.6（`server/src/routes/version.ts` + `server/package.json`）+ 重新构建 bundle。
- 部署脚本 `tmp/deploy/deploy_056.sh` + `deploy_server_056.py`（照 `deploy_055.sh` 模板）：
  stop → 备份 bundle `server.cjs.bak-20260923-1128` → 备份数据 `data/backups/deploy-0.5.6-20260923-1128/`
  （含 `server.sqlite`/`agents.sqlite` VACUUM INTO 快照 + `.secret`/`server-config.json` 等）→ 换 bundle → start → 探针。
- 结构核对 `tmp/deploy/verify_056.js`：**主库 `exam_attempts`(19 行)/`speech_assessments` 已 DROP**；
  默认「学习情况分析」任务已播种（`mastery_task_seeded:86a84278…`，assignments 7 行）；
  **两个活跃孩子库**（闻闻 `09406c05` / 珊珊 `1f050a7f`）四张新表 + `courses` 掌握四列 + `study_plans.result_summary` + 明细富字段全到位、
  旧 `score` 列已删；`journalctl` ERR_COUNT=0；磁盘仍 95%/6.3G。
- 另 3 个孩子库未迁移 —— 核实为**孤儿/测试残留**（`4d6e76fc` 在主家长目录但主库 `children` 无此 id；`kb/test-parent/` 两个是测试家长），
  **懒迁移不碰它们属正常**。

### ⚠️ 0.5.6 首次部署即暴露 P0-a 漏网 bug（读取侧线上 500）

- 自签家长 JWT 直连 201（`data/server-config.json` 的 `jwtSecret` + 纯 Node `crypto` HS256，脚本 `tmp/deploy/verify_056_api.js`）
  跑端到端，发现 **`GET /api/v1/exam/attempts/:childId` → 500 `no such column: topic_key`**
  （另一条 `course-records` 200、`courses/status` 200、`schedules` 200、`config` 200）。
- 根因：P0-a 重写读取侧时写了 `SELECT id, title, topic_key, ... FROM exam_plans`，**而 `exam_plans` 没有 `topic_key` 列**
  （只有 `exam_plan_courses`/`exam_course_results`/`knowledge_point_records`/`courses`/`topics` 有）。
- **为什么漏网**：P0-a 的单测只覆盖写入函数 `persistExamResult`；**路由 handler 的 SQL 全仓零覆盖**（全文无 fastify inject 用例）。
  同一个坑在 P4 的 `mastery-tools.ts` 已踩过一次 —— **记住坑没用，要有机制**。
- 另顺手把 `assess/questions/:id/records` 的**静默 `catch {}` 改成 `req.log.warn` **：静默吞错正是这类结构错在页面
  表现为「没数据」、极难排查的根因。

### 0.5.7（11:38~11:40）——修复 + 补路由级测试

- 修复：SELECT 去掉 `topic_key`，`topic` 改从 `exam_course_results.topic_key` 取（`results.find(...)`）。
- **新增 `test/issue135-exam-routes.test.ts`（7 用例）**：真 fastify（`createRequire` 从 `server/` 解析——根 `node_modules` 没有 fastify）
  + 真 sqlite + 真 JWT，覆盖 4 条读接口 + `POST /exam/attempts` 写入口 + 401/403。
  **有效性已验证**：把原 bug 注入回去 → 测试复现同一个 500 → 还原（非假绿）。
- 端到端复验：4 条读接口 + `scheduler/tasks`（含「学习情况分析」任务、指令完整）+ `exam/fixed-config` **全部 200**，
  `attempts=5` 字段齐全（id/childId/topic/title/startedAt/submittedAt/status/score/perQuestion/courseMastery/reinforcePlan/wrongQuestions/scheduleId）。
- 备份：bundle `server.cjs.bak-20260923-1138`、数据 `data/backups/deploy-0.5.7-20260923-1138/`。

### 仍未验证

**P4 的「学习情况分析」自定义任务还没真实跑过一次**（工具白名单 → agent 调工具 → 写库 的端到端链路未验）；
今晚 21:30 会自然触发，明天可查 `task_runs` + `knowledge_point_records` / `courses` 掌握四列是否有写入。

---

## 0.5.8：掌握分析任务改为「显式添加」（12:00~12:10，用户反馈）

**用户问题**：「学习情况分析用定时任务，为什么会在 21 点 30 分自动触发？我还没有设置定时任务。」

**确认是设计问题，不是 bug**：P4 初版按「默认任务 + 家长可配」实现，服务端**替家长创建**了一条每天 21:30、会花 LLM 调用的任务；
播种点两处（任一命中即出现）——worker 每 2 分钟的 custom tick（**不依赖打开 App**）+ `GET /api/v1/scheduler/tasks`。
用户在「定时任务」页看到它，但自己确实没设过。→ 用户决定：**删掉它，并改成不自动播种**。

**修订内容**
- 删除 `ensureDefaultMasteryTask()` 及 settings 标记 `mastery_task_seeded:<pid>`（"只播一次"机制一并去掉）；
  调用点全清（`worker/custom-tasks.ts` 的 tick、`routes/scheduler.ts` 的 GET）。
- 改为**模板 + 显式添加**：`MASTERY_TASK_TEMPLATE`（key `mastery_analysis`）+ `GET /api/v1/scheduler/task-templates`
  （只读、不建行）+ `POST /api/v1/scheduler/tasks { template: "mastery_analysis" }` → `createMasteryTask()`
  （**固定 id `task_mastery_<pid>`** 保证重复点不多建 + 幂等补分配现有孩子；未知模板 400）。
- `mastery_*` / `parent_db_*` 工具**对所有 custom 任务可用**（不依赖那条默认任务存在）→ 家长自建的自定义任务同样能做掌握分析。

**部署顺序（关键）**：本次是「去掉自动行为」，**必须先换 bundle 再删数据行** —— 反了的话旧版 tick 会在 2 分钟内把任务重建出来。
0.5.8 部署流程：换 bundle → 删 `task_mastery_86a84278…` + `mastery_task_seeded:*` → **sleep 135 跨过一个 worker tick** → 复查 `custom=0`（未重建）✓。
无 schema 变更 → 只备份 bundle（`server.cjs.bak-20260923-1205`），未重复备份 2.7G 数据。

**验证**
- 接口级（自签家长 JWT，`tmp/deploy/verify_058_api.js`）：列表 custom=0（连开两次）→ `GET /task-templates` 200（指令 1000 字、不建行）→
  POST `{template}` created=true → 重复 POST created=false → 未知模板 400 → DELETE 后回到 custom=0（**最终状态就是"没有这条任务"**）。
- 测试：`tsc` 0 错；`issue135-mastery-task` 用例②改写后 6/6 绿；新增 `issue135-scheduler-task-template.test.ts` 6/6 绿
  （真 fastify inject：列表不自动建 / 模板只读 / 显式添加 / 幂等 / 400 / 401）。

**未做（已在 ISSUE-135 §11.6 记为前端待办）**：客户端「定时任务」页还没有「推荐任务」入口，家长要添加得手填名称/时刻/整段指令；
要一键添加需改 `SchedulerTasksPanel.tsx` 并重打客户端包（201 当前 0.1.15）。
