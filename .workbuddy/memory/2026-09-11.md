# 2026-09-11 工作日志


## 16:00 完成 ISSUE-076 实施：判定改读 daily + 写入校验 + 数据修正 + 上生产
- 按用户三决定落地：① missed 不回溯；② 学习记录写入时校验课程名（去 courses 查，不准则报详情错提醒 agent）；③ 删除 recording-prompt 已下线列（掌握度/首次学习）引用。
- 数据修正（201 两孩子库 daily 标题规范化）：**更新 66 条 + 合并重复 9 条**（`课程名（复习）→课程名`），253 条旧格式（4~7 月「论语·巧言令色（学而篇第三章）」「YYYY-MM-DD 学习记录」）不动；备份 `backups/post-daily-title-fix-*`。注意 daily 主键 (date,block,title)，同日重复用「追加 raw + 删重复」合并。
- 代码改动：① `plan-domain.ts` applySignals 学习域 `courses.last_review` → `daily_entries(block='学习')`（标题=course_name 精确 + date∈窗口），保留回写 plan_id；② `routes/db.ts` insertMany 加 `block='学习'` 课程名校验（否则 throw 提醒 agent）；③ `recording-prompt.ts` 清「掌握度/首次学习」字段白名单 + 标题禁后缀 + 「学习状态」措辞对齐。
- 构建 0.3.5 部署 201（备份 server.cjs.bak-20260911-155831）；**验证：珊珊 09-11 为政第三章 pending→done（done_at 09-11 12:00:00）**，无 daily 的仍 pending 无误判；服务 active、0 error。
- 待办：`recording-prompt.ts` 的改动随**客户端打包**生效（服务端已生效）；`last_review` 字段仍在考核候选/course_status 使用，其写入侧问题单列（不在本次范围）。

## 15:45 检查「判定应读 daily 而非 courses」（用户纠偏）→ ISSUE-076 补 §6 改造方案
- **核实**：本地与 201 是同一份代码 —— `server/src/worker/plan-domain.ts:191-194` 与线上产物 `server/dist/server.cjs:374737` 逐字相同，都是 `SELECT ... last_review AS learned_at FROM courses WHERE last_review != ''`（旧口径），**尚未实现与 daily 比对**。
- **目标口径（用户定）**：学习域判定改为读 `daily_entries`（block='学习'）——「标题=课程名」且「daily.date ∈ 计划的 [start_at, due_at]」，**不再读 courses**（与生活域同构）。
- **daily 侧质量实测（珊珊 633 条学习条目）**：标题严格=课程名仅 **106（16.7%）**；带「（…）」后缀可去后缀匹配 37；**不匹配 490（77.4%，几乎全为 4~7 月旧格式**如「论语·巧言令色（学而篇第三章）」）。9 月以来基本规范，但**复习类带「（复习）」后缀** → 严格全等会漏匹配。
- **试算（174 条计划行）**：改口径后新判 done 4 条，含关键的 **为政篇第三章（09-11 窗口 + 09-11 daily）→ 立刻变 done**；无 daily 的 6 条仍 pending，**无误判**。
- **3 个注意点**：① 历史 missed 不回溯（applySignals 只处理 pending，过期行已 missed）；② 后缀取法需择一（A 判定容忍后缀+收紧写入 / B 严格全等+先规范化存量）；③ 写入侧 `recording-prompt.ts` 需禁止标题状态后缀并清理已删除列引用。
- 待用户拍板后实施；未改任何代码/数据。

## 15:35 补全「计划表状态更新机制」并修正 ISSUE-076 归因（commit 下次提交）
- 按用户指正重新梳理：**不存在 todolist 表**，`/plans/today` 直接查三张计划表 + `active=1` + 窗口覆盖当天，行上 `status` 即对勾（客户端 `todo:get` 走同一接口，不读本地表）。
- 补全四条更新路径：①创建（`expandRecurrences` 每 2 分钟展开 `plan_recurrences` / agent 工具 `plan_life|plan_study|plan_exam` / carry 复制）；②`pending→done` 三域信号（学习=`courses.last_review` 落窗口；考核=主库 `exam_attempts` 按 schedule_id 挂接；生活=`daily_entries.plan_id+plan_outcome='done'`）；③`pending→missed`+复制新行（`due_at < now` 时刻，故当天行次日才落 missed，exam 不 carry）；④家长审计 `POST /plans/status`（孩子端无打勾入口）。
- **修正上一条归因**：珊珊 09-11 的 7 条 pending **全部**是 `last_review='-'`，与「无有效日期」100% 一一对应（2/9 章有 09-11 日期→done）；「复习不刷新日期」是与设计冲突的并存缺陷，但对本次现象非直接原因。
- **关键新证据**：`electron/lib/exam.ts:5` 明示考核「不再回写孩子库」，服务端全库无 `UPDATE courses SET last_review` → **该字段唯一写入方是 AI 调 `kb_update`**。
- 排除项三条：非迁移所致（升级前备份对照）；非 worker 停止（日志显示 09-11 12:54/13:12/13:20 仍在判定）；`worker_state` 的 todo_stat/study_plan_carry 停在 09-10 是新版不再写游标（旧残留），非故障。

## 15:25 定位「daily 有学习记录但 todolist 未完成」→ 登记 ISSUE-076（高）
- 现场（201 生产·珊珊）：09-11 daily 有 为政 3/6/7/8 章复习记录，但当日 todolist（`/plans/today` 实调 9 条）里 3 章 pending，6/7/8 章压根不在列表（其计划行是 09-10 的、已 done，done 不 carry）。
- 根因三层：① **todolist 完成状态与 daily 是两条独立链路** —— 打勾只读 `study_plans.status`，判定只认 `courses.last_review`（`plan-domain.ts:186-219`），daily 仅被「回写」和「生活域判定」消费；② `last_review='-'`（✅ 无日期）被 `WHERE last_review != ''` 捞出后 `dateOf('-')='-'` 与窗口比较恒 false → **永远判不了 done**，珊珊库这类课 **383/613（62.5%）**；③ `last_review` 的写入**无确定性代码**，全靠 AI 是否记得在多传字段（`pi-session.ts:108` 只要求写「状态」），复习时普遍不写 → 与 DESIGN-plan-domain-rewrite §289「学或复习都更新」直接冲突。
- 旁证：升级前备份对照证明 `first_learned` 本就为空、`last_review` 本就为 '-'，**非本次迁移造成**；旧表 `todo_items`（74 行）升级后不再更新，09-11 的 9 行仍全 pending，若 UI 误读会显示「全未完成」。
- 产出：`ISSUES/ISSUE-076.md`（含三层根因、影响面：完成率/积分门控/考核候选、4 项修复建议、4 个待确认）+ 两个可重跑探针 `.workbuddy/tmp/probe_todolist.js`、`probe_lastreview.js`。**未改代码、未改数据。**

## 07:35 plan_create→plan_life 改名 + 家长生活计划工具（commit 4d7d76b）
- 改名：孩子生活工具 plan_create → plan_life（孩子三工具统一 plan_life/plan_study/plan_exam）。
- 服务端 /plans/life 参数化 creator：child(默认,optional)/parent(required)；去重按 creator+title+date。
- 新增家长工具 parent_plan_create（childName + days[{date,title,time?}]，走 /plans/life creator='parent'）；家长白名单 + 提示词 2.5b 生活计划节。
- 家长三域工具齐备核对：学习 study_plan_create(/study-plans→孩子kb新表)、考核 exam_schedule_create(/exam/schedules 主库排期真源→提交回写 exam_plans)、生活 parent_plan_create(新增)。
- 验证：server tsc 0 错、根 tsc 无新增错误。

## 10:16 修订《学习伙伴-用户使用说明书》10 处（按用户反馈）
- 问题1/6：课程详情页只展示 教学方法/标签/考核要点；逐题考核记录与掌握度移至「考核记录」页（不再在课程详情展示）。
- 问题2/4：考核计划/学习考核不限于语音考核，含背诵/朗读/选择/问答多题型。
- 问题3/10：排计划靠家长/孩子各自 AI 对话操作；孩子端不能手工勾选任何计划（学习/考核/生活均由系统按条件自动判定打勾）；孩子也可自定学习/考核/生活计划→「加分项」。
- 问题5：题库由「考核类别」改为「知识点」概念（题目↔知识点，知识点→课程→主题）。
- 问题7：§3.3 新增课程组织原则（两层分类：主题→课程，课程是最小学习/考核单位，课程下细分知识点）。
- 问题8：§3.5.1 排考核可指定课程+该课知识点作为抽题范围。
- 问题9：§3.6.1 重写积分计算（todolist 完成率归属日算法、考核每天算加权总分、加分项定义与门控）。
- 备注：本次编辑出现 3 处 Edit 静默未落盘（Q3、§4.5 首句、§4.2 孩子自定计划句），已逐一重放并 grep 复核全部生效。

## 13:29 新建《学习伙伴-技术说明书.md》
- 依据代码真源（server/src/db.ts、db/kb.ts、db/parent-lib.ts、db/assess-content.ts、db/agents.ts、routes/plans-rewards.ts、worker/plan-domain.ts、worker/scheduler.ts、MATERIAL-BRIDGE-PROTOCOL.md）逐表提取 DDL。
- 覆盖：三层架构/真源原则、文件级存储布局、完整数据结构（主库 15 表 + 家长库 topics/courses/tags/meta + 结构化考核 4 表 + 孩子库基础/计划域 5 表/积分域 6 表 + agents 2 表 + 视图 topic_progress/course_progress + 核心 JSON 结构）、功能实现细节（对应使用说明书各章）、REST API 清单、kb RPC op 清单、agent 工具清单、实现红线。
- **发现待确认问题（已文档化 §8.2-A）**：`worker/plan-domain.ts` 的 `settleRewards` 中 `finalOk = now > dayEnd(today)`，而 `now` 与 `today` 同源于 `ctx.now` → 静态分析恒 false，疑似导致积分流水从不写入、积分恒 0（仅写实时 stats）。建议核对并改为「日终结算上一日」或 `due_at < now`。
- 另记：完成率分母实际口径为「当天窗口覆盖的全部 count_in_rate 行（含 pending）」= 代码口径，与 DESIGN-reward-points §2.1「pending 不进分母」不一致，已在 §8.2-B 标注以代码为准。

## 11:0x~11:4x 生产环境（201）升级方案取证 + 演练（未执行，待确认）
- 需求：当前服务端（HEAD `4d7d76b`，含 09-10 计划域重构 S1~S5）与 201 生产数据差异大，需出升级方案与执行步骤。
- **取证（只读探针）**：201 = Ubuntu 24.04 / Node v24.15.0 / systemd `learning-server` / `/opt/learning-server`，跑的是 **0.3.4（9-07 构建）**。主库 schema_version=5、study_plan_items 290（carried37/done61/pending192）、exam_schedules 136（全 pending/started，exam_attempts=**0**）、materials 8381、session_messages 2858；孩子库 2 个（闻闻/珊珊）：courses 1196/1300、daily 263/962、todo_items 38(全parent)/74(child2+parent72)、child_todo_stats 8/9；**孩子库无 uuid、无 11 张新表、daily 无 plan_id/plan_outcome、courses 仍有 mastery/exam_mastery/first_learned**。家长库 courses 1315 **无 uuid**、**assess_rubric 全空 0/1315**、topics 无 method_spec、考核内容 4 表全无。另有孤儿孩子库 4d6e76fc（512 课，不在 children 表）+ test-parent 遗留。
- **差异结论**：主库几乎只差 `study_plan_items.course_uuid` 一列；大头在孩子库（新表/列/数据搬迁）与家长库（uuid/method_spec/考核内容 4 表）。多数结构补齐由新代码 `openDb/openKb/openParentLib` 幂等自动完成，数据搬迁靠 3 个脚本。
- **产出**：`UPGRADE-201-PLAN-DOMAIN-2026-09-11.md`（方案：差异清单/8 步执行/回滚/17 项验收/风险）；`tmp/deploy/upgrade_201_plan_domain.py`（`--precheck` 只读体检 / `--execute` 停服→备份→部署→迁移→验收→起服，验收失败自动回滚）+ `.workbuddy/tmp/deploy201/`（server.cjs.new + 3 个 esbuild 打包的迁移 cjs + snapshot.js + verify-upgrade.js）。
- **演练**：本地用 `server/data/backups/pre-plan-domain-20260910125038`（迁移前旧结构）搭沙盒，跑完整链路全部 exit=0、验收「全部通过」；重跑显示 `跳过(已迁)` 证明幂等。201 只读预检跑出 **17 项待迁移**（预期）。
- **关键结论**：201 无 tsx → 迁移脚本必须 esbuild 打 cjs 后 node 直跑；服务以 root 运行（WAL 属 root）→ 所有写操作走 sudo；迁移含 `DROP COLUMN` → **回滚必须恢复库备份**，仅回退 cjs 会崩；`kb.todo.*` op 已删除 → 旧客户端待办会失效，**服务端升级需与客户端分发同窗口**。
- 待用户决策：①版本号是否 0.3.4→0.3.5 ②是否一并把本地家长库结构化考核内容（题库 3575 题）重映射同步到生产 ③客户端版本确认 ④执行窗口。

## 11:31~11:33 生产环境（201）升级**执行完成**（服务端 0.3.5 上线）
- 用户拍板：先备份再执行；服务端版本升 **0.3.5**；本次**只做服务端**（客户端打包发布由另一任务）；家长库考核内容不同步。
- 动作：`src/routes/version.ts` + `package.json` → 0.3.5 → `node scripts/build.mjs` 重建 → `tmp/deploy/upgrade_201_plan_domain.py --execute`。
- 时间线：11:31 预检/停服/**备份 8 个库 15.4MB → `data/backups/pre-plan-domain-20260911-113102/`（含 manifest.json）**/备份旧 cjs/部署 → 11:32 迁移（学习 290、考核 136、明细 0、生活 2、日统计 34、uuid 2496）→ 验收 → 11:32:44 起服 → 冒烟 `0.3.5` + health ok + **0 条 error 日志**。
- 实测产出与预期完全一致：闻闻 study_plans 116（uuid 111/116）、珊珊 174（170/174）；exam_plans 68/68；life_plans 0/2；courses.uuid 1196/1300 + 家长库 1315/1315；**无 carry 洪水**（过期行 active=0）；**无回溯发分**（points_ledger=0、balance=0）；起服后 stat tick 于 11:32:48 重算 balance 证明 worker 正常。
- ⚠️ **踩坑1：驱动脚本回滚分支失效**。首次验收失败后，Git Bash 下 `python … < /dev/null` 时 `sys.stdin.isatty()` 仍为 True → `input()` 抛 EOFError → **回滚被跳过、生产停在「已迁移未起服」**。已修：改环境变量 `ROLLBACK_ON_FAIL`（默认 yes）+ 去掉 input() + 补 `import os`。
- ⚠️ **踩坑2：验收口径过严 + 回填脚本缺陷**。失败项=主库遗留表 `study_plan_items.course_uuid` 2/290。根因：`backfill-plan-course-uuid.mts` **只按 (topic_key,course_name) 精确匹配**，而生产 288/290 行 topic_key 历史为空 → 假象；且该列**当前代码无读取方**（计划已迁孩子库，`exam.ts:654` 只取 course_name）。已修：①验收改为断言真正有读取方的 **study_plans.course_uuid**（111/116、170/174），遗留表降级 `[参考]`；②回填脚本补「标题唯一命中」兜底（与 migrate-plan-domain 同口径）→ 生产重跑 **279/288**（余 9 行是家长库已无的「（上）/（下）」后缀课程）。
- 处置选择：判为「非数据故障」→ **fix-forward**（未回滚），修正后重验「全部通过」再起服。
- 回滚点：`data/backups/pre-plan-domain-20260911-113102/` + `server.cjs.bak-20260911-113102`。
- 遗留：客户端升级（另一任务）、家长库考核内容为空、废表未物理删除、孤儿孩子库 4d6e76fc 未清理。
- 文档：`UPGRADE-201-PLAN-DOMAIN-2026-09-11.md`（含 §11 执行记录/复盘）；PACKAGING.md 基线改为 客户端 0.1.13 / 服务端 0.3.5。

## 14:30~14:42 家长库考核内容同步上生产（桥梁 + 知识点双写）
- 起因：用户发现 201 家长界面「题库」为空 → 核查确认**生产家长库四表全空**（question_bank/topic_categories/ccq/knowledge_points = 0，assess_rubric 0/1315，method_spec 全 `{}`）；内容是本地开发出来的、从未上过 201。
- 用户指令演变：先「类别表不填，其他都填」→ 我查出 `ccq.category_id` 是 `topic_categories` 子键、且 `listCourseContent` 是 **INNER JOIN 类别表**（不填则课程详情考核要点直接空）→ 追问后用户明确目标模型 **主题→课程→知识点→题目**（类别将被知识点取代）→ 拍板 **桥梁+知识点双写**。
- 落地：本地导出 `assess-content-bundle.json`（8.86MB，按 (topic,title) 索引而非 uuid）→ **下载生产家长库副本先完整排练**（3575 挂载 0 失败）→ 上生产单事务导入。
- 结果：`question_bank` 3575（背诵 440/带选项 352）、`topic_categories` 5、`ccq` 3575、**`knowledge_points` 1969（覆盖 489 课，平均 4.0/课）**、`ccq.knowledge_point_id` 回填 3575/3575、`courses.assess_rubric` 489、`topics.method_spec` 1（5 个类别 uuid 全部可解析）。样例课「论语学而篇第一章」走 UI 同款 JOIN 取到 5 条、为政篇第一章 8 条。服务未停、health ok、0 error 日志。
- 备份：`pre-plan-domain-20260911-113102/parent-86a84278.sqlite`（导入前快照，回滚点）+ `post-assess-sync-20260911-144116/parent.sqlite`。
- ⚠️ **自纠1**：备份步骤用 `node -e` 一行式经 SSH 执行时**引号被 shell 吞掉、VACUUM 静默未执行**（mkdir 成功造成"有备份目录"的假象）→ 靠 11:31 快照兜底 + 补做 post 备份 + 删除误导性空目录。教训：**跨 SSH 一律上传脚本文件执行，不用带引号的 node 一行式**（与 PACKAGING §8 坑15 同类）。
- ⚠️ **自纠2**：`method_spec` 类别引用校验用了 `[0-9a-f]{32}`（无连字符）正则，实际 id 是带连字符 uuid → 匹配 0 条却报「✓ 全部解析成功」，**假通过**。改标准 uuid 正则后确认真实结果（5 个类别 uuid 全在，唯一"缺失"是我的正则把孩子 id 也匹配进去了）。
- **待办（已写入 UPGRADE-201 文档 §12.3，未登记 ISSUES）**：①`server/src/db/assess-content.ts:372` `listAllBankQuestions` 写死 `LIMIT 2000`，而题库 3575 条 → 家长「题库」最多看 2000 题（真 bug）；②类别→知识点的**代码切换**（读取路径 4 处 + electron 端 TopicDetail/QuestionBankPanel 字段），数据已就位，需与客户端排期；③切换后清理 `topic_categories` 5 行。
- 文档：`UPGRADE-201-PLAN-DOMAIN-2026-09-11.md` 新增 §12（方案/结果/待办/自纠），§8.3 改为"已执行"。

## 14:45~15:05 处理上条遗留待办 + 发现独立生产事故（ISSUE-073/074/075）
- **待办①题库上限已修**：`server/src/db/assess-content.ts` `listAllBankQuestions` 写死 `LIMIT 2000` → 抽常量 `BANK_LIST_LIMIT = 10000`（不改响应形状）。生产端到端验证：`GET /assess/questions/list` → **3575 条 / 1.70MB / 118ms**，上下文已带 `knowledgePoint`（证明 §12 的知识点数据在线）。
- **待办②排期截断已修**：`server/src/routes/exam.ts:1068` `LIMIT 100` → `1000`（同属静默截断同类）。tsc 0 错 → 重建 → 停服替换 → 起服，version 0.3.5 / health ok / 0 error。旧件备份 `server.cjs.bak-20260911-144926`。
- **待办③已登记 ISSUE-073**（类别→知识点 代码切换，含逐文件行号：服务端 `assess-content.ts` L17-58/L146-165/L270-290/L297-355/L366+/L400、`assess-selection.ts` L42/L102-152、`routes/exam.ts` L1485-1502/L1517/L1616/L1647、`assess-migrate.ts` L238-241/L290；electron `assess-admin.ts`/`assess-tools.ts`/`assess-guide.ts`/`custom-tools.ts:1058`/`pi-session.ts`；UI `TopicDetail.tsx` L631-693、`QuestionBankPanel.tsx` L47/L104/L166-167、`ExamView.tsx` L61/124-126/228-230/344-346/471-473）。**登记 ISSUE-074**（静默 LIMIT 类问题 + 题库分页设计）。ISSUES.md 计数 71（本就少 1）→ 修正为 75。
- 🔴 **新发现独立生产事故 ISSUE-075（高）**：核查排期接口时发现 `exam_schedules` 从 136 行掉到 **18 行**（每孩子 9 条，全部 created_at=09-03）。定位：主库 `settings.exam_fixed:<pid>` 于 **今天 14:40:52 被写成 `frequencies: []`**（日志显示写入方是 `POST /exam/fixed-config`，**ip 192.168.1.200 = 客户端机器，不是我的脚本**），而 `exam.ts:1247-1256` 的 `changed` 分支执行 `DELETE FROM exam_schedules WHERE kind='fixed' AND status='pending' AND scheduled_at > now` —— **该 DELETE 既无 child_id 也无 parent_id 作用域**（跨家长跨孩子误删），且 `frequencies: []` 让 `ensureFixedSchedules` 首行 `return 0` → **固定档排期从此不再生成（静默停摆）**。
  - 不受影响：孩子库 `exam_plans`（68+68 迁移产物仍在）、`exam_attempts`（生产 0 条）。
  - 可恢复：`ensureFixedSchedules` 是确定性懒生成，配置修好后再打开一次考核页即补齐未来 60 天（至 2026-11-10，覆盖原 11-08 范围）；**不建议**回灌备份的 118 行（会与懒生成按天去重冲突）。
  - **未擅自改用户配置**（涉及孩子的考核设置），已在回复中请用户确认是否恢复 `frequencies=["daily","weekly"]`（依据 09-09 定案「固定档 daily/weekly=plan 周期内必学课全考」；配置里 `time:19:00`、`weekly.weekday:5` 仍在，说明这两档原本开着）。
- 关键教训：**跨 SSH 的 `node -e` 一行式引号不可靠**（本次又踩一次，改用上传脚本文件）；排查接口行为时**必须同时看 DB 现状 + 服务端日志的 ip/时间线**，否则会把「客户端写入导致的删除」误判为「升级导致的数据丢失」。
- 文档：`UPGRADE-201-PLAN-DOMAIN-2026-09-11.md` §12.3 改为「已处理」表 + 新增 §12.4（事故）/§12.5（自纠）。

## 14:58 生产关闭考核（按用户决定：不要任何考核排期）
- 用户要求：**生产里把考核排期全删掉、暂时不开启考核**。
- 执行（脚本 `.workbuddy/tmp/deploy201/exam-off.js`，先 `--dry-run` 验证，再正式）：
  - 备份 → `data/backups/pre-exam-off-2026-09-11T06-58-25-452Z/`（server.sqlite 6244KB + agents + 闻闻/珊珊 kb）。
  - 删：主库 `exam_schedules` **18 行**；孩子 kb `exam_plans` 闻闻 **68** / 珊珊 **68** 行；`exam_plan_courses` 0 行。
  - 删除规则：**只删 `COALESCE(attempt_id,'')=''` 的行**（无真实成绩）→ 真考过的场次一律保留（生产 `exam_attempts=0`，故本次全为排期态，无成绩损失）。
  - 不回归：`GET /exam/schedules/:childId` → `generated=0 schedules=0`，复查主库仍 0（`exam_fixed.frequencies=[]` → `ensureFixedSchedules` 直接 return 0，不会懒生成）。
  - `reward_daily_stats` 全为 `source='todo'` → 无孤儿统计。服务 active / health ok / 0 error。
- **重新开启的前置**（已写入 ISSUE-075）：必须先修 ①`exam.ts:1254` DELETE 缺 child/parent 作用域 ②客户端不得用空 `frequencies` 覆盖服务端配置。重开做法：恢复 `frequencies=["daily","weekly"]` → 打开一次考核页自动铺未来 60 天。

## 11:14 记录 ISSUE-070（仅登记，未处理）
- 背诵/跟读题（`questionType` 有值）一次录音即被锁定为「完成」：根因在 `src/lib/exam-template.ts` `startRec()`→`rec.onstop` 的口语分支在第一段录完就置 `recA.locked=true`（约 L370-376）；口述题（ASR 路径）不锁、可多段拼接。
- 期望：背诵可多段录音拼接、点「下一题」才锁定、页面提示「背错只需再背一遍不必删前面录音」。详情见 ISSUES/ISSUE-070.md，索引已入 ISSUES.md（共 70 条）。

## 14:05 记录 ISSUE-071（家长撤销单条积分变动，仅登记未处理）
- 需求：家长在积分流水可撤销孩子某一条变动，流水备注名「撤销」、撤销后余额重算。
- 数据真源已预留：points_ledger.operator='parent'/reason_code='adjust'（DESIGN-reward-points-2026-09-10.md §五）。
- 入口：前端 src/components/RewardPanel.tsx L313-324（ledger 每行加撤销按钮）；IPC 新增 reward:revoke 参考 plan:setStatus（ipc-handlers.ts L1102）；服务端 /rewards 落点待 grep 确认。
- 待定：是否回滚 reward_daily_stats.points_awarded 快照、撤销范围、是否允许撤销「撤销」。详情见 ISSUES/ISSUE-071.md，索引已入 ISSUES.md（共 71 条）。

## 11:19 处理 ISSUE-070（背诵/跟读题提前锁定）✅ 已实施
- 根因确认：与登记一致——`src/lib/exam-template.ts` `startRec()`→`rec.onstop` 口语分支（`if(q.questionType)`）在**第一段录完即置 `recA.locked=true`**，导致后续补录被 `curLocked()/setBtn()` 阻断。
- 改动（唯一文件 `src/lib/exam-template.ts`）：
  1. 删除口语分支里的提前锁定三行（`answeredAt`/`durationMs`/`locked=true`），改为只 `paintState()`，把本段 `segs.push` 后**不锁**；锁定统一交回 `saveCurrent()`（点「下一题/上一题」切走时）。顺带多段期间本题总用时持续累计。
  2. `renderQuestion()` 背诵/跟读提示语补充明示「可分段录好几遍拼接；背/读错了只需再录一遍，不用删前面的录音」。
- 未改：`saveCurrent()`/`curLocked()/paintState()`、`ExamView.tsx` 的 `voiceMerge` 多段合并（已确认支持）、`rec.onstop` 的 `else` 分支（录音后已切走→立即锁，防漏锁，保留）。
- 验证：提取内联脚本 `node --check` 语法通过（SYNTAX_EXIT=0）；宿主 `voiceMerge` 多段合并评测确认存在。ISSUES.md 索引 070 状态改为「已实施」。
- 待用户实测：201 生产或本地跑一次多段背诵确认。

## 11:31~11:47 客户端打包 0.1.13→0.1.14（仅打包，未发布）
- 按 PACKAGING.md §2.1 稳定配方：版本 0.1.14、output `dist-release-020`、沙箱外执行、默认 AppData 缓存、`ELECTRON_BUILDER_BINARIES_MIRROR`、unset NODE_OPTIONS。
- 产物：`dist-release-020/学习伙伴 Setup 0.1.14.exe`(≈165MB) + `latest.yml` + `.blockmap`，BUILD_EXIT=0。未走发布（未上传 OSS/ECS、未 git push/tag）。
- ⚠️ **本机沙箱打包环境坑（可复用）**：①WorkBuddy Bash 的 `npm` shim 静默返回 exit 1 无输出 → 必须 `node <npm-cli.js> run dist:win`（`C:/Users/79734/.workbuddy/binaries/node/versions/22.22.2-3/node_modules/npm/bin/npm-cli.js`）；②`/tmp` 不可写 → 日志写项目目录；③`rm` shim 坏（`safe_delete_main` 缺失）→ 别用 `rm -rf` 清目录，目标不存在即可跳过；④`wsl.exe` 在程序黑名单，命令结束会报一次策略拦截（无害）。

## 11:48~12:33 客户端 0.1.14 全平台打包 + Ubuntu 客户端部署到 201 ✅
- **Linux/macOS 经 GitHub Actions**：commit `41854ce` + tag `v0.1.14` push 到 `github`（+ `origin` 镜像）→ 触发 `build-linux.yml`(34561501882, success 7m38s) 与 `build-mac.yml`(34561501936, success 1m57s, x64+arm64 两 dmg)。
- 产物下载到 `C:/Users/79734/Downloads/learning-app-0.1.14/`：`learning-app_0.1.14_amd64.deb`(141MB)、`学习伙伴-0.1.14.AppImage`(188MB)、`macos-dmg-x64/学习伙伴-0.1.14-x64.dmg`(178MB)、`macos-dmg-arm64/学习伙伴-0.1.14-arm64.dmg`(170MB)。
- ⚠️ **gh 下载坑**：`gh run download --name linux-x64-installers` 首次报 "The file exists"（前一次被中断的脚本其实已落盘），重跑即幂等覆盖成功；`gh run view --json artifacts` 字段名错（应为 `artifacts` 但 gh 报 Unknown field，不影响 download）。
- **Ubuntu 客户端部署到 201（192.168.1.201）**：脚本 `tmp/deploy/deploy_client_201_014.py`（paramiko 5.0.0，venv）。`dpkg -i /tmp/learning-app_0.1.14_amd64.deb` 成功（0.1.13→0.1.14）；`dpkg -l` 验证 `ii learning-app 0.1.14`；GUI 经 setsid DISPLAY=:0 重启（旧进程 pkill + 新进程 890654 起）。relaunch 日志正常（含 `app.isPackaged: true`）。
- 注：客户端启动日志 `Update for version 0.1.14 is not available (latest version: 0.1.13...)` = 向**公网自动升级 feed** 检查，因公网 `/download/latest.yml` 未发布 0.1.14 所致，**无害**（本地已装 0.1.14）；本次只更新 201 本地、未发布公网。
- 未做：公网 ECS/OSS 发布（§5，需 upload 三件套 + latest-*.yml + curl 拷 ECS）、Windows installer 公网登记、Linux/macOS 公网 feed。

## 14:10 记录 ISSUE-072（自定义考核计划需标题名，仅登记未处理）
- 需求：自定义考核计划要有「标题名」，家长 agent 创建计划时必须填入（勿用日期/「自定义考核」占位）；与现有 note（给孩子的说明）独立。
- 入口链：exam_schedule_create（custom-tools.ts L1051）→ createExamSchedule（exam.ts L185，`scope` 缺 title）→ POST /exam/schedules（服务端落点待 grep 定位）→ 家长端 ExamAdminPanel.tsx（L423-449 列表主标签现只显示日期+L444 note）。
- 待确认：title 与 note 边界、是否必填、服务端 exam_schedules 表字段落点、孩子端排期选择是否同步展示标题。详情见 ISSUES/ISSUE-072.md，索引已入 ISSUES.md（共 72 条）。

## 14:14~14:40 《技术说明书》全量回代码核实（用户追问"是否在真实代码中核实"）
- 方法：把文档里可机械验证的断言全量提取 → 回代码比对（DDL 逐列、路由逐条、op/工具逐名）。
- **结论：结构类内容确实逐条对过代码，功能描述类主要依据设计稿**；新增 §9「文档核实记录」把两者明确分开。
- **核实一致**：§3.1 主库 15 表（含 schema_version=10）、§3.2 家长库 4+4 表 + 视图、§3.3 孩子库基础/计划域 5/积分域 6 + 两视图、§3.4 agents 2 表、§1.3 模块地图文件、§4.9 tick 顺序、§4.10 双评测 provider、§5 全部 REST 端点、§6 query 12 op、§7 42 个工具。
- **本次修正 5 处**：①§4.9 worker cron「每 5 分钟」→ **实为每 2 分钟**（`*/2 * * * *`；5 分钟是 recording 桶匹配，原描述张冠李戴）②§6 exec 漏 `kb.daily_entries.insert`/`kb.daily_entries.update`/`kb.courses.update` 3 个 op（共 14）③§3.3 `reason_code` 枚举误含 `optional`/`adjust`（代码实产 todo_award/todo_deduct/exam_award/exam_deduct/redeem）④`points_ledger.type` 的 `adjust` 全仓未实现 ⑤§2 存储树把 `exam-audit/` 错列在服务端目录（实由客户端 electron/lib/exam-audit.ts 写客户端 dataDir），同时补上服务端实有的 `sessions/<pid>/<cid>/*.jsonl` 镜像与 `logs/server-log.jsonl`。
- **§8.2-A 由「疑似」升级为「已核实缺陷」**：`plan-domain.ts:113 nowStr` 与 `kb-tools.ts:299 formatLocalDate` 均为本地时区、同源 `ctx.now`，`runPlanStat(:558)` 传当天 → `finalOk = now > dayEnd(today)` **恒 false**；`settleRewards` 全仓唯一调用点即此。后果坐实：**积分流水永不写入、points_balance 恒 0**（只更新 reward_daily_stats 实时进度）。**待修**。
- §8.2-B 亦坐实：`computeGroupStats` 分母含 pending（不限 status）。
- 仍未逐条核实的部分（已在 §9.3 标注）：method_spec JSON 结构与优先级、scope_json 细节、PiBridge 五种信封与动作目录、语音评测字数上限/失败模式、公网源 learning-cloud。另记 §3.1 `settings` 表 key 实为 `<parentId>:<key>` 前缀（文档未写明）。

## 14:20~14:45 第二轮补核（原 §9.3 待核项）→ 又修 4 处
- **§3.5 `topics.method_spec` 结构写错**：真源 `assess-selection.ts:86`（读）/ `exam.ts:1712-1738`（写）→ 按孩子覆盖在 **`spec.perChild[childId]`** 下，**不是**顶层 `"<childId>"` 键；优先级「排期级 > perChild > default」正确；`require` 键可用**类别名或类别 uuid**。已改。
- **§3.5 `question_bank.scoring` 形状写错**：真源 `assess-selection.ts serializeScoring` + `QuestionBankPanel.tsx:12-27` → 实为 `dims[].{dim, score, points, note}`，**不是** `{name, weight}`。已改。
- **§4.7 PiBridge 动作目录不准确**：代码可证的宿主能力只有 `tts.speak`（MaterialsPanel:713）/ `lookup`（:475）；`goto-course`/`get-progress`/`submit-answer`/`complete-section`/`request-help`/`self-check` **只是协议文档与注释里的示例名，仓库无动作注册表**（宿主仅透传）。已改。
- **§4.7/§4.8 场景指令多写了 `hide`**：`custom-tools.ts:1737` 枚举实为 6 个 `say/move/act/show/highlight/update`（无 `hide`、无 `end`）；`scene.busy`/`scene.mic.status`/`scene.mic.result` 是**宿主推送状态**不是 agent 指令；页面上行为 `scene.ready`/`item-click`/`mic.press`/`mic.release`。已改。
- **核实一致（本次确认无误）**：PiBridge 五种信封（page-bridge.ts 498/504/528/534/537/541，含 send() 统一附加 seq/ts、page:req 超时 12s、`window.PiBridge` 在 :564）；`scope = {topics?,courses?,note?,methodSpec?}`（exam.ts:951-967 + electron/lib/exam.ts:184，custom 缺 courses 即 400）；SOE 上限 30/60/120/300（tencent-soe.ts:29-35，签名 1h）；SSECP 映射 `cn.pred.score`/`en.sent.score` + warrant 120min + 无长度校验（aliyun-ssecp.ts）。
- **仍存疑**：§4.10 SSECP `core is timeout` 在代码中无字面量（厂商运行时错误码，来自调研）；§1.1 learning-cloud；§4.5 判分三路（未逐行核 exam-engine.ts/ipc-handlers.ts）。
- 文档 §9 已重构为：9.1 第一轮一致项 / 9.2 第一轮修正 / 9.3 第二轮补核 / 9.4 仍未核实。

## 16:51~17:10 按用户三条规则重写《技术说明书》
- 规则：①按使用说明书功能结构排版（数据结构+实现细节放一起）②只写程序行为（工具/库.表.字段/接口），不写函数名与文件行号 ③只写当前状态（删全部历史/迁移/日期/ISSUE 注记）。
- 结构改为：1 系统总览 → 2 家长端 10 节（登录许可/孩子管理/主题课程/三计划/考核管理/积分/家长AI/每日记录/资料库PiBridge/系统设置）→ 3 孩子端 6 节 → 4 通用后台（worker 调度/语音评测）→ 附录 A REST / B RPC / C 工具清单 / D 已知注意点（8 条，行为化描述，保留 finalOk 结算缺陷为注意点 1）。
- 动手前补核：`POST /study-plans` 现直写**孩子库 study_plans**（窗口=当天全天、creator 决定必须完成项/加分项、同日同课去重、按课程名反查 topic_key+uuid）——主库旧表已成纯历史，全文剔除。
- 删除内容：§9 核实记录（属历史）、全部"迁移列/旧表/已下线/ISSUE-NNN/日期"注记、`study_plan_items`、meta 历史键。残留 4 处"旧"均为当前行为正当描述（清空旧挂载/回退路径/提示词历史/内嵌资料回退），grep 验证无 .ts/行号/函数名残留。
- 文件：`学习伙伴-技术说明书.md`（整文重写，约 640 行）。

## 19:31~19:55 技术说明书第二轮调整（用户 4 调整 + 3 疑问，全部回代码核实）
- **重要核实 1：学习完成判定已改 daily 口径**——`applySignals` 注释明写「完成信号 = daily 学习记录，不再读 courses（旧口径、last_review 无确定性写入方）」；判定 = daily_entries(block='学习', title 精确==course_name) 记录日期落窗口 → done，且回写该条目 plan_id+plan_outcome='done'（仅当未关联）。§2.4/§3.2/§4.1 全部改正；courses.last_review 仅保留为进度视图汇总字段。
- **重要核实 2：考核计划现在也会顺延**——`expireAndCarry` 的 specs 包含 exam_plans（代码注释"不 carry"已过时）；顺延标记 = `origin='carry'`，溯源 = `carry_from`=原行 id（仅学习/生活表有该列）。文档原写"考核计划不顺延"已改。
- **家长修正权现状**：`POST /plans/status` + 客户端通道 `planSetStatus`（preload.ts:321）都在，但 src 无任何 UI 调用、custom-tools 无对应工具 → **预留能力，尚未接入**。已如实写入。
- **同日去重不区分制定人**：POST /study-plans 去重键 = topic_key+course_name+mode（无 creator）→ 家长建了同日同课，孩子再建会被跳过。已确认并写入。
- 其余修改：①§2.3 拆分"保存课程"（parent_course_save→courses 整行）与"保存考核内容"（题库 upsert+知识点查建+挂载整课替换）②scoringText=取题时由 question_bank.answer+scoring 拼装的判分文本，非表字段 ③总分=Σ每题 pointGot（原始分和，ExamView.tsx:495 逐题累加）；exam_plans.score 回填不重算（applyExamAttempts 直接取 exam_attempts.score 快照，无计划行则自动新建 origin='conversation'/kind='custom'）④审计=exam-audit/<cid>/<日>.jsonl 的 select/generate/score 事件（结构化直出场次无 generate/select 可区分路径）⑤plan_outcome 用途三条（生活证据/防重挂/家长核对），当前代码只写 'done' ⑥materials 索引=「id→相对路径」解析真源（内容/列表/删除不扫盘，id=base64url(相对路径)，主键含家长 id 防跨家长冲突）。

## 19:48~20:00 修复：考核计划误顺延（用户拍板：考核错过后由家长重排，不进行顺延）
- 根因：`server/src/worker/plan-domain.ts` `expireAndCarry` 的 specs **误含 exam_plans**，导致考核计划过期也复制顺延行（该处代码注释"不 carry"与实际行为相反，属注释先行、实现未同步）。
- 修复：specs 只留 study_plans/life_plans（顺延 + carry_from=原行 id + origin='carry'）；exam_plans 只判 `missed`、不复制新行。删除无用的 copyExtra 与 hasCarry 分支。
- 验证：server tsc --noEmit 通过（0 错误，exit 0）。**未提交 git**（等用户指令）；**未部署 201**（生产仍在跑旧逻辑，下次部署随版本带上）。
- 文档同步：技术说明书 §2.4/§4.1 改回「考核计划只判 missed、不顺延」。
- ⚠️ 连带发现仍未修：积分结算 finalOk 恒 false（流水不写、余额恒 0）——同文件，待用户指令。

## 19:52~20:05 答疑：question_bank.scoring 为什么 JSON / knowledge_summary 用途
- **scoring**：AI 写题规范（assess-guide.ts:52）明写「建议 JSON…写人话也可」；两个消费方（取题拼 scoringText、QuestionBankPanel fmtScoreLines）均 try-JSON 失败回退原文 → 自由文本可跑通。推荐 JSON 的理由：①一字段喂两类消费方（判分文本 + UI 按维度渲染）②dims[].score 可与 point_max 对账（校验未实现但只有 JSON 能做）③判分 prompt 结构化更稳。属"推荐格式"非必需。
- **knowledge_summary**：写入=AI 整课保存随题带（可空，exam.ts:1656 未填回填知识点名 kpName）；唯一消费方=题库浏览灰色小字展示；**无任何向量检索实现**（列注释"供向量检索"是设计预留），取题走 knowledge_point_id 结构化关联与它无关 → 当前为纯预留字段+展示位，实际内容常与知识点名重复，砍掉无功能损失。
- 文档 §2.3 两行描述按当前状态改准（scoring 标注推荐 JSON/容错回退；knowledge_summary 标注展示用/无检索/预留）。

## 20:00~20:40 考核内容模型重构：类别制 → 知识点制（用户拍板 6 条调整）
- **新模型**：每课考核要点 = 该课知识点（knowledge_points.name + **detail 详细描述**，新增列）；课程挂知识点、知识点挂题目（新表 **course_knowledge_questions**(course_id, knowledge_point_id, question_id, seq, overview)）；**topic_categories 与 course_category_questions 废弃并 DROP**（迁移：ccq 中已关联 kp 的行幂等复制到新表，meta 游标 ckq_migrated；纯类别挂载数据随模型废弃）。
- method_spec：require/exclude 键 = **知识点 uuid 或名称**（按名引用可跨课覆盖同名知识点）；校验只验证可解析、键按原样存；排期级覆盖 resolveOverride 改按**课程**知识点解析（原按主题类别）。
- exam_plan_courses：schema 去掉 category_id（存量库残留列无害）；applyExamAttempts 不再写 categoryId。
- 抽题（assess-selection）：按知识点 exclude/require，require 空=全部知识点，每知识点随机抽 N；speech 置前；题目载荷去 categoryId。
- 取题回退（step 4）：未挂题课程 → 按该课**知识点详情** LLM 出题（fetchCoursesWithRubric→fetchCoursesWithKnowledgePoints 下发 knowledgePoints；exam-engine generateForCourse 用 kp name+detail 作材料，recitationFor 去掉 rubric 正则、仅保留结构化 recitation 通道；ExamView 判分锚定 rubric=知识点摘要拼接）。
- 接口：GET /assess/topics/:topic/categories 与 POST /assess/categories 删除，新增 GET /assess/topics/:topic/knowledge-points；/assess/courses/save item={knowledgePointId|knowledgePoint(+detail), overview, questions[]}。
- AI 工具：assess_categories_list/assess_category_create → **assess_knowledge_points_list**（白名单+import+AGENTS 文档同步）；assess_content_save payload 改知识点条目；assess_method_set 键改知识点；assess-guide 全文重写为知识点制规范。
- 前端：TopicDetail 考核要点页改知识点浏览（kp chip+详情展示，删除旧 rubric 编辑器与 saveRubric）；QuestionBankPanel contexts 去 category；ExamView 去 categoryId 透传。
- 删除 server/src/assess-migrate.ts（一次性历史脚本，引用已删函数）。
- 验证：server tsc 0 错误；根 tsc（用 server 的 tsc，根 typescript 安装缺 lib 文件跑不了——环境问题）相关文件仅剩基线错误（defineTool details 缺失遍布全仓库工具、ChildProfile.id 等），本次引入的错误已全部清除。
- ⚠️ 迁移注意：本地开发库家长库有结构化数据（题库 3575 题），**未关联知识点的旧挂载行会丢**（需重新挂知识点）；生产 201 考核内容 4 表为空、无迁移负担。未提交 git、未部署。
