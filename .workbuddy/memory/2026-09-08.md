# 2026-09-08
## parent_content htmlPath 设计修正（07:3x）
- 用户质疑：读库取路径为何要在线验证文件？失败还统一报「未找到」，让人误以为没登记 html_path。认同。
- 已改：①htmlPath 查询不再以「实时拉文件成败」为判据——登记即返回路径（display_content 展示时兜底报真实 404）；保留后台探测仅日志。②返回结构加 reason（not-allocated/no-method/no-course/no-content/no-html-path），parent_content 报错按原因精确区分（未分配/课程名不对要先列清单/无文案/未登记路径），不再误导。tsc/build 过。需重启 app 生效。

## ISSUE-063 审计所有工具报错是否详细/准确/能指导 agent 下一步（已记入 ISSUES.md）
- 用户要求：检查所有工具报错，好的报错要让 agent 知道下一步（例：查课程找不到→报「可能课程名错，建议先列全部课程名核对」）。
- 审计（已读 custom-tools.ts / pi-session.ts / parent-library.ts 等）：
  - **已好（范本）**：parent_content(:1079) 课程找不到建议 kb_query 列标题；parent_library_courses(:2330)/exam_schedule_create(:986)/course_status(:2381) 附现有候选；study_plan(:1925) 建议 list；voice 类带下一步。
  - **问题点**：①**核心反例**：孩子会话查课程教法 `createChildSession`(pi-session.ts:581/584-585) `fetchCourseLessonRemote(...).catch(()=>null)` + `console.warn` 静默降级，agent 不知课程找不到→直接无教法开讲（正是用户例子）；②离线拉取统一静默吞（parent-library:770「禁止静默降级」政策未覆盖孩子会话）；③parent_read_image(:932) 三元恒等文本相同；④schedule_task(:1809)/upload(:802)/programming(:233) 泛化报错缺下一步；⑤英文报错(child-auth:276/303、ipc:1603/1606)应中文化；⑥缺可恢复性分类。
- 建议规范（落 docs/TOOL-ERROR-CONVENTIONS.md 或 ARCHITECTURE.md）：what+why(参数/数据/网络/配置)+next+候选清单+中文+不静默降级+可选错误类型枚举。
- 优先级：中。
## display_content 拉取超时修复（07:5x）
- 会话证据（07:46Z? 23:46Z 会话）：parent_content htmlPath 已正常返回路径 ✓；display_content 报「The operation was aborted due to timeout」（30s abort，3 次都失败）→ agent 无奈在 child cwd 自建劣质 21KB 场景页（write 后 display 读本地成功）→ 孩子看到的是假场景。
- 排查：8788 单实例 pid16544 LISTENING 正常、curl 秒回 200/40KB；dbQuery(server-client 15s) 同会话成功 → 判定主进程 fetch 到 materials 的半开 keep-alive/瞬时抖动。
- 修复：media-protocol fetchMaterialContent 超时 30s→12s + cache:no-store + 失败自动重试一次（绕过陈旧连接）；build 过。
- 清理：珊珊 child cwd 下 agent 自建 changjingyingyu/lesson-01-livingroom-hello/index.html（21286B 劣质副本）已移入 server/data/backups/child-scene-junk-20260908/（防 ls/create/display 混淆）。child cwd 已出现 voice/（场景语音落盘生效）。
- 待用户重启 app（必要时重启本地 server）后验证：新会话学第1课 → display_content 应拉取到真资料（40KB 客厅场景）。

## ISSUE-063 第一阶段：工具报错审计交付
- 产出 `AUDIT-tools-error-messages-2026-09-08.md`（仓库根）：3 组 39 项工具逐条评分 + P0/P1/P2 问题分级 + 推荐实施顺序。
- 关键结论：P0=C1/C2（pi-session 教法预取静默吞 .catch(()=>null) + learning-summary/agent-prompts fetch* 全静默降级）；P1=A7 kb_update、A2 display_content 拉取失败、B20 parent_read_image 三元恒等(:932)、A14 schedule_task(:1809)。
- 已同步 ISSUES.md 阶段进度；代码未改，待用户确认实施顺序。

## ISSUE-063 第二阶段：工具报错修复实施
- P0 不静默降级：learning-summary.ts 三个 fetch* 返回状态(ok/network/not-found)+缓存 meta(lastFetchOk/lastFetchAt/cachedAt)；agent-prompts fetchAgentPromptRemote→{content,status}；pi-session createChildSession/createSceneSession 教法缺失/旧缓存/计划失败 → dataNotice 注入 buildChildPrompt/buildScenePrompt（附 kb_query 列课程核对 next）。
- P1：kb_update/display_content/parent_read_image(拆四类)/schedule_task create 报错补 next。
- P2：page 三兄弟+parent_course_delete+kb_query progress 空结果+parent_upload_material 补 next；child-auth/pi-session/ipc-handlers 英文报错中文化。
- 规范文档 docs/TOOL-ERROR-CONVENTIONS.md（7 条 checklist+三段式+降级政策+范本索引）。
- 测试：修 parent-library.test.ts 一条过时断言(htmlPath 政策 9/8 更新为登记即返回)。验证：tsc 仅 5 条环境噪音、electron-vite build 通过、相关单测 55/55；vitest 全量结果待收。

## ISSUE-063 收尾：全量 vitest 验证通过
- vitest 全量复跑 **340/340 通过**（37 文件全绿）。此前全量偶现的 3 个 parent-stats activity-log 失败（ENOENT parents/default/activity-log.md）单独跑 18/18 稳定通过、复跑不再出现 → 确认属既有 flaky（测试间共享 PI_TEST_DATA_DIR 状态污染），与 ISSUE-063 改动无关，无需修代码。
- 已更新 ISSUES.md 的 ISSUE-063「阶段进度」为 ✅ 验证通过。审计(阶段一)+修复(阶段二, P0/P1/P2)+规范文档+构建与测试验证全部闭环；部署 201/公网未做，git 未提交（用户未要求）。

## ISSUE-064 新增：主页孩子列表溢出无滚动条
- 根因确认：`src/pages/Home.tsx:96` 的 `.avatars`（`styles.css:472`）只有 `max-width:600px` 无 `max-height`/`overflow`；父级 `.child-select`（:458）`height:100%+justify-content:center` 把孩子多时的头像区垂直裁切且无滚动条。
- 修复建议 A（推荐）：`.avatars` 加 `max-height:calc(100vh - 220px); overflow-y:auto; overflow-x:hidden; overscroll-behavior:contain; padding-right:8px;`。B（更稳）：`.child-select` 改 `justify-content:flex-start`+整页 `overflow-y:auto`。主改动点 `styles.css:472`。
- **✅ 已修复（11:1x）**：采用方案 A（最小改动、不动 JSX）。`styles.css:472` 的 `.child-select .avatars` 加 max-height+overflow-y:auto+overflow-x:hidden+overscroll-behavior:contain+padding；新增 `.avatar-card.parent-card` 设 `position:sticky;top:0` 让「家长」卡滚动时始终可见。ISSUES.md 已标 ✅ 并补修复内容。

## SSECP 背诵评测：评测时机改为「提交时批量」（沿现有上传架构）
- 用户澄清：**不要逐题实时评分**（担心阻塞考核），要「考核结束后一次性提交，再对背诵音频批量打分」。
- 修正 DESIGN-ssecp-speech-assessment-2026-09-07.md §14：删「录完即评/实时上传/exam:speech 实时消息」方案 → 改为复用现有 `ExamView.handleSubmit`「提交时上传音频」架构，在 `scoring` 阶段对背诵题 `Promise.all` 并行调 SSECP（经新增 preload 桥 `examAssessSpeech` → `exam.ts assessSpeech` → `/assessment/speech`）。口述题仍走 LLM 判分。iframe 背诵题只改「点击录制」且**不发 `exam:asr`**（不需 ASR），不回填分数。
- 关键边界：背诵不打断答题；评分等待集中在提交后的「老师正在批改」屏（与现状一致）。coreType 仍 `cn_recitation`（声希 `cn.pred.score` 族，⚠️ 待首测）。
- 交互决定（用户定）：背诵题**沿用现有「按住说话」录制**（松手即停，避免长录忘关），支持多段录音、提交时复用现有 `voiceMerge` 拼接为单段（拼接代码已有，不新写）；仅差异是不发 `exam:asr`（背诵不需 ASR）。故 §14 C 已从「点击录制」改回「按住说话+多段拼接」。

## git 本地提交（10:4x，用户要求）
- 自 9d1831e(09-07 release 0.1.13) 之后积累的未提交工作分 7 笔提交（按依赖序）：
  1. d03e260 feat(scene) ISSUE-061 场景会话支持（scene_command/桥接/TTS 磁盘缓存）
  2. 3bdc5da fix(learning) ISSUE-063 P0 预取状态基础设施（fetch* 状态 + 缓存 meta）
  3. 1db7f7e feat(scene)+fix(agent-tools) 场景主进程 + ISSUE-063 报错规范（同文件交织合并）
  4. 5627a5b feat(exam) SSECP 背诵题 refText + 家长端 UI（DESIGN §T6）
  5. 73cdd22 fix(materials) html_path 登记即返回 + display 超时容错 + 课程名宽松匹配
  6. 06397fd fix(agent) ISSUE-063 中文化 + 写文件报错补 next
  7. 51268dc docs 审计/规范文档 + ARCHITECTURE + memory + PACKAGING + package.json(output→dist-release-019)
- 未入库（保持 untracked）：.tmp-asar/、linux-016.zip、lunyu_exam/、tmp/、scripts-*.mts、voice_*_201 csv。
## 用户方向：统一「通讯标准」（PiBridge），不做双轨（13:5x）
- 用户诉求：场景英语、家长端、一切需通讯的网页统一用 ISSUE-060 的 PiBridge 方案；不希望单独维护 scene:*。请先分析能否满足场景需求、下行形态由我判断。
- 已确认：PiBridge 目前零实现（纯讨论）。本轮输出覆盖分析：场景需求（say/move/act 演出下行、child-click/scene-ready/mic 上行、ready gate/ack/mic 状态机/agent-busy、TTS、manual、可信沙箱）逐条可由 PiBridge 承载；下行判断=扩展既有 page:exec 通道（requestId/超时/回执/就绪 gate 复用）+ 页面 PiBridge.on(action) 分发，不新开广播；场景=manual 声明 + scene.* 动作命名空间 + scene_command 工具层不变。缺口：PiBridge.on/off、manifest 上报、tts/mic 能力映射、动作目录命名空间。待用户确认后实施。
## PiBridge 统一通讯标准 落地 ①②（14:1x-14:3x，用户批准四步顺序）
- ①协议文档：根 `MATERIAL-BRIDGE-PROTOCOL.md`（信封表/采集 manual/PiBridge API 四方向/动作目录：通用+scene.* 命名空间/tts 能力/生命周期/编程约定/作者示例/宿主实现要点）。
- ②SDK+宿主落地：
  - `src/lib/page-bridge.ts`：PageEventKind+detail 扩 "app"+payload；BRIDGE_SCRIPT 采集段包 `if(autoCapture)`（读 `__PI_CAPTURE_MANUAL`）；新 `PIBRIDGE_SCRIPT`（window.PiBridge={emit,request,on,off}，page:app/page:req 上行、page:app-cmd 下行分发+page:app-cmd:result 回执、page:app-res 收 request，12s 超时；初版漏挂 window.PiBridge 已修）；`injectBridge` 识别 `<meta pi-bridge capture=manual>` 注入标志并后拼 PIBRIDGE_SCRIPT；MaterialsPanelHandle 加 `appCmd`。
  - MaterialsPanel：message handler 增 page:app（→PageEvent kind app 直上抛不节流）、page:req（handleAppRequest：tts.speak→speakMaterialText，未知能力报错，回 page:app-res）、page:app-cmd:result 并入 exec 回执 pending；新增 `appCmd(action,payload)`（复用就绪/pending/超时）与 `handleAppRequest`；expose appCmd。
  - 主进程 `electron/lib/page-bridge.ts` formatPageEvent case "app"：`资料触发了动作「action」，数据：{payload}`。
  - ②编程 agent：`programming-agent.ts` buildProgrammingPrompt 内嵌「交互通讯协议」段（PiBridge 默认、manual 推荐、tts.speak、scene.* on/emit、禁私有 postMessage）；修复一次注释错位。
- 验证：tsc/build 过；electron headless 冒烟 SDK（注入 object、page:app-cmd→PiBridge.on('scene.say') handler 回写 DOM "SAY:Hello!" 通过）。
- 待做（用户批准顺序中 ③④）：③场景 HTML 与 MaterialsPanel.scene()/scene_command 切 PiBridge 单轨（scene.* 映射 page:app-cmd；scene:ready/child-click/mic 上行改 emit('scene.*')；页面 declare capture=manual）；④家长端测验样例吃狗粮。

## SSECP 凭证路径决策 + 配置落地（15:1x）
- 用户给的 `ssapi.txt`：`appid`+`appsecret`+`warrantid` = **声希直连凭证（Path B）**，与代码 `server/src/assessment/ssecp.ts`/`electron/lib/assessment/providers/aliyun-kid.ts` 同源（api.cloud.ssapi.cn + MD5 签名）。
- 澄清：用户从 help.aliyun.com/2850842.html（API 概览）看到 AccessKey 那条是 SSECP **另一套官方接入（Path A：阿里云 OpenAPI ROA 签名 + RAM AccessKey + aicontent.*.aliyuncs.com）**。两条都官方认可，但我们代码只支持 Path B，故选 B、不改代码。
- 凭证已落地 `data/assessment-config.json`：`{providers:{aliyun-ssecp:{appKey,appSecret,userId:"pi-child"}}}`，结构经 `loadSsecpCreds` 校验可读 ✅。
- 安全：`.gitignore` 已加 `ssapi.txt` 与 `data/assessment-config.json`（data/ 本就被忽略）；密钥不进库。建议用户用后删 ssapi.txt。
- 首测待办：①coreType `cn_recitation`→声希 `cn.pred.score` 族（⚠️ 待真实返回核对）；②服务端需出公网到 api.cloud.ssapi.cn:8080(鉴权 HTTPS) + wss://api.cloud.ssapi.cn(评测)；③确认套餐覆盖中文背诵题型。

## SSECP 真测（15:5x，沙箱）
- **鉴权 ✅ 凭证有效**：独立 node 脚本读 data/assessment-config.json，POST 鉴权返回 `code=0 success`，签发真实 warrant_id（一次性新签 + ssapi.txt 预存都过）。证明 appid/appsecret 正确、SSECP 账号正常。
- **网络发现**：沙箱里 `:8080` 被挡、443 通（gate-01/02/03 + api 都 HTTP 200）；但 `wss://api.cloud.ssapi.cn` 的 WS 升级**整条链路被挡**（sandbox 开/关都 30s 连不上 open）。→ WS 评测只能在本机/201 跑。
- **代码改动（已 tsc 验证）**：
  1. `server/src/assessment/ssecp.ts` + `electron/lib/assessment/providers/aliyun-kid.ts`：鉴权 URL 由写死的 `:8080` 改为 443 网关列表（gate-01/02/03 + api + :8080 兜底），多网关容灾。
  2. 同上两文件：`start` 评测请求加 `warrantId`（request + app 都带）——官方「服务端授权」文档要求设备用授权拿到的 warrant_id 发起评测（原代码漏带，未验证是否必需，加上无副作用）。
- **遗留（需 201/本机验证）**：`cn.pred.score` 后端是否接受、WS eval 真实返回维度结构、warrant_id 字段位置是否正确。测试脚本：`.workbuddy/tmp/ssecp-test.mjs`（cwd 相对读 data/assessment-config.json，WS 多网关容灾），在 201 上 `node .workbuddy/tmp/ssecp-test.mjs` 即可跑完整鉴权+评测。
## 珊珊实测三问（15:2x）
- 会话证据：agent 本次确实调用了 display_content 成功（fetch 修复+重启生效）；scene_command 演出全 OK。
- Q1 左侧资料未 display 就出现=ISSUE-014「恢复会话自动打开最近资料」机制（回填 materials→effect 自动选中最新+展开），与 agent 无关。方案待用户拍板（改为只列列表）。
- Q2「再学一次被拦=忙」：上一轮多工具链长时 working 气泡不结束，输入被 disabled；running 时按钮=停止可打断。建议：长工具轮在气泡已有「正在使用工具…」；可优化为允许输入打断。
- Q3 场景 agent 准备无提示 → 已实现：新 IPC scene:prepare（预建 getSceneSession+取课文+首次预热 TTS，无 LLM 产出）+ preload scenePrepare；MaterialsPanel.scenePreparing → 标准下行 scene.prepare{status}；HTML #scene-preparing「正在准备场景伙伴…」浮层（语音球上方 loading/ready）；Learn handleSceneActive 后按场景 key 首次触发 prepareSceneFlow，就绪后聊天提示「🎭 场景伙伴已就绪…」。materials 同步（含 preparing），tsc/build 过。
## 场景课全托管 scene agent（16:0x-16:3x，用户拍板）
- 用户：只要确定是场景课就进入 scene 会话。根因矛盾：旧规范让课程 agent 当场景主持人（scene_command 在课程会话工具集），与独立 scene agent 并存。
- 落地：①工具物理隔离——课程/主 child 会话移除 scene_command（pi-session.ts tools+customTools），scene 会话独有；②规范改写 LEARNING_NAV_INSTRUCTIONS「场景互动课」段：课程 agent 只负责 parent_content 背景 + display_content 打开 + 一两句交棒，不扮演不演出不讲解；③Learn 自动交棒开场——handleToolEnd 检测 display 内容含 pi-scenario 置 sceneOpenedTurnRef；child pi:reply_end 时若 sceneMode 且打开过场景 → launchSceneIntro(ck)（工作气泡+scenePrompt 开场[角色英文台词欢迎+动作]）；lastAskTextRef 记孩子原话、sceneIntroDoneRef 每 key 防重；handleSend 每轮重置。tsc/build 过。

## 16:50 考核背诵 4 问题排查（珊珊自定义"只考学而篇第一章"）＋修复问题1/3
- 现象：①自定义只考第一章却出其它章背诵 ②出题久 ③背诵页显示原文 ④提交失败。
- 排查数据：珊珊排期 sch_1788849988584_au1oth status=started scope prompt"只考核论语学而篇第一章"；本地主库无 speech_assessments 表、无 server/data/assessment-config.json（SSECP 评测基建缺失→问题4疑点）。
- 问题1根因+修复：exam-engine recitationFor 凡"论语"课注入固定5句跨章种子(为政/述而名句)。改为从该课 rubric「原文背诵」行提取本章原文（正则支持两种格式，489章中438可提取；提取不到不注入）。commit 0c39888。
- 问题3根因+修复：模板对一切 questionType 显示 refText。改 cn_recitation/cn_poem 隐藏原文（判分对照用），跟读/朗读类仍显示。commit 0c39888。
- 问题2（出题慢）与问题4（提交失败）待用户补信息（N门数/红字/server数据目录与阿里SSECP凭证）。
## 白屏修复（16:4x）
- 全托管改动引入白屏：handleReplyEnd@576 依赖数组引用定义于其后的 launchSceneIntro/resolveSceneKey → render 期 TDZ ReferenceError → React 崩溃白屏。tsc 不报（TDZ 属运行期）。
- 修复：handleReplyEnd 移到 launchSceneIntro/resolveSceneKey/prepareSceneFlow 之后；恢复 handleReplyError（一次编辑误删重写）。唯一性与 tsc 校验过，build 通过。教训：useCallback 依赖数组引用后置 const 会运行期 TDZ，tsc 静默。
## UI 真机自动化测试长排（17:0x-17:5x，CDP 驱动真实 app）
- 手段：electron --remote-debugging-port 起真实 app，CDP(Runtime.evaluate)驱动：选珊珊→输密码123456→进入学习→发消息→采样。
- 复现问题：①场景模式没激活（无🎭横条、无 scene.ready 到达）；②课程 agent 反复长篇"等场景伙伴准备好"且不交棒。agent 其实已无 scene_command（隔离生效），但 display/交棒规范未触发。
- 根因锁定过程：注入链/内容在三路对照（手动注入/真实产物/去 base）headless 全部执行成功（[scene] boot→pbBind→ready emit），排除内容与注入破坏。App 内：宿主只收 page:ready（旧 iframe），材料保鲜(materialsRefresh 新 IPC)把 srcdoc 换成最新后 **iframe 未重载/页面未执行**——已知 React/Chromium srcDoc 更新不保证导航的坑（MaterialsPanel key 已按 len:hash 但实测仍复发；本次刷新 key 变化仍不导航）。
- 已保留修复：materials:refresh IPC + preload + Learn refreshStaleMaterials（恢复共享 html 自动拉最新，内容保鲜长期有效）；页面调试日志已清理；tsc/build 过。
- 遗留：app 内"运行中更新 srcdoc 后 iframe 不重载"未根治，跨会话重启（恢复直接最新内容）可绕过；根治需 iframe 渲染机制调整（如 key 加版本戳强重建/换 loadURL）。
## 场景不转入根治：iframe 强重建（18:1x）
- 现象确认：左侧展示了场景页面但从未转入 scene 会话——因为旧缓存资料"运行中更新 srcDoc 内容"时 React/Chromium 不触发 iframe 导航，页面就绪(scene.ready)信号只发一次且已错过 → handleSceneActive 永不执行。
- 证据链：同内容三路对照（手动注入/app 真实产物/去 base）在隔离 iframe 全部正常执行（body 全跑通）；唯 app 内"更新内容不重载"。
- 修复：MaterialsPanel 增加 htmlEpoch——useEffect 监听 cleanHtml 实质变化（去重后内容不同）→ setHtmlEpoch+1；HtmlFrame key 追加 `:${refreshEpoch}` → 内容任何实质变化都卸载重建 iframe 完整重载（新页重发 page:ready + scene.ready → 场景模式激活、交棒链路可用）。覆盖 display 同 path 重发与 materialsRefresh 保鲜两条更新路径。
- 上轮材料保鲜(materials:refresh)与此配合：恢复会话自动拉最新内容 → epoch 变 → iframe 重建加载最新 → 场景正常激活。
- tsc/build 过。待用户重启 app 验证：进珊珊→左侧场景→应出现 🎭 场景条（转入成功）。
## htmlEpoch 白屏修复（18:2x）
- iframe 强重建的 epoch 实现初次放错位：useEffect 依赖数组引用组件下部才定义的 cleanHtml → render 期 TDZ 白屏（同一教训第二次，已在文件注释留警告）。
- 改为顶层可直接用的 activeHtml（selected.content），useEffect deps [activeHtml] 在顶层执行，无 TDZ；内容变化即 epoch+1 → HtmlFrame key 变化 → iframe 强重建。启动冒烟通过（页面渲染、无 TDZ/未定义错误）。
## 场景不转入——srcdoc/dataURL 沙盒 iframe 正文脚本不执行（18:3x 阶段性结论）
- 现象：场景资料在左侧加载（head 桥 page:ready 达宿主）但正文 body 脚本永不执行 → scene.ready 不上报 → 永不转入 scene 会话。加全局 error 捕获无任何 JS 报错。
- 已排除并验证：内容本身/注入链/改写在隔离 iframe 全部正常（body 全跑通）；加载方式对比：隔离环境 iframe src=dataURL 执行成功；但**真实 app（父为 app://bundle 自定义协议）里无论 srcdoc 还是 dataURL，同内容 head 桥运行而正文 script 不执行且无错误**。
- 推断：Electron 自定义协议父页 + sandbox(allow-scripts,无 allow-same-origin) iframe 对大文档正文脚本执行存在怪癖（隔离 data:/http 父页可复现不出）。
- 已落地（保留）：①iframe key+htmlEpoch 强重建；②HtmlFrame 改 dataURL 加载（保留，无害）；③桥注入诊断 error 上抛（page:diag）。
- 下一步候选（未实施）：iframe 直接加载 media://local/parent/<pid>/<rel> 真实 URL（网络级文档，主进程协议已支持）绕过 dataURL/srcdoc 执行怪癖；需处理 iframe CSP frame-src/sandbox 允许 media:。需用户拍板继续。
## 场景跳转根治：display_content 工具级激活（19:0x，用户拍板「改为调用工具判定跳转」）
- 用户方案：自动跳转(等 iframe scene.ready)始终不发生 → 改为「在 display_content 里判定场景课/名单内就跳转」。
- 落地：①主进程 custom-tools.ts displayContentTool 返回 details.panelContent 增加 isScene（HTML 含 pi-scenario 标记 或 filePath 首段 topic ∈ SCENE_TOPIC_KEYS{changjingyingyu,scenario-english}）；②Learn.tsx handleToolEnd display 分支命中 isScene → sceneOpenedTurnRef+**立即 activateSceneFromTool(filePath,title)**（applySceneMode(true)+派生 fallback key+prepareSceneFlow）；③抽出共用 activateSceneFromTool，handleSceneActive 复用之；④恢复路径补强：materials 自动选中 effect 检测内容含 pi-scenario 同样激活（不设交棒标记）。tsc/build 过。
- 真机验证（CDP）：进入珊珊即「🎭 场景英语对话中」横条出现（此前永不出现）+ 发消息由 scene agent 角色口吻回应（我是 Steve! 欢迎来到客厅）。跳转闭环达成。
- 遗留（另条线）：scene_command 演出仍失败（页面 body 脚本不执行→PiBridge.on('scene.say') 未注册→no handler）；scene agent 只能说话无法驱动演出。根治候选：iframe 改 media:// 真实 URL 加载绕过 srcDoc/dataURL 怪癖（需用户确认）。
## 场景演出根治：真实根因 = 服务端 content 截断（19:3x-19:4x，全链路打通）
- 现象：真实 URL(asset doc) 加载后 head 桥执行、body 场景脚本仍不执行 → scene_command no handler。CDP attach iframe target 定位：正文 script 被截断在 `pbBind("scene.say"... Scene.say(p.charact`（缺尾）→ 语法错误 → 整段不执行。
- 根因：服务端 /materials/content 用 **DB materials.size（40483）** 做 Content-Length，但磁盘文件被 cp 覆盖后索引未刷新（真实 41618）→ createReadStream 发全量但客户端按旧 CL 截断 → html 尾部（含场景脚本后半）丢失。
- 修复：server routes/materials.ts content 端点改为 `fs.statSync(abs).size`（真实 stat），不再信任 row.size。验证：重启 server 后拉取 41618B=磁盘大小。
- 配合通道：asset 协议加文档渲染模式（?doc=1 → material-doc.ts：拉原始+rewrite+injectBridge 返回 text/html）；main.ts setAssetDocRenderer 装配；HtmlFrame 对共享 html(filePath 可解析) 改走真实 asset:// 顶层 URL + 加载进度 overlay（CSS .html-frame-loading/hf-progress）。避免 media-protocol↔parent-library 环。
- 真机验证：iframe target scene=object、Scene.say 可用；scene.ready 到达（panel-scene 194227）；scene_command 返回「场景指令已执行（act/say）」不再 no handler；字幕显示 Maggie 台词。**正文脚本随真实导航执行问题解决。**
## 聊天框与 HTML 字幕不一致修复（21:0x）
- 现象：场景对话时 HTML 字幕=逐句 say 台词，聊天框=agent 轮末另写的正文（把台词再总结一遍）→ 两套文字不一致且重复。
- 修复（主进程 scene:prompt 回传组装，ipc-handlers.ts）：遍历本轮新增 messages，收集 scene_command say toolCall 的台词（character 首字母大写作 speaker，与 roleMeta.sub 一致）→ 有台词时 scene:reply 只发台词多行文本；无 say（纯文字解释）才回发 assistant 正文兜底。新增 safeJsonParse。pi-session.ts buildScenePrompt 措辞同步：说话唯一方式=say，正文不复述台词。
- 真机验证：聊天框显示「Maggie: Squeak! The sofa is blue! 💙...」= 角色实际台词（与字幕同源）。tsc/build 过。
## 场景字幕移出画面：下方字幕栏 + 中文可折叠（21:3x）
- 用户要求：场景画面内不再显示字幕（遮挡），改在舞台下方区域显示；中文默认不显示，点按钮展开。
- 改 scenario-demo/lesson01-hello/index.html：删 stage 内 #subtitle 与角色气泡显示（bubble display:none，say 不再写 bubble）；新增 #scene-fit 包裹 #stage + 字幕栏 #captionbar（speaker+英文常驻，中文 #cap-zh 默认折叠，按钮 cap-toggle 切换+文案 中文/收起中文）；Scene.say 写字幕栏、keep zh 状态；fitScene() 按视口等比缩放舞台+字幕整体（iframe 高度不定防超高）。同步 materials 43440B 刷索引。electron 冒烟过（say→英文上屏、中文默认隐藏、toggle 展开/收起、无 subtitle 残留）。
## 场景对话重进后「消失」修复（21:3x）
- 现象：每次重进孩子模式，场景对话历史在聊天框消失。
- 根因：scene 会话对话独立持久于 scene-<topic>/ jsonl（395 条消息在），但聊天框历史只由 piStartChild 回填主/课程会话 history——scene 台词从未写回主会话 → UI 从不显示。agent 记忆其实一直在（jsonl），纯 UI 恢复缺失。
- 修复：①ipc-handlers 新增 scene:history IPC：getSceneSession.messages → composeSceneHistoryDisplay 按与 scene:prompt 实时回发相同规则组装（user 清洗语音识别前缀/页面操作尾巴/系统注入[开场/页面操作]剔除；assistant 有 scene_command say 台词用台词、无才用正文兜底；按轮合并在 say 有值时不重复正文）；返回最近 80 条 {role,text,time=HH:MM}。②preload sceneHistory。③Learn activateSceneFromTool 激活后 restoreSceneHistory(key)（有历史才 setMessages 替换，loading key 防并发）。
- 验证：清洗单测 7/7（含无 / 分隔的 [页面操作] 尾巴修正）；构建过；真机 CDP 进入珊珊 → 🎭 场景条 + 聊天框完整回填上一轮 21:39 躲猫猫/道别对话（Steve:/Maggie: 台词 + 孩子语句）。
## TTS 多角色连读覆盖修复（21:5x）
- 现象：一轮多条角色台词（scene_command say 连发 → 页面逐条 PiBridge.request tts.speak）后一条覆盖前一条（只听到最后一句）。
- 根因：宿主 speakMaterialText 每次收到请求都 pause() 当前 audio → 新合成播放；多条台词到达即互相取消。
- 修复：宿主 TTS 播放改双模式（MaterialsPanel.tsx）：tts.speak（场景页朗读，handleAppRequest）传 {queue:true}——FIFO 队列 pump 串行，播完一条(audio onended/onerror)自动取下一条；资料朗读/点读/查词保持默认打断（seq++ 使进行中与排队全失效）。关键：排队条目快照当前 seq 入队、不递增——若每条独立 seq++，先入队条目合成完被判过期 SKIP（仿真抓到的初版 bug）。stopMaterialTts 同步清队列/active。场景页独立预览 fallback 去掉 speechSynthesis.cancel()（浏览器队列天然顺序）；demo-driver 无朗读不受影响。
- 验证：与组件同构的算法仿真 4/4 PASS（排队3条顺序 ■→■→■ / 播放中打断无■ / 打断清队 / 台词被点读取代）；tsc/build 过；app 内 iframe PiBridge.request 两次均 ok 回执（环境 renderer console 不进日志，听感由用户最终确认）。

## 23:20 考核 AI 会话审计落盘（exam-audit）
- 需求：考核选课/出题/判分会话用完即弃不留痕，需落地保留供开发分析。
- 实现：新文件 electron/lib/exam-audit.ts（append JSONL：{dataDir}/exam-audit/{childId}/{YYYYMMDD}.jsonl；dev=仓库 data/，打包=userData/app-data；写失败仅 console.error 不阻断考核）。
- 接入 exam-engine 三处：select / generate(每课，含 rubric+prompt+reply+parsed+refTexts+costMs) / score。commit a2799fa。tsc/build 通过。
- 后续分析入口：data/exam-audit/。这一批连带此前修复 commit 0c39888（问题1/3）均在本地未部署。
