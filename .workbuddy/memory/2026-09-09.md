# 2026-09-09 工作日志

## 08:00-09:00 考核问题2/4定位 + 选课机制重构（ISSUE-065）
- 审计落盘(exam-audit)已跑出首场数据：select 15.6s(generate? 不，select ok 15.6s 输出仅24字)/generate 92.7s(输出621字)/score 103.9s(输出1KB)——确认慢在模型推理而非网络/输出量。实际模型=mimo/mimo-v2.5（app-settings defaultModel；此前误归因 qwen，已纠正）。mimo API 短请求实测 1.8-3.4s 快 → 慢在大 prompt(select 候选清单 5.2万字符)+逐题推理。
- 提交失败根因：服务端缺 assessment-config.json（SSECP 密钥）。已把客户端 aliyun-ssecp 同款凭据写入 server/data/assessment-config.json。
- 选课机制重构（用户拍板）落地：固定档 daily/weekly=plan周期内必学课全考(内置规则不走AI)；自定义须家长agent解析出精确课程名(courses必填)，UI 去手动 prompt；monthly/halfyear/yearly 下线；删除旧自定义无 courses 排期 5 行(备份 data/exam-schedules-backup-20260909.json)。
- 改动文件：server/src/routes/exam.ts、src/components/ExamAdminPanel.tsx、electron/lib/custom-tools.ts、ISSUES.md。tsc/build 全过。
- 下一步建议：家长 agent 端给一个"查孩子课程+学习痕迹"便捷工具以支撑 courses 精确化；本地联调固定档/自定义。
