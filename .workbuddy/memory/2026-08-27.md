# 2026-08-27 工作日志

## Mac 语音输入失效修复（ffmpeg-static 架构不匹配）

- 现象：Mac 上语音输入报"在 application 的 node_modules 找不到 ffmpeg-static"（实为 ffmpeg 执行失败的类比表述）。用户为 **Intel Mac（macOS 12.7.6）+ x64 dmg**。
- 根因（已验证，非猜测）：`ffmpeg-static` 是带原生二进制的 npm 包，二进制不在 `files` 字段、由 `install.js` 在 `npm install` 时下载。GitHub `macos-latest` 是 **arm64** 机器，`npm ci` 默认下载 **arm64** ffmpeg。x64 dmg 里若塞 arm64 二进制，Intel 芯片无法执行 → 语音输入挂。
  - CI 的 `file` 验证实锤：初版 x64 job 的 ffmpeg 竟是 `Mach-O 64-bit executable arm64`（错误）。
- 修复（已落地并验证通过，run `33026159079` success）：
  1. `build-mac.yml` 改为 matrix `[x64, arm64]`，每个 job 先 `rm -f node_modules/ffmpeg-static/ffmpeg` 再 `npm_config_arch=<arch> npm rebuild ffmpeg-static`。**必须删旧二进制**——`install.js` 第39行见文件已存在会 `process.exit(0)` 跳过下载，否则 arch 环境变量完全不生效（首版就栽在这）。
  2. `package.json` 加 `"asar": true` + `"asarUnpack": ["**/node_modules/ffmpeg-static/**"]`（二进制必须解包到 asar 外才能 exec）。
  3. `electron/lib/voice/audio.ts`：顶层 `import ffmpegStatic` 改为 `getFfmpegStaticPath()` 懒 require + try/catch 容错，模块缺失/不可用时不再拖垮整个语音功能，报错清晰。
- 验证结果：`file` 输出 x64 job = `Mach-O 64-bit executable x86_64`（78MB）、arm64 job = `arm64`（45MB），两 job 均 success。
- 产物：Actions Artifacts 现分 `macos-dmg-x64`、`macos-dmg-arm64`（之前单次构建叫 `macos-dmg`，已废）。用户用 `macos-dmg-x64`。
- 提交链：ae3d611（asarUnpack+容错+matrix初版）→ dded5c0（rm+rebuild 修正）→ 2571cae（PACKAGING.md 记录）。
- 教训：electron 项目用 `ffmpeg-static` 做双架构 Mac 包时，**不能靠单次 `npm ci` + 单 dmg**，必须按目标架构分别安装/构建，且 `install.js` 的「已存在即跳过」是隐形坑。

## Mac 数据路径
- 用户问 Mac 版用户数据路径：`~/Library/Application Support/learning-app/app-data/`（由 config.ts `app.getPath("userData")/app-data"`，userData 在 macOS = `~/Library/Application Support/<appName>`）。未写入 PACKAGING.md（已口头答复）。

## Mac 最大化不能占满屏幕修复
- 现象：Mac 上点最大化后不能像系统其它 App 那样占满屏（无边框 `frame:false` 窗口）。
- 根因：`electron/lib/ipc-handlers.ts` 的 `window:maximize-toggle` 直接调 `w.maximize()/w.unmaximize()`；macOS 无边框窗口的「缩放」不会占满整个工作区，且自定义标题栏无原生缩放承载。
- 修复（已落地并验证，run `33027907578` success，commit `f05ddcb`）：macOS 分支改为 `screen.getDisplayMatching(w.getBounds()).workArea` 精确 setBounds 填满（=系统原生「缩放」行为，顶部留菜单栏，与其余 App 一致）；用模块级 `macMaximizedPrevBounds` 记录还原前 bounds，`window:is-maximized` 同步返回该状态。Windows/Linux 保持原生 `maximize()`。
- 用户需重装 `macos-dmg-x64`（run 33027907578）验证。

## Ubuntu 重新打包新安装包（0.1.0）
- 用户要求到局域网 Ubuntu（192.168.1.201）重新打 Linux 包。探查发现远程 `~/pi` 已**非 git 仓库、但有源码+node_modules（8/25 旧快照）**，且远程能直连 GitHub（`git ls-remote` 返回最新 `f05ddcb`）。
- 改用 git 同步（比 pscp 增量传更干净）：远程 `git init` + `remote add origin https://github.com/shawn-feng/learning-app.git` + `git fetch` + `git checkout -f -B master origin/master`（强检出最新、保留 node_modules）；`npm install --legacy-peer-deps` 显示 `up to date`（依赖复用）。
- 构建：`rm -rf out dist && npm run dist:linux` → 成功产出 `dist/learning-app_0.1.0_amd64.deb`（144,119,384 B）+ `dist/学习伙伴-0.1.0.AppImage`（192,382,266 B）。electron-builder 26.15.3 / electron 43.3.0 / x64。
- 拉回本地：AppImage 中文名先 `mv` 为 `xuexihub-0.1.0.AppImage`（pscp 多源不支持、中文名易乱码）→ 分两次 pscp 到 `dist-ubuntu/` → 本地 `mv` 回中文名。大小与远程逐字节一致。
- 结论：新 Linux 包（含 ffmpeg 修复、Mac 最大化修复等全部最新代码）已落在 `C:\Users\79734\Documents\pi\dist-ubuntu\`。
- 已同步更新 `PACKAGING.md` §3：发版流程新增「git 强检出」推荐方式，pscp 改为兜底。


## 补充核实：setThinkingLevel 可用性（支撑建议 2）
- setThinkingLevel 是 AgentSession 公开方法（pi-coding-agent/dist/core/agent-session.d.ts:454），createAgentSession 返回的 session 可直接调用。
- ThinkingLevel 字面量含 "off"（@earendil-works/pi-agent-core/dist/types.d.ts:260，"off" | "minimal" | ...），setThinkingLevel("off") 类型合法、实现会 clamp 校验。
- 调整建议汇总：①治本=kb_insert 支持批量+prompt 合并调用（减轮数，乘法杠杆）；②一行 setThinkingLevel("off")+配置开关（减每轮 thinking，加法杠杆，别用 minimal）；③UX 别同步卡会话；④先埋点实测轮数再动手。推荐先 2+4，再决定 1。


## 实施：summarize_conversation 提速——首轮注入上下文 + kb_insert 批量 + 一次性完成（用户拍板方案）
- 方案（用户指定，不关 thinking）：首轮把「对话内容+提取要求+当前学习主题信息」一起给 LLM，LLM 首轮返回全部事件与工具调用、一轮收尾；3(后台)/4(埋点)不做。
- 改动：
  1. kb-sqlite.ts：新增 `insertDailyEntries(childDir,date,entries)`（事务批量、逐条幂等跳过，返回 inserted/skipped）+ `queryTopicSummary`（只取 topic_progress 聚合行，不加载逐课明细——论语几百课绝不能全塞上下文）。
  2. custom-tools.ts kb_insert：新增 `entries: [{block,content},...]` 批量参数（date 统一、单事务、重复跳过），description 推荐批量；单条参数兼容保留。
  3. recording-prompt.ts RECORDING_PROMPT：新增「0. 一次性完成（最高优先级）」——首轮返回全部 kb_insert/kb_update、多条合并批量、不要 kb_query 了解信息；原「1. 读取主题映射」改为「使用已提供的上下文」；打标签直接用已提供标签表。
  4. daily-summary.ts：新增 `buildProvidedContext`（主题进度摘要+标签定义表+当天已有条目清单），summarizeDailyConversation 首轮注入（原 dedupSection 并入），prompt=RECORDING_PROMPT+上下文+对话。
  5. 测试：kb-tools.test.ts 加 4 个批量用例；daily-summary.test.ts 加 buildProvidedContext 2 个用例（空库/种子库）。
- 验证：electron-vite build 通过；tsc --noEmit 仅剩 5 条已知环境告警（TS2318×4/TS2552×1）；相关测试 daily-summary 全过、kb-tools/kb-sqlite 各剩 1 条**真实库数据依赖的既有失败**（rules.daily 期望 '3' 实为 '新学3课，复习3课'；2026-08 生活条目 32 行>30 断言），与本次改动无关。
- 反引号坑（踩过）：RECORDING_PROMPT 是模板字符串，正文里写代码片段必须转义为 \`，否则 esbuild 收集阶段直接报错（'Expected ; but found entries'）。
- 待办：真实会话实测耗时（预期从 4 分钟降到 2-3 轮）；真实库 2 条既有失败用例待数据稳定后修正断言。

## Mac 麦克风权限不弹窗 / app 不在权限列表（NSMicrophoneUsageDescription 缺失）
- 现象：Mac 语音输入要用麦克风，但 app 从不在「系统设置→隐私与安全性→麦克风」列表出现、也从弹授权窗，导致无法录音。
- 根因（已验证）：① app 的 `Info.plist` 缺 `NSMicrophoneUsageDescription`，macOS（Ventura+）会**静默拒绝且根本不把 app 登记进权限列表**；② `electron/main.ts` 原只在 Chromium 层用 `setPermissionRequestHandler` 放行媒体权限，**不等于 macOS 系统级 TCC 权限**，且没有「主动申请」。
- 修复（已落地并验证，**带构建内 plist 铁证**，run `33031705565` success）：
  1. `package.json` `build.mac.extendInfo` 增加 `NSMicrophoneUsageDescription`=需要访问麦克风以进行语音输入、`NSSpeechRecognitionUsageDescription`=需要语音识别以将语音转换为文字（electron-builder 的 `extendInfo` 是往 Info.plist 合并任意键的正式字段）。
  2. `electron/main.ts` `app.whenReady` 内、媒体权限放行之后，新增 macOS 专属：`systemPreferences.askForMediaAccess("microphone")`（仅 darwin、权限未决时才弹窗、已决定则直接返回不弹），使 app 启动即出现在权限列表并主动申请。捕获本身仍走 renderer `useAudioRecorder.ts` 的 `getUserMedia({audio:true})`。
- CI 校验教训：首版校验 `find dist -name Info.plist -path "*Contents*" | head -1` 随机命中 `学习伙伴 Helper (GPU).app/Contents/Info.plist`（Helper 无麦克风声明），导致误报失败。修正为 `-path "*.app/Contents/Info.plist" ! -path "*/Frameworks/*"` 精确锁定主 app。最终两个 job 的 PlistBuddy 都打印出两条声明原文，**坐实修复生效**。
- 提交链：4297a08（extendInfo+askForMediaAccess）→ e1d4018（加 plist 校验，初版误判）→ 837bfee（校验改锁主 app）→ 验证 run 33031705565 全绿。
- 用户需重装 `macos-dmg-x64`（run 33031705565）验证；若之前已 deny 过，需去系统设置手动开（askForMediaAccess 对已拒绝不再弹窗）。

## Ubuntu 新安装包重打（0.1.0，含麦克风修复全量代码）
- 本地与 GitHub 已同步至 `4aabed3`（麦克风修复 commit 已含），无需再 push。
- 远程 `192.168.1.201` (`shanshan`，hostkey `SHA256:u90140y+...`)：`cd ~/pi && git checkout -f -B master origin/master && rm -rf out dist && npm run dist:linux` → 成功（build 5m18s，electron-builder 26.15.3 / electron 43.3.0 / x64）。
- 产物：`dist/学习伙伴-0.1.0.AppImage`（192,382,293 B）+ `dist/learning-app_0.1.0_amd64.deb`（144,120,404 B）。
- 拉回本地：AppImage 远程先 `mv` 为 `xuexihub-0.1.0.AppImage`（pscp 中文名乱码规避）→ pscp 两个文件到 `dist-ubuntu/` → 本地 `mv` 回中文名；大小与远程逐字节一致。
- 结论：含 ffmpeg 修复、Mac 最大化修复、麦克风 Info.plist 修复等全部最新代码的新 Linux 包已落在 `C:\Users\79734\Documents\pi\dist-ubuntu\`。

## 架构讨论（进行中）：应用拆分为客户端 + 服务端（均 app 形态）
- 用户提出新方向：客户端（家长/孩子界面与现在一致、本地跑 AI agent、模型参数等服务端下发并 2min 轮询变更、数据库全在服务端、materials 服务端下发+本地缓存）与 服务端（维护 DB 与 materials 版本、家长账号公网认证+授权+套餐）。**仅讨论需求，未定案、未设计方案。**
- 已拍板（AskUserQuestion 两轮）：①核心目标=多设备共享数据；②必须在线，但**已缓存学习资料断网可离线浏览**（读不联网），聊天/记录等写操作必须在线；③**单安装包双角色**——安装时可选装服务端，客户端可配所连服务端地址，服务端可云上 ECS 或家庭局域网、运行时无 UI 常驻，**本机可自用**（装服务端的机器也能开客户端界面）；④认证复用 benefit-auth（客户端→服务端→auth.aixuexihao.top）；⑤多端同时在线（孩子端学习+家长端管理互不阻塞）；⑥会话接续本期不做（换设备=新会话）。
- 语义收敛：本地不再有业务数据库（kb/agents/sessions/scheduler 全上移服务端），本地仅剩缓存目录；「2min 轮询配置变更」=跨设备配置软实时同步，2 分钟延迟为产品选择；materials 本地缓存=性能缓存+离线浏览兜底。
- 待细化（不阻塞）：大文件（录音/图片/视频）存储方式、家长上传 materials 是否纳入服务端版本管理、孩子端是否单独身份、存量本地数据迁移、服务端与已部署 cloud-service/www 的关系、与 pi-web/engine-server 既有路线如何取舍。
- **v0.2 追加拍板（全部明确）**：①大文件（录音/图/视频）存服务端磁盘，不做对象存储；②materials 版本管理=**只记最新更新时间戳**，客户端比对决定是否刷新缓存，**不做版本切换/回滚**；③孩子由家长创建、数据在服务端、**不单独公网认证**，跟随家长设备授权即可；④存量数据迁移**本期不做**；⑤服务端 App **独立于 cloud-service（www）**、与 pi-web **无关联**，三条线互不隶属；⑥**会话 jsonl 留在客户端本地**，agent 会话本地读写，**不上服务端**——原始会话不跨设备（与"会话接续不做"自洽），家长端看孩子学习情况只能依赖服务端的学习记录。
- **v0.3 追加拍板**：①推论A/B 确认——家长端本期只看学习记录、不翻原始会话；**预留扩展点：后续可加客户端功能定时将会话内容推送到服务端数据库**；②**服务端与客户端拆成两个独立安装包**（不为降低在线升级复杂度合一）——版本各自独立升级，需配套版本兼容约定（API 版本协商、最低支持版本）；"自用模式"=同一台机器同时装两个包。
- **需求文档已落盘**：`SPLIT-REQUIREMENTS.md`（项目根，v0.3，D1-D15 决策记录 + 客户端 C1-C7 / 服务端 S1-S5 / 数据边界 / 认证授权 / 兼容升级 / 预留能力 / 本期不做），需求阶段收敛完毕，待进入设计阶段。
- **设计方案已落盘**：`DESIGN-SPLIT.md`（项目根，v0.1 草案待评审）。要点：双组件（客户端 Electron 改造 + 服务端 Node+TS 无 UI 常驻）；通信 HTTPS/JSON + `/api/v1/`；**数据读写=服务端语义化 RPC**（/db/query+/db/exec，kb-sqlite/parent-library/agents 平移、对外签名不变）；认证=客户端→服务端→公网（服务端转发+签 session token，孩子跟随家长）；服务端 SQLite 主库（parents/children/settings/scheduler/materials/files/agents）+ 孩子 kb 分区（平移现有表结构）；materials=时间戳 diff 增量拉取；配置=revision + 2min 轮询、下次会话生效；客户端改造 6 点（DataAccess 抽象/配置来源/materials/认证/连接配置/网络 UX）；双包构建；版本协商（/version + min_client_version）；里程碑 M1-M7。待评审 7 项（RPC vs REST、认证上游 www vs benefit-auth、配置生效策略、服务端框架 Fastify vs Hono、服务端包形态 pkg vs 目录、端口 8788、大文件通道）。
- **设计评审拍板（DESIGN-SPLIT → v0.2）**：1 数据读写=语义化 RPC ✅；2 认证上游=**暂接现有 www/api/auth/\***（benefit-auth 就绪后迁移）✅；3 配置生效=下次会话生效 ✅；4 服务端=Node+TS+Fastify ✅；5/6/7（包形态 pkg、端口 8788、大文件统一 materials 流式）保留建议待拍板。下一步可进入 M1 实施（服务端骨架+认证代理）。
- **Windows 服务端部署拍板（DESIGN-SPLIT → v0.3）**：5 服务端包形态=**纯 Node pkg 单文件 + NSIS 安装器** ✅（Windows 装进 Program Files 并注册为 **Windows 服务（NSSM）**：开机即启、免登录、崩溃自动重启；Linux 同代码 systemd）；服务端更新本期手动覆盖升级、自动更新列为后续增强；6 端口 8788 / 7 大文件通道仍保留建议待拍板。
- **M1 实施完成（服务端包骨架 + 认证代理）**：`server/` 独立包（fastify + jsonwebtoken + node:sqlite 内置，tsx dev / tsc 构建）。文件：src/index.ts（Fastify 入口）、config.ts（server-config.json：端口 8788/上游 www/首次生成 JWT secret/data 目录）、db.ts（主库 parents 表 + meta，WAL）、auth/jwt.ts（session token 签发校验，不含 cloud token）、auth/proxy.ts（转发 www /api/auth/*，FastAPI detail 错误提取，502 网络错误语义化）、routes/version|health|auth.ts。端点：GET /api/v1/version（version+min_client_version）、GET /api/v1/health、POST /auth/login|register（转发→license→upsert parents→签 session）、GET /auth/license（公网刷新，401 强制重登，网络错误用缓存降级）。**验证全过**：tsc 0 错；version/health/无 token 401；无效凭证登录 → 透传 www 401 "Invalid email or password"（转发链路通）。真账号登录未测（避免公网凭证暴露，成功路径各环节已分别验证）。运行：`cd server && npm run dev`（8788）。注意：沙箱内 curl 连 localhost 失败，用 node fetch 验证。M2 下一步：数据层远程化（db RPC：kb/agents/parent_lib 平移 + children 表）。
- **M2 实施完成（数据层远程化）**：主库 +children 表（schema v2）；数据布局=每孩子 kb 文件 `data/kb/<parentId>/<childId>.sqlite`（schema 平移自 kb-sqlite v4，含 topic_progress 视图）、agents 全局 `data/agents.sqlite`、父库 `data/parents/<parentId>/parent.sqlite`。**db RPC**：POST /api/v1/db/query|exec（Bearer 鉴权 + child_id 归属强制校验；op 注册表：kb.daily_entries/topics/courses/tags/progress、agents.get/save/history/restore、parent_lib.topics/courses）；children CRUD：POST/GET/PATCH/DELETE /api/v1/children（删孩子仅删行，kb 文件保留防误删）。**验证 20/20 全过**（scripts/verify-m2.mjs，node fetch）：children 创建/列表/改名、kb 读写+进度视图、agents 多 scope 保存/历史、parent_lib、跨家长越权 403、无 token 401、未知 op 400、缺 child_id 400。**踩坑**：①TaskStop 杀 tsx 只杀 shell、node 子进程残留占 8788（EADDRINUSE，404 假象）→ 需 PowerShell 查 Get-NetTCPConnection 兜底杀；②越权测试必须用"另一个家长 token"（同家长多孩子互访是合法的）。M3 下一步：配置下发（revision + 2min 轮询）。
- **M3 实施完成（配置下发）**：主库 +settings 表（key/value_json/updated，schema v3）+ meta.config_revision（bumpConfigRevision 每次变更+1）。端点：GET /api/v1/config/revision（轮询入口）、GET /api/v1/config（全量 {revision, config}）、POST /api/v1/config/set {key,value}（单键写、revision+1，跨设备 2min 生效）；全部 Bearer 鉴权。**验证 12/12 全过**（scripts/verify-m3.mjs）：初始 rev=0、set 后 rev 递增、多键独立更新互不覆盖、无 token 401、缺 key 400。配置键语义（models/scheduler 等）由客户端接入时映射（对应现 app-settings.json / scheduler-config.json / pi-runtime 参数）。M4 下一步：Materials 同步（索引时间戳比对 + 增量拉取 + 离线浏览）。
- **M4 实施完成（Materials 同步）**：主库 +materials 表（id=base64url(relPath)/parent_id/path/type/size/updated_at，schema v4，索引以磁盘 mtime 为 updated_at=发布即时间戳）；依赖 +@fastify/multipart（上传 200MB 限制）。端点：POST /materials/index（带 client_index 比对 → {updates, removed}）、GET /materials/content/:id（流式，base64url id 解码+路径 resolve 防穿越，404/403）、POST /materials/upload（multipart 单文件 + topic 前缀）、GET /materials/list（管理用）。**验证 11/11 全过**（scripts/verify-m4.mjs）：上传/首拉全量/下载内容一致+Content-Length/已同步无更新/覆盖上传→updated_at 变化出现在 updates（增量核心）/跨家长隔离/401/非法 id 404。**关键坑**：①**multipart 死锁**——`for await (req.parts())` 必须**在循环内立即 pipe 消费 file 流**（busboy 等待排空否则死锁，服务端日志特征=请求挂起无 completed）；解法=先写临时文件、循环后 rename；②rename 目标子目录须先 mkdirSync(dirname(abs))；③沙箱 rm -rf 会被 safe-delete 拦截致 && 链中断，清理用 PowerShell Remove-Item 或 dangerouslyDisableSandbox。M5 下一步：大文件（files 表+上传下载独立通道）或先做客户端 DataAccess 改造——待用户确认顺序。
- **M5 实施完成（大文件通道）**：主库 +files 表（id=uuid/parent_id/child_id?/original_name/stored_path/mime/size/created_at，schema v5）；磁盘 `data/files/<parentId>/<id><ext>`（uuid 文件名防穿越，扩展名白名单）。端点：POST /files/upload（multipart + 可选 child_id 归属校验 403）、GET /files/:id（流式 + 归属校验 404）、DELETE /files/:id、GET /files/list。**验证 13/13 全过**（scripts/verify-m5.mjs）：上传（无/带 child_id）/非法 child_id 403/下载内容一致+MIME+Content-Length/跨家长 404/list/删除后 404/401。**健壮性改进**：删除文件 unlink 失败不再抛 500（try/catch 降级：记录已删 + 日志，孤儿文件后续清理）——沙箱 safe-delete 劫持 unlink 暴露了此问题。**服务端里程碑 M1-M5 全部完成**（认证/数据RPC/配置/Materials/大文件）。下一步：客户端 DataAccess 改造（本地 SQLite → 远程 RPC，端到端 UI 无感）。
- **M8-A 实施完成（客户端接入：连接配置 + 通信层 + 认证改连服务端）**。用户拍板**纯服务端模式**（B）：改造后客户端只认服务端、未配置引导填写、本地业务数据功能移除；会话 jsonl 仍本地（D9）。改动：
  1. `electron/lib/config.ts`：+`getServerConnectionPath/getServerUrl/setServerUrl`（data/server-connection.json，去尾斜杠）。
  2. `electron/lib/server-client.ts`（新）：`serverFetch(path,{method,body,token,timeoutMs})` 封装 /api/v1/*，未配置抛 ServerError(0,"未配置服务端地址…")，非 2xx 提取 `{error}` 语义化，网络不可达 ServerError(0)。
  3. `electron/lib/auth-manager.ts`：login/register 改调服务端 `/auth/login|register`（返回 {session_token, license}）；fetchLicense/verifyLicenseWithCloud 走服务端 `/auth/license`（Bearer session_token；401=无效、网络错误 null 降级）；**License.token 语义=服务端签发的 session_token**（cloud token 只存服务端）；不再直连 www。
  4. ipc-handlers/preload：+`server:get_config` / `server:set_config`。
  5. 前端：ParentLogin 顶部服务端地址配置区（未配置提示+保存，已改未保存禁止登录）；GeneralSettings 通用设置加服务端地址区块。
  **验证**：tsc 过滤环境告警后 0 业务错误；`npm run build` 全绿（main 49.9s/renderer 22s）。真实账号登录联调需真实凭证，留待 M7 端到端。**下一步 M8-B**：DataAccess 远程化（kb-sqlite/parent-library/agents → db RPC）。
- **M8-B 实施完成（DataAccess 远程化：agent 数据工具 + AGENTS）**。
  - **服务端 +5 op**（routes/db.ts）：kb.daily_entries.query（date/month/block/title/tag 过滤）、kb.daily_entries.insertMany（批量 OR IGNORE + title/tags 提取）、kb.daily_entries.updateField（raw 字段行改 + 标签同步）、kb.courses.insert（重复返回 ok:false，sort_order 自动 +1）、kb.courses.updateField（COURSE_FIELD_MAP 白名单 + review_count "+1" 自增 + topic 中文名匹配）。**verify-m6.mjs 16/16 全过**。
  - **客户端**：`electron/lib/client-data.ts`（新）=dbQuery/dbExec RPC 封装（currentSessionToken + childIdFromCwd）；`custom-tools.ts` kb_query/kb_insert/kb_update 三个工具改走远程（topics/progress 组装：progress.list 聚合 + courses.list 明细 + safeParseRules/resolveRemoteTopicKey 辅助）；`agent-prompts.ts` 重写=**远程 + 本地缓存**（getAgentPrompt 同步读 data/cache/agents-cache.json，会话构建链零改动；save/history/restore 异步 RPC + 更新缓存；prefetchAgents 登录后预热 parent 版本；**已知限制：child 首次会话（无缓存）回退代码默认**）；ipc-handlers agents:save/history/restore await 化 + 登录后 prefetch。
  **验证**：客户端 tsc 0 业务错误；build 全绿（main 23s）。**下一步 M8-C**：配置 2min 轮询（config-sync：服务端 /config/revision 轮询 → 全量 → 按映射写回 app-settings.json / scheduler-config.json，读取方无感；设置保存挂接 pushConfig）。
- **M8-C 实施完成（配置 2min 轮询）**：`electron/lib/config-sync.ts`（新）：syncOnce（/config/revision 比对本地 data/cache/config-revision.json → 变化拉 /config 全量 → 按 key 映射写回 app-settings.json（app_settings）/ scheduler-config.json（scheduler_config），读取方零改动）+ startConfigSync/stopConfigSync（2min setInterval）+ pushConfig（设置保存同步服务端并更新本地 revision）。挂接：app-settings.ts saveSettings 与 scheduler.ts saveSchedulerConfig 末尾 fire-and-forget pushConfig；ipc-handlers auth:login 成功 → startConfigSync（+prefetchAgents）、auth:logout → stopConfigSync。设计：**文件级配置粒度**（服务端 key=本地文件名），多设备同键后写覆盖（家长场景可接受）。**验证**：tsc 0 业务错误 + build 全绿（main 24.5s）；config-sync 为 electron 主进程模块，端到端轮询留待 M7 GUI 联调（服务端 /config 接口 M3 已验证）。**下一步 M8-D**：Materials 缓存（客户端拉取缓存目录 + 本地索引 + 断网浏览）。
- **M8-B/C 修正（用户指正）**：①AGENTS 首次会话必须取服务端用户版本——SDK `systemPromptOverride` 是同步回调（resource-loader.d.ts:117，不支持 Promise），修正为**会话创建前远程预取**（createChildSession/getParentSession/getParentContentSession 开头 `fetchAgentPromptRemote` 远程 agents.get + 写缓存），systemPromptOverride 同步读缓存即取到服务端最新版；编辑器 IPC（agents:get/child:getAgentsMd）改实时远程取；本地缓存仅作离线降级。②Materials 缓存=**按需检测**（用户明确：需要文件时才检测，命中即用/未命中拉取，非预同步）。
- **M8-D 实施完成（Materials 按需缓存）**：`electron/lib/material-cache.ts`（新）=ensureMaterial(parentId, relPosix)：本地命中立即返回路径（读不联网、断网可浏览）+ 后台 refreshIfChanged 比对服务端版本（/materials/index 带本地 mtime 索引，变化重拉）；未命中 serverDownload（/materials/content/:id 二进制落盘）；id=base64url(relPath) 与服务端一致、客户端本地可算；缓存目录复用 data/parents/<pid>/materials/（asset:// 零改动）；防穿越 normalizeRel。接入：readParentMaterial 改 async（开头 await ensureMaterial）+ ipc await；display_content 渲染走 material:read 已覆盖。tsc 0 错 + build 全绿。
- **M8-E 实施完成（网络失败 UX + 客户端契约联调）**：①材料读取不再静默降级——readParentMaterial 返回值加 error（ensureMaterial 拉取失败透传，区分网络 vs 不存在），前端 TopicDetail.StudentMaterial 显示红色错误条；聊天链路错误提示已存在（⚠️ 显式）。②**verify-m7-contract.mjs 17/17 全过**：material id 规则两端一致、config key 映射、agents 全链路、db RPC、所有非 2xx 均为 {error} 形状、version 契约。③踩坑：out 目录 Windows 文件锁 EPERM → PowerShell Remove-Item 清理后重建。**客户端改造里程碑 M8-A~E 全部完成**。**下一步 M6**：双包构建（客户端包 + 服务端 pkg/NSIS）。
- **M6 实施完成（双包构建）**：服务端构建链=①esbuild bundle（server/scripts/build.mjs → dist/server.cjs 单文件，format cjs + external node:sqlite；**坑：server 为 "type":"module"，CJS 产物必须 .cjs 扩展名否则被当 ESM 解析报 require is not defined**）②@yao-pkg/pkg（scripts/pkg.mjs → dist/learning-server.exe，node22-win-x64；**pkg 内置 node:sqlite 兼容，exe 独立运行 version/health 验证通过**）。安装脚本：③Windows NSIS（scripts/installer.nsi：装 Program Files + NSSM 注册 LearningServer 服务=开机自启/免登录/崩溃重启+AppRotateFiles 日志，需 makensis + nssm.exe 环境编译）④Linux systemd（scripts/learning-server.service：/opt/learning-server + SERVER_DATA_DIR，NoNewPrivileges 加固）。客户端包确认：electron-builder nsis x64 已有（Phase 10 流程），服务端模式已内置（M8-A~E），无需改。server/dist 已被根 .gitignore 忽略。**M6 完成 → 剩 M7 端到端**（GUI 联调：起服务端+客户端真账号登录/多设备/断网 UX；需真实家长账号，建议用户手动验证）。
