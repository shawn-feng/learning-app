# 2026-09-10 工作日志

## 07:30 课程管理·考核要点：结构化题目浏览（类别→题目+该题考核记录）
- 需求：课程管理 → 课程详情 → 考核要点，用列表展示该课结构化考核题目并按类别分类；
  布局=上行类别选择，左下题目列表，右下题目详情+该题历次考核结果记录。
- 数据通道：server 新 GET /assess/questions/:questionId/records（遍历家长全部孩子 attempts 的 per_question 找 questionId）；
  electron assess-admin 加 questionAssessRecords；ipc-handlers 加 assess:courseContent / assess:questionRecords（动态 import）；
  preload 暴露两方法。
- TopicDetail：assess tab 若该课已结构化 → 类别 chips（计数+behavior）/左侧题列表（题干截断+分值+有无答案）/
  右详情（题干/参考答案/评分标准格式化 dims+special）+ 该题考核记录（孩子/时间/得分/对错/评语）；无结构化回退旧 rubric；
  顶右「编辑」仍可编辑旧版全文。
- tsc×2 + build 过。验证：重启 server+客户端，课程管理→论语→某课→考核要点。

## 08:00 题库菜单 + behavior 下移到题目表 + note/knowledge_summary 字段
- 数据层：question_bank 新增 behavior(默认generic)/note/knowledge_summary；behavior 判题以题级为准（同类别可混
  口述/背诵）；存量按所属类别 speech 回填 440 条。listCourseContent 每题带 behavior/note/knowledgeSummary。
- server：GET /assess/questions/list（全量题库+上下文）；questions POST、courses/save 支持 behavior/note/knowledgeSummary；
  questions/:qid/records 改为按孩子返回各自最近一次（未考孩子占位）。
- UI：家长侧边栏新增「题库」菜单 → QuestionBankPanel（左列表/过滤、右详情+各孩子最近考核记录）；TopicDetail 行为标签按题级。
- 验证：背诵题在题级判定 speech 生效（rq1[背诵]）；speech 类别全部含 speech 题。tsc×2+build 过。

## 08:30 家长 agent 工具/规范对齐 behavior 题级化
- assess-tools 描述更新：categories_list/create 说明类别 behavior=新题默认继承、判题按题目级；
  content_save 说明每道题可带 behavior(speech_recite/speech_read/generic)/note/knowledgeSummary（内联样例带 behavior）；
  course_get 输出带题级行为标签与备注/知识点概要。
- assess-guide §2/§4 同步：内联题可显式 behavior，不写继承类别；可带 note/knowledgeSummary。
- 出题逻辑在上一提交已按题级 behavior（attachStructuredQuestions），验证 speech 生效。

## 07:45 抽查：模拟 agent 写题链路（behavior/note/knowledgeSummary）全通过
- 模拟 assess_content_save 给学而篇第三章「句意白话」追加 2 题（generic 带 note/kSum + speech_recite），
  经 saveQuestion→replaceCourseContent→listCourseContent(course_get 同源)→attachStructuredQuestions 抽查：
  - 入库/读回：两题 behavior/note/knowledgeSummary 字段完整；
  - 抽题按「题级 behavior」：句意白话类仅挂 speech 题时 → 抽为 rq[cn_recitation]、refText=answer、
    speech 题排文字题前（证明同类别混题型正确、不再用类别 behavior）；
  - 还原校验：句意白话题数 2=2、测试题残留 0，数据无脏。
- 结论：服务端核心链路就绪。唯一未覆盖=LLM 是否按工具描述产出正确 payload（需真实家长会话 + 新工具描述已随 commit 9f6d47a 打包）。

## 08:00 英语「学习+场景合一」需求拍板 + 方案文档落盘（仅讨论定案，未动代码）
- 用户诉求：把"英语学习"与"场景英语"合一——先学场景词汇/表达，再场景对话练习；合成一门课放一个主题；学习资料合一单页 HTML。
- 已核实现状：english 资料 51 课 = 01-11 基础 + 12-44 yellow wowenglish(33 单集课，各=1 集剧集 lesson-XXX.mp4+字幕 index.html+teach-data.js) + 45-51 语法；无独立"场景英语"目录（wowenglish Unit1-9 即场景）。
- 拍板（四问全按推荐）：①场景=复用 wowenglish 9 单元 ②合一形态=一课两阶段单页（学→练一次进入）③练=场景角色对话（agent 自演角色）④旧课=重排成场景课。
- 合课映射（脚本实测）：12-44 的 33 集→9 场景课（Hello3集/Colour2/Garden4/Toys4/Body4/Food4/Bugs4/Feelings4/Clothes4；媒体 lesson-012~044 连续；汇总词 18-34、台词 57-120 行/课）；保留 01-11+45-51=18 门 → english 共 27 门。
- 方案落盘：仓库根 `DESIGN-english-scene-courses-2026-09-10.md`（需求/映射/单页两区设计/家长库重排/孩子进度迁移(33→9)/角色扮演 prompt/实施 5 任务/待确认 4 点）。
- 待确认：①重排后 sort_order 编号②进度迁移是否本期必做③scene html 练习区是否需要页面强交互（首版建议 agent 会话引导）④中文场景名入 teaching_copy 不入 title。

## 09:00 迁移选择题选项找回（352 道）+ 选择题考核形态落地
- 背景：存量迁移把选择题去选项变口述；用户反馈 352 道「下列哪种/哪个…」指向性题脱离选项无法标准作答。
- 决策：352 道找回选项 → 题面显示选项 → 孩子看选项口头作答 → 判分按正确项规则对照（不进 LLM）。
- 数据：question_bank.options 列(JSON[{key,text}])+访问层/写入口支持；repair-choice-options.ts 从 rubric 找回选项（352 全恢复、正确项 key 自动匹配）。
- 出题 attachStructuredQuestions 下发 options/correctKey/answerText；答题模板渲染只读选项块+改提示语；ExamView 元数据透传；exam-engine 判分 choice 题本地规则判分（判不出兜底 LLM）。
- UI：题库页/课程详情/assess_course_get 展示选项；assess-guide/工具描述补选择题写法。
- 验证：出题带 ABCD/correctKey；规则判分 6 用例全对；tsc×2+build 过。

## 09:40 ISSUE-068 修复：停止 agent 后仍报 "Agent is already processing"
- 根因（已确认）：SDK 在 `isStreaming` 且调用方未传 streamingBehavior 时抛 "Agent is already processing"；前端 handleStop 在调 piAbort 后**立刻** setBusy(false) 显示「已停止」，但 SDK 会话 isStreaming 要等底层流真正终止（abort 内部 await waitForIdle）才清零，于是「已停止」窗口内重发消息命中 SDK 重入守卫。
- 两层防御：
  - Layer 2（主进程兜底）：`ipc-handlers.ts` 的 `pi:prompt`/`pi:prompt_parent`/`pi:prompt_parent_content`/`scene:prompt` 入口顶部加在途拦截——对应 `*PromptAbort` 非 null 即 `return {success:false, error:"上一条消息还在收尾或停止中，请稍候再发。"}`。其中 `pi:prompt_parent_content` 原本**无 abort 登记**，本次补上 `parentContentPromptAbort` 变量 + 登记 + stopped 跳过回发 + finally 复位，并在 `pi:abort` 增加 `parent-content` 分支。
  - Layer 1（前端锁）：`Learn.tsx`/`ParentChatPanel.tsx` 加 `stopping` 态，handleStop 不再立即解禁，等 reply_end 才解禁；所有结束/错误事件解禁时一并清 stopping；按钮 disabled/running 改 `busy||stopping`；加 5s 安全兜底防卡死。
- 验证：tsc 仅环境级全局类型噪音、编辑文件零报错；复现（长思考点停止→立刻重发）应不再出现英文原错，改为按钮「停止中」禁用或中文友好提示。ISSUE-068.md + ISSUES.md 索引已标 ✅。
# 2026-09-10
## PiBridge 协议文档补齐（08:3x）
- 用户确认「把标准协议文档与实现对齐」。核实：MATERIAL-BRIDGE-PROTOCOL.md 存在（9/8 定稿，9 章）但两处滞后；ARCHITECTURE.md 零登记（grep PiBridge=0）且 §2.3/§11 仍是旧实现描述。
- 改：①协议文档 §5 tts.speak 语义改为「resolve=已受理，宿主 FIFO 队列逐句播完」；§9 宿主实现要点重写（asset:// doc=1 顶层文档加载 + material-doc.ts 协议层 rewrite+injectBridge + 加载进度 overlay + HtmlFrame docUrl/epoch 强重建 + tts 双模式队列 + scene() 委托 appCmd('scene.'+cmd) + 就绪 gate）；顶部加 2026-09-10 修订行。
- ②ARCHITECTURE.md：§2.3 渲染链路改为「html 走 asset doc 顶层文档 / 非 html 走 media 代理」；§11 场景节更新为 PiBridge 单轨 + 独立 scene agent（工具隔离）+ isScene 工具级激活 + 聊天与字幕同文同源 + scene:history 回填，删旧 scene:* 直发与 SCENARIO-HTML-SPEC 引用；新增 §12「资料页↔宿主统一通讯标准（PiBridge）」登记架构落点与迁移状态。
- ③.workbuddy/memory/MEMORY.md 核心文档表登记 MATERIAL-BRIDGE-PROTOCOL.md。
## 编程 agent 提示词对齐协议（08:3x）
- 核实：`buildProgrammingPrompt`（electron/lib/programming-agent.ts）9/8 已内嵌「交互通讯协议」段（emit/request/on、manual、命名、tts.speak、scene.*、引用协议文档），但三处滞后。
- 更新：①补 tts.speak 语义（{ok}=已受理，宿主队列顺序朗读多句，页面勿自行排队/等播完/自建合成）+ 已实现能力清单（tts.speak、lookup，其它透传 agent）；②补 handler 可返回 {data}/Promise 作回执 + PiBridge.off 注销；③scene.* 命令与上行事件补全（say/move/act/show|hide/highlight/update/busy/mic.status；ready/item-click/mic.press|release）。
- 另：create_html_lesson 工具描述（custom-tools.ts）加一段——通讯类页面由编程 agent 自动按 PiBridge 实现，学习 agent 在 requirement 里只写产品需求、不要指定 postMessage/事件名（该工具仅孩子会话挂载；家长端接入编程 agent 时共用同一 prompt，自动合规）。
- tsc/build 过，out/main/index.js 已含新语义（grep 命中 3 处）。

## 家长端接入 create_html_lesson（09:0x）
- 背景：buildParentPrompt 五步向导早已写「需要 html 资料就用 create_html_lesson 生成」，但家长两会话白名单从未注册 → 家长 agent 调不到（与 parent_list_children 同类「prompt 教了、白名单漏了」）。
- 改造 programming-agent.ts：getProgrammingAgentSession(childId,sessionKey) → (workDir, scopeKey, sessionKey)；GenerateHtmlLessonInput.childId → workDir + scopeKey（+可选 parentId）。materials/ 分支落盘改用 getParentMaterialsDir(parentId || getCurrentParentId() || DEFAULT_PARENT_ID)（原先恒落 parents/default，多家长下不正确）。
- custom-tools createHtmlLessonTool.execute 会话自适应：isChildSession = basename(dirname(ctx.cwd))==="children"；孩子→workDir=ctx.cwd/scopeKey=childId；家长→workDir=getDataDir()/scopeKey=当前家长 id。返回文案按会话给下一步（孩子 display_content；家长 parent_upload_material + parent_course_save 登记 htmlPath）。description 通用化（孩子 outputs/ 与 display_content；家长 materials/{topic}/ 与上传登记）。
- pi-session 注册：两个家长会话 tools +"create_html_lesson"、customTools +createHtmlLessonTool；家长 prompt L207 后补「复杂/可交互 HTML 交给 create_html_lesson（outputPath=materials/{topic}/xxx.html）」。
- test/programming-agent.test.ts 适配新签名（workDir/scopeKey）+ 补 mkdirSync parents/_guest（原依赖目录恰好存在，会 ENOENT）。4 测试过。
- ⚠️ 核对结论：家长 prompt 提到的工具现全部在白名单（parent_content 仅孩子提示词/数据流描述，非家长调用）。

## 09:40 自定义考核支持"本次考核方法"（排期级覆盖）+ recitePass 真正生效
- 问题：用户建自定义考核 note 写"只考核背诵"，但 note 不参与出题 → 仍按主题方法（珊珊=背诵+句意白话+道理）出题。
- 方案：排期 scope.methodSpec（require/exclude/recitePass，类别名或uuid）覆盖主题 method_spec；优先级 override > perChild > default；require 全解析失败则回退主题方法。
- 改动：assess-selection 加 MethodOverride 解析+优先级；routes/exam config custom 分支传 scope.methodSpec；
  exam_schedule_create 加 categories/excludeCategories/recitePass 参数；ExamAdminPanel 自定义详情显示"本次考核方法"；
  recitePass 下发到题、ExamView 背诵判分按它（原来硬编码 90）。
- 数据修复：把用户那条"只考核背诵"排期（sch_1789002360268_lfmh9a，16 章学而篇）补 scope.methodSpec={require:{背诵:1}}；
  验证每门课只出 1 道背诵题（rq, pass=90）。
- 设计文档 §2.4 补"排期级方法覆盖"。tsc×2+build 过。

## 19:0x 计划域重构实施 S1~S6（全部提交；S4 前端与 S5 物理删列留下一版）
- 提交：先把当日知识点/文档改动提交（999e622），再提交 S1~S3（04a6c62），最后 S4/S5/S6 合并提交。
- S1：study_plan_items 加 course_uuid（幂等）+ 排课/carry 写入 + scripts/backfill-plan-course-uuid.mts（冒烟 1命中/1未命中）✅
- S2：孩子 kb 建 11 张新表（三计划表/exam_plan_courses/plan_recurrences/reward_configs/reward_daily_stats/points_ledger/points_balance/redemption_items/redemption_requests）+ daily_entries 加 plan_id/plan_outcome + courses 加 uuid + course_progress 视图；scripts/migrate-plan-domain.mts（主库按 child_id 拆入孩子库，历史未完成→missed+active=0、不回溯发分）冒烟 13/13 ✅
- S3：新增 worker/plan-domain.ts（expandRecurrences + runPlanStat：三域判定/到期 carry 复制/归属日统计/积分结算幂等）；scheduler 切换（plan=仅展开重复规则、stat=runPlanStat）；recording 注入当天生活计划清单 + 两份提示词同步（写 daily 带 planId/planOutcome、去 todo_list 勾选）；kb_insert 支持 planId/planOutcome。冒烟 12/12 ✅（含修掉「复跑余额重复累加」bug：改为按流水重算余额 + 仅 changes>0 才计入）
- S4（服务端部分）✅：/study-plans 列表/今日/新建/更新/删除 全部切到孩子 kb study_plans（today 另含 life_plans，items 增 owner 字段区分必须完成项/加分项）
- S5（部分）⚠️：掌握度读取口径已切到 course_progress 视图（listCourseStatus 输出 lastLearnedAt/lastExamRate，mastery 恒空）；**物理删除 mastery/exam_mastery/first_learned 列 + 家长库进度列/视图 + 17 文件清理留下一版**——理由：删列必须与全部读写点同时改完，否则运行时断裂，风险不可控。
- S6：server tsc 0 错、根 tsc 仅 5 条环境噪音、npm run build 通过；ARCHITECTURE §4/§5 重写为新真源；设计文档已带决策汇总。
- 下一版待办（明确遗留给后续）：①S4 前端（移除勾选、家长审计与修正、积分设置页与孩子积分页、未完成解锁提示）②S5 物理删列与 17 文件清理 ③跑 migrate-plan-domain/backfill 到 201 生产库 ④积分兑换目录家长端编辑 UI ⑤旧 todo_* worker 函数与 kb.todo.* ops 删除。

## 18:42 积分域收口（全部定案，表结构冻结）
- 最后两条定案：①不合格档扣分 **待办 −10 / 考核 −15**（仅必须完成项触发，家长端可改）；②**不设最小分母门槛**（提前完成 1/1=100% 照常发满档）。
- DESIGN-reward-points 追加 **§九 决策汇总（表结构冻结版）**：17 条决策 + 4 条实现红线（门控必须按阈值判／负分档对加分项强制0／归属日是分母口径唯一依据／未解锁不写流水只写 stats.gate_ok=0）；§七 改为"决策记录（全部已定案）"。
- 积分域现状：8 张表（reward_configs/reward_daily_stats/points_ledger/points_balance/redemption_items/redemption_requests + 依赖计划域三表与 daily 两列）、结算挂 stat tick、默认档位分值全部冻结 → **可进入实施排期**。
- 关联：计划域重构（DESIGN-plan-domain-rewrite，7 项待确认 A~G 亦全部定案）与积分域同批实施，积分依赖三表 creator 分组。

## 18:38 术语与提示策略定案（改入两份设计文档）
- **组名定案**：家长制定 = **「必须完成项」**（孩子必须完成、未达负分档会扣分）；孩子自定 = **「加分项」**（只加不扣、受门控）。库里仍存 `owner/creator=parent|child`，展示名与存储名分离。已加 §3.6 术语对照表。
- **"未解锁"提示策略定案 A（双向提示）**：家长端文案点明"必须完成项未达门槛卡住孩子加分"+建议动作；孩子端中性措辞"本次未加分：必须完成项还没完成"（不指名道姓、避免"都怪爸妈"）。§2.4 已改写为定案版。
- ⚠️ **命名撞车提醒（已写入文档）**：组名"必须完成项"（=家长制定，组层级）与单条计划的 `task_type='required'`（必做，条目层级）是两个不同层级的概念，实现/文案时勿混。
- 计划域文档 §1 已补展示名说明。剩余待确认仅 2 条：不合格档扣分值（建议 todo −10 / exam −15）、最小分母门槛（建议先按现状实现观察）。

## 18:25 积分口径再定案：归属日 / 100% 独立档 / 加权合并 / 兑换先只建表
- **跨天窗口按"归属日"结算**（取代"当天到期"口径）：每个计划只在一天进统计——**提前/按时完成 → 计入完成那天**（分母+1分子+1，窗口其它天不计）；**missed → 只计入最后一天 due_at**（分母+1分子+0，前面不计）；**pending（未到期）不进分母**；cancelled 剔除。单日窗口与旧行为一致；carry 不重复扣分（missed 属昨天、新计划是 pending）。
- **100% 是独立档**：良好档左闭右开 [80%,100%)、优秀为闭区间 [1.0,1.0]。默认档位改为 **4 档**：待办 [0,60%)不合格 / [60%,80%)合格 / [80%,100%)良好+10 / [100%,100%]优秀+20；考核 [0,80%)不合格 / [80%,90%)合格 / [90%,100%)良好+10 / [100%,100%]优秀+20（两来源良好/优秀分值相同）。
- **一天多次考核**：定案按 **Σ得分/Σ满分** 加权合并。
- **兑换目录**：定案本期**只建表**、不给种子定价，家长端自行编辑（kind 扩为 inapp|video|game|skin|parent）。
- 提醒项：提前完成时当天分母可能仅 1 条（1/1=100% 命中优秀）——已列为待确认 7"最小分母门槛"（建议先按现状实现再观察）。
- 剩余待确认：4 不合格档扣分值（建议 todo −10 / exam −15）、7 最小分母门槛、8 未解锁是否提示家长。
- 18:30 补充：§2.4 补写"未解锁"含义与提示策略三选一（A 双向提示并给孩子端中性措辞／B 仅孩子端／C 都不提示）——数据侧 reward_daily_stats.gate_ok=0 已记录，提示仅是 UI 决策。

## 18:05 积分口径三条定案（改写 DESIGN-reward-points §一/§二/§三/§五/§六/§七）
- **扣分按"当天 missed 的比例"**（完成率命中负分档），**不按 missed 条数**；每天独立结算——昨天 missed 属昨天，carry 复制来的是**新 pending**（不计 missed），故不重复扣分；"不设上限"=负分档按天累积不封顶。
- **当天分母口径**：该组「当天到期(due_at∈当天) + 必做 + count_in_rate=1 + 非 cancelled」；跨天未到期 pending 不进分母（已列为待确认 2）。
- **分档自由配置**：家长端"积分奖罚设置"按比例配区间→分值；**档数默认 3 档、可增删/改名/改区间**；todo_tiers_json/exam_tiers_json 存 [{min,max,label,points}]，校验连续覆盖[0,1]且不重叠。
- **门控连带修正**：因档位可改名/增删，门控**不能存"优秀/良好"这类档位名**，改为按阈值存 —— `todo_gate_parent_min_rate=1.0`、`exam_gate_parent_min_score=0.9`。
- points_ledger 调整：reason_code 改为来源级枚举（todo_award/todo_deduct/exam_award/exam_deduct/optional/redeem/adjust），**唯一索引去掉 reason_code**（档位可自定义）改为 (child_id,biz_date,type,source_table,source_id)；新增 `rate` 与 `meta_json`（结构化快照：命中档位/owner/计数）以保证每分变动可解释可审计。
- 计划域文档 carry 条目措辞已同步（改为"按比例扣分、不逐条扣"）。
- 剩余待确认：2 当天分母口径、3 默认档位分值（todo −10/0/+20；exam −15/0/+30）、4 未解锁是否提示、5 一天多次考核按Σ得分/Σ满分加权、6 兑换目录定价。

## 18:00 积分域表结构（按制定人分组 + 门控 + 每分记原因）—— 改写 DESIGN-reward-points §一/§二/§三/§五~§八
- 两来源：①todolist 完成率（60合格/80良好/100优秀）②考核得分率（80/90/100）。**各自独立结算**、阈值各一套。
- **按制定人分组**（家长组/孩子组）分开算完成率/得分率、分开分档。
- **门控（孩子组才能加分）**：待办=家长组须**优秀100%**；考核=家长组须**良好≥90%**（**两阈值不同，易错点，已单列对照表**）。**孩子组永不扣分**（不合格只不加）。未解锁时 gate_ok=0、不写流水、家长端提示。
- **每分变动记原因**：points_ledger 增 reason_code（todo_good/todo_excellent/todo_fail/exam_*/optional/redeem/adjust）+ 人类可读 reason + source_table/source_id + operator；唯一索引 (child_id,biz_date,type,reason_code,source_table,source_id) 保幂等（stat 重跑不重复发分）。
- 新表：reward_configs（todo_tiers_json/exam_tiers_json/todo_gate_parent_level=excellent/exam_gate_parent_level=good/child_no_deduct/optional_points）、**reward_daily_stats（PK child_id+date+source+owner，含 rate/tier/gate_ok/points_awarded）**、points_ledger、points_balance（余额缓存，真源仍是流水）、redemption_items/requests（兑换扣分也写流水）。
- **命名统一**：原计划域设计的 plan_daily_stats 因无法表达 source×owner 分组，**并入 reward_daily_stats**（计划域文档已全局改名+加说明）。
- 新增结算流程（§六，挂在 stat tick，reward_settle 游标幂等）与 5 条待确认：家长组不合格扣分 Z/C 取值与"是否按天累积不封顶"（建议扣分只在每日分档层面发生、不按 missed 条数单独扣，避免双扣）、未解锁是否提示、一天多次考核按总得分/满分加权、具体分值默认值、兑换目录定价。

## 17:45 定案：计划表落孩子库（B）/ 数据迁移（C）/ 截止精确到时间（D）
- **B 定案：三张计划表 + 考核明细 + 排期规则 + 统计表全部落孩子 kb**（用户："学习考核表本来就是孩子的学习考核记录"；生活记录与 daily 也已在其库）。原则成文：**内容在家长库（课程/题库/资料），孩子的记录在孩子库（计划/成绩/进度/daily/统计），账号与调度在主库**。→ `courses`+计划表+daily 同库，**掌握度视图可直接建在孩子 kb**（原跨库问题消失，`/courses/status` 改读视图）；跨库仅剩对题干/知识点的逻辑引用（无外键）。
- **连带（重要）**：worker/家长端/agent 的计划读写全部改走 kb RPC（现状 study_plan_items/exam_schedules 是主库直查）→ 需新增 kb `plan.*` op 系列（读入 queryHandlers、写入 execHandlers）；家长端跨孩子展示改为按孩子逐个查/服务端循环聚合。
- **库搬迁**：study_plan_items/exam_schedules/exam_attempts(含 per_question) 从主库**按 child_id 拆分迁入各孩子 kb**；迁移顺序+分组对账+迁后主库表停写只读保留一版。
- **C 定案：把数据做迁移**（不做长期只读兼容）——todo_items 的孩子自规划项→life_plans、统计→plan_daily_stats，迁完即下线该表。
- **D 定案：截止时间精确到时间，不设独立 due_time 列**——start_at/due_at 用 `YYYY-MM-DD HH:mm:ss`；只给日期时 due_at 补 23:59:59、start_at 补 00:00:00；日期型信号（daily.date）匹配时展开为当天 00:00:00~23:59:59 与窗口求交；"几点前完成"提醒能力由 due_at 承载。
- 同步更正 DESIGN-reward-points §五：**积分域 4 表也放孩子库**（输入全在孩子库），家长审批走服务端接口读写孩子 kb；事实源改为孩子库 exam_plan_courses + plan_daily_stats。
- 设计文档待确认项已全部定案（A~G），可进入 S1。

## 17:40 定案：missed 无扣分上限 / 仅家长可取消 / first_learned 彻底删除（改入 DESIGN-plan-domain-rewrite §1/§9/§10）
- **missed 扣分不设上限**（就是要让孩子完成）；**`cancelled`（仅家长可操作）是唯一止损手段**——取消后不再 carry、不计分母。原 `exempt` 命名统一为 **`cancelled`**（两份文档已同步替换：三表 DDL status 注释、plan_daily_stats 的 cancelled_count、分母口径、盘点 §13.2/13.3/13.5/13.6）。
- **G 定案：first_learned 彻底删除**（孩子 kb + 家长库 + 判定/展示/工具/前端）。删后的"学习时间"来源：**不新增列**，把现有 `last_review` 语义扩展为"最近学习时间（学或复习都更新）"作 lastLearnedAt 输出源（命名债务可接受）。§9 清理清单已扩为 mastery+first_learned 双列清理，并补 worker/tasks.ts 的判定改动。
- 待确认精简为 3 条实现方式选择：B 聚合接口(B1推荐) vs 同步建视图；C todo_items 保留只读兼容期(C1推荐) vs 直接下线；D life_plans 是否保留 due_time(D1推荐，保"几点前完成"提醒能力) — 并已写清 due_at(哪天截止,日期粒度) vs due_time(当天几点截止) 的区别。

## 17:30 三条定案：carry 换形态 / 学习不辨首次 / E1+F 定案（改入 DESIGN-plan-domain-rewrite §1/§4/§11）
- **carry 保留但换形态**（取代"取消 carry"）：窗口到期未完成 → 原行判 missed（留痕+扣分依据）+ **复制一条新计划到当天**（start_at=due_at=当天、origin='carry'、新 id、其余照抄）；延后时窗口一律当天，不继承原窗口（不管原计划是否跨天）。提醒：长期不做会每天累积一条 missed → 积分需处理扣分梯度。手动改 due_at 路径仍保留。
- **学习域不再区分首次/复习**：first_learned 语义不需要；判定=该课程有学习发生（学习时间）+ 存在窗口覆盖该日期的未完成学习计划。**匹配成功后 stat 回写该日 daily 学习条目的 plan_id**（+plan_outcome='done'）→ daily 的 plan_id 有两条产生路径（生活=recording 写；学习=stat 回写）。
- **E 定案 E1**：多天窗口=容错期限（窗口内任一天学习信号即 done），**一条计划不拆**；分次任务改用 recurrence 展开。
- **F 定案**：逾期迟到信号不回溯改判（不扰已结算）；missed 后已复制新计划，迟到完成信号命中新计划。
- 新增待确认 G：first_learned 列是否彻底删除（判定已不用，但"首次学习时间"可能仍被 learning-summary/看板/course_status 展示）。
- 剩余待确认：B 聚合接口替代视图、C todo_items 兼容期、D due_time、G first_learned 列。

## 17:20 窗口区间匹配规则（补入 DESIGN-plan-domain-rewrite §11.6）
- 用户要点：一个计划可能跨多天完成——匹配按**窗口区间** `start_at ≤ 信号日期 ≤ due_at`，不是同日相等；同日窗口=当天语义，跨 2-3 天只要信号落在区间内即命中。
- 信号日期分域：学习=courses.first_learned(new)/last_review(review) 的日期（不用 daily）；考核=场次 submitted_at；生活=daily 条目 date（用户说的"daily 日期在范围内"）。
- 配套：missed 判定按 due_at（窗口未闭合保持 pending，这是"取消 carry+动态 todolist"的前提）；窗口缺省 start_at=due_at（默认单日，日期粒度）。
- 新增待确认 E：多天窗口语义——E1 容错期限（推荐：窗口内任一天学习信号即 done）vs E2 分次分解（需进度字段，与 courses 二元语义冲突，本期不做；要分次就拆多条计划/recurrence 展开）。
- 新增待确认 F：逾期信号补判——已判 missed 后迟到信号是否回溯改判？建议不改（保稳定，走家长修正权）。

## 16:58 设计修订：recording 只负责生活计划（补入 DESIGN-plan-domain-rewrite §11）
- 定案：recording **不注入三表清单，只注入当天生活计划清单**。理由：学习计划可用「课程名+时间」定位（courses 信号即证据，无需 daily）；考核在独立页面完成、提交时直写考核计划表，不与 agent 对话，recording 不管考核。
- 修正后的信号分工：学习=courses.first_learned/last_review 变化→stat 按课程+日期匹配 study_plans；考核=提交事务直写 exam_plans/exam_plan_courses（**唯一非 stat 写入者例外**，实现集中在 applyExamResult）；生活=only daily 证据（plan_id+outcome）→stat 判定。→ **只有生活域需要 recording**。
- 学习计划匹配规则（必须明确）：优先 course_uuid（S1 回填后）退化 course_name；多条命中取「窗口覆盖该日期+pending+最近 created_at」；new/review 需与 first_learned/last_review 信号对应；匹配不到则不建行不判状态。
- daily 两列用途收敛：plan_outcome/plan_id 当前只由生活计划使用（学习域关联改隐式为 日期+课程名）；列保持通用不加 plan_type。
- applyPlanEvents 职责收窄为只应用生活证据；plan_daily_stats 汇总口径不变（三域完成来源不同）。

## 16:50 第三步详细设计落盘：DESIGN-plan-domain-rewrite-2026-09-10.md
- 内容：三层模型（计划/排期/执行）；完整 DDL（study_plans/exam_plans/exam_plan_courses/life_plans/plan_recurrences/plan_daily_stats + daily_entries 两列 plan_id/plan_outcome）；家长库 mastery 两列删除；worker 改造清单（gen→expandRecurrences、stat 重写含 applyPlanEvents、**carry 取消**、recording prompt 注入三表清单+输出 plan_id/outcome、summarize_conversation 同管线只判当天）；迁移映射（study_plan_items→study_plans、exam_schedules→exam_plans、exam_attempts+per_question→exam_plan_courses、todo_items(child)→life_plans、家长项/历史归档不迁、child_todo_stats→plan_daily_stats）；接口/工具/前端改造（/today 三表 UNION、移除勾选、家长审计视图、todo_list→plan_list）；积分预留（四字段落计划表，category 列可省）；分期 S1~S5；**mastery 清理文件级清单（17 文件）**。
- 关键设计决策：①**取消自动 carry**——窗口模型动态顺延，到期即 missed，顺延需显式改 due_at（待确认 A）②不建 SQL 视图，跨库改由 /courses/status 聚合输出（待确认 B）③客户端 courseMastery 聚合随本次删除。
- 待评审；评审后按 S1（course_uuid 回填）起步。索引 MEMORY.md 已登记本设计与积分设计、盘点文档。

## 16:45 生活域修订：事件载体=daily，取消 plan_events（补入盘点文档 §13.6）
- 核实：孩子 agent 的每日汇总工具 = `summarize_conversation`（electron/lib/daily-summary.ts，按天、date 可省），即**按需触发 recording**，与定时 recording 共用同一管线与提示词；recording-prompt.ts §93/103/112 已有先例（汇总后用 todo_list 核对孩子自定任务 + 写「今日时间规划」daily 条目）。
- 结论：①**撤回 plan_events 建议**——daily 条目即事件记录，实时通道=已存在的 summarize_conversation，零新表；②daily_entries 必须加两列：plan_id + **plan_outcome(done|missed|unknown)**（recording 显式输出；只有 plan_id 分不开"提到"与"完成"，孩子说"今天没做"也是事件）；③recording 改造收敛一处：prompt 注入当天三张计划清单 + 把"核对自定任务"段扩展为核对三表 + 输出 plan_id/outcome；原"来源=家长的项绝不动"改为分工表述——recording 只写证据，三表状态一律 stat 写。
- 新增规则：只判当天/窗口内（历史汇总可写证据但不得回溯改已结算状态）；状态更新幂等（复用 worker_state last_key）。
- 重申风险：孩子主动发起汇总 + 到期无事件判 missed → 误伤不低（做了没说），故 §13.3 家长修正权（撤销/exempt）是必需件。

## 16:10 生活计划判定改 LLM 驱动（补入盘点文档 §13，取代 §12 勾选状态机）
- 定案：①courses.mastery 直接删（不留备注）②不允许勾选，生活完成只能 LLM 判断，daily 记详情供家长事后审计③recording 是兜底——不操作三表，写 daily 带 plan_id，stat 据此更新三表。
- 引入 plan_events 表（建议新增）：daily 由 recording 单源写、孩子 agent 不能写 daily（08-13 决策），故实时"孩子说做完了"需事件落点；事件=判定依据+审计记录。字段 id/plan_id/child_id/date/summary/quote/source(agent|recording)/created_at。
- 状态机简化（取代 claimed/自勾）：pending→done(有事件)/missed(到期无事件)/exempt(仅家长豁免或撤虚报)。**状态唯一写入者=stat**，agent 与 recording 只写事件不改状态。
- 三个必须明确：①无事件默认 missed（会误伤）②**家长修正权必须保留**（撤销 done→missed、标 exempt，且能看事件摘要+对话原文）③积分按 done，missed 扣分，exempt 不进分母。
- recording 改造：worker 构建 prompt 时注入当天三张计划清单（含 uuid），recording 无需新取数工具；prompt 增加"提取事件→核对计划行→写 daily 带 plan_id、不编造"；daily_entries 加 plan_id；同日多事件幂等。建议另做实时通道（agent 工具 plan_event_log），recording 只兜底。

## 15:45 掌握度定案 + 生活计划完成判定（补入盘点文档 §12）
- 定案：掌握度只取**最近一次考核**得分率（视图 lastExamRate/lastExamAt）；学习状态只查**最近学习时间**（lastLearnedAt），不给学习侧掌握判断。
- 连带：courses.mastery（引导掌握度）彻底下线——孩子 agent 写 mastery 的能力无载体（涉及 kb.courses.updateField(mastery) 调用点、孩子 prompt、course_status/learning-summary 输出、家长端展示）；建议改为"学习计划 result 自由文本备注（不参与计算）"或明确删除，二选一。exam_mastery 下线、exam_attempts 迁移后退场；course_status 语义改为"学习时间+考核得分率"。
- 生活计划完成判定（唯一无系统事件源域）：必须人工声明。四通道=①孩子当日勾选(主)②家长确认/代勾(兜底)③对话声明(agent 勾,可选)④窗口到期 stat 判 missed。防虚报=need_confirm 开关三级（确认制 claimed→done 积分按 done 结算/自勾制/混合制，推荐确认制）。状态机 pending→claimed→done；pending|claimed→missed；任一→skip(豁免不计分母)。
- 规则定死：允许当天补勾、禁跨天补勾；日界=local date，窗口到期默认当日 23:59；完成率分母=必做且非 skip，missed 计入未完成（=积分分档输入）；每日固定生活项由排期表展开成每日行（streak 才能连续）。
- 三域完成信号：学习=自动(courses 变化回写)、考核=自动(提交得分)、生活=人工(勾选+确认)。

## 15:15 计划域三问评估 + 拍板（补入盘点文档 §11）
- 拍板：①三张独立表（学习/考核/生活）②历史数据迁进新表。
- Q1 掌握度：courses.exam_mastery(考核回写)+mastery(引导)两列可去掉改视图（消灭掌握度三处并存）；引导掌握度无载体→并入"学习计划表.result"；**视图分列暴露**（学习 result=执行/表现、考核得分=掌握度数值），不合成一个数（学完一次会刷分）；exam_attempts 历史明细被"考核计划表(场次头)+考核计划课程表(逐题明细)"接管后退场；视图需索引 (course_uuid, submitted_at)。
- Q2 daily 关联：不在计划表存 daily id（PK 是 date+block+title 无自增 id，且一次计划→多条 daily 一对多）；改 **daily_entries 加 plan_id 反向归属**，plan_id 由 stat 事后按 (date,课程) 匹配回填（不让 recording LLM 产 ID），计划表 result 只存摘要。
- Q3 recording 是否要改：**核实=不用改**。runRecordingServer(tasks.ts~150-215) 只读 kb.progress.list+daily_entries+主题标签+对话→写 daily，全程不碰 todo_items。要改的是 todo_gen（取消物化/改排期展开）、todo_stat（重写为直接改三表 status/done_at/result + 汇总）、carry（改顺延三表）；附带 kb.todo.* ops、/today、孩子端勾选、todo_list 工具全切三表；**孩子自规划入口要保留**（放生活计划表 制定人=孩子）。
- 改造顺序更新：①course_uuid 回填 → ②三表+排期表+历史迁移 → ③worker 重写+daily.plan_id → ④掌握度列下线+视图+attempts 退场 → ⑤前端/工具切换 → ⑥积分（P0 todo 字段改造改到三张计划表上）。

## 14:50 计划域重构提案评估（用户三表提案 vs 现状，补入盘点文档 §10）
- 用户提案：学习/考核/生活三张计划表（uuid/开始/完成时间/制定人/状态/结果）+ 考核计划课程表（计划uuid/课程uuid/知识点uuid/题目uuid/得分）+ 排期表自动排期。
- 评估结论：方向正确（计划实体化/课程uuid化/考核内容结构化/生活计划补位，全是已登记缺口）；唯一方向性风险=题目计划期固化（破坏随机抽题防背题）→ 化解：计划期只存课程+知识点范围，题目开考抽题后回填。
- 学习结果归属（用户提问）：计划表只存轻量执行结果（status+result 引用 daily_entry），过程留 daily_entries 不双写，掌握度留 courses/exam_attempts。
- 关键提醒：得分/attempt 双真源风险（表内得分=回填快照+attempt_id 引用）；todo_items 保留为统一当日执行视图（source→plan_type+plan_id），孩子自规划不丢落点；执行链 plan→schedule→todo→stat 变长，worker 三件套重构是最大成本。
- 建议三层模型（计划/排期/执行）+ 渐进三步：①course_uuid 回填（P1 前置）②生活计划+todo 积分字段（P0）③计划实体化+排期表+worker 重构。
- 待拍板：三张独立表 vs plans 主表+类型明细（积分跨类型聚合倾向后者）；旧数据是否迁移。

## 14:00 知识点实体落地（考核数据块结构完善，全程 tsc/build/冒烟过）
- Schema（家长库，ensureAssessContentSchema 幂等）：新表 knowledge_points(id,course_uuid,name,seq,UNIQUE(course_uuid,name))+ccq 加 knowledge_point_id 列（关联落挂载关系，同题跨课可挂各自知识点）。
- 数据层 assess-content.ts：listKnowledgePoints/getOrCreateKnowledgePoint；replaceCourseContent 支持 knowledgePointIds（与 questionIds 等长）；listCourseContent JOIN kp 输出每题 knowledgePointId/Name；listAllBankQuestions contexts 带 knowledgePoint。
- 路由 exam.ts content_save：题级 knowledgePoint/knowledgePointId 解析（id 须属本课>名称 getOrCreate）+ item 级 knowledgePoint 默认；内联题未给 knowledgeSummary 时回填知识点名。content_get 自动带出。
- 下发/落库：assess-selection base 加 knowledgePointId/Name；ExamView questionMetaRef+post+handleSubmit 回填+origin() 四处；per_question 原样 JSON 落库（新字段存活），按知识点聚合掌握度后置。
- 工具/规范/UI：assess-tools 两工具描述+course_get 输出关联知识点行；assess-guide §4 知识点关联段+§6 纪律第4条（同名复用/不编造）；QuestionBankPanel contexts 显示 · 知识点、TopicDetail 详情加知识点行。
- 存量回填脚本 server/scripts/backfill-knowledge-points.mts（按课 knowledge_summary 去重建 KP+精确匹配回填 ccq，仅填空不覆盖；跑生产库属部署步骤待做）。
- 验证：server tsc 0 错（修1处类型断言）；根 tsc 仅5条已知环境噪音；npm run build 过；临时冒烟（建课→挂3题→回填→listCourseContent 断言）SMOKE PASS。EXAM-ARCHITECTURE §3/§5.2/§6 已同步。
- 注意坑：家长库新插课程行的 uuid 要到下次 open 才回填（ensure 迁移），同 session 内 getCourseUuid 返回 undefined——冒烟时踩到，真实流程跨请求无影响。

## 13:35 三项拍板 + 表结构评估（补入盘点文档 §9，未动代码）
- 拍板：①知识点建实体，题目关联课内知识点 ②生活记录维持 daily_entries 现状 ③清理家长库课程表进度残留。
- 核实：daily_entries(date/block/title/raw/tags, PK date+block+title)✅不用改；家长库 courses 五个进度字段（status/mastery/first_learned/last_review/review_count）**无任何写入方**（学习只写孩子 kb，upsert 仅回显旧值），唯一读方=家长库 topic_progress 视图→parent_lib.progress.list→listParentTopics→家长页主题列表 learned/total——拆分后语义错误（进度按孩子在孩子 kb，家长库跨孩子共享）。
- 方案（§9）：知识点表 knowledge_points(id,course_uuid,name,seq,UNIQUE(course_uuid,name))+ccq 加 knowledge_point_id 列（关联落挂载关系，符合"题目语义随挂载"约定，同题跨课可挂各自知识点）；连带 assess_content_save/assess_course_get/attachStructuredQuestions 元数据/exam_attempts.per_question 加 knowledgePointId（先溯源，掌握度聚合后置）/TopicDetail+QuestionBankPanel 展示/存量按 knowledge_summary 文本匹配回填。清理：删五列+视图+handler+listParentTopics 调用，家长页主题进度列建议删（跨孩子列表）。均幂等迁移、不动孩子 kb/主库。

## 13:20 架构设想 vs 当前系统对照（用户口述架构模型，补入盘点文档 §8）
- 用户模型：三大块（基础数据=课程+题库 / 拟定计划 / 奖罚）+ 两级归类（学习主题→课程=知识点集合，课程为最小学习/考核单位，不再往下归类）+ 课程五要素（主题归属/知识点集合/教学内容/教学方法/学习资料）。
- 核实结论：**高度吻合，无需推翻重构**。三级块中课程+题库+计划均 ✅；课程五要素全有落点（teaching_copy=教学内容、topics.method+courses.lesson_method 两级教学法、html_path+materials=学习资料）；课程=最低归类层级与现状一致。
- 差异/决策点：①知识点无独立实体（题目+knowledge_summary 隐式承载，符合"不往下归类"本意，暂不改；将来要按知识点聚合掌握度再加实体）②生活记录未归位（建议归基础数据=成长档案，不参与计划/奖惩）③家长库 courses 残留进度字段（status/mastery/first_learned，真源在孩子 kb）可清理 ④考核方法两层（主题 perChild+排期覆盖）比设想细一层，不冲突 ⑤奖罚块唯一空白。
- 已补入 `需求盘点-三大需求场景分析-2026-09-10.md` §8（三张对照表+决策点）。

## 10:45 数据结构优化建议（积分前置，补入盘点文档 §6 + 修订 DESIGN-reward-points）
- 核实：child_todo_stats 已有 date/total/done/parent_*/self_*/rate/streak（结构健康）；生活记录存孩子库 daily_entries 表。
- P0：todo_items 加 task_type(required/optional)/count_in_rate/points/category(study|life 正交，顺补生活安排分类缺口)；child_todo_stats 追加 required_total/required_done/optional_done/optional_points——**取消原 DailyTodoScore 独立表**（一表两用）；积分域 4 表（reward_configs/points_ledger 流水真源不存余额列/redemption_items/redemption_requests）放服务端主库（家长审批跨账号），结算挂 stat tick+worker_state reward_settle 游标。
- P1：课程引用 uuid 化（per_question.course 与 study_plan_items.course_name 均存标题字符串，改名断链；courses.uuid 已存在，写新读旧+回填）；掌握度定口径=服务端 exam_attempts+exam_mastery 唯一事实源，客户端聚合仅展示。
- P2：生活表现奖惩走路线 A（todo category='life' 复用积分链路，零新表），不改 daily_entries。
- 已修订 DESIGN-reward-points §五（存储归属+结算挂 worker+事实源声明+child_todo_stats 扩展取代 DailyTodoScore）。

## 10:25 三大场景数据交互现状核实（补入盘点文档 §5）
- 已打通自动通道：①stat tick 学习进度→自动勾计划/todo+汇总（一→二）⑤学习计划→fixed 档考核排期直读 study_plan_items（二内部）—考核提交回写 courses.exam_mastery（exam.ts:1319）。
- 半自动：②/course_status 一站式聚合（孩子 kb courses+exam_attempts 按课正确率+存量复习计划）→家长 agent 对话决策排课排考，无自动算法。
- 已拆除（有意）：⑥reinforcePlan 考核→复习计划自动回写 09-09 停（exam-engine 返回空兼容），planReviewAt 仅剩存量。
- 未建：③完成率→积分（数据齐，todo 缺 type/countInRate 字段）④exam_attempts→积分 ⑦生活记录→奖惩（自由文本无计分项）。
- 风险：掌握度三处并存（mastery/exam_mastery/客户端 courseMastery），积分报表应以服务端 exam_attempts 为唯一事实源；考核聚合按课程名字符串匹配，改名断链。
- 已补入 `需求盘点-三大需求场景分析-2026-09-10.md` §5（通道明细表+风险）。

## 10:05 需求盘点：三大需求场景 vs 已实现功能（分析文档落盘，未动代码）
- 用户定义三场景：①辅导学习+记录生活（课程/考核/生活记录）②制定每日计划监督完成 ③按完成情况与掌握度奖罚。
- 盘点结论（依据 ARCHITECTURE/EXAM-ARCHITECTURE + grep 佐证）：场景一 ~85%（课程双闭环+场景英语成熟；生活记录只产不显，缺家长端成长档案页）；场景二 ~80%（学习计划 study_plan_items+todo 双列表+stat 回写、考核排期完整；生活安排无专属设计、完成率报表薄）；场景三 ~5%（积分/奖励代码零实现，仅 DESIGN-reward-points 草案；但输入数据 todo 完成情况+exam_attempts 均已备齐）。
- 落盘：仓库根 `需求盘点-三大需求场景分析-2026-09-10.md`（逐项对照表+缺口+补齐优先级：积分模块→完成率报表→成长档案→生活安排→英语场景课重排）。

## 09:45 积分奖励机制设计讨论（讨论定案，未动代码）
- 用户诉求：APP 内孩子完成任务获积分。精神=不兜底/有扣分/必做优先/选做门控/逆水行舟。
- 待办：当天完成率分 60%(达标不扣分,0)/80%(良好,+15)/100%(优秀/满帆,+30) 三档；<60% 可扣分(默认-10,可关)；每 todo 可标 countInRate 排除纯提醒项；任务分必做/选做，选做仅当必做=优秀(满帆)才计积分(optionalItemPoints 默认+5)。
- 考核：得分分 <80%(退步,-15)/80%(达标,0)/90%(优秀,+30)/100%(登顶,+50) 四档；阈值与档名按孩子定制(RewardConfig)。
- 兑换：①APP内(视频/网页游戏/皮肤,即时扣) ②向家长申请(现实权益,家长履约后扣)。
- 数据模型：RewardConfig / TodoItem(type,countInRate,points) / DailyTodoScore / Assessment(tier) / PointsLedger(流水真源) / RedemptionItem / RedemptionRequest。
- 落盘：仓库根 `DESIGN-reward-points-2026-09-10.md`（规则+默认阈值+数据模型+落地建议+待确认4项）。
- 待确认：①低于基线扣分是否默认开 ②进步奖/连续奖励是否二期 ③档名是否用建议命名。

## 09:30 考核模块文档收口（EXAM-ARCHITECTURE.md 新建）
- 新建 EXAM-ARCHITECTURE.md（真源）：模块地图/三表数据模型/两层考核方法/端到端流程（排期→取题→答题→判分→落库）/agent 工具清单/审计/决策索引/7 条实现红线。
- ARCHITECTURE.md §6 重写为摘要+指针（原内容停在 ISSUE-066 旧通道已过时）；EXAM-REQUIREMENTS.md 头部加"部分过时"提示（选课 LLM/复习计划/SSECP/rubric 自由文本均已演进）；记忆索引 MEMORY.md 核心文档表加条目。
