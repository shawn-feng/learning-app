# 工作日志 2026-08-24

- 修复 agent-prompts.ts 保存报错：Node 22.22.2 的 node:sqlite DatabaseSync 无 .transaction() 方法，saveAgentPrompt 抛「db.transaction is not a function」。改为手写 BEGIN/COMMIT/ROLLBACK（与 kb-sqlite.ts 保持一致）。

## 修复珊珊会话 create_html_lesson 报错（ISSUE-020 首用即崩）
- 现象：珊珊要番茄钟，AI 调 create_html_lesson 报 `Cannot read properties of undefined (reading 'startsWith')`，AI 降级自己 write 成功。
- **根因（复现确认）**：`DefaultResourceLoader` 构造时对 `options.agentDir` 调 `resolvePath()`（resource-loader.js:156），**Windows 下 agentDir 传 undefined → normalizeWindowsShellPath(undefined) → undefined.startsWith 崩**（paths.js:50）。编程 agent 会话漏传 agentDir（学习 agent / daily-summary 都传了所以正常）；**getParentSession / getParentContentSession 同样漏传，是潜在同根因崩点**。
- 修复：三处统一补 `agentDir`（编程 agent = childDir/.pi/agent；家长会话 = dataDir/.pi/agent）。
- 验证：最小复现脚本确认「无 agentDir → 崩 / 有 agentDir → OK」，修复后两配置均 reload OK；tsc 0 业务错误；build 过；programming-agent 4 + kb-sqlite 23 用例全过。
- ⚠️ 已写入 MEMORY.md 关键 SDK 坑：**Windows 下 DefaultResourceLoader 必传 agentDir**。

## 珊珊 create_html_lesson 二次报错排查 + outputPath 规范（2026-08-24 上午）
- 新 session（01:20 UTC）报错已变：**「编程 agent 未配置模型」**——agentDir 崩溃已修复，当前是配置缺失的保护提示。查 `data/app-settings.json`：确实无 programmingModel 字段（defaultModel=qwen/deepseek-v4-flash 在）。
- **配置丢失根因（重要）**：8-23 写的 `test/programming-agent.test.ts` afterAll 无条件 `setProgrammingModelKey("")`，跑过测试就清空用户真实配置（01:01 session 曾走到 loader 崩溃 = 当时已配置，01:20 丢失）。**已改为 beforeAll 存原值 / afterAll 恢复原值**。
- **outputPath 规范（用户指出）**：番茄钟这类**工具/游戏/一次性产物应放 `outputs/`**（SPEC 规范），学习资料才放 `learning/{topic}/materials/`；AI 之前造 `learning/tools/番茄钟.html` 是错的（污染主题列表）。已改三处：create_html_lesson 工具 description（加 outputPath 选择规则段）、LEARNING_NAV「内容展示」段（outputs/ vs materials/ 规则）、buildProgrammingPrompt（路径语义）。regenerate-agents.mjs 重新生成 2 个 AGENTS.md。
- 验证：模型列表 11 个可返回（设置页下拉数据源 OK）；tsc 0 业务错误；build 过；programming-agent 4 用例过（含恢复逻辑）。

## 番茄钟耗时分析 + 环节计时日志（2026-08-24）
- 用户反馈：走 create_html_lesson 生成番茄钟耗时 ~4 分钟，AI 直接 write 1 分钟内。从 session 时间戳拆解：user→toolCall 仅 4.5s、toolResult→收尾 1.7s，**3 分 34 秒几乎全在 create_html_lesson 工具内部**。
- 已确认：编程模型 = qwen/deepseek-v4-flash-0731（flash 快档非瓶颈）；产物 19.2KB vs 前两次直接 write 的 8.6KB/12.5KB——**输出 token 多 60%~120% 是主因**（编程 prompt 要求自包含/完整功能，且独立会话无学习 agent 上下文）。
- 已加环节计时：`generateHtmlLesson` 输出 `[programming-agent] ... 会话获取/创建 Xs + LLM 生成 Ys + 落盘校验 Zs`，下次调用可精确归因。
- **追加思考/输出拆分**：编程 agent 会话 inMemory 不落盘 → 历史那次（01:30）无法事后拆 think vs output。已用 `session.subscribe` 加 `collectPhaseStats`（首个 thinking_delta → 首个 text_delta = 思考时长；首个 text_delta → 末次 agent_end = 输出时长；含工具调用次数），下次调用日志直接打印「首 token Xs + 思考 Ys + 正文输出 Zs（工具调用 N 次）」。tsc 0 业务错误、build 过。

## ISSUE-039 实施：精简模型 provider（2026-08-24）
- 需求：去掉国外 provider（anthropic/google/openrouter/groq），保留千问 token-plan，新增 MiniMax 国内 provider。
- 改动：`src/pages/Settings.tsx` PROVIDERS 移除 4 个国外项、新增 minimax；`electron/lib/pi-runtime.ts` 新增 MINIMAX_PROVIDER（国内端点 `https://api.minimaxi.com/v1`，7 个模型，按普通 openai-completions 接入、reasoning 关闭——SDK thinkingFormat 枚举无 minimax 故不猜）+ `getAvailableModels()` 加白名单 `["qwen","deepseek","openai","minimax"]` 过滤 SDK 内置国外 provider（确认 `ModelRuntime.create` 会 `builtinProviders()` 全注册，白名单为必须）。
- 决策（待用户确认）：openai 用户未点名删故保留（同在白名单）；MiniMax 用国内端点（国际账号改 `api.minimax.io/v1`）。
- 验证：tsc 仅余 5 条环境性 TS2318/TS2552（无关）；`npm run build` 通过。
- ISSUES.md ISSUE-039 已追加实施记录。

## 对话 403 AccessDenied.Unpurchased 排查（qwen-tokenplan/deepseek-v4-flash）
- 现象：对话报 `⚠️ 403 {"message":"Access to model denied...","code":"AccessDenied.Unpurchased"}`。
- **根因**：`data/app-settings.json` 里 defaultModel 与 programmingModel 均为 `qwen/deepseek-v4-flash`——**走的是按量付费端点**（dashscope.aliyuncs.com），该账号按量未开通 DeepSeek → Unpurchased。auth.json 里 token-plan 专属 key（`sk-sp-` 开头，len=115）其实一直在，但没被引用（配置选错了 provider 通道）。
- 修复：两处改为 `qwen-tokenplan/deepseek-v4-flash`（走 token-plan 端点 token-plan.cn-beijing.maas.aliyuncs.com，套餐抵扣）；原文件备份为 `data/app-settings.json.bak-20260824`。
- 顺手修正 pi-runtime.ts 过时注释：原 202 行「token-plan 与按量共用同一 API Key」与 318-320 行（独立 key 段）矛盾，已改为「独立 key 段，sk-sp- 开头，勿互拷」。
- ⚠️ 教训：qwen 与 qwen-tokenplan 两个 provider 都注册了同名 DeepSeek 模型（QWEN_DEEPSEEK_MODELS 共用），设置页模型名相同极易选错通道；若再报 Unpurchased = 套餐档位不含该模型（个人版部分档位仅团队版支持），需升级档位或换 deepseek-v4-pro。
- 待用户重启应用验证。

## 移除 token-plan 下已下线的 deepseek-v4-flash（无后缀）
- 用户确认：百炼上 deepseek-v4-flash（无后缀别名）已下线，只有 -0731 定点快照。改动：`QWEN_DEEPSEEK_MODELS` 删无后缀 flash（qwen 按量与 qwen-tokenplan 两个 provider 共用的数组，一并生效）；`DEFAULT_MODEL` 兜底改 `deepseek-v4-flash-0731`；`app-settings.json` 的 defaultModel/programmingModel 同步改 `qwen-tokenplan/deepseek-v4-flash-0731`；测试 `EXPECTED_DS` 减为 3 个。tsc 无业务错误；qwen-deepseek-models 测试 5/6 过（1 个既有环境性失败：getAvailableModels 在 PI_TEST_DATA_DIR 下 auth 空 → SDK 只列已配置 auth 的 provider → 返回 []，与改动无关）。

## ⚠️ git 仓库损坏事故 + 恢复（safe-delete 误删 .git 内部对象）
- **事故**：为验证「getAvailableModels 失败是否我引入」，在 WorkBuddy 沙箱内跑 `git stash push` + `git stash pop`。stash 内部会删 refs/stash、reset --hard 等工作区操作，被环境 safe-delete 机制误删 **.git/refs/ 目录 + 旧 pack-*.idx 对应 .pack 数据文件** → git 报 not a git repository / bad object / missing blob，fsck 大量 invalid reflog entry。
- **恢复步骤**（4 步，已验证有效）：① `git update-ref -d refs/heads/master` 删坏引用 → `git fetch origin`（远端对象完整，先拉回来）；② `git update-ref refs/heads/master <远端hash>` + `git read-tree HEAD` 重建 index（读不了 index 旧 blob 时用 read-tree 绕过）；③ 删孤立 `objects/pack/*.idx`（无配对 .pack）和失效 multi-pack-index；④ `git fsck --full` 确认 missing=0。
- **损失**：本地未推送的 commit 3c9865f（ISSUE-033 终版）对象丢失，但其全部内容都在工作区文件里（git status 18 项未提交改动 = ISSUE-033 + 本次修复），重新 commit 即可。HEAD 回落到远端 0d68e77。
- **⚠️ 教训（写入 MEMORY.md）**：沙箱环境禁止 `git stash`；git 损坏先查 pack 孤立 idx + fetch 远端恢复。
- 恢复后已提交并推送：`7cbff6d`（20 文件 +892/-248，含 ISSUE-033 全部内容 + 本次模型移除），`0d68e77..7cbff6d master -> master`（gitee）。工作区干净。

## create_html_lesson 工具砍掉「学习资料路径」知识（2026-08-24 下午）
- 用户指出：这个工具不需要知道学习资料走什么路径。
- 设计边界厘清：**路径规则的唯一真源是 LEARNING_NAV（学习 agent 的 AGENTS.md）**——工具/游戏→`outputs/`、学习资料→`learning/{topic}/materials/` 都由学习 agent 决定并传给工具；`create_html_lesson` 工具与编程 agent 只负责把 HTML 写到调用方指定的 outputPath，不关心分类归档。
- 改动：① custom-tools.ts 删 create_html_lesson 描述里的「outputPath 选择规则」段（去掉 learning 分支）+ outputPath 参数说明只留 outputs/ 约定；② programming-agent.ts 工作方式段去掉 `learning/{topic}/materials/` 描述，改为「写到调用方指定路径」。
- 保留：LEARNING_NAV（pi-session.ts:54）仍是唯一路径规则真源；display_content/parent_content 本就是学习资料工具，其 materials 路径描述正确保留；custom-tools.ts 的 learning/{topic}/materials/→父库镜像逻辑（按实际 relPath 判定，不依赖描述）保持不变、不受影响。
- 无需重新生成 AGENTS.md（LEARNING_NAV 未改）。

## 全工具去旧路径 + 去镜像/软链接（2026-08-24 傍晚，用户指令）
- 用户指令：「所有工具都不要出现过去的路径，也不要用镜像，软链接的方式。」
- 关键 SDK 发现：`@earendil-works/pi-coding-agent` 的 write 工具 `resolveToCwd`→`resolvePath` 对**绝对路径直接采用**（`isAbsolute?resolve(abs):resolve(cwd,rel)`），且 write 执行体**无 cwd 外路径守卫**。→ 可直接把绝对路径交给编程 agent，它写到父库，无需改 cwd、无需镜像。
- 改动：
  1. `programming-agent.ts` generateHtmlLesson：按 outputPath 前缀锚定根目录——`materials/`→`getParentMaterialsDir()`（父库共享目录），否则→`childDir`（outputs/）；以**绝对路径**交给 agent 写出；删除原「只写 cwd 内」边界，放宽为可写父库 materials/ 与孩子 outputs/。
  2. `custom-tools.ts` display_content：描述去掉「孩子视角旧路径 learning/...」；execute 去掉子目录兜底，只解析 `materials/{topic}/{file}.html`（父库）与 `outputs/{name}.html`（孩子本地）。
  3. `custom-tools.ts` create_html_lesson.execute：删除把 `learning/{topic}/materials/` 镜像到父库的代码块（不再需要）。
  4. `pi-session.ts` LEARNING_NAV：学习资料路径 `learning/{topic}/materials/` → `materials/{topic}/{课程名}.html`（父库共享目录，单一真源）。regenerate-agents.mjs 重新生成 2 个孩子 AGENTS.md。
  5. `media-protocol.ts`：删除 `media://local/{childId}/learning/...` 旧格式兜底分支与注释，仅保留 `parent/{parentId}/...` 新格式。
- 验证：tsc 仅余 5 条环境性 TS2318/TS2552；`npm run build` 通过。
- ⚠️ **数据影响（待用户决策）**：旧 `data/children/<id>/learning/<topic>/materials/` 下已有资料现**不可达**（display_content 不再读孩子目录、media-protocol 不再兜底旧 media）；备份在 `data-backup-20260821-issue029/`。若需保留，应一次性迁移到 `data/parents/default/materials/{topic}/`（可写脚本，待用户确认再跑，避免误删/越权）。

## ISSUE-039 二次修订：token-plan / 按量付费按 URL 拆分（2026-08-24 下午）
- **更正上午错误**：原称"计费代码控制不了只能看账单"是错的——token-plan 与按量付费是两个不同 base URL，打到哪个 URL 即决定走哪种计费。
  - 按量：`dashscope.aliyuncs.com/compatible-mode/v1`（LLM）/ `dashscope.aliyuncs.com/api/v1/...`（千问 ASR）
  - token-plan：`token-plan.cn-beijing.maas.aliyuncs.com/compatible-mode/v1`（LLM）/ 同域名 `api/v1/...`（ASR）
- 方向 A 落地：
  1. `pi-runtime.ts`：`QWEN_PROVIDER` 仅留 qwen 官方+VL（按量 URL）；新增 `QWEN_TOKENPLAN_PROVIDER`（token-plan URL）挂 `QWEN_DEEPSEEK_MODELS`；白名单加 `qwen-tokenplan`；默认兜底从 `deepseek/deepseek-v4-flash` 改 `qwen-tokenplan/deepseek-v4-flash`（DeepSeek 已不属 SDK 内置 deepseek provider）；`getSharedRuntime` 在 create 前把 `auth.qwen.key` 同步到 `auth["qwen-tokenplan"]`（共用同一百炼 key，用户只填一次）。
  2. 语音 `voice/`：`providers/qwen.ts` 的 `transcribe` 支持 `creds.endpoint`（缺省回退按量 ASR）；`voice-config.ts` 增 `qwen-tokenplan` 类型+默认配置（预填 token-plan ASR 端点）+ isProviderConfigured 覆盖；`index.ts` dispatch 复用 qwenTranscribe。
  3. `Settings.tsx` PROVIDERS 删 `deepseek` 项（DeepSeek 归入 qwen-tokenplan、复用 qwen key），现仅 qwen/openai/minimax。
- 验证：tsc 过滤环境告警无业务错误；`npm run build` 通过。
- 未做：MiniMax 仍走 `api.minimaxi.com/v1` 直连（用户"先不管 minimax"）。

## ISSUE-039 三次修订：按量/token-plan 是两套独立 key（2026-08-24 下午晚）
- **更正二次修订错误**：上轮称"token-plan 与按量共用同一 API Key、自动同步一份"——**错**。用户明确：按量付费与 token-plan 的 **API Key 也不相同**，应当两个完全独立的 provider，各自独立 key 段 + 独立 base URL，绝不互相拷贝。
- 改动：
  1. `pi-runtime.ts` `getSharedRuntime`：删掉"把 `auth.qwen.key` 同步到 `auth["qwen-tokenplan"]`"的块。`qwen-tokenplan` 只读自己的 key 段。
  2. `voice-config.ts` `isProviderConfigured`：`qwen-tokenplan` 不再回退 `auth.qwen.key`，改读自身 `auth["qwen-tokenplan"]`。
  3. `Settings.tsx` PROVIDERS：补回 `qwen-tokenplan` 独立 key 入口（四项：qwen 按量 / qwen-tokenplan 套餐 / openai / minimax），各自独立填写、分别写 auth.qwen 与 auth["qwen-tokenplan"]。

## ISSUE-039 四次修订：DeepSeek 直连不要去掉（2026-08-24 14:16）
- **用户纠正**：provider 里不要去掉 deepseek。澄清：此前的"去掉 deepseek"只发生在 `Settings.tsx` 的 key 填写入口（PROVIDERS 数组），而聊天侧 SDK 内置 `deepseek` provider 与白名单 `ALLOWED_MODEL_PROVIDERS` 里的 `"deepseek"` 一直都在、从未删。
- **当前 DeepSeek 双通道（并存、互不冲突）**：
  1. `qwen-tokenplan/deepseek-v4-*`：百炼 token-plan 套餐内的 DeepSeek，走 token-plan key + token-plan URL；
  2. SDK 内置 `deepseek/*`：DeepSeek 官方直连，走用户自己的 deepseek key（auth.json 的 deepseek 段），白名单已保留。
- 改动：`Settings.tsx` PROVIDERS 补回 `{ id:"deepseek", name:"DeepSeek (官方直连)" }` 独立 key 入口（现五项：qwen 按量 / qwen-tokenplan 套餐 / deepseek 直连 / openai / minimax）；`pi-runtime.ts` 把 `getDefaultModel` 兜底注释从"回退到 deepseek flash / SDK 默认"改为明确指向 `qwen-tokenplan/deepseek-v4-flash`，消除误读。
- 验证：tsc 过滤环境告警无业务错误；`npm run build` 通过。
- ⚠️ 需用户手动：当前 `data/shared/auth.json` 的 `qwen-tokenplan.key` 是上轮错误同步的 qwen 副本，请在设置页「通义千问 (token-plan 套餐)」填真正的 token-plan key 覆盖（AI 不动 auth.json 既有密钥值）。

## ISSUE-039 五次修订：两千问 provider 都挂 DeepSeek + 按 provider 筛选（2026-08-24 14:22）
- **用户两点要求**：① 点击 provider 只显示该 provider 的模型；② 千问按量 / 套餐两个 provider 都要显示 DeepSeek 等全部模型。
- **决策（询问后用户拍板）**：DeepSeek 在 `qwen` 按量与 `qwen-tokenplan` 套餐**两个 provider 各注册一份**（`QWEN_DEEPSEEK_MODELS` 同时并入两者 models，分走各自 baseUrl）；按 provider 筛选应用到 Settings 与 ModelSelector 两处。
- 改动：
  1. `pi-runtime.ts`：`QWEN_PROVIDER.models` 加回 `...QWEN_DEEPSEEK_MODELS`（按量也挂 DeepSeek）；`qwen-tokenplan` 本就含。
  2. `ModelSelector.tsx`：新增 `activeProvider` + provider chip 栏（>1 provider 时显示），点 provider 仅显该 provider 模型并切首个；默认激活=默认模型所属 provider；引入 useMemo。
  3. `Settings.tsx`：`selectedProvider` 初值改 `"qwen"`；「当前可用模型」列表与「编程 agent 模型」下拉均按 `selectedProvider` 过滤（复用既有 chip），标题显示当前 provider 名。
- 验证：tsc 过滤环境告警无业务错误；`npm run build`（303 modules）通过。

## ISSUE-039 六次修订：语音配置同样按量/token-plan 拆两个 provider（2026-08-24 14:33）
- **用户要求**：语音配置（VoiceSettings）也像聊天那样把按量付费与 token-plan 拆成两个独立 provider 可选。
- **后端此前已具备**（前轮）：`qwen.ts` 支持 endpoint、`voice-config.ts` 有 qwen-tokenplan 类型+预填套餐端点+独立 key 段、`voice/index.ts` dispatch 已加 case。本次补齐前端 UI 与 key 路由。
- 改动：
  1. `VoiceSettings.tsx`：`VOICE_PROVIDERS` 加 `qwen-tokenplan` 独立项（fields=apiKey+endpoint readonly）；原 qwen 改名「千问 (按量付费)」；fields 类型加 readonly 标记，渲染 disabled 灰底。
  2. `voice-config.ts`：`VOICE_PROVIDER_ORDER` 加 `qwen-tokenplan`。
  3. `qwen.ts`：`loadQwenKeyFromAuth(isTokenPlan)` 按 endpoint 是否含 token-plan 读 `auth.qwen` / `auth["qwen-tokenplan"]` 各自 key。
- 防丢点：token-plan endpoint 由 UI readonly 字段随 fields 保存，经 `{...DEFAULT_CONFIG.providers, ...parsed}` 合并不丢失。
- 验证：tsc 过滤环境告警无业务错误；`npm run build`（303 modules）通过。

## 修复「编程 agent 模型配置退出 app 再启动丢失」（ISSUE-020 后续，14:4x）
- **现象**：设置页配好编程 agent 模型，退出 app 再启动，配置消失。排查发现磁盘 `data/app-settings.json` 的 `programmingModel` **持久化正确、从未丢**（`app-settings.ts` saveSettings 落盘 + ipc-handlers 正确调用）。真正丢失通道是 **`test/programming-agent.test.ts`**：`beforeAll` 无条件 `setProgrammingModelKey("")` 清空 + `afterAll` 恢复——测试套件被中断（Ctrl+C / worker 异常）时 afterAll 不执行，磁盘就停在空值；且测试与 app 共用同一 `./data/app-settings.json`（vitest 默认 cwd=项目根，getDataDir 走 process.cwd()/data）。
- **根因本质**：8-24 早上已把"无条件清空"改为"保存原值/恢复"，但中断仍会丢；缺数据隔离。
- **修复**：
  1. `config.ts` 的 `getDataDir()` 增加 `process.env["PI_TEST_DATA_DIR"]` 覆盖（仅测试设置时生效，生产零影响）；
  2. 新建 `vitest.config.ts`，`test.env.PI_TEST_DATA_DIR = os.tmpdir()/pi-test-data`，测试读写落到临时目录，**彻底不再碰真实 data/**；
  3. `test/programming-agent.test.ts` 去掉"保存原值/恢复"，beforeAll 仅清隔离目录配置 + afterAll 清理临时目录；断言文案同步（`输出路径超出学习目录范围`→`输出路径超出允许范围`）。
- **附带修复（同轮暴露的真实 bug）**：`media-protocol.ts` resolveMediaTarget 第 42 行 `if (first !== "parent")` 引用**未定义 `first`**——上一轮删"旧格式兜底"分支时把 `const first = segs[0]` 一并删了、引用没改，导致 **media:// 任何解析都抛 ReferenceError（真实 app 也会崩）**。改为 `segs[0]`；`test/media-protocol.test.ts` 旧格式用例改为断言返回 null（资料已统一走父库）。
- 验证：programming-agent 4 + media-protocol 6 用例全过；跑测试前后真实 `programmingModel` 保持 `qwen/deepseek-v4-flash-0731`（零污染）；tsc 仅余 5 条环境性 TS2318/TS2552；`npm run build` 通过。
- 其余 22 个失败用例经 baseline 对比确认**均为既有失败**（依赖真实服务器/数据或 ISSUE-039 重构遗留），非本次引入，不在范围。

## 编程 agent 模型下拉解耦「默认模型」配置（14:5x）
- **用户要求**：编程 agent 模型下拉**不跟随上方 provider 切换**、**不随「默认模型」配置即时保存**，要有**独立保存按钮**。
- **改动 `src/pages/Settings.tsx`**：
  1. 新增 `programmingModelDraft`（下拉暂存，未保存）与 `programmingModel`（已保存值，仅用于显示「当前已保存」）分离；初始化时两者都从 `piGetProgrammingModel()` 回填。
  2. 下拉 `value` 改用 `programmingModelDraft`，选项**不再按 `selectedProvider` 过滤**（展示全部 provider 模型），`onChange` 只更新草稿、不触发保存。
  3. `handleSetProgramming` 改名 `handleSaveProgramming`，改为「保存按钮」触发；保存成功后才更新 `programmingModel`（已保存值）。
  4. 新增独立「保存编程 agent 模型」按钮：`disabled` 当草稿 === 已保存值（无改动）或模型列表为空；保存后 status 提示。
- 验证：tsc 0 业务错误；`npm run build`（303 modules）通过。


## ISSUE-033 修订：AGENTS 文件归家长目录，孩子只读不可写（15:2x）
- **用户诉求**：「孩子的数据目录里不能有 AGENTS 文件，孩子不可以修改这个文件」→ 澄清「文件要保留，但不能在孩子的目录里，可以保留到家长的目录里」。
- **拍板**：保留物理文件于 `data/parents/default/agents/<childId>.md`（每孩子一份，内容=用户版本或代码默认）；`data/agents.sqlite`（033 已建）仍为版本权威（当前版+历史版）。
- **核心改动**：
  1. `parent-library.ts` 新增 `getParentAgentsDir` + `getChildAgentsPath(childId, parentId=DEFAULT_PARENT_ID)`；
  2. `pi-session.ts`：`writeAgentsMd` 改写家长目录（不再碰孩子目录）；新增 `resolveChildAgents`（SQLite 用户版本 → 家长目录文件 → 代码默认+custom 段）；`buildChildPrompt(childId,…)` 把 AGENTS 内容**内联注入 system prompt**（孩子目录无 AGENTS.md → SDK 不再附加 `<project_context>`，语义等价、孩子不可写）；`getDefaultPrompt` child 分支改读家长目录；
  3. `ipc-handlers.ts`：`child:getAgentsMd`/`child:saveAgentsMd`/`agents:save`(child) 全改读写家长目录文件；
  4. 测试：app/functional/sync 断言改为「孩子目录不含 AGENTS.md + 家长目录文件存在」，sync ≥3 文件调 ≥2；新增 `test/agents-parent-dir.test.ts` 锁定 3 不变量。
- **存量迁移**：两个孩子的 `data/children/<id>/AGENTS.md` 已删；`data/parents/default/agents/` 生成两份（闻闻=代码默认 2433 字符，珊珊=SQLite 用户版本 886 字符）。
- **验证**：tsc（滤环境告警）0 错；build 通过；agents-parent-dir(3)+agents-md(3)+app 初始化用例+functional 数据架构用例+sync(8) 全过；剩余失败均为既有（云端 ECONNREFUSED / isPackaged / shared/skills 残留 / ISSUE-032 过时断言）。
- 未提交 git（等用户指令）。

## ISSUE-033 终版：AGENTS 纯 SQLite，删物理文件（15:5x）
- **用户诉求**：「把孩子的 AGENTS 文件放到数据库 sqlite 里。修改后的也存在数据库里。查看和编辑就在家长页面里。」
- **终版拍板**：AGENTS 全部存 `data/agents.sqlite`（prompts 当前版 + prompt_history 历史版），**不落任何物理文件**；查看/编辑唯一入口 = 家长页面 AgentPromptEditor（agents:* IPC）。
- **改动**：
  1. `pi-session.ts`：删 `writeAgentsMd`/`extractCustomSection`/custom 段占位；`resolveChildAgents` = SQLite 用户版本 → 代码默认；`getDefaultPrompt` child 分支纯 buildAgentsMd(getProfile(ref))；`createChildSession` 删开会话前刷新调用。
  2. `parent-library.ts` 删 `getParentAgentsDir`/`getChildAgentsPath`；`ipc-handlers.ts` 三个 handler 纯 SQLite；`child-auth.ts`/`user-init.ts` 删 writeAgentsMd 调用。
  3. 存量：`data/parents/default/agents/` 两文件删除（SQLite 已有珊珊用户版本 886 字符）；删过时脚本 `scripts/regenerate-agents.mjs`。
  4. 测试：`agents-parent-dir.test.ts` → `agents-sqlite.test.ts`（4 用例）；app/functional 断言改「家长目录无 agents 目录」；sync 不变。
- **验证**：tsc 0 错（滤环境告警）；build 通过（产物无 getChildAgentsPath/writeAgentsMd 残留）；agents-sqlite(4)/agents-md(3)/app 初始化/functional 必要文件/sync(8) 全过；完整套件 19 失败均既有，零新增回归。
- 未提交 git（等用户指令）。

## ISSUE 状态批量更新（2026-08-24 16:00，用户口述确认）
- 用户确认以下 ISSUE 完成，已在 `ISSUES.md` 将状态行改为「已完成（2026-08-24 …）」：
  - ISSUE-028 长消息自动滚动：手测确认完成
  - ISSUE-029 主题资料+method 入 SQLite：用户确认告一段落（管理端深化列为后续项）
  - ISSUE-032 创建孩子目录结构按 SQLite 化调整：用户确认
  - ISSUE-036 recording 改造为每日学习记录总结：commit 已提交
  - ISSUE-039 精简模型 provider：用户确认完成（auth.json 密钥手动项已交代）
- 更新后清单：38 条，已完成 33 / 待定 4（013、034、037、038）/ 废弃 1（035）。
- 仍处「已实施待最后验证」灰区的：ISSUE-033（构建测试通过、待手测）。
- 16:18 用户确认 ISSUE-031（分配主题设每天学习量）已完成，ISSUES.md 状态改「已完成（2026-08-24）」，灰区仅剩 033。

## KB-SQLITE-SCHEMA.md 补 agents.sqlite 附录（16:4x）
- 用户问「AGENTS 入库是否改了 sqlite 表结构」→ 核查结论：**没改**——`data/agents.sqlite` 的 prompts/prompt_history 表是 033 首版（08-22）建的，终版只改读写方式（纯 SQLite、删物理文件），DDL 零变化。
- 但 KB-SQLITE-SCHEMA.md 此前只覆盖 kb.sqlite，漏了 agents.sqlite → 已补「附录 A：data/agents.sqlite — AGENTS/提示词用户版本库」（表结构、scope/ref 约定、整体替换/历史回退语义、IPC 面对应、与 kb.sqlite 边界），头部适用范围与实现位置同步更新。
- **git 提交（18:2x）**：commit `3c9865f`「feat(ISSUE-033): AGENTS 纯 SQLite 存储（终版），删物理文件 + 补 KB-SCHEMA 附录」（13 files, +305/-181；含 KB-SCHEMA 附录 A 与 memory 记录）。工作区已干净。

## ISSUE-037 修复：家长「课程管理」AI 聊天静默无反应（18:2x-18:3x）
- **根因（三处静默叠加，对照孩子路径确认）**：① SDK `session.prompt()` 出错**不抛异常**，错误记在最后一条 assistant 消息（stopReason=error）——`pi:prompt_parent_content`/`pi:prompt_parent` 两个主进程 handler prompt 后**不做 assistantError 检查**、不发 `pi:reply`/`pi:reply_error`/`pi:reply_end`；② 三个家长前端组件（TopicDetail/TopicEditor/SkillEditor）只 try/catch「invoke 抛异常」、**从不检查返回值 r.success**（主进程把错误包在返回值里）→ 后端任何失败前端不可见 + busy 永不复位（一直转圈）；③ 前端不监听 `pi:reply`/`pi:reply_error`/`pi:error`。对照：孩子路径（`pi:prompt` + Learn.tsx）有完整错误范式，故正常。
- **修复**：① ipc-handlers.ts 两个家长 handler 对齐孩子范式（assistantError 检查 → 回发 reply_error/reply/reply_end，childId="parent"/"parent-content"，失败 logRound ok:false）；② 三个组件增补 onPiReply（替换流式气泡）/onPiReplyEnd/onPiReplyError（⚠️ 气泡）/onPiError 监听 + handleSend 检查 r.success + piStartParentContent/piStartParent 改为 .then/.catch 检查 success。
- **验证**：tsc 无业务错误（滤 TS2318/2552）；`rm -rf out && npm run build` 三环境过；token-stats/parent-library/daily-summary/agents-sqlite 41 用例全过。
- ISSUES.md 已更新为「✅ 根因 + 🔧 已修复」；未提交 git。

## 记录 ISSUE-040：App 版本发布与升级机制（18:37，讨论中）
- 需求：明确新版本发布渠道 + 客户端升级闭环。核实现状：electron-builder 已配（nsis/deb/AppImage/dmg）但**无 publish 配置、无 electron-updater**；`electron/main.ts:17` APP_VERSION 硬编码 + `checkForUpdates()` 拉 `/api/version` 弹窗开下载页（雏形）；`cloud-service/app/main.py:51` `/api/version` 全硬编码 0.1.0、download_url=None。
- 关键事实：打包后数据在 `%APPDATA%/learning-app/app-data`（userData 目录名 = package.json name，非 productName；2026-08-24 用户实测确认），在安装目录之外 → 覆盖安装/升级不丢数据，无需迁移。
- 方案要点（已写入 ISSUES.md ISSUE-040）：发布源推荐阿里云 OSS（仓库根有 aliyun-aksk.txt）+ 自有服务器静态托管；升级推荐 electron-updater（latest.yml + blockmap 差量）；扩展 force/channel/min_version 策略；`APP_VERSION` 改 `app.getVersion()` 消除双源漂移。
- **用户拍板（18:3x）**：① 发布源 = 阿里云 OSS（generic）；② 升级 = B 档 electron-updater 自动更新（差量+静默安装，失败降级下载页；C 档强制升级暂不做，min_version 保留备用）；③ 暂不签名先发布（OV 证书列为后续项）；④ 统一 semver + app.getVersion()，云端 /api/version 改读配置/数据库。ISSUE-040 已标「已拍板（待实施）」，四类待办明确（main.ts 去硬编码 / 云端版本接口化 / 加 electron-updater+publish 配置 / IPC 更新事件）。

## ISSUE-037 续修：家长对话界面对齐孩子聊天（19:5x-20:0x）
- 用户要求「课程管理」页家长对话：① 气泡展示思考过程+工具调用（与孩子界面一致）；② 聊天区独立滚动、整页不被拉长。
- **思考/工具展示**：TopicDetail/TopicEditor/SkillEditor 对齐 Learn.tsx working 气泡范式——handleSend 创建 workingMsg（working/thinking/tools）+ workingIdRef；新增 onPiThinking/onPiToolStart/onPiToolEnd 监听经 patchWorking 更新同一气泡；onPiReply 替换为最终文本、onPiReplyError 替换 ⚠️。主进程 attachSessionEvents 本就发 thinking/tool 事件，前端只补监听。
- **滚动修复**：根因=`.dashboard-main`（overflow-y:auto block）+ TopicDetail 的 calc(100vh-120px) 估算高度。改：dashboard-main 加 flex column；.chat-window 补 min-height:0；TopicDetail/TopicEditor/SkillEditor 外层改 flex:1+minHeight:0+overflow:hidden；Settings 根 div 加 flex column。**教训：flex 滚动布局禁 calc(100vh-xxx) 估算，应父 flex + 子 min-height:0 + overflow 归位。**
- 验证：tsc 无业务错误；build 三环境过；parent-library/token-stats/daily-summary 37 用例全过。ISSUES.md 已补记录；未提交 git。

## 家长提示词统一（20:0x-20:2x，用户需求：不分场景、一个提示词）
- **需求**：家长 AI 提示词不再分「通用家长助手 / 教学内容生成」两个场景，统一为一个；要点：① 家长角色职责（管理孩子/课程/配置 + 查看统计）；② app 数据结构与数据流转；③ 征求补充建议。
- **已实施**：
  1. `pi-session.ts`：`buildParentPrompt()` 重写为统一版（身份=家长工作台助手全职责；数据目录 data/ 全景；两库职责与流转闭环——家长库 parent.sqlite=教学内容真源、孩子库 kb.sqlite=孩子学习数据真源、分配快照不拷 method/teaching_copy、边界=绝不跨库改数据；孩子管理/课程管理/配置查看/统计四类职责；工作方式=引导式+破坏性操作先确认+auth.json 不碰+日期用 get_date）；**删除 `buildParentContentPrompt()`**；`getParentSession` 与 `getParentContentSession` 共用统一 prompt + **家长工作台全量工具**（read/write/edit/get_date/parent_course_save/parent_course_delete/parent_stats）。
  2. **新增 `parent_stats` 只读统计工具**（custom-tools.ts）：背景=parent.sqlite/kb.sqlite 是二进制，read 读不了，家长 agent「查看统计」必须走工具。三档：tokens（汇总/按模型/最近记录，可按 childId 过滤）、progress（孩子主题 learned/total/next+每课状态，必填 childId）、daily（每日记录，date 缺省=最近 7 天）。测试 `test/parent-stats.test.ts` 6 用例。
  3. `AgentPromptEditor.tsx`：PARENT_REFS 只留 main（统一入口）；SQLite 兼容：`buildParentPrompt` 读 main 优先、content 兜底（历史 content 用户版本不丢）；`getDefaultPrompt` 家长统一返回 buildParentPrompt；agent-prompts.ts/KB-SCHEMA.md 注释更新。
- **验证**：tsc 无业务错误；parent-stats/agents-sqlite/agents-md/parent-library 25 用例全过；build 通过。
- **⚠️ 构建受阻插曲（与 prompt 无关）**：`npm run build` 首次失败——工作区遗留 **ISSUE-040 半成品**：`electron/lib/updater.ts`（未跟踪新文件，静态 import electron-updater）+ `main.ts` 已引用 + `electron-updater` 从未安装 → rollup 解析失败。npm install 又被沙箱 EBUSY 卡（ffmpeg-static rename 锁定）。处置：node_modules 已有 electron-updater@6.8.9（某次 install 部分成功）+ **手动把 electron-updater 加进 package.json dependencies**（^6.8.9），构建恢复。提醒：ISSUE-040 仍是未完成功能（updater.ts/main.ts 未提交），后续做该 issue 时需确认 app-update.yml 打包配置。

## 家长 agent 补充 ls 工具（20:4x，用户需求）
- 用户指出：家长会直接把文件放进 materials 目录，agent 需要能 list 目录结构。
- 实施：`pi-session.ts` 两个家长会话 tools 白名单加 `ls`（SDK 内置工具，参数 {path,limit}，返回目录条目、目录带 / 后缀、含 dotfiles；不需要 customTools，前端 TOOL_META 已有 ls 映射）。统一提示词「课程与教学内容管理」段补充：先用 ls 看家长放了哪些文件再处理。
- 验证：tsc 无业务错误 + build 通过。未提交 git。

## 家长会话三件套：落盘 + 操作记录 + 新建策略（20:5x-21:0x）
- **需求**：① 家长会话落盘（与孩子一致，保存/续接历史）；② 对 app 的改动记录到 markdown 文件（家长目录里加一个记录文件）；③ 家长 session 也配置新建 session 策略（autoNewSession）。
- **已实施**：
  1. **落盘**：`getParentSession`/`getParentContentSession` 从 `SessionManager.inMemory()` 改为 `continueRecent(dataDir, sessionsDir)`；**两个会话独立子目录** `data/.pi/agent/sessions/parent/` 与 `.../parent-content/`（避免 continueRecent 互相选中对方历史）。
  2. **操作记录**：新增 `parents/default/activity-log.md`（`parent-library.ts` 的 `getActivityLogPath`/`appendActivityLog`，首次建表头、追加写）；新增 `log_activity` 工具（agent 用 write/edit 改资料后主动记录）；`parent_course_save`/`parent_course_delete` execute 里**自动记录**（含改动字段摘要）；统一提示词补「操作记录」规则 + 数据目录加 activity-log.md。
  3. **新建策略**：scheduler-config.json 加 `parent.autoNewSession`（scheduler.ts `getParentSchedulerConfig`/`setParentSchedulerConfig`，默认关闭 21:00）；pi-session 泛化 `lastMessageTimestampInDir`/`shouldAutoNewSessionInDir`，新增 `shouldAutoNewSessionForParent(sessionsDir)`；`scheduler:config:get` 返回 parent 段 + 新 IPC `scheduler:parent_config:set`；SchedulerSettings 顶部加「家长会话（自动新建会话）」配置区（开关+时间+保存）。
- **验证**：tsc 无业务错误；parent-stats(9)/parent-library(12) 21 用例全过；build 通过（重试一次，out 残留 EPERM）。未提交 git。

## 家长 agent 增加 move_file / copy_file（21:1x，用户需求）
- 家长 agent 需要移动/复制文件（整理资料：散放 html 移进 materials/{topic}/、重命名、音频进 media/、复制到另一主题/备份）。
- SDK 内置工具无 mv/cp（只有 read/write/edit/ls/bash/find/grep）→ 自定义 `move_file`（fs.renameSync）/`copy_file`（fs.cpSync 递归）两个工具，加入两个家长会话 tools 白名单 + customTools；**路径守卫 `guardCwd`（learningGuard 只拦 read/write/edit，自定义文件工具必须自守，越出 data/ 报错）**；目标已存在报错（不覆盖）；源不存在报错；**自动记录 activity-log**。提示词「课程管理」段补「整理资料」说明。
- 测试：parent-stats.test.ts 新增 5 用例（移动/复制/越界拒绝×2/源不存在），共 14 用例全过；tsc 无业务错误；build 通过。未提交 git。

## 调查：珊珊会话「展示千字文第2段资料」display_content 报错（21:3x）
- **现象**：珊珊（childId=1f050a7f-...）要求展示千字文第2段学习资料，AI 调 parent_content 拿到 htmlPath 后调 display_content，报「资料文件不存在: materials/qianziwen/千字文-02-云腾致雨-鳞潜羽翔/index.html」。
- **根因**：家长库资料是「每课一子目录 + index.html」三层结构（`materials/{topic}/{course}/index.html`，历史迁移产物），而 display_content 的路径正则 `^materials/([^/]+)/([^/]+\.(?:html|htm))$` 只支持两层平铺（`materials/{topic}/{file}.html`）→ 三层路径不命中正则 → 走 else 分支解析为孩子 cwd 本地路径 → 孩子目录无该文件 → 报「资料文件不存在」。
- **修复**（custom-tools.ts displayContentTool）：正则放宽为 `^materials/([^/]+)/(.+\.(?:html|htm))$`（第三段允许含斜杠），并加**越界守卫**（resolved 必须在 getParentMaterialsDir 内，防第三段含 ../ 逃逸）；两层平铺与 outputs/ 分支不变。
- **测试**：parent-stats.test.ts 新增 4 用例（三层展示/两层回归/越界拒绝/文件不存在），共 18 用例全过；tsc 无业务错误；build 通过。未提交 git。

## ISSUE-040 App 版本发布与升级机制（已实施，20:15-22:0x）
- **落地内容**：见 ISSUES.md ISSUE-040「实施结果」段。要点：① 版本号统一（main.ts 删 APP_VERSION→app.getVersion()；云端 /api/version 读 app_versions 表 + POST 登记接口 ADMIN_TOKEN 鉴权）；② electron-updater@6.8.9（--legacy-peer-deps 因 edge-tts peer TS^5 与 TS7 冲突）；③ 新模块 electron/lib/updater.ts（启动静默 dialog / 设置页手动，setFeedURL 覆盖内嵌 app-update.yml，失败降级 /api/version 下载页）；④ IPC app:get_version/check_update/download_update/quit_and_install + onUpdateStatus/onUpdateProgress；⑤ GeneralSettings.tsx「软件更新」区块；⑥ scripts/publish-update.py（OSS 中转 + --register 登记）。
- **⚠️ 关键决策变更**：阿里云 2024 新规禁止新建 bucket 公共读（ACL/Policy 均拒）→ 升级包托管从 OSS 降级为自有服务器 `/download/`（FastAPI StaticFiles + Nginx 正则加 |download）；OSS 仅作本地→服务器中转（私有桶+签名 URL+云助手 curl）。**已发布 0.1.1 到生产**（/download/latest.yml 200 + 登记 0.1.1）。
- **环境坑（本次新踩）**：① WorkBuddy 沙箱 NODE_OPTIONS 注入 genie-safe-delete shim → electron-builder 清 tmp 报 safe-delete 失败，须 `NODE_OPTIONS=` 前缀 + 沙箱外跑；② Windows Defender 锁定 dist/win-unpacked/resources/app.asar（EBUSY 删不掉）→ 打包换新目录 dist3；③ electron-builder 26.x schema：linux.arch 非法（需内嵌 target，additionalProperties:false）；④ oss2 2.19 无 bucket_exists/Permission；⑤ aliyun CLI SendFile 用 --InstanceId.n 且大小 ≤32KB base64；RunCommand 命令里 sed 分隔符别用 |（与模式冲突）。
- **遗留**：GUI 升级动作未实测（沙箱无桌面）；NSIS 静默安装 exit 199（环境拦截）；voice-merge 2 用例 ffmpeg 沙箱环境失败（非业务回归）。
- **0.1.2 验证版已发布（22:14）**：用户要实测完整升级闭环，已打 0.1.2（纯升版本号）→ dist4 → OSS 中转 → 服务器 /download/ 覆盖 → 云端登记 0.1.2。现在服务器 latest.yml=0.1.2、/api/version=0.1.2（download_url→/download/ 0.1.2 安装包）。用户测试路径：装 dist3 的 0.1.1 → 启动弹「发现新版本 0.1.2」→ 立即更新 → 差量下载 → 重启安装 → 变 0.1.2。
