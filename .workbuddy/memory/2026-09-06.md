# 2026-09-06 工作日志

## 07:30 修复 ISSUE-052（本地语音 ASR：千问 token-plan 报「未返回识别文本」，真实端点实测通过）
- **根因**（继承 09-05 ISSUE-052 记录）：token-plan MaaS 端点响应是**单层 output**（`output.sentence.text`，sentence 单对象 / `output.text` / 顶层 text），而 qwen.ts 原写死按量 DashScope 的**双层**路径 `output.output.sentence.text` → token-plan 响应取到 undefined → 误报「未返回识别文本」。请求实际成功，只是字段读错。
- **改动全在 `electron/lib/voice/providers/qwen.ts`**（本地验证通过，未提交）：
  - `pickText` 多路径提取识别文本：按量双层数组 → token-plan 单层对象 → output.text → 顶层 text → result.text。
  - `pickError` 多路径取 code/message（顶层/output/error/sentence 内）；错误信息透出 `[code] message` 真因，不再笼统「未返回识别文本」。
  - `isNoSpeech` 静音语义短路统一判定（兼容 `NO_WORDS`/`no words`/`no speech`/中文），`!res.ok` 与 ok-空文本两分支共用，不发起 fallback。
- **验证**：
  1. tsc 0 业务错（仅 5 条环境噪音）。
  2. electron-vite build 过（main+preload+renderer）。
  3. 新增 `test/qwen-asr.test.ts`（mock fetch 喂三种响应结构）**6/6 PASS**。
  4. **真实 token-plan 端点 + 真实 auth key + 真实 16k 语音 wav 端到端实测：20s 返回中文识别文本**（修复前此请求必报「未返回识别文本」）——铁证。
- 遗留：设置页 `onlyProvider` 复测可选（已有流程）；未部署，须先经用户确认。
- ⚠️ 并行会话 ISSUE-049/050 有未提交改动，勿把本改动与其混提交。

## 07:47 新增 ISSUE-053（定时任务·课程时间段过一天丢失）
- **现象**：设置→定时任务→课程时间段配好上课/下课时间，当天触发正常，第二天字段清空需重设。
- **链路定位**：classTimes 存 `scheduler-config.json`；触发逻辑(scheduler.ts:460)每分钟实时读文件、非元凶；本地保存(SchedulerSettings.tsx:102 / SchedulerTasksPanel pushEffectiveConfig)均保留 classTimes（注释 line85「保留classTimes」即旧版掉过）。
- **最强根因**：SPLIT 配置同步 `config-sync.ts:106 mergeJsonFile` 是顶层浅合并，`scheduler_config` 的 `children` 被服务端快照整体覆盖；若服务端 `scheduler_config` 缺 classTimes（有效配置不含，task-runs.ts:6 明示由客户端合并推回），回拉即清本地 classTimes。
- **修法**：mergeJsonFile 对 scheduler_config 改按 childId 深合并（保留本地 classTimes）；服务端 /config/set 存 scheduler_config 时保留非任务驱动字段。写于 ISSUES.md 末尾(ISSUE-053)。

## 07:5x 修复 ISSUE-053（config-sync 深合并保留 classTimes）
- **复核**：服务端 `/config/set` 是哑存储（原样存客户端 push 值，绝不派生 scheduler_config，见 routes/config.ts:92-98）；worker 仅读 legacy scheduler_config（resolveChildConfig `...base` 已含 classTimes）。→ **服务端无需改动**，真凶是客户端 config-sync 拉取浅合并覆盖本地。
- **改动单文件 `electron/lib/config-sync.ts`**：`mergeJsonFile` 加 `key` 参数，对 `scheduler_config` 走 `mergeChildConfigs` 按 childId 深合并：先铺本地全量 child（服务端缺 child 不丢），再逐 child 字段级覆盖；并加 **classTimes 空/缺防丢**（incoming 缺键或 `[]` 而本地非空 → 保留本地；recording/todo/autoNewSession/archiveLimit/classAlertMode 仍以服务端为准）。
- **验证**：tsc 0 业务错（仅 5 环境噪音）；electron-vite build 过；merge 15 场景全 PASS（缺键保留/空[]保留/服务端非空覆盖/本地空用服务端/少 child 不丢/双方空保持空/任务字段覆盖）。
- ⚠️ 未提交、未部署；并行 ISSUE-049/050 改动仍在工作树，提交时分开（本改动仅 config-sync.ts 单文件）。

## 08:24 提交 ISSUE-052 + ISSUE-053 到本地 git
- ISSUE-052 → `14062db` fix(voice): qwen.ts + test/qwen-asr.test.ts（2 文件，180+7）。
- ISSUE-053 → `2709193` fix(config-sync): config-sync.ts 单文件深合并（1 文件，61+3）。
- 均单文件/双文件范围提交，未混入并行会话 ISSUE-049/050 改动（那些文件仍在工作树未动）。
- 三者齐全：051=c7d6edb、052=14062db、053=2709193。未部署，待用户确认后走 201 升级。

## 09:13 部署 server 0.3.3（ISSUE-051）到 201 ✅
- 版本 bump：`server/src/routes/version.ts` SERVER_VERSION 0.3.2→0.3.3（+package.json），commit `91f9151`。
- 构建：`node scripts/build.mjs` → dist/server.cjs 15.8mb（esbuild，勿用 npm run build）。
- 本地冒烟：`scripts/smoke-sessions.mjs` 全过（version 0.3.3 / 会话同步幂等 / 家长回顾 / auth / 任务），spawn dist/server.cjs 即部署产物。
- 部署：tmp/deploy/deploy_server_051.py（paramiko ssh shanshan/123456）——备份 `server.cjs.bak-20260906-0913` → sftp 上传 tmp → sudo mv → systemctl restart。
- **验证 201 生效**：version=0.3.3、systemctl active (since 09:13:40)、health `{"ok":true,"db":"ok"}`；ISSUE-051 码点确认在位：sanitizeSessionFile/appendAndIndexSession/readServerDailyConversation=2、ApiError(400)=28。
- ⚠️ 中文标记 grep 不到是 esbuild 编码（非 ASCII 非原样），用 ASCII 锚点验证。
- 遗留：ISSUE-052(qwen)/053(config-sync) 为 electron 客户端改动，本 deploy 未含；客户端升级走 dpkg+本地重启 GUI 流程。

## 08:22 新增 ISSUE-054（设计讨论：按主题分会话 vs 当前按课程分会话 收益评估）
- **结论**：提案"按主题独立会话"相对现状(按课程隔离+每课干净窗口)在"省token+防串味"两目标都是退步(历史累积更费token、主题内多课仍串)；唯一真收益=同主题跨课连续性(教学性)。建议不整体采纳，改"进新课注入上課摘要"补衔接。
- 写于 ISSUES.md 末尾(ISSUE-054)。

## 12:58 查询：学习资料 html 存放位置 + MEMORY.md 压缩去重
- 资料真源=**服务端 201** 磁盘 `SERVER_DATA_DIR=/opt/learning-server/data` → `materials/<parentId>/<topic>/...`（materialsRoot 见 server/src/db/materials.ts）。客户端(本机 Windows) `data/parents/*/materials/` 仅旧残留非真源；courses.html_path 存相对路径。渲染 srcDoc + asset:///media:// → /api/v1/materials/content/{id} 远程代理。
- 顺手精简 MEMORY.md：把「prompt 反引号坑」从两处并为「构建与验证」区独立一节，删除重复段落；新增强调材料存放位置一条。

## 13:14 新增 ISSUE-055（家长 agent 缺「上传资料到 server」工具）
- **需求**：家长 agent 用 write 做好课程资料后，需把文件推到 server 指定目录(materials/<parentId>/<topic>/)，否则孩子端经 asset:// 读不到(SPLIT 下本地=旧残留)。
- **缺口确证**：服务端 POST /api/v1/materials/upload 已就绪(materials.ts:170)；客户端 uploadMaterialToServer 已封装(parent-library.ts:46)但只被交互式 IPC parent:uploadMaterial(弹窗)复用——家长 agent 工具列表(pi-session.ts:679/725)无此工具。
- **修法**：custom-tools.ts 新增 parent_upload_material(write→upload→parent_course_save 登记 htmlPath)，注册到双家长会话 tools+customTools。写于 ISSUES.md 末尾(ISSUE-055)。

## 13:40 修复 ISSUE：201 材料 other/yunlvcao.html 课程详情 404
- **现象**：家长课程管理→课程详情（韵律操，topic=other）渲染 html 报 404。
- **根因（铁证）**：文件确在 201 磁盘 `/opt/learning-server/data/materials/86a84278.../other/yunlvcao.html`(+media/yunlvcao.mp4)，但 **server.sqlite.materials 索引表无 any `other/*` 行**（全表 8384 行）。`/materials/content/:id` 只查索引表不扫磁盘 → 404「材料不存在」。即文件是绕过 upload 接口手工丢上磁盘的（other 目录 mtime 13:17/13:18）。
- **修复**：node:sqlite 对磁盘 other/* 两文件做等价 upsertMaterialFile 补索引（幂等）；先 VACUUM INTO 备份到 `data/backups/server.pre-yunlvfix-20260906.sqlite`。E2E 验证 content 接口 HTTP 200（html 1937B / mp4 8364864B 均返回）。
- **脚本留存** tmp/deploy/：probe_yunlv_404*.py(诊断) / fix_yunlv_index.js(修复) / e2e_verify_yunlv.py(验证)。坑与教训已沉淀 MEMORY.md「部署边界」。

## 13:5x 修复 ISSUE-055（家长 agent 新增 parent_upload_material 上传工具）
- **改动**：`electron/lib/custom-tools.ts` 新增 `parentUploadMaterialTool`（name=parent_upload_material），复用 `uploadMaterialToServer`(parent-library.ts:46)；本地预校验 topic `[a-zA-Z0-9_-]`、localPath 存在且非目录、subDir 无 `..`/`\`/绝对路径；上传成功 appendActivityLog + 返回服务端相对 path（无 materials/ 前缀）。description 写闭环：write→上传→parent_course_save 登记 htmlPath。
- **注册**：`electron/lib/pi-session.ts` 双家长会话（主 getParentSession + parent-content）tools 白名单 + customTools 各加一处（replace_all，两处相同数组一次改齐）。
- **验证**：tsc 0 业务错（仅 5 环境噪音 global-type，无关本改动）；electron-vite build main+preload+renderer 全过。
- ⚠️ 未提交、未部署；本改动仅 custom-tools.ts + pi-session.ts 两文件，勿与并行 ISSUE-049/050（ipc-handlers/preload/ChildDetailPage 等）混提交。MEMORY.md 已清理压缩（13923→8697B）。
# 2026-09-06 工作日志

## 14:20 修复「闻闻完成泰伯篇第二十章但 todo/家长计划不同步」（201，部署 0261f5eb）
- **现象**：闻闻 09-06 学了泰伯篇第二十章（整章），courses first_learned/last_review=09-06，但 todolist 与家长计划仍显示未完成。
- **根因（拆章课名不匹配课程库）**：家长 agent 把需跨天学的课拆成「××章（上）/（下）」两个 course_name（泰伯20/子路15/子罕12/宪问12 均如此），但课程库 courses 全是整章（带（上）（下）的课程数=0）→ stat/gen/面板按 course_name 精确匹配永远找不到 → 无法判完成。
- **修复两层**：
  1. **匹配容错**：tasks.ts lookupCourseByTitle + study-plans.ts lookupState——course_name 精确匹配不到时剥尾部「（上）/（下）/（中）」再匹配（整章学完，(上)(下) 行都判完成）。无需改历史数据。
  2. **gen 兜底勾已有 todo**：runTodoGenServer 新增 step3——同步已有家长 todo 完成态（同 stat 口径）。否则 gen 预判把 plan 行标 done 后，已有 pending todo 仍等 stat 勾，而 stat 受 daily 新增去重限制不一定及时重跑 → todo 滞后。现 gen 每 2 分钟兜底。
- **验证**：201 真实数据（上）（下）行 done=true；部署后闻闻 09-06 泰伯20（上）（下）todo 均 done@09-06；冒烟 13/13。
- **遗留**：家长 agent 排课仍会造（上）（下）名（容错已兜住判定），若想根治需引导 agent 复用整章名跨天排两行——未做。
- 未提交。

## 14:24 匹配方向改「反向（课程锚定前缀）」（用户指正；201 部署 3a39ce7d）
- **用户意见**：判完成应反向——从 courses 取课程名，去 plan/todo 条目名做前缀/包含匹配，而不是从带后缀的条目名剥「（上）（下）」去猜。
- **改法**：tasks.ts lookupCourseByTitle 与 study-plans.ts lookupState 改为「取 title 是 plan.course_name **（最长）前缀** 的课程」——课程库 title 是权威，plan 名 = title + 任意排法后缀天然命中；取最长避免同前缀多课程歧义。无需剥后缀清单。
- 201 真实数据复算无回归（闻闻泰伯20上下 done；珊珊子路13/14 done；review 按当天复习精准）；冒烟全过；已部署（bak-20260906-1422）。

## 14:46 修复：韵律操视频「有声无画」
- **现象**：闻闻会话渲染 other/yunlvcao.html，video 只有声音没画面。
- **根因**：mp4 视频轨是 **HEVC/H.265 (hvc1, Main, 544×720)** + AAC 音频。Linux Electron 的 Chromium `<video>` 不内置 HEVC 解码（Ubuntu 无硬解）→ 只能解 AAC 出声音，视频黑屏。
- **修复**：201 ffmpeg 转 H.264 覆盖同路径。原 HEVC 备份 `data/backups/yunlvcao.original-20260906.mp4`。新文件 21.9MB（h264 Main 544×720 + aac, faststart）。刷新 materials 索引 size=21958249。E2E content 返回 200。
- **坑沉淀** MEMORY.md「SPLIT/部署边界」新增「视频有声无画=HEVC」条。脚本 tmp/deploy/transcode_yunlv.py + replace_yunlv.py。含视频 moov 的都必须 faststart。

## 14:52 新建 ISSUE-056：家长端 agent 制作学习资料注意事项（视频 H.264 + 走 upload 接口）
- ISSUES.md 末尾新增 **ISSUE-056**：①视频必须 H.264（禁 HEVC/H.265，孩子端 Linux Electron Chromium 无 HEVC 解码→「有声无画」）；②上传必须走 upload 接口（别手工丢 201 磁盘→索引缺 404）。含 ffprobe 判定/ffmpeg 转码命令/排查入口/留存脚本清单。
- 同步把约束写进 `electron/lib/custom-tools.ts` `parent_upload_material` 工具 description（新增「⚠️ 视频编码规范」段：H.264 要求 + 转码命令 + faststart）。tsc 无 custom-tools 业务错。未提交/未部署（ISSUE-055 工具同样未提交，勿混并行 ISSUE-049/050 改动）。

## 16:17 用户确认 ISSUE-047 / ISSUE-041 已做 → ISSUES.md 标已完成
- **047**：`schedule_task` 工具已在(custom-tools.ts:1339, create/list/cancel + 频率 once/daily/weekly/interval + owner:child)，孩子 agent 双数组注册(pi-session.ts:612)；到点播报复用 class:reminder 语音链路。标✅已完成(未提交未部署)。
- **041**：Dashboard.tsx:67-165 侧栏已无孩子列表、纯菜单；添加孩子入口收进「孩子管理」主区(:168-212)。标✅已完成。
- 二者与 049/050/052/053/055 同在工作树，提交时分开。

## 16:2x ISSUE-044（统一日志）只读代码出方案（用户选「先出方案不写码」）
- 范围大 + 工作树有并行未提交(049/050/052/053/055/056)，用户选「先读代码出方案」本轮不动码。方案要点已在回复给出（客户端 app-logger + 服务端 server-log），关键落点核实：
  - 客户端入口仅 `electron/main.ts`（electron.vite.config input=index），logger 初始化须在其模块体最前（import 副作用都在运行时，非 import 期）；71 处 console.* 散落 lib/*.ts。
  - 泛化范式：`sync-logger.ts`（append+prune+吞异常+read/get）+ 导出对话框现成 `sessions:exportLog`(ipc-handlers:144) 可直接照抄成 `app:exportLog`；`app:` 命名空间已存在(app:get_version 等)。
  - 服务端：`index.ts:23 Fastify({logger:true})` 仅 stdout 无 sink；worker scheduler/providers/tasks 用裸 console.*，无 logger 注入；config.dataDir 现成。日志目录建议 `SERVER_DATA_DIR/logs/`。
  - 前端入口：GeneralSettings.tsx「软件更新」下可加「诊断→导出日志」；SessionSyncPanel.tsx 已有导出按钮范式。
- ⚠️ 实施时严格单 issue 范围提交，勿与并行改动混。

## 16:3x 实施 ISSUE-044（统一日志：客户端+服务端，用户选「按方案执行」）
- **新文件**：`electron/lib/app-logger.ts`（client-log.jsonl；log/logInfo/WARN/ERROR + getClientLog/readClientLogFile + installConsoleRedirect（写文件+仍回显，留原始 error 供崩溃回显）+ installCrashHandlers(exitOnUncaught)）；`server/src/log.ts`（server-log.jsonl，SERVER_DATA_DIR/logs/，兜底 cwd/data/logs；initServerLog + installServerConsoleRedirect，**不用 pino transport**：worker 线程在 esbuild 单文件+pkg 易断，用 appendFileSync 对称客户端）。
- **改动**：main.ts（模块体最前 init + app ready 生命周期 + before-quit）；ipc-handlers.ts（app:exportLog 照抄 sessions:exportLog + app:getLogTail）；preload.ts（appExportLog/appGetLogTail）；GeneralSettings.tsx（「诊断·导出应用日志」FileText 按钮）；server/index.ts（initServerLog + console 重定向 + Fastify onResponse 访问日志 hook + boot/DB/监听 logInfo/logError）。
- **验证**：客户端 tsc 0 业务错 + electron-vite build 全过；服务端 tsc 0 错 + esbuild build 过；冒烟 spawn dist/server.cjs → server-log.jsonl 实测含 worker 启动/boot/GET health200/version200/404 访问行 ✅。
- 设计取舍：71 处散落 console 不逐处改，统一 console 重定向捕获（对称客户端/服务端）；服务端会连 SDK 运行时 console 也捕获（仍回显 stdout，行为不变）。遗留：token-stats/parent-library 各自 appendFileSync 未并入；诊断可视化面板未做。
- ⚠️ 未提交/未部署。改动文件与并行(049/050/052/053/055/056)重叠 ipc-handlers/preload/GeneralSettings，提交需 git add -p 只挑本 issue 块，勿混。ISSUES.md ISSUE-044 已标✅已实施。

## 16:39 提交 ISSUE-044 到本地 git
- commit `367d432` feat(observability): 客户端+服务端统一JSONL日志系统(ISSUE-044)，7 文件 513+。
- 并行会话已把各自改动提交(最新 144cc33)，故 ipc-handlers/main/preload/GeneralSettings/server index 的 diff 此时纯属本 issue，直接整文件 add，无需 git add -p。
- 仅提交 7 个代码文件（app-logger/log.ts 新增 + 5 改动）；`.workbuddy/memory/*` 文档改动未提交（含并行内容，留给各会话）。

## 19:4x 家长 Agent 三域落地复盘 + 更新 PARENT-AGENT-REQUIREMENTS.md（用户选「先复盘再定」）
- 逐一读代码把文档(08-30 拍板) vs 现状对账，核心结论：实现走的是 ISSUE 逐项而非文档「阶段1→4」。
- **已落地**：内容域课程级全套(course_save/delete+create_html_lesson+move/copy+log_activity)、ISSUE-055 parent_upload_material、study_plan_*/parent_library_*/course_status、ISSUE-049 每日记录 UI(queryByRange)。
- **未落地（三块真活）**：①配置域 Control 整块(app-config.ts/app_config 工具/schema/.bak/留痕全无，buildParentPrompt §3 仍"只读引导去设置页"，与目标相反)；②主题级 parent_topic_save(upsertParentTopic 仅底层迁移函数、无分配步骤)+「建主题」五步向导纪律(现只覆盖学习计划)；③parent_stats mastery 明细 + progress 多孩子对比。
- **设计被取代**：rules_json.daily 每日目标(→学习计划)、summary 会话摘要(→ISSUE-049/044 已 UI 化,不单做)、P2/P5 更新。
- **文档更新**：PARENT-AGENT-REQUIREMENTS.md 新增 §0「落地对账」(三域状态表+过时点+三块工作量)；header 状态改「方案待实施→ISSUE 逐项推进」；§1 现状表/§2.1-2.3/§3 P决策/§4/§6/§7/§8/§9/§10/§11 均加 ⏳✅⛔ 修订标注(保留原意图档案)。
- ⚠️ prompt「用户保存版本优先」坑已写入文档：配置纪律升级对已存用户版家庭不自动生效，需 AgentPromptEditor 重置/手动采纳(学习计划同款教训)。
- 未提交。下一步待用户定：块1配置域 / 块2主题级+向导 / 块3 mastery 哪个先做。

## 19:5x 按用户要求把「被取代的设计」从 PARENT-AGENT-REQUIREMENTS.md 正文清掉（不再列旧表述）
- 用户第二轮回溯：不要保留 ~~删除线~~ /「已停用」/「⛔ 被取代」这类旧设计档案，把被取代的设计**直接用新方案替换掉**。
- 改动：§2.2 摘要层删掉→改「两层粒度」(会话可见性指向 P2)；§2.3 删「每日目标已停用」注、五步向导 step5 改「随后的每日安排走学习计划」；§3 P2/P5 决策说明改为「以 ISSUE-044 回顾/导出+会话同步为现实口径 / 小结复用 daily_entries」不再写旧隐私模型；§4 删「~~改每日目标~~」行改「学习计划排期落地」；§7 删 rules_json.daily「已停用」注改「每天学什么不进注册表」；§8 整删 `type=summary` 条目与 `parent_allow_raw_session` 提法；§9 删「删除参数 type/dailyGoal 已停用」段改「主题只管内容与方法」；§10 点2、§11 风险表同步去 summary；§1 数据行措辞去「摘要口径」。
- §0 重构为三节：三大能力域落地总览表 / 「关键：被取代的两处设计」(rules_json.daily→学习计划、type=summary→数据小结，正文已按新方案重写) / 「落地环境事实」(SPLIT 真源+白名单现状+用户版本优先坑)。§0 作为唯一变更记录层保留简要说明。
- 交叉引用修正：§2.2「§8摘要口径」悬空→改 P2；§6/§9「§0 ③清单」→「§0 落地环境事实清单」。
- 校验：全文无 ~~ 删除线、无「已停用/纯摘要隐藏」旧设计正文残留（rules_json.daily/type=summary 仅存于 §0 变更记录层）。未提交。

## 20:0x 落地 PARENT-AGENT 三块（块1配置域 / 块2主题级+向导 / 块3 mastery+对比，用户选「全部做完」）
- **块3（parent_stats mastery + 多孩子对比）**：`custom-tools.ts` parentStatsTool——type union 加 "mastery"；progress 改 childId 缺省=listChildren() 循环出「全部孩子学习进度对比」（每孩子复用原组装）；新增 mastery 分支（kb.courses.list 按 topic 分组，status✅=已掌握 / ⬜有first_learned=学习中 / ⬜无=未开始，附 mastery/exam_mastery/首学/最近复习）。注：缺省 daily 的「最近7天」空 stub 仍保留（非本块范围）。
- **块1（配置域）**：新建 `electron/lib/app-config.ts`——`APP_CONFIG_REGISTRY`(12 key：file/scope/type/minmax/desc/highImpact/settable)；`app_config` 工具 get(全读)+set(仅 4 个 app-settings 标量：materialsLimit/defaultModel/programmingModel/visionModel)。set 走现有类型化 setter(自动 pushConfig 同步服务端) + `backupAppSettings()` 写 `.bak-<ts>.json` + appendActivityLog 留痕；高影响项未 confirmed → 返回「请家长确认后带 confirmed:true」两段式。scheduler/profile/agents 项只读(get)，写引导页面/编辑器；`isForbiddenKey` 拦截 auth/license/password/account/server-connection/token 直接报错。注册进 pi-session 双家长会话 tools+customTools(import 新行)。`buildParentPrompt` §3「配置查看」→「配置管理：可读可改、改前确认、改后汇报」+只读项+安全边界，§4 统计补 mastery。
- **块2（parent_topic_save + 五步向导）**：`custom-tools.ts` 新增 `parentTopicSaveTool`(name=parent_topic_save)——topic+name+method+courses[]+assignToChildren(逗号孩子姓名)；先读 parent_lib.topics.list 找旧值做「只覆盖非空合并」；调 `upsertParentTopic`(undefined,...) 写家长库 + `allocateTopicToChild` 分配(快照幂等)；非法 topic 名(非 [a-zA-Z0-9_-])报错。注册双家长会话。`buildParentPrompt` §2 加 parent_topic_save +「建主题」五步向导纪律(①意图②结构草稿③method+courses④html生成上传⑤确认后落库)。删旧「引导去课程管理页」句。
- **验证**：tsc 0 业务错；electron-vite build 全过(main/preload/renderer)；新增 `test/parent-agent-blocks.test.ts` 16 例 + 改 parent-stats 缺 childId 断言；**全套 vitest 336/336 过**。
- ⚠️ vitest 隔离坑：config `getDataDir` 被模块级 memo 缓存，且 vitest.config 设 PI_TEST_DATA_DIR=/tmp/pi-test-data → app_config 测试若只 mock getDataDir 不够，**必须连 getAppSettingsPath/getParentConfigDir/getCurrentParentId 一起 mock 到各自 mkdtemp**，否则写进 /tmp/pi-test-data 跨文件污染（本次 3 例失败根因）。fileParallelism:false 串行。
- ⚠️ 部署提醒（写入 doc §0「三块落地状态」）：app_config/parent_topic_save 新工具 + buildParentPrompt 升级——**已存用户版家长提示词家庭需 AgentPromptEditor 重置/手动采纳**才吃到新纪律。未提交/未部署。

## 20:4x 家长 Agent 会话注入内容审查 + buildParentPrompt 服务端化重写（用户指出数据目录过时）
- **背景**：家长会话(cwd=getDataDir(), buildParentPrompt 单版本)每次注入 = ①system prompt(先读 agents.sqlite 用户版整体替换，无则代码默认) + ②SDK 自动附加 cwd/当前时间/tools schema；noSkills:true 不注入技能；无 planContext/kb 预注入(靠工具现查)。已存用户版家庭不吃代码默认新内容。
- **用户指出两点都对**：①prompt 里塞 ISSUE-xxx 编号是运行时提示词内的历史溯源残留，纯噪声应删；②「数据目录」大量过时——SPLIT 后家长库/孩子库/提示词真源全在服务端(局域网 201)，本地 parents/*/parent.sqlite、children/*/kb.sqlite、data/agents.sqlite 只是缓存副本或根本不存在(default 目录已废弃，config.ts 明确)，prompt 却教 agent 去本地读。
- **只读调查确认的真源边界**：本地真源=data/parents/<真实parentId>/activity-log.md+uploads+app-settings.json+scheduler-config.json、children/*/profile.json+uploads+.pi/agent/sessions、token-log.jsonl、家长自己的 .pi/agent/sessions；服务端真源(走工具)=教学内容(parent_lib.*)、孩子进度(kb.*)、提示词(agents.*)、资料文件(SERVER_DATA_DIR materials，本地 write 不算数须 parent_upload_material 上传)。
- **buildParentPrompt 重写**（electron/lib/pi-session.ts）：「数据目录」树 →「业务数据真源在服务端」+「本地确实存在的文件」markdown 表(删 default/parent.sqlite/kb.sqlite/agents.sqlite 误导项)；资料链路改「本地 write → parent_upload_material 上传服务端 → parent_course_save 登记」；两库职责改服务端措辞+不跨库；**删除全 ISSUE 编号**；改文件路径表述避开模板反引号(用表格/普通文本)。顺手删了上轮发现的数据目录块重复残留。tsc+build+相关测试全过(agents-sqlite/blocks/parent-stats)。
- **修复 log_activity 等写错目录 bug**：custom-tools.ts 6 处 + app-config.ts 4 处 + parent_topic_save 硬编码 `appendActivityLog("default",...)`，default 已废弃 → parent-library.ts 新增 `logActivity(entry)`=appendActivityLog(getCurrentParentId()||DEFAULT_PARENT_ID)；全替换。协议 media:// 的 DEFAULT_PARENT_ID(default 作 URL 占位)保留不动。测试 mock getCurrentParentId=" "→回退 default 恰与 getActivityLogPath() 默认一致，无需改断言。
- 未提交。提醒沿用：已存用户版家长提示词需重置才吃新默认。


## 21:5x buildParentPrompt 措辞去「旧方案/default/hack 前缀」
- 用户反馈 prompt 三处误导：①不该说「旧设置已停用」②media url 写死 parent/default（且渲染时系统自动注入 base href，agent 手写完整绝对 media:// 反而多余易错）③复习不该教「内容前加『复习：』文本前缀」。
- 改动 `electron/lib/pi-session.ts` buildParentPrompt 4 处：L185 删「不再走设置每日目标页面」→「孩子每天学什么以学习计划为准，对话里定」；L190 media 引用改「一律写相对路径 media/文件名，渲染时按登录家长自动解析，勿写死某家长 id 的完整 media:// 绝对地址」；L199 删「旧的每主题每天 X 课已停用」、明确每行 mode 字段 new/review；L202 复习改「课程名存干净课名 + mode 字段标 review（创建标注复习或用 study_plan_update setmode）」。
- 同步 `custom-tools.ts` study_plan_create 描述/L1752、content 参数 L1760：把「加复习前缀」澄清为「前缀会被拆成库内 mode=review，库内存干净课名；也可先建后 setmode」。
- ⚠️ 坑（复用 memory 已有）：buildParentPrompt 是模板字符串，内联**禁字面反引号**——我中途写 `media/文件名` 导致 tsc TS1005，须用「」或去掉。已清空该区域所有内联反引号。
- 验证：tsc 0 错、electron-vite build 通过。未提交 git。

## 22:1x 修「家长界面点 AI 提示词不弹框」（Dashboard 遗留 bug）
- **根因**：commit 36825d6（ISSUE-007 孩子详情页 tabs 替代弹窗）删了 Dashboard.tsx 的 `agentPrompt` state 定义 + `AgentPromptEditor` import/渲染，但**漏删 header 按钮 `onClick={() => setAgentPrompt({scope:"parent",ref:"main",...})}`** → 运行时 `ReferenceError: setAgentPrompt is not defined`，点击无弹框。该 bug 在仓库基线就存在（git show HEAD 仅 1 处引用），非近期改动引入。
- **修复 `src/pages/Dashboard.tsx`**：①import AgentPromptEditor（默认导出=modal 弹窗容器）；②加 `const [agentPrompt,setAgentPrompt]=useState<{scope;ref;title}|null>(null)`；③末尾 AddChildModal 旁渲染 `{agentPrompt && <AgentPromptEditor scope=... onClose={()=>setAgentPrompt(null)}/>}`。onClick 传 ref:"main" 即可——主进程 agents:get/save/history/restore 对 parent scope 一律 `ref=getCurrentParentId()` 归一化。
- 注意：AgentPromptContent 平铺版（详情页用）与 AgentPromptEditor 弹窗版（Dashboard/管理历史入口用）是两个导出，别搞混。scope=parent 时编辑器会渲染 PARENT_REFS(main) 切换按钮。
- 验证：tsc 0 错、electron-vite build 过。未提交 git。

## 22:3x 家长 AI 提示词改为「默认+追加」模型（防改坏功能）
- 用户要求：默认家长提示词不让整体改（怕改坏 app 不支持指令）；家长界面 AI 提示词默认空、家长填的当「补充片段」追加到当前 prompt。仅家长 scope，孩子 AGENTS 编辑器维持整体替换；历史数据不迁移直接切换语义。
- 改动：
  1) `electron/lib/pi-session.ts` buildParentPrompt：把整段默认收进 `const base=\`...\``，不再 `return userVersion` 整体覆盖；改为末尾 `const addition=getAgentPrompt("parent",getCurrentParentId())`，有则 `base + \n\n# 家长的补充要求\n\n<说明>\n\n{addition.trim()}`。child resolveChildAgents 不动。
  2) `electron/lib/ipc-handlers.ts` agents:get：parent scope 无用户版不再返回默认全文当底稿（改返回 ""），child 分支仍返回 getDefaultPrompt 底稿。
  3) `src/components/AgentPromptEditor.tsx`：scope==="parent" 时 customized 提示/placeholder/save/reset 文案改为「追加/清除补充」；child 保持「整体替换」。
  4) `src/pages/Dashboard.tsx`：家长入口 tooltip 与弹窗 title 改「AI 提示词补充（追加到末尾，默认不可改）」。
  5) `electron/lib/app-config.ts`：agents.parent desc 与 parentPromptHasUserVersion 返回值同步「追加」语义。
- ⚠️ getDefaultPrompt("parent") 现为死代码（agents:get 已不走），但无害保留。buildParentPrompt 历史整段版本切换后当追加片段（用户同意不迁移）。
- 验证：tsc 0 错、electron-vite build 过。未提交 git。

## 22:48 新增 ISSUE-057（家长端删孩子：uuid 目录未清理）
- **现象**：家长界面删孩子后,data 里该孩子 uuid 文件夹没删。
- **链路口**：前端 ChildDetailPage.tsx:60 handleDeleteChild→childDelete→ipc child:delete(ipc-handlers.ts:269)→deleteChild(child-auth.ts:323, fs.rmSync data/children/<uuid>)→服务端 DELETE /children/:id(children.ts:169,仅删DB行)。
- **两大缺口**：①client 删目录前未 dispose 内存 activeSessions(主+课程子会话),有重建竞态；②**SPLIT 主因**=服务端 DELETE 明示"本期不做物理删除",kb(<dataDir>/kb/<pid>/<uuid>.sqlite)、sessions(<dataDir>/sessions/<pid>/<uuid>/)、materials 子树、courses/daily/todo/study_plan/exam 行全残留。
- **修法**：deleteChild 删前 dispose+flush 该 childId 所有 activeSessions；服务端 DELETE 删行后物理 rm 上述路径+清表行。写于 ISSUES.md 末尾(ISSUE-057)。

## 23:2x 复盘家长agent「星球研究所」主题制作 —— media 引用机制纠正（我上轮改错）
- 实测制作过程（activity-log + 会话 jsonl）：agent 生成 3 个 html 播放页时首次写了相对路径 `<video src="media/xingqiu1.mp4">` → 用户报「网页能看见视频读不出」→ 改回完整 `media://local/parent/<pid>/xingqiu/media/xingqiu1.mp4` 重新上传才修复。**诱因正是我 21:5x 把 buildParentPrompt L190 改成「一律写相对路径 media/文件名」**——方向错了。
- **根因（Explore 确认）**：`rewriteMaterialHtmlForRender`→`rewriteHtmlAssetRefs`（parent-library.ts L852-887，L885）把 html 里**所有相对 href/src 统一改写为 asset://**；而 asset 白名单（media-protocol ASSET_ALLOWED_EXT）**不含 .mp4/mp3 等音视频** → resolveAssetTarget 返回 null → 403 播不出。只有完整 media://（走 media 白名单）能播。css/js/图片走 asset 正常。孩子 display_content 与家长 readParentMaterial 共用同一 rewrite，规则一致。
- **正确写法**：mp4/mp3 一律 `media://local/parent/{parentId}/{topic}/media/xxx.mp4`（parentId 段仅占位，服务端按 session token 路由不校验，填 default/真实 id 均可，范本用真实 id）。相对路径对音视频不可行。
- **治本建议**：rewriteHtmlAssetRefs 按扩展名分流——音视频(mp4/mp3…∈media白名单)改写为 media://，其余(∈asset白名单)改写 asset://；则 agent 写相对路径也能两端播，prompt 也不必教长协议串。待用户定夺。
- 教训：改 prompt 指引前必须先验证渲染层真实行为，不能想当然（memory 9/6 已记 buildParentPrompt 模板反引号坑，此为第二条教训）。

## 23:3x P1/P2 落地：media 相对引用分流 + upload 路径对齐（用户已认可方案）
- **P1（根治「相对路径对视频 403」）**：`electron/lib/media-protocol.ts` 新增 `buildMediaUrl()` + `isMediaExt()`（isMediaExt 查 media 白名单 ALLOWED_EXT=mp3/mp4/ogg/wav/webm/m4a/aac/flac/m3u8）。`parent-library.ts` `rewriteHtmlAssetRefs`（L852-887）按落点扩展名分流：音视频→`buildMediaUrl`(media://)，css/js/图片/字体→`buildAssetUrl`(asset://)。此前统一 asset:// 会让音视频落 asset 白名单之外→resolveAssetTarget 返 null→403 播不出。P1 后 agent 写相对路径 media/xxx.mp4 两端(家长 readParentMaterial + 孩子 display_content 共用此函数)都能播。
- **P2（upload 相对路径坑）**：根因=`parent_upload_material`(custom-tools.ts L757) execute 裸 `fs.existsSync(localPath)` 要求绝对路径，而同会话 move_file/copy_file 用 `guardCwd(ctx.cwd, p)` 接受相对→agent 从 move 类习惯相对、到 upload 报「不存在」反复试错。修法=给 execute 加 ctx 参，localPath 先 `guardCwd(ctx.cwd, localPath)`（接受绝对或相对会话 cwd，防越界）；工具描述 localPath 参数注明支持相对路径。
- **同步改 buildParentPrompt L189 media 指引**：确认相对路径可行(系统自动解析 media:///asset://)，明确「不要在 html 里写死完整 media:// 绝对地址」，并补「html 与媒体文件都要 parent_upload_material 上传(html 传 topic 根、媒体传 topic/media)」。⚠️buildParentPrompt 是模板字符串内联禁反引号(又踩一次,改用「」)。
- 新增 `test/material-rewrite.test.ts`(4 例：mp4/mp3→media://、图片css/js→asset://、已写死绝对 media:// 不动、fileDir="." 相对上溯)。验证：tsc 0 错、4 测试过、electron-vite build 全过。未提交 git。

## 23:5x P1/P2 已提交归档
- commit `a531325`(feat): P1 media 分流 + 上一轮「家长AI提示词追加模型」合并一笔(两者在 pi-session buildParentPrompt 同段耦合无法拆)。
- commit `eabea80`(fix): P2 custom-tools parent_upload_material 路径对齐(guardCwd 接受相对/绝对)。
- 工作区仅剩 `.workbuddy/memory/ISSUES.md`(ISSUE-057 删除孩子孤儿数据,无关未提交)。
- 验证均过: tsc 0 错、material-rewrite.test.ts 4 例、electron-vite build。

## 23:59 新增 ISSUE-058（201 生产：21:00 recording 当天多次执行 珊珊3/闻闻2）
- **现象**：201 上 recording 任务(21:00) 同一孩子当天跑多次(珊珊3/闻闻2)。
- **根因(去重非原子+并发重叠)**：`runWorkerTick`(scheduler.ts:256) 桶[21:00,21:05)含3跳(21:00/02/04)；`alreadyRanToday`(:166)读 `worker_state.last_key` 后跑 LLM(异步数十秒~>2min)才写去重(:238)——node-cron 不防同回调重叠→21:02/04 跳读到 stale false 再跑(主因,单进程即可3 vs 2)；多进程(>1 server.cjs)共享 sqlite 非原子 claim 竞态(次因)；多 recording 任务行不同 time 各跑一次(待查,需查 scheduler_tasks);旧客户端本地双跑(次要)。
- **确认 SQL**：ps aux 看进程数；task_runs 按 child 数次数/耗时；scheduler_tasks 看每人几条 recording。
- **修法**：进程内 in-flight 锁(最小必做)+worker_locks 原子 claim(多进程)+单实例部署+同类型任务按 time 去重。写于 ISSUES.md 末尾(ISSUE-058)。

## 24:0x P3 落地：家长 agent 识图 + 语音转录工具
- 用户要「给家长agent识图和语音转录工具」，让它能读懂图片/音视频真实内容起草教学文案(替代靠文件名猜)。
- 调研复用：ASR 现成 `transcribeAudio(buf)`(voice/index.ts, ffmpeg 抽 16k wav→千问/MiMo,吃 mp3/mp4/webm,但依赖设置里语音 enabled+凭证)；识图无现成 OCR,复用视觉模型。SDK createAgentSession 支持 tools:[]/noTools 纯问答;exam-engine 的 lastAssistantText+inMemory 临时会话是模板。
- 新增 `electron/lib/parent-vision.ts`:`describeImageViaVision(dataDir,image{mimeType,data base64},question?)` 一次性 in-memory 会话+visionModel(qwen3-vl-flash 默认)+noTools 发 prompt({images})取文字即弃;`imageMimeFromExt`。
- `custom-tools.ts` 加两工具: `parent_transcribe_media`(localPath→扩展名校验→transcribeAudio,未配置透传引导设置) + `parent_read_image`(localPath+可选question→guardCwd读图→base64→describeImageViaVision,>15MB拒)。
- `pi-session.ts` 注册两工具进两个家长会话(getParentSession L683+getParentContentSession L729 的 tools+customTools,replace_all),import 行加两对象;buildParentPrompt §2 加「内容对准真实资料(P3)」提示(起草前先转录/识图)。
- ⚠️ guardCwd 需 ctx.cwd(P2 已确立模式);两个工具 execute 都走 guardCwd。识图用 getDataDir()(config)当会话 cwd。
- 验证: tsc 0 错、electron-vite build 过。未提交。家长需在设置开语音才能用转录;识图默认 vision 即可。
