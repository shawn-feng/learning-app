# 2026-09-15 工作日志

## 新增 ISSUE-102（家长 agent 缺「读取孩子全部会话内容」工具）
- 用户反馈：家长 agent 需要一个工具，能读取孩子的所有会话内容。
- 现状核查：家长 agent 现有工具（parent-tools.ts）只覆盖资料/家长库/计划域，**无读孩子对话逐字稿的工具**；但底层读链路已存在且经「家长对话回顾」页验证——`db/sessions.ts` 的 `indexAgentSessionsIntoDb`/`querySessionMessages`/`listSessionDates` + `routes/sessions.ts` 的 `assertChildOwned` 归属校验。
- **关键：本需求扩张原隐私红线**「家长 agent 只读孩子数据 summary、不触碰原始对话」（见 memory/MEMORY.md 与 DESIGN-server-agent-migration），属家长授权扩张；实现须保留「只读 + 归属校验 + 按天/限量读取」三约束。
- 修复方向：F1 在 parent-tools.ts 新增 `parent_read_child_conversation`（参数 child/date，复用 indexAgentSessionsIntoDb+querySessionMessages，默认按天+总量上限）；F2 同步 prompt 说明并撤销旧红线措辞（落地后改 MEMORY.md 与技术实现文档）。
- 详情见 `ISSUES/ISSUE-102.md`，索引计数更新为 102（标记🟡待处理）。

## ISSUE-102 实施完成（本轮）
- F1：`parent-tools.ts` 新增 `parent_read_child_conversation`（只读）——`resolveConvoChild`（姓名精确匹配 + 归属校验，找不到列出可选名）、`indexAgentSessionsIntoDb` 增量索引 → `querySessionMessages`；`date` 缺省今天 / YYYY-MM-DD / all+days（默认 3、上限 7）；输出 `孩子名：…` / `AI 伙伴名（profile_json.aiName，缺省饺子）：…`，assistant 附工具调用名；单条 600 / 总量 16000 字符截断；无记录时列出有记录日期；已进 return 数组 + `PARENT_AGENT_TOOL_NAMES`。
- F2：`parent-registry.ts` prompt 新增「孩子的对话记录（只读，家长已授权）」段（适用场景/归属/只读/汇报概括不复述）；技术实现文档 §4.1 记录工具与隐私边界变更（原「不触碰原始对话」→「可经只读工具读取」）。
- 验证：server typecheck + esbuild build 通过；真实数据冒烟 5 例全过（今天 15 条 / all 3 天 / 2026-09-13 166 条截断 / 不存在孩子列可选名 / 非法 date 提示）。
- 未部署 201；本地 server（用户 06:30 自启 pid 18184）仍是旧代码，需重启才加载新工具——待用户决定重启方式。

## 环境经验（已写入 skill：windows-monorepo-build-verify §1.5）
- 冒烟脚本必须放**目标包目录内**（server/）才能解析 tsconfig paths（如 @pi/agent-core）；放仓库 tmp/ 会 import 失败。
- 脚本须自写结果文件 + uncaughtException/unhandledRejection 钩子，否则「exit 1 + 零输出」无从定位。
- tsx 退出码不可靠（全通过也可能返回 1），判定以结果文件为准。

## ISSUE-102 后续：用户报「家长 agent 说没有这个工具」（07:23）
- 排查结论：**工具已生效，模型被自己的会话历史锚住**。
  - 进程核对：server pid 28652 started 07:08:40 > parent-tools.ts mtime 07:01:01 → 运行的是新代码。
  - 证据1：自签家长 token 在**全新 parent-content 会话**问「列出你的工具」→ 清单含 `parent_read_child_conversation`。
  - 证据2：在**用户的 parent 会话**里让它实际调用 → toolCall 出现且调用成功（读回珊珊 173 条）。
  - 根因：同一 parent 会话历史里，昨晚 22:49/23:21 模型已答过「我无法读取珊珊的会话内容」→ 追问时顺着旧答案继续否认，不查当前工具表。
- 副作用披露：探针在 parent 会话留下 1 问 1 答（系统自检）、在 parent-content 留下 1 问 1 答。
- 复用方法已沉淀为技能 `pi-local-server-agent-diagnose`（自签 JWT 直连 API + 读 agent-sessions jsonl 判 toolCall 铁证 + 三类原因分流）。

## 两项改进实施（用户要求「两个都做」）
1. 家长会话重置 = 真正新会话（parent-registry.ts：resetMarks + shouldAutoNewSession；resetParentSession 置标记）
2. parent_read_child_conversation 的 date 支持 今天/昨天/前天/today/yesterday（normalizeDateParam）
验证：typecheck+build 通过；date 别名离线冒烟 5 例全过；重置语义端到端——重置后新会话首次追问目录新增 jsonl（2026-09-14T23-28-59-471Z_…），干净上下文里模型正确回答能读孩子对话。
本地 server 已由助手重启（后台任务 ki1n9s）加载新代码；文档 §8.2 记录重置语义；技能 pi-local-server-agent-diagnose §4 已同步（原「需改代码」→ 现已是新会话语义）。

## 打包最新客户端+服务端（08:19，不发布）
- 版本：客户端 **0.1.15**（package.json 已是，无需再 bump）、服务端 **0.4.1**（version.ts + server/package.json，均已就位）；上次 tag v0.1.14 / server 0.3.5。
- 现状：本地 master 比 github 远程**领先 181 个提交**（从未推送）。用户确认「推送后构建 CI，但不发布」。
- 动作：
  1. 服务端：`node server/scripts/build.mjs` → `server/dist/server.cjs`（16.7MB，SERVER_VERSION=0.4.1，import_meta.url 补丁 18 处）。
  2. Windows 本机：`unset NODE_OPTIONS && npm run dist:win`（9m57s）→ `dist/学习伙伴 Setup 0.1.15.exe`(171.6MB)+blockmap+latest.yml；**现已自动 signtool 签名**。
  3. 推送 master → github + gitee（41854ce..95c13aa）。
  4. CI：`gh workflow run build-linux.yml` / `build-mac.yml`（workflow_dispatch，无 tag）→ 两 run 均 success。
  5. 下载产物：Windows 在 dist/；Linux deb+AppImage、Mac x64/arm64 dmg 在 `ci-artifacts/`。
- 踩坑：一次 `gh run download` 拉全部产物时被沙箱 **SIGTERM** 打断（大传输量），仅落部分文件；改**逐个 `--name` 下载**解决。已把该坑写入 PACKAGING.md §2.2。
- **未发布**：未部署 201、未传 OSS/ECS、未登记 /api/version、未打 tag。`server/dist/server.cjs` 为构建产物未提交（gitignored）。
- 待用户决定：是否正式发布（届时按 PACKAGING.md 走 201 部署 + 公网发布 + 打 tag v0.1.15）。

## 201 部署前数据结构核查（09:24，只读探测，未部署）
- 目的：部署服务端 0.4.1 到 201 前，先查数据结构是否有变、有变则定迁移方案。
- 方法：paramiko SSH 只读探测 201（`node:sqlite` readOnly，未写入/未部署）。探测脚本用完即删（含凭据）。
- 201 现状：**learning-server 0.3.5**，features=[session_sync,worker,exam]，node v24.15.0，data 1.9G。
- **核心结论：201 结构已与 0.4.1 完全兼容，无需任何手工结构迁移**（逐库逐列比对，无缺失表/列）。
  - 主库：16 张表全在、关键列全在；**多出 exam_schedules（0 行）** → 启动自动 DROP，安全。
  - 孩子库：2 个真实孩子（珊珊/闻闻）计划域 11 表 + 2 视图齐备、courses 已删旧列且有 uuid、plan_domain_migrated=2026-09-11；多出 todo_items(74/38)、child_todo_stats(9/8) → 新版不读不写不删。
  - 家长库：courses 1315/题 3575/知识点 1969/挂载 3575；多出 course_category_questions(0)、topic_categories(0) → 自动 DROP，安全。
  - agents.sqlite 一致。自动迁移全幂等；`agent-sessions/` 目录不存在 → 代码按需 mkdir。
- **无需跑 migrate-plan-domain.mts**（真实孩子库已置位）；**不要**对 3 个孤儿 kb（4d6e76fc / test-parent×2）跑（缺 todo_items 会报错）。
- 数据连续性：session_messages **4095 行**（08-19~09-14）保留 → 对话回顾不丢；exam_attempts 0；jwtSecret/.secret 保留 → token 与加密 auth 可用。
- ⚠️ **预存问题（非本次引入）**：`routes/exam.ts` 的固定档考核候选读**主库 study_plan_items**（9/11 起冻结、数据 9/3~9/30 快照），而计划真源已是孩子库 study_plans → 9/11 后新建计划不进候选。v0.1.14/0.3.5 已如此。建议单独登记 ISSUE。
- 产出：`DEPLOY-201-server-0.4.1-迁移方案-2026-09-15.md`（结论/逐库对照/自动迁移清单/人工迁移=无/数据连续性/风险/部署+备份回滚步骤/探测证据）。
- **未部署 201**（遵守 09-14 约定，需用户明确同意）。

## 执行部署：服务端 0.4.1 → 201（09:33，用户明确同意后执行）
- 用户先「准备部署服务端到201」→ 核查（见上）→ 用户「执行部署」→ 已完成。
- 实际启动方式确认：201 unit 已是 `ExecStart=/usr/bin/node /opt/learning-server/server.cjs`（无 User=，以 root 跑），**pkg 已弃用**（PACKAGING.md 里"service 仍指向 pkg"的说法对 201 已过时）。
- 流程：stop → 备份 → 传新 bundle → daemon-reload → restart。**停机仅约 6 秒**。
- 备份：旧 bundle `server.cjs.bak-20260915-0933`；数据 `data/backups/deploy-0.4.1-20260915-0933/`（36M = server.sqlite(+wal/shm)+agents+kb+parents+json+.secret）。
- 结果 ✅：`/api/v1/version`=**0.4.1**，features=`[session_sync,worker,exam,server_agent,parent_agent,exam_agent]`；`/api/v1/health`=`{"ok":true,...,"db":"ok"}`；启动日志**零迁移/报错**；worker 起、监听 8788。
- 迁移实测：`exam_schedules` **已 DROP**；行数零减少（session_messages 4095 / study_plan_items 290 / study_plans 186+121 / courses 1315 / question_bank 3575）。
- ⚠️ 更正一条预判：`course_category_questions` / `topic_categories` **不会被自动删**（`assess-content.ts` 的 DROP 在 `if(!ckq_migrated)` 内，201 已 `ckq_migrated=1` → 整段跳过）；两表仍在、各 0 行、无引用。
- 仅部署服务端；201 的 **Ubuntu 客户端仍是旧版**（新服务端向后兼容，客户端仍在正常轮询 config/children/reminders）。如需验证服务端 agent 能力，需另装客户端 0.1.15（GUI 须在 201 本地重启）。
- 文档已回写实测结果：`DEPLOY-201-server-0.4.1-迁移方案-2026-09-15.md` §9 执行记录（含回滚步骤）。

## 执行部署：客户端 0.1.15 → 201（09:40）
- 用户「继续部署客户端」→ 已装 **0.1.14 → 0.1.15**。
- 201 客户端实况（此前记录有误，已更正）：进程属主 = **shanshan**，桌面会话 `:0` 在线，binary `/opt/学习伙伴/xuexihub`（root 属主，dpkg 管），用户数据 `/home/shanshan/.config/learning-app`（升级不动）。
- **GUI 可以远程重启**（推翻 0.1.9 时"SSH 杀不掉"的结论）：`pkill -TERM -f '/opt/学习伙伴/xuexihub'`（进程属主=SSH 用户，无需 sudo）→ `sudo dpkg -i` → `DISPLAY=:0 XAUTHORITY=/home/shanshan/.Xauthority setsid nohup '/opt/学习伙伴/xuexihub' >/tmp/xuexihub-0.1.15.log 2>&1 < /dev/null &`，5 秒后进程数 7、属主 shanshan，服务端 09:40 看到新客户端轮询 config/children/reminders ✅。
- 回滚：`/tmp` 还留有历史 deb（0.1.8/0.1.9/0.1.11/0.1.13/**0.1.14**）→ `sudo dpkg -i /tmp/learning-app_0.1.14_amd64.deb` + 同样方式重启 GUI。
- 客户端启动日志正常；自动更新提示 "latest version: 0.1.13, downgrade is disallowed" 属**预期**（未发布公网 feed，本地 0.1.15 更新，故不升级）。
- 结论：201 现为 **服务端 0.4.1 + 客户端 0.1.15**，均在线。

## 新增 ISSUE-103（家长 agent 管理课程缺「知识点 + 题库」工具）
- 用户反馈：家长 agent 管理课程需"建主题→建课→教学+资料→知识点并关联→题目并关联知识点"全链路；前 3 步已有工具，后 2 步（知识点、题、关联）无 agent 工具，只能走 UI。
- 核查结论：DB 层（`server/src/db/assess-content.ts` 的 `question_bank`/`knowledge_points`/`course_knowledge_questions` + `saveQuestion`/`getOrCreateKnowledgePoint`/`replaceCourseContent`）+ REST 层（`POST /assess/questions`、`POST /assess/courses/save`、`GET .../knowledge-points`、`GET /assess/courses/:topic/:title`）**均已齐备可用**，缺口只是"封装成家长 agent 工具"。
- 分析结论：当前工具**不够**，应新增。建议 2 个：① `parent_library_course_content`（读，复用 listCourseContent）② `parent_upsert_course_content`（写，复用 POST /assess/courses/save 逻辑，事务整课替换挂载，一次性覆盖"建知识点+建题+关联"）。落地须同时进 return 数组 + `PARENT_AGENT_TOOL_NAMES`（防 ISSUE-089 白名单-only 陷阱）+ prompt 补指引；replace 是覆盖语义，prompt 须提示"先读现状再写"。
- 详情见 `ISSUES/ISSUE-103.md`，索引计数 102→103。

## ISSUE-103 实施完成（本轮）
- 新增 2 个家长 agent 工具（`server/src/agent/parent-tools.ts`，均同时进 return 数组 + `PARENT_AGENT_TOOL_NAMES`）：
  - `parent_library_course_content`（读）：某课全部**知识点**（含未挂题的）+ 每个知识点下**挂题**（题干/答案/behavior/分值），带 `id=` 供复用。底层 = `getCourseUuid` + `listKnowledgePoints` + `listCourseContent`。
  - `parent_upsert_course_content`（写）：`topic`/`title`/`items[]`（知识点名或 id + detail/overview + questions[]；题可 `questionId` 引用或内联 `stem`+`answer` 新建）。底层 = `getOrCreateKnowledgePoint` + `saveQuestion` + `replaceCourseContent`（与 REST `POST /assess/courses/save` 同一批 DB 函数）。**一步覆盖「建知识点 + 出题 + 关联」**。
- 实施中加了两道原方案没有的安全闸：① **只读预校验**（全部 items 先扫一遍再落笔——冒烟实测到「报错但残留孤儿知识点」的真实脏数据问题）；② **移除告警**（写后对比替换前后挂载快照，被移除的知识点/挂载显式列出，防「只传增量 → 覆盖家长手建内容」）。
- prompt（`parent-registry.ts`）新增「落库课程考核内容（知识点 + 题库，家长库真源）」段：五步顺序 + **必先读后写** + **items 是整课全量快照不是增量** + behavior 取值 + 先复述给家长确认。
- 验证：`tsc --noEmit` 0 错；`node scripts/build.mjs` 成功（`server/dist/server.cjs` 17.51MB 含新工具）；冒烟脚本（临时家长库）**29 条断言全 PASS**（含错误路径与 DB 落库核对）；脚本跑完即删，输出留 `tmp/smoke-103-result.txt`。
- 文档回写：技术实现文档 §3.1（含修正过时的 ISSUE-089 缺口描述）/ §6.4 / §8.2 / §8.3 / §17 第 15 行 / 附录 A.1。
- **未部署 201**；本地 dev server 需重启才生效（旧会话历史可能锚住模型 → 必要时先「重置家长会话」）。

## 部署服务端 0.4.2 → 201（11:45，用户明确同意后执行）
- 版本 0.4.1 → **0.4.2**（`routes/version.ts` + `server/package.json` 同步 bump，纯代码变更、**无 schema 变更**）→ `node scripts/build.mjs`（bundle 17,510,718 B）→ 部署。
- 流程：sudo 校验 → `systemctl stop` → 备份旧 bundle `server.cjs.bak-20260915-1145`（17,545,271 B）+ 数据 `data/backups/deploy-0.4.2-20260915-1145/`（36M）→ 上传装包 → `daemon-reload` + `restart`。**停机约 3 秒**（11:45:31 stop → 11:45:33/34 started）。
- 结果 ✅：`/api/v1/version`=**0.4.2**（features 不变）、`/api/v1/health`=`{ok:true,uptime:3,db:ok}`、启动日志**零迁移/报错**、worker 起、监听 8788；日志可见 Windows 客户端 `parent-agent/stream` SSE 自动重连。
- **包内自查**（证明新代码真在线上）：`grep -o` 命中 `0.4.2`×1、`parent_upsert_course_content`×8、`parent_library_course_content`×10。
- **部署后只读探测真实家长库**（`node:sqlite` readOnly，未写入）：课程 1315 / 知识点 1969 / 题库题 3575 / 挂载 3575；**单课最多挂题 9、最多知识点 5** → 读工具输出体量可控、无需分页；**未挂题知识点 0 / 未挂课题 0 / 知识点指向不存在课程 0** → 「移除告警」不会被存量数据误触发。磁盘 `/` 90%（剩 12G）。
- 记录写进 `DEPLOY-201-server-0.4.1-迁移方案-2026-09-15.md` §10（含回滚四步）；`memory/MEMORY.md` 的「当前已部署」更新为 0.4.2。
- **未改客户端**（201 与 Windows 侧客户端都无需升级，服务端变更向后兼容）；家长 agent 在服务端跑 → 新工具已对家长端可用。
- 环境经验：Windows 侧 `Remove-Item` 会「假成功」（exit 0 但文件仍在）→ 删临时文件改用 Node `fs.rmSync` + 回读校验；已写入 skill `windows-monorepo-build-verify` §1.3b。paramiko 在 `~/.workbuddy/binaries/python/envs/default`（5.0.0）可用。

## 新增并修复 ISSUE-104（家长端自定义考核白屏 + agent 看不到课程细节，12:13 用户报）
- 用户报：客户端连 **201** 时「家长界面 → 考核管理 → 自定义考核」**白屏**（连本地 dev 正常）；另家长 agent 查考核计划**拿不到课程信息与细节**。
- 现场诊断（201 只读，自签家长 JWT 直连 API + 读孩子库）：**一个根因两个症状**——`exam_plans.scope_json.courses` 自 2026-09-14 起由 `["课程名"]` 变成 `[{title,kps:[{name,count}]}]`（计划生成即约定出题参数），但两个消费方没跟上：
  - `ExamAdminPanel.tsx` 直接把数组项当 React 子节点渲染 → `Objects are not valid as a React child` → 整树卸载白屏（`key={c}` 同源）；**本地 dev 无自定义考核数据**故走空态分支不复现。
  - `parent_exam_plan_list` 用 `String(x)` 转对象 → `"[object Object]"`，课程/知识点/题数全丢，`note`/`methodSpec` 也没展示。
  - 201 实测：珊珊 4 条计划（3 cancelled + 1 pending）全是新格式；闻闻 0 条；`exam_plan_courses` 均 0 行。
- 修复（两侧都改，互为保险）：① 服务端 `assess-selection.ts` 新增 `parsePlanCourses`/`formatPlanCourses`（两格式兼容、不抛错）；② `parent_exam_plan_list` 输出改为「考核内容：课程（知识点×题数）+ 主题 + 考核方法 + 说明」，固定档明示"课程由本周期必学课程决定"；③ `GET /exam/schedules/:childId` 把 `scope.courses` 归一成课程名数组（**老客户端立刻不再白屏**）+ 新增 `scope.courseSpecs` 供新端展示细节（补回 09-14「渲染层零改动」的原意）；④ 客户端新增 `src/lib/plan-scope.ts` + `src/components/PlanCourseList.tsx`（渲染前必过归一化）。
- 验证：server tsc 0 错 + build 通过；客户端 `npm run build` 通过（`out/renderer/assets/index-CRvtav-H.js` 含新逻辑）；新增 `test/plan-scope.test.ts` **9/9 通过**（含"把对象数组直接当子节点渲染确实会抛错"的机理对照）；全量单测 17 条失败均为既有环境问题（`Cannot find module '/electron/lib/assessment'`），无一条引用本次触碰文件。
- 环境坑（值得记）：① vitest 默认 include 只有 `test/**/*.test.ts`，**`.test.tsx` 不会被收集** → 用 `createElement` 写渲染测试放 `.ts`；② 仓库根 `npx tsc --noEmit`（TS 7.0.2）报 `Cannot find global type 'Object'`（本地 typescript 包无 `lib.*.d.ts`）属既有环境问题，客户端类型校验以 `npm run build` 为准；③ 客户端 renderer 产物在 `out/renderer/`（`src/out/` 是 9-03 的陈旧残留，勿当成现行产物）。
- **未部署 201**（等用户明确同意；仅服务端部署即可消除白屏）。

## 部署服务端 0.4.3 → 201（13:33，用户明确同意后执行）
- 版本 0.4.2 → **0.4.3** → 部署；停机约 **2 秒**（13:33:34 stop → 13:33:36 started）；`version`=0.4.3、health ok、`ERR_COUNT=0`；包内 `grep -o` 命中 `courseSpecs`×4 / `parsePlanCourses`×3 / `parent_upsert_course_content`×8。备份：bundle `server.cjs.bak-20260915-1333`；数据 `backups/deploy-0.4.3-20260915-1333/`（36M）。
- **接口级验证（真实数据，自签家长 token 只读）**：`GET /exam/schedules/1f050a7f-…`（珊珊 1 条计划）→ `scope.courses = ["论语学而篇第八章"]`（**字符串数组 ✅，老客户端不再白屏**）+ `scope.courseSpecs = [{title,kps:[字词×1,道理×1,句意白话×1]}]`（✅）；PASS=2 FAIL=0（闻闻无进行中计划）。
- **agent 侧冒烟 11/11 PASS**（临时孩子库覆盖三种 scope 形态）：新格式 → `论语学而篇第八章（字词×1、道理×2）；论语为政篇第一章（背诵×1）`；旧格式 `["课程名"]` 正常；固定档提示「按本周期必学课程的自动确定」；考核方法（只考/不考/背诵通过线）、说明、主题、已完成分数齐全；**输出无 `[object Object]`**。跑完删脚本与临时库，结果留 `tmp/smoke-104-result.txt`。
- 记录写进 `DEPLOY-201-server-0.4.1-迁移方案-2026-09-15.md` §11（含回滚四步）；`MEMORY.md` 已更新为 0.4.3。
- 未改客户端：老客户端连 0.4.3 即不再白屏；若要显示「每课知识点×题数」明细需发客户端 0.1.16（代码已就绪、构建通过，未打包发布）。
