# 2025-08-12 工作日志

## 代码审查：学习应用 MVP (Phase 1-8)

对全部 8 个 Phase 的代码进行了完整审查，对照 REQUIREMENTS.md 和 PLAN.md。

### 审查范围
- 12 个 Electron TS 文件、14 个前端 TSX 文件、5 个 Python 文件、4 个技能模板
- 覆盖：项目脚手架、云端服务、认证数据层、Pi 引擎、技能模板、定时任务、孩子前端、家长前端

### 已修复

**P0:**
1. ContentPanel.tsx 添加 DOMPurify sanitize
2. scheduler.ts cron 改为 "0 21 * * *"

**P1:**
3. Dashboard/ChildSelect useState -> useEffect
4. pi-session.ts 家长 session 缓存
5. ipc-handlers.ts 错误转发 + error 事件
6. preload.ts 事件清理机制

**P2:**
7. Dashboard 删除/重置密码 UI
8. Settings 默认模型选择
9. ProgressView 评估结果表格

**附加修复：**
- tsconfig.json 移除已废弃的 baseUrl，添加 lib: ["ES2022", "DOM"]（TS 7 兼容）
- electron-vite build 验证通过

## 测试验证

全量测试 29/29 通过（3 个测试文件）：
- 云端 API 测试：8/8（注册/登录/许可证/错误场景）
- 集成测试：4/4（数据目录/孩子初始化/Pi Session/云端认证）
- 功能验证：17/17（对照需求 §2-§12 及学习框架）

测试中发现的问题已修复：
- Pi session 测试超时 → timeout 30s
- 沙箱限制 rmSync → afterAll 跳过清理
- bcryptjs 3.0.3 $2b$ 前缀 → 测试匹配改为 $2

## Phase 9-10 实现

**Phase 9 云端同步:**
- cloud-service/app/sync.py: upload/download/status/upload-batch 4 端点
- electron/lib/sync-manager.ts: scanChildFiles/syncChild/syncAllChildren/pushChildChanges/fullSnapshot
- last-write-wins 冲突处理，排除 .pi 目录
- main.ts 启动同步，ipc-handlers 3 个 IPC，preload 暴露 API
- 测试: sync_api_test.py 8/8, sync.test.ts 8/8

**Phase 10 打包部署:**
- package.json electron-builder 配置 (NSIS/AppImage/dmg)
- cloud-service/Dockerfile + docker-compose.yml + .dockerignore
- JWT_SECRET 改为环境变量
- electron-builder 需手动 npm install

全量测试 37/37 通过 (5 文件), electron-vite build 成功

## 测试反馈修复

1. AI伙伴 emoji: ChildProfile 新增 aiEmoji 字段，AddChildModal 10 个 emoji 选项，ChatWindow AI 消息左侧显示
2. API key 保存: pi-runtime.ts setProviderApiKey 改为直接读写 auth.json，SDK 无 setApiKey 方法
3. ELECTRON_RUN_AS_NODE: WorkBuddy 终端继承该环境变量导致 electron 以 Node 模式运行，main.ts 顶部 delete 解决
4. 版本检测: cloud-service /api/version 端点 + main.ts 启动时检查并弹窗提示升级
5. dist:win 打包成功: 115MB 安装包，Electron 国内镜像 + signAndEditExecutable: false

6. win is not a function: attachSessionEvents(getMainWindow()) 多了一对括号导致事件回调崩溃
7. IPC 流式渲染失败 → 改为 pi:reply 一次性完整回复，后端提取 session.messages 最后 assistant 文本
8. 系统提示词从 systemPromptOverride → AGENTS.md 文件：Pi SDK 自动加载，buildAgentsMd+writeAgentsMd
9. 家长 Dashboard AGENTS.md 编辑器：孩子卡片按钮 → 等宽字体 textarea 弹窗
10. 密码输入 autoFocus，孩子登录体验优化
11. auth.json 格式修正：{ apiKey } → { type: "api_key", key }
12. ModelSelector 竞态修复：session 未就绪时 5 次重试
13. display_content 工具不可用：Pi SDK 的 tools 参数是白名单，customTools 会被 filter 掉。修复：tools 数组加 "display_content"
14. 左侧 HTML 交互：ContentPanel 从 DOMPurify+dangerouslySetInnerHTML 改为沙盒 iframe（sandbox="allow-scripts allow-modals allow-forms"），JS 可运行且不威胁主应用
