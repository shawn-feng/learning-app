# 2026-08-20 工作日志

## ISSUE-013 核查 + LEARNING-DATA-REDESIGN.md 实现度评估（讨论，未实施）

**redesign 落地 ~70%**：P0（taxonomy 20 标签四维）✅、P1（learning 结构 8 主题）✅、P2（AGENTS.md 导航，缺「记录打标签」段）⚠️、P3（recording 缺 inquiries/tasks 索引）⚠️、P4（study-tracker）✅、P5（残留未清 + tags 链接失效）⚠️、P6 未核。

**三个实现偏差**（关键）：
1. daily 是详式（### 课程名+字段，recording 定义）≠ 设计第九节行式简表——详式更符合 ISSUE-009「宁详勿略」，保留详式、修订文档。
2. outputs/ 未使用，产物散在孩子目录根（番茄钟.html 等）。
3. 知识点 tags 无结构化字段（进度文件无 tags），tags 倒排「关联知识点」链接是旧路径 learning/lunyu.md（已失效，应为 learning/lunyu/lunyu.md）。

**ISSUE-013 7 缺口方案**：①daily/index.md（recording 维护）；②kb_append 工具（主进程 IO，内容不进上下文）；③topics.md 删正文表格；④清 life-events/study-topics/study-rules 残留 + 修 tag 链接；⑤recording 补 inquiries/tasks 索引；⑥kb_read {kind,ref,section}；⑦AGENTS.md 复核 OK 无需改。

**待用户拍板**：daily 详式是否保留；实施范围 ①② vs ①②③。

## 数据结构定稿（2026-08-20，已更新 LEARNING-DATA-REDESIGN.md）
- **daily 详式定稿**：### 标题 + 字段，孩子表现/概要必填详写；文件名即日期 = 时间线索引，**不建 daily/index.md**。
- **分类索引各管一摊**：life/inquiries/tasks 月索引（指针→daily 区块）+ tags 倒排 + learning 进度；inquiries/tasks 索引待 recording 补维护。
- **知识点 tags**：创建知识点（新增进度条目）时一次性从 taxonomy 选定写入 tags 字段，**不归 recording 维护**（用户拍板）。
- **outputs/ 启用**：任务产物统一放 outputs/，现有 3 个 html 需移入（用户拍板）。
- **AGENTS.md 移出文档**：行为准则，模板维护在 pi-session.ts（用户拍板）。
- 文档第十节改为附注；P2 标注移出；P5 增加清残留/移产物/修 tag 失效链接；待确认事项更新。
- 下一步：讨论创建/维护数据结构的工具（kb_read/kb_append、recording 补索引）。

## 新建 LEARNING-DATA-SPEC.md（2026-08-20）
- 新文档 = 数据结构（定稿）+ 生命周期与维护流程 + AI 工具面（现状 + 规划），是现行唯一权威。
- 含：目录树、9 类文件 schema（daily 详式 4 区块/topics/rules/进度含 tags::/life/inquiries/tasks/taxonomy/tags 倒排/outputs）、4.1-4.8 生命周期表（知识点创建时定 tags→recording 更新→归档不删；life/qa append-only；tasks 产物归 outputs/；写者边界）、5.1 现状工具 4 个 / 5.2 规划 kb_read/kb_append、查询路径速查表。
- LEARNING-DATA-REDESIGN.md 头部加「现行权威见 SPEC」指向。
- **SPEC 第四节重构（用户反馈）**：生命周期改为**场景驱动的数据流转**（4.1 新增主题→4.9 标签/主题移除，每场景只讲数据先写哪个文件、写什么、再流到哪个索引，不掺工具）；第五节改名「维护方式与 AI 工具面」：5.1 维护职责矩阵（谁建/谁更/不可改 + 写者边界）、5.2 现状工具、5.3 规划工具。

## 工具面定稿（2026-08-20，已写入 SPEC 5.3-5.5 + ISSUE-013 定稿）
- **kb_read/kb_patch/kb_append 定稿**：共用定位器 {file,block?,item?,field?} + kb-parser.ts（纯函数，支持 `- 键：值` 与 `键:: 值` 两种字段格式）。kb_read 支持 listOnly + month 聚合（按需生成，替代 daily/index.md——**已推翻 index.md 方案**）；kb_patch 定位更新（无需知道当前值、内容不进上下文、frontmatter 用 frontmatter:key）；kb_append 区块尾追加。
- **schema 约束四层保障（L1-L4）**：L1 数据/内容文件分流，数据文件写入走 kb 工具收口；L2 字段白名单 kb-schema.ts 单一真源（工具与 lint 共享）；L3 AGENTS.md+recording 行为约束；L4 lint 定时校验。
- **lint 定稿（L4）**：确定性脚本 scripts/kb-lint.mjs + electron/lib/kb-lint.ts；app 启动时 + 运行期间每 24h；只报告不修改；报告落 data/children/{childId}/lint-report.md（多孩子隔离）；校验：目录结构/daily 文件名/字段白名单/格式一致性/取值约束/索引指针有效/frontmatter 可解析。
- **已知限制**：SDK write/edit 无法按路径禁止（内容文件需要），L1 依赖 L3+L4 兜底。
- **ISSUE-013 更新为「已梳理定稿」**：不建 daily/index.md（改 month 聚合）；实施清单 Phase 1 结构迁移（删残留/去重/移产物/修 tag 链接/统一 life 格式）→ Phase 2 工具实现（kb-parser/kb-schema/custom-tools+白名单+单测）→ Phase 3 规范（recording 补索引+AGENTS 约束）→ Phase 4 lint+token 量化。

## 指针规范确认（2026-08-20）
- 指针格式 = `daily/{日期}.md#区块`（文件 + markdown 原生锚点，Obsidian 友好），粒度到**区块**；条目级靠**同名约定**（索引条目标题 == daily `###` 标题）。
- kb_read 支持 `ref` 简写（`daily/2026-08-13.md#生活` → {file,block}）；lint 指针校验三级：文件存在 → 区块存在 → **同标题条目存在**。
- 进度文件**不加**指向 daily 的指针（B 方案：进度管进度、复习次数承载频次；反查走 daily 文件名+课程名字段）。
- 实施清单更新为 Phase 0-4（Phase 0 = SPEC 补指针规范/ref 简写/lint 三级校验 + 确认 B 方案）。

## ISSUE-013 Phase 0-4 实施完成（2026-08-20）
- **实测关键发现**：①实际 daily 字段格式是 `- **键：** 值`（加粗），parseFieldLine 需剥星号；②4-6 月 daily 是散文旧格式（大量「句子开头」误判为字段），7 月起才是 ### 条目+字段；③recording 实际字段多样（知识点/汉字/原文等）→ 白名单扩展 + 字段违规降为 **warning**（error 只保留结构/指针/取值/frontmatter）。
- **lint 实测**：主账号 error=0 / warning=1436（历史基线）；跳过无 profile.json 的 ~150 个测试残留目录；daily 内容型附加区块（评估）不检查。
- **同步修复**：sync.test.ts 引用已删 study-rules.md → 改 learning/rules.md（2 处），8/8 过。
- **全量测试**：110 过 / 5 失败（全部既有环境性：app.test 云端 ECONNREFUSED、functional app.isPackaged、learning-summary 真实数据漂移 ×2、voice-audio 超时），无本次引入的新失败。
- **遗留**：①历史 warning 1436 基线不迁移；②study-tracker 评估文案引用已删 study-rules.md 需改；③测试残留目录独立处理。

## 珊珊会话实测修复（2026-08-20 下午）
- **实测发现 3 个真实 bug**（会话 jsonl 分析）：
  1. kb_patch 拒绝「首次学习」——PROGRESS_FIELDS 白名单缺实际字段 → 扩展加 首次学习/上次复习。
  2. **kb_patch 对进度文件「无法定位区块」**（致命）：lunyu.md 正文无 ## 区块，kb_patch 强制找区块 → 修复：无区块时把整个正文当隐式区块切条目。
  3. **kb_append 不支持创建新文件**：当日 daily 不存在 → agent fallback 用 write 裸写 → 修复：kb_append 文件不存在自动创建（daily 建 `# 日期` + `## 区块` 头）。
- 另修：kb_patch **字段不存在时自动追加到条目尾**（recording 真实场景：新学一课补掌握度/首次学习等原没有的字段），格式跟随条目已有 sep。
- AGENTS.md 指引补「新建 daily 用 kb_append（自动创建）、更新字段用 kb_patch（自动追加）」+ regenerate。
- 验证：kb 单测 33/33 过；tsc 0 业务错误；build 过；lint warning 1436→1192（白名单扩展）、error 0。
- daily/2026-08-20.md 实测写入格式：字段合法、结构正确，但缺「课程名」字段（recording 模板要求，agent 未写，轻微）。

## method 记录段改 kb 工具 + 进度文件合规确认（2026-08-20 下午）
- 8 个 method.md（english/hanzigong/lunyu/qianziwen/reading/taodi/xiaojing/xiaozhuan）的「### 记录」段改为 kb 工具指引：更新进度用 kb_patch（条目字段 + frontmatter:learned/next/updated，字段缺失自动追加）、写 daily 用 kb_append（文件不存在自动创建）。脚本 scripts/update-method-recording.mjs（幂等可复用）。
- DAILY_FIELDS.学习 补 method 模板字段：单元/生字/拼音考核（kb_append 校验不误拒）。
- **进度文件确认全部符合结构**：lint 进度文件 0 违规（白名单扩展后首次学习/上次复习合法；格式本就是 `键:: 值`）；总违规 = daily warning 1192（历史基线），error 0。
- 验证：kb 单测 33/33、tsc 0、build 过。

## kb_append 语义修正（2026-08-20 下午，用户澄清）
- **kb_append 不校验字段名**：content 是 method 流程已输出给孩子的学习总结原文（### 课程名 + 原字段行），工具只校验结构（### 开头、区块合法），原样追加；字段质量由 method 输出流程保证、lint 兜底。
- 实现：kb_append 移除字段白名单校验（保留 block 合法 + isItemChunk）；SPEC 5.3/5.4 更新；recording SKILL.md 第 7 节更新；单测改为「不校验字段名：可直接追加总结原文」。
- kb_patch 保留字段白名单校验（更新场景字段应精确）。
- 验证：33/33、tsc 0、build 过。

## kb_append 创建语义确认 + 补缺口（2026-08-20 下午）
- 文件不存在 → 创建文件 + 只写本次的 `# 日期` + `## 区块` + content（**非完整 4 区块框架**，按需创建）。
- **补缺口**：文件存在但目标区块不存在（如当天先写「学习」再追加「生活」）→ 原报错，现自动在文件尾创建新区块（`## 区块` + content），原区块保留。单测覆盖（15/15 kb-tools）。
