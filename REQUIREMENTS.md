# 学习应用需求文档

> 最后更新：2026-08-11

---

## 一、产品形态

Electron 桌面应用（React 渲染层），运行在用户本地 Windows 设备。连接公网认证服务（Python + FastAPI + SQLite，部署阿里云 ECS，研发阶段本地部署）。认证后可离线使用。支持同一家长账号多设备安装。

## 二、认证体系

### 家长 -> 公网服务

- 家长账号在云端注册/登录
- 返回：允许的孩子数量、可用功能、订阅有效期
- 许可证缓存在本地，支持离线使用
- 订阅到期后需联网重新认证

### 孩子 -> 本地 App

- 每个孩子有独立的本地密码（仅存本地，不上传云端）
- 孩子选择头像 + 输入密码进入学习界面

## 三、角色与功能

### 家长

- 管理孩子（添加/删除/重置密码/配置 AI 名字与性格/填写孩子基本情况）
- 导入预制 Skill 包（从本地文件导入到共享技能目录，所有孩子可用）
- 使用专用 AI（聊天界面 + 可视化编辑器）创建和调整主题教学技能，可编辑整个技能目录的所有内容（SKILL.md、教学资料、脚本等）
- 配置模型和 API key（可配多个，所有孩子共用）
- 设置默认模型
- 查看每个孩子的学习情况（进度、日志、评估结果）
- 管理云端同步/备份

### 孩子

- 在 AI 引导下学习，AI 是拟人化的伙伴（有名字、有性格、知道孩子的名字和基本情况）
- 可自行切换模型（家长设默认，孩子可改选）
- 每个孩子独立的学习数据、进度、会话
- 共享家长导入的技能和模型配置

## 四、孩子的 AI 伙伴

- **拟人化身份**：AI 有自己的名字和性格（家长创建孩子时配置）
- **了解孩子**：AI 知道孩子的名字、年龄、年级、兴趣等基本情况（家长填写）
- **工作空间**：AI 的工作空间 = 孩子的整个数据目录，AI 只能读写该目录内的内容，不能访问其他孩子的数据或系统文件
- **个性化系统提示**：每个孩子的 Pi 会话使用定制系统提示，注入 AI 名字、孩子信息、性格设定、学习框架指令

## 五、技能体系

### 统一共享技能目录

所有技能放在 `shared/skills/`，包括：

- 基础技能：recording / study-tracker / learning-topic-setup
- 家长导入的预制 skill 包
- 家长 AI 创建的主题教学技能
- learning-topic-setup 创建的新主题教学技能

每个孩子的 session 从共享技能目录加载技能，从自己的数据目录加载进度文件。

### 技能相关流程

- 家长导入预制 skill 包 -> 安装到 `shared/skills/` -> 所有孩子可用
- 家长用 AI 创建/编辑技能 -> 写入 `shared/skills/` -> 可视化编辑器可编辑整个技能目录
- 孩子触发 learning-topic-setup -> 在 `shared/skills/` 创建新技能 + 在孩子自己的数据目录注册主题（创建进度文件、规则）
- 在线 skill 市场后续迭代

## 六、模型配置

- 家长配置一个或多个模型（提供商 + API key + 模型 ID），存共享 `auth.json`
- 所有孩子共用模型池
- 家长设默认模型，孩子在学习时可自行切换
- Pi ModelRuntime 共享，AgentSession 按孩子隔离

## 七、Pi 会话类型

| 会话类型 | 操作者 | cwd | 用途 |
|---------|--------|-----|------|
| 孩子学习会话 | 孩子 | `children/{childId}/` | AI 伙伴引导学习 |
| 家长技能编辑会话 | 家长 | `shared/skills/` | AI 辅助创建/调整技能 |
| 定时任务会话 | 系统自动 | `children/{childId}/` | recording / study-tracker |

所有会话共享同一个 ModelRuntime（一个 auth.json）。

## 八、学习界面

### 布局：聊天 + 内容面板（方案 B）

```
┌─────────────────────────────────────────┐
│              顶部：AI 伙伴名字 + 头像       │
├─────────────────────┬───────────────────┤
│                     │                   │
│   内容面板            │   对话区           │
│   (Markdown/HTML)    │   (聊天消息流)      │
│                     │                   │
│  AI 推送的教学内容     │  互动、提问、考核    │
│                     │                   │
├─────────────────────┴───────────────────┤
│  [输入框]                          [发送]  │
└─────────────────────────────────────────┘
```

- **内容面板**：AI 在教学过程中将课文原文、图片、知识点卡片等内容推送到面板展示
- **对话区**：AI 与孩子互动引导（朗读、讲解、提问、考核、鼓励）
- 内容面板支持 **Markdown 和 HTML** 两种格式渲染
- HTML 内容经 DOMPurify 消毒后渲染

### 家长技能编辑界面

- 聊天区：家长描述需求，AI 在共享技能目录创建/修改文件
- 可视化编辑器：文件树 + Markdown 编辑器 + 预览，可编辑整个技能目录

## 九、定时任务

- App 运行时才执行
- 如果到点 App 未运行，下次启动时补执行
- 需记录每个孩子每个任务的上次执行时间（`task-state.json`），用于判断是否需要补执行
- recording：定时从会话提取学习总结，写入进度/日志/详情/生活事件
- study-tracker：定时评估当日完成情况

## 十、云端同步/备份

- 多设备安装场景，需要双向同步（非纯备份）
- 孩子学习数据同步到云端
- 多设备间的数据冲突处理：按时间戳 last-write-wins per file

## 十一、学习框架

遵循 `LEARNING-FRAMEWORK.md` 描述的框架：

1. **用户主动学习某主题时**，AI 调用该主题的主题教学技能 -> 读取进度文件确定下一课 -> 教学 + 考核 -> 在会话中输出学习总结
2. **定时触发学习记录技能**，从会话提取学习总结 -> 写入每日日志、进度文件、详情文件；提取生活事件 -> 写入生活事件记录
3. **定时触发学习评估技能**，读取每日目标量和进度文件 -> 评估当日完成情况

### 四种技能

- **主题教学技能**（如 guoxue-learner）：负责特定主题的教学引导、出题考核、输出学习总结
- **学习记录技能**（recording）：从会话提取信息，统一写入各类记录文件
- **学习评估技能**（study-tracker）：评估当日必学内容完成情况
- **学习主题注册技能**（learning-topic-setup）：注册新主题时自动创建教学技能、文件结构、规则

### 文件格式

- `study-topics.md`：主题->技能->文件映射（YAML frontmatter + markdown 表格）
- `{topic}.md`：主题进度+详情（frontmatter 存汇总，`###` 条目存各课程状态）
- `study-rules.md`：每日目标量（YAML frontmatter）
- `daily-logs/YYYY-MM-DD.md`：每日学习日志
- `life-events.md`：生活事件记录

## 十二、数据架构

```
本地设备
├── app-data/
│   ├── license.json                 # 缓存的许可证（离线用）
│   ├── app-config.json              # 模型池配置
│   ├── task-state.json              # 定时任务执行状态（per-child per-task）
│   ├── shared/
│   │   ├── auth.json                # 共享模型凭证
│   │   └── skills/                  # 统一共享技能目录
│   │       ├── recording/
│   │       ├── study-tracker/
│   │       ├── learning-topic-setup/
│   │       ├── guoxue-learner/      # 家长导入或AI创建的
│   │       └── ...
│   ├── children/
│   │   └── {childId}/               # = 该孩子 AI 的工作空间
│   │       ├── profile.json         # 名字、头像、本地密码hash、AI名字、AI性格、基本情况
│   │       ├── .pi/
│   │       │   ├── agent/
│   │       │   │   └── sessions/
│   │       │   └── settings.json    # 指向共享技能目录
│   │       ├── study-topics.md
│   │       ├── study-rules.md
│   │       ├── {topic}.md
│   │       ├── daily-logs/
│   │       └── life-events.md
│   └── parent-workspace/            # 家长AI工作空间（技能编辑草稿等）

云端服务 (Python + FastAPI + SQLite + 文件存储)
├── 家长账号数据
├── 订阅/许可证数据
├── 设备注册
└── 孩子数据同步存储
```

## 十三、技术约束

| 项 | 选型 |
|----|------|
| App 框架 | Electron + React |
| 后端服务 | Python + FastAPI + SQLite |
| AI 引擎 | Pi SDK（嵌入 Electron 主进程） |
| 本地存储 | 文件制（markdown + YAML frontmatter） |
| 前端 | React |
| 部署 | 研发本地，生产阿里云 ECS |
| 离线 | 支持离线使用 |
| 多设备 | 支持，需云端同步 |
| Skill 市场 | 后续迭代 |
