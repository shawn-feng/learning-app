# 需求盘点：三大需求场景 vs 已实现功能（2026-09-10）

> 目的：app 功能是边用边加的，本文把已实现功能对照三大需求场景做一次全面盘点，明确"实现了没有、缺什么"。
> 依据真源：`ARCHITECTURE.md`（功能架构）、`EXAM-ARCHITECTURE.md`（考核模块）、`.workbuddy/memory/` 工作日志；代码 grep 佐证（积分/奖励关键词零命中）。

---

## 0. 三大需求场景（用户定义）

1. **辅导孩子学习并记录生活**：课程设计、考核设计、生活记录。
2. **制定每日计划，监督完成**：学习计划、考核计划、每日生活安排，记录完成情况。
3. **对孩子的学习成果和生活表现进行奖励和惩罚**：根据计划完成情况、课程知识掌握情况奖罚。

---

## 1. 场景一：辅导孩子学习并记录生活

### 1.1 课程设计 —— ✅ 基本实现

| 能力 | 状态 | 实现落点 |
|---|---|---|
| 家长端课程/主题管理（家长库 topics/courses） | ✅ | `parent_lib` 双库、`CourseManager`/`TopicEditor`/`CourseDetail` |
| 家长 agent 创建/编辑课程与资料 | ✅ | `parent_course_save`/`parent_topic_save`/`parent_upload_material`/`create_html_lesson`（家长端已接入） |
| 学习资料呈现（html/音视频/图片） | ✅ | `MaterialsPanel` + asset doc 顶层文档渲染 + PiBridge 统一通讯 + 字号调节 |
| 场景角色扮演课程（场景英语） | ✅ | 独立 scene agent + PiBridge scene.* 命名空间（ISSUE-061） |
| 英语「学习+场景合一」课程改造（33集→9场景课） | 🟡 方案已定稿未实施 | `DESIGN-english-scene-courses-2026-09-10.md` |
| 课程学习状态/掌握度 | ✅ | `first_learned/last_review`、`course_status`、courseMastery（客户端聚合） |

**缺口**：英语场景课重排与孩子进度迁移（有方案，排期待定）；课程教学法的编辑体验依赖对话式（无结构化表单），可接受。

### 1.2 考核设计 —— ✅ 完整闭环（三大场景中最成熟）

| 能力 | 状态 | 实现落点 |
|---|---|---|
| 结构化题库（题干/答案/评分/题级行为/选择题选项/知识点概要） | ✅ | 三表：`question_bank`/`topic_categories`/`course_category_questions`（ISSUE-067） |
| 考核方法两层（主题级 perChild + 排期级本次覆盖） | ✅ | `topics.method_spec` + `scope.methodSpec` |
| 考核排期（固定档 daily/weekly + 自定义档） | ✅ | `exam_schedules`；月/半年/年档已下线 |
| 答题（背诵→发音评测、选择题→看选项口答、问答→ASR 口述） | ✅ | `ExamView` + iframe 答题模板 |
| 判分（选择题规则判分 0 LLM、背诵 recitePass、问答逐题小 prompt） | ✅ | `exam-engine.ts` + `assess-selection.ts` 结构化直出题 |
| 考核记录与溯源（家长查看） | ✅ | `ExamRecords`/`QuestionBankPanel`/`TopicDetail` 考核要点 tab，按 questionId/categoryId 溯源 |
| 审计 | ✅ | `exam-audit/{childId}/{日期}.jsonl` |

**缺口**：无大的功能缺口。可选增强：成绩趋势分析、错题本、按知识点的掌握度报告。

### 1.3 生活记录 —— 🟡 部分实现（有记录、缺展示）

| 能力 | 状态 | 实现落点 |
|---|---|---|
| 自动记录学习+生活事件 | ✅ | recording 定时任务（服务端 worker 每小时 ephemeral agent 会话，ISSUE-024），写每日记录 + 标签（taxonomy 受控词表 + 倒排索引） |
| 记录详细度（概要/考核/难点/错题/表现） | ✅ | recording skill 模板 2026-08-14 升级版 |
| 知识点↔生活事件标签关联 | 🟡 机制在、数据薄 | 倒排索引"关联生活事件"靠后续 recording 补充，存量覆盖率低 |
| **家长端浏览生活记录的界面（成长档案）** | ❌ 未实现 | 记录存于孩子 kb，无专门查看面板；`LearningDashboard` 是学习进度看板，不含生活记录浏览 |

**结论**：记录的"生产"已自动化，但"消费"（家长看孩子的成长档案/生活时间线）是空白。

---

## 2. 场景二：制定每日计划，监督完成

### 2.1 学习计划 —— ✅ 基本实现

| 能力 | 状态 | 实现落点 |
|---|---|---|
| 计划生成（对话生成 + 未完成顺延 carry + 物化今日 todo） | ✅ | `study_plan_items`（一课一行）+ worker `runPlanTick`（≤5min 反映家长改计划） |
| 双 todo 列表（家长派发 + 孩子自规划，互不干扰） | ✅ | `todo_items`（source=parent/child，孩子自规划项绝不动） |
| 完成态自动回写（学完课自动勾计划勾 todo） | ✅ | worker `runStatTick`（事件驱动，按 courses first_learned/last_review 判定） |
| 家长/孩子端计划界面 | ✅ | `StudyPlanPanel`/`TodoModal`/`/today`（服务端下发 done，家长面板以服务端为准） |
| 复习计划 | 🟡 已简化 | 判分侧复习计划已移除，改为家长 agent 按需提供（2026-09-09 决策） |

### 2.2 考核计划 —— ✅ 实现

| 能力 | 状态 | 实现落点 |
|---|---|---|
| 固定档（daily/weekly=周期内必学课全考） | ✅ | 内置规则不走 AI |
| 自定义档（家长 agent 精确排课 + 本次考核方法） | ✅ | `exam_schedule_create`（courses 必填，categories/recitePass 可选） |
| 孩子端考核入口（今天可参加/历史补考/查看成绩） | ✅ | `ExamView` 两区信息架构 |

### 2.3 每日生活安排 —— 🟡 部分实现（能凑合用，无专属设计）

| 能力 | 状态 | 说明 |
|---|---|---|
| 把生活事项放进每日 todo | 🟡 | todo_items 可承载任意条目（家长可加"整理书包/锻炼"等），但没有"生活安排"专属分类、周期规则（如每天固定作息项自动出现） |
| 作息/课程时间表 | 🟡 | scheduler 有课程时间表（模板+星期、上学日/周末，ISSUE-059），但那是**会话调度**（几点开新会话），不是生活安排管理 |
| 提醒 | ✅ | `MyRemindersModal` |

### 2.4 完成情况记录 —— ✅ 记录在、📊 报表薄

| 能力 | 状态 | 实现落点 |
|---|---|---|
| 逐项完成状态（status/done_at/note） | ✅ | todo_items + 服务端真源 |
| 汇总统计 | ✅ | `child_todo_stats`（由 todo_items 汇总） |
| **完成率/趋势报表（按周/月，面向家长）** | ❌ 薄 | Dashboard/LearningDashboard 有当前进度，但无历史完成率、连续天数、趋势图 |

**结论**：计划的下发、执行、勾选闭环完整；"监督"的报表维度（完成率、连续性、历史趋势）不足——而这恰好是场景三奖惩机制需要的输入。

---

## 3. 场景三：奖励和惩罚 —— ❌ 未实现（仅有设计稿）

| 能力 | 状态 | 说明 |
|---|---|---|
| 积分规则（待办完成率分档 / 考核得分分档 / 必做选做门控 / 扣分） | ❌ 代码零实现 | grep `积分|奖励|兑换|points|reward|redeem` 于 src/electron/server 无业务命中 |
| 积分流水与余额 | ❌ | 无 PointsLedger |
| 兑换（APP 内权益 + 家长申请审批） | ❌ | 无兑换目录、无申请流 |
| 设计文档 | 🟡 已有草案待拍板 | `DESIGN-reward-points-2026-09-10.md`（分档规则/数据模型/兑换双路/落地建议，含 3 个待确认项） |

**有利条件**：奖惩机制需要的两个数据输入**都已存在**——计划完成情况（todo_items/study_plan_items/child_todo_stats）和知识掌握情况（exam_attempts/courseMastery）。所以场景三的实施主要是新增积分域（配置/结算/流水/兑换），不需要动考核和计划的存量逻辑。

---

## 5. 三大场景间的数据交互现状（2026-09-10 代码核实）

> 状态依据代码核实：`server/src/worker/scheduler.ts`、`server/src/routes/exam.ts`（listCourseStatus / exam_mastery 回写）、`electron/lib/custom-tools.ts`（course_status 工具）、`electron/lib/exam-engine.ts`（reinforcePlan 已拆除）。

| # | 通道 | 方向 | 现状 | 实现机制 |
|---|---|---|---|---|
| ① | 学习进度 → 计划完成态 | 一→二 | ✅ 自动 | worker `runStatTick` 读孩子 kb `courses.first_learned/last_review` → 自动勾 `study_plan_items`/todo + 汇总 `child_todo_stats`（当天多次、事件驱动） |
| ② | 课程/考核综合状态 → 计划决策 | 一→二 | 🟡 agent 间接 | 服务端 `/courses/status/:childId` 一站式聚合（孩子 kb courses + `exam_attempts` 按课正确率/次数/最近时间 + 存量复习计划）→ 家长 agent `course_status` 工具 → **对话里**决定排什么课/考什么；无自动算法 |
| ③ | 计划完成率 → 积分 | 二→三 | ❌ 未建 | `todo_items`/`child_todo_stats`/`study_plan_items` 数据齐备，但无积分结算消费者；且 todo 缺「必做/选做」「计入积分范围」字段（积分设计的前置改造） |
| ④ | 考核成绩 → 积分 | 一→三 | ❌ 未建 | `exam_attempts`（score/per_question）可直接分档，未消费。**积分判分建议以服务端 exam_attempts 为唯一事实源**（见下方风险 1） |
| ⑤ | 学习计划 → 考核排期 | 二内部 | ✅ 自动 | fixed daily/weekly 档 = 学习计划周期内「必学」课全部考核，直读 `study_plan_items`，不走 AI |
| ⑥ | 考核结果 → 复习计划 | 一→二 | ❌ 已拆除（有意） | 2026-09-09 起 `reinforcePlan` 不再回写（exam-engine 判分返回空兼容）；`course_status` 的 `planReviewAt` 仅剩存量数据；复习建议改由家长 agent 按需生成 |
| ⑦ | 生活记录/生活表现 → 奖惩 | 一→三 | ❌ 未打通 | 生活记录为自由文本+标签（taxonomy），无结构化计分项；「生活表现奖惩」目前没有数据通道 |
| — | 考核结果 → 掌握度 | 一内部 | ✅ 自动 | 提交时服务端回写孩子 kb `courses.exam_mastery`，供课程状态/家长决策读取 |

### 数据交互层面的风险与注意

1. **掌握度三处并存**：`courses.mastery`（引导掌握度）/ `courses.exam_mastery`（考核回写）/ 客户端 courseMastery 聚合。后续积分与报表应统一以**服务端 `exam_attempts` + courses 表**为准，避免口径打架。
2. **考核聚合按课程名字符串匹配**（`per_question[].course` = 课程标题），课程改名会断链；积分/报表同样依赖此字段，需维持课程名一致性约束。
3. **场景三的前置数据改造**：积分"必做完成率"需要 `todo_items` 增加 `type(required/optional)` 与 `countInRate` 字段——这是场景二→三通道打通的第一步。
4. **②号通道是"半自动"**：数据聚合是全的，但"依据掌握度调整计划"目前完全依赖家长在对话里问 agent；要不要做自动化的"掌握度→建议计划"（如薄弱课自动进明日待办），是产品决策点。

---

## 6. 数据结构优化建议（按优先级）

> 原则：孩子进度/统计 → 孩子 kb；跨账号读写（家长审批）与计划类 → 服务端主库；迁移沿用项目幂等模式（ALTER + meta 标记，参考 migrateStudyPlanV2）。

### P0 —— 积分落地前置（必须改）

1. **`todo_items`（孩子 kb）加 3+1 个字段**：
   - `task_type` TEXT：`required`（必做）/ `optional`（选做），存量回填 `required`
   - `count_in_rate` INTEGER：是否计入完成率与积分（默认 1），排除纯提醒项
   - `points` INTEGER：选做项单件积分（空=取 RewardConfig 默认值）
   - `category` TEXT：`study` / `life`（学习/生活分类）——与 task_type 正交，顺手补上场景二"生活安排无分类"的缺口，避免二次迁移
2. **`child_todo_stats`（孩子 kb）扩展必做/选做维度**：现有 `date/total/done/parent_*/self_*/rate/streak` 结构健康（rate、streak 已有），追加 `required_total/required_done/optional_done/optional_points`。**必做完成率分档直接在此表算，取消原设计稿的独立 DailyTodoScore 表**（一表两用：家长报表 + 积分结算输入，避免两张表记同一事实）。
3. **积分域 4 张新表（服务端主库，与 study_plan_items 同库）**：
   - `reward_configs`：child_id 唯一；todo/考核分档阈值 JSON、optionalItemPoints、deductBelowBaseline（家长端可改）
   - `points_ledger`：**流水唯一真源**（child_id/ts/type earn|deduct|redeem/amount/source_ref/balance_after），余额由流水推导不存列，建 (child_id, ts) 索引
   - `redemption_items`：兑换目录（kind `inapp`|`parent`/cost/payload JSON 预留视频/游戏入口）
   - `redemption_requests`：家长兑换申请（status `pending`|`fulfilled`|`rejected`/parent_id/fulfilled_at，履约后才扣分）
   - 结算挂 worker：stat tick 末尾追加积分结算步，`worker_state` 加 `reward_settle` 游标去重（沿用 todo_stat 模式）

### P1 —— 已知断链的结构性修复（建议近期）

4. **课程引用 uuid 化**：`exam_attempts.per_question[].course` 与 `study_plan_items.course_name` 均存课程**标题字符串**，改名即断链（考核聚合、未来积分都依赖）。家长库 `courses.uuid` 已存在（考核三表在用）——建议 per_question 加 `courseId`、study_plan_items 加 `course_uuid`，写新读旧 + 存量按 title 回填一次。
5. **掌握度统一口径**（约定为主、动表为辅）：`courses.mastery`（引导）/ `courses.exam_mastery`（考核回写）/ 客户端 courseMastery 聚合三处并存。定死：**服务端 exam_attempts + courses.exam_mastery 为唯一事实源**，客户端聚合仅作展示缓存，不作为任何判分/积分输入。

### P2 —— 生活表现通道（产品决策后再动）

6. **生活表现参与奖惩走路线 A**：生活事项 = `todo_items` 里 `category='life'` 的条目（如"整理书包"=必做·生活），完整复用完成率/积分链路，**零新增表**。路线 B（给 `daily_entries` 加结构化评价字段）自由文本抽取不可靠，后置不做。
7. `daily_entries`（生活记录）保持自由文本+标签档案定位，不参与计分；家长端成长档案页是纯展示需求，无表改动。

---

## 7. 汇总

| 场景 | 完成度估计 | 一句话结论 |
|---|---|---|
| 一、辅导学习并记录生活 | **~85%** | 课程+考核双闭环成熟；生活记录"只产不显"，缺家长端成长档案 |
| 二、制定计划监督完成 | **~80%** | 学习/考核计划闭环完整；生活安排无专属设计，完成率报表薄 |
| 三、奖励和惩罚 | **~5%** | 仅有设计草案，代码零实现；但数据输入已齐备，实施成本低 |

### 建议的补齐优先级

1. **场景三·积分模块**（当前讨论热点，设计稿已就绪）：先拍板 3 个待确认项（低于基线扣分默认开关 / 进步奖与连续奖励是否二期 / 档名），再落地 RewardConfig + 每日结算 + PointsLedger + 兑换双路。
2. **场景二·完成率报表**：积分模块做"必做完成率"结算时顺手产出每日完成率数据，家长端补周/月报表，一举两得。
3. **场景一·生活记录家长端浏览**：成长档案页（时间线 + 标签筛选），数据已存在，纯展示工作。
4. **场景二·生活安排**：todo 增加"生活"分类 + 周期模板（每天固定项自动生成），优先级最低。
5. **场景一·英语场景课重排**：按 `DESIGN-english-scene-courses-2026-09-10.md` 择期实施。

---

## 8. 架构设想 vs 当前系统对照（2026-09-10，依据家长库/孩子库真实表结构核实）

> 用户的架构设想：三大块（基础数据=课程数据+考核数据/题库；拟定计划；按执行结果奖罚）+ 学习数据两级归类（学习主题→课程，课程=知识点集合，课程为最小学习/考核单位，不再往下归类）+ 课程五要素（主题归属/知识点集合/教学内容/教学方法/学习资料）。

### 8.1 三大块

| 用户设想 | 当前实现 | 状态 |
|---|---|---|
| 基础数据·课程数据 | 家长库 `topics` + `courses`（真源），孩子 kb 为分配副本 | ✅ |
| 基础数据·考核数据（题库） | 三表：`question_bank` / `topic_categories` / `course_category_questions` | ✅ |
| 拟定计划 | `study_plan_items` + `todo_items`（家长/孩子双列表）+ `exam_schedules` | ✅ |
| 按执行结果奖罚 | 无（仅 `DESIGN-reward-points` 设计稿；P0 数据结构清单见 §6） | ❌ 未建 |

### 8.2 学习数据两级归类

| 用户设想 | 当前实现 | 状态 |
|---|---|---|
| 学习主题（语文/阅读/论语/孝经…） | `topics`（name/topic_key） | ✅ 一致 |
| 课程 = 若干知识点的集合，**课程为最低归类层级** | `courses`（PK = topic+title），无更下层归类实体 | ✅ 一致 |
| 知识点（课程内，不单独管理） | **无独立实体**——由题库题目 + `knowledge_summary`（知识点概要文本）+ 考核类别隐式承载 | 🟡 隐式（见 8.4 决策点 1） |
| 每次学习针对一个课程 | `study_plan_items` 一课一行；完成态回写 courses | ✅ 一致 |
| 每次考核针对一个课程 | 按课抽题（course_category_questions），per_question.course 溯源 | ✅ 一致 |

### 8.3 课程五要素

| 用户设想 | 当前实现 | 状态 |
|---|---|---|
| 学习主题归属 | `courses.topic` 外键到 topics | ✅ |
| 知识点集合 | 题库题目（挂课+类别）+ knowledge_summary 文本 | 🟡 隐式 |
| 教学内容 | `courses.teaching_copy`（讲稿）+ `material` | ✅ |
| 教学方法 | **两级**：`topics.method`（主题级教学法，改一次全主题生效）+ `courses.lesson_method`（课级覆盖） | ✅（比设想更细） |
| 学习资料 | `courses.html_path` + materials 索引表（html/音视频/图片） | ✅ |

### 8.4 差异与决策点

1. **知识点隐式 vs 显式**：你设想"课程是知识点集合但不再往下归类管理"——当前实现正好如此（知识点以题目+概要文本隐式存在），**无需改造**。唯一代价：无法按知识点精确聚合掌握度（只能按课/考核类别）。将来若需要"按知识点看掌握度"，再考虑加知识点实体 + 题-知识点关联，现在不动。
2. **生活记录未归位**：三大块的基础数据你只列了课程+考核（学习相关），生活记录（孩子库 `daily_entries`，自由文本+标签）没进架构图。建议归入基础数据作"孩子成长档案"（只产不消，见 §1.3），不参与计划与奖惩。
3. **内容与进度分离**（设想未提、现状如此、建议保持）：基础数据真源在家长库（内容），孩子库只存副本+学习进度。⚠️ 遗留问题：家长库 `courses` 表也带 status/mastery/first_learned 等进度字段，属旧架构残留（进度真源在孩子 kb），可列入清理项。
4. **考核方法比设想多一层**：现状是"主题级 method_spec（按孩子）+ 排期级本次覆盖"两层，均服务于题库抽题——与设想不冲突，是考核数据块的内部细化。
5. **奖罚块未建**：三大块中唯一空白，数据结构清单见 §6 P0，输入数据（完成率/考核成绩）已就绪。

### 8.5 结论

**架构设想与当前系统在结构上高度吻合，不需要推翻性重构。** 两级归类、课程最小单位、课程五要素全部有落点；唯一结构性缺口是奖罚块（§6 P0 已给清单）+ 生活记录的架构归位（决策点 2）+ 家长库课程表进度残留清理（决策点 3）。

---

## 9. 表结构评估与改造清单（2026-09-10 三项拍板后）

> 拍板：①知识点建实体，题目关联到课内知识点；②生活记录维持 `daily_entries` 现状；③清理家长库课程表进度残留。以下为逐项评估（schema 均已按代码核实）。

### 9.1 决策一：知识点实体 —— 当前不符，需新增

**现状**：无知识点实体。最接近的承载是 `question_bank.knowledge_summary`（自由文本概要，2026-09-10 加，供检索）；题目挂课走 `course_category_questions(course_id, category_id, question_id)`，且架构约定"**题目本身无课程语义，含义由挂到哪个(课,类别)决定**，可跨课复用"。

**评估**：知识点必须建表，且关联落点应放在**挂载关系上**而非题目上——这样与现有"语义随挂载"的约定一致（同一题挂到不同课时可关联各自课的知识点）。

**改造清单**（家长库，沿用 ensureAssessContentSchema 幂等迁移）：

```sql
-- 新表：知识点（挂在课下）
CREATE TABLE IF NOT EXISTS knowledge_points (
  id          TEXT PRIMARY KEY,
  course_uuid TEXT NOT NULL,            -- 外键 courses.uuid（关系表同款）
  name        TEXT NOT NULL,
  seq         INTEGER NOT NULL DEFAULT 0,
  UNIQUE (course_uuid, name)
);

-- 挂载关系加一列：该(课,类别,题)挂载关联的知识点
ALTER TABLE course_category_questions ADD COLUMN knowledge_point_id TEXT;
```

**连带改动**：
1. `assess_content_save`（家长 agent 工具）每项可带 `knowledgePointName/id`，保存时 upsert knowledge_points 并回填关系列；`assess_course_get` / `course_status` 输出带知识点。
2. `attachStructuredQuestions` 下发的题面元数据带 `knowledgePointId`，经宿主 questionMetaRef 回填——提交时写入 `exam_attempts.per_question[].knowledgePointId`（现阶段只落库溯源，掌握度按知识点聚合留到需要时再做）。
3. UI：TopicDetail 考核要点 tab / QuestionBankPanel 展示知识点列。
4. 存量迁移（可选、一次性）：按课取其题目非空 `knowledge_summary` 去重生成知识点，关系列按文本精确匹配回填；匹配不上的留空，后续由家长 agent 补挂。
5. `EXAM-ARCHITECTURE.md` §3 数据模型表补知识点表（实施时同步）。

### 9.2 决策二：生活记录维持现状 —— 已符合，零改动

`daily_entries`（孩子 kb）：`date/block/title/raw/tags`，PK(date, block, title)，按日期/分块建索引。就是"每日汇总日志表"，与你的认知一致，**不改**。它继续作纯档案，不参与计划与积分（生活表现奖惩走 todo `category='life'` 路线，见 §6 P2）。

### 9.3 决策三：清理家长库进度残留 —— 确认是死数据，可安全清

**现状核实**：家长库 `courses` 的 `status/mastery/first_learned/last_review/review_count` 五个进度字段：
- **无任何写入方**：学习进度唯一写入路径是 worker stat tick 与考核回写，全部落在**孩子 kb**；家长库 upsert 只是客户端编辑课程内容时把读到的旧值原样回写（回显），从不产生新进度。
- **有一个读方**：家长库 `topic_progress` 视图（聚合上述字段）→ `parent_lib.progress.list` → `listParentTopics` → 家长页主题列表的 learned/total。**但拆分架构后这是语义错误**：进度是按孩子的（在孩子 kb），家长库是全孩子共享的内容库——这个列显示的是永远不更新的死值。

**改造清单**：
1. `parent-lib.ts`：建表语句去掉五个进度列；`PARENT_SCHEMA_VIEWS` 删除 `topic_progress` 视图（视图依赖这些列，须先删）；加幂等迁移（`DROP VIEW IF EXISTS topic_progress` + `ALTER TABLE courses DROP COLUMN` 逐列、容错列不存在）。
2. `db.ts`：`parent_lib.courses.list` / `parent_lib.courses.upsert` 去掉进度字段；删除 `parent_lib.progress.list` handler。
3. `parent-library.ts` `listParentTopics`：去掉 `parent_lib.progress.list` 调用。家长页主题列表的进度列二选一：**删掉**（推荐——主题列表本就跨孩子）；或改为按当前选中孩子读 `kb.progress.list`。
4. 分配快照链路（家长库→孩子 kb 拷贝骨架）不受影响（进度列本来就是默认值）。

### 9.4 汇总判定

| 决策 | 现状符合度 | 需动结构 |
|---|---|---|
| 知识点实体+题目关联 | ❌ 无实体 | 新表 knowledge_points + ccq 加 1 列 + 工具/下发/落库 3 处连带 |
| 生活记录维持现状 | ✅ 符合 | 无 |
| 清理进度残留 | ❌ 死数据且视图语义错误 | 家长库 5 列 + 视图 + 1 handler + 1 客户端调用 |

三项均不动孩子 kb 与服务端主库的现有表，家长库改动全部可幂等迁移（沿用 ensureAssessContentSchema 模式）。

---

## 10. 计划域数据结构重构提案评估（2026-09-10，用户提案 vs 现状）

> 用户提案：三张计划表（学习/考核/生活，统一字段形态：uuid/开始时间/计划完成时间/制定人/状态/结果）+ 考核计划课程表（计划uuid/课程uuid/知识点uuid/题目uuid/得分）+ 排期表（自动排期用，按新结构调整）。
> 现状：`study_plan_items`（按日期一课一行，无计划实体）+ `exam_schedules`（scope JSON）+ `todo_items`（统一当日执行，source 分家长/孩子）+ `child_todo_stats`；考核内容存 `exam_attempts.per_question` JSON blob；生活计划无专属表。

### 10.1 逐表评估

**学习计划表（提案 vs study_plan_items）**

| 维度 | 评估 |
|---|---|
| ✅ 计划实体化（uuid 主键） | 解决现状"计划=行集合、无表头、一次计划无对象"；"制定人（孩子/家长）"比 todo 的 source 语义更准（制定 vs 执行） |
| ✅ 课程 uuid | 正好落地 §6 P1 的 uuid 化改造，消除断链 |
| ✅ 开始/计划完成时间（窗口） | 现状单日粒度，没有"一个计划覆盖几天"的表达；窗口支持"本周内完成" |
| ⚠️ 每日重复表达丢失 | 现状按日期一行，天然支持"每天出现今日计划+未完成顺延（carry）"；实体化后"每日重复"必须由排期表展开承担。计划=意图、排期=展开日程、todo=当日执行——三层模型比现状多一层 |

**学习结果写到计划表还是留 daily（用户明确提问）**

结论：**计划表只存轻量执行结果（状态 + 结果引用），过程记录留 daily，掌握度留课程/考核域——不抄写**。理由：
1. 单一事实源：学习过程（难点/错题/表现）天然在 `daily_entries`，计划表重复存会双写不一致；
2. 用途不同：计划表的"结果"是执行状态（完成/未完成/一句话摘要），daily 是过程档案；积分结算与家长报表只需前者可 SQL 直查；
3. 掌握度/成绩继续以 `courses`/`exam_attempts` 为唯一事实源（§5 风险 1 口径延续）。
落法：`result` 存短摘要或 JSON `{ref: daily_entry_key}` 引用；`status` 仍由 stat tick 回写（现有逻辑不变）。

**考核计划表 + 考核计划课程表**

| 维度 | 评估 |
|---|---|
| ✅ 考核内容结构化成表 | 现状 per_question 是 JSON blob（不可 SQL 查询/聚合）；结构化后**按知识点聚合掌握度**（刚落地的知识点实体）可直接 SQL，家长报表/错题本受益 |
| ⚠️ 题目固化时机（关键取舍） | 现状"开考时按孩子方法从例题池随机抽题"——防背题、方法可按孩子调。若计划时就定死题目 uuid，随机抽题失效。**建议：计划期只存 课程uuid+知识点uuid（考什么范围），题目uuid 开考抽题后回填**——表结构不变，语义从"计划内容"变"计划范围+实际内容"，两全 |
| ⚠️ 得分存计划课程表 | 计划表与结果表合一，已完成/未完成混表。可接受（简化家长心智），但建议同时存 `attempt_id` 引用，综合得分以 attempt 为准、表内得分是回填快照，避免双真源 |
| ✅ 综合得分放考核计划表 | 同上：回填快照 + attempt_id 引用 |

**生活计划表**

- ✅ 补上场景二"生活安排无专属设计"的缺口；字段与另两表同构，一致性好。
- ⚠️ 与 `todo_items` 的关系要定：**保留 todo_items 作为统一"当日执行视图"**，三计划的排期都物化进去（source 扩展为 plan_type+plan_id）——孩子自规划事项仍有落点，勾选/提醒/统计链路全部复用，不推翻。

**排期表（自动排期）**

- ✅ 把现状 gen tick"计划→todo 直接物化"的隐式步骤显式化：排期可预览（家长看未来日程）、可手工调整、carry 顺延简化（排期行顺延，不再直接动 plan）。
- ⚠️ 代价：执行链变长（plan → schedule → todo → stat 回写），worker 三件套（gen/stat/carry）全部重构，`/today`、家长面板、agent 工具（todo_list/study_plan_update）连带改。

### 10.2 总体判断

**方向正确，与既有演进同向**：计划实体化、课程 uuid 化、考核内容结构化、生活计划补位——每条都在解决已登记缺口（§6 P1、§8 决策点）。**唯一方向性风险是"题目计划期固化"**，用"计划存范围、开考回填题目"化解。**主要成本是执行链重构**：这套提案实际上要动 worker 管线和 todo_items 语义，不是加表就完事，建议渐进迁移。

### 10.3 建议的目标模型与迁移路径

目标模型（三层）：

```
计划层（意图）   study_plans / exam_plans / life_plans（uuid、窗口、制定人、状态、result 轻量引用）
                  exam_plan_courses（计划=课程+知识点范围；开考回填题目uuid/得分/attempt_id）
排期层（展开）   schedules：plan_id → 具体日期行（自动排期规则展开；可预览/可调/可顺延）
执行层（当日）   todo_items（保留，source→plan_type+plan_id）+ child_todo_stats（加必做/选做维度，积分用）
```

渐进路径（每步独立可用）：
1. **第一步（不动结构）**：`study_plan_items`/`exam_schedules` 补 course_uuid（P1 uuid 化）——新结构的前置，先做不亏。
2. **第二步**：建生活计划表 + todo_items 加 category/task_type/count_in_rate/points（积分 P0）——生活计划可先以"todo category='life'"跑起来，实体表随后。
3. **第三步（大改）**：计划实体化 + 排期表 + worker 管线重构，一次切换；考核计划课程表按"范围计划、开考回填"实现。

> 拍板待定：①三张独立计划表 vs 一张 plans 主表+类型明细（前者直观；后者跨类型报表/积分聚合更干净——积分要跨学习+生活算完成率，倾向后者）；②旧 study_plan_items 历史数据是否迁移进新表。

> **2026-09-10 拍板（已定）**：①**三张独立表**（学习/考核/生活）；②**历史数据迁进新表**。以下 §11 为随之而来的三个具体问题的评估结论。

---

## 11. 掌握度视图化 / daily 关联 / todolist 动态化 评估（2026-09-10）

### 11.1 掌握度是否还需要留在 courses/exam_attempts —— 列可去、表不可全去

**可以去掉的**：`courses.exam_mastery`（考核回写列）与 `courses.mastery`（引导掌握度列）——改为**视图动态计算**，一次性消灭"掌握度三处并存"（§5 风险 1）。
- **考核掌握度**：视图取该课**最近一次考核**的得分率（来源=新结构"考核计划课程表"，按 course_uuid 取最新 submitted_at）。
- **引导掌握度（学习侧）**：现状由孩子 agent 在学习中写 `courses.mastery`。纯视图化后它没有载体——**建议并入"学习计划表.result"**（每次学习一条：完成状态+学习表现/掌握情况备注），视图取最近一条。这样"最近一次结果为准"就有两个来源：学习(result) 与 考核(得分)。

**不建议合并成一个数**：学习 result 是**执行/学习表现**（完成、会了/不熟），考核 result 才是**掌握度数值**。若合成单一数值，"学完一次"会把掌握度刷成"完成"。视图建议**分列暴露**（`lastLearnedAt/learnResult` 与 `lastExamAt/examRate`，各带 source 与时间），前端按需展示"最近一次"，而不是把两者压成一个分。

**不能删的**：`exam_attempts` 的**历史明细**——但它的角色被新结构接管：新"考核计划表"= 场次头（uuid/状态/综合得分），新"考核计划课程表"= 逐题明细（题目uuid+得分）。两张表合起来覆盖 `exam_attempts` + `per_question`，因此 `exam_attempts` 可在迁移后**退场**（历史数据迁入新表，见 §10 拍板②）。

**注意**：视图 latest-per-course 需索引 `(course_uuid, submitted_at)`；家长端列表调用频繁但单家长数据量小，可接受。

### 11.2 学习计划表要不要关联 daily 条目 id —— 不要正向存 id，改反向归属

- `daily_entries` 主键是 `(date, block, title)`（无自增 id），且**一次计划 → 多条 daily 条目**（一对多）；在计划表里存 id 数组会变形、易不一致。
- 结论：**`daily_entries` 加 `plan_id` 列反向归属**（一对多天然），计划表 `result` 只存人可读摘要。
- **plan_id 由谁写**：不要交给 recording agent（LLM 写文本的环节不该产出 ID 关联，易错）。建议**事后匹配回填**——stat/新任务按 `(date, 课程/主题)` 匹配当次计划行写回 `daily_entries.plan_id`，零改 recording。
- 收益：计划→过程记录（`WHERE plan_id=?`）与 记录→计划 双向可查，且过程详情仍以 daily 为唯一真源。

### 11.3 todolist 动态化后，哪些定时任务要改 —— recording 不用改，改的是 gen/stat

**核实结论（代码级）**：`runRecordingServer`（tasks.ts ~150-215）只读 `kb.progress.list` + `kb.daily_entries.queryByDate` + 主题/标签 + 当天对话，然后写 daily——**全程不碰 todo_items**。所以 todolist 改成"动态从三张计划表取"后，**recording 本体不需要改**。

要改的是另外两个任务：
| 任务 | 现状职责 | 新结构下的改法 |
|---|---|---|
| `todo_gen`（runTodoGenServer） | 以 study_plan_items 当日排期为真源，物化 todo_items（source=parent, plan_id） | **取消物化**或改为"排期展开到三张计划表"（若采纳排期表）；todolist 不再落表 |
| `todo_stat`（runTodoStatServer） | 勾 todo_items + 汇总 child_todo_stats | **重写**：直接改三张计划表的 `status/done_at/result`；统计改从三张表汇总（必做/选做维度随之调整，积分用） |
| `recording` | 写 daily（过程档案） | **不改**；仅新增"事后回填 daily.plan_id"的小步（可由 stat 兼任） |
| `carry`（顺延） | 顺延 study_plan_items 未完成行 | 改为**顺延三张计划表**中未完成的计划行（或排期表的排期行） |

**附带影响**：`kb.todo.*` 系列 op（list/add/set/remove/addParent/removeByPlan）、`/today`、孩子端勾选 UI、agent 工具 `todo_list` 全部要改为面向三计划表读写——**孩子自规划事项要有落点**：建议仍保留一个轻量"自规划"入口（可放进生活计划表，制定人=孩子），避免这个能力丢失。

### 11.4 结论与改造顺序（更新版）

1. 先做 **course_uuid 回填**（P1，§6）——三表都以课程 uuid 为键，必须前置。
2. 建三张计划表 + 排期表 + 历史数据迁移（study_plan_items/exam_schedules/exam_attempts+per_question → 新表）。
3. 重写 worker：gen 取消/改排期、stat 改三表、carry 改三表；daily 加 `plan_id` 并事后回填。
4. 掌握度列下线 → 建 latest-per-course 视图（学习 result + 考核得分分列）；`exam_attempts` 迁移后退场。
5. 前端/agent 工具切换到三表；保留"孩子自规划"入口（生活计划表 制定人=孩子）。
6. 积分模块在此结构上落地（todo 概念被三计划表取代后，P0 的 todo 字段改造改为在三张计划表上做：task_type/count_in_rate/points/category）。

---

## 12. 掌握度定案 + 生活计划完成判定（2026-09-10）

### 12.1 定案：掌握度只取最近一次考核；学习状态只查最近学习时间

视图设计收敛为两列，语义互不混淆：

| 视图列 | 含义 | 来源 |
|---|---|---|
| `lastExamRate` / `lastExamAt` | **掌握度** = 最近一次考核的得分率 | 考核计划课程表（按 course_uuid 取最新 submitted_at） |
| `lastLearnedAt` | **学习状态** = 最近学习时间（只报时间，不给掌握判断） | 学习计划表（最近一条学习类计划的完成时间） |

**连带后果（要一并处理，否则留死代码）**：
1. `courses.mastery`（引导掌握度）**彻底下线**——学习过程中孩子 agent 写 mastery 的能力不再有载体。涉及：`kb.courses.updateField(mastery)` 调用点、孩子 prompt 里"判断掌握度并记录"的指令、`course_status`/`learning-summary` 输出的 `mastery` 字段、家长端展示。**建议改为"学习计划 result 的自由文本备注"（不参与任何计算）或明确删除**，二选一，不要留着又不用。
2. `courses.exam_mastery` 下线，`exam_attempts` 迁移后退场（§11.1）。
3. `course_status` 工具语义更新：从"8 类信息含 mastery/examMastery"改为"学习时间 + 考核得分率"。

### 12.2 生活计划怎么判定完成 —— 唯一没有系统事件源的域

**核心事实**：学习有自动信号（学完课 → `courses.first_learned/last_review` 变化），考核有自动信号（提交 → 得分）。**生活事项（整理书包/锻炼/做家务/作息）没有任何系统内事件源**，必须依赖**人工声明**。因此完成判定 = 声明 + 确认，不是"检测"。

**触发时机（四通道，按优先级）**：
1. **孩子在当日列表勾选**（主通道，即时）——todolist 改为动态查三表后，勾选 = 直接写该计划行 `status=done + done_at`。
2. **家长端确认/代勾**（兜底，低龄必需）——家长可见今日全部生活项并确认或补勾。
3. **对话声明**（可选通道）——孩子对 AI 说"我整理完书包了"，agent 调工具勾选（比 recording 事后抽取可靠）。
4. **到点判定**（自动兜底）——窗口到期后由 stat tick 把仍 `pending/claimed` 的行判为 `missed`，计入完成率分母的未完成。

**防虚报（因为积分不兜底且有扣分，孩子有虚报动机）**：给计划项加 `need_confirm` 开关，分三级实现：
- **确认制（推荐默认，低龄）**：孩子自勾 → `claimed`（待确认），家长确认 → `done`；**积分以 `done` 结算**。
- **自勾制**：孩子自勾即 `done`，家长端可事后撤销/纠正（适合阅读、锻炼等信任项）。
- **混合制**：按项配置 `need_confirm`（家务/作息类需确认，阅读类自勾）。

**状态机（生活计划行）**：
`pending` →（孩子勾）`claimed` →（家长确认）`done`；`pending/claimed` →（窗口到期）`missed`；任一态 →（家长豁免）`skip`（不计分母，处理生病/外出，避免误扣分）。

**规则定死**：
- **允许当天补勾，禁止跨天补勾**（跨天补勾会破坏 missed 与 streak 口径）。
- 日界 = 本地日期（现有 `formatLocalDate` 口径）；窗口到期时间取计划行 `计划完成时间`（默认当日 23:59）。
- 完成率分母 = 窗口内**必做**项（`skip` 剔除；`missed` 计入未完成）；这正是积分分档的输入。
- **每日固定生活项由排期表展开成每日一条计划行**——只有这样每天才能真正勾选、streak 才能连续计算。

### 12.3 三域完成信号对照（改造后的判定口径）

| 域 | 完成信号 | 是否自动 | 结果字段 |
|---|---|---|---|
| 学习计划 | 课程 `first_learned/last_review` 变化 → stat 回写 | ✅ 自动 | status/done_at + result 摘要 |
| 考核计划 | 考核提交（得分回填） | ✅ 自动 | status + attempt_id + 综合得分 |
| 生活计划 | **LLM 判定（对话事件）+ 家长事后审计** | LLM 判定 | status + evidence（事件/对话片段） |

---

## 13. 修订：生活计划判定改由 LLM 驱动（2026-09-10 第二次定案，取代 §12.2 的勾选状态机）

> 用户定案：①`courses.mastery` 直接删除；②**不允许勾选**，生活计划完成只能由 LLM 判断，daily 记录该计划详情供家长事后审计（防虚报）；③recording 是兜底策略——它不操作三张表，只需在写 daily 时带上 `plan_id`，stat 据此更新三张表。

### 13.1 新的职责分工（核心变化：引入"事件"作为判定与审计载体）

> ⚠️ **已被 §13.6 修订**：事件载体改用 `daily_entries`（+ plan_id + plan_outcome），**不新建 plan_events 表**。

```
孩子与 AI 对话  ──①实时──→  agent 记事件（plan_events: plan_id + 事件摘要 + 对话片段）
                ──②兜底──→  recording 定时扫今日对话，补漏事件 + 写 daily（带 plan_id）
                                    │
                          stat（三表状态的唯一写入者）：有事件 → done；窗口到期无事件 → missed
                                    │
                          daily_entries（plan_id 关联）＝过程详情 → 家长事后审计/否决
```

**为什么需要 `plan_events` 表（新增，建议）**：- daily 是 recording 单源写入的产物（2026-08-13 架构决策），**孩子 agent 不能直接写 daily**；而"孩子当场说做完了"需要一个实时落点 → 事件表；
- 事件表让判定依据从"解析自然语言"变成"查事件"，比 stat 去读 daily 文本可靠；
- 事件表天然就是**审计记录**：家长看到"孩子说 19:20 整理完书包"+ 对话原文，判断是否虚报。

`plan_events`：`id / plan_id / child_id / date / summary / quote(对话片段) / source(agent|recording) / created_at`，UNIQUE 幂等（同一 plan 同日多事件不重复计分）。

### 13.2 状态机（简化版，取代 §12 的 claimed/自勾模型）

| 状态 | 触发 | 说明 |
|---|---|---|
| `pending` | 计划生成 | 窗口内待办 |
| `done` | stat 发现有事件 | 据事件判定完成（含 evidence 指针） |
| `missed` | 窗口到期仍无事件 | 计入未完成（不兜底、有扣分原则） |
| `cancelled` | **仅家长**标记 | 豁免（生病/外出）或撤销虚报，不计分母 |

**状态唯一写入者 = stat（worker）**：agent 与 recording 只写事件，都不直接改三表状态——避免并发写与口径分裂。

### 13.3 必须明确的三件事（否则方案有洞）

1. **"无事件"的默认**：孩子做了但没跟 AI 说 → 到期判 `missed`。这符合"不兜底"原则，但会误伤，因此…
2. **家长修正权必须保留**（这不是"勾选"，是审计权）：家长端可 ① 撤销 `done`→`missed`（判定虚报）② 标记 `cancelled`。**建议家长端务必能看"事件摘要 + 对话原文 + 该计划详情"再决定**。若连家长都不能修正，系统会出现无法纠正的误判。
3. **判定与积分的关系**：积分按 `done`（据事件）；`missed` 触发扣分；`cancelled` 不进分母。虚报被发现 → 撤销后按 `missed` 处理，可对"撤销"留档作为诚信记录（可选）。

### 13.4 recording 的改造（对应你的第 3 点）

你说得对：**recording 不需要操作三张表**，只要带 `plan_id`，stat 就能更新。具体落地：
- **worker 构建 recording prompt 时注入当天三张计划清单（含 uuid/标题/窗口）**——这样 recording 无需新增取数工具，"知道核对哪些事件"靠注入上下文实现；
- recording 的提示词增加一段职责：**从当天对话中提取与计划项相关的事件，核对落到具体计划行，写 daily 时带 `plan_id`；对不上任何计划行的自由事件照常记录但不带 id；不得编造计划项**；
- `daily_entries` 加 `plan_id` 列（§11.2 已定），`kb.daily_entries.add` 支持写入；
- 同一计划项当天多次事件 → 幂等（stat 判 done 一次，不重复计分）。
- **实时通道同样受益**：孩子当场说"做完了"时，学习/生活 agent 通过一个新工具（如 `plan_event_log`）写事件 → 即时判定，不必等 recording 每小时兜底。**建议把实时通道做出来，recording 的定位就是兜底（你的表述），而不是唯一通道。**

### 13.5 修订后的积分输入口径

- 完成率分母 = 窗口内必做项 − `cancelled`；
- 分子 = `done`；`missed` 计入未完成；
- 判定依据全部可追溯（事件 + 对话原文）→ 家长审计闭环成立，这是"不兜底"能站得住的前提。

### 13.6 修订（16:45）：事件载体 = daily 本身，取消 plan_events 表

**关键事实（已核实）**：孩子 agent 有 `summarize_conversation` 工具（`electron/lib/daily-summary.ts`），它就是**按需触发 recording**——与定时 recording 共用同一管线与提示词；孩子/家长想回顾或总结某天时调用它。而且 recording 提示词**现已有先例**（`recording-prompt.ts` §93/103/112）：汇总写库后用 `todo_list` 核对孩子自定任务完成情况，并在 daily 写「今日时间规划」条目。

**由此得出三条结论**：

1. **不需要 `plan_events` 表**（撤回 §13.1 的建议）。recording 已按天把经过写进 `daily_entries`——**daily 条目就是事件记录**；实时通道也不是"新的"，而是已存在的 `summarize_conversation`。零新表。
2. **必须在 daily 上补两个结构化列**（否则"提到"与"完成"分不开）：
   - `plan_id`：该条目属于哪条计划；
   - `plan_outcome`：`done` / `missed` / `unknown`——**由 recording 显式输出**。
   光有 `plan_id` 不够：孩子可能说"今天没做"，那也是事件，不能等同完成。有了 outcome，stat 直接可判，不必去解析自然语言。
3. **recording 的改造点收敛为一处**（两条通道同时生效，很经济）：
   - worker 构建 recording prompt 时**注入当天三张计划清单（含 uuid/标题/窗口）**；
   - 提示词把现有"核对孩子自定任务"段**扩展为核对三张计划表**，并按 §13.4 输出 plan_id + plan_outcome；
   - 现有"来源=家长的项绝不动"改为分工表述：**recording 只写证据（daily + plan_id + outcome），三表状态一律由 stat 写**。

**新增两条规则（防止翻旧账）**：
- **只判当天/窗口内**：`summarize_conversation` 汇总历史某天时，写证据可以，但**不得回溯修改已结算的历史状态**；
- **状态更新幂等**：stat 复用 `worker_state` 的 `last_key` 模式（现有 `todo_stat` 同款），同一计划项当天多次汇总只判一次、不重复计分。

**风险提示（重申）**：汇总由孩子主动发起 + 到期无事件即 `missed` → **误伤概率不低**（做了没说）。因此 §13.3 的**家长修正权（撤销 / cancelled）是这套方案的必需件**，不是可选项。
